# A股分析系统 — 运维手册

> 版本: 2.1 | 更新日期: 2026-08-30
> 
> **v2.1 更新：** 新增§5.5数据管道监控（13步管道查询/OUT完整性检查/常见问题排查）；新增§5.6数据消费规则（353号架构前端数据源白名单/禁止表/审查清单）；§1.1架构说明更新为COL/RAW/SIG/JUD/OUT管道。
> 
> **v2.0 更新：** 双进程架构（254号方案）。数据采集与 API 分离：`data_daemon.py` + `run.py`。日终同步改用批量 API。启动方式：数据进程 launchd 开机自启，API 进程 `start.command` 手动启动。
> 
> **v1.2 更新：** §14 从"只读存档"改为"备份定稿 + 工作副本"双目录模式，新增 `_原型定稿-不可修改/` 备份目录。
> 
> **v1.1 更新：** 新增 §12 后端模块开发流程（6阶段排序/底盘交付清单/开发规范）；§11.10 新增 apscheduler/akshare 依赖

---

## 一、系统架构

```
┌─ 数据进程 (data_daemon.py) ───────────┐   ┌─ API 进程 (run.py) ────────────┐
│  mootdx TCP 采集 (5s)                 │   │  Flask REST :5001               │
│  AKShare 低频补充 (30min)              │   │  SocketIO WebSocket              │
│  日终同步 (15:30, 批量API)             │   │  读 ECM 只读                    │
│  完整性检查/补采                       │   │  写 PG（用户数据）              │
│  分钟K线回填                           │   │                                 │
│  写入 ECM SQLite                      │   │                                 │
└────────────────┬──────────────────────┘   └────────────────┬────────────────┘
                 │                                           │
                 └─────────── ECM SQLite (WAL) ───────────────┘
                                    ↓
                           ┌────────────────┐
                           │  PostgreSQL    │
                           │  (业务数据)     │
                           └────────────────┘

启动方式：
  数据进程 → launchd 开机自启（轻量~50MB，全天静默采集）
  API 进程 → start.command 手动启动（较重~200MB，用完即关）
```

## 二、启动与停止

```bash
# 一键启动 + 浏览器（推荐）
open start.command

# 仅后端（不打开浏览器）
open start-backend.command

# 停止
open stop.command

# 查看日志
tail -f backend/logs/app.log           # API 进程日志
tail -f backend/logs/data_daemon.log   # 数据进程日志
```

### 架构说明

系统由两个独立进程组成，启动策略不同：

| 进程 | 文件 | 职责 | 启动方式 |
|------|------|------|---------|
| **数据进程** | `backend/data_daemon.py` | 12步管道驱动（COL→RAW→SIG→JUD→OUT）、实时采集、日终同步、完整性检查、分钟K线回填 | launchd 开机自启，后台静默运行 |
| **API 进程** | `backend/run.py` | Flask REST API、SocketIO 推送、响应前端 | `start.command` 手动启动 + 打开浏览器 |

## 三、健康检查

| 端点 | 用途 | 预期响应 |
|------|------|---------|
| `/api/v3/health` | 综合健康检查 | `{"success":true,"status":"healthy"}` |
| `/api/v3/health/live` | 进程存活 (liveness) | `{"success":true,"status":"alive"}` |
| `/api/v3/health/ready` | 依赖就绪 (readiness) | `{"success":true,"status":"ready","dependencies":{...}}` |

## 四、数据备份

```bash
# 手动备份到默认目录
./scripts/backup.sh

# 手动备份到指定目录
./scripts/backup.sh /path/to/backup

# 定时备份 (添加 crontab)
crontab scripts/crontab.txt
```

备份内容:
- PostgreSQL: `pg_dump` 完整导出
- ECM SQLite: 数据目录 `data/duckdb/stock_cache.db` 快照
- .env: 配置文件副本

## 五、常见故障排查

### 5.1 前端页面白屏

```bash
# 检查前端容器
docker compose logs frontend | tail -20
# 检查 Nginx 是否正常
curl -sI http://localhost:9000 | head -5
# 检查 API 代理
curl -s http://localhost:9000/api/v3/health
```

### 5.2 后端 API 超时

```bash
# 检查后端日志
docker compose logs backend | grep ERROR | tail -20
# 检查数据库连接
curl -s http://localhost:5001/api/v3/health/database
# 检查 Redis
docker compose exec redis redis-cli ping
```

### 5.3 数据不更新

```bash
# 检查数据源状态
curl -s http://localhost:5001/api/v3/data-source/status
# 强制同步
curl -s -X POST http://localhost:5001/api/v3/data/sync
# 检查缓存状态
curl -s http://localhost:5001/api/v3/cache/stats
```

### 5.4 数据库问题

```bash
# 检查 PostgreSQL
docker compose exec postgres pg_isready -U stock_user -d stock_analysis
# 检查表结构
docker compose exec postgres psql -U stock_user -d stock_analysis -c "\dt"
# 修复迁移
docker compose exec backend flask db upgrade
```

### 5.5 数据管道监控（353号/371号架构）

系统采用12步管道驱动（`_drive_pipeline()` 30s tick自动推进）：

```
COL-1~6（采集）→ RAW-1~3（原料加工）→ SIG（策略分析）→ JUD（判定）→ OUT（成品仓）
```

**管道状态查询**：
```bash
# 查看当日管道状态（12步全done=完成）
sqlite3 data/duckdb/stock_cache.db "SELECT step_id, step_name, status, detail FROM pipeline_status WHERE pipeline_date=date('now') ORDER BY rowid"

# 查看是否有失败/卡住的步骤
sqlite3 data/duckdb/stock_cache.db "SELECT step_id, status, retry_count FROM pipeline_status WHERE pipeline_date=date('now') AND status IN ('failed','running')"
```

**成品表完整性检查**：
```bash
# treemap_snapshot行数（应≥4000）
sqlite3 data/duckdb/duckdb/snapshot_cache.db "SELECT COUNT(*) FROM treemap_snapshot"

# status_snapshot行数（应≈active_codes数）
sqlite3 data/duckdb/duckdb/snapshot_cache.db "SELECT COUNT(*) FROM status_snapshot"

# one_liner_detail填充率
sqlite3 data/duckdb/duckdb/snapshot_cache.db "SELECT COUNT(*) FROM status_snapshot WHERE one_liner_detail IS NOT NULL"
```

**常见管道问题**：
| 现象 | 可能原因 | 处理 |
|------|---------|------|
| 某步骤卡在running | daemon异常退出后旧running未清理 | 等待4小时自动重置，或重启daemon |
| 某步骤failed且retry≥3 | 步骤内不可恢复错误 | 检查data_daemon.log定位具体错误 |
| 管道done但treemap行数<4000 | JUD部分股票evaluate()返回None | 检查pre_feat_cache覆盖率 |
| 管道done但one_liner_detail全NULL | SIG未生成seven_dim_json | 检查strategy_signal_detail.seven_dim_json |

### 5.6 数据消费规则（353号架构）

> 本节定义前端消费层（routes/services）的数据读取规范，确保所有前端模块统一从OUT成品表读取数据。

#### 前端数据源白名单

前端消费层**只允许**读取以下OUT成品表和特定场景表：

| 数据源 | 所属分库 | 允许场景 | 说明 |
|--------|---------|---------|------|
| **status_snapshot** | snapshot_cache.db | 个股状态/判定/九维灯/仪表盘汇总 | JUD产出 |
| **treemap_snapshot** | snapshot_cache.db | 图谱展示/方块大小/颜色 | JUD产出 |
| **strategy_signal_detail** | snapshot_cache.db | 七维描述/策略信号 | SIG产出 |
| **watchlist_status_diff** | snapshot_cache.db | 自选股变更追踪 | OUT产出 |
| daily_cache | market_cache.db | 实时行情推送/K线图表 | 盘中专用 |
| daily_basic_cache | market_cache.db | 实时行情推送 | 盘中专用 |
| pre_feat_cache | compute_cache.db | 雷达/深度分析 | 特征级数据 |
| factor_cache | compute_cache.db | 因子分析页面 | 因子级数据 |
| minute_kline_cache | market_cache.db | K线图表 | 历史分钟数据 |

#### 禁止读取的表

| 表名 | 原因 |
|------|------|
| opportunity_tags_cache | 已废弃（P4删除后无写入源，读到的是陈旧/空数据） |
| strategy_signals | 已删除（功能由strategy_signal_detail替代） |
| indicator_ma/macd/other | 管道中间产物，前端不直接消费 |
| chip_distribution_cache | 管道中间产物，前端不直接消费 |

#### 新增模块审查清单

新增API路由时必须逐项确认：

- [ ] 数据源是否在白名单内？
- [ ] 是否读取了禁止表？
- [ ] 是否有预计算替代方案（应优先读OUT成品，非实时计算）？
- [ ] 红线5：是否经由DataManager读取（禁止直接sqlite3.connect/conn.execute）？
- [ ] 盘中实时数据可直接读daily_cache/daily_basic_cache，但日终数据必须读OUT成品

#### 管道阶段与数据源映射

```
COL（采集）→ daily_cache等原始表
RAW（加工）→ pre_feat_cache / indicator表 / factor_cache
SIG（策略）→ strategy_signal_detail（signal_json + seven_dim_json + dim_results_json）
JUD（判定）→ status_snapshot + treemap_snapshot
OUT（成品）→ one_liner_detail归档 + watchlist_status_diff + history归档
```

**前端核心页面数据流**：
- 仪表盘 → status_snapshot（汇总）
- 机会图谱 → treemap_snapshot（方块+颜色+标签）
- 个股分析 → status_snapshot + strategy_signal_detail（七维+信号）
- 诊断 → status_snapshot + cross_validate
- 自选看板 → watchlist_status_diff + status_snapshot
- 机会雷达 → pre_feat_cache + strategy_signal_detail

## 六、升级流程

```bash
# 1. 备份
./scripts/backup.sh

# 2. 拉取最新代码
git pull

# 3. 重建并启动
docker compose up -d --build --wait

# 4. 执行数据库迁移
docker compose exec backend flask db upgrade

# 5. 验证
curl -s http://localhost:5001/api/v3/health
```

## 七、安全配置

| 配置项 | 位置 | 说明 |
|--------|------|------|
| `AUTH_TOKEN` | `.env` | API 访问令牌 |
| `SECRET_KEY` | `.env` | Flask 密钥 (90天轮换) |
| `TUSHARE_TOKEN` | `.env` | Tushare Pro 数据源令牌 |
| `DEEPSEEK_API_KEY` | `.env` | DeepSeek AI 密钥 |
| CSP 策略 | `nginx.conf` | Content-Security-Policy 头 |

## 八、日志管理

```
日志位置 (Docker 内):
  /app/logs/                     # 后端日志
  /var/log/nginx/                # Nginx 日志 (Docker 外映射)
  
日志轮转:
  后端: 每日轮转, 保留 30 天
  Nginx: Docker 日志驱动自动管理

关键关键词:
  ERROR     - 系统错误, 需关注
  WARNING   - 资源不足或降级
  TRIGGERED - 策略触发事件
  SWITCH    - 数据源切换
```

## 九、恢复操作

```bash
# PostgreSQL 恢复
gunzip -c backups/postgres_20260609_030000.sql.gz | docker compose exec -T postgres \
  psql -U stock_user -d stock_analysis

# DuckDB 恢复
cp backups/duckdb_20260609_030000.duckdb data/stock_cache.duckdb

# 环境变量恢复
cp backups/env_20260609_030000.txt .env
```

## 十、联系与报警

- 所有异常日志包含 `request_id`，可在日志中追溯完整请求链
- 数据源切换时自动记录日志
- 超过 1 个交易日数据未更新时前端展示警告

---

## 十一、生产环境约束

> 本系统最终部署为 **macOS + Windows 双平台单机桌面应用**，供持有团队数人在各自设备上独立运行。所有开发必须遵守以下约束。

### 11.1 部署架构

```
用户设备（macOS / Windows）
  ├─ Flask (eventlet) port 5001       ← 唯一服务端口
  │   ├─ 前端 SPA 静态托管 (send_from_directory)
  │   ├─ REST API (25+ Blueprint, ~179 端点)
  │   ├─ WebSocket (SocketIO 实时推送)
  │   └─ Flask-SQLAlchemy ORM
  │
  ├─ SQLite app.db                     ← 应用数据（交易/报告/规则/配置）
  ├─ DuckDB stock_cache.db             ← 市场数据缓存（OHLCV/资金/基本面 ~180MB）
  └─ TTLCache (内存)                   ← 热点缓存（替代 Redis，零依赖）

外部依赖（出站，必须联网）:
  ├─ Tushare Pro（主数据源，5000 分）
  ├─ AkShare/Sina（备用数据源，离线降级）
  ├─ DeepSeek V4（AI 叙事/研判/条件生成）
  └─ LM Studio（可选本地 LLM）
```

### 11.2 存储层约束

| 数据库 | 用途 | 文件位置 | 约束 |
|:------|:------|:---------|:------|
| SQLite | 应用数据（OLTP） | `$DATA_DIR/app.db` | 使用通用 SQLAlchemy ORM，禁止 PostgreSQL 特有类型 |
| DuckDB | 市场数据（OLAP） | `$DATA_DIR/duckdb/stock_cache.db` | 列式存储，持久化缓存，只写不修改 |
| TTLCache | 热点缓存 | 内存 | 替代 Redis，TTL 1h，maxsize=1000 |

**SQL 兼容性规则**（开发者必须遵守）：
- 所有模型使用通用 SQLAlchemy 数据类型 → ❌ 禁止 ARRAY、TSVECTOR、SERIAL
- JSON 列使用 `db.Column(JSON)` → SQLAlchemy 自动适配 SQLite TEXT 存储
- 布尔值使用 `db.Boolean` → 自动映射到 INTEGER(1)
- 时间戳使用 `db.DateTime` → SQLite 存储为 ISO 8601 字符串

### 11.3 禁止使用的外部服务

| 服务 | 状态 | 替代方案 |
|:-----|:------|:---------|
| Docker Desktop | ❌ 不使用 | 裸跑 |
| Docker Compose | ❌ 不使用 | 仅保留为本地开发备选 |
| Nginx | ❌ 不使用 | Flask `send_from_directory` |
| PostgreSQL | ❌ 不使用 | SQLite + DuckDB |
| Redis | ❌ 不使用 | `cachetools.TTLCache` |
| Gunicorn | ❌ 不使用 | eventlet（Flask + SocketIO 内置） |
| MySQL / MariaDB | ❌ 不使用 | SQLite |

### 11.4 数据目录

- **生产环境**：
  - macOS: `~/Library/Application Support/Astock/`
  - Windows: `%APPDATA%/Astock/`
- **开发环境**：项目根目录下 `data/`
- **自定义覆盖**：在 `.env` 中设置 `DATA_DIR=/path/to/custom/data`
- 代码必须使用 `os.getenv('DATA_DIR', default)` 读取，禁止硬编码路径

### 11.5 启动/停止

| 平台 | 启动 | 停止 | 打包方式 |
|:-----|:------|:------|:---------|
| macOS 开发 | `python backend/run.py --port 5001` | `stop.command` | — |
| macOS 生产 | 双击 Astock.app | `stop.command` | Platypus |
| Windows 开发 | `start.bat` | `stop.bat` | Inno Setup |

### 11.6 端口

- **唯一端口**：5001（Flask 应用，托管 SPA + REST API + WebSocket）
- 禁止引入其他监听端口（如 Nginx 9000、Redis 6379 等）

### 11.7 日志

- 写入 `$DATA_DIR/logs/app.log`
- 30 天轮转（`TimedRotatingFileHandler`）
- 控制台同时输出（调试用）

### 11.8 前端构建

- 构建命令：`cd frontend/vue-project && npm run build`
- 产出 `dist/` 目录，Flask 通过 `send_from_directory` 托管
- `dist/` 不存在时，Flask 仅提供 REST API（适用于 Headless 开发）

### 11.9 开发 → 发布流程

```
日常开发: python backend/run.py --port 5001
    ↓ 裸跑测试通过
npm run build  # 构建前端
    ↓
macOS: Platypus 打包 → Astock.app → 复制到 /Applications/
Windows: 嵌入式 Python + Inno Setup → .exe 分发
```

### 11.10 包依赖变化

- 已从 `requirements.txt` 移除：redis
- 新增至 `requirements.txt`：`cachetools>=5.0`、`akshare>=1.14.0`、`apscheduler>=3.10.4`

---

## 十二、后端模块开发流程

> 本节定义 14 个后端模块（#213-#226）的开发顺序、前置检查和交付规范。
> **详细排序与依赖分析见 [233号方案](002-方案存档/233-14模块后端开发最优排序规划.md)。**

### 12.1 开发顺序（6 阶段）

| 阶段 | 模块 | 工时 | 核心交付 |
|:----:|:-----|:----:|:---------|
| 一：基础设施 | ①225系统管理 → ②213仪表盘 + ③219因子管理 | ~8天 | 底盘全量交付 + 数据管道验证 |
| 二：核心分析 | ④214选股 + ⑤220策略模板 → ⑥215个股策略 | ~14天 | 策略引擎完整API链路 |
| 三：验证体系 | ⑦218回测 → ⑧222沙箱 + ⑨223报告 + ⑩216自选 | ~16天 | 回测引擎 + 策略验证闭环 |
| 四：高阶分析 | ⑪221复盘 + ⑫217 AI投研 → ⑬226账户 | ~23天 | 诊断引擎 + AI + 账户复盘 |
| 五：运营功能 | ⑭224监控通知 | ~13天 | 规则引擎 + 推送管道 |

**关键路径：** 225→215→218→221→226 = 37 天（决定最短完工周期）

### 12.2 阶段一底盘交付清单（225号系统管理）

225号是全站第一个开发的模块，需一次性交付以下底盘级能力：

| 底盘能力 | 涉及文件 | 说明 |
|:---------|:---------|:-----|
| Config 类接入 create_app() | `__init__.py` + `config.py` | `app.config.from_object(Config)` + DATA_DIR修正 + REDIS_URL移除 |
| RuntimeConfigManager | `services/runtime_config.py`（新建） | 内存缓存 + DB持久化 + 点号路径读取 |
| system_config 表 + 迁移 | `models/` + Alembic | key/value/updated_at + scheduling JSON域 |
| APScheduler 初始化 | `__init__.py` + `scheduler_manager.py`（新建） | BackgroundScheduler + create_app()中启动 |
| daily_close_sync 编排器 | `scheduler_manager.py` | 复用现有sync脚本逻辑 + blinker信号 |
| 定时配置 API（5端点） | `routes/system_config.py`（新建） | schedule/config(GET+POST) + sync-now + logs + next-runs |
| Alembic 迁移初始化 | `migrations/` | `flask db init` + env.py + versions/ |

### 12.3 启动新模块前的检查清单

每次启动开发新模块前，必须依次完成：

| 步骤 | 操作 | 目的 |
|:----:|:-----|:-----|
| ① | 读取本手册 §11 + §12 | 确认生产约束和开发流程 |
| ② | 读取 `_工作上下文.md` | 了解当前项目状态和已完成模块 |
| ③ | 读取该模块的规格书（`002-方案存档/XXX-xxx.md`） | 掌握API规格、JSON Schema、数据源映射 |
| ④ | 读取该规格书中的「跨模块依赖」章节 | 确认上游模块已就绪 |
| ⑤ | 参考 [228号依赖总览](002-方案存档/228-系统级跨模块依赖关系总览.md) | 确认无遗漏依赖 |
| ⑥ | 参考 [233号排序规划](002-方案存档/233-14模块后端开发最优排序规划.md) §四 | 了解该模块的排序理由 |

### 12.4 模块开发交付规范

每个模块完成后，必须执行以下交付动作：

| 动作 | 说明 |
|:-----|:-----|
| API 端点全量可调用 | 规格书中标注的所有端点均可通过 curl/Postman 测试 |
| Mock 替换为真实数据 | 所有 Mock 数据替换为数据库/API 真实数据 |
| 错误处理矩阵验证 | 规格书中的错误处理矩阵逐项验证 |
| 降级方案验证 | 数据源不可用时的降级路径测试 |
| 更新 `_工作上下文.md` | 标记模块完成状态 |
| 更新 `001-沟通记录/` | 记录开发过程中的关键决策和发现 |
| 更新规格书状态 | 将规格书 frontmatter 中的 `status` 标记为 `✅ 已实施` |
| **前端API同步（按§12.6规则）** | 本阶段所有涉及的前端UI页面替换为API优先加载，更新171号/166号文档 |

### 12.5 生产环境约束提醒

所有模块开发必须遵守 §11 的生产约束：

| 约束 | 说明 |
|:-----|:-----|
| 数据库 | SQLite + DuckDB，禁止 PostgreSQL 特有类型 |
| 缓存 | TTLCache（内存），禁止 Redis |
| 端口 | 仅 5001，禁止新增监听端口 |
| 数据目录 | `os.getenv('DATA_DIR', default)`，禁止硬编码路径 |
| 配置读取 | 通过 `RuntimeConfigManager.get('key')`，禁止 `os.getenv` 直读 |
| 定时调度 | 通过 APScheduler + RuntimeConfigManager，禁止硬编码 cron |

### 12.6 逐阶段前端同步规则

233号方案将后端开发分为6阶段（~65人天），前端UI同步采用**逐阶段同步**策略。

**核心原则：** 每个阶段后端开发完成后，在切换对话/启动下一阶段之前，花 0.5-1 天同步对应原型页面。

| 维度 | 要求 |
|:-----|:------|
| **策略** | **不等6阶段全部完成**，每个阶段后端完成后立即同步前端 |
| **原因** | 避免API契约偏差在3个月中累计；后端逻辑还在"热记忆"中；每个阶段交付可演示的完整UI |
| **时间预算** | 每阶段后端完成后 0.5-1 天 |
| **并行容忍** | 同一阶段的多模块可以并行后端开发，但前端同步仍需在阶段切换前完成 |

**前端同步交付清单（每个阶段完成后）：**

| 动作 | 涉及文件 | 说明 |
|:-----|:---------|:------|
| 替换Mock为API调用 | `_ui-prototype/<对应页面>.html` | 用 `fetch(API+'/...')` 替换 `Math.random()`/硬编码数据 |
| API优先+本地降级 | 同上 | API 不可用时 fallback 到本地 Mock，确保页面可渲染 |
| 更新171号映射状态 | `002-方案存档/171-功能视图API映射对照表.md` | 对应页面的 Mock 状态列从 ❌ → ✅ |
| 更新166号调整记录 | `002-方案存档/166-前端UI原型逐页调整记录.md` | 对应 § 节记录变更 |
| 更新内存文件 | `.claude/memory/*.md` | 同步阶段完成状态 |
| 提交代码 | git | 与前阶段后端提交分开，独立提交 |

**未完成的 Mock 标注规则：**
- 因后端API尚未就绪而必须保留的 Mock，在 171 号文档中标注 🔴 **仍为Mock** + 原因
- Mock 占比 > 50% 的页面不允许标记为"阶段完成"

**相关文档：** 234号方案（后端分阶段前端同步策略评估）

---

---

## 附录一：原型UI修改执行规则

> **适用范围：** 所有涉及 `_ui-prototype/` 目录下 HTML 文件的修改、评估和沟通。
> **不适用范围：** 后端代码、数据库、策略引擎、API 等非前端原型的工作项。

### A.1 修改前检查清单

开始任何原型 UI 修改前，必须依次完成以下检查：

| 步骤 | 操作 | 验证标准 |
|:----:|:-----|:---------|
| ① | 读取 `_项目运行手册.md` 全文 | 确认手册内容已加载到上下文 |
| ② | 读取 `_工作上下文.md` | 了解项目当前状态和待办 |
| ③ | 搜索 `002-方案存档/` 中最近相关评估文档 | 是否存在已有评估方案或冲突记录 |
| ④ | 搜索 `001-沟通记录/` 中最近会话的对应主题 | 确认用户之前提出的需求和决策 |
| ⑤ | 浏览器打开目标原型 HTML 文件 | 确认当前实际展示状态 |

### A.2 修改流程

```
用户提出修改需求
    ↓
① 理解需求本质 —— 问清楚"要什么效果"，不假设
    ↓
② 浏览器实测 —— 确认当前实际状态，记录测量数据（像素、位置、颜色等）
    ↓
③ 根因分析 —— 不直接打补丁，找到问题的根本原因
    ↓
④ 评估方案（如有必要） —— 复杂修改先出评估方案存档到 002-方案存档/
    ↓ 用户确认后
⑤ 执行修改 —— 手术刀式修改，只改必要的代码，不重构无关部分
    ↓
⑥ 浏览器验证 —— 逐项验证，输出实测数据
    ↓
⑦ 同步更新所有相关文档
```

### A.3 浏览器验证标准

每次修改后必须进行浏览器验证，并记录以下数据：

| 维度 | 要求 |
|:-----|:------|
| 视觉对齐 | 精确到像素级的坐标/位置测量（使用 `getBoundingClientRect()` 或 Elements panel）|
| 异常值覆盖 | 边界情况测试（最大值/最小值/0值/空值）|
| 交互覆盖 | 所有触发路径测试（鼠标/键盘/弹窗/切换）|
| 回归检查 | 验证修改未破坏现有功能 |

### A.4 文档同步要求

每次完成原型 UI 修改后，必须同步更新以下文档：

| 文档 | 更新内容 |
|:-----|:---------|
| `001-沟通记录/` 当日纪要 | 按 §XXX 编号记录，含关键结论、决策、验证数据 |
| `001-沟通记录/沟通索引.md` | 索引当天条目 |
| `002-方案存档/`（如适用） | 评估方案更新状态→"已实施" |
| `_工作上下文.md` | 记录完成状态、涉及文件及行号 |
| `002-方案存档/166-前端UI原型逐页调整记录.md` | 更新对应页面 § 节的调整记录 |
| `002-方案存档/171-功能视图API映射对照表.md` | 如有 API 关联变更，更新对应 § |

### A.5 禁止事项

| 禁止行为 | 原因 |
|:---------|:------|
| 不经浏览器实测直接改代码 | 无法确认真实现状，可能修复了不存在的问题 |
| 不更新 166 号文档对应 § 节 | 166 号方案是本项目唯一的前端调整记录总账 |
| 不更新 171 号文档 API 映射 | 171 号是后端 API 与前端视图的映射基准，不同步会导致后续开发方向错误 |
| 评估后不修改，但也不标注状态 | 导致无法追溯"已评估/已确认/已拒绝"的状态 |
| 修改后不验证就告知完成 | 验证不是可选项，是必经步骤 |

### A.6 坐标系校验规则（筹码面板相关）

筹码面板涉及 **两套独立的坐标系统**，修改时必须特别注意：

| 坐标系 | 来源 | 用途 |
|:-------|:-----|:------|
| **ECharts grid Y 轴** | `chart.convertToPixel()` | 十字光标横线、价格标签定位 |
| **chip-chart 自身坐标** | `(1-(price-lo)/range)*containerH` | 筹码条 Y 位置 |

**原则：** 凡是需要与十字光标对齐的视觉元素（白色线、红蓝分界），必须使用 DOM `getBoundingClientRect()` 进行坐标换算，**不得使用 chip-chart 自身的比例映射公式**。

---

## 十三、数据完整性规则

> **核心原则：生产环境中，模拟数据（Mock）绝对不能作为数据源失败的降级方案。** 
> 因网络/API/数据源故障导致数据不可用时，必须向前端返回明确的错误信息，由前端展示"数据不可用"状态，而非假造数据和指标。

### 13.1 定义

| 术语 | 含义 |
|:-----|:------|
| **模拟数据（Mock）** | 完全由算法/随机数生成的、不反映真实市场行情的数据 |
| **缓存数据** | 上次正常运行时从真实数据源获取并持久化的历史数据 |
| **降级** | 首选数据源不可用时的备选方案 |

### 13.2 允许的降级方式（按优先级）

| 优先级 | 降级方案 | 说明 | 标注要求 |
|:------:|:---------|:------|:---------|
| 1 | **缓存数据** | 使用 DuckDB/TTLCache 中最近一次成功获取的数据 | 返回中增加 `cached_at` 字段标注最后更新时间 |
| 2 | **仅结构但不假造** | 返回空列表/空对象，字段名和结构保留 | 前端展示空状态，标注"暂无数据" |
| 3 | **明确错误** | 返回 HTTP 503 和明确的错误信息 | 前端展示"当前无法获取数据"状态 |

### 13.3 禁止的行为

| 禁止 | 说明 |
|:-----|:------|
| ❌ 用随机数代替缺失的数据值 | 用户可能据此做出投资决策 |
| ❌ 假造板块/行业回调 | 用户可能误以为"半导体板块领涨"而买入 |
| ❌ 用上一期数据充当前数据 | 用户可能基于过期信息操作（延迟超过1个交易日必须标注） |
| ❌ 假造涨跌幅/资金流向 | 用户可能基于假资金流向判断大盘方向 |
| ❌ 假造 K 线走势 | K 线是最基础的行情数据，造假直接导致技术分析失效 |
| ❌ 假造策略信号 | "买入信号"等判断标注不能由随机数生成 |

### 13.4 数据结构要求

数据不可用时的响应格式：

```json
{
  "success": false,
  "message": "数据源不可用: Tushare行业分类接口返回空数据，暂无法展示板块资金流向。请检查网络或稍后重试。",
  "error_type": "DataUnavailable",
  "data_source": "Tushare"
}
```

HTTP 状态码使用 **503 Service Unavailable**（可自动恢复的临时故障）或 **502 Bad Gateway**（上游 API 故障）。

### 13.5 mock 代码保留规则

`_mock_*` 等模拟数据方法**可以在代码中保留**，仅供：
- 前端原型独立验证（不依赖后端数据源）
- UI 布局/交互测试
- 开发环境本地调试

但必须满足：
- 所有 mock 方法注释中标注 `# ⚠️ 仅供开发/原型测试使用 — 生产环境不得调用`
- 生产环境下不得有任何代码路径调用 mock 方法

---

## 十四、原型工作规则

> 修订记录：2026-07-01：从"只读存档"改为"备份定稿 + 工作副本"双目录模式。

### 14.1 目录结构

```
项目根目录/
├── _原型定稿-不可修改/     ← 只读备份（UI 规范真理源）
│   ├── README.md        ← 备份说明
│   ├── index.html
│   ├── dashboard.html
│   ├── ... (16 个原型文件 + assets/ + 工作规则)
│
└── _ui-prototype/         ← 工作副本（可在开发中修订）
    ├── index.html         ← 基于定稿原型迭代
    ├── dashboard.html
    └── ...
```

### 14.2 各目录职责

| 目录 | 职责 | 是否可修改 |
|:-----|:-----|:----------|
| `_原型定稿-不可修改/` | UI 规范真理源，用于回溯、对比、验收验证 | ❌ **禁止修改** |
| `_ui-prototype/` | 开发中的前端工作副本，基于定稿原型迭代 | ✅ **可修订** |

### 14.3 工作流程

```
新模块 / UI 修订需求
    ↓
① 检查 `_原型定稿-不可修改/` 中对应定稿是否存在
    └─ 不存在 → 先创建定稿再开始（备份当前 _ui-prototype/ 中的文件）
    ↓
② 在 `_ui-prototype/` 中直接修订对应文件
    ↓
③ 修订完成后同步更新对应模块的规格书（002-方案存档/XXX-xxx.md）
    ↓
④ 如需验证 UI 是否偏离原始设计 → 对比 `_原型定稿-不可修改/` 中的快照
```

### 14.4 定稿更新

当 `_ui-prototype/` 经历重大迭代后，如需创建新的定稿基线：

1. 清空 `_原型定稿-不可修改/`
2. 重新复制 `_ui-prototype/` → `_原型定稿-不可修改/`
3. 更新 `_原型定稿-不可修改/README.md` 中的版本号
4. 在沟通记录中标注"定稿基线更新"

> ⚠️ 定稿更新是重大事件，仅在模块整体交付/验收通过后执行，日常迭代无需更新。

### 14.5 禁止事项

| 禁止 | 说明 |
|:-----|:------|
| ❌ 修改 `_原型定稿-不可修改/` 中任何文件 | 快照必须保持原始状态 |
| ❌ 删除 `_原型定稿-不可修改/` 目录 | 除非已建立新的定稿基线 |
| ❌ 在 `_ui-prototype/` 中引入与原型无关的文件 | 保持目录内容干净可维护 |
