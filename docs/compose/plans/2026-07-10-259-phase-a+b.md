# 259号方案 Phase A+B 实施计划

> **For agentic workers:** Inline execution in current session.

**Goal:** 完成个股策略分析板块 E14 AI叙事渲染（Phase A）+ 挂载 arbitrate/uidf/consolidate 路由 + 筹码真实数据渲染（Phase B）

**Architecture:** 前端 `indicator-ide.html` 为主战场（~135行），后端 `ai_analysis.py` 挂载2个路由（~30行）

**Tech Stack:** 前端 Vanilla JS + ECharts, 后端 Flask + SocketIO

---

### T1.1: Phase A — E14 AI叙事渲染打通

**Covers:** G1

**Files:** `_ui-prototype/indicator-ide.html`

- [ ] **A1: renderNarrative() 将卡1-4叙事从E14 data填充** — 将 `narrative` 字段渲染到维度卡下的叙事区 `.card-narrative`
- [ ] **A2: 情景A/B/C卡片从 E14 `scenarios[]` 动态渲染** — 替代硬编码概率/条件HTML
- [ ] **A3: 行动建议区从 E14 `action_advice` 填充** — 替代硬编码建议文本
- [ ] **A4: 数据来源追溯从 E14 `sources[]` 动态生成** — 替代硬编码"知识:xxx"标签
- [ ] **A5: E14响应缓存延长TTL** — 切换股票时优先显示前一次缓存
- [ ] **A6: 验证** — `renderNarrative()` 在E14返回后使卡1-4显示DeepSeek真实叙事

### T1.2: Phase B — 挂载3路由 + 筹码渲染

**Covers:** G2, G3, G6 *(注: G6筹码路由已在chart.py:588就绪，仅需前端对接)*

**Files:**
- Modify: `backend/app/routes/ai_analysis.py` (B1, B2)
- Modify: `_ui-prototype/indicator-ide.html` (B4, B5)

- [ ] **B1: 挂载 `POST /api/v3/ai/arbitrate`** — 调用 `WikiConceptMatcher.match()`
- [ ] **B2: 挂载 `POST /api/v3/uidf/consolidate`** — 调用 `UIDFEngine.consolidate_with_conflicts()`
- [ ] **B4: 筹码面板从 E11 真实API渲染** — 替代 `Math.sin()+Math.random()` 模拟
- [ ] **B5: 切换股票时调用 E11** — `switchStock()` 中串联 `loadChipDistribution()`
- [ ] **B6: 验证** — 新路由返回正确数据、筹码面板显示真实分布

---

### 参考: 259号方案原文

详见 `002-方案存档/259-个股策略分析板块全量功能补齐方案.md` §3 Phase A+B
