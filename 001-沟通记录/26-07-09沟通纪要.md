# 2026-07-09 沟通纪要

### 001 — 自选监控WebSocket实时数据推送实施

**用户需求：** 全局数据系统已具备盘中数据能力，自选监控需要实现自动推送数据更新的能力

**技术方案：**
- **数据流链**: mootdx TCP (5s采集) → InMemoryStateStore → WsBridge.on_collect_complete() → SocketIO `watchlist` 房间 → 前端自动更新表格
- **后端 WsBridge**：新增 `_watchlist_codes` 集合 + 线程安全管理方法（update/remove/clear）+ `_broadcast_watchlist_quotes()` 推送 `stock:quotes` 事件
- **后端 realtime.py**：`subscribe_watchlist` 处理器增强，将前端订阅的代码注册到 WsBridge
- **前端 watchlist.html**：SocketIO 客户端连接 → 订阅 `subscribe_watchlist` → 监听 `stock:quotes` → 每秒更新价格/涨跌幅/成交量/成交额/最高/最低/开盘/昨收/振幅，3秒渲染节流

**涉及文件：**
- `backend/app/data/ws_bridge.py` — +40行（推送管理+房间推送）
- `backend/app/routes/realtime.py` — 3行（注册自选代码）
- `_ui-prototype/watchlist.html` — +80行（SocketIO客户端+实时更新）

**状态：** ✅ 已完成。盘中数据管道已通路，目前 WebSocket 状态标签从静态 Mock → 真实连接状态指示

### 002 — 自选监控策略评估Bug修复（评分项全显示"—" + 切页面评分丢失）

**问题：**
1. **评分项显示"—"**：`evaluateStock()` 调用 `/strategy/analyze`，该端点返回 `{code:0,...}` 不含 `success` 字段。前端检查 `resp.success` 恒为 false → 走 `fallbackEvaluate` 把评分设成字符串 `'—'`
2. **切页面评分丢失**：评估结果仅存 `stockData` 内存变量，切页面后 `loadWatchlistData()` 从 `/watchlist/quotes` 重新加载（该端点不返回策略评分）→ 评分全部变回 null

**修复：**
- `evaluateStock()` 单只评估改为调用 `/screener/evaluate-batch`（同批量刷新一致的后端链路，返回 `success:true`）
- 抽取 `_applyEvalToStock()` / `_saveEvalToCache()` / `_loadEvalFromCache()` 三函数
- 评估成功后自动写入 `sessionStorage`（key=`wl_eval_{code}`）
- `loadWatchlistData()` 加载 API 数据后用 sessionStorage 中缓存的评分结果覆盖
- 同一 tab session 内切页面/刷新不丢失评分

**涉及文件：**
- `_ui-prototype/watchlist.html` — 约+30行

### 003 — 个股策略分析板块数据连接现状核查及改善方案（258号方案）

**用户需求：** 结合系统全局数据体系及个股策略分析板块功能规划，核对该板块的数据连接现状，输出改善措施报告存档审核。

**执行内容：**
- 全面梳理了双存储引擎（SQLite ECM + PostgreSQL）18+20张表的数据布局
- 核查了 `data_daemon.py` 双进程架构下6类数据的日终批量同步链路
- 核查了 `SignalComputationService.compute_for_stock()` 的五维策略数据依赖链（缠论/量价/筹码/BOCIASI/因子）
- 定位了 **P0级缺口**：财务数据（fina_indicator/income/balancesheet/cashflow）虽已定义表结构和DataManager方法，但未被纳入 data_daemon 日终自动同步，导致BOCIASI慢线和因子估值维度数据缺失
- 定位了 **P1级缺口**：数据源降级时策略分析对前端无任何提示（静默跳过扩展数据加载）
- 定位了 **P1级缺口**：factors/builtin/中的因子族未与 SignalComputationService 实际联通
- 定位了 **P2级缺口**：板块资金流向缺乏持久化聚合层、完整性检查未覆盖龙虎榜
- 报告存档为 `002-方案存档/258-个股策略分析板块数据连接现状核查与改善方案.md`

**涉及文件：** 仅此存档文档，无代码修改

**状态：** ⏸ 待审核。用户通知后再实施代码工作

### 004 — 个股策略分析K线页面9项操作端口深入核查（258号方案第二阶段）

**用户需求：** 核实股票输入筛选窗口、自选股选择、添加自选、实时价格、周期切换、主图辅助图表、K线时间轴、成交量完整度、副图切换、策略同步选股系统配置共9项操作端口的正确性。

**重要发现（核心问题）：**
`indicator-ide.html` 当前在 **`L1094-1201` 生成全量闭包模拟K线数据**（750根随机K线，起始日期2023-06），后续所有指标计算（MA/BOLL/BBI/ENE/九转/MACD/KDJ/RSI/DMI等）均基于此mock数据。`switchStock()` 虽然调用 `GET /api/v3/chart/kline` API，但返回数据仅更新头部价格和股票名称，K线绘制从未切换到真实数据。

**逐项核查结论：**
1. 股票搜索 ✅ 真实API / 自选股选择 ❌ 调用已移除端点 `/api/v3/phase3/watchlist`（404回退）
2. "+非自选"添加 ❌ 调用已移除端点（404），添加只走本地缓存未写数据库
3. 价格显示 ❌ 来源为ECM daily_cache盘后日线，未接入mootdx TCP实时推送
4. 9个周期切换 ❌ 标签页无onclick事件绑定，后端分钟级全降级为日线
5. 主图辅助指标 ❌ 100%前端闭包mock，BBI/ENE/九转后端chart.py中无实现
6. K线时间轴 ❌ mock日期2023-06起与真实日期不吻合
7. 成交量 ❌ mock数据 + ECharts默认dataZoom截断(仅显示14%)
8. 副图18项 ❌ 16项为mock/空数据，仅"大单净量"部分真实
9. 策略同步 ❌ `syncFromScreener()` 为空壳函数仅弹Toast

**措施：** 258号方案已全面修订为全量实施路线图，覆盖后端数据管道 + 前端K线生产化两大领域共8个Phase。

**状态：** ⏸ 待审核

### 005 — 个股策略分析板块全量缺口审计（259号方案规划阶段）

**用户需求：** 全面梳理页面上所有未实现的功能模块——操作端口、展示、K线线型切换、策略分析现状输出、DeepSeek+LLM Wiki联动等，制定259号方案并存档。

**主要发现：**

**后端已编码但路由未挂载（3项）：**
- `POST /api/v3/ai/arbitrate` — 验证链冲突→LLM Wiki匹配→仲裁意见，WikiConceptMatcher已编码
- `POST /api/v3/uidf/consolidate` — UPF+验证链+仲裁→三级决策融合
- `GET /api/v3/chip/distribution/{tsCode}` — 筹码分布，ChipDistributionEstimator已有

**前端未实际对接E14 AI叙事（核心缺口）：**
- 卡1-4叙事文本为硬编码Mock，未调用E14 Layer1
- 情景A/B/C推演+行动建议为硬编码Mock，未调用E14 Layer2
- 数据来源追溯（"知识:xxx"标记）为硬编码HTML，未对接LLM Wiki

**右侧面板仍有Mock（2项）：**
- 盘口五档10行(E8) 无路由 → 硬编码Mock
- 笼子价格+连续竞价(E9) 无路由 → 硬编码占位

**副图仍有Mock（3项）：**
- 主力密码/资金仓位 → 灰色占位虚线
- 融资融券按钮+函数残留 → 已取消未清理

**性能测试结果（000002.SZ）：**
| API | 耗时 | 数据量 |
|-----|:----:|:-----:|
| K线200根 | 67ms | 92KB |
| 行情E07 | 209ms | 431B |
| 资金流向E10 | 14ms | 413B |
| 策略分析E12 | 75ms | 1.3KB |
| 交叉验证E13 | 43ms | 2.2KB |
| **AI叙事E14** | **3.04s** | 3.8KB |
| 自选股列表 | 13ms | 523B |

**关键瓶颈：** E14（AI叙事）耗时3.04秒，是页面加载后策略分析区迟迟不刷新的主要原因。其他API均在200ms以内，体验良好。

**措施：** 259号方案已存档至 `002-方案存档/259-个股策略分析板块全量功能补齐方案.md`

**状态：** ⏸ 待审核
