## 2026-07-11

### 001 — DeepSeek模型迁移全量完成

**用户需求：** deepseek-chat-v4 模型将于 2026/07/24 废弃，需迁移至 v4-flash

**执行内容：**
- 全库搜索 31 处 deepseek-chat-v4，修改 15 个文件
- 后端代码6处 + 配置文件3处 + UI原型2处 + 文档3处 + 工作上下文1处
- 迁移后验证：rg deepseek-chat-v4 返回 0 条残留
- 5 个 Python 文件 AST 语法全部通过
- 详见 _工作上下文.md

### 002 — API配置全量补充与验证

**用户需求：** 查找系统中所有API配置并进行补充和验证

**执行内容：**
- 系统排查全部外部 API/服务配置点（Tushare/DeepSeek/LLM Wiki/Auth/Alert/QMT/CORS）
- 修复3项配置缺陷：
  1. LLM Wiki 搜索缺 Token 认证（ai_analysis.py:372 未传 Authorization 头）
  2. Auth Token 持久化未实现（auth.py 不读 data/auth_token.txt）
  3. 健康检查缺外部 API 连通性验证（新增 _get_external_api_status 函数）
- 4个文件语法通过，5项单元测试+5项集成检查全部通过
- 发现但未修改：.env 使用 PostgreSQL/Redis 但生产规范要求 SQLite

### 003 — 双渠道策略规则一致性评估（262号方案 v1）

**用户需求：** 澄清双渠道定位（筛选 vs 解读），评估策略规则是否统一

**关键结论：**
- 双渠道定位确认：渠道一=筛选(评分制)，渠道二=解读(描述制)；渠道不统一但规则要统一
- 逐策略评估结果：
  - **缠论** ⚠️ 部分不统一 — 渠道一用简化版评分（5维度），渠道二用完整版（11维度）
  - **量价** ⚠️ 参数不统一 — 核心引擎相同，但渠道一缺少 market_env 传递
  - **筹码** 🔴 完全不同 — 渠道一实为"资金行为评分"，渠道二才是真实筹码分布
  - **因子** 🔴 完全不同 — 渠道一 UI 显示34因子但仅9个可计算，渠道二仅5因子但全实现
- 262号方案v1已存档

### 004 — 第二论架构确认与方案全面修订（262号方案 v2）

**用户澄清的关键原则：**
1. 策略后台目标**真实有效**，不能因为方便核算而简化
2. 策略分类应为三层四类：**L1风控 → L2主力筹码 → L3缠论+量价+因子组合(含情绪)+Vibe(含自建)**
3. BOCIASI 情绪策略应是**因子组合中的一个组合**，并非独立策略
4. Vibe策略**两个渠道都需使用**
5. **所有策略由用户选择是否参与**（两个渠道均有可选设置）
6. 渠道一需要设计**统一性评分体系**为最终结果合理性提供依据
7. 策略变化对**回测评价标准的影响**需评估

**执行内容：**
- 全面修正策略架构为三层四类体系
- BOCIASI归入因子组合（作为情绪类因子）
- Vibe引入渠道二（输出形式为策略解读文本）
- L2主力筹码统一为含5个维度的统一策略（资金流向+筹码分布+集中度+散户反向+主力阶段）
- 因子体系统一使用 FactorRegistry 注册表模式（淘汰内联函数）
- 完成统一评分体系设计（z-score归一化保留）
- 完成回测影响评估：5项变更中4项需要回测基准调整
- 提出**增量回测+策略版本化**方案
- 涉及 LLM Wiki 策略知识查阅（筹码分布分析/跨策略融合框架/风险剔除体系）
- 262号方案已修订为 v2 版本（7章+附录，替代v1）

### 005 — 基于LLM Wiki知识库的策略有效性评估（263号方案）

**用户需求：** 针对L1风控、L2主力筹码、L3缠论/量价/因子组合逐一核对LLM Wiki知识库内容，评估策略有效性，找出可提升精准概率的改进方向（Vibe除外）

**执行内容：**
- 查询LLM Wiki 10+概念页面和来源页面
- 逐策略对照评估，识别差距和改进方向
- 产出263号方案存档

**核心发现TOP3：**
1. **级别递归（缠论）** — 当前标注为"长远规划"但其实是缠论根基，没有级别递归买卖点信号性能无法多时间框架验证
2. **BOCIASI四象限聚合（因子组合）** — 代码有快慢线独立算法但缺少四象限判断框架，Wiki回测慢线策略年化10% vs 基准1.3%
3. **融资融券反向信号（L2主力筹码）** — 融资余额暴增是最危险的反向信号，但margin_cache当前0行未接入评分

**产出文件：** 002-方案存档/263-基于LLMWiki知识库的策略有效性评估与提升建议.md

### 006 — 全局数据体系支撑能力评估（补充更新263号方案v2）

**用户需求：** 核实全局数据系统支持的数据类型，评估263号方案中的升级是否有数据支撑，现有数据渠道是否满足数据获取，输出匹配度、缺失性、可获取性

**执行内容：**
- 全表审计ECM 23张表（4.6亿+行）及5条数据通道
- 逐项评估14条改进建议的数据支撑（47%已就绪、33%可获取、7%渠道不足、13%依赖前置）
- 发现3个核心数据瓶颈：① `margin_cache` API选型错误（调用了交易所汇总而非个股明细）② 财务数据未纳入 data_daemon 日终同步 ③ 分钟K线冷门股覆盖不全

**关键结论：**
- **93%改进项在现有数据体系内有支撑或可通过现有渠道获取**
- 仅股债收益差（需国债收益率）需要新增外部数据源，但简化版BOCIASI可绕过
- 3个数据修复项（margin_detail API/财务同步/分钟补齐）相互独立，可并行执行
- 修复后数据支撑率可由93%→100%

**产出文件：** 002-方案存档/263-基于LLMWiki知识库的策略有效性评估与提升建议.md（已更新v2）

### 007 — 数据补充与修复计划制定（264号方案）

**用户需求：** 针对数据端的问题，结合最新全局数据架构和要求，制定数据补充和修复计划

**执行内容：**
- 全面审计 data_daemon.py（750行）的同步流程、完整性检查、财务同步
- 制定5项修复（D1~D5）：margin_detail API / 财务同步补齐 / 分钟补齐优化 / 预计算消费打通 / 巡检增强
- 按3个迭代组织实施：I1数据修复(2天) → I2数据积累(1天) → I3数据消费(2天)
- 明确了与263号方案P0/P1改进的依赖关系：D1+D2是两个"闸门式"修复，解锁BOCIASI四象限和因子覆盖的数据支撑

**产出文件：** 002-方案存档/264-全局数据体系修复与预计算消费打通计划.md

### 008 — 修复计划评估确认：D3周末全量补齐 + D1/D2/D4一次性全量开展

**用户确认方向：**
- D1/D2/D4 一次性全量开展，直接完成
- D3 利用 Tushare 5000积分权限，本周末后台运行全量补齐

**评估结论：**
- D3周末全量补齐可行：pro_bar实测 ~233ms/只，5000只约需30分钟（含限流等待），数据量增 ~31.5M行（ECM增长~8%），完全可接受
- 发现现有 `_batch_backfill_minute_kline()` 存在2个bug（trade_date→trade_time列名错误、vol→volume映射相反），导致分钟回填从未正确执行过
- 关于批量调取：pro_bar不支持官方批量ts_code参数，采用ThreadPoolExecutor并行+限流器方式在单周末脚本中实现高效补齐
- 计划已更新为两批次：批次A(D1/D2/D4/D5 ~2天) + 批次B(D3周末脚本 ~30分钟)
- 264号方案已同步更新

### 009 — 全量实施 D1/D2/D4/D5 完成

**执行内容：**
- D1 margin_detail API：修复 data_daemon.py + __init__.py 两处（pro.margin→margin_detail）
- D2 财务同步：新增 `_batch_balancesheet()` / `_batch_cashflow()` / `_batch_forecast()` 3个函数，纳入日终同步
- D4 预计算消费：修复 `_run_precompute()` 改为仅处理当日活跃股票；chart.py 新增 indicator_cache 预检查；SignalComputationService 新增 factor_cache 优先读取
- D5 巡检增强：完整性检查追加 margin 和 fina_indicator 自动补采
- 新建 scripts/backfill_minute_kline.py — 周末全量补齐脚本（支持批量+并行+缓冲区写入）
- 修复 `_batch_backfill_minute_kline()` 的 trade_time/vol 2个bug
- 5个文件全部 AST 语法验证通过

### 010 — 全量执行完成（D1/D2/D4/D5 ✅ + D3排程）

**执行结果：**
- **D1 margin_detail**: ✅ margin_cache 从0→194,106行，4,380只股票，45个交易日。修复了表结构列名不匹配（`rqchl`字段过滤）
- **D2 财务同步**: ✅ 3个batch函数已添加，纳入日终同步
- **D4 预计算消费**: ✅ chart.py + SignalComputationService 缓存优先读取就绪
- **D5 巡检增强**: ✅ margin + fina自动补采已添加
- **D3 分钟补齐**: ⏳ 脚本已就绪。Tushare stk_mins 接口触发1次/小时限流惩罚，已排程13:30自动执行（预计~30分钟补齐全市场）
- 264号方案已更新实施状态

### 011 — P0策略改进实施完成（BOCIASI四象限 + 级别递归）

**P0-② BOCIASI四象限聚合+情绪动态加权（~1天）**
- 创建 `BociasiQuadrantAnalyzer`（`bociasi_quadrant.py`）— 基于全市场数据计算快线(MA20占比/换手率/涨跌停比/RSI)和慢线(ERP/融资趋势/PE分位)的四象限聚合
- 集成到 Channel 1 L3 `_compute_factor_score()` — 极端情绪(LL/HH)时自动调整因子评分±15%
- 基于 margin_detail 修复后的真实融资余额数据计算慢线

**P0-① 级别递归（缠论多时间框架，~3天）**
- 创建 `ChanlunLevelValidator`（`chanlun_level_validator.py`）— 日线→周线→月线重采样+逐级别缠论分析+级别定理验证
- 集成到 Channel 1 L3 `_compute_chanlun_score()` — 传参 df 触发自动级别验证
- 买卖点级别定理：月线趋势向上+日线买点→确认；月线趋势向下→降权
