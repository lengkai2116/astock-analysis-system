# 2026-07-22

### 001 - 287号方案审核修正（v2.3）

**用户输入**：对287号方案进行审核思考，针对配置寻找更简洁高效的方式，同时审核方案设计是否存在漏洞和问题

**AI执行内容**：
1. 全文审核287号方案（v2.2，732行），出具评审意见报告
2. 配置简化建议（全部被采纳）：
   - 废弃 `strategy_signals` 双表冗余，统一为 `strategy_signal_detail` 单表
   - 因子系统扩展 `FactorRegistry` 而非在 core 内另起一套
   - 直接删除 `SignalComputationService`（不保留委派层）
   - `market_context` 在预计算时一并缓存（避免读取时重复查表）
3. 设计漏洞修正（2.1-2.6采纳，2.7为关键修正）：
   - `_adjust_score_with_context` 保留在选股适配器（不删除）
   - 增量更新因复杂度舍弃，保持全量重算
   - 并发模型增加单股错误隔离（try/except + 超时）
   - 缓存增加 `schema_version` 版本号校验
   - 按 `trade_date` 分区清理替代 DELETE+VACUUM
   - **DeepSeek 不属于策略层功能，不进入预计算**，改为用户触发独立端点
   - 预计算必须在数据采集全部完成后启动

**关键决策**：
- 方案升级至 v2.3
- DeepSeek 只作为用户触发的独立功能，预计算仅覆盖五维数据 + NLG 规则文本
- 287号文档已完成全文修改

### 002 - 287号方案实施完成

**用户输入**：全面实施287号方案，包括定义执行计划、编码实现、验证、交付

**AI执行内容**：
1. 创建实施计划文档 `docs/compose/plans/2026-07-22-287-strategy-precompute.md`
2. 实施6个任务模块：

**T1 - UnifiedStrategyCore 核心类** ✅
- 新建 `backend/app/engine/unified_core.py`（157行）
- `StandardizedResult` / `StrategySignal` 数据类 with JSON serde
- `compute()` / `compute_batch()`（4线程、单股异常隔离）

**T2 - 缓存层 strategy_signal_detail** ✅
- `enhanced_cache_manager.py`：新表 + 3个方法（cache/get/has）
- `data/__init__.py`：DataManager 代理方法（3个）
- schema_version 版本号校验

**T3 - Daemon 预计算接入** ✅
- `_run_precompute()` 策略信号部分改调 `UnifiedStrategyCore.compute_batch()`
- 写入 `strategy_signal_detail` 新表

**T4 - Strategy Analyze 路由** ✅
- E12 优先读缓存（5-50ms），未命中降级实时计算
- `_restore_signals_from_cache()` 将缓存 dict 恢复为 List[Dict]
- DeepSeek 从 E12 移除，新增独立端点 `GET /api/v3/strategy/deepseek`

**T5 - Screener L3 因子体系统一** ✅
- `screen_l3_candidates()` 阶段一改用 `UnifiedStrategyCore.compute_batch()` 替换分散调用
- 保留 `_adjust_score_with_context`、`_compute_chanlun_score` 等评分函数
- 删除 `_FACTOR_COMPUTERS`、`_register_factor`、`_register_ecm_factor` 及全部因子函数（~600行）
- 因子评分通过 FactorRegistry（`backend/app/factors/`，257个因子）

**T6 - 因子预计算 + 外部引用清理** ✅
- `_precompute_preset_combos()` 改用 `FactorPrecomputeManager` + `FactorRegistry` 名称映射
- `routes/screener.py` 因子评分改用 `FactorCalculator`
- `signal_computation_service.py` 删除 `_FACTOR_COMPUTERS` 引用
- 测试文件同步更新（8/8 通过）

**验证结果**：
- screener 集成测试: 8/8 ✅
- UnifiedStrategyCore serde 测试: 7/7 ✅
- 全部组件导入验证: ✅
- lint 无新增错误: ✅

**文件变更统计**: 9个文件（+286行 / -928行）

### 003 — P2 板块策略分析现状调研与方案设计

**用户输入**：要求直白说明 P2 的问题是什么、影响什么、现状如何

**AI执行内容**：
1. 全面调研板块分析现状：数据源（7张表/存储结构）、现有代码（SectorRotationModel/五维卡4/Dashboard/资金流向）、前端消费（5个页面）、ECM归档路径
2. 发现 SectorRotationModel 为**完全孤立代码**——269行已编写但从未被任何Python代码导入或实例化
3. 产出 288 号专项方案 `002-方案存档/288-板块策略分析与行业指数数据补齐方案.md`
4. 产出执行计划 `docs/compose/plans/2026-07-22-288-sector-analysis.md`

**关键决策**：
- 数据源选择申万行业指数（Tushare），31个一级行业
- SectorRotationModel 保留框架、替换数据源后集成
- 6 个阶段实施，预计 3.3 天（核心）

### 004 — 288号方案审核与精炼

**用户输入**：对288号方案进行审核，寻找更简洁高效的实现方式

**AI执行内容**：
1. 全面审核288号方案v1.0，发现4个可简化点：
   - SectorRotationModel 无人调用，不应花0.8天修复 → 内联到 SectorAnalysisService
   - 独立板块API端点不必要 → 数据合入 E12 响应
   - Stock.industry 实际值分布未验证 → 需实施前确认
   - 历史数据回填缺失 → 60日 backfill
2. 六阶段压缩为三阶段（3.8天→1.9天），方案升级至 v1.1

**关键决策**：
- 实施前先执行 `SELECT DISTINCT industry FROM stocks` 验证映射可行性
