# 沟通记录

## 2026-09-08

### 1 - 416号方案实施：RAW计算配置错误修复

**用户输入**：请调用serena MCP工具实施416号方案，完成后进行检查验证 / 请你直接重启daemon即可

**AI执行内容**：
1. 核查 416号方案 5 项修复的代码落地状态（上一轮核查确认均未落地，全部实施）
2. **修复1**：`backend/app/factors/builtin/a_stock.py` 新增 `VOL_RATIO_20` 因子类（20日量比，与 VOL_RATIO_5/10 同构），使 `FACTOR_NAME_MAP['20日换手率']` 映射生效
3. **修复2**：`backend/app/factors/builtin/academic.py` 三个学术波动率因子改名——`ACADEMIC_VOLATILITY_10/20/60` 的 `name` 由 `VOLATILITY_10/20/60` 改为唯一名 `ACADEMIC_VOLATILITY_10/20/60`，消除与 a_stock 版同名覆盖冲突
4. **修复3/4/5**：新建 `backend/scripts/migrate_indicator_columns.py` 迁移脚本，对主库 `stock_cache.db` + 分库 `compute_cache.db` 幂等补列：`indicator_ma` 补 `ma120/ma250`、`indicator_other` 补 `bbi/ene_upper/ene_lower/nine_buy/nine_sell`、主库建 `market_stats_cache` 表
5. 运行迁移脚本（主库列此前已被 daemon 启动时 `_init_tables` ALTER 补上——脚本报"已存在跳过"；分库 7 列全部由脚本补上）
6. **重启 daemon**：向旧 daemon(1374) 发 SIGTERM 优雅停止，`start_daemon.sh` while 看守循环自动拉起新进程(8728, 09:39:21)

**验证结果**：
- registry 解析：`VOL_RATIO_20` → a_stock 版 ✅；`VOLATILITY_10/20/60` → a_stock 版（source="A-Stock"，此前被 academic 覆盖）✅；学术版唯一名全部注册、无 VOLATILITY 覆盖警告 ✅
- FACTOR_NAME_MAP 无死映射：8 个唯一因子全部可解析（此前仅 7 个）✅
- 表结构：主库/分库 `indicator_ma` 含 ma120/ma250、`indicator_other` 含 bbi/ene/nine 5 列、主库 `market_stats_cache` 存在 ✅
- 读路径：`get_indicators_wide` 不再抛 `no such column`，返回含 bbi/ene/nine 列数据（R22 chart.py 信号路径受益）✅
- 新 daemon 启动日志：`从SQLite恢复市场级统计缓存: 8个指标`（旧 daemon 因表不存在无法恢复，仅实时计算）→ 修复5 运行态生效 ✅
- AST 语法校验：3 个修改文件全部通过
- pytest 全量回归不可用（预存在收集错误 test_394/395_dim8 + 运行超时，与本次改动无关）

**待观察**：BBI/ENE/九转、MA120/250 新列数据为空——因缺列期间数据被 `_insert_from_df` 过滤丢弃，需 daemon 下次日终同步触发 RAW 预计算后自动回填（属预期异步行为）。

**修改文件**：
- backend/app/factors/builtin/a_stock.py（新增 VOL_RATIO_20 类）
- backend/app/factors/builtin/academic.py（ACADEMIC_VOLATILITY_10/20/60 改名）
- backend/scripts/migrate_indicator_columns.py（新建迁移脚本）
- data/duckdb/compute_cache.db（分库补 7 列）
- data/duckdb/stock_cache.db（market_stats_cache 建表确认）

### 2 - COL采集端数据清洗核查

**用户输入**：请你验证一下系统COL采集端对于采集的数据是否进行了有效的数据清洗？

**AI执行内容**：核查 COL 采集端全链路清洗机制（data_daemon.py 批量采集函数 + enhanced_cache_manager.py 写入网关 + mootdx/akshare 实时采集器）。

**核查结论**：

**已做（基础格式清洗）**：
1. **统一写入网关 `_insert_from_df`**：所有批量采集最终走此方法，先经 `_validate_and_fix_data_format` 做日期统一（YYYY-MM-DD）、代码统一（大写+补 .SH/.SZ 后缀）
2. **去重**：`INSERT OR REPLACE` 依赖各表主键（daily_cache 主键 ts_code+trade_date），重复采集自动覆盖
3. **批量采集字段级清洗**：`_batch_lhb` 列白名单过滤+重命名、`_batch_margin` 剔除多余列、`_batch_lhb_detail` 跳过空记录+side 归一、`_batch_moneyflow` 补齐 net_* 列、`_batch_index_daily` 列白名单
4. **实时采集器**：`_safe_float` NaN→0、`_fetch_eastmoney` 跳过 price==0/prev_close==0 快照、`_get_a_share_codes` 严格过滤非 A 股代码、`_feed_minute_aggregator` 跳过 price==0、`_collect_sentiment_pool` 代码清洗+连板数异常兜底

**未做（深度质量清洗）**：
1. **NaN/空值未显式处理**：批量 Tushare 数据无 dropna/fillna，NaN 直接落库为 NULL，下游 RAW 需自行防御
2. **OHLC 一致性校验缺失**：无 high≥low、open/close 在区间内等合理性校验
3. **异常值/离群值检测缺失**：无涨跌幅超限、价格突变等离群过滤
4. **类型转换不彻底**：仅日期/代码做格式清洗，数值列未统一 to_numeric

**总体判断**：清洗强度"够用但非严格"——能保证格式规范、无重复、无空快照，但异常数值透传 RAW 层由下游自行防御。符合四层架构设计意图（COL 只负责采集+基础规范化，深度质量校验留给 RAW 计算层）。如需加强，建议在 `_validate_and_fix_data_format` 补充 NaN 处理与 OHLC 一致性校验。

### 3 - 417号方案存档：COL深度清洗增强

**用户输入**：制定COL深度清洗增强方案并存档

**AI执行内容**：基于上一轮核查的 4 处清洗缺口，制定 **417号方案** 并存档至 `002-方案存档/417-COL采集端深度数据清洗增强方案.md`。

**方案要点**（4 项增强，落点在统一写入网关 `_validate_and_fix_data_format` + 新增 `_apply_deep_clean`）：
1. **增强1（HIGH）数值列类型统一 + NaN 处理**：数值列统一 `to_numeric`；按表类型差异化处理 NaN——K线价格列 NaN→0（与 `_safe_float` 一致）、财务/基本面列（PE/PB）NaN→NULL 保留缺失语义不伪造 0、资金流 net_* 列 NaN→0
2. **增强2（HIGH）OHLC 一致性校验**：仅 K 线类表——high<low 交换、open/close 夹取到 [low,high]、价格全≤0 丢弃
3. **增强3（MED）离群值检测**：仅 K 线类表——|pct_chg|>50 剔除并告警（阈值保守避免误杀新股/ST）
4. **增强4（MED）清洗可观测性**：清洗前后行数差异 >0 时输出告警日志

**设计原则**：不改变四层架构职责（COL 采集+规范化 → RAW 计算）；幂等可回滚（不改变表结构）；新增 `backend/tests/test_col_cleaning.py` 覆盖 4 项增强。

**存档文件**：`002-方案存档/417-COL采集端深度数据清洗增强方案.md`

### 4 - 417号方案实施：COL深度清洗增强

**用户输入**：请调用serena MCP工具针对417号方案进行实施，完成后需进行检查验证

**AI执行内容**：实施 417号方案 4 项增强，全部落在 `backend/app/data/enhanced_cache_manager.py` 统一写入网关。

**代码改动**：
1. **模块级常量**：新增 `_NUMERIC_COLUMNS`（各表数值列清单，列名与实际表结构对齐）、`_OHLC_TABLES`（K线类表：daily_cache/minute_kline_cache/as_minute_kline/as_market_snapshot）、`_NULL_NAN_TABLES`（财务/基本面表保留 NULL）
2. **`_insert_from_df`**：在 `_validate_and_fix_data_format` 后新增 `_apply_deep_clean` 调用 + 剔除行数差异告警日志（增强4）
3. **新增 `_apply_deep_clean`**：规则4 数值列 to_numeric + 按表类型 NaN 处理（K线填0、财务保留NULL）；规则5-6 仅 K 线类表执行
4. **新增 `_fix_ohlc`**：high<low 交换、open/close 夹取到 [low,high]、价格全≤0 丢弃（增强2）
5. **新增 `_detect_outliers`**：|pct_chg|>50 剔除并告警（增强3）
6. **新增 `backend/tests/test_col_cleaning.py`**：9 个测试覆盖 4 项增强

**验证结果**：
- 新增测试：`test_col_cleaning.py` 9/9 通过 ✅
- 既有回归：`test_t25_f4_ohl.py` + `test_snapshot_db.py` 8/8 通过 ✅
- ruff lint：`enhanced_cache_manager.py` + `test_col_cleaning.py` 全部通过 ✅
- 真实数据验证：5000 行 daily_cache 无 OHLC 异常、无价格≤0，清洗零误伤 ✅
- 真实写入验证：构造 high<low + pct_chg=60 异常数据，清洗正确剔除异常行并输出告警日志 ✅（写入遇 `database is locked` 为 daemon 运行持有写锁的并发预期行为，与本次改动无关；无残留测试数据）

**修改文件**：
- backend/app/data/enhanced_cache_manager.py（常量 + _insert_from_df + 新增 _apply_deep_clean/_fix_ohlc/_detect_outliers）
- backend/tests/test_col_cleaning.py（新建）

---

### 5 - 后台变更提交并上传 GitHub 备份

**用户输入**：检查后台所有的变更进行提交，同步上传github备份

**AI执行内容**：
1. 核查全部工作树变更：264 个已修改 + 84 个合法未跟踪 = 350 个（跨 371-417 号方案实施 + 沟通记录/方案文档/审计报告归档），无临时垃圾（DS_Store/__pycache__/.log 均无）
2. 识别 `.qwen/`(4K)、`.serena/`(70M)、`backend/.serena/`(20K) 为 agent 工具元数据（缓存索引/会话内存，从未入库）→ 在 `.gitignore` 追加排除规则，`git check-ignore` 验证生效
3. 核验无敏感文件入库（.env/secret/token 均不在清单）
4. `git add -A` 暂存 350 个变更 → 创建综合提交
5. `git push origin main` 成功（4225ae6..2a34db1），工作树干净

**提交信息**（`2a34db1`，350 files，+36601/-8268）：
```
feat: 371-417方案实施（dim体系/RAW预计算/COL深度清洗）+ 文档归档
```
- COL深度清洗增强（增强1-4）、416号RAW配置修复、opportunity_atlas 引擎、sync_request 模型、status_engine.yaml、371-417 文档归档、.gitignore 排除工具元数据目录

**验证结果**：
- 提交成功：`[main 2a34db1] feat: ... 350 files changed`，工作树 clean ✅
- Push 成功：`4225ae6..2a34db1 main -> main`（GitHub: lengkai2116/astock-analysis-system）✅

**注意**：本次提交将 `backend/tests/test_273_implementation.py` 与 `backend/app/opportunity_atlas/signal_decay_detector.py` 删除（2 个删除项，属既有重构清理），非误删。

---

### 6 - 409号方案实施核查 + 418号方案存档

**用户输入**：请详细阅读409号方案，并针对系统实际代码情况进行核查，检查是否已按此方案内容完成了系统的配置。如有问题在对话框内进行说明，不要修改方案和代码。 / 针对问题1和问题2制定补齐方案并存档

**AI执行内容**：
1. **409号方案全面核查**（只读，未修改任何方案与代码）：发现 5 个 Phase 均未按方案原文完成——实际代码已演进为 411/412 号方案形态（dim1 门禁化 + data_context 传递 + signal_analyzer 迁移已落地），但 409号 自身关键设计（DataContext 数据类、DataValidator RULES 表、STG 同步补算闭环 `_stg_repair_loop`、JUD v3.1 完整接入）均未实现
2. 核查中定位两个独立问题：
   - **问题1（HIGH）**：`status_engine.yaml` 声明 `jud_engine_version: "v390"` 但 status_engine.py 无任何代码消费该配置，主流程直接走 legacy `_aggregate`（arbiter.arbitrate）；v390 组件（consensus_engine/conflict_matrix/factor_arbiter/advice_engine）全部已实现但生产代码零调用；`JUD环节代码实审报告.md` 声称的 `_aggregate_v390` 经 `git log -S` 证实从未存在于代码库
   - **问题2（MED）**：411号 Phase 4 后 `results['signal']` = dim1 门禁结果（无 judgment.attribute），signal_analyzer 输出被放在 `results['signal_analysis']` 键 → dim_adapter/reliability_assessor/dim8 读取 `'signal'` 键全部拿不到数据（direction 恒 0、reliability 恒 0.5、dim8 冲突检测失效），键名错位
3. **418号方案存档**：`002-方案存档/418-JUD管线接入与signal消费链补齐方案.md`，核心设计：
   - Step 1（P0）：signal_analyzer 输出并入 `results['signal']`（叠加 judgment 子结构，修复问题2，降级保护天然满足）
   - Step 2（P0）：实现 `_aggregate_v390`（L1 convert_to_factors→L2 assess→L3 consensus→L4 conflict→L5 factor_arbiter→L6 compute_advice，逐层 try/except 降级）
   - Step 3（P0）：`evaluate()` 版本分支（消费 jud_engine_version）+ `_assemble` 390号字段扩展（final_score/semantic_type/reliability_summary/consensus_detail，仅新增不改旧键）
   - Step 4-5（P1）：test_418_jud_v390.py 9 用例 + 回归 + 新旧分布对比（±10% 切换标准）
   - 总工时 2.1 天；legacy 路径完整保留，配置一键回滚（390号 P5.4）

**验证结果**：
- 核查结论已完整呈现在对话框（4 个问题 + Phase 判定表）
- 418号方案已存档（title/type/date 元信息齐备）
- 未修改任何方案与代码（仅新建 418 号方案文档 + 沟通记录）

---

### 7 - 418号方案实施：JUD管线接入 + signal消费链补齐

**用户输入**：请调用serena MCP工具，实施418号方案，完成后需进行检查和验证

**AI执行内容**（serena 语义编辑工具实施，仅改 status_engine.py + 新建测试）：

**Step 1（问题2修复）**：`_build_dim_engine_results` Step 3 将 `analyze_signal` 输出并入 `results['signal']`（在 dim1 门禁结果之上叠加 status_description/judgment/audit 子结构）——修复 dim_adapter/reliability_assessor/dim8 读取 `'signal'` 键全部落空的问题

**Step 2（问题1核心）**：新增 `_aggregate_v390`（L1 convert_to_factors → L2 assess → L3 consensus_engine.compute（weights=MARKET_REGIME_WEIGHTS，emotion_phase 归一化到 ice/ebb/normal/recovery/positive/climax）→ L4 conflict_matrix.detect → L5 factor_arbiter.arbitrate → L6 advice_engine.compute_advice），逐层 try/except 降级；硬否决→avoid、致命冲突→wait 对齐 legacy 行为；新增 `_v390_result` 静态组装器（含 390号 新字段 final_score/semantic_type/reliability_summary/consensus_detail/advice）

**Step 3**：`evaluate()` 消费 `jud_engine_version` 配置分支（v390/legacy）；`_assemble` 在 v390 输出下追加 390号 字段 + L6 advice 并入 advice_params（仅新增不改旧键）

**Step 4**：新建 `backend/tests/test_418_jud_v390.py`（11 用例：signal 合并 4 + v390 管线 3 + 版本分支/输出扩展 4）

**验证结果**：
- 新增测试 11/11 通过；回归 60/60（test_411_pipeline/test_390_integration/test_dim1_gate）+ 补充 25/25（test_col_cleaning/test_main_path_unified/test_dim3_patterns）全部通过
- ruff：status_engine.py + test_418_jud_v390.py 全部通过
- 真实数据验证（000001.SZ）：
  - **v390 分支**：opportunity_state=avoid、final_score=0.0、semantic_type=初现型、direction=bear、consensus_rate=-0.784、reliability_summary/consensus_detail 全部产出 ✅（配置 `jud_engine_version: "v390"` 首次被真正消费）
  - **legacy 分支**（临时覆盖 cfg）：opportunity_state=wait、无 final_score 字段（结构不变）✅
  - 配置切换真实生效，双管线并存
- 首次全流程 5.4s（含冷启动 + daemon 锁竞争），未达 390号 ARCH-8 <500ms 基准（后续需 warm cache / 性能优化）

**额外发现（与本次改动无关，既有问题）**：
- dim7_valuation_engine.evaluate()（L731）为残缺实现：`_compute_valuation` 返回 None 时 `val['valuation_level']` 解包崩溃，且 evaluate 主体仅访问两个键即结束、无 try/except——`'NoneType' object is not subscriptable` 为 pre-existing bug，建议后续单独修复
- 单只 evaluate 耗时超 390号 500ms 基准（v390 首次 5.4s），需后续性能优化

**修改文件**：
- backend/app/opportunity_atlas/status_engine.py（_build_dim_engine_results 合并 signal + 新增 _aggregate_v390/_v390_result + evaluate 版本分支 + _assemble 字段扩展，+149/-1）
- backend/tests/test_418_jud_v390.py（新建，11 用例）

---

### 8 - dim7 valuation 残缺实现修复

**用户输入**：修复 dim7 valuation 残缺实现

**AI执行内容**（修复 418 号实施中发现的 pre-existing bug）：

**根因**：`2a34db1` 提交中 dim7_valuation_engine.py 被截断（1082→744 行，git 对比 9f77d50 确认）：
1. `_compute_valuation` 在 df_cf 加载后截断——无四锚估值/level/deviation/return，返回 None → evaluate L731 `val['valuation_level']` 解包崩溃（`'NoneType' object is not subscriptable`）
2. `evaluate` 只剩 2 行主体（访问两个键即结束），无 status_description/judgment/audit 输出
3. `_fina_health` 方法（四维财务健康检查）整体丢失
4. `_adjust_composite` 孤函数引用了未定义的 `data_context`（签名无此参数）

**修复措施**：
1. **补全 `_compute_valuation`**：恢复 a1-a5 四锚加权（PB/PE/现金流/调整PE/股债）+ 周期股 PE 陷阱 PB 权重切换 + 小市值调整 + 财务质量调整（接线 `_adjust_composite`）+ level/deviation + PE/PB/PS 分位 + FCF/股息/营收增长 + 完整 return（保留 411号 Phase 6 / 412号 B4 的 data_context-first 加载）
2. **恢复 `_fina_health`**：ROE>6% / ROCE>15%（三重数据源）+ 负债率<70%（金融除外）+ 经营现金流覆盖净利润，fail/suspicious/pass 判定
3. **补全 `evaluate`**：完整 status_description（含 plain 白话）+ judgment（valuation_level/overall_direction/continuous_value）+ audit（6 条件）
4. **修复 `_adjust_composite`**：新增 `data_context` 参数
5. **修复 dim_adapter 3 处 `int(potential_score)` 崩溃**（418 恢复 evaluate 后暴露的旧版格式不匹配）：dim7 的 `potential_score` 为展示字符串（"潜力评分53/100"，dim8 消费），dim_adapter 期望数字——新增 `_potential_score_int` helper（优先读新增数字字段 `potential_strength`，回退正则解析字符串），dim7 status_description 新增数字字段 `potential_strength`

**验证结果**：
- 真实数据：000001.SZ valuation_level=high→fair、plain 白话、audit confidence=0.83 完整输出 ✅；v390 全流程 L1 convert_to_factors 不再失败，factor 维 strength=0.53 ✅
- 多股票抽样（000001/600000/000002）：valuation 维度全部正常无崩溃 ✅
- 新增回归测试 4 用例（test_418 追加 TestDim7Restored：字符串/数字解析兼容 + convert_to_factors 不崩溃 + evaluate 结构完整性）
- 回归 70/70 通过（test_418 15 + 411/390/dim1_gate/dim3_patterns 55）；ruff 全部通过
- 注：test_395_dim8_builders.py 存在预存在收集错误（import DIM_TEXT_BUILDERS 失败，与本次改动无关）

**修改文件**：
- backend/app/opportunity_atlas/dimensions/dim7_valuation_engine.py（_compute_valuation 补全 + _fina_health 恢复 + evaluate 补全 + status_description 新增 potential_strength 数字字段）
- backend/app/opportunity_atlas/dim_adapter.py（新增 _potential_score_int helper + 3 处 int() 防御化）
- backend/tests/test_418_jud_v390.py（追加 TestDim7Restored 4 用例，11→15）

---

### 9 - 419号方案实施：dim5数据分拨合规 + 性能修复

**用户输入**：制定专项方案存档并实施（针对 dim5 违反 dim1 唯一数据入口 + 性能根因）

**方案存档**：`002-方案存档/419-dim5数据分拨合规与性能修复专项方案.md`（修复A 缓存短路 / B 板块热度持久化+分拨 / C dim5直调清理 / D 索引兜底，2.5天）

**AI执行内容**（6 文件 + 2 测试文件）：

**Step 1（修复A）**：`bociasi_quadrant.py` 7 处 `raise StopIteration` + `except pass` 缓存短路失效重构为 if/else——market_stats 命中后不再 SQL 回退（实测 0.1ms，7 个回退方法零调用，limit_ratio 缓存值 0.1 不再被回退值覆盖）

**Step 2（修复B）**：板块热度纳入 dim1 分拨——ECM 新增 `sector_heat_cache` 表 + `cache_sector_heat`/`get_cached_sector_heat`（仿 market_stats 模式）；daemon `compute_all_heat` 后持久化；framework 版 `compute_all_heat` 补 `indicator_ma_dict` 参数（消除与 dim5 副本类双实现分叉）；dim1 data_context 加载 `sector_heat`；dim5 改从 data_context 消费

**Step 3（修复C）**：删除 dim5 副本类 `SectorRotationModel`（TTLCache 未定义 NameError 根源）+ `_time_rhythm` 死代码（直调 DataManager）；板块热度改为 data_context 读取

**Step 4（修复D）**：新建 `backend/scripts/migrate_indicator_date_index.py` 幂等补 `idx_ind_other_date`——分库已建，主库因 daemon 持锁待空闲窗口

**实施中发现并修复的隐藏缺陷（性能主因的深层根因）**：
1. **dim1 取不到 ts_code**：`_load_tags` 的 pre_feat 扁平化后无 ts_code 键，dim1 `tags.get('ts_code')` 得空 → 整个加载块跳过 → market_stats 进不了 dc → analyzer 回退。修复：dim1 evaluate 新增 `ts_code` 参数（status_engine 显式传入）
2. **request_data 锁等待拖慢门禁**：缺失项映射错误（财务表→full_daily 应为 finance_report）+ 逐项串行 2s×N（daemon 持写锁超时）。修复：task_map 对齐 daemon 消费器 + 无映射项跳过 + **通知体整体放入后台线程**（不阻塞 evaluate 主链路）

**验证结果**：
- **性能：5.4s → 54-69ms（降幅 98.9%）**，远超 390号 ARCH-8 <500ms 基准；4 只股票（000001/600000/000002/600519）多轮稳定 avg 88-128ms
- 测试：新增 test_419_dim5_compliance.py 11 用例（缓存短路 3 + sector_heat 2 + dim5 合规 4 + ts_code 2）+ 回归 89/89 = **100/100 通过**；ruff 全绿
- test_main_path_unified.py 2 个旧测试更新引用（dim5 副本类→framework 版）
- 板块热度：sector_heat_cache 表已建、daemon 下轮日终预计算后回填（预期异步行为）；当前 sector_heat 在 dc 中暂缺（daemon 未跑新预计算），dim5 降级不崩溃
- 主库 idx_ind_other_date 待 daemon 空闲窗口补建（分库已建）

**修改文件**：
- backend/app/engine/framework/bociasi_quadrant.py（7 处缓存短路重构）
- backend/app/engine/framework/sector_rotation_model.py（compute_all_heat 补 indicator_ma_dict + _get_ma）
- backend/app/data/enhanced_cache_manager.py（sector_heat_cache 建表 + 读写方法）
- backend/data_daemon.py（compute_all_heat 后持久化 sector_heat，+12行）
- backend/app/opportunity_atlas/dimensions/dim1_signal_engine.py（ts_code 参数 + sector_heat 加载 + task_map 修正 + 通知后台线程化）
- backend/app/opportunity_atlas/dimensions/dim5_emotion_engine.py（删副本类 + 删 _time_rhythm + data_context 消费板块热度）
- backend/app/opportunity_atlas/status_engine.py（传 ts_code 给 dim1）
- backend/scripts/migrate_indicator_date_index.py（新建索引迁移脚本）
- backend/tests/test_419_dim5_compliance.py（新建 11 用例）
- backend/tests/test_main_path_unified.py（2 旧测试改引用 framework 版）

---

### 10 - 420号方案实施：dim8状态总结引擎消费面增强

**用户输入**：制定dim8增强方案并存档实施

**方案存档**：`002-方案存档/420-dim8状态总结引擎消费面增强方案.md`（基于上轮核查：dim8 仅消费灯色/方向/plain 三层，76 个量化富字段未消费 + LLM Wiki 知识库对照）

**AI执行内容**（仅改 dim8_summary_engine.py + 新建测试）：

**增强1（HIGH）**：共识率置信度加权——`_calc_consensus_rate` 用各维 `continuous_value`（0-1 置信度）修正权重（eff_w = base_w × (0.3+0.7×confidence)，保底 30%），对齐知识库"多指标共识机制"；缺失回退等权等价

**增强2（HIGH）**：盈亏比 rr 状态修正——`_derive_status_bar` 纳入 risk 的 `rr_value`（rr≥2 上修一档、rr<1 下修），对齐"回报风险比"概念

**增强3（MED）**：信号老化降级——`_is_signal_decaying`（lifecycle_stage 末期/衰竭 或 maintenance 非 healthy）→ strong_confirm 降级 light_confirm + 文字"信号老化"提示，对齐"因子衰减/生命周期"

**增强4（MED）**：冲突检测扩展 3 条——散户减仓+结构上升、量价形态空头+信号确认、波动率高+结构下降（对齐筹码主力视角/多形态共振/Barra）

**增强5（LOW）**：综合文字富化——text 补充关键量化锚点（盈亏比/支撑压力/情绪温度/信号天数），增强股票现状描述信息密度

**增强6（LOW）**：审计接线——audit.confidence 均值 <0.7 时 status_description 加 data_warning

**验证结果**：
- 新增 test_420_dim8_enhancement.py 18 用例（18/18 通过）；回归 100/100（419/418/411/390/dim1_gate/main_path/dim3/col_cleaning）= **118/118 通过**；ruff 全绿
- 真实数据（000001/600000/000002/600519）：text 全部含盈亏比/支撑/情绪温度/信号锚点；状态条稳定（全中性观望无跳变）；data_warning 正确提示
- **验收标准**：等权共识 0.47 → 加权 0.51（Δ=0.04 ≤0.1），置信度加权合理微调不颠覆既有判定 ✅
- 定位保持：dim8 仍只读 lifecycle['dim_results']，纯整理输出不变，零下游影响

**修改文件**：
- backend/app/opportunity_atlas/dimensions/dim8_summary_engine.py（新增 3 辅助 _extract_dim_confidence/_extract_dim_rr/_is_signal_decaying + 增强1-6）
- backend/tests/test_420_dim8_enhancement.py（新建 18 用例）

---

### 11 - 420号方案落地核查 + 类型标注修正

**用户输入**：请结合420号文档内容，对系统实际代码进行逐项逐行检查，如有执行遗漏和错误请进行修正

**AI执行内容**（逐项逐行核查 420号方案 6 项增强落地状态 + 修正遗漏）：

**核查结论（6 项增强全部已落地，无执行遗漏）**：
1. **增强1 共识率置信度加权**：`_calc_consensus_rate` 已用 `continuous_value` 修正权重（eff_w = base_w×(0.3+0.7×confidence)）✅
2. **增强2 盈亏比 rr 状态修正**：`_derive_status_bar` 已纳入 `rr_value`（≥2 上修、<1 下修）✅
3. **增强3 信号老化降级**：`_is_signal_decaying` + strong_confirm 降级 + 文字提示 ✅
4. **增强4 冲突检测扩展**：散户减仓/形态背离/波动率放大 3 条新规则 ✅
5. **增强5 综合文字富化**：盈亏比/支撑压力/情绪温度/信号天数锚点 ✅
6. **增强6 审计接线**：audit.confidence 均值 <0.7 → data_warning ✅

**修正的 2 处类型标注错误（mypy，dim8 文件自身）**：
1. **`_extract_dim_rr`**：返回类型标注缺失 + `float(sd.get('rr_value'))` 传入 `Any|None` 崩溃风险 → 补 `-> float | None` 返回标注 + 先判空再 float
2. **`evaluate` 签名**：`signals`/`lifecycle` 默认 None 未标注 Optional（PEP 484 no_implicit_optional）→ 改为 `dict | None = None`

**验证结果**：
- 修正后 dim8 文件自身 mypy 0 错误、ruff 全绿 ✅
- test_420_dim8_enhancement.py 18/18 通过 ✅
- 回归：test_418_jud_v390.py + test_419_dim5_compliance.py 26/26 通过 ✅
- 完整套件（排除预存在收集错误 test_394/395_dim8）：671 passed，1 failed（test_t8_snapshot_fields.py 快照字段映射，与 dim8 无关，预存在问题）
- 真实数据 000001.SZ：text 含全部锚点（盈亏比1.86/支撑11.5/压力12.08/情绪温度42.5/信号0天）、data_warning 正确触发（60%<70%）、状态条"中性观望"稳定 ✅
- 验收标准：等权 0.47 → 加权 0.51（Δ=0.04 ≤0.1）✅

**预存在问题（与 420 号无关，方案 §5.2 已注明"预存在收集错误除外"）**：
- test_394_dim8_description.py / test_394_dim8_quality.py / test_395_dim8_builders.py 收集错误（import 的 `_generate_six_dim_report`/`RECALC_LIMIT`/`DIM_TEXT_BUILDERS` 等符号在 HEAD 已提交版本即缺失，非本次改动引入）

**修改文件**：
- backend/app/opportunity_atlas/dimensions/dim8_summary_engine.py（_extract_dim_rr 返回标注+判空、evaluate 签名 Optional 标注）
- 002-方案存档/420-dim8状态总结引擎消费面增强方案.md（status: 待实施 → 已实施）

---

### 12 - 394/395测试收集错误根因核查 + 过时测试删除

**用户输入**：请核查"test_394_dim8_description.py / test_394_dim8_quality.py / test_395_dim8_builders.py 存在收集错误"的问题根因并说明解决方式 / 测试文件如何无用就进行删除，另外需要你评估现在的系统情况是否需要添加缺失的质量检查功能

**根因核查结论**：
1. **符号从未在生产代码实现**：`git log -S` 确认 `_generate_six_dim_report`/`RECALC_LIMIT`/`DIM_TEXT_BUILDERS`/`_check_dim_quality`/`_calc_quality_score`/`_translate_fields`/`_build_*_text` 等符号**从未**出现在 `dim8_summary_engine.py` 任何历史版本，只存在于方案文档 + 测试文件
2. **394号方案声称"已实施"但实际未落地**：status 标注"已实施（2026-09-18）"，但 Phase 2 质量检查（_check_dim_quality/_calc_quality_score/RECALC_LIMIT）、Phase 4 描述生成器（_collect_dim_plain/_unify_dim_plain/_build_seven_dim_report）、Phase 5 删除判定功能（_calc_consensus_rate/_detect_conflicts/_derive_status_bar/DIM_WEIGHTS 全部保留）均未实现
3. **395号方案（status: 待实施）设计从未落地**：三层描述体系（DIM_TEXT_BUILDERS/SIX_DIM_PRIORITY/_translate_fields）未实现，但测试文件已随 2a34db1 入库
4. **420号方案实际取代 394/395 部分设计**：420 用 `_generate_text` 实现描述生成（覆盖 394 Phase 4 / 395 描述意图）；420 基于 `_calc_consensus_rate`/`_detect_conflicts`/`_derive_status_bar` 做增强，**实际否决了 394 Phase 5"删除判定功能"**；但 420 未实施 394 Phase 2 质量检查

**质量检查功能评估（结论：无需新增，已有等价能力）**：
- **dim1 门禁层已承担数据质量校验**（411/412号方案）：`_validate` 检查 20+ 原料表完整性 + 日期对齐 + completeness_score + quality_level（good/degraded/failed），`_notify_missing_data` 写 sync_requests 异步补采——这是**数据源级**质量门禁，比 394 设计的"dim8 检查 dim_results 有无输出"更前置、更根本
- **dim8 已有 audit 稽核**：7 维输出完整性检查 + confidence 评分；420号增强6 已接线 audit.confidence 均值 <0.7 → data_warning
- **394 Phase 2 的 `_check_dim_quality` 是"输出层"检查**（judgment 非空/plain 非空/confidence≥0.3/方向一致性），与 dim1 门禁职责重叠，且 dim8 定位为"纯整理输出"（358号），不应承担判定/重算职责
- **394 Phase 3 重算功能（_trigger_recalc）违反四层架构红线**：dim8 触发重算 = 调用层触发数据计算，违反"数据计算在数据到达时后台自动触发"原则，且与 sync_requests 异步补采机制重复
- **结论**：394 Phase 2/3 设计已被 dim1 门禁 + sync_requests 机制取代，无需新增；394 Phase 4/5 已被 420 号取代。三个测试文件为"从未落地或被取代功能"的测试，属无用测试

**执行**：
- `git rm` 删除三个过时测试文件（无其他引用）
- 完整测试套件收集错误消除：671 passed，仅剩 1 个与 dim8 无关的预存在失败（test_t8_snapshot_fields.py 快照字段映射）
- 394/395 方案文档 status 标注"已作废（被 420号取代）"

**修改文件**：
- backend/tests/test_394_dim8_description.py（删除）
- backend/tests/test_394_dim8_quality.py（删除）
- backend/tests/test_395_dim8_builders.py（删除）
- 002-方案存档/394-SIG环节dim8系统配置规划与实施方案.md（status: 已实施 → 已作废）
- 002-方案存档/395-dim8现状描述能力评估与改进方向.md（status: 待实施 → 已作废）

---

### 13 - B2（dim4字段错位）+ B1（分库迁移）处理

**用户输入**：处理 B2（dim4 字段错位）和 B1（分库迁移）

**AI执行内容**（承接 SIG 实数测试问题清单的 B 类数据层问题）：

**B2（dim4 字段错位）——已解决**：
1. **PHASE_UNKNOWN/PHASE_MAP 未定义**：369号物理合并 phase_detector.py 进 dim4 时遗漏这两个模块级常量，导致 `compute_tags` 抛 NameError（被吞掉 → phase 恒为 unknown）。修复：在 dim4_chip_fund_engine.py 模块级定义 `PHASE_UNKNOWN = "unknown"` + `PHASE_MAP`（主力吸筹阶段中文映射）
2. **fund_flow 'mixed' 误判为"强流出"**：`'inflow' in 'mixed'` 为 False 导致 mixed 落入强流出分支。修复：显式 mixed 分支 → 中性
3. 修复后 dim4 phase 正确显示"unknown"（真实判定：票数不足）、fund_flow 显示"中性"（不再误报强流出），audit 一致

**B1（fina_indicator_cache 分库迁移不完整）——已解决**：
1. **根因**：356号分库迁移时 fina_indicator_cache 仅迁移 191 行（总库 22407 行），dim7 走分库路由读不到 roe
2. **修复**：新建 `backend/scripts/migrate_fina_indicator_to_shard.py`，按两库交集列（54 列，源库多 interestdebt/roce 2 列）INSERT OR IGNORE 分批迁移；修复脚本损坏的计数逻辑（引用未定义 fin_before）
3. **迁移结果**：financial_cache.fina_indicator_cache **191 → 22407 行**（与源库一致），roe 非空 **22155**
4. **验证**：`get_cached_fina_indicator("600519.SH")` 最新 **roe=16.75**（end_date 2026-06-30）✅；`_query_shard` 直查同样读到 ✅
5. 迁移前已备份两库至 `data/backup/fina_migrate_b1/`

**最终回归**：
- `sig_full_test.py` 重跑 → **OK: 40, WARN: 0, FAIL: 0** ✅
- `ruff check dim4_chip_fund_engine.py` → All checks passed ✅

**修改文件**：
- backend/app/opportunity_atlas/dimensions/dim4_chip_fund_engine.py（PHASE_UNKNOWN/PHASE_MAP 定义 + fund_flow mixed 分支）
- backend/scripts/migrate_fina_indicator_to_shard.py（新建迁移脚本 + 计数逻辑修复）
- data/duckdb/financial_cache.db（fina_indicator_cache 191→22407 行）
- data/backup/fina_migrate_b1/（迁移前备份）

**待办**：B3（sector_heat_cache=0）需在 data_daemon 运行环境定位写入失败点，另行排查。

---

### 14 - B3 根治方案：板块热度写盘独立于 RAW-2 触发

**用户输入**：重启 daemon 恢复正常运行，B3 的根治方案（让板块热度写盘独立于 RAW-2 触发、或修复写锁竞争）需要单独设计，请现在处理

**AI执行内容**（B3 根治：板块热度持久化从 RAW-2 内嵌块抽离为独立管道步骤 RAW-2B）：

**根因链（3 层）**：
1. **触发耦合**：`cache_sector_heat` 写盘块内嵌在 `_precompute_raw_features`（RAW-2）内，仅当 RAW-2 执行时运行；20260908 RAW-2 已于 10:27:44 由旧进程完成 → 重启后跳过 RAW → 写盘块永不执行
2. **静默吞错**：写盘块包在 `except Exception: pass` 内，失败无日志
3. **写锁竞争**：写盘用共享 `_ecm.conn` + `_execute`（无 busy_timeout）→ 与 daemon 主循环争写锁报 "database is locked"

**根治方案（data_daemon.py + enhanced_cache_manager.py）**：
1. **`_precompute_raw_features`**：移除 `cache_sector_heat` 写盘块，保留 `sr.compute_all_heat(all_data, indicator_ma_dict=indicator_ma_dict)`（仍需填充 `sr._cache['all_heat']` 供 `sr.evaluate` 使用）
2. **新增 `_precompute_sector_heat(codes)`**：加载日线 + indicator_ma_dict → `compute_all_heat` → 取最新 trade_date → 用**独立短连接**（`sqlite3.connect(_ecm.db_path, timeout=10)` + `PRAGMA busy_timeout=10000` + DELETE + executemany + 单次 commit）写 `sector_heat_cache`，日志"板块热度持久化完成: {len(rows)} 个行业"
3. **`_drive_pipeline`**：RAW 步骤 done 后、SIG 前，新增 `if status.get('RAW-2B', {}).get('status') != 'done': _run_pipeline_step(today, 'RAW-2B', _precompute_sector_heat, codes); return`
4. **`_is_pipeline_complete`**：step_id IN 列表加 'RAW-2B'，count 阈值 13→14
5. **`enhanced_cache_manager.py`**：`ensure_pipeline_steps` 列表加 `('RAW-2B', '板块热度持久化')`

**验证结果（端到端，重启后真实管道）**：
- 重启 daemon（PID 61898）加载新代码，消费完 sync_requests 积压后进入管道
- **RAW-2B 独立步骤执行成功**：`20260908|RAW-2B|板块热度持久化|done|13:56:44→13:57:36|OK (52.5s)`
- **sector_heat_cache 回填 66 行**，stat_date=2026-08-26（与 daily_cache 最新交易日一致），cached_at=2026-09-08 13:55:31（本次重启后写入）
- 写盘独立于 RAW-2 触发 + 独立短连接 busy_timeout=10s，无写锁竞争
- 隔离验证（verify_b3_fix.py，已删）：`_precompute_sector_heat` 写 66 行业、读回 66；语法 OK、ruff 新代码干净

**修改文件**：
- backend/data_daemon.py（_precompute_raw_features 移除写盘块 + 新增 _precompute_sector_heat + _drive_pipeline 加 RAW-2B 步骤 + _is_pipeline_complete 阈值 14）
- backend/app/data/enhanced_cache_manager.py（ensure_pipeline_steps 加 RAW-2B）

**待观察**：SIG 阶段 pending（历史上需数小时，属正常）；daemon 持续运行中。
