# 2026-09-13 沟通记录

## 001 — SIG 环节配置全面核查（只读）+ 430号核查报告存档 + 5 处自纠

**用户输入**：请全面检查系统中 SIG 的所有配置，看是否存在问题和错误，在对话框内进行说明，不要修改方案和代码；随后要求将上述情况编制为方案进行存档，完成后核对对话框内容，如有遗漏和错误请再次修正方案。

**AI 执行内容**（全程只读：代码零改动、配置零改动、既有方案文档零改动）：

### 核查范围与方法
- 范围：SIG 环节（策略信号层）的**全部配置来源** —— ① YAML（`status_engine.yaml` / `signal_registry.yaml` / `strategy_defaults.yaml`）；② 环境变量（`.env` / `.env.local` / `.env.example`）；③ 代码内硬编码常量（阈值/权重/系数）。
- 手段：逐文件全文审读 + 全仓库交叉引用检索（消费方/生产方计数）+ 隔离运行时探针（`importlib` 直载 `status_config.py`，规避 `app` 包 `dotenv` 依赖）+ `diff -u` 双份文件比对 + git 溯源。
- 数据快照：**2026-09-13**，核查时 `git status --porcelain config/ backend/config/` 为**空**。

### 核查结论（13 项，按等级）
| # | 问题 | 等级 |
|---|---|---|
| 1 | `status_config._CONFIG_DIR` 解析指向 `backend/config/`（docstring/注释却称"项目根 config/"），而 `signal_registry.yaml` **只存在于项目根** `config/` → `get_signal_registry()` **恒返回 `{}`**，注册表整份未加载 | **P0** |
| 2 | 承接 #1：`advice_builder.py:233-235` / `advice_engine.py:584-586` 的 `_ext_threshold` 兜底 `12` 写在 `except` 内，空注册表**不抛异常** → `_ext_threshold = 0` → 任何"现价 > 信号价"标的被误判"信号已延伸"，`enter`/`light` **被降级为 `wait`** | **P0** |
| 3 | 同名 `status_engine.yaml` **双份且分叉**：项目根版 1831B（缺 3 项）vs `backend/config` 版 2701B（实际被加载）；方案文档路径口径错位（**8 份**指向项目根、**1 份**418 指向 backend/config、2 份未写前缀） | P1 |
| 4 | backend 内并存**两套独立配置加载器**（`app/services/status_config.py` / `backend/config/__init__.py`），目录与缓存策略不同、无统一入口 | P1 |
| 5 | `status_engine.yaml` 存在**写入但零消费方**的死键：`dimension_weights`、`conflict_rules`、`consensus.color_high/color_low/neutral_is_zero`、`l0.hold_only_stages`、`l0.hard_risks` | P1 |
| 6 | `l0.emotion_position_cap` **有消费方（`advice_engine.py:56`）无生产方**（`status_engine._apply_l0` 不写入该键）→ 387号情绪-仓位联动上限**实际未生效** | P1 |
| 7 | 硬编码常量与 YAML **双份真相**：`arbiter.py:57/59` 阈值、`cross_validate.py:1299-1313` 软风险系数、`consensus_engine.STATE_WEIGHTS`、`weight_engine.STATIC_WEIGHTS`、`status_engine.MARKET_REGIME_WEIGHTS`（函数内局部矩阵）、dim5 阈值组 | P2 |
| 8 | 维度引擎 `dim1..dim8` 对 YAML/config 引用数 **= 0**，SIG 维度行为 100% 由代码常量驱动；`signal_registry.yaml` 的 `trigger`/`verify_days`/`verify_rule`/`lifecycle.*.days` **全无消费方** | P2 |
| 9 | 环境变量：`QUALITY_*`（4 键）在三份 env 中均未声明且被 2 处重复定义；`L4_EXTRA_VOTES` 全文件未声明；`CN_10Y_BOND_YIELD` 在 `.env`/`.env.example` 有、`.env.local` 无 | P2 |
| 10 | **IC 权重链路断裂**：`data/ic_weights.json` 实际存在（97B），`potential_engine.IC_WEIGHTS_FILE = None` 且全仓库无注入代码 → 恒用默认权重、保存恒 no-op；与 **399号 §902 / 2026-09-03 沟通记录 §46**"IC 死代码已全部删除"记载**直接矛盾**（相关符号均仍在） | P2 |
| 11 | 文档漂移：`invalidate_config_cache()` 被 **3 份文档共 4 处**声称"已存在/已新增"（`JUD环节代码实审报告.md:480`、`393号:342`、`26-09-17沟通纪要.md:31`/`:49`），代码中**不存在** | P2 |
| 12 | 配置加载**零测试覆盖**（`tests/` 检索 `status_config`/`signal_registry`/`_CONFIG_DIR` 命中 = 0）→ 本类缺陷无回归防护 | P2 |
| 13 | 收尾项：`scripts/calibrate_signal_thresholds.py` 写入**孤立**的项目根 `config/signal_registry.yaml`；`.env.bak.331` 残留；三份 env 键集不一致 | P3 |

**一句话结论**：SIG 名义上存在两套配置体系，实际"**注册表整份失效、聚合配置只生效 3 个键、情绪仓位上限未落地、IC 权重链路断裂**"，且因缺路径断言与配置加载测试而长期静默 —— 表象为"配置驱动"，实态为"**代码常量驱动**"。

### 存档与自纠
产出 **`002-方案存档/430-SIG环节配置全面核查报告.md`**（v1.0，560 行，13 项结论 + §8 证据索引 + §9 建议处置顺序 10 条）。编号说明：按规则应为 429，但 **429 已被 425/426 号引用占用**（无独立文档），故取 430 并保留 429 既有引用含义。

写入后以对话框已交付结论为基准**逐项回核**，发现 **5 处表述不准确（均属"过度概括/细节失真"，不影响结论定性）** 并就地修正：
1. 文档路径口径"全部 11 份"→ 实测 **8 份**指向项目根 + **1 份**（418）指向 backend/config + **2 份**未写前缀；
2. `invalidate_config_cache` 文档面"全仓库仅 1 处"→ 实为 **3 份文件共 4 处**；
3. `hard_risks` 生产处函数名 `arbiter._evaluate_gate()` → 实为 **`arbiter._derive_gate()`（L125-150）**；
4. 硬编码清单细节：`chanlun_dataclass` **不存在**（已改为实测常量表）；`MARKET_REGIME_WEIGHTS` 由"类级字典"更正为**函数内局部矩阵**；
5. 建议层 `_ext_threshold` 行号区间 → 统一为 **`575-587`**。

回核同时补充 3 条关联证据（`26-09-17沟通纪要.md:31`/`:49` 声称"✅ 已新增 `invalidate_config_cache()`"、同文档 `:11` 与 `:26` 口径并存、418号 `:23` 属"写法正确不同类"）。**结论：核对未发现遗漏的核查项，亦未发现结论性错误。**

**产出**：`002-方案存档/430-SIG环节配置全面核查报告.md`（v1.0）。零代码改动、零配置改动、零既有文档改动。

---

## 002 — 430号 P0 实施（Serena 执行）：注册表归位 + 空注册表兜底

**用户输入**：请调用 serena mcp 工具执行 430号方案。

**执行范围确认**（对话框四选一）：
- **执行范围 = 仅 P0**（第 1、2 条）；
- **yaml 权威位置 = 保留 `backend/config` 版**（不改动 390 号 `jud_engine_version: "v390"` 所在文件，避免静默回退 legacy）；
- **IC 链路 = 接上链路**（决策记录，**列入本轮范围外**，未实施）。

### 实施内容（3 个文件）
1. **P0-1 注册表归位**：`config/signal_registry.yaml` → `backend/config/signal_registry.yaml`（`git mv`，已 staged rename，**内容零改动**）。
   - 关键判断：`_CONFIG_DIR` **逻辑本就正确**（解析结果 = `backend/config`，与 `backend/config/__init__.py`、`config_manager.py:28` 两个模块一致）；真正的缺陷是**注册表放错了目录**，而非解析层多上一级。
2. **P0-1 附带 doc 订正**：`backend/app/services/status_config.py` 补订 2 处误导性 docstring/注释（原称"配置位于项目根 config/，与 .env 同级"）→ 改为"配置位于 `backend/config/` 目录（与本模块同属 backend 包）"。**逻辑零改动**。
3. **P0-2 空注册表兜底**：`advice_engine.py::build_operation_advice`（L584 起）与 `advice_builder.py::build_operation_advice`（L233 起）各新增 2 行，把兜底值从 `except` 分支移出为**显式判空**：
   ```python
   if _ext_threshold <= 0:  # 空注册表不抛异常，同走兜底默认值
       _ext_threshold = 12
   ```
   - 缺陷机理：注册表为空 → `_max_ext_pct = 0` → `0 <= 1` 走 `*100` 分支得 `0` → `_dist > 0` 恒真 → 所有 `enter/light` 被误降级 `wait`。`initial.dist_pct = 0.05` 恰与代码默认值相同，故该缺陷此前不可见。

### 验证证据
| 检查 | 结果 |
|---|---|
| 运行时探针（`_CONFIG_DIR` / `jud_engine_version` / registry 条数 / `_ext_threshold` / 空表兜底） | `backend/config` · `v390` · 5 · `12.0` · `12` |
| `ruff check` 三个改动文件 | All checks passed! |
| 沙盒定向 5 文件（含 test_390 / test_418） | **59 passed in 0.10s**，EXIT=0 |
| 全量沙盒套件（改动后） | 34 failed, 741 passed, 3 skipped, 9 xfailed |
| 全量沙盒套件 **A/B 基线**（`git stash` 撤改动后重跑） | 34 failed, 741 passed, 3 skipped, 9 xfailed |
| 失败集 diff（基线 vs 改动后） | **IDENTICAL FAILURE SETS** —— 零回归 |
| 恢复改动（`git stash pop stash@{0}`，pre-existing stash@{1..3} 未触碰） | 两处 `@@ -584,6 +584,8 @@` / `@@ -233,6 +233,8 @@` hunk 原样恢复 |

### 两点环境事实（非本次改动引入）
1. **测试"卡死"根因已定位**：faulthandler 栈显示阻塞点为 `compute_advice`（L24-154）的 `DataManager()` → ECM `_init_tables()`；`.env:29` 的 `DATA_DIR` 指向**生产数据目录**，测试因此去连正在运行的 `data_daemon.py`（pid 1151）持有的 live DB，`PRAGMA busy_timeout=30000` 使每条 `CREATE TABLE IF NOT EXISTS` 各等约 30s。`compute_advice` 的 `git diff` 为空，与本次改动无关；按 AGENTS.md 沙盒规程（`TEST_DATA_DIR=...`）跑即为 0.03s 通过。
2. **34 个全量失败全部为空沙盒缺数据**（`no such table` / `table_exists(...)` False / `assert ''`），A/B 完全一致；另有 **13 个 pre-existing lint 错误**在未触碰文件中（`akshare_collector.py:270`、`mootdx_collector.py:33`、`condition_evaluator.py:19/341/757`、`test_426_p0_correctness.py:295`、`test_428_gap_fixes.py` 7 处），按"仅 P0"边界未处理。

### 已记录但未实施（待确认）
- 第 3 条：yaml 唯一权威位置 = **保留 `backend/config`**（已定案；项目根 `config/status_engine.yaml` 仍为孤立副本，未清理）；
- 第 6 条：**IC 链路接上**（已决策，需在 `data_daemon` 注入 `IC_WEIGHTS_FILE = <DATA_DIR>/ic_weights.json`）；
- 430号结尾建议"**另立编号文档承接实施记录**"。

**产出**：`backend/config/signal_registry.yaml`（归位）、`status_config.py`（doc 订正）、`advice_engine.py` / `advice_builder.py`（各 +2 行）。P1/P2/P3 项未触碰。

---

## 003 — 430号 §9 第 3/4/5/6 条实施（Serena 执行）：yaml 唯一权威位置 + 情绪仓位上限落地 + 死键接线/清理 + IC 读侧链路

**用户输入**：继续实施 430号 §9 第 3/4/5/6 条。

**对话框四项决策**（均取推荐项）：
1. **根副本处置 = 删除（`git rm`）** 项目根 `config/status_engine.yaml`（1831B 死副本，缺 3 项）；
2. **文档口径 = 改纯路径 7 份**（336/337/338/342/352/387/393），**391 号留待第 9 条**（其 4 处引用与"文件不存在/新管线从未激活"的错误结论纠缠，需随结论一并订正）；
3. **死键处置 = 接线 3 + 移除 4**；
4. **IC 链路 = 仅注入 `IC_WEIGHTS_FILE`（读侧生效）**，不做写侧调度。

### 实施内容（按 §9 条目）
- **第 3 条（唯一权威位置 + 文档口径）**：`git rm config/status_engine.yaml` → 权威位置定于 **`backend/config/`**（与 P0 归位的 `signal_registry.yaml` 同目录，亦与 `_CONFIG_DIR` 解析结果一致）；7 份文档共 **11 处** `config/status_engine.yaml` → `backend/config/status_engine.yaml`（336×3、387×3、337/338/342/352/393 各 1，含 336 号 yaml 代码块内的注释行）。**391 号（4 处）与 430 号（14 处，属历史证据）刻意保留**。前置核查：全仓库无任何非 md 文件读取项目根该路径（仅 docstring/注释提及通用名，另有 `003-代码备份/` 下无关备份）。
- **第 5 条（死键接线 3 + 移除 4）**：`backend/config/status_engine.yaml` 重写 —— **移除** `dimension_weights` 全块、`consensus.color_high` / `color_low` / `neutral_is_zero`；`version: 1 → 2`（遵该文件自身"修改后 version+1"约定；已核实**全仓库无代码读 `version` 键**，bump 安全）；`l0.hard_risks: [regulatory, st, delist] → [regulatory]`（对齐 `_apply_l0` 实际语义：L0a 仅监管类不可逆项硬否决）；`conflict_rules` 注释改为"（336号 §4；由 `arbiter.arbitrate` 按名消费）"。**接线**：
  - `status_engine._apply_l0`：新增 `_l0_cfg = self.cfg.get('l0', {}) or {}`；L0a 按 `l0.hard_risks` 名单判定（命中即 `hard_veto=True`，`hard_reason` 区分 `regulatory`/其它，默认名单仍 `['regulatory']`）；L0b 读 `l0.soft_risk_coeff`（5 分支判定与消息**逐字节不变**）；L0c `hold_only` 读 `l0.hold_only_stages`。
  - `arbiter.py`：新增 `_DEFAULT_CONFLICT_RULES`（6 条全量）与 `_load_conflict_rules()`（读 yaml `conflict_rules`；`enabled=false`→空集=关闭冲突暴露；`rules` 缺失→全量默认；异常→全量默认），`arbitrate()` 的 7 个冲突判定块改为**按名 gating**（`'xxx' in _cr`），7 条 append 文案逐字节不变 → **出厂 yaml（enabled + 全 6 条）下 `conflict_evidence` 逐位一致**，仅当运维改 yaml 才改变行为。P0–P7 优先级逻辑未触碰。
- **第 4 条（补 `emotion_position_cap` 生产写入）**：`_apply_l0` 新增 **L0b2** 块 —— 读 yaml `l0.emotion_position_cap`，按 `tags['emotion_phase']` 取上限并写入 `l0['emotion_position_cap']`（`climax` 有意回落 `normal`；yaml 无该块时不写键，保持旧行为），从而让 `advice_engine.py:56` 的消费方首次真正生效（387号 §5.4）。
- **第 6 条（IC 读侧链路打通）**：`data_daemon.main()` 在分库管理器就绪后、426 号自检块之前注入 `potential_engine.IC_WEIGHTS_FILE = <DATA_DIR>/ic_weights.json`（try/except + 启动日志）。**同步订正 2 处"已删除"错误表述**：`399-dim梳理产出报告.md` §902 表格行、`001-沟通记录/2026-09-03.md` §008（`recompute/load/save_ic_weights` + `IC_WEIGHTS_FILE` 实际仍在 `potential_engine.py` 且零调用点；`compute_fund_strength` 为活代码，原记录误列为死函数）。

### 验证证据
| 检查 | 结果 |
|---|---|
| 运行时探针（真实加载器 + `StatusEngine.__new__` 直调 `_apply_l0`，不触 DB） | `version=2`、`dimension_weights` 已移除、`consensus` 仅剩 `enter_threshold`/`bearish_strong`、`hard_risks=['regulatory']`、`hold_only_stages=['已延伸']`、conflict 规则 **6** 条 |
| 情绪仓位上限（第 4 条） | `positive→0.8`、`climax→0.6`（回落 normal）；硬否决 `regulatory→True` / `earnings→False`；hold `已延伸→True` / `中期→False` |
| IC 链路（第 6 条） | `IC_WEIGHTS_FILE=/…/data/ic_weights.json`、`exists=True`、`load_ic_weights()` 返回 6 维权重（读侧真实生效） |
| `ruff check`（本次改动文件） | `status_engine.py` / `arbiter.py` **All checks passed!**；`data_daemon.py` 插入区间（L6286-6295）零告警（该文件 32 个 pre-existing 错误本不在 `make lint` 范围内） |
| 沙盒定向回归（改动后） | `test_390_l6_advice_engine` / `test_418_jud_v390` / `test_322_s1_advice` / `test_323_s6_fields` / `test_321_arbiter` → **59 passed in 0.12s**（与改动前基线同数） |
| 沙盒扩展批（6 文件：integration/valuation/gate 消费者/s7/s6/fix_315） | **38 passed / 5 failed**；5 项失败**全部为空沙盒缺数据**（`no such table: moneyflow_cache` / `opportunity_tags_cache`、`assert rows`，对应测试文档明写"用真实数据""真实股票 300705.SZ"验证），与本次改动无因果 |
| `make lint`（app/ + tests/） | **13 errors**，与改动前同构（`akshare_collector.py:270`、`mootdx_collector.py:33`、`condition_evaluator.py:19/341/757`、`test_426_p0_correctness.py:295`、`test_428_gap_fixes.py` 6 处），**改动文件零新增** |
| `git status` | `D config/status_engine.yaml`（已 staged 删除） |

### 行为变更披露（第 4 条，需知悉）
`emotion_position_cap` 由"从不写入"变为"**每只股票都写入**"，`advice_engine` §8.2 Step 3 自此生效。因基础仓位上限为 0.6 且 `position_coeff ≤ 1.0`，**`normal`(0.6) / `recovery`(0.6) / `positive`(0.8) 三个上限永不可触达**，实际压缩仅发生在 `ebb`(0.3) 与 `ice`(0.1) 阶段 —— 即 387号 §5.4 的**预期激活**，属温和的生产口径变化。现有测试均在 l0 输入中自造 `emotion_position_cap`（多为 `None`），故不覆盖这条新写入路径。

### 遗留（未实施，越界项）
- §9 **第 7/8 条**（P2：硬编码常量收敛、配置加载测试）与 **第 9/10 条**（P3：含 391 号错误结论订正、`scripts/calibrate_signal_thresholds.py` 仍指向项目根 `signal_registry.yaml`）**未触碰**；
- 小残留：`338-…md:63` 同行仍为 `config/signal_registry.yaml` 口径；`342-…md:185` 仍提及已移除的 `color_low`；`backend/docs/JUD环节代码实审报告.md:709` 口径与其自身 `:4/:439` 自相矛盾；
- IC **写侧**（`recompute_ic_weights` / `save_ic_weights`）仍无调用点 → `data/ic_weights.json` 维持与初始 `DIM_WEIGHTS` 相同的静态快照（读侧已打通，写侧待接入月度滚动重估调度）。

**产出**：`backend/config/status_engine.yaml`（重写至 v2）、`backend/app/opportunity_atlas/status_engine.py`（`_apply_l0` 重写）、`backend/app/opportunity_atlas/arbiter.py`（+`_load_conflict_rules` 与按名 gating）、`backend/data_daemon.py`（+8 行 IC 注入）、`config/status_engine.yaml`（删除）、7 份方案文档共 11 处路径口径、2 处文档订正。§9 第 3/4/5/6 条**全部落地**。

---

## 004 — 430号遗留项归集为 431 号存档（零改动）：全仓复扫 + 五类残留清单

**用户输入**：遗留问题的具体情况是什么？本次是否可以顺带进行修复？

**AI 首次交付**：逐条列出 003 节"遗留"三项之外的全部残留（文件:行 + 原文引用），并给出 A/B/C/D 风险分级与 3 个修复范围选项，请用户选择。

**用户决策**：**先编辑为新的方案进行存档，后续再讨论具体的解决方案** → 本轮**不修复任何残留**，改为编制新编号方案文档承接核查结论。

### 复扫方法（全只读）
- 全仓正则：`config/status_engine\.yaml`（69 命中）、`config/signal_registry\.yaml`（17 命中）、`color_high|color_low|neutral_is_zero|dimension_weights`（29 命中）；对照扫描 `config/strategy_defaults\.yaml`（无残留）。
- 存在性验证：`ls -1 .env*`、`ls -1 config/`、`ls -1 backend/tests/test_390_integration.py`、`git ls-files docs/compose/`。
- 逐行读取：`scripts/calibrate_signal_thresholds.py`（全文）、`backend/docs/JUD环节代码实审报告.md`（头/§3.1/尾）、`391-…md`（结论段）。

### 五类残留（14 项）
| 类 | 条目 | 要点 |
|:--:|:--:|---|
| A（7） | A1–A7 | 纯路径/失效键文案：`338:64`（同行 signal_registry 漏改）、`342:186`（引用已删键 `color_low`）、`334:147`/`361:49`/`364g:92`（项目根 signal_registry 口径）、`calibrate_signal_thresholds.py:17,:91`、`336:137-140/:158` 与 `352:126`（草案含已删键 `color_*`/`dimension_weights`） |
| B（1） | B1 | `backend/docs/JUD环节代码实审报告.md`：`:709` 路径口径与同文 `:5`/`:441` 自相矛盾；`§3.1` 内嵌 yaml 快照失真 4 处（version 1、color_*、dimension_weights、hard_risks）；`(56行)` 实为 **41** 行；`:480` 文档化不存在的 `invalidate_config_cache()` → 建议加「订正注记」而非重写审计快照 |
| C（2） | C1/C2 | **430 号扫描盲区**：`docs/compose/{plans,reports}/方案390集成与旧组件清理.md` 共 **11 处** 项目根口径，其中 `plans:64` `cat config/status_engine.yaml \| head -20`、`plans:91`/`:835` `git add config/status_engine.yaml` **三条命令现已必然失败** |
| D（1） | D1 | `391-…md`：4 处路径 + 7 处**结论与事实相反**（yaml「不存在」、`jud_engine_version`「不存在」、新管线「永远不会被调用」、`test_390_integration.py`「❌ 不存在」——该文件实存 6984B/9月6日）；与同文 `:106-108` 自相矛盾 |
| E（3） | E1–E3 | 真功能/破坏性：IC **写侧**零调用点（`recompute_ic_weights`/`save_ic_weights`，属第 6 条"仅读侧"的有意残留）；`.env.bak.331`（1241B）残留；三份 env 键集漂移（`QUALITY_*` 4 键 / `L4_EXTRA_VOTES` / `CN_10Y_BOND_YIELD`） |

### 两处 430 号自身口径订正（复扫发现）
1. **`calibrate_signal_thresholds.py` 实为 2 处**（非 430 所记 4 处）：仅 `:17`（docstring）与 `:91`（print）带项目根路径前缀；`:33`/`:77` 仅裸文件名。且该脚本**全程只读**（只 print 结论，从不写 yaml），430 号"仍写项目根 signal_registry.yaml"的措辞易被误解。
2. **`docs/compose/` 属扫描盲区**：430 号 §233 的"8 份文档"统计只覆盖 `002-方案存档/`，未含 `docs/compose/`（经 `git ls-files` 确认受版本管理）。

### 与 430 §9 的映射
- 第 9 条（P3）→ **D1**；第 10 条（P3）→ **A6 / E2 / E3**；
- 第 3、5 条的"未扫全"残留 → **A1/A3/A4/A5/C1/C2**、**A2/A7**；
- 第 6 条的"仅读侧"决策 → **E1**（写侧缺口）；
- **§9 未列**：**B1**（430 §7.3 已识别但未进处置清单）、**C1/C2**（扫描盲区）；
- 第 7、8 条（P2 硬编码收敛、配置加载测试）属**未启动的规划项**，不在本清单内。

### 建议处置顺序（本次均未执行）
1. P3-low 纯文案：A3/A4/A5/A1/A6 → A2/A7 → C2 → C1（C1 中三条命令优先）；
2. P3-med 归档可信度：B1（`:709` + §3.1 订正注记）→ D1（勘误头 + 4 路径 + `:118` 订正）；
3. 需单独决策：E1（新功能）、E2（破坏性删除）、E3（需先定以哪份 env 为准）。

### 明确保留（不改）
430 号报告本体 14 处（历史证据）、`418-…md:23/:239`（已被 430 §436 判定"描述准确，已被超越"）、全部 `001-沟通记录/**`、`003-代码备份/**`、`backend/$TMPDIR/`。

**产出**：`002-方案存档/431-430号遗留项核查清单与处置建议.md`（v1.0，首版核查存档）；`002-方案存档/001-目录索引.md` 新增 §十四 并登记 430/431（同时说明 407-429 号索引滞后待补齐）。**零代码改动、零配置改动、零既有文档内容改动**（仅新增 431 号 + 索引登记）。

---

## 005 — 430号"未完成面"复核（只读）+ 并入 431 号清单（v1.0 → v1.1，仍零修复）

**用户输入**：先继续看一下430还有哪些未完成。

**AI 执行内容**（前半程全程只读，未对 430/431 写入）：以 430 报告 §0 的 13 项结论、§9 的 10 条建议处置顺序与 431 v1.0 为基准，逐条复核落地状态，给出结论：**430 的"未完成面"由两块互不重叠的子集组成**。

### 一、§9 第 7/8/9/10 条——从未启动
| 条目 | 等级 | 内容 | 状态 |
|---|---|---|---|
| 第 7 条 | P2 | 硬编码常量 5 组收敛：`cross_validate.py:1295-1316`（`0.3/0.5/0.7/0.8`）、`weight_engine.py:14` `STATIC_WEIGHTS`、`weight_engine.py:22` `EMOTION_MULTIPLIERS`、`status_engine.py:567` 函数内 `MARKET_REGIME_WEIGHTS`、`QUALITY_*` 4 常量双份（`valuation_estimator.py:32-35` + `dim7_valuation_engine.py:35-38`），另含 dim5 阈值组 | ❌ 未启动 |
| 第 8 条 | P2 | 配置加载零单测——`backend/tests/` 对 `status_config`/`signal_registry`/`_CONFIG_DIR`/`get_status_engine_config` 命中 **0** | ❌ 未启动 |
| 第 9 条 | P3 | 文档漂移收尾：391 号 4 处路径 + 7 处结论与事实相反、`test_390_integration.py` 实存（6984B·9月6日） | 仅 431 D1 归集，未修 |
| 第 10 条 | P3 | `calibrate_signal_thresholds.py` 项目根口径、`.env.bak.331` 残留、三份 env 键集漂移 | 仅 431 A6/E2/E3 归集，未修 |

### 二、§9 从未列入的 4 项发现——431 v1.0 §五 只记了"§9 未列：B1、C1/C2"，故这 4 项在此前**任何文档中均无承接**
| 项 | 等级 | 位置与事实 |
|---|---|---|
| 430 问题 #4 | **P1**（430 唯一 P1） | 两套配置加载器并存：`backend/app/services/status_config.py:18/27/32`（`load_yaml`/`get_status_engine_config`/`get_signal_registry`，`@lru_cache`）vs `backend/config/__init__.py:7/22`（`load_strategy_config`/`get_strategy_config`），后者唯一消费方 `backend/app/engine/pipeline.py:97-98` |
| 430 问题 #8 | P2 | 维度引擎零配置引用：`app/opportunity_atlas/dimensions/` 对 YAML/注册表引用数 **0**；注册表 `verify_days`/`verify_rule` 消费方 **0** → dim1–8 实由代码常量驱动 |
| §7.4-3 | P3 | `arbiter.py:72-73`（`:96-97` 同类）`except Exception: pass` 静默吞异常，阈值读取失败静默退回硬编码 `0.67` |
| §7.4-4 | P3 | `dim6_risk_engine.py:269` 自注 T42 循环依赖致 `dims['l0']` 恒空 → `:271`/`:324` 的 `if l0.get('hard_veto')` 为**不可达死码** |

**小结**：430 执行后仅 P0（§9 第 1/2 条）与 §9 第 3/4/5/6 条落地；第 7/8/9/10 条未启动，另有 4 项发现连 §9 都未列入。**本轮零改动**（未改代码、配置或既有文档，仅只读核对）。

**用户决策**（对话框选项）：**处置去向 = 并入 431 号清单** → 使 431 号成为 430 号收尾的**唯一入口**；选项原文含"本轮仍不修任何东西"。

### 三、431 号升 v1.1（A–E 五类 → A–G 七类，仍零修复）
把上述两块无承接的未完成项补录入册：

- **F 类｜430 §9 从未列入的 4 项发现**（v1.1 补录）：F1 = 问题 #4 两套加载器（**430 唯一 P1**）、F2 = 问题 #8 维度引擎零配置、F3 = §7.4-3 静默吞异常、F4 = §7.4-4 dim6 死码；
- **G 类｜§9 第 7/8 条＝未启动的规划项**（非缺陷）：G1 = 硬编码常量 5 组未收敛、G2 = 配置加载零单测。

**8 处编辑**：①frontmatter `version: v1.1（首版核查存档 + 补录 §9 未列入发现与未启动规划项）`；②卷首新增 **v1.1 增补** blockquote（注明"本文档同时作为 **430 号收尾的唯一入口**"，仍**不实施任何修复**）；③§一 范围由"五类"改为**七类 A–G**（新增 F/G 两行 + 范围边界重写为 3 条）；④§三 结论汇总表追加 **F1–G2** 行；⑤§四 逐项详情新增 **F 类**（F1 loader 对照表 + F3 代码块）与 **G 类**（G1 五行常量表）两节；⑥§五 与 430 §9 的映射：第 7 条→**G1**、第 8 条→**G2**、"§9 未列入"行补 **F1/F2/F3/F4**；⑦§六 分级处置新增 `P3-low｜F3（低风险小改）` 与 `需单独决策｜F1/F2/F4 + G1/G2`；⑧§八 证据索引新增 7 条 v1.1 取证命令 + footer 更新。

**两条关联性结论**（写入 §六）：**F2 与 G1 同源**（不先定"哪些字段真接线"，收敛后仍是死配置）；**G2 是 F1 合并加载器的前置回归网**；**F3 是唯一的低风险小改**（其余均属重构/功能/需定口径）。

**同步登记**：`002-方案存档/001-目录索引.md` §十四 431 行改为 `📋 核查存档（v1.1，待确认）`，注明"**430 号收尾的唯一入口**"并列出 F/G 两类。

**验证**：431 号 **447 行 / 33644 B**；全文复核仅第 33 行保留"五类"（有意，指"A–E 为首版五类"），F/G 行与两节均在、无残留"不在本文档范围"表述；`git status --short` 显示本轮仅 `001-目录索引.md` 变动叠加在既有（430 §9 第 3 条等）改动之上。

**本轮零修复**：未改任何代码、配置或既有正文；除 431 号本体与目录索引 §十四 两处外无其他改动。**F1–F4、G1/G2、A–E 各项全部未修**，待后续按性质分批推进。

**产出**：`002-方案存档/431-430号遗留项核查清单与处置建议.md`（v1.0 → **v1.1**，A–G 七类）；`002-方案存档/001-目录索引.md` §十四 431 行更新。

---

## 006 — 431号批次1 落地（A 类 + C 类纯文案）：10 个文件 21 处

**用户输入**：现在开始处置 431 号清单。

**批次决策**（对话框选项）：**起始批次 = 批次1｜A 类 + C 类（纯文案）** —— 只做纯路径/失效键文案（A1–A7 + C1/C2），**零代码逻辑、零配置改动，风险最低**；选项原文含"优先 C1 中三条必失败命令"。

### 实施内容（10 个文件 21 处，全部为文案订正）

| 类 | 位置 | 订正内容 | 处数 |
|:--:|------|----------|:--:|
| **C1** | `docs/compose/plans/2026-09-17-方案390集成与旧组件清理.md` | `config/status_engine.yaml` → `backend/config/status_engine.yaml`，含 **3 条现已必然失败的命令**：`:64` `cat config/status_engine.yaml \| head -20`、`:91`/`:835` `git add config/status_engine.yaml` | 8 |
| **C2** | `docs/compose/reports/方案390集成与旧组件清理.md` | `:36`/`:49`/`:55` 路径口径改 backend；其中 `:49` 表格行**行数 56 → 41** | 3 |
| **A1** | `338-332总纲整体实施计划….md:63` | 同行漏改：`config/status_engine.yaml` + `config/signal_registry.yaml` 一并改 backend 口径 | 1 |
| **A2** | `342-全链路核查方法论方案….md:186` | 已移除键 `color_low` 的路径引述改为现行 `backend/config/status_engine.yaml`（仅剩 `enter_threshold`/`bearish_strong`） | 1 |
| **A3** | `334-L1维度判定细则与信号注册表….md:146` | 目录树标注 `config/signal_registry.yaml` → backend 口径 | 1 |
| **A4** | `361-【实现方案】七维现状描述系统实现.md:49` | 表格内路径 → backend 口径 | 1 |
| **A5** | `364g-Phase7-第1维信号确认7类分类.md:92` | 标题内路径 → backend 口径；**行区间 `:1-41` 经核验本就正确**（yaml 实测 41 行），保留不改 | 1 |
| **A6** | `scripts/calibrate_signal_thresholds.py:17,:91` | docstring/print 中的路径 → backend 口径；`:33`/`:77` 仅裸文件名，**未动** | 2 |
| **A7** | `336-L2聚合与闸门收敛细则….md:157/:160`、`352-332-337架构落地修订方案….md:126` | **标注而非重写**：草案代码块原样保留，加订正块/括号注明"`color_high`/`color_low`/`neutral_is_zero` 与整个 `dimension_weights` 块已由 430 号 §9 第 5 条移除，勿照抄" | 3 |

边界遵守：**未触碰 431 §七 保留清单**（430 号本体 14 处、`418-…md:23/:239`、全部 `001-沟通记录/**`、`003-代码备份/**`、`backend/$TMPDIR/`）；A7 严格"只标注不重写"，草案快照原样保留。

### 验证证据

| 检查 | 结果 |
|---|---|
| `docs/compose/{plans,reports}` 残留扫描（`grep -v backend/config/`） | **0 处残留**（11 处全部改到位） |
| `002-方案存档/` 残留扫描 | 仅剩 **430 / 391 / 431** 三份 —— 430、431 属 §七 保留项，391 为 D1 待办 |
| `scripts/calibrate_signal_thresholds.py` | `:17`/`:91` 已改 backend 口径，`:33`/`:77` 保持裸文件名；脚本**仍全程只读**（从不写 yaml） |
| `py_compile`（改动脚本） | 通过 |

**产出**：10 个文件 21 处文案订正，**零代码逻辑、零配置改动**（脚本仅改 docstring/print 两处字符串）。批次2（B1 + D1）待续。

---

## 007 — 431号批次2 落地（B1 + D1 归档可信度）：2 份文档 15 处

**用户输入**：**下一批次 = 批次2｜B1 + D1（归档可信度）**。

**批次决策**（对话框选项）：按 431 §六 顺序执行第 5、6 项 —— 均为**归档文档可信度**订正（路径口径 + 失真标注），**零代码逻辑、零配置改动**。

### 实施内容（2 份文档 15 处）

**B1 — `backend/docs/JUD环节代码实审报告.md`（3 处）**

| 位置 | 订正内容 |
|---|---|
| `:439` | 文件头行数 `(56行)` → **`(41行)`** |
| `:471` | §3.1 yaml 快照围栏与 `### 3.2 配置读取逻辑` 之间新增「**⚠️ 订正注记**」：注明快照为审计时原样留存、**勿照抄**，并列出与现行 v2 的 **4 处不一致**（① `version: 1`→`2`；② `dimension_weights` 整块已移除；③ `consensus.color_high`/`color_low`/`neutral_is_zero` 已移除；④ `l0.hard_risks: [regulatory, st, delist]`→`[regulatory]`），另附「§3.2 所称 `invalidate_config_cache()` 在代码中并不存在（全仓库 0 处实现），该缓存实由 `lru_cache` 承载」 |
| `:711` | 附表行 →  `\| \`backend/config/status_engine.yaml\` \| 41 \| 全量配置 \|` |

快照**本体原样保留**（`:441-442` 的 `version: 1` / `jud_engine_version: "v390"` 未改）—— 431 §四-B1 明定「加订正注记而非重写审计快照」。

**D1 — `002-方案存档/391-390号方案落地实施全面核查报告.md`（12 处）**

| # | 位置 | 订正内容 |
|:--:|---|---|
| 1 | `:13` | 卷首新增「**⚠️ 勘误头**」：声明第一（T1 行）/ 2.1 / 2.8 / 2.9 / 2.10 / 五 / 六 节中「`status_engine.yaml` 缺失 → `jud_engine_version` 恒取 `'legacy'` → **新管线在生产环境中从未被执行**」的结论**已被证伪**；成因＝核查以**项目根 `config/`** 为口径而 `status_config._CONFIG_DIR` 实际解析至 `backend/config/`；并订正「`test_390_integration.py` 不存在」（**实存 192 行 / 6984 B，2026-09-06**）；同时声明**报告主体保留为历史核查记录** |
| 2–5 | `:39`/`:126`/`:389`/`:416` | 4 处路径口径 `config/status_engine.yaml` → `backend/config/status_engine.yaml` |
| 6 | `:118`（改后行号 `:120`） | 集成测试行 `\| \`test_390_integration.py\` \| ❌ 不存在 \|` → `**✅ 存在**（192 行 / 6984 B，2026-09-06）—— 原记"❌ 不存在"有误` |
| 7–14 | `:27`/`:39`/`:126`/`:133`/`:134`/`:389`/`:406`/`:416` | 8 处受该错误结论污染的判定行**就地加注** ` 🚫 **见卷首勘误头**`（**加性标记，不重写原判定文字**） |

### 验证证据

| 检查 | 结果 |
|---|---|
| 391 残留裸路径扫描（`grep -n "[^k]config/status_engine\.yaml"` 排除标记行） | **0 处残留** |
| 391 标记计数（`grep -c '见卷首勘误头'`） | **9** = 8 处行内标记 + 勘误头自身 1 处（自洽） |
| JUD 文档残留扫描 | **0 处残留** |
| 行数 | JUD **712** 行、391 **427** 行 |
| JUD yaml 快照完整性 | `version: 1` / `jud_engine_version: "v390"` **原样保留**（未重写历史快照） |
| `git status --short` | 本轮仅上述 2 份文档 ` M`，无其他改动 |

### 需知悉的三点

1. **轻度超出 431 §六 字面三项**：431 §六 D1 仅列「勘误头 + 4 处路径 + 订正 `:118`」，本轮**另行加注 8 处行内标记**（否则读者仍会读到与勘误头矛盾的判定行）。该处置为**加性**、未改动任何历史结论文字，已向用户披露并说明**如需严格遵循字面范围可回退**。
2. **同类新发现（未擅自修改）**：JUD 报告 §6.2 `:679`（`consensus.enter_threshold` 引用已移除项）与 `:680`（`dimension_weights.*` 引用已移除键 + 裸 `status_engine.yaml` 无 backend 前缀）属同类失真，但**不在 431 §四-B1 列举范围**内 → 建议并入 **431 v1.2 补录**，本轮未动。
3. **刻意保留未改**：JUD `:679`/`:680`、JUD §3.2 关于「`dimension_weights` 在 390 新管线中未直接消费」的 bullet、`002-方案存档/370-JUD环节独立化实施计划.md:203` 的 `_dimension_weights`（**不同 token**，与 A7 所指 `dimension_weights` 非同一串）。

**产出**：`backend/docs/JUD环节代码实审报告.md`（3 处）、`002-方案存档/391-390号方案落地实施全面核查报告.md`（12 处）。**零代码逻辑、零配置改动**。批次3（F3）待续。

---

## 008 — 431号批次3 落地（F3 静默吞异常）：1 个文件 3 处，零行为变更

**用户输入**：**下一批次 = 批次3｜F3 静默吞异常（推荐）**；**沟通记录 = 补记（推荐）**。

**批次决策**（对话框选项）：按 431 §六 第 ③ 组执行唯一条目 —— 430 §7.4-3 的 `except Exception: pass` 静默吞异常，处置＝**改为显式日志（低风险小改），不动阈值逻辑**。

### 实施内容（`backend/app/opportunity_atlas/arbiter.py`，3 处）

| # | 位置 | 订正内容 |
|:--:|---|---|
| 1 | 模块头（`from __future__ import annotations` 之后） | 新增 `import logging` + `logger = logging.getLogger(__name__)`。此前本模块是 `app/opportunity_atlas/` 包内**唯一无 logger 的模块**（`advice_engine.py:11-13` / `conflict_matrix.py:20-21` / `reliability_assessor.py:24-25` / `dim_adapter.py:11-12` 等均同惯例），本次对齐 |
| 2 | `_load_thresholds()` 原 `:72` | `except Exception:` / `pass` → `except Exception as e:` + `logger.warning(f"共识阈值读取失败，沿用当前值 bearish_strong={_BEARISH_STRONG} / enter_threshold={_BULLISH_ENTER}: {e}")` |
| 3 | `_load_conflict_rules()` 原 `:96` | 同上 → `logger.warning(f"冲突规则配置读取失败，回退全量默认（{len(_DEFAULT_CONFLICT_RULES)} 条）: {e}")` |

**阈值/规则逻辑零改动**：仍沿用原值、保留原有 `return set(_DEFAULT_CONFLICT_RULES)` 回退路径与 `enabled=false → set()` 语义；P0–P7 优先级逻辑未触碰。措辞用「**沿用当前值**」而非「沿用硬编码默认值」，因异常前可能已成功加载过一次配置（此时全局量非硬编码值），如此表述在两种情形下均准确。

### 验证证据

| 检查 | 结果 |
|---|---|
| `ruff check app/opportunity_atlas/arbiter.py`（`.venv/bin/ruff`） | **All checks passed!**（exit 0） |
| `python3 -m py_compile` | 通过 |
| 沙盒定向回归（`DATA_DIR`/`TEST_DATA_DIR` 指向 `/tmp` 临时目录） | `test_321_arbiter.py`（18）+ `test_conflict_arbiter_integration.py`（11）→ **29 passed in 0.04s** |
| **F3 生效证明**（注入故障：monkeypatch `get_status_engine_config` 抛 `RuntimeError`） | 捕获 **2 条 WARNING**：`共识阈值读取失败，沿用当前值 bearish_strong=0.67 / enter_threshold=0.67: …`、`冲突规则配置读取失败，回退全量默认（6 条）: …`；**返回值与改前逐位一致**（`0.67/0.67`、6 条默认规则集） |
| **正常路径无噪音** | 不注入故障直接调用：**0 条 WARNING**；阈值 `0.67/0.67`、规则集与全量默认一致（该 yaml 现未配 `rules` → 走全量默认，属**改前既有行为**） |
| 残留裸吞异常（`grep -n -A1 "except Exception"`） | 仅剩 2 处，均为已改站点的 `as e` 分支；窄捕获 `(TypeError, ValueError)`（原 `:110/:166/:170/:223`）**不在 F3 范围，未动** |

### 需知悉的两点

1. 新增告警**只在真实故障时出现**：正常配置下 `conflict_rules.rules` 为空 → `_load_conflict_rules()` 按设计返回全量默认集（`return` 在 `try` 之外，非异常路径），因此日常运行**不产生新增日志**，仅**可观测性**发生变化。
2. `arbiter.py` 的 `git diff` 中另含**本会话之前既有的未提交改动**（docstring 路径改 `backend/config/`、`_cr` 按名 gating 等，属 430 §9 第 5 条产物）—— 本次**未触碰、未回退**，本轮净新增仅为上述 3 处。

**产出**：`backend/app/opportunity_atlas/arbiter.py`（+`import logging`/`logger`，2 处 `except` 改显式告警）。至此 431 §六 第 ①②③ 组（纯文案 / 归档可信度 / F3）**全部落地**，仅剩第 ④ 组「需单独决策」（E1/E2/E3 + F1/F2/F4 + G1/G2）。下一步：**批次4-G2（配置加载单测）**。

---

## 009 — 431号批次4 落地（G2 配置加载单测）：新增 1 个测试文件 / 17 个测试

**用户输入**：**下一批次 = 批次4-G2｜配置加载单测（推荐）**；**沟通记录 = 补记（推荐）**。

**批次决策**（对话框选项）：431 §六 第 ④ 组中唯一纯**加性**项（不修改任何生产代码/配置），定位为「**G2 是 F1 合并加载器的前置回归网**」——先把两套加载器的现行契约与口径锁死，再谈合并。

### 实施内容（新增 `backend/tests/test_431_g2_config_loading.py`，214 行，17 个测试）

| 分组 | 覆盖点 | 测试数 |
|:--:|---|:--:|
| 1｜权威位置 | `status_config._CONFIG_DIR` → **`backend/config/`**（basename 双断言 + `isdir`）；`status_engine.yaml` / `signal_registry.yaml` / `strategy_defaults.yaml` 三份实存；**项目根 `config/{status_engine,signal_registry}.yaml` 必须不存在**（锁 430 §9 第 3 条 `git rm` 成果，防回归） | 6 |
| 2｜`load_yaml` 语义 | 文件缺失 → `{}`；`lru_cache` 命中（`is` 同一对象 + `cache_info().hits ≥ 1`）且 `cache_clear()` 后等价重载；空文件 → `{}`（`tmp_path` + `monkeypatch.setattr(_CONFIG_DIR)`，**不污染真实配置目录**） | 3 |
| 3｜内容契约 | `status_engine.yaml`：`version=2` / `jud_engine_version="v390"` / 共识 `0.67/0.67` 对称 / `conflict_rules.enabled=True` + 6 条规则 / `l0.hard_risks==['regulatory']` / `soft_risk_coeff.deep_position_cap==0.3` / `emotion_position_cap` 5 档 / `hold_only_stages==['已延伸']`；**死键防回归**：`dimension_weights` 整块与 `consensus.color_high`/`color_low`/`neutral_is_zero` 均不得再现（430 §9 第 5 条）；`signal_registry.yaml`：`version=1` + 5 信号 × 5 必需字段 + `verify_days` 为正整数 + `lifecycle` 双段且 `initial.dist_pct < extended.dist_pct` | 3 |
| 4｜策略加载器 | `config/__init__.py.__file__` 所在目录 **== `_CONFIG_DIR`**（两套加载器同指一处，**F1 口径锚点**）；`load_strategy_config()` 默认路径 == 显式路径结果（且 `chip_distribution.lookback_period==120`、`chanlun.min_kline==5`）；`get_strategy_config()` 惰性缓存（`is` 同一对象）+ `reload=True` 换新对象 + 内容等价 | 2 |
| 5｜消费侧接线 | `arbiter._load_thresholds()`：注入 `{bearish_strong: 0.5, enter_threshold: 0.8}` 后**全局量确实随之改变**（证阈值由 YAML 驱动而**非代码常量**），撤销后复位 `0.67/0.67`；`arbiter._load_conflict_rules()` 四态语义（`enabled=false`→`set()`／`rules` 白名单→仅该条／`rules` 缺失→全量默认／配置整体为空→全量默认）且真实 yaml 下等于 `_DEFAULT_CONFLICT_RULES`（6 条）；**F3 回归网**：注入 `RuntimeError` 时产出 2 条 WARNING（`共识阈值读取失败…`、`冲突规则配置读取失败…`）且沿用当前值与回退集 | 3 |

**零生产代码/零配置改动**：仅新增测试文件，`backend/app/` 与 `backend/config/` 未触碰。

### 验证证据

| 检查 | 结果 |
|---|---|
| `ruff check tests/test_431_g2_config_loading.py`（`.venv/bin/ruff`） | **All checks passed!** |
| 定向运行（`TEST_DATA_DIR=/tmp/431g2_sandbox` 沙盒） | **17 passed in 0.06s** |
| 相邻回归（G2 + `test_321_arbiter` + `test_conflict_arbiter_integration` + `test_352_g2_diagnose_l1` + `test_390_l6_advice_engine` + `test_322_s0_cache_fix`） | **59 passed / 1 failed**；唯一失败为 `test_322_s0_cache_fix.py::test_read_signal_cached_falls_back_to_latest`（断言「非交易日应命中 600519 最新缓存」），**单跑同样失败** → 空沙盒**缺缓存数据**所致，与本次改动**无因果**（该文件未触碰） |
| 全量 lint（`ruff check app/ tests/`） | **13 errors，与基线同数**；新文件命中 **0** |
| 真实配置目录洁净性 | `backend/config/` 仍为 `__init__.py` + 3 份 yaml（空文件用例走 `tmp_path`，未落盘污染） |
| `git status --short` | 本轮净新增仅 `?? backend/tests/test_431_g2_config_loading.py` |

### 需知悉的三点

1. **强度最高的两条断言**：5 组的「注入 0.5/0.8 即生效」（证明消费侧确由 YAML 驱动）与 4 组的「两加载器同指 `_CONFIG_DIR`」（F1 合并前的口径锚点）——这两条正是 F1/G1 动手前必须成立的约束。
2. **第 3 组属强断言**：日后**有意**修改 `status_engine.yaml`（例如 G1 把硬编码常量收敛进 YAML）需同步更新本测试文件，否则会红；这是回归网的**预期行为**，不是脆弱测试。
3. 4 组测试会重置 `config._strategy_config` 与 `load_yaml` 缓存，均在 `finally` 中还原，**无跨测试状态泄漏**。

**产出**：`backend/tests/test_431_g2_config_loading.py`（新增，214 行 / 17 测试）。至此 **431 §六 第 ①②③ 组 + 第 ④ 组中的 G2 已落地**，第 ④ 组剩余 E1/E2/E3/F1/F2/F4/G1。下一步：**批次5-F1（合并两套配置加载器）**。

---

## 010 — 431号批次5 落地（F1 合并两套配置加载器）：3 个文件，**重构**（430 号唯一 P1）

**用户输入**：**下一批次 = 批次5-F1｜合并两套配置加载器（推荐）**；**沟通记录 = 补记（推荐）**。

**口径决策**（对话框选项，两项均取推荐项）：

| 口径 | 决策 | 含义 |
|---|---|---|
| 结构口径 | **config 承载实现 + 兼容壳，消费方不动** | `backend/config/__init__.py` 承载全部实现并成为**唯一权威入口**；`app/services/status_config.py` 退化为**薄再导出壳**；4 处生产消费方 import **零改动** |
| 语义口径 | **单缓存 + 缺失一律返回 `{}`** | 统一为 `load_yaml` 单一 `@lru_cache`（`get_strategy_config(reload=True)` 改为 `cache_clear()` 后重读，移除 `_strategy_config` 模块级全局）；所有缺失文件一律返回 `{}`、不再抛 `FileNotFoundError` |

两口径合起来即：F1 的 P1 关切（「两套加载器各自缓存、各自解析路径 → 行为不一致」）被**彻底消除**——只剩一份实现、一个缓存、一种缺失语义；同时因保留兼容壳，**4 处既有消费方（含 `status_engine.py:21` 的模块级顶层导入）零风险**。

### 调研结论（动手前，只读）

| 项 | 加载器① `app/services/status_config.py` | 加载器② `backend/config/__init__.py` |
|---|---|---|
| 路径解析 | 上溯 3 级 → `backend/config/` | `os.path.dirname(__file__)` → `backend/config/` |
| 缓存 | `@lru_cache(maxsize=4)` | 模块级 `_strategy_config` 全局 + `reload` 热重载 |
| 缺失文件 | 返回 `{}` | `open()` 直接抛 `FileNotFoundError` |
| 消费方 | **4 处生产代码**：`advice_builder:225`、`advice_engine:576`、`arbiter:71/96`（均函数级惰性）+ **`status_engine.py:21`（模块级顶层导入）** | **仅 1 处**：`app/engine/pipeline.py:97-98`（相对导入 `from ...config`，外包 `try/except ImportError`） |

同时确认：两者**已同指** `backend/config/`（430 §9 第 3 条已消除路径分歧），故 F1 真正剩余的分歧只有**缓存机制**与**模块归属**；`app/core/config.py`（`ai_consensus_service:487` 的 `from app.core import config`）是**另一个模块**，不计入 F1 消费方；根 `config/` 非包（仅 `data_sources.yaml`/`description_config.json`/`prompts/`）；`backend/config/__init__.py` 当时**无未提交改动**（改前已确认，可安全动）。

### 实施内容（3 个文件）

1. **`backend/config/__init__.py`（27 → 58 行）——唯一实现与权威入口**：新增私有 `_read_yaml(path)`（缺失/空文件一律 `{}`）；`_CONFIG_DIR = os.path.dirname(os.path.abspath(__file__))`；`load_yaml(name)` 改 `@lru_cache(maxsize=8)` 并复用 `_read_yaml`；新增 `get_status_engine_config()`/`get_signal_registry()`（迁自加载器①）；`load_strategy_config(path=None)` 默认走 `load_yaml('strategy_defaults.yaml')`、显式 `path` 仍直读；`get_strategy_config(reload=True)` 改为 `load_yaml.cache_clear()` 后重读，**移除** `_strategy_config` 全局与 `global` 语句。
2. **`app/services/status_config.py`（34 → 16 行）——薄再导出壳**：整体替换为 `from config import _CONFIG_DIR, get_signal_registry, get_status_engine_config, load_yaml` + `__all__`；docstring 明示「实现与权威入口已统一到 `backend/config/__init__.py`，保留本模块仅为兼容既有消费方，不要在此新增加载逻辑」。**4 处消费方 import 一字未改**。
3. **`backend/tests/test_431_g2_config_loading.py`（214 → 240 行，17 → 19 测试）——回归网同步**：`_CONFIG_DIR` 猴补目标由壳改为权威模块 `config`（空文件用例）；`get_strategy_config` 用例改用 `cache_clear()` 并删除对已移除全局 `_strategy_config` 的无效操作；**新增 2 条**：`test_status_config_is_thin_shim`（`load_yaml`/`get_status_engine_config`/`get_signal_registry` 均与权威模块**同对象**且 `_CONFIG_DIR` 相等）与 `test_strategy_missing_file_returns_empty_dict`（缺失一律 `{}`，含显式 `path`）；模块 docstring 与两处用例 docstring 同步改写。

### 验证证据

| 检查 | 结果 |
|---|---|
| `ruff check` 3 个改动文件（`.venv/bin/ruff`） | **All checks passed!** |
| 全量 lint（`ruff check app/ tests/`，与 `make lint` 同口径） | **13 errors，与基线同数**；3 个改动文件命中 **0** |
| G2 定向（`TEST_DATA_DIR` = 空沙盒） | **19 passed in 0.06s**（原 17 + 新增 2） |
| 生产态导入冒烟 A（cwd=`backend`） | `_CONFIG_DIR`=…`/backend/config`；`status_engine.version=2`；5 信号齐；**壳与实现同对象**（`sc.load_yaml is config.load_yaml` = True）；`strategy lookback=120` |
| 生产态导入冒烟 B（cwd=项目根，`sys.path` 含根 `config/` 命名空间目录） | `config.__file__` = `backend/config/__init__.py`（**常规包必胜同名命名空间目录**，按 PEP 420，无歧义） |
| 全量回归（空沙盒）+ **基线对照** | 改动后 **34 failed / 760 passed**；基线（**仅**回退本 3 文件、其余工作树不变）**34 failed / 741 passed**；**失败集逐条 `diff` 完全一致 → 零新增回归**；`760-741=19` 即本轮新增测试数 |
| 残留引用 | `_strategy_config` 全局已彻底移除（全仓 0 处读写）；`get_strategy_config`/`load_strategy_config` 的生产消费方仍**仅** `pipeline.py:97-98`（未受影响） |
| `backend/config/` 洁净性 | 仍为 `__init__.py` + 3 份 yaml（无落盘污染） |
| `git status --short`（3 文件） | `M backend/config/__init__.py`、`M backend/app/services/status_config.py`、`?? backend/tests/test_431_g2_config_loading.py` |

### 需知悉的四点

1. **基线对照用的是「只回退本 3 文件」的就地法**（备份 → `git show HEAD:` 覆盖两个已跟踪文件的加载器 → 暂时移除新测试文件 → 跑 → 同条命令内还原），因此**其余未提交改动（430 §9 产物等）在两次运行中逐字一致**，差异只能归因于本 3 文件。全量 34 failed 全为空沙盒「无数据」类失败（缺 ECM 表 / 快照 / 缓存），与本改动**无因果**。
2. **壳的导入解析依赖 `backend/` 在 `sys.path`**：`from config import …` 与既有 `app.` 绝对导入、`pipeline.py:97` 的 `...config` 是同一种解析前提，已在 A/B 两种 cwd 下实测。
3. **G2 回归网如期同步更新**（正对应 §009 需知悉第 3 点的预告）：`_CONFIG_DIR` 猴补**必须打在权威模块**上——壳再导出的 `_CONFIG_DIR` 是**值快照**，猴补不会传导；这是「实现集中 + 保留壳」的正常代价，非缺陷。
4. **一处语义变更需留意**：`load_strategy_config(path)` 传**显式不存在路径**时，过去抛 `FileNotFoundError`，现返回 `{}`（与 `load_yaml` 一致）——即所选「缺失一律 `{}`」口径的直接结果。生产侧仅按默认路径调用（`pipeline.py` 用 `get_strategy_config()`），**无实际影响**。

**产出**：`backend/config/__init__.py`（唯一实现 / 权威入口）+ `backend/app/services/status_config.py`（薄兼容壳）+ `backend/tests/test_431_g2_config_loading.py`（回归网同步）。**430 号唯一 P1（F1）就此关闭**。至此 431 §六 第 ④ 组剩余 **E1/E2/E3/F2/F4/G1**（F2 与 G1 同源、F4 需功能决策、E1/E2/E3 为功能/破坏性项）。下一步：**待定**。

---

## 011 — 431号批次6 落地（E2 删 `.env.bak.331` + E3 env 键集对齐）+ 431 v1.2 补录 JUD 报告残留文案

**用户输入**：对话框选项确认 **下一批次 = 批次6-E2+E3｜env 文件卫生（推荐）**，并同时选定存量候选 **431 v1.2 补录 JUD 报告 `:679`/`:680`（推荐）**。

**口径决策**（对话框选项，3 项均取推荐项）：

| 决策项 | 选定口径 |
|---|---|
| E2 `.env.bak.331` 残留 | **删除**（不可恢复已确认） |
| E3 键集对齐方向 | **补模板 + 补运行态 `.env`**（两侧同补，不删键） |
| `.env.local` 孤儿文件 | **保留不动，记入 v1.2 待议** |

### 调研结论（动手前，只读）

- `.env.bak.331`（1241 B / 8月13日 20:06）＝**331 号 PG→SQLite 迁移的回滚点**：与 `.env` 的唯一实质差异为 `DATABASE_URL`；被 `.gitignore:105 *.bak.*` 覆盖 → **删除即无 git 兜底，不可恢复**。
- `.env.local`（754 B / 14 键 / 7月11日 08:58）＝**孤儿文件**：`docker-compose.yml` 无 `env_file:`、项目根无 `run.py`；`.env` 仅由 `backend/app/__init__.py:4,10`、`backend/sync_data.py:13`、`backend/data_daemon.py:26-28` 显式加载 → **零消费方**（但其 10 个同名键若误接线会覆盖运行态）。
- E3 键集实测：E3 命名范围内 `QUALITY_*`（4 键）与 `L4_EXTRA_VOTES` 在 `.env` / `.env.example` **双双缺失**，`CN_10Y_BOND_YIELD` 两处已在。

### 实施内容（4 个对象）

| # | 对象 | 改动 |
|:--:|---|---|
| 1 | `.env.bak.331` | **删除**（经用户确认；gitignored → 不进 `git status`） |
| 2 | `.env` | 补 5 键（2 个注释分组）：`QUALITY_ROE_THRESHOLD=12.0` / `QUALITY_ROE_NORM=20.0` / `QUALITY_PREMIUM=0.25` / `QUALITY_FAIL_PENALTY=0.5`（315 号阶段2）、`L4_EXTRA_VOTES=0`（316 号 P3）——取值＝**代码默认值**，行为零变更 |
| 3 | `.env.example` | 同两组注释块同步补入，模板与运行态键集对齐 |
| 4 | `002-方案存档/431-430号遗留项核查清单与处置建议.md` | **v1.2 补录 7 处**（`wc -l` = **494**）：frontmatter `version`/`status`、v1.2 增补 blockquote、**B1 补录**（JUD `§6.2` 命名映射表 `:679`/`:680` + 新发现 `:26`）、P3-med 第 7 行、§八 取证块、footer（核查日期/范围/`处置状态`）+ 新增「**处置进度（按批次）**」表、**E 类处置补记**（E2 已删 / E3 已对齐命名范围 / `.env.local` 保留待议 / 残留 7 键漂移） |

### 验证证据

| 项 | 证据 |
|---|---|
| E2 删除 | `ls -1 .env*` → 仅 `.env` / `.env.example` / `.env.local` |
| 行数（`wc -l`） | `.env` 44 / `.env.example` 61 / `.env.local` 22 |
| E3 键取值探针 | `CN_10Y_BOND_YIELD=1.7`、`QUALITY_ROE_THRESHOLD=12.0`、`QUALITY_ROE_NORM=20.0`、`QUALITY_PREMIUM=0.25`、`QUALITY_FAIL_PENALTY=0.5`、`L4_EXTRA_VOTES=0`——6 键全部＝代码默认值 |
| G2 沙盒定向回归 | 空沙盒 `backend/tests/test_431_g2_config_loading.py` → **19 passed in 0.06s** |
| git 状态 | 本批次唯一受控文件改动 = ` M .env.example`（`.env` 未跟踪；`.env.bak.331` 受 gitignore 覆盖） |
| JUD 报告取证（只读） | `grep -n "status_engine\.yaml"` → `:4 / :21 / :26 / :437 / :439 / :471 / :479 / :679 / :680 / :711`；`grep -n "dimension_weights"` → **0 命中**；`wc -l` → JUD 报告 712、`status_engine.yaml` **41** |

### 需知悉的四点

1. **E3 只关闭了「命名范围」**：探针另暴露 **E3 范围外的 7 键漂移**，本次**未动、仅记录**（已并入 431 E 类补记）——**模板独有 4 键** `AUTH_TOKEN` / `DEEPSEEK_BASE_URL` / `QMT_ACCOUNT_ID` / `QMT_ENABLED`（可选／默认值类，运行态未设不影响启动）；**运行态独有 3 键** `FLASK_ENV`（消费方 `backend/run.py:37`，另由 3 个 scripts 以 `os.environ` 就地设定）、`LLM_WIKI_API_TOKEN`（消费方 `routes/ai_analysis.py:379`、`routes/health.py:165,168`）、`REDIS_URL`（**全仓无任何消费方**，仅 `app/config.py:28` 定义，属旧 Gunicorn 多 Worker SocketIO 广播设计的遗留 → **失效键候选**）。
2. **`.env.local` 当前无害但需防误接线**：其 10 个键与 `.env` 同名（含 `DATABASE_URL` / `DATA_DIR` / `SECRET_KEY`），现无加载方；去留已记入 431 v1.2 待议。
3. **`.env` 用 python heredoc 写入、未用 `edit` 工具**：避免把含密钥的文件内容带进工具回显；本次写入的 5 个值均为代码默认值（非密钥）。
4. **431 v1.2 的两项自查**：①核查中一度把 `:679` 表述为「v1.1 已归入引用已移除项」，经 `grep` 复核（`consensus\.enter_threshold` 全仓仅 2 处命中、均为本次新增文本）**证伪并改正**为「口径澄清」段；②B1 表内 `:709` 行号在 §007 订正注记插入后**已漂移**（JUD 报告当前末行 `:711`），而 `:439` / `:441` / `:479-484` 仍吻合 → **非统一偏移**，故本次**未改** v1.1 本体，留待后续批次。

**产出**：`.env.bak.331`（删除）+ `.env` / `.env.example`（键集对齐）+ `002-方案存档/431-430号遗留项核查清单与处置建议.md`（v1.2）+ `002-方案存档/001-目录索引.md`（431 行状态升至 v1.2）。**431 §六 第 ④ 组剩余 E1 / F2 / F4 / G1**（F2↔G1 同源、F4 需功能决策、E1 为功能缺口）——**均需单独决策，本轮未动**。下一步：**待定**。

---

## 012 — 431号 v1.3 补录（存量待议事项归档：残留 env 键漂移 7 键 / `.env.local` 孤儿 / B1 行号漂移；纯文案）

**用户输入**：对话框选项确认 **下一批次 = 批次7｜431 v1.3 补录（纯文案）**。

**口径决策**：

| 决策项 | 选定口径 |
|---|---|
| 本轮性质 | **纯文案**：只动 431 文档 + 两处索引，**不动**代码 / 配置 / `.env*` |
| 残留 7 键 env 漂移 | 归档入 **431 §九 9.1**（含 `REDIS_URL` 失效键候选），本轮**不处置** |
| `.env.local` 孤儿文件 | 归档入 **431 §九 9.2**，维持「**保留不动**」 |
| B1 `:709` 陈旧行号 | 归档入 **431 §九 9.3**，**不改 B1 本体**（有日期的审计快照） |

### 调研结论（动手前，只读）

- 残留 7 键**逐个复测消费方**（未沿用 v1.2 的粗分类）：**模板独有 4 键均有真实消费方且带默认值** → 属「可选」而非缺失（`AUTH_TOKEN`→`app/auth.py:25`（未设即跳过鉴权＝开发模式，后备 `data/auth_token.txt`）；`DEEPSEEK_BASE_URL`→`app/config.py:32`；`QMT_ENABLED`→`app/config.py:43`、`init_qmt_config.py:29`、`app/__init__.py:346`；`QMT_ACCOUNT_ID`→`app/config.py:44`、`init_qmt_config.py:30`）；**运行态独有 3 键**中 `FLASK_ENV`（`run.py:37`）、`LLM_WIKI_API_TOKEN`（`routes/ai_analysis.py:379`、`routes/health.py:165,168`）有真实消费方，`REDIS_URL` **全仓仅 1 处命中（即 `backend/app/config.py:28` 自身定义）→ 0 消费方**。
- `.env.local`：`wc -l` = 22 / 754 B / **14 键** / 7月11日 08:58，**零加载方**（`docker-compose.yml` 无 `env_file:`、项目根无 `run.py`；`.env` 仅由 `app/__init__.py:4,10`、`sync_data.py:13`、`data_daemon.py:26-28` 加载）。
- B1 漂移复核：`:709` 现位于 `:711`，且该行内容**已由批次2（§007）修正**为 `| backend/config/status_engine.yaml | 41 | 全量配置 |`；文档 711 → **712 行（+1）** 而目标行位移 **+2** ⇒ **非统一偏移，成因未确认**；`:439` / `:441` / `:479-484` 仍吻合。

### 实施内容（3 个对象 + 本文件，纯文案）

| # | 对象 | 改动 |
|:--:|---|---|
| 1 | `002-方案存档/431-…md` | **v1.3 补录 5 处**（`wc -l` **494 → 551**）：frontmatter `version`、v1.3 增补 blockquote（`:25`）、**新增 §九 存量待议事项**（`:492`：9.1 七键表 / 9.2 `.env.local` / 9.3 B1 漂移）、§八 v1.3 取证块、footer（核查日期 + 核查范围 + 处置进度 3 行） |
| 2 | 同上 §八 note | `**v1.1 / v1.2 存档时**` → `**v1.1 / v1.2 / v1.3 存档时**` |
| 3 | `002-方案存档/001-目录索引.md` | 431 行状态 **v1.2 → v1.3**，补 §九 摘要与 §012 落地记录、两个待议项 |
| 4 | 本文件 + `沟通索引.md` | 补记 §012 |

### 验证证据

| 项 | 证据 |
|---|---|
| 431 行数 | `wc -l` → **551**（v1.2 为 494，净 +57 行） |
| 431 章节结构 | `grep -n "^## "` → §一…§八 + **§九（:492）** + `### 处置进度`（:538） |
| 处置进度表 | 新增 `§012` ✅ 行 + 2 个 ⬜ 待议行（`.env.local` 去向 / 残留 7 键漂移），与 §9.1 / §9.2 互指 |
| 环境文件未动 | `ls -1 .env*` 仍为 `.env` / `.env.example` / `.env.local`；`wc -l` 仍为 **44 / 61 / 22** |
| git 状态 | 本批次受控改动 = ` M 002-方案存档/001-目录索引.md`、` M 001-沟通记录/沟通索引.md`；431 与本文件仍为未跟踪（`??`）；**`.env*` 零改动** |

### 需知悉的三点

1. **§九 与 §四 性质不同**：§四 是「扫出来的问题」，§九 是**事实已查清、处置方式未定**的存量事项——故 §九 **不新增编号类目**（A–G 仍为七类），也不进 §六 分级处置建议；仅在 footer 处置进度表新增两个 ⬜ 待议行。
2. **`REDIS_URL` 是残留 7 键中唯一可考虑删除的键**：全仓零消费方（仅 `app/config.py:28` 定义 + 该行注释），属旧 Gunicorn 多 Worker SocketIO 广播设计遗留；删键 + 澄清注释**需单独决策**，本轮未动。
3. **`grep -c "^### 011"` = 21 已解释（非异常，前次误判已更正）**：`001-沟通记录/沟通索引.md` 是**跨日期的总索引**（每条目 = 某日期文件的某一节），故 `### 011` 按「含该节的日期文件数」重复出现；21 即该数目，本次 `grep_search "^### 01[0-2] "` 得 **57 命中**、与该结构一致（末条 `:3942` 即 2026-09-13 的 §011）。此前按「单一日期文件」理解而记为异常，属**误判，现更正**。

**产出**：`002-方案存档/431-430号遗留项核查清单与处置建议.md`（**v1.3**，新增 §九 存量待议事项）+ `002-方案存档/001-目录索引.md`（431 行 → v1.3）+ `001-沟通记录/2026-09-13.md` §012 + `001-沟通记录/沟通索引.md`。**431 §六 第 ④ 组剩余 E1 / F2 / F4 / G1** 与 §九 两项待议（`.env.local` 去向 / 残留 7 键漂移）——**均需单独决策，本轮未动**。下一步：**待定**。

---

## 013 — 431号 §9.1 残留 env 键漂移 7 键（登记模板 2 键 + `REDIS_URL` 注释澄清）

**用户输入**：对话框选项确认 **下一批次 = 批次8｜§9.1 残留 7 键（推荐）**；选项描述＝「落地低风险部分：把 FLASK_ENV、LLM_WIKI_API_TOKEN 登记进 .env.example（占位空值），并评估删除失效键 REDIS_URL（含澄清 backend/app/config.py:28 注释）。仅动 .env.example 与 config.py 注释，不动 .env 值。」

**口径决策**（以选项描述为准，未再追问）：

| 决策项 | 选定口径 |
|---|---|
| 第 5、6 键（`FLASK_ENV` / `LLM_WIKI_API_TOKEN`） | **登记进 `.env.example`**（默认 `production` / 空占位 + 注释） |
| 第 7 键（`REDIS_URL`） | 仅**澄清 `backend/app/config.py:27` 注释**；**`.env` 侧删键仍待议**（本轮不删） |
| 第 1–4 键（模板独有） | **无动作**（v1.3 已判「可选，模板保留即可」） |
| 改动面 | 仅 `.env.example` + `backend/app/config.py`（注释）；**`.env` / `.env.local` 值一字未动** |

### 调研结论（动手前，只读）

- **7 键漂移逐个复测**（不沿用 v1.3 文字表述）：`FLASK_ENV` 消费方 `backend/run.py:37`（`debug_mode = os.getenv("FLASK_ENV", "production") == "development"`，**值比较**）；`LLM_WIKI_API_TOKEN` 消费方 `backend/app/routes/health.py:164-166`（`wiki_token = os.getenv('LLM_WIKI_API_TOKEN','')` → `'configured' if wiki_token else 'not_configured'`）与 `backend/app/routes/ai_analysis.py:379-380`（`wiki_token = os.environ.get(...,'')` → `headers = {...} if wiki_token else {}`）——**两处均为「空值即假」判断**。
- **`REDIS_URL` 零消费方复验**：`grep_search "REDIS_URL"` 全仓 43 命中，`backend/` 代码内**仅 `backend/app/config.py:27`（注释）/`:28`（定义）**；其余为历史方案文档、`docker-compose.yml:38`、`com.stock.analyzer.backend.plist:28`、`docs/deployment/可移植性体检清单.md:49/55`、`_项目运行手册.md:426`。旁证：`.env.example` 既有「# --- 缓存 ---」段已写明「Redis 已不再需要，使用 cachetools.TTLCache 作为内存缓存」；`backend/*.py` 中 `redis` 命中 12 处**全为退役说明**（`app/__init__.py:153`「单进程模式：SocketIO 直接广播，无需 Redis」、`routes/health.py:7`「无 Redis → 使用 TTLCache」、`routes/realtime.py:266/:360`「替代旧的 Redis 推流服务」等）。
- **行为中性证明**：上列三处消费方对「未设」与「空串」的判定**完全一致**（`run.py` 为值比较 → 非 `development` 即 `False`；`health.py` / `ai_analysis.py` 为真假判断）⇒ 模板登记空占位 / 默认 `production` **不可能改变任何运行时决策**。

### 实施内容（2 个对象 + 4 处文档）

| # | 对象 | 改动 |
|:--:|---|---|
| 1 | `.env.example` | **新增 6 行 / 18 → 20 键**（`grep -c ''` 61 → 67）：① 头部新增 `# --- 运行环境 ---` 段 + `FLASK_ENV=production`（注「消费方：run.py；仅 development 开启 debug」）共 4 行；② LLM 段末（LM Studio 注释行之后）新增 `LLM_WIKI_API_TOKEN=` + 注释「留空则健康检查显示未配置」共 2 行 |
| 2 | `backend/app/config.py` | `:27` 注释 `# Redis（Gunicorn 多 Worker SocketIO 广播）` → 两行澄清 `# REDIS_URL 为残留键：当前全仓无任何消费方（旧 Gunicorn 多 Worker SocketIO 广播设计，` / `# 现已改为单进程直接广播 + TTLCache 内存缓存）`；**`:28` 定义行未动** |
| 3 | `002-方案存档/431-…md` | §九 引言加「v1.3 补录时」限定（防「全部未动手」被误读为现状）；**§9.1 新增处置状态注记**（`:512`）；处置进度表新增 `§013` ✅ 行（`:551`），并把原「残留 env 键漂移 7 键」⬜ 行**收窄为「`REDIS_URL` 删键」** |
| 4 | `002-方案存档/001-目录索引.md` | 431 行「落地进度」补 §013（E1/F2/F4/G1 与两待议项表述不变） |
| 5 | 本文件 + `001-沟通记录/沟通索引.md` | 补记 §013 |

### 验证证据

| 项 | 证据 |
|---|---|
| `.env.example` 键 | `grep -oE '^[A-Za-z_][A-Za-z0-9_]*'` → **20 键**（新增 `FLASK_ENV` / `LLM_WIKI_API_TOKEN`）；`grep -n "^FLASK_ENV\|^LLM_WIKI_API_TOKEN"` → `:8` / `:21` |
| `.env.example` 行数 | `grep -c ''` → **67**（改前 61） |
| 环境文件未动 | `.env` **44** / `.env.local` **22**（与 §011 一致；**值一字未改**） |
| 语法 | `backend/.venv/bin/python -m py_compile backend/app/config.py` → **OK** |
| 定向回归 | `backend/.venv/bin/python -m pytest tests/test_431_g2_config_loading.py -q` → **19 passed in 0.80s** |
| git 状态 | ` M .env.example`、` M backend/app/config.py`（`--numstat` 18/0 与 2/1，见下注） |
| 431 结构 | `wc -l` → **554**；`grep -n "^## \|^### 9\.\|^### 处置进度"` → §九 :492 / 9.1 :496 / 9.2 :514 / 9.3 :521 / 处置进度 :540；§013 行 :551、处置状态注记 :512 |

### 需知悉的三点

1. **`git diff --numstat` 的 `18 0` 里有 12 行不是本批次产物**：`.env.example` 的 HEAD 版本**尚不含**「315号阶段2 估值质量修正」与「316号 P3 扩展票源」两段（共 12 行）——它们属**此前既有的未提交工作树改动**（本批次**未触碰、未回退**）；本批次净新增仅 **6 行**（4 + 2），`18 = 12 + 6`。
2. **`REDIS_URL` 只做了注释澄清、没有删键**：选项口径限定「仅动 `.env.example` 与 config.py 注释，不动 `.env` 值」，故 `.env` 中该键**原样保留**；另注意 `docker-compose.yml:38` 与 `com.stock.analyzer.backend.plist:28` 等**非代码位置仍设该键**，将来决定删键时需一并确认（已写入 §9.1 注记）。
3. **模板登记属「补文档」而非「补默认值」**：`run.py` / `health.py` / `ai_analysis.py` 三处消费方**均已自带默认值**，模板只是把**事实上的隐式契约显式化**（避免后续读者误判这两键无来源）；§9.1 第 1–4 键维持「无动作」，理由同前（有消费方且有默认值）。

**产出**：`.env.example`（**20 键**：登记 `FLASK_ENV` / `LLM_WIKI_API_TOKEN`）+ `backend/app/config.py`（`:27` 注释澄清）+ `002-方案存档/431-430号遗留项核查清单与处置建议.md`（§9.1 处置状态注记 + 处置进度 `§013` 行）+ `002-方案存档/001-目录索引.md` + `001-沟通记录/2026-09-13.md` §013 + `001-沟通记录/沟通索引.md`。**未改** `.env` / `.env.local`。**431 §六 第 ④ 组剩余 E1 / F2 / F4 / G1** 与 §九 两待议（`.env.local` 去向 / `REDIS_URL` 删键）——**均需单独决策，本轮未动**。下一步：**待定**。

---

## 014 — 431号批次9 落地（`REDIS_URL` 删键收尾）

**用户输入**：对话框选项确认 **下一批次 = 批次9｜`REDIS_URL` 删键收尾（推荐）**；随后两项范围追问均选「推荐」项：
- **删键范围** ＝ 「**`.env` + compose:38 + plist:28 三处同删（推荐）**」（描述：删 `.env` 中 `REDIS_URL`（1 行）；同步清两个会**实际注入环境变量**的活配置——`docker-compose.yml:38` 的 `- REDIS_URL=...` 行、`com.stock.analyzer.backend.plist:28-29` 的 `<key>`/`<string>` 两行。不动 compose 的 redis 服务结构…不动 `.env.local`（属 §9.2 待议）。）
- **文档口径** ＝ 「**只更新体检清单，保留运行手册历史条目（推荐）**」（描述：更新 `docs/deployment/可移植性体检清单.md`：从 `:49` 键名清单移除 `REDIS_URL`（或标注已废弃）、`:55` 说明改为「已废弃：单进程 + TTLCache，无消费方」。`_项目运行手册.md:426` 保留不动（历史规划快照，不重写历史）。）

**口径决策**（以选项描述为准，未再追问）：

| 决策项 | 选定口径 |
|---|---|
| 删键位置 | **三处同步删**：`.env` + `docker-compose.yml:38` + 启动器 plist |
| compose `redis` 服务结构 | **保留不动**（`redis:` 服务 / `depends_on` / `redis_data` 卷均不动，仅删 backend 的 env 注入行） |
| `.env.local` | **不动**（14 键仍有 `REDIS_URL`，属 §9.2 待议） |
| `backend/app/config.py` | **不再改动**（`:29` 定义行按「零消费方残留」保留，`:27` 注释继续有效） |
| 文档口径 | 体检清单（活文档）同步；`_项目运行手册.md:426`（历史规划快照）**保留不动** |
| 431 版本号 | **不升版**（属 fix 批次，仍为 **v1.3**；仅补录事件才升版） |

### 调研结论（动手前，只读）

- **零消费方复验**：全仓 `grep_search "REDIS_URL"` 改前 **56** 命中，`backend/` 代码内**仅 `backend/app/config.py:27`（注释）/`:29`（定义）**——**零消费方**成立。其余命中＝活配置（`.env` / `docker-compose.yml:38` / plist:28-29 / 体检清单:49,55）＋历史文档（`_项目运行手册.md:426`、`002-方案存档/*`、`001-沟通记录/*`）。
- **行号漂移（§013 副作用）**：§013 在 `config.py:27` 插入第二行注释，把 `REDIS_URL` **定义行由 `:28` 推到 `:29`**——本批次在 431 §9.1 行 7 就地标注，避免旧引用被误读为现行行号。
- **活配置 vs 历史文档分类**（决定删除范围）：`docker-compose.yml:38`＝容器 env 注入（活）；`com.stock.analyzer.backend.plist:28-29`＝launchd 加载的 API 进程 env 注入（活，`launchctl list` 有该项、状态 0）；`docs/deployment/可移植性体检清单.md:49,55`＝活文档（未跟踪文件）；`_项目运行手册.md:426`＝225号阶段一交付清单（历史规划快照）。
- **`.env` 里该键带专属注释行**：`:22 = # Redis（Gunicorn 多 Worker SocketIO 消息广播）` 紧邻键行 ⇒ 删键必须连带删注释，否则留下孤立注释（故实删 3 行，见「需知悉」1）。
- **删前安全性核对**：探针（只打印布尔/计数，**不打印值**）→ `value_equals_code_default: True`，即被删值本就等于代码默认 `redis://localhost:6379/0` ⇒ **可无损还原**。

### 实施内容（4 个对象）

| # | 对象 | 改动 |
|:--:|---|---|
| 1 | `.env`（未跟踪，敏感） | 经 `python3` heredoc（**不用 `edit` 工具、不打印值**）删除 1-based 第 **21/22/23** 行（空行 + Redis 注释行 + 键行）：**44 → 41 行**、键 **17 → 16**，`grep -cE '^[[:space:]]*REDIS_URL'` = **0** |
| 2 | `docker-compose.yml` | 删 backend `environment:` 中 `- REDIS_URL=redis://redis:6379/0` 一行；**`redis:` 服务 / `backend.depends_on.redis` / `volumes: redis_data` 全部保留** |
| 3 | `com.stock.analyzer.backend.plist` | 删 `<key>REDIS_URL</key>` + `<string>…</string>` 两行 |
| 4 | `docs/deployment/可移植性体检清单.md`（未跟踪） | ① `:49` 键名清单 `DATABASE_URL  REDIS_URL  FLASK_ENV  DATA_DIR` → `DATABASE_URL  FLASK_ENV  DATA_DIR`；② `:55` 条目改为「**REDIS_URL**（已废弃并移除）：旧 Gunicorn 多 worker + SocketIO 广播设计的遗留，全仓零消费方（现为单进程直接广播 + TTLCache 内存缓存），已从 `.env` / `docker-compose.yml` / 启动器 plist 清出」 |
| 5 | `002-方案存档/431-…md` | §9.1 表行 7 改判「**已处置（§014）**」并标注 `:28`→`:29` 漂移；§9.1 新增 **§014 处置状态注记**；处置进度表新增 `§014` ✅ 行、§013 行去掉「（删键仍待议）」括号、删原 `REDIS_URL` ⬜ 行（`REDIS_URL` 残留改注到 `.env.local` ⬜ 行） |
| 6 | `002-方案存档/001-目录索引.md` | 431 行「落地进度」补 §014，尾句收窄为「另 `.env.local` 去向待议」 |
| 7 | 本文件 + `001-沟通记录/沟通索引.md` | 补记 §014 |

### 验证证据

| 项 | 证据 |
|---|---|
| `.env` | `grep -c ''` → **41**（改前 44）；`grep -oE '^[A-Za-z_][A-Za-z0-9_]*'` → **16 键**（无 `REDIS_URL`）；`grep -cE '^[[:space:]]*REDIS_URL'` → **0** |
| 行为中性 | dotenv 加载后 `env_has_REDIS_URL: False`；`Config.REDIS_URL_equals_default: True` ⇒ 解析值仍为 `redis://localhost:6379/0`，与删键前**逐位一致** |
| `docker-compose.yml` | `git diff --numstat` = **`0 1`**；YAML 可解析 → `services: ['backend','frontend','postgres','redis']`、backend env **14 键**（无 `REDIS_URL`）、`redis_service_still_present: True`、`volumes: ['postgres_data','redis_data']`（无悬空 `depends_on`） |
| plist | `git diff --numstat` = **`0 2`**；`plutil -lint` → **OK**；（`launchctl list` 中该项需重载才生效，零消费方 ⇒ 无行为影响） |
| 文档 | `grep -nE 'REDIS_URL' docs/deployment/可移植性体检清单.md` → 仅 `:55`（新废弃说明）；`_项目运行手册.md:426` 未动 |
| 全仓命中数 | 改前 **56** → 改后 **53**（减 3：`.env` / compose / plist 各 1 处注入点；体检清单 2 处改为 1 处说明） |
| 未动项 | `.env.local` 仍 **22 行 / 14 键、含 `REDIS_URL`**（§9.2 待议）；`backend/app/config.py` 未改（仍 91 行） |
| 定向回归 | 沙箱 `TEST_DATA_DIR=/tmp/b9_sandbox.fMd3dV backend/.venv/bin/python -m pytest tests/test_431_g2_config_loading.py -q` → **19 passed in 0.05s** |
| git 状态 | 受控改动 = ` M docker-compose.yml`、` M com.stock.analyzer.backend.plist`；`docs/deployment/可移植性体检清单.md` 与 431 仍未跟踪（`??`） |

### 需知悉的三点

1. **`.env` 实删 3 行，而非选项文字里的「1 行」**：该键上方有一行专属注释（`# Redis（Gunicorn 多 Worker SocketIO 消息广播）`）与一个空行，只删键行会留下孤立注释；故连注释与空行一并删除（`grep -c ''` 44 → 41）。删除内容已记录：被删值 = 代码默认值（可无损还原）。
2. **compose 的 `redis` 服务体**按决策**保留**：删的只是 backend 的 env 注入行；`redis:` 服务、`depends_on.redis: service_healthy`、`volumes: redis_data` 仍在（栈内自洽，无悬空依赖）。若后续要连服务一起清，属**新一轮决策**，本批次未做。
3. **`.env.local` 仍含 `REDIS_URL`，431 不升版**：前者属 §9.2 待议（孤儿文件，14 键），本批次明确未碰；后者因 fix 批次不触发版本号变更 ⇒ 431 **仍为 v1.3**（`wc -l` 554 → **556**；**订正**：原记 `557` 系读取工具显示行数比 `wc -l` 多计 1 所致，`wc -l` / `awk 'END{print NR}'` / `grep -c ''` 三法同为 **556**），`001-目录索引.md` 的 431 行状态保持 `📋 核查存档（v1.3，按批次落地中）`。

**产出**：`.env`（**16 键**）+ `docker-compose.yml`（backend 14 键）+ `com.stock.analyzer.backend.plist` + `docs/deployment/可移植性体检清单.md` + `002-方案存档/431-430号遗留项核查清单与处置建议.md`（§9.1 行 7 改判 + §014 注记 + 处置进度 `§014` 行，**仍 v1.3**）+ `002-方案存档/001-目录索引.md` + `001-沟通记录/2026-09-13.md` §014 + `001-沟通记录/沟通索引.md`。**未改** `.env.local` / `backend/app/config.py` / `_项目运行手册.md`。**431 §9.1 残留 7 键全项闭环**。剩余待决策：**E1 / F2 / F4 / G1** 与 §九 **`.env.local` 去向**——均需单独决策。下一步：**待定**。

---

## 015 — 431号批次10 落地（§九 `.env.local` 去向：决策＝**保留不动** + 键数口径订正 + 新增 §9.4）

**批次口径**（用户选定「下一批次：批次10｜§九 `.env.local` 去向」）：本批先做**只读调研**并给出处置选项（保留 / 删除 / 改名归档 / 并入 `.env`），**不直接动文件**。调研报告给出后经用户确认，**决策＝「保留不动」**（推荐项：零风险、完全可逆，且与 v1.2 记录的「保留不动、记入待议」一致）。

### 调研结论（动手前后均只读）

- **文件事实**：**754 B / `wc -l` = 22 / 14 键 / mtime 2026-07-11 08:58:13**（远早于 430/431 全部工作），本批**未改一字**。
- **版本控制**：未跟踪——`git check-ignore -v .env .env.local` → `.gitignore:12:.env` / `.gitignore:13:.env.local`（另有 `:14` `.env.*.local`）；`git ls-files .env .env.local .env.example` → **仅 `.env.example`**。
- **零消费方三证**：① `grep -rIn -F '.env.local'` 扫 `backend` / `frontend` / `scripts` / `docker-compose.yml` + 两个启动器 plist + `Makefile` → **0 命中**（全仓其余命中全在文档/方案/沟通记录）；② `docker-compose.yml` **无 `env_file:`**，且只自动读 `.env`；③ 全仓 `load_dotenv*` 仅 4 处（`backend/app/__init__.py`、`backend/sync_data.py`、`backend/data_daemon.py`、`backend/app/config.py` 注释）**全部加载 `.env`**；`backend/run.py` 自身不调 dotenv；**项目根无 `run.py`**；frontend 无 `.env*`、无 `envDir`。
- **键集实测**：**9 键与 `.env` 同名 / 5 键独有**；同名 9 键中 **5 键值不同**＝`DATABASE_URL` / `DEEPSEEK_API_KEY` / `FLASK_ENV` / `LLM_PROVIDER` / `SECRET_KEY`（**覆盖风险核心**）；同名 4 键值相同＝`DATA_DIR` / `DEEPSEEK_MODEL` / `LLM_WIKI_API_TOKEN` / `TUSHARE_TOKEN`（后两者为**真实凭据的重复副本**）；独有 5 键＝`DEEPSEEK_BASE_URL` / `FLASK_APP` / `LM_STUDIO_ENDPOINT` / `LM_STUDIO_MODEL` / `REDIS_URL`（`REDIS_URL` 在 **`.env.local:4`**）；**无空值键**。
- **来源（设计意图已死）**：162/164 号记载「`.env` 供 `docker-compose.yml` 生产部署、`.env.local` 供本地 `python run.py` 开发调试」，但**该消费者（项目根 `run.py`）从未存在** ⇒ 纯静态残留。
- **431 §9.2 原口径的成因（三处订正共用）**：原记「10 键与 `.env` 同名 / 独有 3 键」——"10"在 v1.3 时点（`.env` 尚含 `REDIS_URL`）**成立**，§014 从 `.env` 删该键后降为 **9**；原列举漏列 `DEEPSEEK_BASE_URL`，故 10+3=13 与 14 键不符 ⇒ 属**列举不全，非计数错**；**「一旦被接线即覆盖运行态同名键」的主判断不受影响**。
- **附带发现（本批登记）**：`项目整体评估与升级建议.md:267`（§2.8.1）记「`.env` 和 `.env.local` 都在版本控制中，包含敏感 Token（TUSHARE_TOKEN）」并标 🔴 高——**今日为假**，且 164 号当时已写明「两个文件均已加入 `.gitignore`」⇒ **自始即为误述**。

### 实施内容（3 个文档对象，零文件本体改动）

| # | 对象 | 改动 |
|:--:|---|---|
| 1 | `002-方案存档/431-…md` | ① §9.2 标题「——待议」→「**——已决：保留不动**」；② 「独有键 + 10 同名」条重写为**键集实测**（9 同名 / 5 独有 / 5 键值不同 / 4 键值相同 / 无空值）＋**成因订正说明**；③ 新增 **消费方复验三证**、**来源**、**版本控制状态** 三条；④ 原「处置建议」条 → **§015 处置状态 blockquote**；⑤ **新增 §9.4**（登记 `:267` 陈旧断言，**仅登记、不改该文档本体**，与 §9.3「不改 B1 本体」同理）；⑥ 「v1.3 增补」blockquote 同处加注 §015 订正；⑦ 处置进度表新增 `§015` ✅ 行、**删原 `.env.local` ⬜ 行**（该去向已决） |
| 2 | `002-方案存档/001-目录索引.md` | 431 行「落地进度」补 §015（保留不动 + 键数订正 + §9.4）；尾句「另 `.env.local` 去向待议」→「`.env.local` 去向已决＝保留不动；§9.4 处置建议待定」 |
| 3 | `001-沟通记录/2026-09-13.md` + `001-沟通记录/沟通索引.md` | 补记 §015 |

### 验证证据

| 项 | 证据 |
|---|---|
| `.env.local` **未被触碰** | `wc -l` **22** / **754 B** / **14 键** / mtime **2026-07-11 08:58:13**（与改前逐位一致） |
| 零消费方 | 代码/配置/脚本 grep **0 命中**；compose **无 `env_file:`**；`load_dotenv*` 4 处**全部指向 `.env`** |
| 键集口径 | 探针（**只打印键名/计数/布尔，不打印任何值**）：同名 **9**、同名值不同 **5**、同名值相同 **4**、独有 **5**、空值 **0** |
| 版本控制 | `git ls-files .env .env.local .env.example` → 仅 `.env.example`；`git check-ignore -v` → `.gitignore:12` / `:13` |
| 431 行数 | `wc -l` **556 → 574**（§9.2 扩写 + 新增 §9.4 + 处置进度 `§015` 行 − `.env.local` ⬜ 行）；`awk 'END{print NR}'` / `grep -c ''` 三法同为 **574**；结构锚点 §九 `:492` / §9.1 `:496` / §9.2 `:516` / §9.3 `:533` / §9.4 `:546` / 处置进度 `:560` |
| 未动 | `项目整体评估与升级建议.md`（`:267` 仅登记，`:268`/`:269` 未复核）；`.env` / `.env.example` / `docker-compose.yml` / plist / `backend/app/config.py` / `_项目运行手册.md` |
| git 状态 | 本批**未新增受控改动**——`git status --porcelain` 中 ` M` 集合与 §014 时逐项一致（另含 §013 的 ` M .env.example`）；431 与 `001-目录索引.md`… |

### 需知悉的三点

1. **本批零文件本体改动**：决策＝「保留不动」，故 `.env.local` 连同 `.env` / `.env.example` / compose / plist **一律未碰**；改动全部落在**文档措辞**（431 键数口径 + §9.4 新增 + 索引/沟通记录）。
2. **「保留不动」不等于「隐患已消除」**：该文件仍含 **5 个值不同的同名键**（`DATABASE_URL` / `DEEPSEEK_API_KEY` / `FLASK_ENV` / `LLM_PROVIDER` / `SECRET_KEY`）与 **2 个真实凭据重复副本**（`LLM_WIKI_API_TOKEN` / `TUSHARE_TOKEN`）。风险仅在**将来有人给 `.env.local` 接上加载路径**时才显现（当前零消费方已在 431 §9.2 留证）；将来若清理，须先确认无历史脚本或本地流程依赖。
3. **§9.4 是「登记」而非「处置」**：`项目整体评估与升级建议.md:267` 的陈旧断言已被计入 431，但**该文档本体一字未改**（与 §9.3 不改 B1 本体同理），其处置建议仍为**待定**；另 `:268`（松散脚本重复）/ `:269`（无 `Makefile`）两条本批**未复核**。431 **仍为 v1.3**（fix 批次不 bump 版本），`001-目录索引.md` 的 431 行状态保持 `📋 核查存档（v1.3，按批次落地中）`。

**产出**：`002-方案存档/431-430号遗留项核查清单与处置建议.md`（§9.2 改判为「已决：保留不动」+ 键集实测与成因订正 + §015 处置状态 + **新增 §9.4** + 处置进度 `§015` 行，**仍 v1.3**，`wc -l` **574**）+ `002-方案存档/001-目录索引.md`（431 行落地进度）+ `001-沟通记录/2026-09-13.md` §015 + `001-沟通记录/沟通索引.md`。**未改** `.env.local`（仍 22 行 / 14 键 / 754 B / 7月11日 08:58）/ `.env` / `.env.example` / `docker-compose.yml` / 启动器 plist / `backend/app/config.py` / `项目整体评估与升级建议.md` / `_项目运行手册.md`。**§九 三项存量事项现状**：§9.1 残留 7 键**已闭环（§013/§014）**、§9.2 `.env.local` 去向**已决＝保留不动（§015）**、§9.3 仅登记不改本体、**§9.4 新增待定**。剩余待决策：**E1 / F2 / F4 / G1** 与 §9.4 的处置建议。下一步：**待定**。

---

## 016 — 431号批次11 落地（§9.4 `项目整体评估与升级建议.md` §2.8 陈旧断言：决策＝**段落级订正注记**）

**批次口径**（用户选定「下一批次：§9.4 处置建议（纯文档）」）：本批**先做只读复核**——重查 `项目整体评估与升级建议.md` 的 §2.8 一整段（`:267` ＋ 相邻 `:268`/`:269`），再给处置口径选项，**复核阶段不动该文档本体**。

### 复核结论（只读，先于任何改动）

- **目标文档性质**：头载「评估日期：**2026-05-28**／评估范围／评估方法」，属**有日期的审计快照**（与 B1／JUD 报告同类）；改动前 `wc -l` = **377**、受 git 跟踪、工作区**干净（无 ` M`）**。
- **§2.8 全表六条逐条复核 ⇒ 五条不成立、一条数量失真**：

| # | 原文断言 | 复核实况 | 判定 |
|:--:|---|---|:--:|
| 2.8.1 | `.env` 和 `.env.local` 都在版本控制中（含敏感 Token） | 两者均被 `.gitignore:12` / `:13` 覆盖；`git ls-files` 仅跟踪 `.env.example` | **不成立** |
| 2.8.2 | 根目录有 **6 个** `chanlun_*.py` 和 `sync_data.py`，与 `backend/` 重复 | 根目录 `chanlun_*.py` 实为 **3 个** ＋ `sync_data.py`；与 `backend/` 同名者 2 个且**内容已分叉** | **数量失真** |
| 2.8.3 | 没有 `Makefile` | 根目录 `Makefile` 已存在（138 行，20+ 目标） | **不成立** |
| 2.8.4 | `backend/tests/test_core.py` 存在但为空 | 该文件 **10593 B** 非空 | **不成立** |
| 2.8.5 | compose 5000→5001 与 `run.py` 5001 内外不一致 | compose 现为 `"5001:5001"`，与 `run.py:35` `default=5001` 一致 | **不成立** |
| 2.8.6 | `CacheManager` 与 `EnhancedCacheManager` 并存 | `backend/app` 下已无 `class CacheManager`，仅 `EnhancedCacheManager` + `memory_cache.py` | **不成立** |

- **顺带发现（同文档）**：§2.8「建议」两条已完成（`.env` 入 `.gitignore` ＋ `.env.example`；`Makefile` 已建）；**Phase 1** 的 **1.4**、**1.6** 亦已实施（`run.py:37` 改为 `FLASK_ENV` 驱动的 `debug_mode`）；1.1–1.3 / 1.5 / 1.7 **未复核**。
- **2.8.1「自始即误」取证**：`.gitignore` 自 **2026-05-28 首次提交**即含 `.env` / `.env.local`（`git log -S'.env.local' -- .gitignore` 仅命中初始提交 `b02ba50`，与本报告评估日期**同日**）；`002-方案存档/164-…md:126` 当时已写明「两个文件均已加入 `.gitignore`」⇒ 非后续漂移。
- **范围校正**：§015 登记的 `:267` 实为 §2.8 表头**分隔行**（`|---|------|---------|`），六条断言实际在 **`:268`–`:273`**；本批复核范围已扩至整段而非单行。

### 口径选项与用户选择

对话框给出 4 项：**A｜段落级「订正注记」（推荐）**／B｜本库不改、全量归档进 431／C｜维持现状（仅 `:267` 登记）／D｜重写 §2.8 本体表格。**用户选择＝A**。

### 实施内容（目标文档 2 处，均**仅追加、不改原文**）

| # | 对象 | 改动 |
|:--:|---|---|
| 1 | `项目整体评估与升级建议.md` | 头部「评估日期／范围／方法」之后**新增一行「阅读须知」**（`:6`）：声明本文为 **2026-05-28 评估快照**、原文一律保留不改、各节变化另附带日期的「订正注记」，阅读以注记为准 |
| 2 | 同上 | §2.8「建议」块之后**追加 `> **订正注记（2026-09-13）**` blockquote**（`:281` 起）：六条逐条复核表 + 「建议」两条已完成 + Phase 1 1.4/1.6 顺带发现 + 指向 431 §9.4。**原表格 6 行（`:268`–`:273`）、建议 4 行（`:276`–`:279`）、Phase 1 任务表一律未改** |
| 3 | `002-方案存档/431-…md` | ① §9.4 标题改判「**已决：段落级订正注记**」（`:546`）；② 正文补入六条复核表 + 结论（五条不成立／2.8.2 数量失真且实质部分尚待处置）+ 顺带发现 + 性质取证 + 归属说明；③ 原「处置建议待定」→ **`§016 处置状态` blockquote**；④ 处置进度表新增 `§016` ✅ 行（`:588`） |
| 4 | `002-方案存档/001-目录索引.md` | 431 行「落地进度」补 §016；尾句 §9.4 由「处置建议待定」→「已决＝段落级订正注记」 |
| 5 | `001-沟通记录/2026-09-13.md` + `001-沟通记录/沟通索引.md` | 补记 §016 |

### 验证证据

| 项 | 证据 |
|---|---|
| **目标文档原文未改（append-only）** | `git diff --numstat -- 项目整体评估与升级建议.md` = **`14  0`**（14 增 / **0 删**）；`git diff \| grep -c '^-[^-]'` = **0** |
| 原表格原样 | `sed -n '268,273p'` 六条断言与「建议」4 行（`:276`–`:279`）逐字未动 |
| 行数 | `项目整体评估与升级建议.md` `wc -l` **377 → 391**（`git show HEAD:… \| wc -l` = 377）；**431** `wc -l` **574 → 589** |
| 结构锚点（目标文档） | 阅读须知 `:6` / §2.8 `:262` / 表格 `:268`–`:273` / 「建议」`:275` / 订正注记 `:281` |
| 结构锚点（431） | §九 `:492` / §9.1 `:496` / §9.2 `:516` / §9.3 `:533` / **§9.4 `:546`** / 处置进度 `:574` / §015 `:587` / `§016` `:588` |
| 取证 | `git log -S'.env.local' -- .gitignore` 仅初始提交（2026-05-28） |
| git 状态 | 新增受控改动 ` M 项目整体评估与升级建议.md` |

### 需知悉三点

1. **本批首次改动 `项目整体评估与升级建议.md` 本体**：§015 的口径是「**仅登记、不改本体**」，本次经用户**明确决策**放宽为「段落级订正注记」；但实施方式**严格只追加**（阅读须知 1 行 + 段末 blockquote），原审计证据（有日期的快照表格与建议）**完整保留**，删除行数为 **0**。
2. **2.8.2 的实质部分仍未处置**：根目录 3 个 `chanlun_*.py` ＋ `sync_data.py` 是否与 `backend/` 收敛（含 `backend/scripts/` 归置）属**新的改动项**，本批**只登记**于 431 §9.4「尚待处置」，**须另行决策**。
3. **431 仍为 v1.3**（fix 批次不 bump 版本；`001-目录索引.md` 的 431 行状态保持 `📋 核查存档（v1.3，按批次落地中）`）。另**订正 §015 记录的两处口径**：① `2026-09-13.md` 行数实为 **688**（§015 验证表内记 `689` 系读取工具显示值 = `wc -l` + 1）；② §015 git 状态单元格「本批**未新增受控改动**」不精确——`001-沟通索引.md` 与 `001-目录索引.md` 当批实为**两条受控 ` M`**。

**产出**：`项目整体评估与升级建议.md`（头部「阅读须知」＋ §2.8 段末订正注记，`wc -l` **377 → 391**）+ `002-方案存档/431-430号遗留项核查清单与处置建议.md`（§9.4 改判「已决：段落级订正注记」＋ 六条复核表 ＋ `§016` 处置状态 ＋ 处置进度 `§016` 行，**仍 v1.3**，`wc -l` **589**）+ `002-方案存档/001-目录索引.md`（431 行落地进度）+ `001-沟通记录/2026-09-13.md` §016 + `001-沟通记录/沟通索引.md`。**未改**：§2.8 原表格／建议／Phase 1 原文、`backend/app/**`、`.env` / `.env.local` / `.env.example`、`docker-compose.yml`、启动器 plist、`backend/docs/JUD环节代码实审报告.md`。**§九 现状**：§9.1 已闭环（§013/§014）、§9.2 已决＝保留不动（§015）、§9.3 仅登记不改本体、**§9.4 已决＝段落级订正注记（§016 落地）**。剩余待决策：**E1 / F2 / F4 / G1** ＋ **2.8.2 实质部分（根目录松散脚本收敛）**。下一步：**待定**。

---

## 017 — 431号批次12 落地（§2.8.2 实质部分——根目录松散脚本与 `backend/` 同名文件收敛：决策＝**分类处置**）

### 复核结论（只读，先于任何改动）

- **原文断言**：「根目录有 **6 个** `chanlun_*.py` 脚本和 `sync_data.py` 等松散脚本，与 `backend/` 下有重复」（`项目整体评估与升级建议.md:269`，§2.8.2）。
- **复核实况**：`chanlun_*.py` 实为 **3 个**；项目根**受控** `*.py` 共 **13 个（全部 TRACKED）**；「重复」实为 **4 组同名对**（3 组已分叉 ＋ 1 组同名无关）。
- **13 个根脚本两组冻结**：
  - **甲组 8 个** @ 2026-05-28 `81579fe`「Round 1: 关键 Bug 修复」：`bulk_sync.py` 6773 B/196 行、`chanlun_correct.py` 14024 B、`chanlun_demo.py` 10881 B、`chanlun_demo_simple.py` 11069 B/352 行、`init_duckdb_cache.py` 6657 B/172 行、`init_duckdb_simple.py` 5598 B、`scheduler.py` 6836 B/195 行、`sync_data.py` 4453 B/146 行。
  - **乙组 5 个** @ 2026-08-06 `1b2cc7f`「313/315/316方案实施」：`rebuild_snapshot.py` 901 B、`verify_311_v2.py` 18348 B/377 行、`verify_312_b1.py` 13632 B/326 行、`verify_312_impl.py` 4085 B/100 行、`verify_313_full.py` 3973 B/94 行。
- **4 组同名对**：① `sync_data.py` 4453 B/146（`Stock.query` / `DailyData.query` ＝ **PG ORM**）↔ `backend/sync_data.py` 2761 B/83（`DataManager()` 写 DuckDB；@ 2026-07-04 `e78b7df`）**已分叉**；② `bulk_sync.py` 6773 B/196 ↔ `backend/bulk_sync.py` 6732 B/193（DuckDB COUNT；@ 2026-07-07 `fdffee5`）**已分叉**；③ `chanlun_demo_simple.py` 11069 B/352 ↔ `backend/chanlun_demo_simple.py` 10889 B/348（`get_cached_daily_data()`；@ 2026-07-04 `e78b7df`）**已分叉**；④ `scheduler.py` 6836 B/195（`import schedule`、class `ScheduledTasks`）↔ `backend/app/scheduler.py` 14233 B/**396 行**（docstring「定时任务调度器 - Phase 4.5」、`python -m app.scheduler`、月度回测赢率、APScheduler `register_scheduler_jobs`）＝**同名无关**。
- **244 号迁移证据链（决定性）**：`e78b7df`（2026-07-04）stat **仅含 `backend/` 7 文件（130 insertions(+) / 640 deletions(-)）**，**根副本一律未被触碰** ⇒ 根副本为**迁移前僵尸**；`backend/chanlun_demo.py` 与 `backend/chanlun_correct.py` 已于 2026-06-29 `865fb54` 删除。244 号方案实体＝`002-方案存档/244-全局数据体系分层架构方案（规则C修订版）.md`（正文不含这些脚本名，迁移仅在 commit 消息留痕）。
- **引用面扫描 ⇒ 全仓零调用**：`*.{sh,command,bat,plist}`、`Makefile`、`AGENTS.md`、`_项目运行手册.md`、`*.{json,js,vue,ts,yml,yaml,toml,cfg,ini}` 全部 **0 命中**。**唯一活代码子进程调用**＝`backend/app/routes/cache.py:189` `script_path = os.path.join(current_dir, '..', '..', 'bulk_sync.py')`（`current_dir = os.path.dirname(os.path.abspath(__file__))`），自 `backend/app/routes/` 上溯两级 = **`backend/bulk_sync.py`（活版）**，配 `subprocess.run(cmd, capture_output=True, text=True, timeout=300)`。
- **僵尸程度**：根副本仍 `from app.models import Stock, DailyData` + `db` 走 ORM；`chanlun_demo.py:336` 与 `chanlun_correct.py:417` 硬编码旧路径 `/Users/kalence/Desktop/测试/01-A股股票分析系统/...`；`rebuild_snapshot.py` 为硬编码绝对路径一次性调试脚本（`sys.path.insert(0, '/Users/kalence/Desktop/01-A股股票分析系统/backend')`、`import data_daemon as dd`、`EnhancedCacheManager`、重建 `treemap_snapshot`）；`verify_311_v2.py` 用 `DB = "data/duckdb/stock_cache.db"`（相对 CWD，须自项目根运行）；`verify_313_full.py` docstring 自称「只写 `opportunity_tags_cache` 的 `signal_strength`/`potential_breakdown`」。
- **归置去向**：`backend/scripts/`（27 文件，含 `_426_*` 探针、`backfill_all.py`、`run_daily_sync.py`、`sync_adj_factor.py`、`sig_direct_test.py`、`sig_full_test.py`）与项目根 `scripts/`（5 文件）**均已存在**；`backend/scripts/` 下**无命名冲突**（`ls` 复验）。
- **活代码 chanlun 位置**（与根脚本无关）：`backend/app/engine/framework/{chanlun_config,chanlun_level_validator,chanlun_multi_level,chanlun_strategy}.py`。

### 口径选项与用户选择

- **处置口径**：对话框给出 4 项（**A 分类处置** / 归置 / 保留标注 / 维持现状 等），**用户选择＝「A 分类处置（推荐）」**。
- **附带产物**：追问 **「`chanlun_demo.png` / `chanlun_correct.png` 两张受控图片」→「一并删除（推荐）」**。
- **同名无关的根 `scheduler.py`**：追问 **「去向」→「删除（推荐）」**。
- 三问均取推荐项，无一项被取消；`ask_user_question` 本轮**未发生** `[Operation Cancelled]`。

### 实施内容（15 处，全部为删除与归置，**零业务代码改动**）

| # | 对象 | 改动 |
|:--:|---|---|
| 1 | 根目录 10 个受控文件 | **`git rm`**：3 组已分叉同名对根副本（`sync_data.py` / `bulk_sync.py` / `chanlun_demo_simple.py`）＋ 同名无关的根 `scheduler.py` ＋ 废弃 demo／初始化脚本 4 个（`chanlun_demo.py` / `chanlun_correct.py` / `init_duckdb_cache.py` / `init_duckdb_simple.py`）＋ 受控图片 2 个（`chanlun_demo.png` / `chanlun_correct.png`） |
| 2 | 根目录 5 个受控脚本 | **`git mv` 至 `backend/scripts/`**：`rebuild_snapshot.py`（901 B）、`verify_311_v2.py`（18348 B）、`verify_312_b1.py`（13632 B）、`verify_312_impl.py`（4085 B）、`verify_313_full.py`（3973 B） |
| 3 | 同上其中 2 个 | **就地各改 1 行**：`sys.path.insert(0, os.path.join(os.path.dirname(__file__), "backend"))` → `".."`（尾注 `# backend/`）。理由：迁移后原路径解析为**不存在的 `backend/scripts/backend`**。**其余 3 个脚本内容一字未改** |
| 4 | `002-方案存档/431-…md` | ① §9.4 结论「**尚待处置**」→「**已决（§017 分类处置，已落地）**」（`:561`）；② 新增 **`§017 处置状态` blockquote**（`:568`）；③ 处置进度表新增 `§017` ✅ 行（`:591`） |
| 5 | `002-方案存档/001-目录索引.md` | 431 行「落地进度」补 §017；尾句补「§9.4 2.8.2 实质部分已决＝分类处置」（431 行状态仍 `📋 核查存档（v1.3，按批次落地中）`，**未升版**） |
| 6 | `001-沟通记录/2026-09-13.md` + `001-沟通记录/沟通索引.md` | 补记 §017 |

### 验证证据

| 项 | 证据 |
|---|---|
| **本批受控改动** | `git status --porcelain` 本批范围 = **10 `D` + 5 `R`（其中 2 个为 `RM`，即含内容改动）**；未混入用户自有改动 |
| **根目录无残留** | 根目录 `ls *.py` → `No such file or directory`（13 个受控脚本已全部处置） |
| **归置就位** | `backend/scripts/` 下 5 个文件，体积 901 / 18348 / 13632 / 4085 / 3973 B |
| **语法检查** | `./backend/.venv/bin/python -m py_compile backend/scripts/verify_312_impl.py backend/scripts/verify_313_full.py` → **OK** |
| **未动** | 根目录剩 3 个 PNG（`after_analysis.png` / `indicator_ide_initial.png` / `test_screenshot.png`）经核为 **untracked（用户自有）**，本批**未动** |
| 行数 | **431** `wc -l` **589 → 592**；**`项目整体评估与升级建议.md` 仍 391**（本批一字未改）；`001-目录索引.md` **251**；`001-沟通记录/沟通索引.md` **3958 → 3961** |
| 结构锚点（431） | §9.4 标题 `:546` / 结论 `:561` / §016 blockquote `:566` / **§017 blockquote `:568`** / 处置进度 `:576` / §016 行 `:590` / **§017 行 `:591`** / 末行 `:592` |
| git 状态 | 新增受控改动 = 10 `D` ＋ 5 `R`（根脚本删除与归置）；431 与 `001-目录索引.md` 仍 `??` |

### 需知悉三点

1. **本批是「删除 + 归置」，非重构**：15 处动作**零业务代码改动**，**唯一内容改动**即上表第 3 项那 **2 行 `sys.path`**（属迁移的必要配套，已在 431 §017 与本记录**显式披露**）。
2. **归置后的 3 个脚本仍须自项目根调用**：`verify_311_v2.py`（`DB` 为相对 CWD 的 `data/duckdb/stock_cache.db`）与 `rebuild_snapshot.py`（硬编码绝对路径）内容未改，`verify_312_b1.py` 亦然。
3. **431 仍为 v1.3**：**fix 批次按约定不 bump 版本**，本批仅登记处置进度；`001-目录索引.md` 的 431 行状态保持 `📋 核查存档（v1.3，按批次落地中）`。

**产出**：根目录 13 个受控松散脚本**全部收敛**（10 删 / 5 归置至 `backend/scripts/`，其中 2 个各改 1 行 `sys.path`）＋ `002-方案存档/431-430号遗留项核查清单与处置建议.md`（§9.4 结论改判「已决（§017 分类处置，已落地）」＋ `§017` 处置状态 ＋ 处置进度 `§017` 行，**仍 v1.3**，`wc -l` **592**）＋ `002-方案存档/001-目录索引.md`（431 行落地进度）＋ `001-沟通记录/2026-09-13.md` §017 ＋ `001-沟通记录/沟通索引.md`。**未改**：`项目整体评估与升级建议.md`（仍 391 行）、`backend/app/**`、`.env` / `.env.local` / `.env.example`、`docker-compose.yml`、启动器 plist、`backend/docs/JUD环节代码实审报告.md`、`003-代码备份/`、`backend/$TMPDIR/`。**§九 现状**：§9.1 残留 7 键**已闭环（§013/§014）**、§9.2 `.env.local` 去向**已决＝保留不动（§015）**、§9.3 仅登记不改本体、**§9.4 已决＝段落级订正注记（§016）＋ 2.8.2 实质部分已决＝分类处置（§017 落地）**。剩余待决策：**E1 / F2 / F4 / G1**。下一步：**待定**。

---

## 018 — 431号批次13 落地（F2 ＋ G1 同源：决策＝**标注 + 最小真冗余收敛**）

### 复核结论（只读，先于任何改动）

- **F2（维度引擎零配置引用）**：`backend/app/opportunity_atlas/dimensions/` **11 文件**逐一扫描 —— **无 `import yaml`**、**无 `app.services.status_config`**、**无 `get_status_engine_config` / `get_signal_registry` / `get_strategy_config` 调用**。唯一「配置类」读取是 `dim7_valuation_engine.py` 的 `os.getenv`（env 变量，**非 yaml**）。
- **真接线面（live 消费方）**：
  - `status_engine.yaml`：`arbiter._load_thresholds()` 读 `consensus.{enter_threshold,bearish_strong}`；`arbiter._load_conflict_rules()` 读 `conflict_rules.*`；`StatusEngine._apply_l0` 读 `l0.hard_risks` / `l0.soft_risk_coeff`（7 键） / `l0.emotion_position_cap`（5 档） / `l0.hold_only_stages`。
  - `signal_registry.yaml`：**仅** `*.lifecycle.{initial,extended}.dist_pct` 被读（`status_engine.py` / `advice_builder.py` / `advice_engine.py`）。
  - `strategy_defaults.yaml`：仅 `chip_distribution` 被 `app/engine/pipeline.py:97-98` 读 ⇒ **部分接线**。
- **死字段（消费方 0）**：`*.trigger`、`*.verify_days`、`*.verify_rule`、`*.lifecycle.*.days`。`status_engine.yaml` 中**已无 `dimension_weights` 键**（本批**未复活**）。
- **G1 六组定位**：#1 `cross_validate.py` `_build_operation_advice` 软系数块（**双方 live**）；#2 `weight_engine.py:14 STATIC_WEIGHTS`（**整模块零消费方＝死码**，仅 `dim_adapter.py:368` docstring 文字提及）；#3 同模块 `EMOTION_MULTIPLIERS`（**无任何对应物**）；#4 `status_engine.py:570 MARKET_REGIME_WEIGHTS`（类属性，**live 权威**，消费于 `StatusEngine._aggregate` / `_aggregate_v390`；与 #2 **逐字节相同**，实测 `==` → `True`）；#5 `QUALITY_ADJUST` 双份；#6 dim5 阈值组（**无 yaml 对应物**）。
- **本批复核新发现（新A–新E）**：新A `LEVEL_WEIGHTS`（`dim2` :148-152 ≡ `chanlun_level_validator.py:25-29`）；新B `dim2_structure_engine.py:29-108` 内联整份 `chanlun_config.py`（注释 `# === chanlun_config.py ===`）；新C `_DIM_WEIGHTS` `{"chip":3.0,"fund":3.0,"stage":2.5,"asr":2.0,"trend":1.5,"ssrp":2.5,"chan":2.0}`（`dim4` ≡ `phase_detector`）；**新D `INDUSTRY_CATEGORY` / `CATEGORY_WEIGHTS`**；**新E 潜力 4 常量**（`SENTIMENT_WEIGHT` / `EVENT_SCORE` / `TREND_SCORE` / `POTENTIAL_DIM_WEIGHTS`）。
- **同名异义（非重复，勿并）**：`potential_engine.py:25 DIM_WEIGHTS`（6 潜力维）↔ `dim8_summary_engine.py:155 DIM_WEIGHTS`（7 灯色维）。`_DIM_WEIGHTS` 双份的**宿主类同名**＝`PhaseDetectionEngine`（两份都叫这个，取值实测相等）。
- **配置加载权威入口**：`backend/config/__init__.py`（`load_yaml` `@lru_cache(maxsize=8)`；文件缺失返回 `{}`）；`app/services/status_config.py` 为**薄再导出壳**。

### 口径选项与用户选择

- **处置口径**：对话框给出 4 项，**用户选择＝「标注 + 最小真冗余收敛（推荐）」**（＝先把行号/权威源/引用关系在 431 与代码注释中写清（零行为变更），同时**只收敛「双方 live 的字节级重复」**；不动死代码、不动矩阵、不动 dim5）。
- **收敛范围**：追问 **「是否纳入本批复核新发现」→「含本批复核新发现（共 9 组）」**。
- 两问均取推荐项；`ask_user_question` 本轮**未发生** `[Operation Cancelled]`。

### 实施内容（收敛 4 组 ＋ 标注 8 处 ＋ 登记）

| # | 对象 | 改动 |
|:--:|---|---|
| 1 | `cross_validate.py` `_build_operation_advice`（软系数块） | 7 处字面量 → **读 `status_engine.yaml l0.soft_risk_coeff`**（新增 `from app.services.status_config import get_status_engine_config`；与 `StatusEngine._apply_l0` **同源读法**）；字面量**降级为配置缺失兜底**（与 yaml 现值一致）。**未动**同方法内 `state == 'light'` 的 `× 0.5`（opportunity_state 语义）及另两处 |
| 2 | `dim7_valuation_engine.py` | **删 8 个逐字节重复常量** → **单向 import**：`CN_10Y_BOND_YIELD_PCT` / `QUALITY_ADJUST` / `INDUSTRY_CATEGORY` / `CATEGORY_WEIGHTS` ← `valuation_estimator`；`SENTIMENT_WEIGHT` / `EVENT_SCORE` / `TREND_SCORE` / `POTENTIAL_DIM_WEIGHTS` ← `potential_engine.DIM_WEIGHTS`。**保留**本模块独有的 `LEVEL_CN` / `LEVEL_LIGHT`（加注释「本模块独有，非重复；勿删」） |
| 3 | `weight_engine.py` | 模块 docstring 追加 **431 G1 标注**（整模块零消费方＝死码；`STATIC_WEIGHTS` ≡ `MARKET_REGIME_WEIGHTS`；`EMOTION_MULTIPLIERS` 无对应物）。**不删、不改值** |
| 4 | `status_engine.py::MARKET_REGIME_WEIGHTS` | 注释标注**唯一 live 权威**（消费于 `StatusEngine._aggregate` / `_aggregate_v390`），并指明 `weight_engine.STATIC_WEIGHTS` 是其逐字节相同的死码孪生。**不改值** |
| 5 | `dim5_emotion_engine.py` | 常量清单标注（`FAST_HIGH/LOW`、`SLOW_HIGH/LOW`、`PHASE_BASE_TEMP`、`TEMP_WEIGHTS`、`BANDWIDTH_TIGHT/NARROW`、`RANGE_TIGHT`、`CONSOLIDATION_MIN_DAYS`）：**无 yaml 对应物**、属引擎内部算法常量 ⇒ **仅标注，不收敛** |
| 6 | `dim2_structure_engine.py` | **2 处标注**：`# === chanlun_config.py ===` 区块（整份 vendored 副本 / 7 类中 6 类逐字节相同 / 仅 `BiConfig` 少 `@dataclass` / 两份均 live）；`class ChanlunLevelValidator`（**同源分叉** 23 行差异 / framework 份无 live 消费方） |
| 7 | `dim4_chip_fund_engine.py` ＋ `phase_detector.py` | `_DIM_WEIGHTS` **双份标注**（逐字节重复且**双方均 live**；收敛需先定权威源并改 import＝行为变更 ⇒ 仅标注） |
| 8 | `002-方案存档/431-…md` | ① 新增 **`§018` 处置状态 blockquote**（`:568` 位，紧随 §017）；② 处置进度表新增 `§018` ✅ 行；③ pending 行由 `E1 / F2 / F4 / G1` **收窄为 `E1 / F4`**（`:595`）；**仍 v1.3** |
| 9 | `001-沟通记录/2026-09-13.md` ＋ `001-沟通记录/沟通索引.md` | 补记 §018 |

### 验证证据

| 项 | 证据 |
|---|---|
| **ruff（E/F/W/I）** | 8 个改动文件 ＋ 目录级 `app/opportunity_atlas/ app/engine/framework/` 复扫 → **`All checks passed!`**（EXIT=0） |
| **常量等价（收敛前）** | 8 常量对权威源逐一比对 → **`identity=True` ＋ `equal=True`** |
| **行为断言 13 项** | A 组：真 yaml 结果与旧字面量**逐一相同** ⇒ **零行为变更**；B 组：哨兵 `0.111/0.222/0.333/0.444` **随 yaml 变化** ⇒ **真 yaml 驱动**；C 组：空配置回落 `0.5/0.7/0.3/0.8` ⇒ **兜底有效** |
| **反向依赖面** | `grep` `dim7`/`Dim7` 仅 **4 处**，且**全部只导入 `Dim7ValuationEngine` 类**（无模块级常量导入方） |
| **既有用例** | `tests/test_431_g2_config_loading.py` → **19 passed**；`tests/test_fix_315_316.py` **DB-free 子集 8/8 passed** |
| **矩阵/权重等价（标注前核验）** | `weight_engine.STATIC_WEIGHTS == StatusEngine.MARKET_REGIME_WEIGHTS` → **True**；`dim4.PhaseDetectionEngine._DIM_WEIGHTS == phase_detector.PhaseDetectionEngine._DIM_WEIGHTS` → **True** |
| **dim2 精确化依据** | `inspect.getsource` ＋ `difflib.unified_diff` 实测：`chanlun_config` 7 类 **6 类 IDENTICAL**、`BiConfig` DIFF（少 `@dataclass`）；`ChanlunLevelValidator` **23 行差异**（added 16 / removed 7：多级别重采样改动态 `agg_dict`、`amount` 列可选、月度 `'ME'` 取代已废弃 `'M'`）；`dim2` 内亦内联 `ChanlunAnalyzer`（`:2548`）⇒ **整份 vendored 副本** |
| **import 冒烟** | 8 个改动模块逐一 import → **IMPORT_OK**（无循环导入） |
| 行数 | **431** `wc -l` **592 → 595**（read_file 显示 596，勿采信）；`weight_engine.py` **374**、`status_engine.py` **1054**、`dim5` **567**、`dim2` **4000**、`dim4` **6030**、`phase_detector` **813**、`dim7` **1019**、`cross_validate.py` **1884** |
| git 状态 | 本批受控改动 = `M cross_validate.py` / `M dim7_valuation_engine.py` / `M weight_engine.py` / `M status_engine.py` / `M dim5` / `M dim2` / `M dim4` / `M phase_detector.py` ＋ 431 与两份沟通记录；**未混入用户自有未提交改动**（`R config/signal_registry.yaml`、`D config/status_engine.yaml`、`.env.example`、`backfill_all.py`、`?? backend/scripts/_426_*.py` 等一律未触碰） |

### 需知悉三点

1. **本批对 431 原判有 2 处「精确化」（属纠错，非新发现）**：(a) 431 原判 dim2 的 `ChanlunLevelValidator` 与 framework 份「逐字节重复」——实测为**同源分叉（23 行差异）**，dim2 份较新；framework 份**无 live 消费方**（仅被 `003-代码备份/` 引用）。(b) `dim2_structure_engine.py` 实为缠论框架的**整份 vendored 副本**（config ＋ level_validator ＋ `ChanlunAnalyzer` ＋ multi_level），其中 `chanlun_config` 的 `BiConfig` **少一个 `@dataclass` 装饰器**（dim2 份为普通类）⇒ 已在 431 记为「**已另立待议，不在本批处置范围**」（**尚未开号**）。
2. **环境阻塞（非本批引入）**：`tests/test_319_valuation_consistency.py::test_gate_level_low_with_high_pe_pct_no_valuation_risk` 与 `test_fix_315_316.py` 的 `test_f2_*` 等 DB 型用例，在 **`data_daemon.py`（pid 1151）持有开发态共享库**时挂起于 `EnhancedCacheManager._execute`（`faulthandler` 栈为证：`_evaluate_gate` → `_get_dm` → `get_ecm_instance` → `_init_tables` → `_execute`）。**未 kill daemon**（共享状态，需用户授权）；DB-free 子集**全绿**。
3. **431 仍为 v1.3**：**fix 批次按约定不 bump 版本**，本批仅登记处置进度；`001-目录索引.md` 的 431 行状态保持 `📋 核查存档（v1.3，按批次落地中）`。

**产出**：`backend/app/opportunity_atlas/cross_validate.py`（软系数块 yaml 驱动）＋ `dimensions/dim7_valuation_engine.py`（8 常量改单向导入）＋ 6 文件 8 处 431 G1 标注（`weight_engine.py` / `status_engine.py` / `dim5_emotion_engine.py` / `dim2_structure_engine.py` ×2 / `dim4_chip_fund_engine.py` / `phase_detector.py`）＋ `002-方案存档/431-430号遗留项核查清单与处置建议.md`（`§018` 处置状态 ＋ 处置进度 `§018` 行 ＋ pending 收窄为 `E1 / F4`，**仍 v1.3**，`wc -l` **595**）＋ `001-沟通记录/2026-09-13.md` §018 ＋ `001-沟通记录/沟通索引.md`。**未改**：`weight_engine.py` 一个字符（仅 docstring 注释）、dim1–8 的配置引用面（**未接线 yaml**）、`dim5` 常量值、`MARKET_REGIME_WEIGHTS` 取值、`dimension_weights` 键、`项目整体评估与升级建议.md`（仍 391 行）、`.env` / `.env.local` / `.env.example`、`docker-compose.yml`、启动器 plist、`backend/docs/JUD环节代码实审报告.md`、`003-代码备份/`、`backend/$TMPDIR/`、**E1 / F4**。**§九 现状**：§9.1 已闭环、§9.2 已决＝保留不动、§9.3 仅登记不改本体、§9.4 已决（§016 ＋ §017 落地）⇒ **§九 全部条目均已处置**。剩余待决策：**E1 / F4**（另有待议：dim2 缠论 vendored 副本是否开号收敛）。下一步：**待定**。

## 019 — 431号批次15 落地（E1 就地部分收敛 + F4 删死码登记）：potential_engine 写侧查询改分库 + dim6 死码清除

用户指示**继续执行批次15（431 号未完成工作）**——① 代码改动（唯一剩余）：potential_engine E1 收敛 5 处（dim6 侧 F4 删死码 + 签名收敛已于 2026-09-13 完成并验证，勿重做）；② 431 精确化登记（dim6 F4 完成状态 + E1 收敛，**431 保持 v1.3 不 bump**）；③ 文档登记（`2026-09-13.md` §019 ＋ 沟通索引 ＋ `001-目录索引`）；④ 只在报告中提出、待单独决策（本批不动）。

### 实施内容（①代码 5 处 ＋ ②431 登记 7 处 ＋ ③文档 3 处）

| # | 对象 | 改动 |
|:--:|---|---|
| P1 | `potential_engine.py` `recompute_ic_weights` 签名 | 删 `sample_size: int = 500` 死参数（函数体零引用、全仓仅此一处；`recompute_ic_weights`/`save_ic_weights` 仍零调用点 ⇒ 删参零风险） |
| P2 | 同上 `dates` 查询（原 `:298`） | `_query_df` → `_query_shard('daily_cache', ...)` ＋ 「356号分库」标记注释（daily_cache 属 `market_cache.db`） |
| P3 | 同上 `px`/`px10`/`basic`/`mf`（原 `:312/:314/:316/:318`） | 4 处 `_query_df` → `_query_shard`（`daily_cache`×2 / `daily_basic_cache`×1 / `moneyflow_cache`×1）＋ 汇总注释 |
| P4 | 同上 `px_prev`（原 `:327`） | `_query_df` → `_query_shard('daily_cache', ...)` ＋ 注释 |
| P5 | 同上 `roe_df`（原 `:340`） | `_query_df` → `_query_shard('fina_indicator_cache', ...)`（`financial_cache.db`）折行 ＋ 注释 |
| 6 | `002-方案存档/431-…md` | ① §三 E1 行（`:84`）问题摘要补「写侧 7 处查询走总库连接恒空（阻断性；§019 已收敛）」，建议列补「另立新功能号，§019 待决策」；② §三 F4 行（`:90`）建议列改「删死码并记录（§019 已落地）；修 T42 另议」；③ §四 E1 详情后新增**处置状态 blockquote**（批次14 精确化：7 处查询恒空阻断性；批次15 落地明细；分库实测行数 daily_cache 641 万 / daily_basic 92 万 / moneyflow 21 万 / fina 2.2 万；未动 4 项待决策）；④ §四 F4 详情后新增**处置状态 blockquote**（决策＝删死码并记录；6 处落地明细；验证证据）；⑤ §六「真功能/破坏性」表 E1 行加注「另立新功能号；写侧 7 处查询恒空已收敛，§019 落地」；⑥ §六「F1/F2/F4 与 G1/G2」表 F4 行改「删死码（§019 已落地）；修 T42 另议」；⑦ 处置进度表末行 `\| — \| E1 / F4 \| ⬜ 待确认 \|` → **§019 ✅ 已落地行**（仿 §018 格式） |
| 7 | `001-沟通记录/2026-09-13.md` ＋ `001-沟通记录/沟通索引.md` ＋ `002-方案存档/001-目录索引.md` | 补记 §019（本文件）＋ 索引条目 ＋ 431 条目落地进度更新 |

### 验证证据

| 项 | 证据 |
|---|---|
| **ruff** | `.venv/bin/ruff check app/opportunity_atlas/potential_engine.py` → **`All checks passed!`** |
| **py_compile** | `.venv/bin/python -m py_compile app/opportunity_atlas/potential_engine.py` → **COMPILE_OK** |
| **残留扫描** | `grep "_query_df\|_query_shard\|sample_size" potential_engine.py` → `recompute_ic_weights` 内 **7 处 `_query_shard`**（`:299/:316/:318/:320/:322/:332/:346`）、`_query_df` 仅存于历史注释说明文字、`sample_size` **0 命中** |
| **既有用例** | `tests/test_322_s7_stretch_fix.py -k "map_score"` → **5 passed, 1 deselected**（模块导入正常） |
| **行为断言（mock ecm）** | 空 `_query_shard` 返回 → `recompute_ic_weights(ecm)` 无签名错误、首查 `shard_calls == ['daily_cache']`、`_query_df` **零调用**、返回 `dict(DIM_WEIGHTS)`（早退兜底路径） |
| **dim6 F4（2026-09-13 已验）** | ruff `All checks passed!`；py_compile `COMPILE_OK`；`pytest tests/test_411_pipeline.py -k "Dim6" -q` → **3 passed, 27 deselected**；dim6 行数 **1261 → 1247** |
| 行数 | **431** `wc -l` **595 → 599**；`potential_engine.py` **415 → 421** |
| git 状态 | 本批受控改动 = `M backend/app/opportunity_atlas/potential_engine.py` ＋ 431 ＋ 两份沟通记录；**未混入用户自有未提交改动**（`R config/signal_registry.yaml`、`D config/status_engine.yaml`、`.env.example`、`backfill_all.py`、`?? backend/scripts/_426_*.py` 等一律未触碰） |

### 需知悉四点（均本批不动、待单独决策）

1. **IC 权重月度滚动重估作业**：E1 的写侧链路（`recompute_ic_weights` / `save_ic_weights` 零调用点、`data/ic_weights.json` 静态快照）**另立新功能号**——需为 `data_daemon` 新增月度滚动作业，含承载面评估：`scheduler_manager.py` / `scheduler.py` / `rule_scheduler.py`。
2. **`recompute_ic_weights` docstring「预测未来 10 日收益」与 `horizon=20` 不一致**：docstring 与代码 `d10 = dates_sorted[i + horizon]`（=20 交易日）口径矛盾，本次未改（涉及语义判断）。
3. **`_assess_risk_level` 中 `tr` 潜在 NameError**：try/except 之后仍有 `'高' if tr < 1.0` 引用，若 `tr` 在异常路径未赋值会 NameError——属 dim6 侧遗留，本次未动。
4. **dim2 缠论 vendored 副本是否开号**：431 §018 已记「另立待议」；候选号 **432 已被本会话 432 号 daemon 诊断占用**，开号需另择号。

**产出**：`backend/app/opportunity_atlas/potential_engine.py`（`recompute_ic_weights` 删 `sample_size` 死参 ＋ 7 处 `_query_df` → `_query_shard` ＋ 分库标记注释，415 → 421 行）＋ `002-方案存档/431-430号遗留项核查清单与处置建议.md`（§三 E1/F4 行 ＋ §四 E1/F4 处置状态 blockquote ＋ §六两处 ＋ 处置进度 `§019` ✅ 行，**仍 v1.3**，`wc -l` **599**）＋ `001-沟通记录/2026-09-13.md` §019 ＋ `001-沟通记录/沟通索引.md` ＋ `002-方案存档/001-目录索引.md`。**未改**：`data_daemon.py`（月度重估作业未做）、docstring「10 日收益」表述、`_assess_risk_level`、dim2 vendored 副本、`.env*`、`docker-compose.yml`、启动器 plist、`003-代码备份/`、`backend/$TMPDIR/`。**431 现状**：E1 查询恒空部分已收敛、F4 已删死码；剩余待决策＝**IC 月度重估作业（另立新功能号）** / docstring-horizon 口径 / `tr` NameError / dim2 开号。下一步：**待定**。
