# 2026-09-12 沟通记录

## 001 — 427号方案制定：全环节虚拟流程测试（真实数据沙盒重放）v1.0→v1.2

**用户输入**：评估利用真实数据开展虚拟 col/raw/stg/sig/jud/out 全环节流程测试的可行性，若可行制定模拟运行测试计划（对话框说明，不改方案和代码）；随后要求补入 COL 真实联网采集并更新 v1.1；再要求以"模拟真实生成环境"为标尺自查优化（v1.2）；最终确认按 v1.2 执行测试。

**AI 执行内容**（纯只读核查 + 方案文档，未改任何代码/生产数据）：

### 可行性评估（三项成立）
1. **数据完备**：全部核心表最新交易日 = 2026-09-11（daily 5,553 / pre_feat 5,550 / factor 44,216 / indicator 5,504 / market_stats 12 行真实值）；pipeline_status 09-11 15/15 done（实测）。
2. **隔离机制**：`DATA_DIR` 环境变量全路径接管（data_daemon.py:22/6015/6141/5874 + app/config.py:24）→ 克隆 data/ 25G（APFS `cp -c` 零拷贝）+ 注入 DATA_DIR 即得隔离沙盒。
3. **时机无冲突**：周六非交易日 + daemon/API 均停运 → 无写锁竞争、采集器不启动。

### 方案三版演进
- **v1.0**：沙盒重放方案（克隆 → 重置 pipeline_status → 启动 daemon 状态机自然推进 → 判定矩阵 PASS/FAIL/OBS）。
- **v1.1**（用户要求补 COL 联网采集）：§2.4 幂等性表级实测（全部 COL 目标表具 PK/唯一约束 + `INSERT OR REPLACE` → 重采无重复）；新增步骤 2.5 真实联网采集 09-11 日终数据（沙盒内）；C-5~C-8 采集有效性检查。
- **v1.2**（用户要求模拟真实生成环境自查）：对照 main() 启动时序/时段判定/run_daily_sync/QA 重算闭环逐项核查代码事实，发现 7 项保真度差距并修正：
  - F1 步骤 3 = 完整启动路径模拟（425 HIGH 闭环/回填跳过/未登记自检，新增 N-1~N-6 观察项）
  - F2 三级基线 B0/B1/B2（消除联网采集对 C-6 对比的干扰）
  - F3 可选步骤 3.5 run_daily_sync 评估模式（量比回写等计算类子任务）
  - F4 S-4 预期修正 + S-6 QA 重算双路径（RECOMPUTE_KEYS 不含 COL 表，实测确认）
  - F5 时段差异显式声明（maintenance vs settlement，E10）
  - F6 非交易日采集器跳过 = 验证点（N-5）
  - F7 可选步骤 3.5B 中断恢复测试（SIGKILL→重启→断点续跑，T-5）

**产出**：`002-方案存档/427-全环节虚拟流程测试方案（真实数据沙盒重放）.md`（v1.2，642 行）。全程零代码改动、零生产数据触碰。

---

## 002 — 427号方案执行（沙盒重放测试）+ 发现 3 项真实问题

**用户输入**：按 v1.2 执行测试；将问题记录到沟通记录后继续。

**AI 执行内容**：

### 执行进度（截至 20:31）
| 步骤 | 结果 |
|---|---|
| 0 预检 | ✅ 无进程、磁盘 592G、日志起点 90064 行 |
| 1 B0 基线 | ✅ 全表行数/日期/15 行 pipeline_status 落盘；QA-CHECK detail 实证"2项失败, 已调度补算 0 项, 问题: stk_limit_"（印证 S-6 双路径） |
| 2 沙盒克隆+重置 | ✅ `cp -c -R data data_virtual_test_20260912`（8 分库齐备）；pipeline_status 15/15 → pending |
| 2.5 联网采集 | ⚠️ 见问题③；concept 三级降级链正常（5568 行不变）；margin 降级写 09-10（4444 行，幂等）；COL-1~5 零写入无污染 |
| 3 启动重放 | 🔄 进行中（20:29:42 启动；COL-6 done 20:30:21；fina Series 错误 20:30:35-45；管道进入 RAW 阶段） |

### 发现的问题（3 项真实问题）
1. **【新复现缺陷】fina_indicator_cache Series 绑参错误（426 P2-2 未根治）**
   - 现象：daemon 启动消费 sync_requests（真实积压 76,030 条）触发 `_batch_fina_indicator` → `分库写入失败: fina_indicator_cache on financial_cache.db, ProgrammingError: Error binding parameter 29: type 'Series' is not supported`，20:30:35-45 约 50+ 次；随后仍输出"【财务指标】逐只同步完成，共 100 条"（**成功计数虚报**）。
   - 判定：426 号 §5.3 P2-2 点名问题（`cache_fina_indicator_data(latest)` iloc 切片 Series 绑参 + `_insert_from_df` 值类型未归一化 + 成功计数与写失败无关）**当前工作区代码仍触发**。真实 daemon 恢复后遇 76,030 条积压同样发生。→ FAIL 项，修复需另行方案。
2. **【遗留小缺陷】未登记表自检报 2 张在用表**
   - 现象：启动日志"未登记分库路由的表清单（2）: ['qa_audit_log', 'watchlist_status_diff']"。
   - 判定：两表实际写总库（qa_audit_log 14 行实测），但不在 `sharding_manager._table_to_db`（grep 零命中）→ 426 阶段二 S1/D8"未登记表零静默"验收（§7 验收 15）未完全闭环。→ OBS/低优先。
3. **【外部数据源】Tushare daily 系接口静默不可用**
   - 现象：步骤 2.5 采集 COL-1~5（daily/daily_basic/moneyflow/stk_limit/top_list）全部返回 0 行（0.06s 本地级），margin_detail 空；但 trade_cal（12 行）/stock_basic（5562 行）/index_daily（3 行）/adj_factor 复权补采（5562 条）/mootdx K线补采（5639 条）**均正常**。
   - 判定：token 有效（0ef90eac...56 字符），**日线行情类接口被服务端限制/降权**（非网络、非代码缺陷）；系统侧行为正确（`_ts` 超时不崩溃、概念三级降级链正常、幂等无污染、管道"数据完整跳过"保护生效）。→ 外部问题，记录观察；真实日终采集会受影响需关注。

### 启动段验证（N 系列）
- N-1 ✅ 完整性检查执行（K线深度补采 5639 条 / 复权补采 5562 条成功）
- N-2 ⚠️ 未登记表 2 张（见问题②）
- N-3 ✅ market_stats 恢复 8 个指标
- N-4 ✅ 指数日线（37 天）/资金流（40 天）回填跳过，基线未污染
- N-5 ✅ 非交易日采集器正确跳过
- N-6 ✅ sync_requests 每轮 50 条（429 限流生效）

### 当前状态
- 沙盒 daemon（PID 42658）后台运行，管道推进中（RAW 阶段，预计 ~14min → SIG ~11min → JUD ~7min → OUT → QA，总计 60~90min）。
- 待完成：步骤 4 结果判定（判定矩阵）、步骤 5 清理归档；输出 `427_report.txt`。

**产出**：`data_virtual_test_20260912_B0.txt`（基线）、沙盒 `data_virtual_test_20260912/`（重放中）。详见 **427号方案**（v1.2）。

---

## 003 — 427号测试完成：15/15 收敛 + 判定矩阵（24 PASS / 1 FAIL 外部 / 4 OBS）+ 4 项问题

**用户输入**：继续测试至管道跑完汇总结果。

**AI 执行内容**（沙盒重放全程 20:29:42 → 23:01:50，152 分钟，daemon PID 42658）：

### 全环节时间线（沙盒，本地时间）
| 环节 | 完成 | 耗时 | 备注 |
|---|---|---|---|
| COL-1~5 | 20:30:15 | 跳过 | 数据已完整（完整性判定 ✅） |
| COL-6 | 20:30:21 | 5.8s | 概念采集（Tushare→AKShare→stock_basic 三级降级） |
| COL-7 | 21:51:44 | **4879.9s** | 财务全量同步（fina_indicator 全失败仍 done） |
| RAW-1/2/3 | 22:39:40 | parallel | **RAW-2 超时 1800s 后强制 done**（见问题④） |
| RAW-2B | 22:41:11 | 49.5s | **sector_heat 补到 09-11**（109 行业，停更问题恢复） |
| SIG | 22:52:11 | 621.5s | 5553/5553（与生产 641s 持平） |
| JUD | 22:59:41 | 411.6s | 5550/5553 |
| OUT | 23:00:21 | 1.9s | 归档正常 |
| QA-CHECK | 23:01:50 | — | **15/15 done**（detail 与生产 B0 一致：stk_limit 2359/5275=42% 捕获+告警、补算 0 项） |

### 判定矩阵结果（24 PASS / 1 FAIL / 4 OBS）
- **426 修复回归全部确认**：R-1 market_stats 重算非常量且 ma20_ratio=0.2521 与 p3 权威值一致（P0-1 ✅）；R-2 indicator 三表 5504 对齐（P0-2 ✅）；R-4/5 pre_feat 5550/factor 44216 覆盖（P1-3 ✅）；S-4 QA 门禁捕获缺口不触发补算（P0-3 ✅）；C-2 幂等无重复；G/J/O 输出行数与基线全等。
- **R-6 新发现**：sector_heat 停更（09-07）在重放中**自动恢复**（RAW-2B 补到 09-11）→ E3 属调度时机问题，非代码缺陷。
- **唯一 FAIL = C-5**：步骤 2.5 联网采集 daily 系接口返回 0 行（**外部数据源 Tushare 不可用**，非系统缺陷）。

### 最终问题清单（4 项）
1. **【新复现缺陷·高】fina_indicator_cache Series 绑参（426 P2-2 未根治）**：全 COL-7 期间每批 100 只全失败 + 虚报"完成 100 条"；同步触发点=真实 sync_requests 积压 76,030 条。**真实 daemon 恢复后同样发生**。修复=426 §5.3 动作落地（`_insert_from_df` 值类型归一化 + period 参数修复）。
2. **【新发现·中】RAW-2 原料加工耗时 1714.6s（生产 815s 的 2.1 倍）+ 1800s 超时强制 done 掩盖**：数据虽完整（5550/5553 失败 0）但超时后 `mark_step_done` 无条件执行——**若超时线程数据不全也会被静默标记 done**（426 P0-4 续算机制隐患场景复现）；RAW-2 卡因待定位（疑似同库 compute 写锁竞争或单股 hang）。
3. **【外部】Tushare daily 系接口静默不可用**：daily/daily_basic/moneyflow/stk_limit/top_list/margin_detail 全空（0.06s），trade_cal/stock_basic/index_daily/adj_factor/mootdx 正常——token 有效、日线行情接口被服务端限制；概念三级降级链与幂等性表现正确。
4. **【遗留·低】未登记表自检报 qa_audit_log/watchlist_status_diff**（426 S1/D8 验收 15 未闭环）。

### 方案偏差（测试暴露，修正记录）
- **COL-7 重置连带 81min 财务全量同步**：v1.2 重置全部 15 行 pending → COL-7 真实执行（09-11 生产亦 6308s）——方案预期耗时 60-90min 低估，实际 152min；建议后续虚拟测试重置时排除 COL-7（标 done）或明确计入。
- **RAW 超时兜底**：RAW-2 线程 hang 时靠 1800s 超时强制推进，测试暴露"超时强制 done"语义风险。

### 产出
- 报告：`data/alerts/427_virtual_test_report.txt`（沙盒 `data_virtual_test_20260912/427_report.txt`）
- 沙盒保留 7 天供复查；生产数据全程零触碰
- 修复建议待用户决策（fina Series→428 号方案候选；COL-7 提效=增量跳过+Series 修复，方案 A period 批量已实测不可行）
