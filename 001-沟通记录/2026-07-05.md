# 2026-07-05 沟通纪要

## ### 1 - 选股系统全局数据连接及策略匹配连接审计

**用户输入**：结合项目中的方案文档检查选股系统的全局数据连接及相关策略匹配连接的有效性

**AI执行内容**：
- 读取了214号方案（选股系统后端开发技术规格书）和123号方案（选股系统与个股分析解耦重构方案）
- 审查了后端 `routes/screener.py`、`engine/framework/screener.py`、`engine/pipeline.py`、`data/__init__.py`、`enhanced_cache_manager.py` 等核心文件
- 审查了前端 `frontend/vue-project/src/views/screener/index.vue` 和 `screenerService.js`
- 审查了 `engine/framework/` 下所有策略文件（volume_price_strategy.py 3539行、chanlun_strategy.py 2642行、chip_strategy.py等）
- 发现 6 项问题，报告存档为 245 号方案

**核心发现**：
1. 🔴 L3策略验证是空壳（仅检查`len(df)>=60`），缠论/量价/因子三大策略均未接入
2. 🔴 API参数命名前后端不一致（camelCase vs snake_case）
3. 🔴 因子组合API路径与214号方案声明的`/api/v3/`前缀不符
4. 🟡 Vibe策略返回硬编码Mock
5. 🟡 ChipScorer是简化版而非真实筹码分析
6. 🟡 前端串行调用L1→L2→L3时未传递各层筛选结果

**整改执行**（245号方案审计后全量修复）：
1. ✅ 修复API参数命名：后端统一兼容 `stock_pool`/`stockPool`/`candidates`
2. ✅ 前端改用 `/api/v3/screener/run` 全流程端点，不再三层串行裸调
3. ✅ 新建 `screener_strategy_integration.py` 集成缠论+量价双引擎到L3
4. ✅ `compute_screening()` L3 现在使用真实策略评分，含 `strategy_detail`
5. ✅ `/layer3` 单层端点也改用真实策略
6. ✅ Vibe策略改为从 `strategy_templates_v2` 动态读取，DB无数据时回退默认
7. ✅ 新增 `GET /api/v3/screener/factor-combinations` 端点供选股页加载因子组合
8. ✅ 选股页 `mounted()` 时并行加载因子组合和Vibe策略
9. ✅ 更新214号方案文档，修正因子组合API路径

**复核结论**（全量通过）：
- ✅ 问题1 L3接入：`compute_screening()` 第176行调 `screen_l3_candidates()`，集成VolumePriceStrategy + ChanlunAnalyzer，输出214号格式strategy_detail
- ✅ 问题2 参数名：4个端点全部兼容 `stock_pool`/`stockPool`/`candidates`
- ✅ 问题3 因子路径：新增 `GET /api/v3/screener/factor-combinations`，214号文档已更新
- ✅ 问题4 Vibe策略：优先从 `strategy_templates_v2(vibe=True)` 读取，TODO已删除
- ✅ 问题5 ChipScorer V2重写：四维评分（成本位置+量能剖面+趋势健康+价格稳定），能区分建仓/洗盘/拉升/出货各阶段
- ✅ 问题6 前端串行：改用 `/run` 全流程端点，旧串行调用已删除
- ✅ L3降级安全：不再把L2筹码评分冒充L3结果，分级降级（逐策略重试→跳过→拒绝输出）
- ✅ 生产监控：L3引擎健康度指标可通过 `GET /api/v3/screener/stats` 查看
- ✅ 代码质量：`py_compile` + `ruff` — 3个修改文件 0 errors

---

### 2 - 全局数据体系全维度梳理（246号方案存档）

**用户输入**：在245号审计基础上，要求从获取渠道、获取方式、不同数据渠道的功能定位、数据管理、数据处理、数据存储、数据前后端支持等全维度进行一次系统性梳理

**AI执行内容**：
- 读取244号（全局数据体系分层架构）、239号（全局数据体系）、240号（稳定性方案）、241号（AKShare缓存层）、242号（架构深度分析）、243号（生产化方案）等6份全局数据体系相关方案文档
- 逐文件审查后端数据层：`data/`目录（DataManager、enhanced_cache_manager、akshare_collector、tushare_provider、akshare_provider）、`scheduler_manager.py`、`routes/screener.py`、`engine/framework/`下策略文件
- 产出246号方案（~600行/10章节）：全维度梳理报告

**核心发现（累计8项问题）**：
- **P0×3**:
  1. `sync_all_daily_data()` 无断点续传
  2. `catch_up_if_needed()` 仅查全局最新日期，不检查个股数据量
  3. DarwinRiskFilter 直调 Tushare HTTP API（红线违规R3）
- **P1×1**:
  4. `load_stock_data_batch()` 数据不足时静默跳过，无补采触发
- **P2×3**:
  5. moneyflow_cache 数据未被任何策略消费
  6. daily_basic_cache 大量字段未被策略消费
  7. market_context 参数未从调用方传递到评分函数
- **P3×1**:
  8. 财务数据无 DuckDB 缓存

**方案存档**：`002-方案存档/246-全局数据体系全维度梳理与审计发现.md`

### 3 - 全量问题修复（5项代码修改）

**用户指令**："请先将所有的问题包括之前对话中的问题进行全量的修复"

**实施范围**：5个文件修改，覆盖3个P0 + 1个P1 + 1个P2

**修复详情**：

**Fix 1 - P0: DarwinRiskFilter Tushare HTTP 直调** (`screener.py`)
- 移除 `_tushare_pro()` / `_fetch_stock_name()` / `_is_st_stock()` 三个函数
- `_filter_st` / `_filter_low_liquidity` / `_get_stock_name` 全部改为只通过 DataManager / Stock ORM 获取数据
- 清理 `requests` / `functools.lru_cache` / `dotenv` 依赖

**Fix 2 - P0: sync_all_daily_data() 断点续传** (`data/__init__.py`)
- 逐股检查 daily_cache 数据量：>120天跳过，不足的增量补齐，无数据全量同步
- 每100只 batch commit，支持 resume_from 参数

**Fix 3 - P0: catch_up_if_needed() 按个股数据量补采** (`scheduler_manager.py`)
- 保留全局日期检查
- 新增 `_replenish_sparse_stocks()` 查询 daily_cache 中数据量<120天的股票，按数据从少到多补齐（最多100只/轮）

**Fix 4 - P1: load_stock_data_batch() 按需补采** (`routes/screener.py`)
- 数据<lookback天时调用 dm.sync_daily_data() 按需同步，然后重试读取

**Fix 5 - P2: market_context 接入L3策略评分** (`screener_strategy_integration.py`)
- 新增 `_compute_index_condition()` / `_build_market_context()` / `_adjust_score_with_context()` 三函数
- 从 daily_basic_cache (turnover_rate) + moneyflow_cache (net_lg_amount) 拉取数据
- 构建大盘条件(GOOD/POOR/NORMAL)，调整缠论评分±0.3~0.5

**验证**：5个文件全部 Python 语法检查通过（ast.parse）✅

**未修复项（需用户决策）**：
- P2: moneyflow_cache 数据未被策略消费 — 需策略层实现
- P2: daily_basic_cache 大量字段未被策略消费 — 需策略层实现
- P3: 财务数据无 DuckDB 缓存 — 需DDL+同步管道
