# 2026-08-15 沟通纪要

### 001 - 332总纲待细化讨论项核对与332-1计划清单建立

**用户输入**：给出 08-14 梳理的 11 处待细化清单（A 类与 333 不同步 3 处 / B 类架构含糊 3 处 / C 类需决策 3 处 / D 类其他 2 处），说明 A 项已沟通完毕（333 号分档方案），要求核对与 AI 梳理的差异；随后要求建立临时编号 332-1 的待讨论计划清单于方案存档，整合讨论项（关联性/同模块合并）、制定讨论计划，后续逐项沟通并制定类似 333 号的分档方案。

**AI执行内容**：

1. **核对结论**（通读 332/333 v3.0 当前文件 + grep 精确核查 Vibe/引擎数表述）：
   - **已过时 4 项（非待办）**：A1（Vibe 散落 4 处）已随 332 v3.0 全同步（仅历史记录保留 Vibe 实测数据）；A2（P0 目标含 Vibe）P0④已写"Vibe 已剔除不纳入"；A3（"P2 消费标签"表述过时）**方向反转**——333 已 v3.0（016）确认"P2 先算→标签派生"是 v2.0 表述错误已废弃，正确方向即"P4 统一计算落标签→P2 消费标签"，与 332 v3.0 完全一致；D2（P0 标注含 Vibe 剔除）P0⑥已显式标注。
   - **一致 5 项**：B1（九维构成/因子归属）= 事项7、B3（成品仓载体）= 事项13、C1（共识阈值重校）= 事项9、C2（颜色保守化）= 事项12、D1（L0b 数值校准）= 事项3。
   - **用户比 AI 更细 2 项（AI 遗漏）**：B2（L0 时序位置：L0a 输入前过滤 vs L0b/c 输出侧标注）、C3（实时套算机制：接口/数据形态未设计）。
   - **AI 梳理中用户未覆盖 6 项**：S1 排期确认（frontmatter 明示）、L0a 造假/退市标签缺失（"无标签，暂缺"）、信号生命周期算法、conflict_evidence 规则与展示、treemap 快照字段来源同步、日频成品生成时机/范围。
   - **净结论**：13 项有效讨论项（7 项来自用户清单 + 6 项补入）。

2. **建立 332-1 待讨论计划清单**（`002-方案存档/332-1-待讨论计划清单（332总纲·分档讨论编排）.md`，临时编号不占正式序列）：
   - **13 项整合为 5 组**：组1 L0 风险分级细则（D1 数值校准 + L0a 标签缺口 + B2 时序位置）；组2 L1 九维构成与判定细则（B1 九维构成 + 信号生命周期算法）；组3 L2 聚合与闸门收敛（C1 阈值重校 + 快照字段同步 + conflict_evidence + C2 颜色保守化）；组4 成品仓落库与套算机制（B3 载体 + 日频成品范围 + C3 套算路径）；组5 实施排期确认（S1/S2/S3）。
   - **讨论顺序**：组5 排期（独立，与 P0 实施并行）→ 组2 九维构成（S2 前提，B1 最优先）→ 组1 L0（独立可并行）→ 组3 L2/闸门（依赖组2）→ 组4 成品仓（依赖组2/3）。
   - **分档编号预留**：334=L0 风险分级实施细则、335=L1 九维构成与判定细则、336=L2 聚合与闸门收敛细则、337=成品仓落库与实时套算机制（338 起留新话题）。
   - 状态跟踪表待后续逐组勾选。
   - **（003 修正）**：用户指示编号调整——334=L1 维度判定细则与信号注册表（组2，已存档）、335=L0 风险分级实施细则（组1 顺延对调）、336=L2、337=成品仓。

**后续建议**：按计划先开展组 5（S1 排期确认，立即可做）或组 2（B1 九维构成，决定共识算法与 S2 设计）。

### 002 - 组2讨论：L1维度判定细则与信号注册表（334号定稿）

**用户输入**：从组2开始讨论九维构成与 L1 判定细则，要求结合 LLM Wiki 知识库与系统现阶段配置说明并提供详细建议（直白表述），在对话框说明不改方案代码；随后给出范式指示——不纠结维度数量（9/10/20 均可），核心是把现状讲清楚+右侧信号识别机会；弹窗=亮灯简述版、个股页=七维现状描述详述版（贴出完整《面向普通用户的量化系统：股票现状描述框架（右侧交易信号视角）》）；询问"信号生命周期初始阈值"含义；后决定方案2（信号注册表一步到位）、考虑独立运行阈值调整机制、评估历史数据回算验证可行性、个股页验证完整通过信号注册表规范；最终确认 5 类信号够用（龙虎榜待扩展）、阈值 yaml 配置化版本化可回滚、方案编号应为 334。

**AI执行内容**：

1. **知识库核查**（LLM Wiki A股研究，本地文件直读）：九维全息框架（走势结构/筹码/情绪/多因子/财务/资金博弈/收益结构/时间/风险）、七维现状模板（信号确认/结构位置/量价健康/资金筹码/情绪环境/风险边界/状态总结，右侧信号场景精简）、三层分析框架（结构先行→位置确认→时间验证）、多维度七步法、信号生命周期（初期/中期/已延伸，+4%/+18% 示例）、PIERS、五维特征（道天地将法）；识别知识库关键张力（多因子"预测"哲学 vs 现状描述"不预测"）。
2. **系统配置核查**：322 五维（chanlun/volume_price/chip/emotion/factor，含 factor）、329 号 83 标签 9 组（估值17/阶段主力8/情绪板块5/量价方向8/缠论结构10/筹码深度17/资金风险2/事件5/聚合结论10）、332 §5.2 九维 vs 知识库九维对照（因子被括号、估值/位置/事件为系统独有）。
3. **范式确认**：判定层（内部维度池，每维输出 {state/evidence/conclusion/plain/light}）与输出层（弹窗亮灯简述 / 个股页七维体检详述，同一判定源）分离；七维模板组装映射表（7 字段 ← L1 维度/标签）。
4. **信号生命周期解释**："初始阈值"= 初期/中期/已延伸三档的量化分界（dist≤+5% 或 ≤3 天→初期；+5~12% 或 4-10 天→中期；>+12% 或 >10 天→已延伸），取自知识库示例取整、未经分布验证，为"先跑起来"初值，正式定标交校准脚本。
5. **历史数据回算可行性评估（实测查库）**：signal_records（app.db）1081 条 07-09~08-13（330 改进2 验证环，含 T+5/10/20 收益/触达/胜率/回撤）=在线积累主路径；strategy_signal_detail 存 13 个交易日 7.2 万条历史；daily_cache 634 万行 5 年=离线重放定标基础；opportunity_status_history/advice_history/library 表已建 0 行（成品仓载体候选）。局限：signal_records 未挂接右侧信号类型，实施需扩展字段。
6. **决策落地**：信号注册表 5 类（缠论三买/放量突破/均线多头/平台突破/量价强势形态）×触发/验证（N 日收盘站稳）/生命周期规则，龙虎榜待扩展；阈值管理=yaml 配置化+版本化可回滚+校准脚本（样本≥100 触发）+双轨（在线积累/离线重放）。
7. **编号核查**：334 未占用；按用户指示 334=L1 判定细则（组2）、335=L0 风险分级（组1 顺延对调）、336=L2、337=成品仓；332-1 计划清单同步更新（状态跟踪组2 ✅）。
8. **存档 334 号**《L1维度判定细则与信号注册表（332总纲·分档二）》：范式分离/L1 维度池输出协议/输出双形态/七维模板组装映射/信号注册表 5 类/阈值管理机制/验证方案。

**后续建议**：组2 完成。下一位候选讨论组：组 1（L0 风险分级，335 号）或组 3（L2 聚合与闸门收敛，336 号）或组 5（S1 排期确认）。

### 003 - 组1讨论：L0风险分级实施细则（335号定稿）

**用户输入**：开展组1（L0 风险分级），要求核查系统风险配置、LLM Wiki 知识库及同类软件做法并说明（不改方案代码）；随后提出简化方向——系统已有风险核查（原选股器链路已停用）应整合评估合理性并镶嵌现有环节、清理原停用部分，询问是否可行；最终确认：①选股系统页面弃用替换为机会图谱板块 ②deep（深度高估）不硬否决——估值非绝对精准、部分股票有较大价格突破可能性，实为高风险机会，需高价格风险强提示。

**AI执行内容**：

1. **系统风险配置核查**（代码级）：gate 门禁（cross_validate._evaluate_gate：估值分级 deep/moderate/mild + hard_risks event_negative + soft_risks 五类系数）、arbiter P0-P7（P1 监管硬否决/P2 deep 硬否决/P3 强看空）、event_monitor（20 类事件→10 枚举，质押检测明示 pledge_stat 未入库=数据缺口）、chip_pre_filter.FinancialRiskFilter（六大禁区：退市/业绩/财务/流动性/监管/ROCE）、screener L1 剔除（亏损微盘三重条件）。实测分布：catalyst_event regulatory 70/pledge 51/fraud_sign 897，fina_health fail 513（9.2%），ST 检测仅在选股器。
2. **知识库核查**：五层防护体系（硬性否决造假/退市/质押/监管 + ROCE<15% + 2% 规则/单票 30%）、PIERS（P 造假/I 商业模式/E 高杠杆/R 利益错位/S 快速迭代，永不纳入）、硬性否决（零容忍/永久/不可覆盖）、六大禁区。
3. **关键差异发现**：①fraud_sign 语义已降级为"经营恶化非欺诈"（2026-08-05）→ 真造假无标签，L0a 缺口确认、暂缓 ②质押数据未采集（pledge_stat 缺口）③deep 系统现状=硬否决（arbiter P2）vs 332 文档=仓位压缩——冲突待决 ④L0c 持有期完全缺失。
4. **选股器链路核查**：前端页面仍在（screener.html + /api/v3/screener/*），后端组件被 strategy_analyze:659（个股页 Step 0 前置风控，与 L0 重复双轨）、pipeline.py:175（大盘熔断，独立价值）、enhanced_cache_manager:2066（fina_health/roce_pass 标签来源）引用——"半停用"，不可整条删。
5. **用户决策落地**：选股器页面弃用→清理路由/集成/前端；deep 改高风险机会强提示（非否决），潜力侧 dev>30→×0.3 已有、状态侧删 arbiter P2 + cross_validate deep 分支改提示+仓位压缩 + test_319 更新。
6. **存档 335 号**《L0风险分级实施细则（332总纲·分档三）》：零新建原则、L0 映射表（L0a regulatory/ST/退市 + L0b soft_risks 系数复用 + L0c 生命周期接入）、deep 定位修正影响面、ST 检测迁移 event_monitor、清理清单（可清理/必须保留/重复去重）、验证方案（deep 回归/ST 拦截/L0c/清理回归/剔除比例 3-8% 核查/L0b 系数校准复用 334 回算）。

**后续建议**：组1 完成。下一位候选：组 3（L2 聚合与闸门收敛，336 号）或组 4（成品仓机制，337 号）或组 5（S1 排期确认）。

### 004 - 组3讨论：L2聚合与闸门收敛细则（336号定稿）

**用户输入**：开展组3（L2 聚合与闸门收敛），要求先核查现有共识阈值和 conflict_evidence 配置再说明方案；随后追问"调整的影响是什么、输出功能是什么"，并要求对比 LLM Wiki 知识库相关内容判断哪个更合理有效；最终确认方向并整理 336 号方案草案（要求把系统配置细节考虑清楚）。

**AI执行内容**：

1. **共识/冲突现状核查**（代码级）：
   - **三套共识口径并存**：L4 标签投票（cross_validate._compute_consensus，VOTE_MAP 14 票源 + data_daemon._compute_snapshot_consensus_rate）用于弹窗摘要+快照；五维方向推导（advice_builder._approximate_consensus）用于个股页建议卡；轻量兜底（arbiter._derive_consensus）。603897 实测 100% vs 67% 双口径即来源于此。
   - **阈值 4 处硬编码**：arbiter _BEARISH_STRONG=0.65 / _BULLISH_ENTER=0.55、前端色板深蓝≥0.7·浅蓝 0.55-0.7（opportunity-treemap.html:560-581）、daemon 门禁≥0.65。
   - **conflict_evidence 4 条规则**（arbiter.py:168-194）：缠论vs多周期、高位获利盘（≥0.8+高位）、出货vs确认、结构风险HIGH vs 确认；输出 diagnose → indicator-ide.html:2145 风险边界行；快照无 conflict 字段。
   - **快照字段来源**（data_daemon.py:2925-2999）：right_side_confirm/consensus_rate/opportunity_state 全来自 P4 标签快照。
2. **输出功能说明**：共识=决策级（arbiter P3/P6 门禁、前端颜色细分、弹窗摘要、daemon 门禁）；conflict_evidence=风险边界展示（暴露矛盾交还用户决策）。
3. **调整影响**：共识原料从 14 标签等权投票（票源不同质、技术面同质重复放大、虚假 100%）→ 10 维度独立判定后归一化计票（每维语义纯净、UIDF 同构、自然消除虚假完美）；影响 arbiter 阈值/前端颜色/弹窗摘要/快照字段 4 处；分布必然下移。
4. **知识库对比结论**（《信号冲突处理机制》《多指标共识机制》《UIDF》）：九维推导更合理——UIDF 维度分层校验一致、共识计数 ≥2/3 门槛、多智能体风控否决（已保留）、"不要追求完美一致"（conflict 哲学依据）、"少即是多"（过滤优先）；提示"融合优于仲裁"（维度权重预留但默认等权）。
5. **决策落地**：可入场门槛 0.55→0.67（知识库 ≥2/3）、强看空 0.65→0.67、浅蓝下限 0.55→0.67（0.50-0.67 降 wait/轻仓语义）；conflict 扩展 2 条（高位+无主力、deep+强确认）；快照字段来源改 L1/L2 输出 + 新增 conflict 列；颜色保守化确认；新增 config/status_engine.yaml（共识阈值/维度权重预留/conflict 规则登记，版本化可回滚）。
6. **存档 336 号**《L2聚合与闸门收敛细则（332总纲·分档四）》：决策依据/共识口径切换（10 维方向映射表）/阈值重校表/conflict 扩展/快照字段同步/颜色保守化/status_engine.yaml 配置细节/落地时序（S1 五维过渡→S2 九维切换→S3 呈现）/验证方案。

**后续建议**：组3 完成。下一位候选：组 4（成品仓机制，337 号）或组 5（S1 排期确认）。

### 005 - 组4讨论：成品仓落库与实时套算机制（337号定稿）

**用户输入**：开展组4（成品仓机制），要求先核查成品仓机制再说明方案；最终确认：按 B 方案新建 status_snapshot、建议规则参数预生成、七维模板文本按需组装，定稿 337 号。

**AI执行内容**：

1. **现状盘点**（代码级）：系统无日频现状成品；operation_advice 每次实时完整计算。已有积木：①treemap_snapshot（ts_code 主键单日快照，每日全量原子替换 _new→DROP→RENAME，data_daemon._build_treemap_snapshot L2817，日终 S1 管道 L3285）②signal_records（330 改进2 验证环，strategy_analyze:765-794 落库仅 enter/light + 轨A 后台同步，1081 条）③三张候选表 0 行（opportunity_status_history=状态变更历史 / opportunity_advice_history=建议历史 / opportunity_library=机会池含 park 停放机制）④AdviceGenerator 闲置组件（L4 诊断旧链路，0 行从未调度）。
2. **B3 载体决策**：三选一分析——扩展 treemap_snapshot（❌ 表变 40 列、破坏图谱轻量定位）/ 新建 status_snapshot（✅ 职责清晰、同生产环节无双源分裂）/ 复用候选表（❌ 语义不符）。用户确认 **B 新建 status_snapshot**。
3. **补入6 生成范围**：日频现状成品全市场结构化落库（九维状态/状态条/opportunity_state/conflict/建议规则参数）；七维模板文本按需组装不落库（避免 5571×7 文本膨胀）；用户确认。
4. **C3 套算路径**：三选一——前端套算（❌ 口径漂移）/ 轻量套算层（✅ 参数预生成+现价→毫秒级，advice_builder 重构为套算函数）/ 完整重跑（⚠️ 慢）。用户确认参数预生成+轻量套算。
5. **存档 337 号**《成品仓落库与实时套算机制（332总纲·分档五）》：成品仓两类结构、status_snapshot 表设计（草案 DDL）、日频成品生成（全市场、S1 管道扩展）、七维模板按需组装（build_seven_dim_report）、轻量套算层（advice_params JSON + apply_advice_params 函数 + SignalRecord 验证环衔接）、与现有机制关系（treemap 不动/候选表不启用/AdviceGenerator 不沿用）、验证方案。

**后续建议**：组4 完成，组1-4 全部定稿（334-337 号）。仅余组 5（实施排期确认：S1 开工时点、S2/S3 范围）。

### 006 - 组5收口：332整体实施计划（338号存档）

**用户输入**：继续做组5，先确认 S1 开工时点；随后确认——332 是一套整体工作，若无其他需沟通细化的工作，即可开展整体修复工作排期，根据总纲及相关文档排布工作计划。

**AI执行内容**：

1. **P0 状态核查（代码实证）**：P0 尚未实施——①量价 NameError 仍在（signal_computation_service.py:945 `market_env` 变量未定义，仅 L1150 构建为 market_context 子键）②因子 ImportError 仍在（:2461 `from app.data import get_data_manager` 不存在，grep 零命中）③筹码静默失败未处理 ④Vibe 未剔除（:1007-1009 调用 + :3111 方法定义仍在）。
2. **S1 开工时点结论**：S1 主体（共识统一改五维推导）**硬依赖 P0**（五维需真实输入，量价 0%/因子 0%/筹码 0.7% 不修则五维是空壳）；仅 S1.1（state_reason 修复）不依赖可独立先行。→ S1 开工时点 = P0 完成并验证（覆盖 ≥90%）之后。
3. **阻塞性检查**：332 主线无其他需沟通细化项（组1-4 已定稿 334-337；关联待办个性地图/国债收益率源属独立后续不阻塞）。
4. **排布整体实施计划并存档 338 号**《332总纲整体实施计划》：四阶段 P0→S1→S2→S3 任务分解（P0 八项含 333 落地项 1/3/6 管道顺序+SQLite 预调；S1 五项含 state_reason/共识五维过渡；S2 九项含 status_engine.py 落地 334/335/336/337+清理；S3 五项含九维灯/七维模板/颜色校准）+ 验证标准 + 风险前置（重算耗时/SQLite 锁/测试环境锁冲突）+ 状态跟踪表；P0 待用户发起实施。
5. **同步更新**：332-1 计划清单 v2.0（5 组全部完成，使命完成收口）、332 号 frontmatter 追加分档标注（333-338）、沟通索引 #129、工作上下文。

**后续建议**：实施阶段启动——用户发起 P0（生产引擎健康修复）后，按 338 号计划 P0.1-P0.8 执行；P0 完成后进入 S1。

### 007 - P0生产引擎健康修复实施完成（338号 P0.1-P0.8 全量落地）

**用户输入**：按 338 号 P0 执行（生产引擎健康修复）；确认暂停 daemon 做验证重算后重启。

**AI执行内容**：

1. **代码修复（signal_computation_service.py + chip_pre_filter.py + data_daemon.py）**：
   - P0.1 量价 NameError（L945 `market_env` 未定义 → `market_context.get('market_env')`）
   - P0.2 因子 ImportError（L2461 `get_data_manager` 不存在 → `self.data_manager`）+ **额外根因**：动态权重注入 dict（dynamic_weight_detail）致 `max(scores.values())` 崩溃（L2555 过滤非数值）
   - P0.3 筹码异常降级（warning+回退默认信号）+ **额外根因①**：流动性单位 bug（chip_pre_filter._check_liquidity_risk：daily_cache.amount 千元按元与 500 万比较，误杀全部）修复（×1000）+ **额外根因②**：filter_stock 可逆化（ROCE/流动性/财务硬剔除 → 风险标注不硬剔，仅 ST/退市硬过滤——335 号 L0b 可逆不否决哲学）
   - P0.4 Vibe 剔除（调用移除 L1007 + 方法删除 L3111；个股页消费方返回空对象不报错）
   - P0.7 管道顺序调整（data_daemon._run_precompute：P4 标签先算 → P2 后算，杜绝昨日标签）
   - P0.8b P2 计算缓存扩展（筹码/BOCIASI快线/因子加 analysis_cache）
2. **环境处理**：daemon 自身卡锁循环（16:00 起"缓存分钟K线失败 database is locked"，印证待解决事项锁问题）→ 暂停守护（start_daemon.sh PID 968）+ daemon（1167/19283，守护自动重启机制发现）→ 活跃库 WAL checkpoint 恢复（16GB 库 62MB WAL 截断）
3. **全量重算（P0.6）**：后台 _run_precompute（5571 只）：P4 标签预计算完成（703.3s）→ P2 策略信号预计算完成（5571/5571）——新管道顺序生效
4. **覆盖度验证（20260815，5571 只）**：**量价 98.9% / 因子 98.9% / 筹码 98.9%**（目标 ≥90% 全达标；修复前 0%/0%/0.7%）；缠论 98.8%；BOCIASI快线 64.2%/慢线 34.8%（非 P0 目标，依赖涨停池/指数数据，记录观察）
5. **回归**：55 个测试通过（315/316/319/321/322/323）
6. **收尾**：临时脚本删除、daemon 守护重启（PID 26236 正常预热）、task 完成

**遗留观察**：①BOCIASI 覆盖低于 90%（非 P0 目标，数据源依赖，S2/S3 观察）②P0.8a 写串行化单写队列（333 落地项3 剩余）为观察项（已有 read_conn/busy_timeout 基础）③ChipPreFilter 过滤原因现保留在 evidence（风险标注），S2 清理时整合 335 号 L0 分级。

**后续建议**：P0 完成，进入 **S1 口径统一**（338 号 S1.1-S1.5：state_reason 修复/共识五维过渡/降级共享/字段对齐/维度提取补全），待用户发起。

### 008 - S1口径统一实施完成（338号 S1.1-S1.5 全量落地）

**用户输入**：按 338 号 S1 执行（口径统一）。

**AI执行内容**：

1. **S1.1 state_reason 覆盖修复**（advice_builder.py）：`_apply_degradation` 改为返回 (state, reason)（≥2维反向→"方向冲突"、高潮/退潮→"情绪过热"）；盈亏比降级原因不再被无条件覆盖（elif 分支：`state_reason = _degrade_reason or 兜底`）。603897 实测 reason="盈亏比不足（目标收益/止损风险≈0.36，止损过宽）"（差异#4 修复）。
2. **S1.2 弹窗摘要层五维共识**（cross_validate.py diagnose）：verdict/opportunity_summary/顶层 cross_validation.consensus 改用五维推导共识（advice_builder._dim_directions/_consensus_from_dirs，5 维方向 +1/0/-1 计票）；`_build_verdict` 加 final_state 感知（五维看多但实时风控降级 wait/avoid 时输出"但实时风控已降级，建议观望"，不再"优质机会建议关注"——差异#1 根治）；`_build_opportunity_summary` 文案"标签"→"维度"。603897 实测：共识 bullish 75%（3/5，five_dim，替代虚假 100%）、verdict 降级提示、summary "✓ 中等确认（共识率 75%）"。
3. **S1.3 仓位降级共享函数**（advice_builder.py）：新增 `apply_soft_risk_position(max_pct, soft_risks, valuation)`（fina_weak/fail ×0.5、distributing ×0.7、low_liquidity ×0.7、估值 moderate ×0.5/mild ×0.8，口径对齐 _evaluate_gate）；build_operation_advice 应用（从 tags 推导 soft_risks + valuation_level 简化映射，S1 过渡、S2 统一）。analyze 实测 max_position_ratio 0.16（wait 0.2 × 软风险）。
4. **S1.4 analyze 顶层字段对齐**（strategy_analyze.py）：operation_advice 补旧字段（action/label/max_position_ratio/entry_plan/stop_loss/target_price，同 diagnose L489-499 兼容）。analyze 实测 stop_loss='close < 27.2'。
5. **S1.5 emotion 维证据补全**（cross_validate.py `_extract_real_dimensions`）：emotion 增加 rotation_state（SectorAnalysisService 读缓存计算）+ sector（板块服务优先、Stock.industry 兜底，与个股页同源）。603897 实测 emotion={direction: bullish, rotation_state: LEADING, sector: 电气设备}（差异#3 修复）。
6. **验证**：S1 相关 82 个测试全过；端到端 603897 diagnose vs analyze 双端一致（state=wait、reason=盈亏比不足、stop_loss 同源、emotion 证据同源）。全量 pytest 362 passed/9 failed——9 失败均为已知环境问题（8 个 daemon 并发写锁[待解决事项十三节]、1 个 test_t14 快照旧数据[P0 重算后日终待重建]），非 S1 引入。

**后续建议**：S1 完成，进入 **S2 生产流水线模块化**（338 号 S2.1-S2.9：status_engine.py 落地 334/335/336/337 + 配置化 + 快照切换 + 清理），待用户发起。

### 009 - S2生产流水线模块化实施完成（338号 S2.1-S2.9 全量落地）

**用户输入**：按 338 号 S2 执行（生产流水线模块化）。

**AI执行内容**：

1. **S2.4 ST 迁移**（event_monitor）：`_detect_st_warning` 补"退"字检测（*ST/ST/退市整理归 regulatory → L0a）；经核查 event_monitor 已有 ST 检测，chip_pre_filter 重复实现随清理。
2. **S2.2 配置 yaml**：`config/status_engine.yaml`（共识阈值 0.67/维度权重/conflict 规则/L0 系数）+ `config/signal_registry.yaml`（5 类信号 × 触发/验证/生命周期），`status_config.py` 加载器（版本化可回滚）。
3. **S2.3 arbiter 调整**：删 P2 deep 硬否决（改强提示 `_DEEP_HINT`，正常走 P3-P7）；阈值 `_BEARISH_STRONG/_BULLISH_ENTER` 0.65/0.55→**0.67**（yaml 加载 `_load_thresholds`）；cross_validate deep 分支改仓位压缩（×0.3）；3 测试更新（34 通过）。
4. **S2.9 清理**：删除 routes/screener.py + screener_strategy_integration.py + screener.py；screener_bp 注册移除；strategy_analyze Step 0 DarwinRiskFilter 重复风控删除；framework_integration/chanlun_strategy 死代码清理（DarwinRiskFilter 壳/MultiLayerStockScreener/analyze_single_stock）；44 测试通过。
5. **S2.1 status_engine.py 核心**（新建）：生产环节引擎——L1 十维判定（334 §2，标签+P2 信号 → {state/light/confidence/evidence}）+ 信号生命周期（334 §5.3，active_signal 距离/天数 → 初期/中期/已延伸）+ L0 分级（335：L0a 硬否决/L0b 仓位系数/L0c 已延伸持有期）+ L2 聚合（336：维度共识 +1/0/-1 计票、conflict 6 条规则、arbiter 状态机）+ 成品仓输出（337：dim_states/status_bar/consensus/conflict/advice_params）。603897 evaluate：enter/0.857/强趋势/10 维完整。
6. **S2.6 status_snapshot**：ECM 建表（含 advice_params 列）+ data_daemon `_build_status_snapshot`（原子替换，挂日终 S1 管道 _build_treemap_snapshot 后）。建表被 daemon 写锁暂缓（历史锁问题），但生成函数自建表（NEW→RENAME）不阻塞，日终验证。
7. **S2.7 套算/组装**：advice_params 预生成（L0 系数仓位）+ `apply_advice_params` 套算（盈亏比门禁降级——603897 rr=0.36→wait+仓位 0.3，与 diagnose 实时建议一致）+ `build_seven_dim_report` 七维组装（7 段）。**日频成品 enter vs 实时套算 wait = 两类成品预期差异**（337 §6.3）。
8. **S2.8 signal_records 挂接**：`_derive_signal_source_type` 推导右侧信号类型（chan_third_buy/volume_breakout/ma_bullish/platform_breakout/pattern_up/other）写入 signal_snapshot JSON（表结构不变，回算按类型分群校准生命周期阈值）。
9. **验证**：63 回归测试通过（arbiter/advice/snapshot/health）；S2.5 由 S2.6 status_snapshot 承载（337：treemap 不动）。

**遗留/待验证**：①status_snapshot 日终生成待日终/daemon 空闲验证（建表被锁暂缓但自建表不阻塞）②treemap_snapshot 旧数据（test_t14）待日终重建 ③daemon 写锁历史问题（待解决事项）④S3 呈现层（弹窗九维灯/个股页七维模板渲染/颜色校准）待发起。

**后续建议**：S2 完成，进入 **S3 呈现层对齐**（338 号 S3.1-S3.5：弹窗九维灯/个股页七维模板/颜色校准/conflict 展示/文案），待用户发起；建议先做 status_snapshot 日终生成验证（下次 daemon 日终）。

### 010 - S3呈现层对齐实施完成 + 338号全链路收口（P0/S1/S2/S3 全部落地）

**用户输入**：按 338 号 S3 执行（呈现层对齐）。

**AI执行内容**：

1. **后端 API 透出成品仓数据**：①analyze 响应加 `seven_dim_report`（build_seven_dim_report 组装）+ `status_snapshot`（日频现状成品行）——status_snapshot 空时=None 不报错（日终生成后生效）②diagnose 响应加 `dim_states`（`_get_status_dim_states` 读 status_snapshot，弹窗九维灯数据源）。
2. **S3.1 弹窗九维灯**（opportunity-treemap.html）：弹窗 d-verdict 区加 `d-dim-lights` 容器 + diagnose.dim_states 渲染 10 维灯（🟢/🟡/🔴 + evidence hover）+ status_bar。
3. **S3.2 个股页七维模板**（indicator-ide.html）：建议卡后追加 `apSevenDim` 段——analyze.seven_dim_report 七段体检报告渲染（信号确认/结构位置/量价健康/资金筹码/情绪环境/风险边界/状态总结，红绿灯+文本+大白话）。
4. **S3.3 颜色阈值对齐**：TIMING_COLORS/STATE_COLORS 注释 55-70%→67-70%（336 号 C1 门槛）。
5. **S3.4 conflict 独立展示**（indicator-ide）：conflict_evidence 从"追加风险边界行"改为独立 `dimConflictBlock`（多条逐条渲染）。
6. **S3.5 图例文案**：showLegendHelp 加"可入场门槛=维度共识≥67%（336 号 知识库 ≥2/3；三处结论一致优先于颜色激进）"。
7. **验证**：后端 44 测试通过；前端括号平衡检查通过（+67/+8 各配对增量一致；indicator-ide 的 -3 为历史遗留非本次引入）。

**338 号全链路收口**：P0（生产引擎健康修复）→ S1（口径统一）→ S2（生产流水线）→ S3（呈现层）全部完成；338 号状态跟踪表更新 ✅。

**遗留/后续（记录）**：①status_snapshot 日终生成验证（建表被 daemon 写锁暂缓，生成函数自建表；日终后前端九维灯/七维模板自动生效）②treemap_snapshot 旧数据待日终重建 ③daemon 写锁历史问题（待解决事项）④关联待办（个性地图搜索第四地图/国债收益率真实源/量价 3 处收敛细化）⑤BOCIASI 覆盖观察（64.2%/34.8%）。

### 011 - 332体系落地全面核查与修订（8 项遗漏确认 + 2 处修订）

**用户输入**：全面检查对应方案的落地情况，如有遗漏和错误及时修订。

**AI执行内容**：

1. **全面核查**（代码实证，对照 332/333/334/335/336/337/338 各方案落地项）：
   - **已确认遗漏 8 项**：①333 落地项4 P2 消费标签（3 引擎改造）未实施——筹码引擎仍重算（signal_computation_service:1377 chip_strategy.analyze）②336 核心"双模块消费成品仓"未完成——status_engine.py 已建但 diagnose/analyze 主链路仍走 cross_validate/advice_builder（仅 S3 透出展示字段）③335 L0c 未接入 advice_builder 实时路径（个股页实时建议无"已延伸只持有不开仓"）④333 落地项2 标签日期基线校验未实施 ⑤334 校准脚本 scripts/calibrate_signal_thresholds.py 未创建 ⑥334 信号注册表触发/验证条件检测未接入 status_engine（仅生命周期阈值用了）⑦333 落地项3 SQLite 单写队列未实施（P0.8a 观察项）⑧arbiter conflict 缺 336 §4.2 新 2 条规则。
   - **设计错误 1 处**：334 的 L1 维度池漏**量价维**（332 §5.2 修订③"量价→量价维"明确，但 334 表无）→ 七维模板"量价健康度"段错误用筹码资金数据填充。
2. **修订（本轮完成）**：①arbiter.py conflict_evidence 补 2 条（高位无主力在场/deep+强确认）——验证触发正确 ②status_engine.py 补**量价维**（_DIM_DIRECTION/_DIM_ORDER/_build_dimensions 加 vp：P2 量价信号 trend.direction 回退 volume_price_fit 标签；11 维共识计票）——603897 量价=背离/red/0.44 ③build_seven_dim_report 七维模板"量价健康度"段改用 vp 维（语义修正）④334 文档补量价维行（标注修订）⑤336 文档 conflict 扩展标注已补全。
3. **记录**：待解决事项.md 新增"十五、332 体系落地核查遗留"（7 项：P2 消费标签[🔴]/成品仓消费切换[🔴]/L0c advice_builder[🟡]/日期基线[🟡]/校准脚本[🟡]/注册表触发检测[🟡]/单写队列[🔵]）。
4. **验证**：26 回归测试通过；量价维/conflict 修正端到端验证（603897 11 维、arbiter 新规则触发）。

**后续建议**：遗留 7 项中 2 项 🔴（P2 消费标签/成品仓消费切换）是 333/336 核心未竟，建议优先安排实施；其余 🟡/🔵 随迭代补。另有 status_snapshot 日终生成验证待做。

### 012 - 遗留项补齐实施 + 再次核查（P2消费标签/成品仓切换/L0c/日期基线/校准脚本/注册表触发）

**用户输入**：继续实施 P2 消费标签和成品仓切换两项优先项，并将其他遗漏问题一并解决，完成后再次检查。

**AI执行内容**：

1. **P2 消费标签（333 项4，🔴）**：筹码引擎消费标签结论——新增 `_build_chip_signal_from_tags`（阶段主力组+筹码深度组标签构建信号，不重算 120 日筹码分布），`_compute_chip_signal` 优先标签、不足回退完整分析；验证 603897 筹码 bullish（washing+5d_inflow，证据含标签数据，标注"标签原料信号"）。量价消费端已统一 P2 权威（status_engine vp 维用 P2 信号）；情绪双视角保留。
2. **成品仓切换（336 核心，🔴）**：diagnose/analyze 透出 `status_verdict`（status_engine 实时 evaluate 权威结论：opportunity_state/status_bar/consensus_rate/direction/conflict_evidence/dim_states/advice_params）；前端弹窗优先消费 status_verdict（11 维灯 + 维度共识状态行）。双模块现在消费同一 status_engine 结论（332 §5.4 成品仓唯一消费达成）。
3. **L0c advice_builder（🟡）**：build_operation_advice 加 active_signal 已延伸检查（距突破位>12% → wait + "只可持有、不新开仓（L0c）"）；验证构造场景 +50% → wait ✅。
4. **333 项2 日期基线（🟡）**：`_tags_date_ok` 校验标签 updated_at ≥ 数据日期，不符回退完整分析。
5. **334 校准脚本（🟡）**：创建 `scripts/calibrate_signal_thresholds.py`（signal_records 在线分群统计 win_rate + 样本≥100 校准触发；离线重放预留）。
6. **注册表触发检测（🟡）**：status_engine `_detect_registered_signals`（5 类信号触发检测），evaluate 输出 signals；验证 603897 触发 chan_third_buy + pattern_up 2 类。
7. **333 项3 单写队列（🔵）**：评估为长期优化（已有 read_conn 读写分离+busy_timeout=30s+WAL checkpoint 基础，daemon 重构时实施，不进行高风险改造）。
8. **再次核查**：待解决事项十五节 7 项中 6 项已解决（标注 ✅），剩余 #1 量价标签侧粗逻辑废弃（P4 修订，消费端已统一）+#7 单写队列（长期）；38 回归测试通过；端到端全链路验证（status_verdict/注册表触发/L0c/标签消费）。

**最终状态**：332 体系讨论+实施+核查修订+遗留补齐全部完成；剩余记录项（status_snapshot 日终验证/量价标签侧 P4 修订/单写队列长期/BOCIASI 观察/关联待办）见待解决事项。

### 013 - 日终后实测验证（339号报告）+ 3 个问题修复

**用户输入**：今天非交易日且时间已到 20 点后，可开展日终后实际验证，形成全面验证报告，输出实测验证的问题。

**AI执行内容**：

1. **现状检查**：status_snapshot 表未建成（daemon 持锁，历史问题）、treemap 快照 08-13 旧数据、信号 20260815。
2. **验证准备**：暂停守护+daemon（持锁）→ WAL checkpoint → status_snapshot 建表（14 列）→ 手动触发快照重建+现状成品生成（全市场 5571 只，treemap 9.2s / status_snapshot 16.2s，trade_date=08-14）。
3. **验证结果**：①覆盖度 量价/因子/筹码 98.9% 持续达标 ②status_snapshot 分布：avoid 46.2%/wait 43.4%/light 9.7%/enter 0.6%（33 只共识≥0.71）；时间维 中期 3454/回撤 1563/初期 366/已延伸 188；status_bar 风险区 2577/趋势确认 2459/转弱 337/强趋势 237 ③端到端 4 只（603897/300759/301119/000066）status_verdict 双端一致、seven_dim_report 7 段完整、弹窗 11 维灯就绪。
4. **实测发现并修复 3 个问题**：①生命周期负距离误判"初期🟢"→ 加"回撤"态（603897 跌破信号价 → 回撤🔴）②active_signal 单引号字面量 json.loads 失败 → 改 ast.literal_eval（status_engine+advice_builder 两处）③_signal_lifecycle 用 tags.get('ts_code') 取码失败（tags 无此键）→ 显式传参。
5. **观察项 3 条**：avoid 46.2%（右侧否决 41.9%，大盘弱+保守策略预期，待交易日复验）/ enter 0.6%（C2 颜色保守化效果，需用户确认数量级）/ status_snapshot 日终未自动生成（daemon 持锁阻碍，需优先解决）。
6. **收尾**：重启 daemon 守护（PID 37475）、临时脚本删除、**存档 339 号验证报告**（含实测问题清单）、38+ 回归测试通过。

**后续建议**：①**优先解决 daemon 持锁**（阻碍日终自动生成，待解决事项十二/十三节历史问题）②交易日复验 avoid 占比（右侧否决阈值是否过严）③确认 enter 0.6% 数量级是否可接受 ④关联待办（个性地图/国债源/量价收敛）。

### 014 - daemon 锁根因排查与修复（双 ECM 实例 → 单例统一）+ 日终复核

**用户输入**：优先处理 daemon 持锁问题，完成后复核日终生成；仔细全面核查根因并解决。

**AI执行内容**（compose:debug 四阶段）：

1. **Phase 1 根因调查**：①锁失败日志统计——**全天持续 831 次**（00:00-16:12 每时段 1-2 次，"缓存分钟K线失败 database is locked"），非偶发 ②ECM 连接结构核查——conn/read_conn/snapshot_conn 均 check_same_thread=False；`cache_minute_kline` 已有 `_write_lock`（实例级 RLock）；`_insert_from_df` 有 commit ③发现 `get_ecm_instance()` 全局单例已实现，但 **`data_daemon.py main()` L3710 直接构造 `EnhancedCacheManager()`**（绕过单例）。
2. **Phase 2 模式分析**：main 的实例 A（模块 _ecm）+ 采集器/回填/其他模块的 get_ecm_instance() 实例 B——**同进程双实例并存**，各自独立 `_write_lock`（实例级不互斥）。
3. **Phase 3 复现验证**：双连接并发写（40 次×2，busy_timeout=1s）→ **100% 失败（A=40/B=40）**——SQLite WAL 写者互斥，双写连接并发必锁。根因证实。
4. **Phase 4 修复**：`main()` 改 `_ecm = get_ecm_instance()`（统一全局单例）——daemon 内所有写走同一实例同一锁。
5. **验证**：重启 daemon（PID 38473）后 **8+ 分钟 0 次锁失败**（修复前全天 831 次）；单例一致性确认（is 同一实例）；**复核日终生成**——daemon 运行中（单实例）手动触发 `_build_status_snapshot` 5571 只无锁冲突。
6. **记录**：待解决事项新增十六节（根因/复现/修复/遗留：跨进程 API 冲突待进程级协调、pytest 并发测试纪律）。

**遗留**：①跨进程写冲突（API 进程 run.py 与 daemon 各持单例）——API 不常写，若并发写增多需进程级协调（333 项3 单写队列）②pytest 与 daemon 并发写测试仍锁（daemon 空闲时跑全量）③交易日复验 avoid 占比 ④确认 enter 0.6% 数量级。

### 015 - 遗留收尾（量价标签统一 P2 权威）+ 全面复查完成

**用户输入**：daemon 锁已解决，继续实施 P2 消费标签和成品仓切换，并处理遗留问题，完成后全面复查。

**AI执行内容**：

1. **状态核查**：P2 消费标签（筹码 `_build_chip_signal_from_tags`）与成品仓切换（`status_verdict` 双端）此前已实施（012/135）；未完成遗留 = **量价标签侧 P4 修订**（333 项4 收尾：`VolumePriceStrategy.get_tags` 粗启发式）。
2. **量价标签统一**：`get_tags`（volume_price_strategy.py:4069）的 volume_price_fit 判定改调 **P2 完整 analyze 组件** `compute_volume_price_signal('', df)`（仅依赖 df，可无 ts_code/market_env 运行）映射 healthy/diverging/neutral，废弃粗启发式（异常回退兜底）——P4 标签量价与 P2 信号共用组件、逻辑统一（333 项4"共用同一组件"落地）。验证：603897 get_tags=diverging（完整 analyze 背离细节命中），与 P2 同源。
3. **T5 遗留项任务标记完成**（P2 消费标签/成品仓切换/L0c/日期基线/校准脚本/注册表触发/量价标签统一/单写队列[长期优化记录]）。
4. **全面复查**：①端到端 603897 diagnose/analyze status_verdict 一致（wait/0.667/强趋势/11 维/七维 7 段）②覆盖度 量价/因子/筹码 98.9% 持续达标 ③daemon 锁修复后持续 0 次（PID 38473）④41 回归测试通过。
5. **记录**：待解决事项十五节 #1 量价标签侧修订完成（仅剩 #7 单写队列长期优化）。

**最终状态**：332 体系（讨论+实施+核查+遗留+daemon 锁修复）**全部闭环**。剩余记录项：跨进程写协调（333 项3）、pytest 并发纪律、交易日复验 avoid/enter、关联待办（个性地图/国债源/量价收敛）。

### 016 - 332 实际系统数据测试（测试3/6/9）+ 340号报告

**用户输入**：按方案全面测试，先跑 3（双端一致性）、6（L0 分级分布）、9（前端实机渲染），完成后汇总测试报告，务必全面问题核查。

**AI执行内容**：

1. **测试 3 双端一致性**（抽样 100 只 = 96 随机 + 4 历史问题股）：diagnose vs analyze 的 status_verdict（opportunity_state+consensus_rate）——**不一致数 0/100**，成品仓唯一消费源生效（332 §5.4 红线达成）。
2. **测试 6 L0 分级分布**（全市场 5571 只 status_snapshot.l0）：L0a 硬否决 65（监管立案）；L0b 软风险 distributing 1457/fina_weak 657/fina_fail 509/deep_valuation 440；L0c 持有期 188；仓位系数 0.05-1.0 梯度——L0 三级分级分布健康（335 号落地）。
3. **测试 9 前端实机渲染**（playwright + 临时 API 5001 + serve 8082，测后清理）：①treemap 弹窗——ECharts canvas 渲染成功 + 九维灯（d-dim-lights 11 维 span+hover 证据）+ 维度共识状态行（"★ 维度共识 67%（看多）观望"）②indicator-ide——选中 603897 运行分析 → 七维模板（apSevenDim 体检报告 7 段）+ 建议卡 state_reason（"等待：盈亏比不足（≈0.36）"——S1.1 前端可见）——均无 JS 错误。
4. **实测问题核查**：①`/api/v3/watchlist` 400（get_watchlist NoneType.strftime）——**既有问题非 332 引入**（前端已容错，自选监控页受影响，记录待办）②2 个测试方法注意项（treemap canvas 无 DOM 节点；需点击联想项正确选股——此前 apSevenDim 空系测试交互未选中，非 332 问题）。
5. **存档 340 号测试报告**（结果汇总/问题清单/结论）；沟通纪要 016、索引 #139、工作上下文、task T6 完成。

**后续**：①watchlist 400 待核查（既有问题）②交易日复验 avoid 46.2%/enter 0.6% ③关联待办（个性地图/国债源/量价收敛）。
