# 2026-07-06 沟通纪要

### 001 — mootdx 接入全局数据体系方案起草（248号方案）

**沟通主题：** 继续 mootdx 对接全局数据体系的沟通，完成方案起草并存档

**关键结论：**
- 验证了当前环境中 TCP 7709 端口全部连通失败（被 Claude 注入代理拦截），非正式机器问题
- 梳理了当前 AkshareCollector 7 个痛点（令牌桶/monkey-patch/三层兜底/PG三重写入/WAF对抗/可靠性/字段少）
- 设计 mootdx 接入方案核心架构：TCP 为主 + AKShare 低频补充 + 去除 monkey-patch/令牌桶/PG三重写入
- 方案详述了 Thread 1~5 的逐项变更、可移除代码清单（~525行）、新增代码估算（~400行）
- 提出 3 项待沟通确认：正式机器 mootdx 连通性/分钟K线切换/PG写入移除

**产出文件：**
- `002-方案存档/248-mootdx接入全局数据体系方案.md`

### 002 — Phase 0‑C 全量实施（248号方案执行）

**沟通主题：** 完成 248 号方案 Phase 0、A、B、C 四个阶段的实施

**关键结论：**
- **Phase 0** mootdx TCP 连通性验证通过：Quotes.factory() 成功，46 列 L1 行情 + 五档盘口，批量 0.05s/500只
- **Phase A** 创建 mootdx_collector.py (403行)：分批采集机制(1909 只有效/2761 只A股)、自算涨跌榜+涨跌停池、Flask 启动链路接入
- **Phase B** 瘦身 akshare_collector.py (847→326行)：移除令牌桶/monkey-patch/三层兜底/PG写入/涨跌停池采集/Thread 1+2，保留 3 个低频补充线程(板块/分钟/龙虎榜)
- **Phase C** 删除 realtime_pg.py + clean_realtime_tables.py（~250行），清理 3 处外部引用（__init__.py/gunicorn_config.py/scheduler_manager.py）
- **总代码变更**：新增 403 行（mootdx_collector.py）- 移除 771 行（akshare_collector 521 + realtime_pg 200 + clean_tables 51 + 引用清理）= 净减少 ~368 行
- **遗留问题**：北交所需 AKShare 补充；分钟K线 mootdx 切换待决策；DuckDB 僵尸锁问题仍存在

**产出文件：**
- `backend/app/data/mootdx_collector.py`（新建）
- `backend/app/data/akshare_collector.py`（重写）

### 003 — Phase D+E 实施（248号方案配置清理与验证）

**沟通主题：** PG 配置残留清理、端到端验证

**关键结论：**
- **Phase D** 验证 5 个检查点（模型/手册/docker/迁移/配置），0 处残留
- **Phase E** 15 项验证通过：mootdx 采集 2057 只、涨跌榜 Top20、涨跌停池 40+43 只、5 个 REST API 端点均返回 200
- **遗留问题**：指数代码 mootdx 市场标记与 WsBridge 查找码不匹配；DuckDB 持续损坏需修复

### 004 — mootdx 全面能力调研与全局数据体系文档链审计

**沟通主题：** 系统性调研 mootdx 全部 29 个 API，评估 4 项遗留问题的解决方案；审计全局数据体系相关文档链（239~248号），标记过时文档

**关键结论：**
- **mootdx 新发现 API**：
  - `client.index()` — 指数 OHLC 数据（含涨跌家数），可解决问题①指数代码不兼容
  - `client.block()` — 板块隶属数据（384K 行），可替代 AKShare 板块排行（问题③）
  - `client.transactions()` — 逐笔成交 Tick 数据（含买卖方向标记）
  - `client.bars(freq=9)` — 日线 OHLC 备选（800 交易日）
- **遗留问题方案**：
  - 问题①指数代码 → 使用 `index()` 而非 `quotes()` ✅
  - 问题②DuckDB 损坏 → 删除损坏文件重建
  - 问题③板块排行 → `block()` + `quotes()` 内存聚合 ✅
  - 问题④北交所 → 🔴 待解决
- **文档链审计**：
  - 标记 5 份已过时文档（239/241/242/244/246），更新 frontmatter status
  - 标记 1 份部分过时（243），更新 frontmatter
  - 更新 248 号方案为全量实施完成
  - 新建 249 号方案（mootdx 全面能力调研报告）

**产出文件：**
- `002-方案存档/249-mootdx全面能力调研与数据获取提升方案.md`

### 005 — 实施（SQLite切换 + 指数修复 + 板块排行 + 北交所 + 全维度问题修复）

**沟通主题：** 实施 250 号方案 SQLite WAL 切换、指数代码修复、板块排行自算、北交所腾讯API补充、审计发现的7项问题修复

**关键结论：**
- **DuckDB→SQLite WAL 切换**：ECM 重写（1498→398行），清理 1.3GB corrupted 文件，移除 duckdb 依赖
- **指数修复**：`client.index()` 替代 `quotes()`，5 个指数正确 SH/SZ 后缀
- **板块排行**：Tushare stock_basic 行业分类 + L1 快照自算，105 个行业
- **北交所**：腾讯 API `qt.gtimg.cn/q=bj{code}`，324 只全部覆盖，10s 轮询
- **全维度审计修复**：P1 clean_as_minute_kline 缺失 → 补充；P2 CacheManager 死引用 → 移除；P3 健康检查伪实现 → 真实 API 调用；P4 mootdx 断连无重连 → 自动重置；P5 adj_factor/index_daily 验证通过；P6 import requests 优化 → 移到模块顶部
- **APScheduler**：安装完成（此前从未安装，日终同步永不触发的问题已修复）

**产出文件：**
- `002-方案存档/250-DuckDB问题根因分析与存储层替代方案评估.md`（更新为全量实施完成）
- `002-方案存档/251-全局数据体系全量架构说明（2026-07-06版）.md`
- `backend/app/data/enhanced_cache_manager.py`（重写）
- `backend/app/data/mootdx_collector.py`（多次修订）

### 006 — 仪表盘6个缺失API端点全量修复

**沟通主题：** 修复仪表盘6个后端API端点中5个数据不可用/返回空的问题

**关键结论：**
- 路由和服务方法实际已存在，但数据缓存为空导致返回503或空结果
- **修复项**：
  1. `/market/index-daily` 503 → 指数日线数据同步（20764行）
  2. `/dashboard/summary` ✅ 已正常
  3. `/market/daily-top` ✅ 已正常
  4. `/market/sector-sector` 行业数据空 → Stock表同步(5527条) + Tushare直调降级
  5. `/market/moneyflow-summary` 503 → 资金流向同步(5194行)
  6. `/market/sector-moneyflow` 503 → 同上
- **额外发现Bug**：`ECM.cache_moneyflow_data()` 中 `str.replace` 不能处理 `datetime.date` 类型；`sync_index_daily_data()` 传递了Tushare多余字段到 `cache_daily_data()` 导致SQLite列不匹配。
- **验证结果**：6/6全部通过，返回真实数据

**产出文件：**
- `backend/app/services/dashboard_service.py`（修改）
- `backend/app/data/__init__.py`（修改）
- `backend/app/data/enhanced_cache_manager.py`（修改）
- `backend/scripts/sync_dashboard_data.py`（新建）

### 007 — 全局数据体系集采补齐：资金流向120日历史回填

**沟通主题：** 利用盘后时间补齐 moneyflow_cache 的120个交易日历史数据

**关键结论：**
- 之前仅1天(2026-07-06)，通过 Tushare moneyflow API 逐日补齐
- 119天共 616,697 行数据写入 moneyflow_cache，耗时 94s
- **最终状态**：120天完整（2026-01-05 ~ 2026-07-06），621,891 行
- **验证**：moneyflow-summary 返回20日真实数据，sector-moneyflow 返回8流入+5流出板块

**产出文件：**
- `backend/scripts/bulk_sync_moneyflow.py`（新建，可复用的资金流向批量同步脚本）

### 008 — 全局数据体系全量集采补齐（Phase 1-3）

**沟通主题：** 利用盘后时间补齐全部缺失缓存数据

**Phase 1 — 复权因子缓存（adj_factor_cache）：**
- 新增 ECM 表+方法：`adj_factor_cache` 建表，`cache_adj_factor_data()`/`get_cached_adj_factor()`
- 按交易日批量同步 120 天，5536 只/天
- 结果：5,080,366 行，5548 只股票，1212 交易日
- 耗时 93s

**Phase 2 — 全市场筹码分布（chip_distribution_cache）：**
- 运行 `ChipDistributionEstimator` 对 5003 只 A 股逐只估算
- 结果：4998 只成功，599,734 行筹码数据
- 耗时 78s

**Phase 3 — 因子预计算（factor_cache）：**
- 30 个核心技术因子（均线/RSI/MACD/KDJ/布林带/量价等）
- Top 1000 成交活跃股
- 结果：994 只股票，17,892 因子计算，37,146,171 行
- 耗时 75s

**总耗时：** ~4 分钟（3 个 Phase 串行执行）

**新建文件：**
- `backend/scripts/sync_adj_factor.py`（复权因子批量同步）
- `backend/scripts/compute_chip_distribution.py`（筹码分布全量计算）
- `backend/scripts/compute_core_factors.py`（核心因子预计算）
- `backend/app/data/enhanced_cache_manager.py`（增加 adj_factor 表和方法）

**最终全局缓存状态（2026-07-06 盘后）：**
| 缓存表 | 行数 | 覆盖 |
|--------|------|------|
| daily_cache | 572万 | 1993~2026 全量 |
| daily_basic_cache | 65.9万 | 120天 × 5500只 |
| **moneyflow_cache** | **62.2万** | **120天 × 5194只** |
| **adj_factor_cache** | **508万** | **1212天 × 5548只** |
| **chip_distribution_cache** | **60万** | **4998只股票** |
| **factor_cache** | **3715万** | **994只 × 30因子** |
| indicator_cache | 0 | 引擎计算（未使用） |
| as_* 表 | 0 | 已废弃（mootdx替代） |
