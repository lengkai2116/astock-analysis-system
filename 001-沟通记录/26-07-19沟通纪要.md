# 2026-07-19 沟通纪要

## 001 — 276号方案实施：筹码策略未完成项推进 + 质量修正

**用户输入：** 核查276号文档执行情况，从完成度和完成质量两维度审查；审查后要求推进未完成项并修正质量问题，全面测试。

**AI执行内容：**
- 审查276号文档实施状态：完成度55%（4/7项基本完成，2/7项未实施），质量评分70/100
- 修复 **chip_data 接线遗漏**（`chip_strategy.py:1118`）— `_score_chip_distribution` 缓存筹码指标，`identify_phase` 自动取用，激活 ASR/CYQKL 阶段识别分支
- 实现 **P2-2 交叉比对比法**（`_calc_cross_compare`）— 主力资金+散户情绪+股价位置三维交叉，6种结论类型
- 更新 `_build_default_chip_signal` 包含 `cross_compare` 字段
- 确认 P2-1（筹码集中度因子）的筹码分布评分已增强，大股东持仓变化率因子待数据管道就绪
- 确认 P1-3（假机构识别）席位名称解析需 Tushare lhb_detail API 数据源（超出本轮范围）
- 全面测试：语法检查通过 + 12/12 核心测试通过 + 交叉对比 5/5 通过 + identify_phase 增强验证通过

**质量修正：**
- chip_data 接线修复前：ASR/CYQKL 分支休眠，仅 OHLCV + RSI 规则生效
- chip_data 接线修复后：ASR/CYQKL/筹码集中度全接入阶段识别
- `MainForceScorer.__init__` 新增 `_chip_indicators` / `_chip_bins` 实例变量

**验证结果：**
- `chip_strategy.py` + `signal_computation_service.py` 语法检查 ✅
- `test_core.py` 12 核心测试全部通过 ✅
- `_calc_cross_compare` 5 种场景验证通过 ✅
- `identify_phase` 3 种调用方式（无chip_data/显式chip_data/缓存chip_data）全部通过 ✅

## 002 — 筹码策略数据源依赖关系重新评估

**用户输入：** 结合全局数据体系情况重新评估筹码策略数据源依赖，不进行代码修改。

**AI执行内容：**
- 审计全局数据体系：251号（全量架构）、266号（缺口全览）、ECM 表结构、采集管道
- 逐项核对 276 号方案 7 项改进的数据源依赖 vs 19 张活跃表的实际状态
- 发现 **核心修正**：假机构席位解析需**新数据源**（席位级 LHB），而非原评估的"lhb 已就绪"
- 发现 **P2-1 最佳路径**：应使用 `top10_holders_cache`（1.28M）+ `daily_basic_cache` 联合，非 `stk_holder_cache` 单表
- 其余 5 项数据均完全就绪，其中 4 项已实施
- 产出 `002-方案存档/277-筹码策略数据源依赖关系重新评估报告.md`

## 003 — 席位级龙虎榜数据源可行性调研与接入设计

**用户输入：** 调研现有数据源是否支持席位级龙虎榜采集，制定存储和调用规则，不修改代码。

**AI执行内容：**
- 深度调研代码库中全部 LHB 相关代码：akshare_provider、tushare_provider、akshare_collector、enhanced_cache_manager、in_memory_store、akshare_reader、data_daemon
- 发现 **2 条可行路径**：
  - **路径A（推荐）**：AKShare `stock_lhb_jgmm_em`（机构买卖明细），使用现有 AKShare >=1.14.0 + 已有 Thread 5 采集基础。按股票查询，返回各买卖席位名称和金额
  - **路径B（备选）**：Tushare `top_inst`（逐席位明细），需新增 provider 方法 + data_daemon 同步
- 设计完整方案：新增 `lhb_detail_cache` 表结构（含 seat_name/seat_type/buy_amount/sell_amount 等字段）、InMemoryStateStore 数据槽、DataManager 接口、席位类型推断规则（"机构专用"→institution）、假机构识别增强逻辑
- 确认全局五层架构合规、Windows 兼容、不与现有 19 张表冲突
- 估算 **~143 行代码**，集中在采集层和存储层
- 产出 `002-方案存档/278-席位级龙虎榜数据源可行性调研与接入设计.md`

## 004 — 实施席位级龙虎榜采集（278号方案代码实现）

**用户输入：** 按278号方案开始实施代码修改，进行实质的采集和存储，确保数据可用。

**AI执行内容：**
- **ECM** (`enhanced_cache_manager.py`): 新增 `lhb_detail_cache` 表（9列+时间戳），`cache_lhb_detail_data` 覆盖式写入，`get_cached_lhb_detail` 双条件查询，`clean_lhb_cache` 联动清理
- **InMemory** (`in_memory_store.py`): 新增 `_lhb_detail: Dict[str, List[Dict]]` 数据槽，`update_lhb_detail/get_lhb_detail/clear_lhb_detail` 三方法
- **DataManager** (`__init__.py`): 新增 `get_lhb_detail` 方法（InMemory优先 → ECM回退）
- **采集器** (`akshare_collector.py`): `_collect_lhb_and_news` 扩展——`stock_lhb_detail_em` 取上榜名单 → 逐只调用 `stock_lhb_jgmm_em` 取席位明细 → 写入 mem_store + ECM；新增 `_classify_seat` 席位类型推断（7个机构关键词）
- **假机构识别** (`chip_strategy.py`): `_detect_fake_institution` 新增席位级检测——机构买入占比 vs 营业部买入占比
- **验证通过**：语法检查✅ 模块导入✅ ECM读写✅ InMemory读写✅ 席位分类✅ DataManager接口✅ pytest 12/12✅

## 005 — 历史数据补采 + 完整性检查 + 假机构识别实测

**用户输入：** 检查补采数据完整性，补采缺失数据，测试假机构识别功能。

**AI执行内容：**
- 发现 AKShare `stock_lhb_jgmm_em` 不可用（East Money API 屏蔽），改走 Tushare `top_inst` 路径
- TushareProvider 新增 `get_top_inst()` 方法
- DataManager 新增 `sync_lhb_detail_data()` 同步方法 + `_classify_seat_name()` 席位类型推断
- 补采 `lhb_detail_cache`：**73,685 条席位级记录**，129 天，2,743 只股票（机构 8.6% / 营业部 91.4%）
- 同步 `lhb_cache`（股票级）：2,117 条，22 天
- 修复 `sync_lhb_data` 列名映射 Bug（`pct_change`→`change_pct` 等）
- 修复 `_detect_fake_institution` 回退逻辑：`lhb_cache` 空时自动用 `lhb_detail_cache`
- 创建 `scripts/backfill_lhb_detail.py` 补采脚本
- **假机构识别实测**：5 只重仓股全部检测通过，席位级增强（"机构买入仅0%"）正确触发

## 006 — 每日自动采集后台注册

**用户输入：** 每天自动运行的数据采集后台是否补充上了该数据的采集规则，可以实现每日的自动启动自动采集？

**AI执行内容：**
- 审计两套自动采集系统：`data_daemon.py`（独立数据进程，15:30日终同步+完整性巡检）和 `scheduler_manager.py`（API进程 APScheduler）
- **data_daemon.py** 新增：
  - `_batch_lhb_detail()` 函数：调用 Tushare `top_inst` API，映射席位字段，写入 `lhb_detail_cache`
  - `_classify_seat_name()` 席位类型推断函数
  - 注册到完整性巡检（`run_integrity_check`）：每日检查 `lhb_detail_cache` 今日数据，为空则自动补采
  - 注册到日终同步（`run_daily_sync`）：15:30 自动全量同步席位数据
- **scheduler_manager.py** 新增：
  - `data_type == 'lhb_detail'` 分支 → 调用 `dm.sync_lhb_detail_data()`
- 实测验证：`_batch_lhb_detail(20260717)` 成功采集并写入 467 条席位记录（780原始），非交易日返回 0

## 007 — 数据采集变动全量审计与自动后台核对

**用户输入：** 检查近期沟通记录，梳理数据采集变动，核对是否已添加到每日自动采集后台，检查数据存储/加工/调用是否正常。

**AI执行内容：**
- 梳理近期（7/17~7/19）所有数据采集变动：

| 日期 | 数据表 | 采集方式 | 自动采集注册状态 |
|:----:|:-------|:---------|:---------------:|
| 7-17 | `sentiment_pool_cache` | AKShare → Thread 6 (1800s) | ✅ `akshare_collector.py` |
| 7-17 | `finance_report_cache` | Tushare → DataManager → ECM | ✅ `scheduler_manager.py` |
| 7-19 | `lhb_detail_cache` | Tushare `top_inst` → ECM | ✅ data_daemon + scheduler_manager |
| 7-19 | `lhb_cache`(修复) | Tushare `top_list` → ECM | ✅ data_daemon(原有) |

- 全链路端到端验证：
  - **存储层**：日线 577万 ✅ / 龙虎榜席位 73,685 ✅ / 融资融券 194,106 ✅ / 筹码分布 599,734 ✅
  - **调用层**：`get_lhb_detail(000004.SZ)`=295行 / `get_cached_lhb(000004.SZ)`=13行 ✅
  - **内存层**：InMemoryStateStore 接口正常（AKShare 运行时填充）✅
  - **消费层**：假机构识别 ✅（000004.SZ 高位 suspected=1.0 机构买入仅0%，无数据股票 suspected=False）

## 008 — 全市场筹码策略扫描 + 选股系统 L1→L2→L3 实测

**用户输入：** 运行全局A股筹码策略找TOP3；按选股系统L1→L2→L3全链路测试（缠论50%+量价50%）。

**AI执行内容：**
- 全市场4980只扫描：瑞晨环保(7.85)/莱茵生物(7.61)/日上集团(7.31) — 均为低位+放量建仓形态
- L1→L2→L3 全链路：4980只→L1通过3845(77.2%)→L2通过70(1.8%)→L3缠论50%+量价50%排名
- TOP1：佐力药业(300181.SZ) 综合分57.2/B评级，缠论bullish(7.70)+L2建仓期确认
- 修复3项：(1) sync_index_daily_data增加HS300 (2) pipeline.py导入降级 (3) 清理项目进度文件
- 产出：`_工作上下文.md`/`待解决事项.md`/`沟通索引.md` 全面更新

## 009 — P3-1 龙虎榜高成功率战法实施 + concept_cache 修复

**用户输入：** P3-1 是否有解决办法，确认后要求实施。

**AI执行内容：**
- 修复 `concept_cache` 空表（G1缺口）：加入 `data_daemon` 巡检检查列表 + 日终同步任务列表，`_batch_concept` 通过 stock_basic 行业降级成功补采 5,528 条/110 行业
- **P2-1 筹码集中度因子**：新增 `_calc_chip_concentration_factor`，使用 `top10_holders_cache` 计算前十大持仓变化率，降级到 `stk_holder_cache` 股东户数变化率。集成到 chip_status 输出。已验证：000001.SZ（-3.71%分散）、300181.SZ（+65.53%集中）
- 实现 `_calc_lhb_high_success_strategy` 四条件评分：
  - 条件1 机构重金（0-40分）：席位级 LHB 机构买入占比
  - 条件2 攻击形态（0-30分）：量价策略信号 / 均线排列降级
  - 条件3 板块效应（0-20分）：行业板块排行热度
  - 条件4 大盘环境（0-10分）：HS300 5日/20日收益率
- 集成到 `_compute_chip_signal` 输出（chip_status.lhb_high_success）
- 3 场景单元测试 + 全链路集成测试 + pytest 12/12 全部通过
