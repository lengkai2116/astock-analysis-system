# 2026-05-30 - 续

### 007 - Phase 4 & 5 实施

**主题**：筹码分布策略 Phase 4 回测赢率评估体系 + Phase 5 输出对齐完成

**关键词**：筹码策略、Phase4、Phase5、SignalWinRateEvaluator、回测赢率、输出对齐、信号匹配

**关键结论**：

Phase 4 完整实施：
- **SignalWinRateEvaluator**（新建 `backtest_evidence.py`, 368行）
  - SignalClassifier 信号类型分类器
  - 核心评估：5/10/20日胜率、平均收益、最大回撤、夏普比率
  - 条件概率分支：按RSI背离/大盘环境/换手率三个维度统计
  - 赢率证据构建器 + attach_win_rates_to_signal() 注入
- **DuckDB 赢率缓存**（`enhanced_cache_manager.py` 新增）
  - win_rate_cache 表 + conditional_win_rate_cache 表
- **Scheduler 定时任务**（新建 `scheduler.py`, 208行）
  - `run_monthly_backtest()` 全量回测入口

Phase 5 完整实施：
- **信号格式对齐**（`signal_computation_service.py` 增强）
  - 每个信号输出新增 `backtest_win_rates` 字段（从缓存读取历史赢率）
  - 新增 `_get_signal_win_rates()` 方法
- **双表持久化** — 信号同时写入 SignalModel 和 StrategyOutput 表
  - 新增 `_sync_to_strategy_output()` 方法
- **信号匹配检查API**（`routes/strategy.py` 新增）
  - `POST /api/v2/strategy/match-check`
  - 输入：用户交易列表 [{ts_code, trade_date, action}]
  - 输出：摘要匹配度 + 每笔交易的信号对齐情况
  - 支持数据库查询 + 实时计算双重溯源

**决策**：
- 信号持久化双写策略：SignalModel 保持向后兼容，StrategyOutput 供账户管理系统读取
- match-check 的 BUY 信号与 BULLISH/WATCH 匹配，SELL 与 BEARISH 匹配
- 账户管理系统尚未实现，match-check API 已就绪可供对接

**整体项目状态**：
- Phase 1 (数据接入): ✅ 完成
- Phase 2 (信号+K线): ✅ 完成
- Phase 3 (风控+仓位): ✅ 完成
- Phase 4 (回测赢率): ✅ 完成
- Phase 5 (输出对接): ✅ 完成
- **11周路线图全部完成**

**涉及文件**：
- 新建: `backtest_evidence.py`, `scheduler.py`
- 修改: `enhanced_cache_manager.py`, `signal_computation_service.py`, `routes/strategy.py`
