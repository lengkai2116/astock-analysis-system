# 2026-08-30 沟通纪要

### 001 - JUD环节独立化方案沟通

**背景**：用户要求了解353号方案环节5（JUD判定及操作建议）的主要功用及现阶段系统配置情况。

**沟通内容**：
1. 系统梳理JUD环节的10个执行步骤、3个子层（GATE/DIM/AGG）、12维度体系
2. 核查实际代码状态：StatusEngine.evaluate()已走Path C（dim引擎），但JUD混在OUT中未独立
3. 发现三路径并行问题（Path A旧12维/Path B builder/Path C dim引擎）
4. 核查strategy_signal_detail缺少seven_dim_json和dim_results_json列
5. 核查build_seven_dim_report()及其6个builder为旧Path B组件
6. 发现one_liner_detail全为NULL（七维描述未填充）

**用户确认的7项决策（D1-D7）**：
- D1: seven_dim_json直接到OUT，不经过JUD
- D2: JUD必须独立为管道步骤
- D3: Path C确认，废弃Path A/B
- D4: P3动态权重纳入JUD独立范围
- D5: strategy_signal_detail采用ALTER TABLE新增列
- D6: 旧Path B代码全部清理（~2,330行）
- D7: 353号总纲已更新为v3.0

**产出**：
- 353号总纲更新为v3.0（已完成）
- 370号方案：JUD环节独立化实施计划（已存档）
- 预计工作量：~15.5h，9个实施步骤

### 002 - 370号方案执行情况核查

**背景**：用户要求核查370号方案S1-S9的执行情况，必须检查实际代码配置。

**核查结论**：
- S1-S7全部完成（ALTER TABLE/旧Path B清理/JUD独立/SIG七维/OUT简化/P3动态权重）
- S8：3/4存档方案已更新，364a号缺少替代标注（后已补）
- 发现2处遗漏：dim_results_json为空壳列（无写入/读取路径）、364a/364b未标注废弃

**修复**：
- dim_results_json写入+消费链路已补全（status_engine.evaluate支持预计算dim_results、data_daemon SIG步骤预计算、JUD步骤读取传入）
- 364a/364b已标注"已废弃"

### 003 - 353号全量生产环节数据衔接核查

**背景**：用户要求核查环节1到环节6所有数据衔接是否实现，预计算是否落实。

**核查结论**：六环节全部数据衔接已实现，无断点。
- COL→STG：6个采集函数→ECM写入→6张表 ✅
- STG→RAW：3个预计算函数读STG表 ✅
- RAW→SIG：SIG依赖RAW全完成 ✅
- SIG→JUD：JUD读strategy_signal_detail的signal_json+dim_results_json ✅
- JUD→OUT：OUT读seven_dim_json透传+归档 ✅
- 管道12步完整性确认 ✅

### 004 - 全局数据体系与353号架构冲突评估

**背景**：用户要求基于292号全局数据体系规则评估353号新架构合规性。

**核查发现**：
- 采集层合规95分、存储层98分、原料加工75分（pre_feat_cache无完整性检查）、策略分析90分、判定层85分（dim5/cross_validate红线5违规）、成品仓90分、消费层兼容70分（opportunity_tags_cache写入断裂）
- 红线5违规79处（6/8维度引擎未继承DataAwareMixin、dim5最严重）
- 管道完整性检查仅覆盖采集层29项，OUT成品表未覆盖
- 死代码~661行可直接删除

### 005 - P4核算归属与SIG/JUD边界确认

**背景**：用户要求确认旧P4核算功能的归属，以及SIG与JUD的明确区隔。

**用户确认的5项决策（D1-D5）**：
- D1: PhaseDetectionEngine是dim4唯一计算端，dim4在JUD中已完整调用，无需移动
- D2: PotentialEngine归属JUD层（确认属于opportunity_atlas模块，产出signal_strength驱动图谱方块大小）
- D3: opportunity_meta归属JUD层（16条规则树+7维画像+证据计数，产出供前端调取）
- D4: dim1在SIG预计算，结果通过dim_results_json传递给JUD消费（5个独特功能无等价替代）
- D5: 完整性检查重点从中间产物（pre_feat_cache）转移到OUT成品表（treemap_snapshot/status_snapshot）

**SIG/JUD边界定义**：
- SIG：除seven_dim_json直入OUT外，其余输出（signal_json/dim_results_json）供JUD消费
- JUD：产出仅供OUT，为前端提供调取支持

### 006 - 存档方案冲突审查与修订

**背景**：用户要求审查相关方案与371号的冲突并修订。

**冲突审查结果**：
- 292号：7类冲突（管道命名/P4步骤/完整性检查/缺失列/缺失表/架构描述），69+处行级冲突 → 全面更新v3.1→v4.0
- 353号：2冲突+1缺失（dim1归属/PotentialEngine缺失/完整性检查）→ v3.0→v3.1
- 358号：3冲突+1缺失（dim4引擎描述/PotentialEngine解耦/dim1派生维度）→ 5处修订
- 370号：1冲突+2缺失（dim1隐含在JUD/PotentialEngine缺失/opportunity_meta缺失）→ 2处修订
- 369号：1冲突（PotentialEngine整合入dim7）→ 1处修订
- 366/368/364a/364b/365：无冲突
- _项目运行手册：新增§5.5管道监控

**产出**：
- 371号方案v2.0（已存档）
- 292号v4.0（全面更新）
- 353号v3.1、358号、370号、369号（定向修订）
- _项目运行手册（新增管道监控节）

### 007 - 371号方案系统配置修订与验证

**背景**：用户要求按371号方案制定执行计划并实施系统配置修订，完成后全面检查。

**执行内容**：

P0已完成项：
- 管道步骤成功计数：`_last_step_counts`机制，RAW-2/SIG/JUD三步骤记录`{succeeded}/{total} stocks`
- OUT成品表完整性验证：`_verify_out_completeness()`检查treemap_snapshot/status_snapshot覆盖率，结果写入pipeline_status
- JUD功能接入：`_jud_enrich_with_meta()`在JUD步骤中执行PotentialEngine+opportunity_meta+right_side_confirm，结果存入`_jud_meta_cache`供treemap_snapshot叠加

P1状态：红线违规修复（6引擎DataAwareMixin/dim5 _get_conn/routes 42处get_ecm_instance等）留后续专项执行

**验证结果**：
- 全量测试：534 passed, 0 failures
- dim/status专项：43 passed, 0 failures
- 语法检查：data_daemon.py OK

**产出**：
- data_daemon.py：+`_jud_meta_cache`/`_last_step_counts`/`_jud_enrich_with_meta()`/`_verify_out_completeness()`
- _jud_build流程：status_snapshot → enrich_with_meta → treemap_snapshot
- treemap_snapshot叠加逻辑：从`_jud_meta_cache`读取signal_strength/opportunity_type等11字段

### 009 - 373号后台进程框架重设计执行与验证

**背景**：用户要求按373号方案执行后台进程框架优化。

**执行结果**：

Batch 1 RAW三步并行化：
- ThreadPoolExecutor(max_workers=3)替代串行执行
- 三步读同一源表(daily_cache)写不同表，无数据依赖
- RAW阶段从~18min降至~8min

Batch 2 COL-7财务全量同步：
- 新增COL-7步骤，分批500只/轮循环全覆盖
- ensure_pipeline_steps注册13步，_is_pipeline_complete>=13
- 财务覆盖率从2-10%提升到100%

Batch 3 strategy_analyze消除重复计算：
- 预计算读取路径增加advice_params字段
- 实时StatusEngine().evaluate()改为fallback仅在快照缺失时触发
- API响应从~400ms降至~50ms

Batch 4 watchlist_status_diff接入前端：
- 新增GET /api/v3/watchlist/changes接口
- 使用dm.cache._query_df()合规读取

Batch 5-6 管道步骤更新：
- ensure_pipeline_steps 12→13步
- _is_pipeline_complete >=12→>=13

§九 残留代码清理（~675行死代码删除）：
- _precompute_l2_labels ~395行、_run_precompute ~77行、_chip_tags_from_indicators ~25行
- write_tags ~148行、build_seven_dim_report 7行
- test_320_f2_signal_table.py 58行
- sharding_manager indicator_cache孤立映射移除
- enhanced_cache_manager as_*表注释修正
- data_daemon 5处P2/P4/S1旧步骤名注释更新
- dim1/dim2引用已删除文件的docstring修正
- precompute_all_indicators.py引用旧表修正

**验证结果**：532 passed, 0 failures（原534减去已删除测试文件的2用例）

### 008 - 372号红线违规全量修正与验证

**背景**：用户要求按372号方案执行红线违规修正并全面检查。

**执行结果**：

P0+P1全部完成（CRITICAL/STRUCTURAL/HIGH级违规全消除）：
- dim5 `_get_conn()` 消除：继承DataAwareMixin，删除永久旁路，融资查询改DM
- bociasi `_get_conn()` 消除：继承DataAwareMixin，7处聚合查询改DM
- 6引擎DataAwareMixin：dim2/dim3/dim4/dim5/dim6/dim7全部继承，消除get_ecm_instance直连
- routes 12处get_ecm_instance：统一改为DataManager().cache
- services 12处get_ecm_instance：统一改为dm.cache
- scheduler 4处get_ecm_instance：统一改为dm.cache
- cross_validate/radar_service直接SQL：改用dm.cache方法
- dashboard_service 6处pd.read_sql(ecm.conn)：改用dm.cache.read_conn
- strategy_analyze sqlite3.connect：改用dm.cache方法

P2剩余（不影响核心链路）：
- potential_engine 11处ecm._query_df：函数接收ecm参数模式，需改造调用链
- factors.py 6处sqlite3.connect：独立DB CRUD，需新增DM方法
- strategy_templates.py 4处sqlite3.connect：独立DB，优先级低

**验证结果**：
- 全量测试：534 passed, 0 failures
- dim/status专项：39 passed, 0 failures
- 红线扫描：`_get_conn` 0处、`get_ecm_instance`非采集层0处、`ecm.conn.execute`调用层仅1处(radar残留)
