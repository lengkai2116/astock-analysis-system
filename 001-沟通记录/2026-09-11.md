# 2026-09-11 沟通记录

## 013 — sync_requests 积压排空（daemon 外集中补采）

**用户输入**：昨日（09-10 §12）积压排空 ~1.5h 仍在进行，用户指出排查方向不对、耗时过长，要求先想清楚可能问题再核查，重点看 daemon 相关代码配置；若需补采，可在 daemon 之外用临时程序集中补充。用户确认：系统处于修复阶段未上线，daemon 非必须常开，必要时可随时关停。

**AI 执行内容**：

### 根因确认
- daemon `_consume_sync_requests_batch`（data_daemon.py:4604）每轮仅补采 50 只（`MAX_PER_TICK=50`，预算对半拆给 finance/stk_holder），每轮 ~30s。8745 条 pending 需数百轮，排空极慢且阻塞主循环的管道驱动/完整性检查/WAL checkpoint。
- 用户"daemon 外集中补采"思路正确：绕开 daemon 消费逻辑，独立批量补采 + 批量标记 done。

### 实施（daemon 外临时脚本）
1. **`backend/scripts/backfill_sync_requests.py`**（新建）：读取 pending 去重 finance_report/stk_holder 代码，用 TushareProvider 批量补采，写入 history_cache 分库（`ecm.cache_finance_report_data`/`cache_stk_holder_data`），完成后批量标记 done。带 `--limit N`/`--dry-run`，复用 `_ts` 限流（0.2s）。
2. **`backend/scripts/mark_sync_requests_done.py`**（新建）：独立 sqlite3 连接 + `PRAGMA busy_timeout=30000`，读取 history_cache 已有数据的 ts_code 集合，标记对应 pending 为 done。用于补采脚本因 `database is locked` 退出后的补标记。
3. **执行**：先停 daemon + watchdog（start_daemon.sh PID 7560 被 kill，daemon PID 14208 被 kill），避免抢写锁。补采脚本用 `setsid` 脱离进程组（Qwen Code 的 run_shell_command 返回时会杀进程组，nohup 也被杀，setsid 成功）。

### 结果
- **history_cache 落库确认**：
  - `finance_report_cache`：64625 行，5394 只，MAX end_date=2026-06-30（正确最新季度）
  - `stk_holder_cache`：41517 行，5080 只，MAX end_date=2026-09-02
- **pending 排空**：finance_report 4845→0，stk_holder 3822→0（含补采 689009.SH 8 行后标记 done）。
- **剩余 53 条 pending 全为 `precompute_*`**（precompute_indicators 22 / precompute_raw 13 / precompute_strategy 18）：由 P3 管道统一处理，daemon 消费时走默认路径直接 `mark_request_done`，重启后自动清空，非补采范畴。
- **确认无数据代码已标记 done**（避免 daemon 死循环重试）：指数代码 399001.SZ/399006.SZ/899050.BJ（合法无数据）+ 北交所 920xxx.BJ 若干（Tushare 无股东户数数据）。

### 当前状态
- daemon 与 watchdog 均已停止（修复阶段，用户确认可关停）。
- 核心 sync_requests 积压已全部排空，数据完整落库。

### 待确认
- 是否重启 daemon（start_daemon.sh / launchd）恢复系统正常运行。

---

## 014 — 数据端写入优先级调度与 STG 启动补采闭环（架构评估）

**用户输入**：重启 daemon 前，先核查 5 个问题（sync_requests 积压风暴/财务同步失效/COL-7 参数错位/分钟脏数据/快照 job 报错）是否还有未解决；随后提出 3 个架构考虑：①daemon 综合功能；②补采/补预计算与盘中采集冲突→调度新进程；③STG 缓存写入调度。用户澄清业务优先级：**SIG/JUD 核心分析依赖完整前一日日终数据，盘中数据仅服务前端展示 → 补采/补预计算优先级高于盘中采集**。

**AI 执行内容**（纯只读核查+评估，未改代码）：

### 5 个问题核查结论
1. sync_requests 积压风暴：✅ 已修复（单只补采+去重+MAX_PER_TICK=50，积压已排空）
2. 财务同步失效（raw.empty）：✅ 已修复（5 财务函数改 if raw:），09-10 22:17 日志实证 0→25549 行
3. COL-7 参数错位：✅ 已修复（_batch_fina_indicator 无参、其余传 codes）
4. 分钟脏数据：✅ 非脏数据（2026-09-11 = W 5484 周K线标注 + 1min 25979 今日实时）
5. 快照 job 报错：✅ 正常现象

**唯一残留**：income/balancesheet/cashflow/forecast 4 表仅 499 只（09-09 写入），修复后 COL-7 未重跑。重启 daemon 后 09-11 管道会自动全量补齐。

### 架构评估
- **daemon 综合功能属实**：单进程 30s 串行主循环承担采集/补采/管道进度/预计算/日终/维护全职责；补采阻塞管道（09-10 实证）。
- **分库隔离关键发现**：写锁是 per-db RLock，冲突仅集中在 **market_cache.db**（分钟 vs 日线补采）；财务/预计算写 financial/history/compute/snapshot 与盘中天然无冲突。冲突窗口远小于预判。
- **进程模型**：不建议新进程（跨进程 RLock 失效，引入 cross-db 锁复杂度）；**单进程优先级调度更优**。
- **STG 启动补采闭环是核心缺口**：_check_data_timeliness / run_integrity_check 只告警/记录，不触发高优补采。
- **建议**：优先级状态机（补采/补预计算 HIGH，盘中 LOW 可降频让路）+ STG 启动检查→高优补采→QA→恢复管道闭环。已起草 **425号方案**（待实施，未改代码）。详见`2026-09-11.md` §14、**425号方案**。

---

## 015 — COL/RAW → STG 存储完整性核查（只读，daemon 停止态）

**用户输入**：win_rate/strategy_signal_detail 问题先记待办待确认；结合前述进展，核查 COL 与 RAW 两个环节产生的 STG 存储是否仍存在数据缺失。要求只读说明不改方案/代码。

**AI 执行内容**（读分库文件 + data_daemon 管道定义 + daemon 日志，未改任何方案/代码）：

### 核查范围
- COL（采集存储，每日/低频）：daily/basic/moneyflow/stk_limit/margin/lhb/concept/index_member/financial/adj_factor/top10_holders
- RAW（原料加工，预计算落宽表）：indicator_ma/macd/other、factor_cache、pre_feat_cache、pattern_score_cache、as_sector_ranking、opportunity_tags_cache
- 基于 daemon 停止态读取 9 个分库（data/duckdb/*.db，实际 SQLite WAL）最新日期 + 行数 + 覆盖股票数。

### 关键实证（09-10/09-11 数据日期）
| 表 | 行数 | MAX日期 | 覆盖股票 | 判定 |
|----|------|---------|---------|:----:|
| daily_cache | 6,409,114 | 09-10 | 5552 | ✅ |
| daily_basic_cache | 919,266 | 09-10 | — | ✅ |
| moneyflow_cache | 212,684 | 09-10 | — | ✅ |
| stk_limit_cache | 143,489 | 09-11 | — | ✅ |
| **margin_cache** | 53,264 | **09-09** | 4443 | 🔴 缺09-10/09-11 |
| lhb_cache | 906 | 09-10 | 472 | ✅ |
| concept_cache | 5,567 | — | 5565 | ⚠️ INDUSTRY_nan 脏 |
| index_member_cache | 10,450 | — | 737 | ✅ |
| **income/balancesheet/cashflow/forecast** | 499 | 09-30-499 | 499 | 🔴 仅499只 |
| fina_indicator_cache | 22,407 | 06-30 | 5555 | ✅ |
| adj_factor_cache_2026 | 2,300,604 | 09-10 | 5575 | ✅ |
| **indicator_ma/macd/other** | 6.41M | 09-10 | — | ✅ |
| factor_cache | 10,448,231 | 09-10 | — | ✅ |
| pre_feat_cache | 49,965 | 09-10 | — | ⚠️ 偏少待核 |
| **pattern_score_cache** | 11,071 | **08-26** | — | 🔴 停更 |
| **as_sector_ranking** | 0 | — | 0 | 🔴 空 |
| opportunity_tags_cache | 822,328 | 08-19 | — | ⚠️ 停更(疑口径) |
| **win_rate_cache** | 0 | — | 0 | 🔴 空 |
| status_snapshot / treemap_snapshot | 5.5K | 09-10 | 5552 | ✅ |

### 结论：COL/RAW → STG 仍存的 5 类缺口
1. **财务4表（income/balancesheet/cashflow/forecast）仅 499 只**（COL-7）：09-10 daemon 22:17 修复后 COL-7 未重跑，重启 daemon 后 09-11 管道应全量补齐（待重启验证）。fina_indicator 已正常 5555 只。
2. **margin_cache 缺 09-10/09-11**（COL-3 margin）：MAX 停在 09-09。429号/424号 P0-1 日期格式修复在工作区未运行，重启后应补。
3. **pattern_score_cache 停更 08-26**（RAW-1 派生）：15:00 run_daily_sync 执行当日日线未写导致回退旧日期，需修正触发时机。
4. **as_sector_ranking 空 0 行**（RAW-2B 板块热度）：今日日志 `[sector_ranking] 采集失败 RemoteDisconnected`（AKShare 网络），板块热度未落库。
5. **win_rate_cache 0 行 + signal_json.signals 恒空**（SIG 层缺信号内容，影响 win_rate 原料）——已记待办第二十一节待确认。

### 附带发现（非本次核心，记录观察）
- `duplicate column name: ma120/ma250/bbi/ene/nine_buy...` 日志反复出现（indicator_* 建表 ALTER 重复列），9-10/9-11 每次 daemon 启动必现——待查是否为幂等建表缺陷导致 RAW-1 建列中断。
- concept_cache 存在 `INDUSTRY_nan` 脏行（板块名 None→'nan'）低影响。
- `[lhb_detail]`/`[news]`/`[concept_ranking]` 采集失败多为 AKShare 网络/正则，非关键路径。

### 待确认后续动作
- 重启 daemon 验证：财务4表补齐 / margin 补采 / pattern_score 触发时机修正。
- 是否针对 as_sector_ranking 空、pre_feat_cache 偏少、dup column 告警做专项核查。
