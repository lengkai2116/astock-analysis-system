# 2026-08-04 沟通纪要

### 001 - 313号系统配置完成情况全面核查与运行验证

**用户输入**：请全面且仔细的核查313号文档的系统配置完成情况，并使用系统数据进行运行验证。

**AI执行内容**：

1. **对照核查**：读取 313号《机会信号强度识别框架》，对照 §八 待实施清单 8 项逐一核对代码实现：
   - 事项1（7 维潜力计算）✅ potential_engine.py compute_potential（截面百分位+缺失0.5中性+关联修正）
   - 事项2（IC 加权）✅ recompute_ic_weights 月度滚动+持久化，函数级验证 15.5s 可运行
   - 事项3（综合公式替换）✅ data_daemon 4c2 接入，旧 `_compute_signal_strength` 已无调用者（死代码）
   - 事项4（时机色接入）✅ 快照 consensus_rate/right_side_confirm 落库透出+前端蓝/黄/灰/棕色板
   - 事项5（前端交互）⚠️ 部分：只看重点/矛盾弱化/悬停解释/时间标注已实现；颜色过滤、组合筛选未实现
   - 事项6（主力成本潜力）⏸ 按设计未启用（main_force_presence 已落库 5578 只）
   - 事项7（冲突缓解 C1-C6）✅ 全部落地
   - 事项8（L4 消费适配）✅ signal_strength 阈值迁移 ≥70/+1、≤40/-1
2. **数据运行验证**（data/duckdb/stock_cache.db，5579 只快照 + 119 万标签）：
   - 分布：80+ 4.3%（目标 5-10% ✅）、40-60 16.3%（目标 40-60% ❌）、<40 72%（目标 30-40% ❌）、0 分 0
   - 覆盖：signal_strength/potential_breakdown/consensus_rate 全 5579 全覆盖
   - 差异性：唯一值 132、均值 30.4、中位 26
   - 时机：强确认 2.6%/基础确认 25.9%/未确认 42.2%/否决 29.2%；共识细分 ≥70% 仅 14 只
   - 交叉：大+可入场 0.73%（基线 1.3%）、大+回避 2.83%（基线 13.2%）
   - 样本：美的/藏格/招金 100 分，breakdown 可解释
   - IC 重估：可运行，当前窗口无正 IC 保留初始权重（设计内）
3. **发现待修复问题**（按优先级）：
   - 【高】前端旧 0-10 口径残留 2 处：opportunity-treemap.html L1625 `>=7`（81.9% 股票被判机会，应 ≥70）、L1543-1545 建议档位
   - 【中】分布未达 §九：中分 16.3%（目标 40-60%）、低分 72%（目标 30-40%），线性拉伸映射整体偏低
   - 【低】快照缺 presence_evidence 列；颜色过滤/组合筛选未实现；死代码 `_compute_signal_strength`；VOTE_MAP 注释残留旧阈值
   - 【观察】ic_weights.json 未生成（daemon 启动触发）；fina_health 修正值待日终重算（035 遗留）
4. **存档**：核查报告已输出至 `002-方案存档/314-313号系统配置完成情况核查与运行验证报告.md`

**结论**：8 项待实施事项 6 完整、1 部分、1 按设计未启用；§九 验证 7 条 6 达标。主要遗留为前端旧口径阈值残留（高）与分布中分偏移（中）。

**待办**：
- 修复前端 2 处旧 0-10 口径阈值（L1625/L1543-1545，一行级改动）
- 复核潜力分布映射参数，抬升中位分至 40-50 区间（保持 80+ 5-10%）
- 低优先级：快照补 presence_evidence 列、前端颜色过滤/组合筛选、清理死代码

### 002 - 313号核查问题全部修复与验证

**用户输入**：请核查并修复所有问题。

**AI执行内容**：

1. **前端旧 0-10 口径迁移（高）**：opportunity-treemap.html 8 处全部迁移 0-100（L1363 tooltip `/100`、L1492 详情 `/100`、L1510 强弱分级 ≥70/≥40、L1544-46 建议档位 ≥70/≥50/≥30、L1551 `/100`、L1562 风险 <30、L1565 ≥40、L1625 高分统计 ≥70）。修复前 `>=7` 使 81.9% 股票被判"机会"（正确口径 ≥70 仅 6.9%）。
2. **分布映射参数复核（中）**：全市场 5579 只重算原始加权分（均值 0.374/中位 0.379），网格搜索映射参数 → `(score-0.14)/0.52`（替代 (0.26,0.44)）：80+ 7.5% / 40-60 40.7% / <40 36.1%，三区间全命中 §九 目标（修复前 16.3%/72.2%）。
3. **快照补 presence_evidence 列（低）**：data_daemon `_build_treemap_snapshot` 补列（MAX CASE + 表定义 + INSERT 43 列占位符核对通过）。
4. **前端颜色过滤/组合筛选（§七2/3）**：新增时机色 4 按钮（🟦可入场/🟨可轻仓/⬜等待/🟫回避）+ 潜力阈值输入，与只看重点/有主力/行业过滤链组合；setMode 非机会模式自动重置；clearFilters 联动重置；补 `.btn.active` 通用高亮样式。
5. **死代码清理**：删除 data_daemon `_compute_signal_strength`（1862-1921，313 号替代后无调用者）；cross_validate VOTE_MAP 注释更新为 0-100 口径。
6. **数据层生效**（用户确认"重启 daemon + 全量重算"）：停旧 daemon（PID 1201，阻塞于完整性检查约 1.5 分钟）→ 生产库备份 /tmp/stock_cache_backup_before_fix.db → verify_313_full.py 全量重算（5579/5579 OK 0 异常）→ 新版快照重建（44 列、presence_evidence 5578 非空、trade_date 更新至 08-04）→ 重启 daemon（PID 34854）。
7. **验证**：pytest 218 passed / 0 failed（修复前 216+2，2 个数据新鲜度失败测试因快照更新至 08-04 通过）；node --check 前端 JS 通过；ruff 修改文件无新增错误（存量 4463 为历史遗留）。

**结论**：5 类问题全部修复并经生产数据验证，314 号报告已追加 §六 修复完成情况。

**待办**：无（fina_health 修正值仍待日终 precompute 重算生效——035 遗留，非本次范围）。

### 003 - fina_health 修正值日终生效验证（035遗留项关闭）
**用户输入**：遗留仅 035 项（fina_health 修正值待日终 precompute 生效）现在已经是16:51了，日终应该结束了，请启动数据采集后台进行验证。

**AI执行内容**：
1. **daemon 确认**：重启后的 daemon（PID 34854，16:46）运行正常，管道全链路推进：C1-C6 → P1（指标 208.8s）→ P2（策略信号 5688.9s）→ P3（因子 875.5s）→ P4（L2 标签 183.3s）→ S1（快照 2.6s），18:44 全部 done。
2. **新标尺代码验证**：ValuationEngine._fina_health（ROE>6% + 负债<70% + ocf/ni>0.8）全市场 5579 只实测：pass 6.7%→**20.7%**、suspicious 83%→70.1%（平安银行等 ROE 高股正确转 pass）。
3. **发现问题并修复**：P4 内 `ve.compute_tags` 对部分股票未更新 fina_health（独立复现 compute_tags 成功，P4 环境失败被 try/except 吞）；且 patch 脚本与 daemon 写锁竞争（P2/P3 阶段逐只写库）致 86 只失败。管道全 done 后写锁空闲，重跑全量更新（5560/5560 OK）+ 重建快照。
4. **最终验证**：快照 fina_health pass 20.7%（旧 6.7%）✅、000001.SZ→pass ✅、快照 44 列 5560 行（signal_strength/consensus_rate/presence_evidence 全非空）、daemon 运行正常。

**结论**：035 遗留项（fina_health 修正值）已生效关闭。附带发现：P4 内 compute_tags 异常被吞（部分股票 fina_health 不更新的隐患）与 daemon 写锁竞争问题已记录，待后续排查 P4 失败根因。

**待办**：P4 内 ValuationEngine 部分失败根因排查（compute_tags 在 daemon 环境对部分股票异常，try/except 静默吞掉——建议后续给 P4 加失败计数日志）。

### 004 - P4 compute_tags 静默失败排查：根因=标签表缺唯一索引（138万行膨胀）
**用户输入**：好的，继续处理下一个遗留项吧。（确认处理：P4 compute_tags 静默失败排查）

**AI执行内容**：
1. **排查过程**：①全市场 5560 只 compute_tags 独立运行 0 异常；②P4 单只测试估值失败 0；③_get_active_codes 含 000001.SZ 且与快照一致——compute_tags 本身无问题。
2. **根因定位**：opportunity_tags_cache **缺 (ts_code, tag_name) 唯一约束**（仅 id 主键）→ write_tags 的 INSERT OR REPLACE 每次**插入新行**而非替换 → 表膨胀至 138 万行（108 万冗余，占 78%）；000001.SZ 的 fina_health 累计 6 行（5 行旧 suspicious + 最新 pass）。此前"P4 未更新 fina_health"实为**查询无 ORDER BY id DESC 取到旧行**的误判；快照重建 SQL 的 `MAX(CASE WHEN tag_value)` **字符串序偏置**（'suspicious'>'pass'）→ 历史行覆盖最新值。
3. **修复**：①DELETE 重复行（保留每组最新 id）：138 万→30.5 万行（3.3s）；②建唯一索引 `idx_tags_unique(ts_code, tag_name)`（未来 INSERT OR REPLACE 正确替换）；③重建快照（MAX 聚合现取唯一值）；④验证 write_tags 替换语义（行数保持 1）、快照 fina_health 分布正确（pass 20.7%，000001.SZ='pass'）。
4. **顺带修复 daemon bug**：主循环 `_running._cleanup_done = True`（_running 是 bool）必然抛 AttributeError → 数据清理每次主循环重复执行且标记失败。改为模块级 `_cleanup_done` 变量（需重启 daemon 生效，当前进程仍旧代码）。
5. **P4 失败计数日志**已加（估值引擎失败计数+样例+整只失败计数，完成日志输出明细）。
6. 备份：`/tmp/stock_cache_backup_before_dedup.db`。

**结论**：P4 静默失败假象解除——真实根因是标签表唯一索引缺失导致的多行冗余与读取错值。表结构已修复，138 万行精简至 30.5 万行。测试 218 passed。

**待办**：①daemon 重启使 _cleanup_done 修复生效；②`_cleanup_done` 修复验证（重启后日志不再出现"数据清理异常"）。

### 005 - daemon 重启验证 _cleanup_done 修复生效
**用户输入**：收到，先重启 daemon 验证清理修复，再继续处理下一项。

**AI执行内容**：停旧 daemon（PID 34854，30s 退出）→ 重启（PID 45927，新代码）→ 监控数据清理触发：20:53:59 **"数据清理完成" 1 次、数据清理异常 0 次**（修复前每次主循环报 "'bool' object has no attribute '_cleanup_done'"）→ _cleanup_done 标记生效（只执行一次）。daemon 运行正常。

**结论**：_cleanup_done 修复验证通过，004 待办①关闭。

### 006 - #8 仪表盘 signal_summary 伪信号移除
**用户输入**：收到，先重启 daemon 验证清理修复，再继续处理下一项。

**AI执行内容**：处理待解决事项 #8（仪表盘策略信号汇总移除）。核查发现：
1. signal_summary 唯一来源是 dashboard_service.py 的**伪信号计数**（bullish=pct>2 数、chanlun=振幅>5% 数、chip=股票数//20、ai=股票数//50——捏造数据，违反 §13 规则）；前端 screener/run dashboard 模式实际不返回该字段（期待落空→降级伪数据→MOCK_SIGNALS mock 兜底）。
2. **修复**：①后端 dashboard_service.py 移除 signal_summary（含未使用计数变量）；②前端 dashboard.html 移除伪信号面板（HTML+CSS+applySignals+MOCK_SIGNALS+loadSignalSummary+调用点），保留说明注释；③API 验证返回字段仅 radar_stocks/total_signals/update_note/updated_at。
3. 验证：前端 JS 语法通过、后端语法通过、pytest 218 passed、API 端点 200 无 signal_summary。

**结论**：#8 已解决——伪信号汇总移除，前端面板移除（避免展示伪数据）。

### 007 - 机会地图筛选控件按模式分区 + 行业边框统一灰色
**用户输入**：问题1接受建议（按模式分区显隐），问题2采用统一行业配色（取消彩色边框统一灰色边框），请修改。

**AI执行内容**：
1. **问题1 模式分区显隐**（opportunity-treemap.html）：
   - 控件分组：面积模式（市值/成交额）+ 竖线分隔 → `mode-market-only`；只看重点/有主力/时机色 4 按钮/潜力≥/机会类预设（主力跟庄/短线催化/做T）→ `mode-opportunity-only`；价值低估预设 → `mode-value-only`
   - 新增 `_applyModeControls(mode)`：setMode 切换时按模式显示/隐藏分组；初始 market 模式调用一次
   - 搜索框/标签栏/行业/topN 保持通用（三模式均有意义）
2. **问题2 行业边框统一灰色**：
   - 行业组块边框 `SECTOR_COLORS[sectorName] || '#555'` → 固定 `'#555'`（灰）
   - 删除原型遗留 mock 映射 `SECTORS` + `SECTOR_COLORS`（银行蓝 #3b82f6 等 5 行业色，仅此一处引用）
3. **验证**：JS 语法通过；node 模拟 DOM 验证 `_applyModeControls` 三模式显隐正确（market/opportunity/value 各组 display 切换符合预期）；SECTOR_COLORS 无代码残留（仅注释提及）；pytest 218 passed。

**结论**：两项修改完成。银行板块蓝色边框消除（统一灰），筛选控件按地图模式分区展示（market=面积/opportunity=潜力时机/value=价值预设）。

### 008 - 机会地图移除渐变条改四色块标记（方案A）
**用户输入**：按方案A修复，移除渐变条，改用四色块标记。

**AI执行内容**（opportunity-treemap.html）：
1. updateLegend 机会分支：隐藏渐变条（legendGradient）+ 两端标注（legendEdgeLow/High，原"← 低/高 →"为估值语义错用），改图例区**四色时机色块**（legend-chip：蓝#2563eb 可入场/黄#eab308 可轻仓/灰#6b7280 等待/棕#8b5a2b 回避，与 TIMING_COLORS 一致）；market/value 分支恢复渐变条显示（showBar(true)）。
2. 删除 `.legend-gradient.opportunity` CSS（原灰→蓝→绿→橙→红五色渐变，含红绿违背 313 §5.2）；新增 `.legend-chip` 色块样式。
3. 分享文案无需改（已是四色描述）。
4. 验证：JS 语法通过；node 模拟 updateLegend 三模式（market/value 渐变条显示 ✅、opportunity 隐藏+四色块 ✅）；.legend-gradient.opportunity 无残留；pytest 218 passed。

**结论**：机会地图图例与四色时机标记一致（蓝/黄/灰/棕色块），红绿渐变条消除。
