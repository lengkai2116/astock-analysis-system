# 沟通记录

## 2026-09-09

### 1 - B3 收尾核查：管道推进观察 + 红线核查 + 索引补建

**用户输入**：结合上下文记忆的内容，开展下述工作项（B3 已完成，推进待办清单）

**AI执行内容**：
1. **核查 daemon/管道状态**：daemon PID 1218（07:24:42 重启）运行中；20260908 管道
   COL-1..7/RAW-1/2/3/RAW-2B 均 done，SIG 已启动（07:25:32 周线预热完成 = SIG 前置步骤，
   主线程 sample 在大 SQLite 读取 compute_batch，CPU 活跃）
2. **判断 SIG=pending 为显示问题**：`mark_step_running` 无 commit，running 状态在事务内
   未提交，外部 CLI 查询显示 pending，实际已在计算
3. **架构红线违规核查（待办3）**：工作上下文记录的 4 处违规逐一验证，全部已修复
   （dim2/dim5/dim4 的 MA 直调函数已不存在，BociasiQuadrant 已改 market_stats 参数注入）；
   **新增发现 2 处残留**：dim2:507 / dim3:338 `_load_precomputed_macd()` 直调
   `dm.get_cached_indicators()`（缠论背驰 MACD 预计算，读 RAW-1 表非数据源，有缓存兜底），
   修复需把 data_context['indicator_macd_df'] 传入 ChanlunAnalyzer，侵入面中等，暂缓
4. **主库索引补建（待办4）**：主库 stock_cache.db 缺 idx_ind_other_date（分库已有）；
   首次尝试被 daemon 写锁阻塞（30s 超时）→ 后台重试任务（6轮×120s busy_timeout），
   日志 /tmp/build_idx_other_date.log

**核查依据（关键证据）**：
- pipeline_status：`20260908|RAW-2B|done|13:56:44→13:57:36|OK (52.5s)`，SIG/JUD/OUT pending
- 昨晚日志：21:57:36 `[管道] RAW-2B → done (OK (52.5s))`（同时有一次板块热度写盘 locked 警告但步骤 done）
- 今晨日志：07:25:32 周线缓存预热完成（批次 1）→ SIG 进入 compute_batch
- sample 1218：主线程 sqlite3_step B-tree 读取（非锁等待）；多个 worker 线程 PyThread_acquire_lock_timed
- strategy_signal_detail：33,448 行，最新 trade_date=2026-08-26（SIG 尚未写入新一轮）
- bociasi_quadrant.py：`__init__(self, ecm=None, market_stats: dict = None)`（412号B2 v3.0 已落地）

**发现的问题（2 项）**：
1. `mark_step_running` 无 commit —— 管道 running 状态对外不可见，诊断困难（建议未来补 commit，本次不动）
2. dim2/dim3 `_load_precomputed_macd` 直调 DM —— 红线合规性瑕疵，待评估修复

**修改文件**：
- _工作上下文.md（更新：SIG 已启动确认、红线核查结论、索引补建中）

---

### 2 - ⛔ 索引撞锁回归 + SIGKILL 补救（08:05-08:16）

**用户输入**：无（本轮由 AI 主动发现并报告，用户确认 SIGKILL 重启方案）

**AI执行内容**：
1. **回归发生**：主库 idx_ind_other_date 索引创建成功（08:05:37，后台任务 attempt=3）——
   但 CREATE INDEX 持排他写锁期间，恰逢 SIG 批量写 strategy_signal_detail（08:05:35）
   → `批量写 strategy_signal_detail 失败: database is locked`，5555 条新信号未落盘
2. **连锁确认**：SIG 标 done 但数据缺失（08-26 仍为 09-04 旧版本）；JUD 08:05-08:13
   基于旧信号构建（453.6s/5549只）后 done——若不干预会产出陈旧快照
3. **用户确认补救方案（SIGKILL 重启）**：
   - 08:16 SIGKILL 旧 daemon（PID 1218）
   - SIGKILL 空窗（看守脚本 30s 内未拉起）重置 pipeline_status：
     SIG → pending（`RE-DO: SIG批量写失败(索引撞锁)重置重跑`）、
     JUD → pending（`RE-DO: SIG信号重跑后需重算`）
   - 看守脚本 start_daemon.sh 自动拉起新 daemon（PID 6321，08:16），
     完整性检查后 _drive_pipeline 自动重跑 SIG→JUD→OUT

**教训（后续执行注意事项）**：
- daemon 活跃写期（管道推进 SIG/JUD 阶段）禁止大表 CREATE INDEX——排他写锁与批量写盘撞锁
- 数据库写操作前必须先查 pipeline_status 推进状态；SIG/JUD 计算期不做 DDL
- 本次索引已建成并保留（主库/分库一致），但代价是 SIG 全量重跑（约 40min）+ JUD 重算（约 48min）

**验证结果**：
- 主库索引确认：`idx_ind_other_date` 存在于 sqlite_master ✅
- SIG/JUD 均重置为 pending ✅，新 daemon PID 6321 运行中 ✅

**待办（继续观察）**：
1. 观察重跑推进：SIG（~08:17-09:00）→ JUD（~09:00-09:50）→ OUT
2. 验证 strategy_signal_detail 08-26 出现 09-09 新写入（cached_at 时间戳）
3. 验证 B3 sector_heat 消费：dim_results_json 中 dim5 heat 字段（首次真实消费）

---

### 3 - SIG 写失败根因分析（对照 356/357/377/398 号方案）+ 421号方案存档

**用户输入**：存储写库之前有明确的方案确认具体操作标准，请查阅后再做根因分析（不改代码）；
随后指示：根因结论写入沟通记录和方案存档，制定详细修订实施计划（不修改实际代码）

**AI执行内容**：
1. **按用户提示先查阅既有写库方案标准**（未先改代码）：
   - 356号§3.1：strategy_signal_detail（成品）归 snapshot_cache.db
   - 357号（355号L239注记）：迁入 snapshot_cache.db；snapshot 分库 busy_timeout 需 10s→30s
   - 375号§4.2/§7.1（08-28已实施）：读方走 sharding_manager 读分库；记录"跨库存储"问题
   - 377号§1.2：SIG 输出表 = snapshot_cache.db 的 strategy_signal_detail
   - 398号§1.2/§3.2（待实施）：缺陷= _batch_write_signal_detail 直连 _ecm.db_path 路由不一致
2. **根因链（3层，与 B3 同构但更根本）**：
   - **偏差1（写库目标≠357号标准）**：_batch_write_signal_detail (data_daemon.py:5021)
     直连 _ecm.db_path（主库 stock_cache.db），而 sharding_manager 路由已指向 snapshot_cache.db
     → SIG 写入主库残留副本，分库权威副本不被更新
   - **偏差2（busy_timeout 10s < 355号注记 30s）**：主库与采集/主循环写者争锁
     （08:05:35 与索引撞锁叠加；08:19:48 无 DDL 复现→证明与索引无关，主库路径天然冲突）
   - **偏差3（失败静默吞错，398号已列）**：写失败仅 warning → SIG 标 done；
     run_integrity_check 无补写（注释声称的"完整性检查兜底"不存在）
3. **关键事实核验（实测）**：
   - snapshot_cache.db（标准分库）：33,440 行，08-26=5,578 行（08-27写入）✅ 权威副本完整
   - stock_cache.db（主库残留）：33,448 行，08-26=5,586 行（09-04写入）⚠️ 错误写目标
   - read_conn 与主 conn 同指主库 → JUD _load_signals 读主库残留；
     写方与读方（标准读法）目标不一致 = 375号问题#6 同源
4. **存档 421号方案**：`002-方案存档/421-SIG信号写库路径对齐与失败暴露根治方案.md`
   - 4 修复项：R1 写库走 sharding_manager 分库路由（P0）；R2 snapshot busy_timeout 10s→30s（P0）；
     R3 写失败抛出→SIG failed 自动重试（P0）；R4 读方切分库+主库残留备份清理（P1）
   - R1 与 R4a（读方迁移）必须同批实施（避免写分库后消费链读不到）
   - 含 L1-L5 验证方案、回滚策略、风险清单、改动清单
5. **已核实支撑**：sharding_manager 提供 execute_batch_insert(L168)/get_connection(L104)/
   execute_insert(L152)/table_exists(L195)，带分库级写锁——R1 可复用无需新增 API

**结论修正**：
- 此前"索引撞锁"归因需修正：索引是第一次失败的直接诱因（时间吻合），但更深根因是
  写库路径绕过 357 号分库隔离——按标准写 snapshot_cache.db 则主库锁完全无关
- SIG 数据未丢失（分库权威副本 08-27 完整），但 SIG 标 done 掩盖了写主库失败，
  双写不一致持续存在（375号问题#6 同源）

**修改文件**：
- 002-方案存档/421-SIG信号写库路径对齐与失败暴露根治方案.md（新建，待实施）
- 001-沟通记录/2026-09-09.md（本条）

**待办（继续观察，代码未改）**：
1. 观察 20260908 管道（SIG 第2次 done 但写主库失败；分库数据仍权威）
2. 用户确认后按 421 号方案实施 R1-R4（分阶段，重启 daemon）

---

### 4 - 421号方案 R1-R4 实施完成（serena MCP，2026-09-09 全天）

**用户输入**：调用 serena MCP 工具实施 421号方案 R1-R4（含后续确认：重启 daemon + R4b 清理）

**AI执行内容**（serena 符号工具实施 + 验证）：
1. **R1（写分库）**：`_batch_write_signal_detail` 改走 `sharding_manager.execute_batch_insert`
   → snapshot_cache.db 分库（不再直连主库 `_ecm.db_path`）
2. **R2（busy_timeout 30s）**：确认 snapshot 分库连接 busy_timeout=30000
   （sharding_manager.get_connection 内置，实测 30000，无需改码）
3. **R3（失败暴露）**：新增 `SignalWriteError` 异常类；`_batch_write_signal_detail` 失败抛出；
   `_precompute_strategy_signals` 增加 `except SignalWriteError: raise` 透传
   → `_run_pipeline_step` 捕获 → SIG failed → 管道自动重试（≤3 次），不再静默标 done
4. **R4a（读切分库）**：`get_latest_signal_detail`/`has_signal_detail`/`get_signal_detail`/
   `cache_signal_detail` 全改走 sharding_manager 读/写分库；OUT 七维透传 UPDATE 改在
   snapshot 分库连接执行（修 375号问题#6 跨库 UPDATE 静默 0 行）；JUD dim_results 预取走分库；
   health.py `data_freshness`+`get_cache_freshness_stats` 按 sharding 路由
5. **R4b（主库清理）**：新建 `backend/scripts/migrate_signal_detail_shard.py`
   （备份→校验→清空，幂等可回滚）

**验证结果**：
- 端到端：R1 写落分库 True/主库 False ✅；R4a 读分库回读正确 ✅；F3 消费 000007.SZ 摘要完整 ✅
- R2 busy_timeout=30000 实测 ✅；R3 异常链确认 ✅
- ruff：ECM/health/迁移脚本/测试全通过；data_daemon 34 个 lint 均为既有历史问题（diff 零新增）✅
- 320 测试回归 4/4 通过（`_sample_with_signals` 改读分库）✅
- **R4b 最终验证（daemon 空窗期执行）**：主库残留 0 ✅、备份 33,448 行 ✅、分库权威 33,440 行 ✅

**执行过程要点**：
- 新 daemon 两次重启（PID 16188→30450）加载新代码
- R4b 清理遇 daemon 内部连接持未释放写锁（运行 4.5h 积累）→ 用户确认重启 daemon 空窗执行成功
- 主库 TEST.R1 测试残留随 R4b 一并清理

**最终状态**：SIG 信号写/读全链路收敛到 snapshot_cache.db 分库（对齐 357 号标准），
双写不一致（375号问题#6）根治。

**修改文件**：
- backend/data_daemon.py（R1/R3 + OUT 透传 + JUD dim 预取）
- backend/app/data/enhanced_cache_manager.py（R4a）
- backend/app/routes/health.py（R4a）
- backend/scripts/migrate_signal_detail_shard.py（新建 R4b）
- backend/tests/test_320_f3_p2_consumer.py（R4a 测试适配）
- 002-方案存档/421-SIG信号写库路径对齐与失败暴露根治方案.md（实施记录 §10）

**待办（后续）**：
1. 观察下次 SIG 日终同步（15:30 后）写入 snapshot_cache.db 无 database is locked
2. 20260908 管道 SIG/JUD/OUT 已 done（旧路径完成），下次管道推进验证新路径
3. （可选）dim2/dim3 MACD 直调残留修复
4. （建议）mark_step_running 补 commit

---

### 5 - SIG data_context 根因核实与修复（ts_code 遗漏）+ 重跑全量验证

**用户输入**：先核实清楚 SIG 的问题根因 → 重启 daemon 并重跑 SIG 验证 → 更新归档

**AI执行内容**：
1. **根因定位**：`data_daemon.py:4992` SIG 预计算路径调用
   `_build_dim_engine_results(_tags, _signals, {}, _lifecycle)` 时**遗漏 `ts_code=ts_code`**。
   dim1 evaluate 中 `ts_code = ts_code or tags.get('ts_code','')`，而 tags（pre_feat 扁平化）
   无 ts_code 键（419号方案已注明）→ ts_code 为空 → `if ts_code:` 数据预加载块整体跳过
   → loaded_data 空 → data_context=None、quality_level=failed、missing_tables=[]（未进加载
   故无缺失检测——解释"failed 但 missing_tables 空"的矛盾）
2. **为何 JUD 正常**：JUD 主路径 status_engine.py:88 显式传 ts_code；且 370号修正后 JUD
   复用 SIG 预计算的 dim_results → SIG 的 null 传导到 JUD 成品
3. **修复**：data_daemon.py:4992 补传 `ts_code=ts_code`（与 JUD 路径对齐，1 行改动）
4. **重启 daemon（39078）重跑 SIG**：SIGKILL 30450 → 空窗重置 SIG/JUD pending → 看守拉起
5. **SIG 重跑全量验证（21:06 done，650.7s——比 207s 慢因 data_context 真实加载 14 项）**：

| 验证项 | 修复前 | 修复后 |
|--------|--------|--------|
| data_context null | 5549/5549（100%） | **0/5549（0%）** ✅ |
| quality_level | failed | **degraded**（data_loaded=True） |
| loaded_keys | 0 | **14 项**（daily/fina + 3 indicator + 8 ext 组） |
| sector_heat 消费 | 无（fallback） | **5549/5549 data_context 含 sector_heat**（`{"饲料":{heat_level:top_20,rank:14}}`）|
| dim5 emotion audit | — | 「板块热度 top_20」条件正常执行（银行股不在榜→none 合理） |
| 主库残留 | — | **0 行** ✅ |
| 批量写失败 | — | **21:06 后零失败** ✅ |

**最终结论**：
- ts_code 修复使 dim1 门禁在 SIG 预计算环境真实加载 data_context（14 项原料 + sector_heat），
  dim2-dim7 走 data_context 主路径（AGENTS.md 红线 #9 完全满足）
- **B3 sector_heat 首次真实消费实证完成**（B3 回填 66 行 → dim1 加载 → dim5 audit 消费）
- 421 号方案 R1-R4 + ts_code 补充修复全部闭环

**修改文件**：
- backend/data_daemon.py（ts_code 补传，data_daemon.py:4992）
- 002-方案存档/421-SIG信号写库路径对齐与失败暴露根治方案.md（补充修复 §10 更新为已验证）

**待办（后续）**：
1. JUD 重跑（pending，将用修复后 dim_results 重算成品仓）
2. （可选）dim2/dim3 MACD 直调残留修复
3. （建议）mark_step_running 补 commit

---

### 6 - JUD/OUT/treemap 写分库修复（375号T1 不完整修复根治，待明日验证）

**用户输入**：修复 JUD 写分库（Recommended）→ 重启重跑询问时选择"待明日启动时进行验证"

**AI执行内容**：
1. **发现**：JUD/OUT 重跑验证时，status_snapshot 最新批次（snapshot_date=2026-09-09、5,549
   行）在**主库**，而分库仍 08-27 旧批次——JUD 写 status_snapshot 仍走 `_ecm.conn`（主库），
   OUT 透传读分库 → **读写分叉**（375号T1 只切读方、写方遗留主库）
2. **修复 4 处写路径切分库**（snapshot_cache.db）：
   - `_build_status_snapshot`（JUD）：建表/INSERT/commit/原子替换 → `_snap_conn`
   - `_build_treemap_snapshot`（JUD）：建表/INSERT/commit/原子替换 + status_snapshot
     读取 → `_tm_conn`（原 `_query_df` 读主库 miss 分库数据）
   - `_out_transmit_seven_dim`（OUT）：`_snap_conn` 提升函数级，status/treemap 归档→分库；
     watchlist_status_diff 段保留主库（路由 None=主库，正确）
   - `_verify_out_completeness`：treemap/status 计数 → 分库连接
3. **验证**：AST 通过；全 daemon 无 `_ecm.conn` 写 status_snapshot/treemap_snapshot
   （grep 空）；watchlist 段 `_ecm.conn` 保留正确

**当前状态**：代码修复完成；**全链路重跑验证待明日启动**（用户确认）。
treemap/status 主库残留（5,544/5,549 行）待验证后清理。

**修改文件**：
- backend/data_daemon.py（_build_status_snapshot / _build_treemap_snapshot /
  _out_transmit_seven_dim / _verify_out_completeness 4 函数写分库）
- _工作上下文.md（待明日验证状态）

**待办（明日）**：
1. 重启 daemon → 自动重跑 20260909 管道 → 验证 JUD/OUT 写分库
2. 清理主库 status/treemap 残留
3. （可选）dim2/dim3 MACD 直调残留修复
4. （建议）mark_step_running 补 commit