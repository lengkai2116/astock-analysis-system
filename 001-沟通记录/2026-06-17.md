# 2026-06-17 沟通纪要

## ### 167 - indicator-ide.html 全部4个Bug修复

**用户输入**：浏览器检查左侧Y轴数值、键盘滚动、副图数值、十字光标残留共4个问题，并请调取合适技能制定改善方案后执行修复。

**AI执行内容**：
1. 加载 `plan`、`spec-driven-development`、`frontend-ui-engineering` 技能
2. 浏览器交互验证 4 个检查点，确认全部存在 Bug
3. 制定完整修复方案（`.hermes/plans/2026-06-17_152700-fix-all-4-bugs.md`）
4. 执行全部修复并验证：

| Bug | 问题 | 根因 | 修复 | 验证 |
|-----|------|------|------|------|
| ① | 左侧Y轴数值不显示 | 侧栏折叠IIFE无chart.resize()，canvas初始648px | IIFE末尾+50ms resize；删除错误closeLine静态初始化 | canvas=784px ✅ |
**涉及文件**：`_ui-prototype/indicator-ide.html`（共9处修改）

### 168 - 二次验证与十字光标残留优化

**用户输入**：要求再次验证4个检查点，确认键盘滚动、子图同步、Y轴收盘价、十字光标清除效果。

**AI执行内容**：
1. 验证①：键盘到边界能触发dataZoom滚动（start从86→81） ✅
2. 验证②：鼠标在副图时主图同步显示十字光标（垂直线+水平线+Y轴浮标） ✅
3. 验证③：tooltip axisPointer type='line'→'cross' → Y轴左侧显示收盘价浮标（如13.92） ✅
4. 验证④：主图和子图均绑定mouseleave→hideTip+closeLine隐藏；zr mousemove入口加_isSyncing阻断
5. 初始页面干净无残留十字光标 ✅
6. ECharts原生`trigger:'axis'`在真实鼠标操作下自动处理mouseleave，结合额外安全网确保不残留

**关键改进**：
- Y轴axisPointer.type从`line`改为`cross`，使Y轴浮标随十字光标显示收盘价
- 副图mouseleave handler + _isSyncing标志位阻断后续重触发
- zr mousemove入口增加_isSyncing检查

### 169 - 统一十字光标方案（路线A）实施与存档

**用户输入**：确认统一十字光标方案的全部规范，要求存档。

**AI执行内容**：
1. 实施单ECharts实例 + 多grid + graphic全渲染十字光标架构
   - subDefs迁移为getSeries(gi)格式（20个指标）
   - renderSubs()改为仅创建DOM header
   - rebuildChart()构建多grid布局
   - updateCrosshair(idx)统一渲染入口
   - 删除约120行冗余同步/防环代码
2. canvas高度=0修复：rebuildChart中加chart.resize()
3. dataZoom范围外索引自动滚动：convertToPixel返回null时自动平移
4. 恢复默认副图配置 `['成交额','vol']`
5. 存档至 `002-方案存档/175-统一十字光标技术方案.md`

### 170 - 4个十字光标/副图/拖拽Bug修复

**用户输入**：5个需要解决的问题：
1. 十字光标横轴左端收盘价保留小数点后两位
2. 日期通知在竖轴与时间轴相交处
3. 副图header定位到各副图左上角
4. 主副图时间轴长度一致且同步
5. 左键拖拽到右端停止放大

**AI执行内容**：

**确认问题**：
- Issue 1 ✅ 价格标签已有正确2位小数显示
- Issue 2 ❌ 日期标签在lastGrid底部(626) → 应移到firstGrid底部(337)
- Issue 3 ❌ 副图header在canvas底部(646/666) → 应定位到各sub-grid左上角(340/488)
- Issue 4 ❌ dataZoom `xAxisIndex:0`仅控制主图 → 需包含所有grid
- Issue 5 ❌ 拖拽到边界后range从14%缩小到10.43%

**修复实施**：
| Bug | 修复 | 文件 |
|-----|------|------|
| ② 日期标签 | `cdl.y`从 `lastGrid.y+height+2` → `firstGrid.y+height+2` | indicator-ide.html:810 |
| ③ 副图header | CSS `#sub-chart-area`改为 `position:absolute; left:0; top:0; pointer-events:none`；`.sub-chart-header`加 `position:absolute; left:0; pointer-events:auto`；rebuildChart中按grid rect定位header `top: rect.y-20` | indicator-ide.html:25,730 |
| ④ dataZoom | `xAxisIndex:0` → `xAxisIndex:Array.from({length:1+activeSubs.length},...)` | indicator-ide.html:712 |
| ⑤ 拖拽边界 | 移除 `Math.max(0,...)`/`Math.min(100,...)`预截断→边界检查时用原始range计算 | indicator-ide.html:897-900 |

**浏览器验证结果**：
| # | 检查项 | 结果 |
|---|--------|------|
| 1 | 价格标签 `cpl.text="17.75"` 2位小数, x=53 | ✅ |
| 2 | 日期标签 `cdl.y=337` firstGrid底部(335+2) | ✅ |
| 3 | 副图header `top:340px` `top:488px` 各sub-grid上方 | ✅ |
| 4 | dataZoom `xAxisIndex:[0,1,2]` 三grid同步(同一像素返回同dataIndex) | ✅ |
| 5 | 拖拽边界逻辑: `ne=106→clamp→ne=100,ns=86,range=14` 不缩放 | ✅ |
| — | 十字光标竖线 y1=10 y2=624 覆盖全部3grid | ✅ |
| — | 副图切换(MACD)后十字光标保留 "8/4"→"8/4" | ✅ |
| — | 叠加切换(BBI)后十字光标保留 "8/4"→"8/4" | ✅ |
| — | 副图数值随十字光标更新 "额: ¥31400" "MA5:17.76" | ✅ |

### 171 - 十字光标层级/日期标注/副图布局重叠修复

**用户输入**：三个问题：1) 价格标签可能被遮蔽需调至最上层 2) 日期显示在主图左上角而非十字光标与时间轴相交处 3) 副图名称/按钮同主图时间轴重叠，第二副图名同左侧Y轴重叠

**根因分析**：
| 问题 | 根因 |
|------|------|
| 价格标签遮蔽风险 | z-index 仅101（series最高z=2），但ECharts graphic层同上下文 |
| 日期不在交叉处 | cdl.y=337靠近grid底部，但旧布局header[1] y=340仅3px差距→视觉上被header遮掩 |
| 布局间距过紧 | grid[0] bottom=335 → header[1]=340（5px）；header left=0（覆盖Y轴标签x=55） |

**修改内容**（`_ui-prototype/indicator-ide.html`）：
1. **z-index提升**：cv/ch → z:9998，cpl/cdl → z:9999（line 801-810）
2. **布局间距扩大**（line 677-694）：
   - `SUB_FULL=152`（原128+20=148）
   - `totalH=395+activeSubs.length*SUB_FULL`（原350+subH）
   - `nextTop=395`（原360），grid[0]底部→header[1]间距从5px→40px
3. **header左对齐修正**（line 732）：`hdr.style.left='55px'`（原'0'，对齐grid left）

**验证结果**（默认2 sub + 切换至3 sub MACD）：
| 项目 | 值 | 判定 |
|------|-----|------|
| 价格标签 z-index | 9999 > series z:2 | ✅ 最上层 |
| 日期标签 y | 337（grid[0] bottom+2=x轴交叉处） | ✅ 正确位置 |
| header[1] top | 375px（距grid[0] bottom 40px） | ✅ 不重叠 |
| header left | 55px（对齐grid左边界） | ✅ 不覆盖Y轴 |
| cv y1→y2 | 10→815（全部4grid） | ✅ |
| 多grid同步 | idx=665全て一致 | ✅ |
| 键盘移动 | 8/4→8/7 | ✅ |

### 172 - 十字光标价格/日期标签不可见修复

**用户输入**：仍看不到横轴左端价格，日期在左上角红色而非交叉点。

**根因**：ECharts 5.4.3 graphic text 元素通过 `shape.x/y` 或 `left/top` 定位不可靠，实际渲染位置与设置值不一致 — 文本落在 (0,0) 而非目标坐标。

**修改方案**（`_ui-prototype/indicator-ide.html`）：
放弃 ECharts graphic text 渲染，改用 **DOM 元素叠加**（绝对定位 div 覆盖 canvas）：
1. HTML 新增 `#cpl-dom`（价格标签）和 `#cdl-dom`（日期标签）div（line 232-233）
2. CSS：`z-index:10000`，`position:absolute`，`pointer-events:none`（line 25-27）
3. JS `updateCrosshair()`：移除 graphic 中 text 元素，改为设置 DOM 元素的 `style.left/top` 和 `textContent`（line 747-822）
4. 隐藏路径：设置 `style.display = 'none'`；显示路径：`style.display = 'block'`

**验证结果**：
| 检查项 | 值 | 状态 |
|--------|-----|------|
| 价格标签 | "17.45" at left=53px, top=103px, display=block | ✅ 可见 |
| 日期标签 | "8/5" at left=286px, top=337px, display=block | ✅ 交叉点 |
| 键盘移动更新 | 价格/日期同步更新 | ✅ |
| 鼠标离开隐藏 | display=none | ✅ |

### 173 - 十字光标标签微调（位置/颜色/样式）

**用户输入**：
1. 价格标签上移8px，日期标签左移8px
2. 字体白色
3. 加文字框：深蓝色底，半透明
4. 取消半透明
5. 改为蓝色
6. 调整内边距（根据字体大小）

**修改内容**（`_ui-prototype/indicator-ide.html`）：
| 修改 | 值 |
|------|-----|
| 价格标签 top | `y - 11` → `y - 19`（上移8px）|
| 日期标签 left | `x - 20` → `x - 28`（左移8px）|
| 字体颜色 | `#EF4444` → `#FFFFFF`（白字）|
| 文字框背景 | `rgba(15,23,60,0.75)` → `#0F173C` → `#1e3a5f`（蓝色）|
| 内边距 | 1px 4px → 2px 6px/2px 5px（适配12px/10px字体）|
| 边框圆角 | 2px → 3px |

**样式值**：
```css
#cpl-dom { /* 价格标签: 12px字体 */
  background:#1e3a5f; color:#FFFFFF; padding:2px 6px; border-radius:3px;
  font-family:monospace; font-weight:bold; font-size:12px;
}
#cdl-dom { /* 日期标签: 10px字体 */
  background:#1e3a5f; color:#FFFFFF; padding:2px 5px; border-radius:3px;
  font-weight:bold; font-size:10px; white-space:nowrap;
}
```

### 174 - 筹码分布面板行业规范研究与代码比对

**用户输入：** 要求搜索了解股票筹码分布图概念，对比现行修订方案是否存在问题，先整理说明不修改。

**AI执行内容：**
1. 研究同花顺、东方财富、通达信等主流软件筹码分布图行业标准
2. 识别5项规范与代码实现偏差：

   | 偏差 | 行业标准 | 现行代码 |
   |------|---------|---------|
   | 颜色反向 | 红色=获利盘(涨)、蓝色=套牢盘(跌) | 蓝色=获利、红色=套牢 |
   | 方向反向 | 从价格轴向右延伸 | 从右向左延伸 |
   | 线条高度 | 薄线~2px（同花顺标准） | 粗柱~10px |
   | 价格标签 | 左侧需显示5档价格标签 | 完全缺失 |
   | 白线关联 | 白色 = 十字光标横线（收盘价同一水平） | 未与十字光标联动 |

3. 确认用户补充的2个要点：
   - 白线应与十字光标所选日期的K线收盘价一致
   - 筹码分布数据应随十字光标日期动态刷新（非固定不动）

4. 更新166.md §6.7.2颜色规范（红=获利/蓝=套牢/白色线=十字光标横线）

**涉及文件：** `_ui-prototype/indicator-ide.html`、`002-方案存档/166-前端UI原型逐页调整记录.md §6.7.2`

### 175 - 筹码分布面板全部修正实施与验证

**用户输入：** "请按此评估进行修正"，修正过程以中文显示。

**AI执行内容：**
1. 重写 `renderChipChart()` 函数：
   - 颜色：红色(`#EF4444`)=获利盘，蓝色(`#3B82F6`)=套牢盘
   - 方向：从 `left:42px` 向右延伸
   - 线条高度：2px（薄线非粗柱）
   - 新增5个价格标签：`font-size:8px; color:#94A3B8;` 边界钳位
   - 白色线：接收 `closePrice` 参数，与十字光标横线同Y坐标

2. 更新 `updateCrosshair()`：传入 `closePrice` 参数同步调用 `renderChipChart()`

3. 更新 `toggleChipPanel()`：
   - Flex宽度修复：同时设置 `style.width='140px'` + `style.flex='0 0 140px'`
   - 关闭时同步复位 `style.width='0'` + `style.flex='0 0 0'`
   - `chart.resize()` 在 350ms 动画完成后执行
   - 无十字光标时默认使用最新日期

4. 浏览器验证（1440×900）：
   - chip-chart: 132×325px ✅
   - 5个价格标签可见 ✅
   - 14根红色（获利）+ 16根蓝色（套牢）线条 ✅
   - 白色线在收盘价水平 ✅
   - 十字光标idx=400时数据同步更新 ✅

**涉及文件：** `_ui-prototype/indicator-ide.html`（约80行修改）、`002-方案存档/166-前端UI原型逐页调整记录.md §6.7.2`

### 176 - 筹码面板4项问题评估与实施（含白线对齐修复）

**用户输入：** 反映三个问题：① 价格纵坐标移到右侧让图表左移 ② 筹码线以0.01元为档位紧密排布 ③ 鼠标移出恢复到最后交易日。并补充了一个第四个问题：筹码白线与十字光标横线不在同一水平线。

**AI执行内容：**

**分析与评估（未改）：** 产出176号评估方案文档，详细分析每个问题的根因、修正方案和风险。

**核心发现 ④：** 白线与横线偏差**153.48px**。根因：`renderChipChart()` 使用独立60日价格范围 `[11.55,13.77]` 映射Y坐标，ECharts grid使用可见K线范围 `[12.00,20.00]` — 两套坐标系统不同。

**实施修正（4项全部完成）：**

| # | 问题 | 修正方法 | 验证结果 |
|:--|:-----|:---------|:---------|
| ① | 价格标签左→右 | `priceAxisLeft:42→2`，标签 `right:2px` | 5档标签都在右侧 ✅ |
| ② | 30档→0.01元档位 | `bins=Math.max(20,round(range/0.01))`，线高动态 | 436条DOM，紧密排列 ✅ |
| ③ | mouseleave恢复最后交易日 | 监听 `.chart-section`，`updateCrosshair(-1)`补充恢复 | chipTime从2/28→10/23 ✅ |
| ④ | 白线未对齐 | 传 `crosshairY` 像素，`getBoundingClientRect()` DOM坐标换算 | 偏差从153.48px→0.0001px ✅ |

**涉及文件：** `_ui-prototype/indicator-ide.html`（4处修改~25行）、`002-方案存档/176-筹码分布面板三项问题评估.md`

### 177 - 红蓝分界线与白色线/十字光标不统一问题评估与修复

**用户输入：** 验证发现白线与十字光标已匹配，但红蓝分界线与白线不在同一水平；筹码形态随日期无变化。

**AI执行内容：**

**评估分析（先分析不修改）：**
1. 浏览器实测确认：红蓝理论分界Y=155.26px，白线Y=216.05px，**偏差60.79px**。根因：白线已改为DOM坐标对齐，但红蓝判定仍使用独立price公式
2. 筹码形态变化：模拟数据使用固定sin曲线公式，与日期无关。真实API接入后将基于OHLCV计算，每日形态不同

**修正实施（红蓝分界）：**
- 将 whiteY 计算提前到筹码线循环之前
- 颜色判定从 `price <= closePrice`（独立坐标系）改为 `lineY >= whiteY`（与白线同基准）
- 消除重复的 whiteY 渲染代码

**验证结果：**
| 检查项 | 改前 | 改后 |
|:-------|:-----|:-----|
| 红蓝分界Y vs 白线Y | 偏差60.79px ❌ | **一致（同基准whiteY）** ✅ |
| 白线 vs 十字光标 | 0.0003px | 0.0003px ✅（不变）|
| 红线在白色线下方（获利） | — | 356条✅ |
| 蓝线在白色线上方（亏损） | — | 240条✅ |

**涉及文件：** `_ui-prototype/indicator-ide.html`（约10行重构）、`002-方案存档/177-筹码面板红蓝分界与形态变化评估分析.md`

