# 沟通记录

## 2026-09-10

### 1 - 启动验证清单执行：暴露并根治 421号"JUD/OUT 写分库"致命 bug

**用户输入**：启动验证清单（已记录工作上下文）：1. 重启 daemon → 自动重跑 20260909 管道；
2. 验证 JUD/OUT 写分库（status_snapshot 分库出现 09-09 新批次）；3. 清理主库 status/treemap
残留（5,549/5,544 行）

**AI执行内容**：
1. **核查现状**：daemon 今早 09:00 已由看守脚本自动拉起（PID 1256，已加载昨日修复代码），
   但主库 `pipeline_status` 20260909 全部 done（SIG/JUD/OUT 为昨日旧路径结果）→
   `_is_pipeline_complete` 返回 True → **正常重启不重置 pending，未自动重跑**（与清单"重启→自动重跑"
   预期不符）
2. **触发重跑尝试**：首次尝试运行中直接 UPDATE 重置 pending → 撞 `database is locked`
   （daemon 长期持有主库写锁，印证 `mark_step_running` 无 commit 问题）→ 确认须走 SIGKILL
   空窗重置（昨日流程）。用户确认"验证配置阶段 daemon 可让路，先保证验证与调整实施"→ 授权重启
3. **第一轮重跑（09:28-09:38）暴露重大 bug**：日志报 `status_snapshot 构建完成 5549` + `[管道] JUD/OUT
   done`，但分库 `snapshot_cache.db` 未更新、主库也无 09-10 批次。排查发现
   `data/duckdb/status_snapshot`(290MB)/`treemap_snapshot`(8.7MB) **野文件**——
   昨日"写分库修复"用错 API：`get_connection('status_snapshot')` 的参数应为**分库文件名**，
   误传表名 → 生成以表名命名的独立 SQLite 文件。**昨日验证只 grep 了"无 `_ecm.conn` 写"，
   漏检连接目标 → 分库根本没被写入（读写分叉依旧）**
4. **根治**：修正 data_daemon.py 4 处为两段式 `get_connection(get_db_for_table('...'))`
   （3989/4133/4349/4885）；删除 4 个野文件；第二轮重跑（09:55-10:06）验证
   **分库 status_snapshot=2026-09-10·5549、treemap_snapshot=2026-09-10·5544** ✅

### 2 - 附带修复 3 处同源缺陷（野文件运行掩盖，切真实分库后暴露）

1. **treemap 归档列错位**：OUT `INSERT OR REPLACE INTO treemap_snapshot_history SELECT *` 因历史表
   残留旧列 `seven_dim_report`（48 列 vs 源表 47 列）失败 → 改**显式列名**归档
2. **watchlist 变更检测读主库**：OUT 自选股段 3 处 `status_snapshot` 读取走 `_ecm.conn`（主库）
   → 切 `_snap_conn`（分库）
3. **R4a 读方迁移不完整**：发现 **8 处读取仍走主库** `_query_df`/`read_conn` 读分库表
   （valuation_estimator / cross_validate / potential_engine / get_snapshot_max_date /
   get_snapshot_data_date / get_treemap_snapshot 回退 / get_status_snapshot_row /
   strategy_analyze 预加载）。用户选择"**先补齐读取再清理**"→ 全部改 `_query_shard` / 分库连接

### 3 - 主库残留清理 + 最终验证

**执行**：新建 `backend/scripts/migrate_status_treemap_shard.py`（备份→校验→清空，幂等可回滚）
- 主库 status_snapshot 5549→0、treemap_snapshot 5544→0（备份 `_bak` 5549/5544 可回滚）
- 重置 OUT→pending → 重启 daemon 重跑 OUT（10:27:27 done）→ **treemap 归档成功**
  （不再报 48/47 列错位）

**最终状态**：

| 对象 | 结果 |
|------|------|
| 分库 status_snapshot | 2026-09-10 · 5549 ✅ |
| 分库 treemap_snapshot | 2026-09-10 · 5544 ✅ |
| 分库 status_snapshot_history | 含 09-10 批次 ✅ |
| 分库 treemap_snapshot_history | 含 09-10 批次 ✅（新写入正确） |
| 主库 status/treemap | 0 行（已清理，备份 _bak 可回滚）✅ |
| 读取方路由 | `get_snapshot_max_date=2026-09-10`、`get_status_snapshot_row`/`get_treemap_snapshot` 均命中分库 ✅ |
| pipeline_status 20260909 | SIG/JUD/OUT done ✅ |
| 代码质量 | py_compile ✅、ruff 改动文件全通过 ✅ |
| 测试回归 | 670 passed / 2 failed / 2 skipped / 9 xfailed |

**遗留发现（未修，待议）**：
1. **测试 2 failed 为数据依赖**（非本次回归）：`strategy_signal_detail` 中 600519 等股票
   `signals` 为空 dict → `_restore_signals_from_cache` 返回 `[]`（test_322 / test_320::p2_signal_summary）。
   信号链代码本次未触及
2. **treemap_snapshot_history 5571 行历史错位脏数据**（rowid 1..5571，snapshot_date 列存股票名）
   —— 旧归档代码 `SELECT *` 历史遗留，非本次引入；新写入 5544 行正确
3. `_verify_out_completeness` 的 `active_count` 仍读 `_ecm.conn` 的 daily_cache（分库表）→
   OUT-CHECK 被跳过（低影响）
4. `mark_step_running` 无 commit（管道 running 状态对外不可见，诊断困难）
5. （可选）dim2/dim3 `_load_precomputed_macd` 直调 DataManager 残留

**关键教训**：验证"写分库"必须校验**连接目标（文件路径）**，而非仅 grep 是否调用了旧连接；
`sharding_manager.get_connection()` 参数是分库文件名，须经 `get_db_for_table()` 转换。

### 4 - 提交与 GitHub 推送

**用户输入**：提交这些变更，并上传 github 备份

**AI执行内容**：`git add -A` 暂存全部（40 文件，含 418-422 方案、09-08/09/10 沟通记录、
索引、工作上下文、迁移脚本、测试），提交 `0502a14`
`feat: 418-422方案实施（JUD接入/dim5合规/dim8增强/SIG写分库根治）+ 文档归档`，
推送 `a2f9b92..0502a14 main -> origin/main`（git@github.com:lengkai2116/astock-analysis-system.git）。
工作树干净，本地与远程同步。

### 5 - 398号方案评估 + 423号方案v2.0草案创建（3 个决策点待拍板）

**用户输入**：1. 详细评估 398号方案是否还有优化空间；2. 评估现阶段系统配置是否具备实施可行性
（对话框内说明，不改方案和代码）；确认后：将 v2.0 草案编辑为 423号方案 + 详细说明 3 个待确认点。

**AI执行内容**：
1. **核查**：398号方案（09-02 编制）诊断基线已部分失效——§1.2 写入现状表中
   SIG 路由不一致（421R1 ✅）、写失败静默（421R3 ✅ SignalWriteError→SIG failed 重试≤3）、
   busy_timeout 30s（421R2 ✅）、JUD/OUT 直连（422R1/R5 ✅）、管道不可恢复（既有
   pipeline_status 幂等锁+4h timeout 重置 ✅）均已闭环。四大核心组件（WriteGateway/
   QualityChecker/RecomputeScheduler/OutputSemanticController）grep 确认均未实现。
   分库体系完整（market_cache.db→compute_cache.db→snapshot_cache.db，均 SQLite 3.x，
   data/duckdb/ 为目录名遗留）；pipeline_status 在 stock_cache.db 主库；sync_requests 在
   app.db（327号）；SIG/JUD/OUT 写入已全部收敛分库。
2. **评估产出**（11 项优化建议）：方案基线需修订；5 个设计缺陷——①`time.sleep(300)`
   阻塞 daemon 主循环（应改事件驱动）、②RecomputeScheduler 与 sync_requests 双通道需划界、
   ③WriteGateway 不应重造锁（sharding_manager 已有 _write_locks）、④QualityChecker
   硬编码 min_rows=4000 错误（factor_cache 3715万行累积表/4000≈全市场73%含义不清，应改
   动态覆盖率基准）、⑤OutputSemanticController 门禁放错层（dim8 内抛异常无法触发 daemon
   补算，应上移门禁到写入前）；适配优化——告警通道（单机无邮件）、健康监控指标（无连接池，
   改监控写锁冲突计数）、缺收益度量、测试改故障注入、历史日期修复；工期 14 天→10 天
   （Phase A dim 梳理已完成：399号 dim2-dim7 + 408号 dim1）。
3. **可行性结论**：基本具备实施条件（10 项基础设施就位），剩余 8 项缺口全部为增量开发；
   阻塞项为方案基线修订 + 5 个设计缺陷修正。
4. **创建 423号方案**（v2.0 草案，替代 398号）：修订摘要表 + 已闭环项剔除（§1.1）+ 真实
   缺口 8 项（§1.2 G1-G8）+ 四组件重定义（薄封装/动态覆盖率/复用既有通道/门禁上移）+
   事件驱动多轮核查（QA-CHECK）+ 健康监控适配 + 告警分级 L1/L2/L3 + 实施计划 10 天 +
   故障注入测试 + 验收标准 + **3 个待确认决策 D1/D2/D3**。398号文档头部已标注作废。

**决策确认（2026-09-10，已修订 423号定稿）**：
- **D1**：✅ 并入 397号方案（dim8 语义化 + evidence 消费合并一次改造；423 只做 daemon 侧门禁，不改 dim8）
- **D2**：✅ 仅 L1+L2（日志 + health 聚合）；代码预留 webhook 扩展点（配置项 + 空实现接口）
- **D3**：✅ 复用 pipeline_status step_id='QA-CHECK'（问题明细存 detail 字段 JSON）
- 423号方案已更新：头部 status=已定稿·待实施，§2.5/§3/§5/§6/§7 落实确认结果，§9 改为"已确认决策"定稿记录

### 6 - 423号方案实施（C-1~C-7 代码完成，serena MCP）

**用户输入**：调用 serena MCP 工具开展 423号方案的实施。

**AI执行内容**：
1. **新增核心模块** `backend/app/data/stg_quality.py`（462 行）：
   - `QUALITY_RULES` 校验规则注册表（动态覆盖率基准，factor_cache 按 DISTINCT ts_code）
   - `WriteGateway`（薄封装：validate_before/after + write_batch + write_audit，锁/事务委托 sharding_manager）
   - `QualityChecker`（check_table/check_cross_table/check_pipeline_date/validate_signal_rows G3）
   - `RecomputeScheduler`（RECOMPUTE_KEYS 问题→步骤映射，置 pending 由管道重跑，不写 sync_requests）
   - `run_quality_round`（事件驱动单轮核查）
2. **DDL 与状态机**：ECM 建 `qa_audit_log` 表；`ensure_pipeline_steps` 增 `QA-CHECK` 步骤
3. **写入点接入**：`_batch_write_signal_detail`（G3 写前自检 signal_json/dim_results_json/seven_dim_json + 写后行数校验，失败抛 SignalWriteError 触发 SIG 重试）；`_build_status_snapshot`/`_build_treemap_snapshot` 原子替换后写后校验（失败仅告警+审计，不抛——避免原子替换后重试丢数据）
4. **管道集成**：`_drive_pipeline` OUT 完成后插入 QA-CHECK（failed→pending 重试≤3→超限 L2 告警+标 done 防死循环）；`_run_qa_check`/`_alert_qa_failure`/`_run_history_qa_patrol`（G8 整点巡检最近 3 交易日）
5. **监控/health**：写锁冲突计数（monitor 上报）+ `health.py` QA 状态聚合（qa_audit_log 7 日失败率）
6. **测试**：`tests/test_423_stg_quality.py` 17 用例（monkeypatch 故障注入，不污染真实库）——G3 JSON 损坏/键数不足/空 signal、写前格式、覆盖率不足、跨表不对齐、补算调度、多轮核查汇总

**验证结果**：py_compile ✅；test_423 17/17 ✅；test_api_health 4/4 ✅；管道/写入/合规回归 61/62 ✅（1 失败=既有数据依赖 600519 signals 空 dict，09-10 §3 已记录）；API/服务/核心回归 39 passed + 9 xfailed + 2 skipped ✅（1 失败=同上数据依赖）；ruff 零新增（data_daemon 基线 34=现状 34）；mypy stg_quality 0 错误 ✅

**423号方案已更新**：status → 🔨 已实施（代码完成）· 待 daemon 运行验证；新增 §10 实施记录（含待运行验证 4 项：真实数据 QA-CHECK、qa_audit_log 落库、G3 性能、G8 巡检日志）

**遗留**：daemon 尚未重启，真实管道 QA-CHECK 验证留待下次启动；`cache_indicators_wide`/`_insert_from_df` 未接入 write_batch（RAW 批量写已走 sharding_manager，避免重复改造，423号 §10 已注明）

### 7 - 423号方案运行验证闭环（QA-CHECK 真实运行 + 11 项数据管道缺陷根治）

**用户输入**：重启 daemon 验证 QA-CHECK 真实运行。

**AI执行内容**（多次 SIGKILL 空窗 + launchd KeepAlive 重启 daemon，monitor + loop 观察补算链）：
1. **QA-CHECK 首次运行（12:25）检出 daily_base 读主库残留**（daily_cache 已分库 market_cache.db）→ 基准=0 → 9 表误判失败。修复 QualityChecker.daily_base 走分库
2. **根因深挖**：QA 检出 strategy_signal_detail/status/treemap/pre_feat 09-09 无数据（数据滞后约 10 交易日，最新仅 08-26/08-28）→ 定位 **356号分库迁移的读写路径未补齐**：
   - SIG 引擎 unified_core.py:120 读主库残留 daily_cache（08-26）→ 信号写 08-26
   - treemap 3 个 df（daily/basic/tags）、status/OUT 日期、G8 巡检、prewarm 周线均读主库残留
   - RAW-2 cache_pre_feat 写主库残留（09-09 落主库 61055 行、分库无）
   - 主库 daily_cache 残留 620 万行 → 备份 27877 + 清空；pre_feat 残留 → 备份 27764 + 清空
   - 全部改走 sharding_manager 分库（`_shard_query_df`/`_shard_fetchall` 新 helper）
3. **补算调度修复**：QA 调度日期格式（数据表 '-' vs pipeline_status compact）→ `run_quality_round(status_date=)` 双日期；QA-CHECK 失败立即重试 vs 补算需数十分钟 → 增加"等待补算"逻辑（_inflight 检查保持 pending 不空转 retry）
4. **G3 门禁真实拦截 + 校准**（13:06）：SIG 重跑后 G3 检出 seven_dim_json 键数=2<7 → 拦截 5552 行。核查确认 seven_dim_json 为**条件性产出**（status_engine.generate_seven_dim_from_signals 仅 risk/summary 恒有）→ G3 规则 7 键过严，改为"可解析 + 含 summary 键"
5. **闭环达成（14:04）**：`[QA] 仓储质量校验通过: 9 项全部 passed`、QA-CHECK done、pipeline_status 09-09 **15/15 全 done**；strategy_signal_detail 09-09=5552、status_snapshot=5549、treemap=5552、pre_feat=5549（全部追平 09-09）
6. **health L2 验证**：`/api/v3/health/data-freshness` 返回 `qa={"status":"done","retry_count":2,"failed_7d":0}`、`pipeline={"done":15}`（启动 API run.py 5001 验证）
7. **423号方案已更新**：status → ✅ 已实施并运行验证闭环；§10 补充 11 项缺陷根治表 + 运行验证结果

**核心价值**：QA-CHECK 真实运行**自动发现并驱动根治了 11 项系统性数据管道缺陷**（分库读写路径未补齐），将 SIG/JUD/OUT/pre_feat 数据从滞后 10 交易日追平到 09-09，实现了 423 号方案"数据质量自动检测+定向补算自愈"的完整闭环。G3 门禁在真实 SIG 上拦截了坏数据（虽规则过严需校准，但证明门禁有效）。

**遗留**：主库 stock_cache.db 8.8GB 未 VACUUM（清理后空间未释放，可选后续处理）；treemap_snapshot_history 5571 行旧脏数据（09-10 §3 已记录）；`cache_indicators_wide`/`_insert_from_df` 未接入 write_batch（RAW 批量写已走 sharding，423 §10 已注明）

### 8 - 423号方案实施后核对 + 6 项偏差补齐（serena MCP）

**用户输入**：核对 423 号方案（398 号存储层中心化构思）实际落地，逐项核对遗漏问题；随后按建议顺序补齐。

**AI执行内容**：
1. **逐项核对**（15 项已完整落地 + 6 项偏差）：
   - 已落地：QUALITY_RULES 9 表 / qa_audit_log / WriteGateway / QualityChecker / RecomputeScheduler / QA-CHECK 管道接入（等待补算+超限告警）/ _is_pipeline_complete 15 / G3 自检 / 写后校验（SIG/status/treemap）/ G8 巡检 / 写锁冲突计数 / health QA 聚合 / 18 测试 / 分库读写根治 / 运行闭环
   - 偏差：①5 处写入点只落地 3 处（cache_indicators_wide/_insert_from_df 未包 WriteGateway）；②sync_requests 积压监控缺失；③WAL 500MB 阈值未整合；④webhook 未接线 QA 告警；⑤写锁冲突基线未建立；⑥缺告警升级/非阻塞 2 测试
2. **按建议顺序补齐**（B6→B1→B2→B3→B4→B5）：
   - **B6** 测试：新增 `test_quality_round_non_blocking`（事件驱动无 sleep 计时断言）+ `test_quality_round_continuous_failure_for_escalation`（连续 3 轮失败供上层升级告警）→ 测试 20/20
   - **B1**：`_precompute_indicators` 完成后对 indicator_ma/macd/other 写后校验+审计（validate_after_write + write_audit）；`_insert_from_df` 保持（356号已走分库路由，通用底层）
   - **B2**：health `data-freshness` 返回 `sync_requests_pending` + daemon 消费上报 monitor `sync_requests_pending`
   - **B3**：主循环非交易时段 + WAL>500MB + throttle1h → `PRAGMA wal_checkpoint(PASSIVE)`（轻量非阻塞，与 wal_maintenance TRUNCATE 深收缩互补）
   - **B4**：`_alert_qa_failure` 接线 `AlertNotifier().send()`（JSONL+logging，预留 webhook/email 扩展）
   - **B5**：`_run_qa_check` 将写锁冲突计数快照持久化到 qa_audit_log（`_write_lock_baseline` 特例表名）
3. **验证**：py_compile ✅ / ruff data_daemon 33（< 基线 34，零新增）✅ / 其他改动文件 All checks passed ✅ / test_423 20/20 + 管道回归 54 passed ✅
4. **423号方案已更新**：status → ✅ 已实施并运行验证闭环 + 6 项偏差补齐；§10 追加"偏差补齐记录"表（B1-B6）

**结论**：423 号方案（398 号存储层中心化构思）核心机制 100% 落地并运行验证闭环，6 项偏差已全部补齐。剩余仅为可选优化（主库 VACUUM、treemap 历史脏数据）

### 9 - STG 存储层原始数据存储全面核查 + 424 号方案存档（serena MCP）

**用户输入**：全面核查存储层 STG 所有需通过采集层 COL 获取的原始数据存储情况——①跨分库重复数据核实；②分库数据表不足与集中补采；③采集数据消费情况；④采集管路配置评估；⑤COL 入库合规与锁风险。要求对话框内说明，不修改方案和代码。随后要求整理为方案存档并在末端编辑实施计划。

**AI执行内容**（纯只读核查，未修改任何代码）：
1. **数据侧直查** `data/duckdb/` 8 分库全部表行数与日期范围，结合 3 个并行调查 agent（分库路由 sharding_manager/ECM/DataManager、采集写入 data_daemon `_batch_*`、消费端 routes/services/dim 引擎）+ daemon 日志核对。
2. **五大核查结论**：
   - **① 跨分库重复数据（存在）**：总库 stock_cache.db 残留 15+ 张表旧副本（daily_basic 68万至08-26、moneyflow 4.4万08-17~26、margin、minute、stk_limit、indicator* 617万至08-21、财务/股东/概念/成分/龙虎榜、tag_history 82万×2 完全重复、opportunity_tags 82万×2 完全重复、status_snapshot_history、finance_report 疑似双写 25241 vs 25549）；423 仅清理 daily_cache/pre_feat。另有 E1-E4 数十处"写分库/读总库残留"读写分叉（get_tags 系列、get_pre_feat 系列、get_cached_stk_limit/lhb、市场统计8方法、pre_feat 三路不一致、DataManager.sync_daily_basic_history 等）。
   - **② 分库数据表不足（4 处实质缺口）**：margin 停更 08-26 滞后 10 交易日（根因=确定性 bug：`_batch_margin_range` 收 YYYYMMDD today 但按 '%Y-%m-%d' strptime → ValueError 被吞，日志 13 次实证；且 margin 不在管道 COL/QA 规则内无告警）；win_rate_cache 0 行（写入失败被吞）；as_market_snapshot 停更 08-31（实时线程正常但落盘通道断）；as_* 系列全空。snapshot_cache.db 0 索引（信号/标签 82 万行全表扫描）。
   - **③ 消费情况**：38/52 表被消费；14 张未消费——6 张完全死表（tag_history/as_quote_cache/as_top_stocks/as_limit_pool/strategy_signals/chip_distribution 缓存形同虚设）+ 只写不读 8 张（index_member/conditional_win_rate/pattern_score/treemap_history/as_minute_kline/as_lhb_detail/as_news/as_concept_ranking）+ akshare_reader 6 个读取方法调用未定义 ECM 方法被吞返回空。
   - **④ 采集管路**：主链路 15 步闭环 ✅、sync_requests 无积压 ✅；但 QA QUALITY_RULES 只覆盖 9 张成品表不覆盖 COL 原始表、margin 不在管道、`_check_data_timeliness` latest 未赋值 bug 致时效审查空转、东财主源今日全天降级新浪（2203 vs 22 次）、`_batch_*` 未走数据源适配层。
   - **⑤ 合规与锁风险**：合规总体达标（分库单写+INSERT OR REPLACE 幂等+busy_timeout 30s+RLock 串行写 ✅）；**P0 风险=compute_cache.db-WAL 5.26GB 处于 checkpoint 与监控盲区**（checkpoint/大小监控只覆盖总库 stock_cache.db，分库无 checkpoint 入口）；另有 sector_heat 裸连写库、volume_ratio 写错库、DATA_DIR 路径解析分叉。
3. **存档**：创建 **424号方案**（`002-方案存档/424-STG存储层原始数据存储全面核查与整改方案.md`）——§1-§7 核查报告存档 + §8 实施计划（阶段一 P0：margin bug 修复 0.5天 / 分库 WAL 治理 1天 / 残留清理+读写分叉根治 2天；阶段二 P1：QA 规则扩展+时效审查修复 1天 / win_rate 修复 0.5天 / as_market_snapshot 修复 1天；阶段三 P2：snapshot 补索引 0.5天 / 死表清理 1天 / 东财主源排查+适配层 1-3天可选）+ §9 验收标准 + §10 关联决策（4 决策点待确认）。

**结论**：STG 存储层主链路健康（09-09 全链路覆盖率 ≥96%、管道 15 步闭环），但存在 1 确定性采集 bug（margin）、1 处 P0 级 WAL 风险（compute_cache 5.26GB 盲区）、17 张表残留/死表/消费断裂及数十处读写分叉。核查过程零代码改动；424 号方案已存档，实施计划待批准后执行（决策点：as_* 通道去留、chip_distribution 缓存接入、finance_report 双写收敛、适配层接入）。

**修订（424 号方案 v1.1，用户要求核对遗漏/错误后修订）**：用户要求核对 424 方案是否包含对话中全部稽核内容。核对后补齐 8 项遗漏 + 修正 1 处错误：①treemap_snapshot_history 双 snapshot_date 列结构异常（§3.7）；②as_sector_ranking 有真实消费者但表空→板块排行拿空（§3.4）；③文件系统遗留空壳清单（data/ 根 4KB 空库、0 字节文件、app.db.sync_requests 空表等，§2.2）；④pattern_score_cache 不在分库映射致读分叉（§2.1 E4 + P0-3 Step A）；⑤cache_pattern_score 跨库持总库锁（§6.3）；⑥sentiment_pool_cache 归属偏差（§3.6）；⑦data_inventory.check_data_duplicates 库定位过时（§2.1 E4）；⑧附录 A 已排查正常项 7 条（周K线 09-11 预写、今日管道未开始、market_stats/sector_heat 无分叉等）。错误修正：stock_cache.db 体积 9.3G→9.4G（§1）。P2-2 同步补充 treemap 结构修复与空壳清理。424 号 version → v1.1。

### 10 - 424号方案实施逐项核查 + P2-3 东财主源修复 + treemap_snapshot_history 冗余列修复

**用户输入**：请先根据系统实际的代码配置情况逐项逐行核对424方案的实施情况。如有遗漏和错误请进行系统修正。然后进行文档的更新。

**AI执行内容**（serena MCP 逐项逐行核查 + 2 处修复）：
1. **逐项核查 P0-P2 全部落地**（代码 + 数据双维度）：
   - **P0-1 margin 修复** ✅：`_batch_margin_range`（:576）日期归一化、`_batch_margin`（:546）走 `_shard_query_df('daily_cache')`
   - **P0-2 WAL 治理** ✅：`_wal_maintenance_all_dbs`（:5640）遍历全分库 PASSIVE + 监控；8 分库 WAL 全 <500MB（health 实测 compute 0/financial 0/history 11.7/market 4.3/market_snapshot 1.1/snapshot 0/stock 1.0/system 0.1）
   - **P0-3 Step A 读路径根治** ✅：E1 读方法全改 `_query_shard`（get_cached_stk_limit:1992/get_cached_lhb:2012/get_cached_win_rates:1613/get_tags:2795/get_pre_feat:2339/get_snapshot_history:2887/get_previous_trade_date:2918/get_cache_stats:1384）；E2 cache_pre_feat:2289/cache_pre_feat_batch:2314 统一分库；E4 pattern_score 读写走 compute_conn/compute_read_conn（:2426/2445）；**Step B 残留清理** ✅：死表写方法已删（write_as_top_stocks 等无匹配）、read_as_concept_ranking:2495/read_as_top_stocks:2537 返回空、cache_market_snapshot_data:2542 写 snapshot_conn
   - **P0-4 决策①②④** ✅：①保留 as_sector_ranking:2476 + as_market_snapshot:2502（读 market_snapshot.db），废弃其余 as_*；②chip_distribution_cache 不接入缓存消费，`_score_chip_distribution`（chip_strategy:919 + dim4_chip_fund_engine:3293）消费 chip_fund_ext；④6 个 `_batch_*`（finance_report:1046/income_recent:1179/balancesheet:1204/cashflow:1226/forecast:1248/margin:546）均收敛 `_get_tushare_provider()`
   - **P1-1 QA 规则扩展** ✅：stg_quality.py QUALITY_RULES（:27）扩展 margin/moneyflow/minute/lhb/concept/index_member/fina/income/balancesheet/cashflow/forecast/top10/stk_holder/adj_factor，`_check_nonempty`（:333）
   - **P1-2 win_rate 写入** ✅：precompute_indicator_manager.compute_win_rates（:61）解析 signal_json、get_win_rates（:182）用 `_query_shard`
   - **P1-3 as_market_snapshot** ✅：akshare_collector.write_as_sector_ranking（:209）保留；as_concept_ranking:233/as_minute_kline:297/as_lhb_detail:392/as_news:486 停写注释
   - **P2-1 snapshot 索引** ✅：sharding_manager `_ensure_snapshot_indexes`（:118）idx_status_ts/idx_signal_ts/idx_treemap_ind/idx_status_hist_date/idx_treemap_hist_date
   - **P2-2 死表清理** ✅：`_table_to_db`（:40）无死表映射（chip_distribution_cache/tag_history/as_* 已移除）；chip_distribution_service.cache/get_cached 已删；compute_chip_distribution.py 已删；data/__init__.py get_tag_history/get_cached_chip_distribution 委托已删
   - **P2-3 东财主源排查+修复** ✅（本会话）：实测 push2.eastmoney.com 5 次重试全 RemoteDisconnected、3 种 UA 均失败；push2delay/push2his 正常 → `_fetch_eastmoney`（:671）host 改 push2delay，验证 `_fetch_eastmoney(['600519','000001'])` 返回 2 条（1285.13/11.85，source=eastmoney）；`_SnapshotSourceManager`（:390）三源热备 eastmoney 主/sina 备/tencent 备
2. **发现并修复 1 处遗漏**：`treemap_snapshot_history` 实际 48 列（多废弃 `seven_dim_report` 列，370号S6 已废弃，无代码引用，仅 data_daemon:4237 注释提及）vs 代码 CREATE 47 列 → 重建为 47 列规范结构（ALTER RENAME → CREATE → 显式列迁移排除 seven_dim_report → DROP 旧表）。校验：47 列、22240 行保留、seven_dim_report 移除，与代码 CREATE 完全对齐
3. **§9 验收标准 10 项全部验证通过**：覆盖率≥95%、WAL<500MB+health 暴露、stock_cache.db 仅主库表、QA-CHECK 检出 COL 缺口、回归测试、无锁、as_market_snapshot 更新、chip 死表清理、finance 残留清理、无新增 import tushare
4. **运行态确认**：daemon(53544)/API(53546) 运行中，health healthy，8 分库 WAL 全 <500MB，管道 20260910 15/15 done（treemap 重建未影响运行进程）

**结论**：424号方案全部 P0-P2 项确认落地，仅发现 1 处遗漏（treemap_snapshot_history 冗余列）并已修复。424 号方案 status → ✅ 已实施并核查闭环。
