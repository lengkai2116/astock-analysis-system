
### P0.6 - SectorRotation 重写 + ORM 整改

**用户输入**: 执行任务 P0.6
**AI 执行内容**:
1. `backend/app/data/__init__.py` — DataManager 新增 `get_stock_industry` 和 `get_stock_industry_batch` 两个方法，使用 `db.session.query()` 模式查询 Stock 表
2. `backend/app/engine/framework/sector_rotation_model.py` — 269行→138行完全重写，核心算法改为缠中说禅板块强弱指标法（MA5/MA20女上位/男上位统计）
3. 验证：linter 通过、模块加载正常

### P0.1 - 估值引擎（四锚加权合成框架）实现

**用户输入**: 执行任务 P0.1
**AI 执行内容**:
1. `backend/app/opportunity_atlas/valuation_estimator.py` — 新建 ValuationEngine 类，约290行
2. 四锚框架实现: 资产锚(PB百分位)、收益锚(PE分位+PEG+股息率)、现金流锚(FCF/EV)、修正锚(调整后PE法)
3. 七大行业分类权重分配器，31个申万一级行业映射
4. 财务质量评分(ROE/ROCE/负债率/经营现金流比)
5. 验证：导入正常、compute_tags('000001.SZ') 返回完整字典、各锚计算有合理值

### P2.5 - 各引擎标签化改造 + 补强（9 项子任务）

**用户输入**: 执行任务 P2.5
**AI 执行内容**:
1. `volume_price_strategy.py` — VolumePriceStrategy 新增 get_tags()（ma_alignment/volume_price_fit/volatility_level/gap_type/breakout_attempts 六个标签）+ EnhancedPatternDetector 新增 get_tags()（pattern_signal 标签）
2. `chanlun_strategy.py` — 新增 get_chanlun_tags() 函数（提取 buy_sell_point 标签）
3. `chip_strategy.py` — MainForceScorer 新增 get_tags()（fund_flow/capital_nature 标签）
4. `chip_distribution_service.py` — ChipDistributionEstimator 新增 get_tags()（chip_position/chip_concentration 标签）
5. `chip_pre_filter.py` — FinancialRiskFilter 新增 get_risk_tags()（roce_pass/fina_health 标签）
6. `opportunity_atlas/time_rhythm_engine.py` — 新建 TimeRhythmEngine 类（compute_tags 输出 time_rhythm 标签）
7. 验证：所有文件 ruff check 通过
