# 2026-06-16 沟通纪要

## ### 002 - 侧栏折叠 + 个股策略分析 + 仪表盘重构

**用户输入**：继续调整个股策略分析页面，包括侧栏折叠、成交额副图、▼下拉切换；仪表盘页面重构

**AI执行内容**：

### 侧栏折叠功能（所有页面）
- 折叠按钮置顶（logo下方），默认收起态，点击展开
- 收起后仅图标，hover 显示名称标签（`position:fixed; z-index:9999`）
- 折叠/展开时自动 resize 所有 ECharts 图表
- CSS 提取到 `assets/base.css` 共享，16个页面全部添加

### 个股策略分析页面调整
- **成交额默认副图**：`activeSubs` 从 `['macd','vol']` 改为 `['成交额','vol']`
- **成交额柱体颜色**：收>开（红K）→ `#EF4444`，收≤开（绿K）→ `rgba(16,185,129,0.5)`
- **▼下拉选择框**：每个副图标题旁 ▼ 按钮切换指标；底部 ＋▼ 按钮新增副图
- **IIFE 加 chart.resize**：修复侧栏折叠后主图K线canvas宽度不同步

### 仪表盘页面重构（按§3.6规划）
- 新增四大交易所合计·每日成交额柱状图（volume-chart）
- 全市场资金流向改为横跨全屏
- 新增行业板块净流入排行 + Top5板块·个股净流入（5卡片）
- 新增 Top5板块·个股净流出（5卡片，右栏下半部）
- 修复3个重复toggle脚本 + radar-track撑大main-content + 图表resize

### 文档更新
- 166号方案：新增§8（2026-06-16调整记录，含8.1-8.5）
- 171号方案：更新2026-06-16 API映射（新增2个端点）
- 001-沟通记录：本文件

### 后续完善（同一日，十字光标+右侧栏+筹码同步）
**十字光标修复**
- 副图轴标签：`type:'cross'` → `type:'cross',label:{show:false}`
- 双向同步：放弃 `echarts.connect` → `instance.on('showTip')` + `_isSyncing` 防循环
- 锁定收盘价：`chart.getZr().on('mousemove')` → `convertFromPixel` → `dispatchAction(seriesIndex:0)`
- 灵敏度修复：移除 `!param.target` 过滤

**右侧栏重构**
- 招商证券样式：股票头 + 3列27字段行情grid + 盘口信息 + 资金流向饼图
- 策略信号/缠论分析/策略解读移至副图下方（`sub-below` 区域）
- 字体：统一 16px（股票价格 20px），去掉了 max-height 约束自然延展

**筹码面板**
- `toggleChipPanel()` 手动同步 `dataZoom` 到所有 `subInstances`

**文档更新**
- 166号方案：追加§8.6-§8.9（十字光标、右侧栏、筹码同步、文件变更）
- 171号方案：追加十字光标/右侧栏/筹码同步映射
