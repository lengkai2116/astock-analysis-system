# 2026-07-29

### 001 — 机会图谱页面性能瓶颈分析 + 305号优化方案定稿

**用户输入**：要求核查机会图谱页面点击打开耗时较长的原因，详细说明不修改代码。

**AI 执行内容**：
1. 全链路代码审计：从前端 `opportunity-treemap.html` 到后端 `opportunity_atlas.py` 到 ECM `enhanced_cache_manager.py` 数据查询
2. 定量分析各环节数据读取量：`daily_cache` 580万行、`daily_basic_cache` 75万行、`opportunity_tags_cache` 21万行，实际只需要每只最新1条（4800行），**读入量是实际需要量的1400倍**
3. 识别6个叠加瓶颈，首要瓶颈为 `get_cached_daily_batch()` 全表扫描580万行
4. 用户提出三个优化方向（预计算层/日终预提取/缓存机制），要求评估生产可行性
5. 深入评估三个方向后，形成**三层递进优化方案**：快照表（日终预提取）→ 内存缓存 → HTTP 浏览器缓存
6. 存档为 `002-方案存档/305-机会图谱页面性能优化方案（快照表+缓存）.md`

**关键结论**：
- 根本方案是 **`treemap_snapshot` 快照表**（4800行/2MB，日终基于预计算结果构建），将 API 扫描行数从 677万降至 4800行
- 辅助层是 TieredMemoryCache 新增 treemap 级别（3600s TTL）+ HTTP Cache-Control
- 全部操作合规：写操作在 data_daemon 计算层，读操作通过 DataManager 网关
- 预期优化效果：首次加载 6-12s → 0.8-1.5s（快照表），热加载 <0.1s（内存缓存），页面切换零请求（浏览器缓存）
