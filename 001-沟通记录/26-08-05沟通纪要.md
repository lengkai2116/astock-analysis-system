# 2026-08-05 沟通纪要

### 001 - 机会弹窗信号矛盾 + 方块差异/无蓝色方块核查修复
**用户输入**：①弹窗"趋势向上+估值偏低+主力提升 一致性中"但风险提示"估值偏高"（建设银行示例）；②方块差异不大+今日无蓝色方块，核查后台执行问题。

**AI执行内容**：
1. **问题1（弹窗矛盾）**：根因=前端 valuation_deviation 符号反——数据实证正=低估/负=高估（extreme_low 平均 +22、high -12），但 devWord/风险提示按"正=偏高"判断（建设银行 deviation=-8 实际高估被判"偏低"）。修复前端 4 处：devWord、风险提示 2 条、value 地图 devKey、估值结论显示（数值→等级）+ stats 低估统计。
2. **问题2（后台三根因）**：
   - **a. potential_engine 估值方向反**（`1.0-val_pct` 把负偏离当低估）→ 高分股全是 valuation_level='high'（高估）。修复：去掉反向（正偏离低估→高分）。
   - **b. P4 循环缺 `df=all_data.get(code)`**（最核心）→ 首只 NameError 跳过、后续股票量价/缠论用上一只错位 df → buy_sell_point/volume_price_fit 不落库 → 闸门2 无输入 → 无强确认（无蓝色）/无否决。修复：循环开头补 df。
   - **c. 缠论 position None 崩溃**（4 处 `.position.get`：first_buy/first_sell/divergence 等）→ analyze 静默失败。修复：position or {} 防护。
   - **d. 情绪 climax 权重 0.5**：今日涨停 243 只（真实高潮）→ 情绪权重 0.5 压缩分数（80+ 1.0%→修复后 3.3%）。设计内行为（313 §4.2 高潮 0.5）。
3. **验证**：全量 P4 重算（523s，buy_sell_point 恢复 first_buy 129/first_sell 1205 等）+ 重建快照：right_side_confirm 四档恢复（**强确认 245 蓝色方块**/否决 2132/基础 1783/未确认 1400）；高分股=低估（泸州老窖 low+16.7、悍高 extreme_low+20.8）；建设银行 level=high+deviation=-8 → devWord"偏高"一致；测试 218 passed。daemon 08:59 已用新代码运行。

**结论**：两问题全部修复。弹窗估值语义一致；方块恢复四色（蓝色可入场 245 只）、高分股为低估股。climax 情绪为今日真实状态（设计内）。

### 002 - 估值体系修正全量实施（315号方案）
**用户输入**：按方案内容及计划全量开展，完成后进行检查和测试。

**AI执行内容**：
1. **S1 利率配置化**：CN_10Y_BOND_YIELD_PCT 硬编码 2.5 → env 配置（默认 1.7）+ .env/.env.example。
2. **S2-S4 校准评估 + 方案B**：利率校准后 high 仅 55.2%→53.0%（效果有限——锚3 绝对比较在 A 股系统性偏负，非利率问题）→ 触发方案 B：composite 截面百分位分档（对齐 416 设计 5/20/80/95）+ composite 精度 round2→round4（消除重复值失真）。
3. **关键 bug 修复**：_level_by_composite 百分位方向反（composite 大=低估被判 high）→ 修复后 high 股 PE 分位 51.6%/extreme_high 58.1%（真高估）、extreme_low 16.1%（真低估）——语义正确。
4. **S5 PB-ROE 质量修正**：fina_health fail → composite-0.5；pass+ROE>12% → +0.25×(ROE/20)（茅台 ROE 30% 从 high 回归 low）。
5. **最终分布**：extreme_low 2.7%/low 17.4%/fair 64.2%/high 11.1%/extreme_high 4.5%；银行回归 fair；矛盾率（蓝色∩门禁不推荐）73.5%→33.3%。
6. **新发现**（锚3 语义）：high/extreme_high 仍受锚3（现金流绝对比较）影响，PE 分位 51-58% 已合理但非极端；锚3 相对化（FCF 截面分位）列为远期 F5。
7. **验证**：218 测试通过；快照 5560 行；daemon 重启（PID 12690，新代码）。

**结论**：315 方案阶段 1+2 全量实施完成。估值体系分布正常化（55%→11.1% high）、语义正确（高估=真贵）、下游兼容（潜力引擎 val 方向不变、矛盾率减半）。锚3 相对化（F5）与 L4 门禁调整（S7）为后续任务。

### 003 - L4 仲裁层升级方案存档（316号）
**用户输入**：整体梳理闸门1/2/3，确认闸门1/2 无需调整、闸门3（L4）为升级对象；详细梳理 L4 运行逻辑；就三个方向（门禁/票源/操作建议）升级研究后形成正式方案。

**AI执行内容**：
1. **三闸门梳理**：闸门1=机会监控（daily_change 8 维度，估值修正后语义自动改善无需调整）、闸门2=右侧确认（纯技术面正交，输入 bug 已修复无需调整）、闸门3=L4（估值门禁投票前截断 + 共识率偏低 → 升级对象）。
2. **L4 全流程梳理**：13 步（标签加载→Step A 估值门禁→退出跟踪→Step B 情绪加权→Step C 14票源共识→Step D 映射→操作建议合成→风险/清单/概览/摘要→日常变化→信号补偿）。
3. **三大方向研究（数据实证）**：
   - **门禁**：一刀切（high=截面15%非极端泡沫也被 gate）+ 投票前截断 + 维度单一 → 升级：偏离度分级（深度高估才否决）+ 门禁后移（合成后风险约束）+ 风险项扩展（财务/事件/流动性/质押）
   - **票源**：sentiment_phase 全市场 5560 全 -1（环境变量误作个股票源）、fina_health 80% 看空（suspicious→-1 过严）、sector_heat 87% 看空（none→-1 过严）、pattern_signal 93% 中性 → 共识率被系统性压低（72.8% <55% 根因）→ 升级：sentiment 移除个股票源、fina/sector 校准、动态加权（状态依赖因子合成）、补充规模/速动/低波动票源（A股五大有效因子）
   - **操作建议**：机械映射 + entry_plan 硬编码 + stop_loss/target 恒 None → 升级：结构化输出（入场区间/建仓节奏/止损止盈/时间止损/仓位联动），借鉴多层次动态风控网络（回撤4%/亏损5%/时间止损）、凯利公式、资产配置权益仓位图
4. **方案存档**：`002-方案存档/316-L4仲裁层升级方案.md`（P1 门禁→P2 票源→P3 扩展→P4 建议→P5 数据→P6 验证，含验收标准/风险回滚）。

**结论**：316 号方案定稿（待实施）。下一步可开始 P1（门禁升级）实施。

### 004 - P1 门禁升级实施（316号）完成
**用户输入**：开始 P1 门禁升级实施。

**AI执行内容**：
1. **_evaluate_gate 新增**（cross_validate.py）：估值分级（PE 分位 >90/dev<-20 深度、>80/-12 中度、>60/-6 轻度）+ 风险项（硬：监管立案；软：财务 fail/出货/流动性<1% 换手/财务异常）。
2. **门禁后移**：移除 diagnose 投票前硬截断（原 valuation high → 直接 return），改为合成后约束——深度高估/监管立案强制不推荐，中/轻度与软风险压缩仓位（mild×0.8 / moderate×0.5 / fina×0.5 / 出货×0.7 / 流动性×0.7）。
3. **风险提示升级**：估值分级文案（深度/中度/轻度）+ 流动性风险 + 财务异常提示。
4. **关联发现并修复**：矛盾率首测 84.6% —— 根因 catalyst_event=fraud_sign 占 74.9%（A5 财务异常判定过宽：ROE<3%/现金流<0.5 太松，且"财务质量差"被误标"欺诈"）。修复：event_monitor 收紧（ROE<0、现金流为负）+ 语义降级（fraud_sign=财务异常→软风险仓位减半；仅 regulatory 保留硬否决）。
5. **结果**：fraud_sign 74.9%→24.8%；矛盾率 84.6%→49%→**20%**（剩余 39 深度高估 + 1 监管立案——均设计内硬约束，真正误伤=0）；招金黄金"建议建仓"矛盾消除。
6. **验证**：218 测试通过；API（20274）与 daemon（20275）重启加载新代码。

**结论**：P1 完成。剩余"矛盾"均为设计内否决（深度高估 PE>90%/监管立案），符合方案 §3.2 设计。

### 005 - P2 票源调整实施（316号）
**用户输入**：继续 P2 实施。

**AI执行内容**：
1. **VOTE_MAP 校准**：①sentiment_phase 移出个股票源（313 §4.1 情绪=环境变量，由 Step B 权重矩阵处理）；②fina_health suspicious→0（存疑≠看空，对齐 033 标尺）；③sector_heat none→0、top_20→+0.5（无热度≠看空）。
2. **共识率定义重构**（实施中发现的结构问题）：原 rate=看多/总票（中性稀释分母，72.8% <55%）；改为 **rate=优势方向票/方向票总数**（中性不稀释，全中性→0）——cross_validate + data_daemon 快照轻量版同步。
3. **阈值全局校准**（方向分母语义）：门禁 0.55→0.65、强共识 0.75→0.80、持有 0.40→0.50、减仓 0.25→0.35（_map_consensus_to_action/_build_verdict/_build_opportunity_summary/max_ratio 映射/前端 0.55→0.65/data_daemon 门禁模板）。
4. **测试适配**：test_t66 tie 场景补 pattern 看跌形态（sector none 现 0）。
5. **结果**：共识 ≥65%（门禁）55.5%、≥80%（强共识）26%；方向分布多空均衡（bull 360/bear 314/neutral 31/tie 95——无系统性偏向）；快照重建一致（≥65% 56.9%）；218 测试通过；API（21889）/daemon（21897）重启。

**结论**：P2 完成。票源健康化（无环境票源污染、中性不稀释）、共识分布合理化（55.5% 通过门禁、有区分度）。

### 006 - P3 票源扩展与动态加权实施（316号）
**用户输入**：继续 P3 实施。

**AI执行内容**：
1. **动态加权增强**（_apply_sentiment_weight）：新增 MOMENTUM_TAGS（trend/buy_sell/pattern/ma）与 QUALITY_TAGS（valuation/fina/price/small_cap/low_vol）——climax 时动量票源看多 ×0.6（追高风险，比短线标签 0.7 更狠）；ice 时质量/估值票看多 ×1.2（冰点期均值回归机会）。
2. **扩展票源**（316 §4.3，默认关闭 L4_EXTRA_VOTES=1 启用）：VOTE_MAP 加 small_cap（市值<100亿小盘+1/>500亿大盘-1）、low_vol（20日日收益std<2%低波+1/>3.5%高波-1）、liquidity（换手<1%低流动-1）；diagnose 实时计算（DataManager）；_lookup_vote 特殊映射。
3. **验证**：开启扩展票源抽样 300 只——共识≥65% 54.0%（298/300 含扩展票源）；美的=大盘-1/低流动性-1 计算正确；默认关闭不影响现有行为；218 测试通过。
4. **进程管理**：重启 API（23093）与 daemon（23767）——期间发现多次启动累积 4 个 daemon 进程（nohup 残留孤儿 PPID=1），已清理至单 daemon。

**结论**：P3 完成。扩展票源默认关闭（IC 验证后启用），动态加权增强情绪环境联动。

### 007 - P4 操作建议结构化输出实施（316号）
**用户输入**：继续 P4 实施。

**AI执行内容**：
1. **diagnose 统一加载 df**（ECM get_cached_daily——修复 DataManager 无该方法的 AttributeError 被吞问题）：供 P4 交易计划与 P3 扩展票源复用。
2. **_build_trade_plan 新增**（§5.3 规则落地）：
   - 入场区间：回踩 MA10-MA20 带（首仓 40% 按 max_ratio 缩放）→ 放量突破 60 日前高加仓 30% → 回踩 MA20 补仓 30%（分批金字塔）
   - 止损：结构位 max(MA20, 60 日前低)×0.98（跌破离场）
   - 止盈：60 日前高压力位（或保守 15% 目标）
   - 时间止损：持仓 20 交易日未创新高则减半
   - risk_notes：门禁软风险注记（财务/出货/流动性/估值级）
3. **_build_operation_advice**：替换硬编码"60% 当前价+40% 回踩"，接 _build_trade_plan；返回结构扩展（entry_plan/stop_loss/target_price/time_stop/risk_notes）。
4. **验证**：招金黄金（建仓类）完整输出（回踩 MA10 13.60-13.11 首仓/突破前高 16.57/止损 12.84/目标 16.57/时间止损）；建设银行（否决类）无入场计划合理；抽样 40 结构化覆盖 42.5%（建仓类全要素）；218 测试通过；API 重启（24813）。

**结论**：P4 完成。操作建议从机械标签升级为可执行结构化交易计划（入场/仓位/止损/止盈/时间止损/风险注记）。

### 008 - P5 pattern_signal 覆盖排查实施（316号）
**用户输入**：继续 P5 实施。

**AI执行内容**：
1. **排查结论**：pattern_signal 覆盖率实际已正常（5560 只全部 08-05 更新、非 none 82.9%——之前 93% none 是快照旧态+统计 LIMIT bug 的误判）。**真正问题**：EnhancedPatternDetector 输出 70+ 中文形态（格兰维尔 8 法则/均线排列/楔形/三角形等），VOTE_MAP 只映射旧 30 种 → 大量形态投 0 中性，票源区分度低。
2. **修复**：VOTE_MAP pattern_signal 扩展覆盖检测器实际输出（看涨补 34 种：MA金叉/均线多头/格兰维尔买点1-4/涨停/W底/下降楔形等；看跌补 19 种：MA死叉/均线空头/格兰维尔卖点1-4/头肩顶/上升楔形/M顶等；持续/待变盘形态保持中性）。
3. **验证**：pattern 票源看多 1407/看空 3321；**非 none 覆盖 98.2%**（未映射仅收敛三角形/孕线十字/陀螺线——持续形态中性合理）；218 测试通过；API 重启（26012）。

**结论**：P5 完成。pattern 票源从"93% 中性"变为"98% 方向覆盖"，形态信号完整参与 L4 投票。

### 009 - P6 全量验证实施（316号收官）
**用户输入**：继续 P6 实施。

**AI执行内容**（316 §八 验收标准全量验证）：
1. **验收1 矛盾率**：强确认（蓝）200 只 → 不推荐 40（20%）——**剩余全为设计内否决**（深度高估 39 + 监管立案 1，方案 §3.2 明确保留），真正误伤 = 0。
2. **验收2 共识分布**：抽样 ≥65%（门禁）49.0%、≥80%（强共识）19.6%——有区分度（P2 校准后稳定）。
3. **验收3 票源健康**：sentiment 已移出（环境不投票）、fina/sector 校准无系统性同向、pattern 非 none 覆盖 98.2%。
4. **验收4 建议可执行**：抽样 60 建仓类 8 只全部含止损+目标+入场区间（100% 可执行覆盖）。
5. **验收5 闸门2 四档**：否决 2132/基础 1272/强确认 200/未确认 1956——齐全。
6. **验收6 四类股票**：建设银行（fair+右侧否决）、茅台（low+建仓+止损 1261）、宁德时代（fair+减仓）、招金黄金（fair+建仓+止损 12.84）——判定合理。
7. **回归**：pytest 218 passed；API 端到端 diagnose 招金黄金完整输出（建议/仓位/止损/目标/共识 0.9）。
8. **验收7 时间稳定**：需连续 3 交易日数据，单日无法验证（数据限制，标注 P6 后 3 日复核）。

**结论**：316 号 L4 仲裁层升级 P1-P6 全部实施完成。门禁分级后移、票源健康化、共识定义重构、操作建议结构化、pattern 覆盖扩展——判定有效性与准确性全面升级。

### 010 - daily_basic 历史数据补采（5年）
**用户输入**：按此优先级规划，先补采 daily_basic 历史数据。

**AI执行内容**：
1. **F1-F5 数据能力评估**（对话框说明）：F2 行业中性化/F4 股债差完全支持；F1 IC 窗口偏短、F3 ROE 历史浅、F5 支持；**核心约束 = daily_basic 仅 142 天（估值分位"5年"实际 7 个月）**。
2. **补采执行**：TushareProvider 批量补采（2021-01-01 起，0.3s 限速防限流，断点续传）：**1209 交易日全部成功（0 异常），daily_basic_cache 142 天/78 万行 → 1354 天/688 万行**（2021-01-04 ~ 2026-08-05）。
3. **重算验证**：5 年分位窗口下锚1/锚2 重算（P4 两轮收敛，每轮约 30 分钟）：level 分布 extreme_low 4.2%/low 16.2%/fair 57.2%/high 15.9%/extreme_high 6.5%；**high 股 PE 5 年分位 77.9%（真高估）**、extreme_low 35.0%（低估方向）；快照重建一致。
4. **回归**：218 测试通过（测试耗时 69s——分位计算变慢所致）。

**结论**：5 年估值分位数据基础就绪——估值分位从"7 个月窗口"升级为"5 年窗口"，F1（IC 回算）/F2（行业中性化）数据能力解除瓶颈。F4/F5 可立即实施，F1/F3 待财务历史累积。

### 011 - F2 行业中性化实施（315号）
**用户输入**：好，那就先做 F2 行业中性化吧。

**AI执行内容**：
1. **设计**：composite 层行业中性化——build_composite_percentile 扩展计算 7 大行业（INDUSTRY_CATEGORY）composite 均值（行业基准，≥30 只才统计）；compute_tags 分档用 `composite - 行业均值`（行业内相对估值，避免"行业整体贵→全判高估"）。
2. **一轮收敛**（F2 只改 level 分档，composite 值不变 → 基准不变 → 无需两轮）：level 分布 extreme_low 10.3%/low 15.3%/fair 66.0%/high 7.2%/extreme_high 1.2%（高估 15.9%→8.4%，低估 20.4%→25.6%）。
3. **效果验证**：半导体（整体贵行业）行业内低估 23%/高估 11%——行业内分化，不再全判高估；银行（整体便宜）高估仅 2%。快照重建一致。
4. **回归**：218 测试通过（测试耗时 77s——5 年分位计算量）。

**结论**：F2 完成。行业中性化消除"行业整体估值主导个股判定"的系统偏差，估值判定转为行业内相对。
