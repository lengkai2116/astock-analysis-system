# 2026-07-13

### 001 - 仪表盘图表不显示根因排查与修复

**用户输入**：仪表盘各数据图表未能正常显示，要求先核查不修改代码

**AI 执行内容**：
- 核查确认后端 API 全部正常工作，问题在前端
- 使用 Playwright 浏览器测试发现根因：**`_ui-prototype/assets/echarts.min.js` 被截断**（778KB，完整应 1MB），运行时 `ReferenceError: echarts is not defined`
- 同时发现聚合加载路径下 3 处 JS Bug：
  - `renderSectorChart()` 多余 `.data` 属性检查（聚合路径数据无 `.data` 包装）
  - `renderSectorMoneyflow()` 多余 `.data` 引用
  - 资金流向图日期标签 `v.date.slice(5)` 将已格式化日期变空
- 修复：重下载完整 echarts.min.js + 修复 3 处 JS Bug + 更新 268 号文档实施追踪

**关键决策**：仪表盘全量修复，Playwright 验证 8 图表全部渲染、0 错误

---

### 002 - 136 号方案 9 突破方向实施状态审计

**用户输入**：核实 136 号方案是否已被后续工作替代或完成

**AI 执行内容**：
- 使用 explore 子代理全面审计 9 个方向在文档 137-269 和代码中的实施状态
- **审计结论**：
  - ✅ F（拥挤度因子）已完成
  - 🔶 B/C/E/I 部分完成
  - ❌ A/D/G/H 未启动
- 更新 `待解决事项.md`，将原"136报告9个突破方向"替换为审计实际结论

**关键决策**：原 136 号 9 方向标记为已过时，替换为实际状态

---

### 003 - 多智能体架构 vs AI多角色研投评估

**用户输入**：多智能体架构(136方向D)是否同已有AI多角色研投或AI仲裁一样功能

**AI 执行内容**：
- 分析结论：三者不同层次。AI多角色投研=个股分析场景，UIDF=信号融合层，多智能体=策略调度层
- 评估 UIDF 是否可替换为多智能体架构：**范式不匹配**（互补维度 vs 竞争提案者），不推荐替换
- 建议务实路径：打通 UPFEngine → 启用 ConflictArbiter → 远期策略达 10+ 后再评估多智能体

**关键决策**：不追求多智能体架构，集中优化个股策略分析流水线

---

### 004 - 个股策略分析流水线构思评估

**用户输入**：提出三环节构思（独立策略→5维归集→AI+Wiki解读），评估系统是否满足要求

**AI 执行内容**：
- 逐项对照用户构思与系统现状，识别 5 大具体差距：
  1. 缺自然语言渲染层（结构数据→中文段落）
  2. LLM Wiki 知识管道未接入 AI 解读 prompt
  3. interpret_status 使用统计摘要而非 5 维自然语言
  4. 前端未对接三条后端路由
  5. signal 与 status_recognition 主次关系需调整
- 记录为 `002-方案存档/270-个股策略分析流水线现状评估与优化方向.md`

**关键决策**：后续围绕 5 项差距逐步细化确定后，做逐项修改优化

---

### 005 - 策略质量标准与内嵌知识库方案

**用户输入**：要求纠正策略解析质量评判标准，链接 LLM Wiki 知识库定义质量标准

**AI 执行内容**：
- 通过 LLM Wiki API 检索知识库，建立缠论/量价/筹码/BOCIASI/因子五策略的 A/B/C/D 四级质量标准
- 逐策略对照 LLM Wiki 要求 vs 系统当前输出
- 评估 LLM Wiki 三种使用模式（离线基准/在线注入/校验回退）
- 选型确认：SQLite FTS5 内嵌知识库方案（方案 B），零外部依赖
- 完成文件选择标准（5 条）、三级嵌入目录（T1 135+T2 53+T3 20 文件）、二次加工要求（4 项）

**关键决策**：选定零依赖内嵌知识库方案，替代外部 LLM Wiki API 作为冷回退

---

### 006 - 策略双路径输出调整计划

**用户输入**：明确具体策略调整计划，兼顾选股系统和个股分析双路径

**AI 执行内容**：
- 审计 5 策略在筛选 L3 和个股分析两条路径中的调用方式
- 逐策略制定调整项，标注对筛选/个股各自的影响和工作量
- 发现关键问题：ConflictArbiter 死代码、E12/E13/A7 三次重复计算、A7 prompt 未用 V2 验证链
- 10 项调整按 P0-P3 分级，最小 P0 仅 15 行

**关键决策**：P0 筹码 phase→status（15行）→ P1 缠论+量价+BOCIASI+因子 → P2 补齐 → P3 缓存

---

### 007 - 自然语言渲染层与 DeepSeek Prompt 分析

**用户输入**：调研 NLG 方案、确认 DeepSeek 接收内容是否完整

**AI 执行内容**：
- 调研工业界 NLG 方案（SimpleNLG、金融终端做法），结论中文不需要语法库
- 推荐分层架构：规则句子组合器（零依赖微秒级）+ 可选 LLM 润色
- 分析 `interpret_status()` prompt 构建代码：当前只发 8 行摘要（3 聚合值 + 4 state 标签）
- 确认应发三段式：全量五维 + 验证链冲突 + 知识库概念
- 270 号方案新增附录（AI 解读 Prompt 内容差距分析），任务 3f 升级为 3f'

**关键决策**：prompt 从 8 行扩展为三段式，工作量 30→50 行

---

### 关联文档

- `002-方案存档/270-个股策略分析流水线现状评估与优化方向.md`（本会话核心产出）
- `001-沟通记录/待解决事项.md`（136 方向审计结论已更新）
- `002-方案存档/268-系统操作效率瓶颈分析与优化方案.md`（仪表盘修复追踪已更新）

---

### 008 - 270号方案批次1-3全量实施

**用户输入**：开始实施批次1、2、3

**AI 执行内容**：
- **批次1（P0，基础设施）**：5项全部完成
  - 1a: 筹码 phase→status 映射（`chip_strategy.py`）
  - 1b: 知识库 787 文件嵌入到 `data/knowledge/concepts/`
  - 1c: KnowledgeReader + FTS5 全文索引（`services/knowledge_reader.py`）
  - 1d: NLG 中文术语表 80+ 条目（`services/nlg/templates.py`）
  - 1e: WikiConceptMatcher 替换为 KnowledgeReader 动态查询
- **批次2（P1，策略增强）**：9项全部完成
  - 2a-2b: 缠论三类买卖点 + 级别信息（trend.stage含"日线"）
  - 2c-2d: 量价具体形态命名 + 因果链描述增强
  - 2e: 量价筛选路径保留 status_recognition
  - 2f-2g: BOCIASI BEARISH三维状态 + 慢线 direction 语义修复
  - 2h: 因子趋势方向补充
  - 2i: NLG trend_renderer + momentum_renderer + chinese_rendering
- **批次3（P2，补齐增强）**：6项全部完成
  - 3a-3b: 筹码峰/ASR/集中价 + 主力集中价补充
  - 3c: BOCIASI快线注册为筛选因子
  - 3d: 因子扩展 MFI/MA_CROSS/ACCEL 三项
  - 3e: NLG volume/sr/risk 三个渲染器补齐
  - 3f': interpret_status 三段式 prompt 重构（NLG+验证链+知识库）
- **补齐工作**：级别背驰对比 + BOCIASI情绪周期定位 + NLG集成到路由

**测试验证**：60 passed, 12 pre-existing failures, 0 regression

---

### 009 - 剩余工作实施（差距5+批次4+前端修复）

**用户输入**：按优先级建议开展剩余工作

**AI 执行内容**：
- **差距5**: 5策略引擎 `status_recognition` 提升为首位字段，`signal` 降级
- **NLG集成路由**: `strategy_analyze.py` 5个 `_build_*_dimension()` 使用 NLG 渲染器
- **批次4（P3）**：双路径缓存（`services/analysis_cache.py`）+ 知识库热重载
- **前端修复**：`indicator-ide.html` switchStock 内联样式覆盖问题（3次修复）

---

### 010 - 验收测试与Bug修复

**用户输入**：做浏览器验证，发现内容不显示

**AI 执行内容**：
- Playwright 浏览器测试确认：
  - 根因：`switchStock()` 设 `results.style.display='none'`，但 `finishAnalysisReal()` 只加 class 未清内联样式
  - `strategyEmpty`/`strategyResults`/`crossVerify`/`aiArbitration` 4个容器均有此问题
  - 3 轮修复后浏览器验证通过：`Results display: flex`，5/6 卡片可见
- 测试通过：63 passed, 9 xfailed, 0 failed

---

### 011 - 数据采集进程启动问题排查

**用户输入**：今日K线未更新，要求核查数据进程

**AI 执行内容**：
- 发现 `data_daemon.py` 未运行（launchd 配置因 macOS TCC 安全策略被拦截）
- launchd plist 存在但已退出（exit 78/126）
- 根因：macOS 10.15+ TCC 阻止 launchd 执行桌面路径下的脚本/Python
- 创建 `start_daemon.sh` 作为 Login Items 入口

---

### 012 - 开机自启动配置（Login Items）

**用户输入**：要求最可靠的方案解决开机自启动

**AI 执行内容**：
- 创建 `start_daemon.sh`（30秒延迟启动 data_daemon.py）
- 通过 cua-driver 打开系统设置 → 通用 → 登录项与扩展
- 在文件选择器中定位到 `start_daemon.sh`
- 用户已手动完成添加

---

### 关联文档（新增）

- `002-方案存档/270-个股策略分析流水线现状评估与优化方向.md`（v2.0 已标记✅已实施）
- `backend/app/services/knowledge_reader.py`（新建）
- `backend/app/services/analysis_cache.py`（新建）
- `backend/app/services/nlg/`（新建目录，7个文件）
- `backend/app/services/signal_computation_service.py`（多轮修改）
- `backend/app/services/deepseek_analysis_service.py`（prompt重构）
- `backend/app/routes/strategy_analyze.py`（NLG集成）
- `_ui-prototype/indicator-ide.html`（前端展示修复）
- `start_daemon.sh`（新建开机启动脚本）

---

### 剩余待办
- 明日重启后需验证 `start_daemon.sh` Login Items 开机自启动是否生效
- 验证数据进程启动后日终同步是否正常拉取今日K线数据
