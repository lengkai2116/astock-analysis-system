# 2026-06-02

### 009b — PreFilter 资产核实与 138 文档修正

**主题**：核实第一层风险筛选（达尔文策略）资产，纠正 138 文档 v1 的架构概念混淆，重写为准确的资产核实与修复方案

**关键词**：PreFilter、第一层风险检验、DarwinRiskFilter、ChipPreFilter、多层筛选系统、138文档修正、方案一/方案二

**关键结论**：

v1 版 138 文档存在概念混淆，将 `chip_pre_filter.py` 错误定义为"第一层风险筛选"，并声称 `DarwinRiskFilter` 未接入主路径。经核实 132 文档"六、多层筛选系统"后，确认实际情况：

**两条独立的过滤路径：**

**路径一（达尔文筛选 — 方案一）**：API `/api/v3/screener/run` 已完整跑通
- L1 `DarwinRiskFilter` → L2 `ChipScorer` → L3 策略验证
- **工作流完整**但**规则质量不达标**：
  - `_filter_st`：代码字符串匹配，不可靠
  - `_filter_low_liquidity`：`vol > 10000`，阈值无依据
  - `_filter_continuous_loss` / `_filter_high_valuation`：均为 `return True` 占位符
  - L3 的缠论验证（`ChanlunAlphaModel`）未接入

**路径二（筹码策略信号 — 方案二）**：单只股票分析路径
- `chip_pre_filter.py` 的5个子过滤器（MarketEnvironment / CircuitBreaker / Eligibility / Liquidity / MarketCap）设计完整但**全部零引用**
- `ChipDistributionStrategy.analyze()` 和 `SignalComputationService` 均无前置过滤

**修正后的修复方案（Phase 优先级排序）：**
- Phase 1：修复 DarwinRiskFilter L1 规则质量（`_filter_st` 改为基于名称、`_filter_low_liquidity` 改为换手率/成交额）
- Phase 2：接入 ChipPreFilter 到芯片策略路径（`ChipDistributionStrategy.analyze()` 开头）
- Phase 3（可选）：完善 L3 策略验证（接入 `ChanlunAlphaModel`）

**涉及文件**：
- 查阅: `002-方案存档/132-策略体系完整方案与输出体系报告.md`（六章）
- 查阅: `backend/app/engine/framework/screener.py`
- 查阅: `backend/app/engine/framework/chip_pre_filter.py`
- 查阅: `backend/app/routes/screener.py`
- 查阅: `backend/app/services/signal_computation_service.py`
- 重写: `002-方案存档/138-PreFilter前置过滤层接入与重构方案.md`（v2修正版）
