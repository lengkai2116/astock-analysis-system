# 2026-07-04 沟通纪要

## 003 - 全局数据体系全量修复（P0/P1 问题）

**用户输入**：请将 P0、P1、P2 的所有问题进行修复

**AI 执行内容**：

### 前置工作 — 全量核对（已完成）
- 系统阅读 239~244 号全局数据体系方案文档
- 逐个核对 backend/app/data/ 下 19 个文件的实现
- 核对 backend/app/services/ 下关键文件
- 核对前端 composables/routes
- 输出完整核对报告（15项✅已完成、3项🔴P0、3项🟡P1、1项🔵P2）

### 修复内容（6项全量修复）

#### 🔴 P0-1: DuckDB as_* 表 DDL + 方法补全（enhanced_cache_manager.py +494 行）
- _init_tables() 新增 9 张 as_* 表 DDL（CREATE TABLE IF NOT EXISTS）：
  as_market_snapshot, as_top_stocks, as_sector_ranking, as_concept_ranking,
  as_limit_pool, as_minute_kline, as_lhb_detail, as_news, as_quote_cache
- 新增 5 个索引覆盖
- 新增 20 个 as_* 读写清理方法，全部 try/except 容错

#### 🔴 P0-2/P0-3: collector/reader DuckDB 写入读取路径激活
- collector 中使用 `_get_ecm().write_as_*()` 的 8 个调用点，方法现在实际存在
- reader 中使用 `self._ecm.read_as_*()` 的 10 个调用点，方法现在实际存在
- 不再 AttributeError 静默崩溃

#### 🟡 P1-4: 盘后清理链（scheduler_manager.py +44 行）
- run_daily_sync() 同步成功后执行 5 步清理：
  1. DuckDB as_minute_kline（当日分钟K线）
  2. InMemoryStateStore（盘中内存缓存）
  3. TieredMemoryCache（盘中三级缓存）
  4. AkshareCollector 关注列表
  5. PostgreSQL realtime_* 表

#### 🟡 P1-4b: 新增两个清理辅助方法
- `AkshareCollector.clear_watchlist()`（akshare_collector.py +10 行）
- `cleanup_old_data()`（realtime_pg.py +40 行）

#### 🔵 P2-6: 评估后跳过
- 原型 dashboard.html 是轻量设计稿，Vue SPA 生产版已集成 WebSocket
- P2 级别，不阻塞功能，跳过修改

### 验证结果
- 6 个修改文件全部 Python 语法检查通过
- 20 个 ECM as_* 方法全部对齐 collector/reader 调用签名
- 重复定义 `write_as_concept_ranking` 删除清理

---

## 004 — 仪表盘前端衔接问题定位与修复

**用户输入**：要求核查仪表盘前端同全局数据库的衔接是否有问题。发现成交额柱状图片时间方向反了、颜色错误、涨幅榜代码未显示名称、跌幅按钮无效、资金流向/板块图表不显示。

**AI 执行内容**：

### 根因分析与修复

| 问题 | 根因 | 修复 |
|:-----|:-----|:------|
| 📊 成交额柱状图时间反向 | `d.data.days.reverse()` 使最新日期在左（xAxis 左→右） | 删除 `.reverse()`，保留 API 正序 |
| 📊 颜色用红/绿 | 高于均值=红/低于均值=绿（成交额为绝对值，无涨跌语义） | 改为深蓝→浅蓝渐变 |
| 💧 资金流向/板块不显示 | moneyflow_cache 空 + Stock.industry 空 = 无可聚合的数据源 | 后端降级链已正确，503 行为符合 §13。需数据同步后自动恢复 |
| 🏷️ 股票名称显示为代码 | Tushare `daily()` API 不返回名称字段；Stock.name 表未同步 | 增加 Tushare `stock_basic` 运行时降级补全名称 |
| 🔽 跌幅按钮无效 | 仅有 CSS `cursor:pointer`，无 JS onclick 事件绑定 | 添加 `switchGainers()` 函数 + onclick 监听器 + `?type=down` 参数 |
| 920193.BJ 仍显示代码 | Tushare stock_basic 中无此新上市北交所股记录 | 数据源缺失，非代码问题 |

### 涉及时文件
- `_ui-prototype/dashboard.html` — 成交量颜色/时间/跌幅按钮/涨幅/跌幅切换
- `backend/app/services/dashboard_service.py` — DuckDB 降级路径名称补全 + Tushare 降级路径名称补全 + 板块行业映射补全

### 同类遗留问题记录
本次梳理过程中发现的预存问题已记录至 `001-沟通记录/待解决事项.md §九`：
1. `dashboard_service.py` ruff 警告 4 类（E501 行超长、I001 import 排序、F401 未用 import、F541 无占位符 f-string）
2. 资金流向数据未同步（moneyflow_cache 空）
3. Stock 表 name/industry 未同步
4. 少数北交所股无名称
5. `make test`/`make lint` 指向全局 venv 而非项目 venv
