# 2026-08-13 沟通纪要

### 001 - 机会地图→策略分析页 5 项问题核查（只核查不改码）

**用户输入**：通过机会地图查看正强股份（301119.SZ）的机会弹窗并进入个股策略分析页，发现 5 个问题需核实评估：①弹窗显示现价 22.35、强右侧可入场，但止损 16.69（≈26%，不合理）；②个股页 K 线正常但图表上方股票名未显示、显示两个一样的代码；③缠论构图一直"缠论分析中"不出图；④标签解说有英文且与"标签解说全量"内容相同、混乱看不懂；⑤三情景内容全堆在情景A框内，B/C 空白。要求详细核查并在对话框说明，**不修改方案和代码**。

**AI执行内容**（全部通过代码+接口+数据库三重验证）：

1. **问题1 止损 26% —— 属实，规则设计问题**：
   - 数据链：`opportunity-treemap.html:1870-1879` 弹窗止损取自 `/api/v3/opportunity-atlas/diagnose` → `operation_advice.stop_loss`（字符串 `close < 16.69`，正则提取 16.69）。
   - 后端根因：`advice_builder.py:89-90` `_geometric()` 将 support=60日低点（lo60）作为唯一防守位；`advice_builder.py:290` `exit_rules` 止损 = `close < 60日低点`。实测 301119.SZ 近60日最低 low=16.69（数据库 stock_cache.db daily_cache 验证），现价 22.35 → 止损距离 -25.32%（diagnose 实测 `dist_to_support_pct=-25.32`）。
   - **不合理性确认**：系统同时给出"右侧强确认+共识100%可入场"与 26% 宽止损，盈亏比 dist_res 19.98% / dist_sup 25.32% = 0.79（<1，盈亏比倒挂），且几何指标 risk_reward=0.79 已明示。**根因**：止损仅锚定 60 日低点，对已从底部右侧拉升的股票（16.69→22.35 已 +34%）止损必然过宽；缺少"止损距现价上限/近端结构位"约束。
   - 附带发现：diagnose 中 `cross_validate.py:362-373` 传给 advice_builder 的 `_dims` 是**硬编码全看多**（factor bullish/chanlun 上升/volume_price up/chip bullish/emotion bullish）→ 共识被算成 100%；而个股页 analyze 接口真实五维共识仅 67%（实测）——弹窗"共识100%"虚高。

2. **问题2 股票名显示两个代码 —— 属实，前端 bug**：
   - 根因链：`indicator-ide.html:2396` URL 跳转时 `_currentTsName=''`（未携带名称）→ `switchStock` 内 `2227-2231` `stockNameEl.querySelector('span')` 取到**第一个 span（#sh-name-text 名称位）**直接覆盖为 `tsCode` → `2827` `sn.textContent = _currentTsName || d.ts_code`（空→显示代码）→ `2828` 代码位也显示 ts_code → 名称/代码两处均为 301119.SZ。
   - kline 接口实际返回 `stock.name='正强股份'`（实测），但前端未回填；quote 接口无 name 字段。

3. **问题3 缠论一直"缠论分析中" —— 属实，前端直接 return bug**：
   - **主根因**：`indicator-ide.html:2930` `loadChanlunCharts` 首行 `var container = document.getElementById('chanlunCharts')` —— 但 HTML 中**不存在 id="chanlunCharts"**（只有 `class="chanlun-col"`、`id="chanlunChartLong/Short"`）→ container=null → 函数直接 return，占位符"缠论分析中..."永不替换。
   - **次根因（数据）**：即使修复 id，`period=short`（60分钟）接口实测返回 HTTP 500 "数据不足，需要至少30根K线"——分钟库仅 692 条、60m 聚合仅 3 个交易日；`period=long` 接口正常（200，129KB HTML）。
   - 触发条件：`3575-3579` 缠论图仅在"运行分析"完成且缠论勾选时加载，页面初始只显示静态占位。

4. **问题4 标签解说英文 + 与全量相同 —— 属实，两处前端 bug**：
   - 英文：`renderTagExplainGrid`（2167-2173）直接渲染 `h.name`（**英文字段键**如 right_side_confirm/main_force_phase/valuation_level）作粗体标签，词典解释文本也含英文值（如 'enter 可入场 / light 轻仓'）。
   - 内容相同：`2112-2123` 标签解说 `tagsBody.innerHTML = html` 与全量 `fullBody.innerHTML = html` **使用同一个 html 字符串** → 两处必然完全相同（设计本意全量应展示全部命中项+来源标注，实际完全复制）。

5. **问题5 三情景内容全在A、B/C空白 —— 属实，前端合并渲染 bug**：
   - `3838-3844`：存在 operation_advice 时，把 建议卡+入场+失效+依据+七维+全部情景概率步骤（`cardHtml+entryHtml+invHtml+evHtml+dimHtml+scnHtml`）**全部塞进 apStepsA（情景A容器）**，随后 `['apStepsB','apStepsC'].forEach(id=>innerHTML='')` 清空 B/C → 呈现"内容全在A、B/C空白"。

**关键结论**：5 项问题全部属实，根因 7 处（前端 6：chanlunCharts id 不存在、名称回填缺失/span 覆盖、标签解说双写同串、情景合并入A；后端 1：止损=60日低点无距离约束；另 diagnose 硬编码五维致共识虚高）。均未修改任何方案与代码。

**后续建议**（待用户决定）：①止损增加"距现价上限（如 10-15%）+ 近端结构位取高"约束；②URL 跳转后从 kline/quote 回填名称；③修复 chanlunCharts 容器 id；④标签解说键名中文化 + 全量单独渲染；⑤情景改回三卡独立渲染。

### 002 - 5项问题修复完成（后端止损 + 前端4项）

**用户输入**：关于止损设置请核查系统自带知识库确认最合理设置方式，其他无异议请进行修复，完成后检查。

**AI执行内容**：
1. **知识库核查**（data/knowledge/concepts/）：止损应锚定**近端结构位**（《短线风险控制/交易计划制订》：突破日大阳线开盘/收盘/实体一半三档线；《短线高手的交易语言》：止损≤1/2止盈即盈亏比≥2；《R乘数》：R=(目标-入场)/(入场-止损)；《朗德里资金管理》：2%风险规则止损距离与仓位联动；《多层次动态风控》：日内回撤4%/亏损5%止损）。
2. **后端止损修复**（`advice_builder.py`）：`_geometric()` 止损位从 60日低点改为**近端结构位 max(MA20, 近20日低点)**，距现价≤15%上限，止损必须低于现价（322号H3教训）；新增**盈亏比门禁**（rr<1 时 enter/light 降级为 wait，避免"强右侧+26%宽止损"自相矛盾）。实测 301119：止损 16.69(-25.3%) → 19.92(-10.9%)，盈亏比 0.79→1.84。
3. **diagnose 共识去硬编码**（`cross_validate.py`）：chanlun/volume_price/emotion 三维权重的硬编码看多改为真实标签推导（buy_sell_point/volume_price_fit/sentiment_phase）。
4. **前端 4 项**（`indicator-ide.html`）：①URL跳转回填股票名（kline stock.name）+删除名称位被覆盖逻辑；②缠论 loadChanlunCharts 容器 id 修复（chanlunCharts→chanlunChartLong）；③标签解说中文化（TAG_CN_NAME 键名+组名映射+英文值清理），全量块独立渲染含来源标注；④三情景按 oa.scenarios 分发各自卡片+标题/概率更新。
5. **测试**：test_322_s5_executable 断言更新（止损=近端结构位 11.84）；相关 54 测试通过；ruff 无新增错误（25 vs 原始 26）。

**验证**：浏览器端到端全部通过（headless Chromium 实测：止损 19.92、名称"正强股份"、缠论日线 iframe 加载、标签全中文、三情景独立 60%/20%/20%）。全量 pytest 13 失败均为 daemon 并发锁（已记待解决事项 §十三，stash 验证与本次无关）。

### 003 - 按326定稿结构归位（策略建议卡收口+深度依据段+建议卡两列网格）

**用户输入**：先搜索之前沟通确认的个股策略页面的排布规划（326号定稿+rev2原型），比对差异；确认后"请按定稿进行结构归位，修复后检查"。

**AI执行内容**：
1. **差异核查**：定稿右列三段式（现状描述/操作建议/深度依据）vs 当前三卡（现状/策略建议/操作建议）——发现 6 处差异（策略建议双轨未收口、缺深度依据段、建议卡非两列网格、标签全量位置错、三情景形态、总览位置）。
2. **结构归位**（`indicator-ide.html`）：
   - 删除 strategyRec 卡（主信号/价位/仓位由操作建议卡承载，关注点/风险迁入深度依据）；
   - 新增 depthPanel「📖 深度依据」（标签解说全量+DeepSeek追问+数据来源+关注点/风险评估）；
   - 操作建议卡改 advice-card 两列字段网格（入场区间/仓位/目标/止损/预期持有/触发/失效 7 字段+依据条），七维红绿灯从建议卡移除（归位现状描述）；
   - 上部 main-area grid 改 flex 镜像布局（1.7+space-4+1），sub-below 去边框，上下两行边缘像素级对齐。
3. **验证**：浏览器实测完全镜像（左 88=88 / 右 1576=1576 / 宽比 1.7/1.7）；区块顺序=现状→操作建议→深度依据；建议卡 7 字段齐全；三情景 60/20/20 独立；标签全量 9 项含来源；JS 零错误；后端 54 相关测试通过。

### 004 - 标签解说方向性修正（用实际值叙述现状，非词典条目）

**用户反馈**：标签解说不应是对标签"是什么"的讲解（值域枚举），而应通过该股票的实际标签结果**说明股票现状**。当前输出如"机会状态 — 机会状态：可入场/轻仓/等待/回避"完全未使用实际命中值。

**AI执行内容**（`indicator-ide.html`）：
1. `collectHitTags` 每条命中携带实际值（value）；
2. 新增 `TAG_VALUE_CN` 值域映射（opportunity_state/right_side_confirm/main_force_phase/fina_health/valuation_level/sentiment_phase/main_force_presence/trend_alignment/fund_flow/volume_price_fit/risk_level/price_position/style_exposure/capital_nature/sector_heat/phase_conflict/opportunity_type 等 → 中文现状叙述）；
3. 渲染改为 `<字段中文名>：<实际值现状>（组）`；opportunity_label 等值即中文的字段直接展示（TAG_VALUE_ASIS + 与 opportunity_type 去重）；fina_health 映射收窄避免字段名重复；
4. 简版与全量同步使用实际值渲染。

**验证**（浏览器实测）：301119 = 机会状态：当前可入场 / 右侧确认：强确认 / 主力阶段：主力洗盘期 / 财务健康：健康 / 估值档位：合理 / 市场情绪：情绪复苏期 / 价格位置：价格处低位——全部为该股实际现状；000066 = 当前建议回避 / 右侧否决 / 主力出货期 / 财务存疑，通用性正常。JS 零错误，后端 50 测试通过。

### 005 - 83标签与知识库核对 + 实质矛盾修订（asr/cyqkl量级对齐）

**用户输入**：将梳理的83标签规范与系统知识库逐一核对评估；针对实质矛盾和偏差进行修订。

**AI执行内容**：
1. **知识库核对**（788概念逐项比对）：22 个一致（缠论买卖点/情绪周期/龙虎榜/财务门槛等核心框架忠实知识库）、12 个偏差（方法论差异）、2 个实质矛盾（asr/cyqkl）、10 个知识库缺失（自研）。
2. **更正上轮误判**：重新精读《ASR指标》后确认 asr 与知识库**语义方向一致**（高浮筹=筹码密集=突破前蓄势），真实差异仅为**量级**（知识库 0-100 vs 系统 0-1），非"方向相反"。
3. **修订 asr/cyqkl 量级对齐 0-100**（4 文件）：
   - `chip_indicators.py`：calculate_asr/cyqkl ×100，分级阈值同步（asr<20低浮筹…≥90极限；cyqkl<10弱…≥60极强，对应知识库 >30强/>60加速）
   - `phase_detector.py`：_dim_asr 与 _chip_distribution_analysis 阈值 ×100（0.9→90、0.15→15、0.3→30）
   - `chip_strategy_impl.py`：建仓/洗盘 asr 阈值同步（0.7→70、0.3-0.6→30-60）
   - 前端词典描述更新（0-100 口径）
4. **偏差项评估**：10 项 ⚠️ 判定不改（方法论差异或知识库无量化定义，改属过度设计）；zhongshu_strength 随 asr 修订自动正确。
5. **验证**：真实数据 300759 asr=3.11（0-100）、cyqkl=0 语义正确；46 测试通过；ruff 无新错误（stash 对比一致）。

**关键结论**：asr/cyqkl 与知识库实质矛盾已修订（量级统一 0-100，语义对齐"高浮筹=密集=蓄势"）；其余偏差项为方法论差异，维持系统自研口径。

### 006 - 双体系共用核查结论记录至329号

**用户输入**：核实标签体系是否同时被机会图谱及个股策略分析两套体系使用，是否存在问题；将核查结论记录到 329 号报告。

**AI执行内容**：
1. **核查**（只读）：确认同一套 opportunity_tags_cache（83 标签）被双体系共用——机会图谱（treemap/diagnose）与个股策略分析（analyze）各自调用 build_operation_advice + arbitrate。
2. **3 处实质问题**（实测 300759/301119/000066）：①共识率双口径（弹窗标签轻量推导 100%/90% vs 个股页真实五维 67%）；②仓位双轨未收口（弹窗 0.6 vs 个股页 0.3，低置信度降级只在个股页）；③标签矛盾未暴露（300759 risk_level=HIGH + 缠论下降 + 获利盘99% 却输出可入场）。
3. **记录**：329 号报告新增"附2：标签体系双体系共用核查"章节（含消费链路图、3 问题实测表、定性、4 项修复建议待决策）；同步修正附1 汇总表中 asr/cyqkl 已修订状态（🔴→✅，更正"语义方向相反"为"量级差异"）。

### 007 - 系统现状评估存档（330号）

**用户输入**：评估标签体系和两套策略引擎现阶段能否支撑"现状描述清楚 + 操作建议可参考/有效"两大目标，整体问题与根因；将评估内容建立方案存档。

**AI执行内容**：
1. **评估结论**：两大目标均未被充分支撑——现状描述 ⚠️30%（数据真实但呈现层只露8/83标签、结构位失真、矛盾维度被硬合成、共识口径分裂），操作建议 ❌15%（止损拍脑袋/目标虚构/情景空话/无有效性闭环）。
2. **9 项问题清单**（4🔴/3🟡/1🔵）与**根因**：数据链断裂（执行层与呈现层各丢一次真实数据）+ 无闭环验证（回测只消费信号层未消费建议层）+ 双引擎各自为政（传参不同口径分裂）。
3. **改进方向 4 项**（待决策）：执行层接真数据、建议接验证环、双引擎口径统一、呈现层扩源。
4. **存档**：330号方案《系统现状评估：标签体系与双引擎支撑能力诊断》。

### 008 - 4项改进方向详细展开补充至330号

**用户输入**：针对 330 号方案的 4 项改进方向逐项详细展开说明，并评估修改后对机会图谱板块和个股分析板块的影响；补充到 330 号方案。

**AI执行内容**：
1. **4 项改进逐一展开**（含现状问题/修改内容/影响评估）：
   - 改进1 执行层接真数据：止损改"真实近端结构位选择器（MA20/前低/筹码峰/中枢下沿，须低于现价）+15%兜底"；目标改真实压力位；失效解析 exit_conditions；情景注入真实价位
   - 改进2 建议接验证环：确认 signal_records 已有 entry_price/risk_line/target_price 字段（建议级验证结构就绪），但 operation_advice 从未写入——补落库+复用 T+5/10/20 回调+win_rate_cache 增加操作建议维度
   - 改进3 双引擎口径统一：diagnose 复用 analyze 同源输出（真实五维），共识去虚高，仓位降级逻辑抽共享函数
   - 改进4 呈现层扩源：collectHitTags 26→83 键、结构位按现价过滤（>±40% 标 historical）、arbiter 暴露 conflict_evidence
2. **双板块影响评估**：每项附机会图谱/个股分析两张影响表 + 风险项；汇总表（4项×2板块）+ 优先级建议（改进3收益最大风险最小 → 改进1 → 改进4 → 改进2）。
3. **存档**：330号新增"六之二"章节（详细展开+影响评估，264行），frontmatter 状态更新。

### 009 - 330号方案修订实施（4项改进全部完成+验证）

**用户输入**：开展330号方案的修订工作，并进行修订检查。

**AI执行内容**（按优先级 改进3→1→4→2）：
1. **改进3 双引擎口径统一**（cross_validate.py）：新增 `_extract_real_dimensions` 从 strategy_signal_detail 缓存提取真实五维（chanlun/volume_price/chip/emotion/factor 方向），替换弹窗 diagnose 的标签轻量推导 `_dims`。实测：双端共识 67% 一致（原弹窗 100%/90%）、仓位 0.3 一致（原 0.6）。
2. **改进1 执行层接真数据**（advice_builder.py）：目标位改真实压力（60日高点=目标1、MA60=目标2 且须高于目标1，去 ×1.15 虚构）；失效接入 exit_conditions（4 条：止损+MA20破位+出货+估值退出）；情景注入真实价位（"回踩 42.33 不破可加仓/站稳 51.30 追进"），压力位过滤须高于现价；结构维同源修正。
3. **改进4 呈现层扩源**（indicator-ide.html + arbiter.py + cross_validate.py）：collectHitTags 26→60+ 候选键（补 trend_alignment/fund_flow/ma_alignment/volatility_level 等），标签解说 8→12/16 项；arbiter 新增 conflict_evidence（缠论vs趋势/高位获利盘/结构风险vs确认 4 类检测），diagnose 透传，前端风险边界展示；结构位 historical 过滤。
4. **改进2 建议接验证环**（strategy_analyze.py）：operation_advice 生成后落库 SignalRecord（entry/risk/target/zone/快照，PG 优先+SQLite 回退双路径，探测 5432 端口）；实测 PG 落库 3 条（300759 entry=49.8/risk=42.33/target=51.3，pending）——复用现有 T+5/10/20 回测回调自动验证。
5. **修订检查**：67 个相关测试全部通过；ruff 无新引入错误（F821 logger 未定义为预存问题，已顺手修复）；浏览器端到端 4 项改进全验证、JS 零错误。
6. **环境发现**：本机 PG 运行中（DATABASE_URL=postgresql:///stock_analysis）；API 单实例保护在 kill 后 TIME_WAIT 期误判占用（非本次范围）。

**关键成果**：330号4项改进全部落地——现状真实可见（12/16 标签+冲突暴露）、建议可执行（真实价位+4条失效）、双端一致（共识67%/仓位0.3）、有效性可回答（建议级验证环接通）。

### 010 - 业务数据库对齐实施方案存档（331号，方向1）

**用户输入**：核查 DATABASE_URL 配置矛盾（Docker 废止 vs PG 残留）；按方向1（对齐292权威改回SQLite）制定实施方案。

**AI执行内容**：
1. **核查确认**：①当前 API 实际连 PostgreSQL（load_dotenv 加载 .env 的 postgresql:///stock_analysis，signal_records 08-13 写 PG）；②根因=251号方案（07-06）曾定 PG 为业务库写入 .env，292号权威（改回 SQLite app.db）未同步清理；③本机 PG 为 Homebrew 裸装（非 Docker，daemon 已废止）；④PG 仅 strategy_templates_v2(20)+signal_records(11) 有数据，37 表全空；⑤SQLite app.db 29 表=PG 39 表子集，缺 10 张（opportunity_library 有 ORM 模型会自动建，realtime_* 无模型不影响）；⑥db.create_all() 启动自动建表。
2. **331号方案存档**（5 步实施）：Step1 改 .env DATABASE_URL=sqlite:///data/app.db；Step2 PG→SQLite 数据迁移（两表取并集去重）；Step3 启动兼容验证（opportunity_library 自动建表/analyze 落库 SQLite）；Step4 文档同步（AGENTS.md L62 / 292号 L744）；Step5 防静默（load_dotenv 后打印业务库日志+非sqlite warning）。
3. **影响评估**：机会图谱/个股分析板块无影响（读 ECM 缓存），改进2 验证环改善（本地无 PG 也可靠），业务数据对齐 AGENTS.md/292。7 项验收标准 + 回滚方案（.env 备份/PG 数据保留）。

### 011 - 331号方案实施完成（业务库PostgreSQL→SQLite）

**用户输入**：开始执行331号实施方案。

**AI执行内容**（5 步全部完成）：
1. **Step1 改 .env**：`DATABASE_URL=sqlite:///data/app.db`（备份 .env.bak.331）。
2. **Step2 数据迁移**（scripts/migrate_pg_to_sqlite.py）：PG→SQLite 两表合并——strategy_templates_v2（20 条，插 9 去重 11）、signal_records（1080 条，插 1050 去重 30）；app.db 备份 app.db.bak.331。
3. **Step3 兼容验证**：发现关键坑——**Flask-SQLAlchemy 将 `sqlite:///data/app.db` 相对路径重定向到 `backend/instance/data/`（不存在→unable to open）**，改为绝对路径 `sqlite:////Users/.../data/app.db` 后 create_all 成功、opportunity_library 自动建表、analyze 落库 SQLite（20:12 新增，PG 停写）、复盘写入 review_units 验证通过。
4. **Step4 文档同步**：AGENTS.md 开发环境段改为单机直跑（删 Docker Compose 指令）；292号 L744 更新"5432 PG 历史残留已清理"。
5. **Step5 防静默**：config.py 新增 verify_business_db()，app/__init__.py 日志配置后调用——启动日志明确打印"业务数据库连接: sqlite:///..."，非 SQLite 告警。
6. **修订检查**：90 测试通过；双库一致性（templates 20=20、signal_records 1081 vs 1080 正确差 1 条新落库）；业务功能（策略模板 API/复盘池）读写正常。

**关键成果**：业务库正式从 PostgreSQL 对齐到 SQLite data/app.db（292权威），251号残留彻底清理；Flask-SQLAlchemy 相对路径坑已规避（绝对路径）；防静默机制防止未来误配。
