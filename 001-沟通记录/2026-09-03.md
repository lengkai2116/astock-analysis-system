## 2026-09-03

### 001 — 400号dim4资金筹码引擎修复方案实施
按400号方案逐项实施dim4资金筹码引擎修复。共完成5项BUG修复和16项死代码清理：

**F-01(HIGH) 筹码成本结构补全**：修改`chip_distribution_service.py`的`ChipDistributionEstimator.get_tags()`方法，添加`ChipIndicators`导入，将已计算的asr数值写入tags，调用`ChipIndicators.calculate_profit_ratio()`和`calculate_cyqkl_entity()`补全profit_ratio和cyqkl字段。修复后data_daemon的pre_feat_cache将包含完整的筹码成本数据，dim4 evaluate()的float()转换正常工作。

**F-02(MEDIUM) retail_institution逻辑修复**：扩展散户/机构判定条件，从`mfp == 'building'`扩展为`mfp in ('building', 'raising', 'lifting')`，使拉升期+资金流入也判定为"机构参与"而非"中性"。

**F-03(MEDIUM) overall_direction对齐**：将`overall_direction`的1值判定从`('building', 'raising')`扩展为`('building', 'raising', 'lifting')`，与light=green保持一致。

**F-04(LOW) signal_evidence空值清理**：bsp为空时输出"无明确筹码信号"而非"买卖点=→无明确筹码信号"。

**F-05(LOW) pde_vote_ratio类型保持**：确认pde_vote_ratio保持dict类型输出，不转str。

**死代码清理**：安全删除PhaseDetectionEngine中7个旧版方法、MainForceScorer中2个未使用方法、7个独立辅助函数，共约400行。所有函数经`find_referencing_symbols`确认无引用、无getattr动态调用后删除。

验证结果：py_compile语法检查通过，AST解析确认16个死函数全部移除，ChipIndicators导入和asr/cyqkl/profit_ratio计算代码确认存在。

### 002 — 质检发现问题修复（daemon过滤器 + asr语义）
代码质检发现2个遗留问题并修复：①daemon过滤器缺少profit_ratio导致F-01修复未闭环，在data_daemon.py的chip过滤器中补充'profit_ratio'（4字段→5字段）；②get_tags()中asr值语义不一致（CV 0~3 vs 百分比0~100），改用ChipIndicators.calculate_asr()百分比值，chip_concentration阈值同步调整为80/30。

### 003 — dim4前端数据源核查与401号方案制定
从dim4引擎输入原料数据角度进行深度核查，发现4项问题：P-01(HIGH)daemon derived.profit_ratio用chip_position字符串覆盖了chip.profit_ratio导致F-01仍未闭环；P-02(MEDIUM)margin_change_5d不在pre_feat_cache中导致融资维度永远缺失；P-03(MEDIUM)两套PhaseDetectionEngine并存可能产生不一致；P-04(INFO)旧SIG路径仍在使用7维卡片不经过dim4。编制401号修复方案，总工时1.1天。

### 004 — 管道架构核查与前端数据获取待办
核查368号SIG-JUD-OUT管道架构：①7维卡片的筹码数据实际来自dim4引擎（Dim4ChipFundEngine），经SIG信号包装后由_build_chip_dimension消费，但369号重构后compute_via_engines不再为dim4信号写入status_recognition字段，导致7维卡片筹码维度当前为空壳（chip_peak=0/concentration=0/direction=neutral）；②前端直接从SIG(signal_json)和JUD(status_snapshot)中间产物读取，绕过OUT成品仓，违反353号COL→RAW→SIG→JUD→OUT→USE管道设计；③当前前端尚未全面排查修订，此项记录为待办，待后续统一处理。

### 005 — 401号方案实施与代码清理
实施401号方案3项修复+代码清理：P-01删除daemon derived.profit_ratio覆盖（HIGH，闭环F-01）；P-02补充margin_change_5d预计算到chip_fund_ext（MEDIUM）；P-03统一PhaseDetectionEngine调用，dim4优先使用tags中daemon预计算值（MEDIUM）；清理deprecated_framework_integration.py和chip_strategy.py中4个未使用类（ChipUniverseSelectionModel/ChipAlphaModel/ChipRiskManagementModel/ChipScorer）+无用import。全部13项验证通过。

### 006 — dim5引擎逐行核查与402号方案制定
对dim5情绪环境引擎（1349行）进行逐行代码核查。发现4项BUG（B-01 emotion_ext作用域越界/HIGH、B-02 _assess_stock_emotion死调用/MEDIUM、B-03 breadth永远None/MEDIUM、B-04 margin_trend不走分库/LOW）、4项死代码（D-01~D-04）、3项提效项（S-01~S-03）、2项输出有效性问题（E-01~E-02）。编制402号修复方案，总工时1.0天。

### 007 — 402号方案实施
实施402号方案全部7个Phase：B-01 emotion_ext作用域修复(HIGH)、B-02 _assess_stock_emotion死调用清理(MEDIUM)、B-03 breadth标记跳过(MEDIUM)、B-04 margin_trend分库路由(LOW)、D-01~D-04死代码清理(LOW)、S-01~S-03提效优化(LOW)、E-02 judgment补phase字段(LOW)。13项验证全部通过。

### 008 — 406号dim7估值引擎代码质量审计方案核查
对406号方案（dim7估值引擎代码质量审计与修复方案，状态"已实施Phase 1-6全部完成"）进行逐项逐行代码执行核查，覆盖3个BUG修复、8项死代码清理、6项简化提效、3项优化提效，涉及`dim7_valuation_engine.py`、`dim_adapter.py`、`conflict_matrix.py`三个文件。

**BUG修复核查结果**：
- BUG-01(CRITICAL) `df_fina` NameError：✅ 已修复。`_compute_valuation()`中line 723-726添加`df_fina`获取，`_empty_tables`中`fina_indicator`检查已删除。
- BUG-02(MEDIUM) `deviation` key名不匹配：⚠️ 方案diff描述有误（修改前后代码相同），但代码本身已正确实现双key fallback（`valuation_deviation`→`deviation`），功能无误。
- BUG-03(LOW) `int()`截断float锚评分：✅ 已修复。`conflict_matrix.py:131-132`的`int()`截断已移除。

**死代码清理核查**：4个死函数（`recompute_ic_weights`/`load_ic_weights`/`save_ic_weights`/`compute_fund_strength`）已全部删除。

**简化提效核查**：`_pe_percentile`、`_net_profit_col`、`_adjust_composite`、`_build_valuation_plain` 4个helper已全部提取；`bisect`重复import已清理。

**优化提效核查**：`_adjust_composite`已提取；`build_potential_percentile_tables()`空表保留（ponytail注释说明设计意图）；BUG-01作用域问题已通过独立获取`df_fina`修复。

**方案文档问题**：BUG-02的diff描述修改前后代码相同（文档错误）；死代码#6称`json`/`os`为死import但实际均有使用者；死代码#1称`_empty_tables`为死代码但BUG-01修复后实为活代码（用于数据降级警告）。

### 009 — 406号方案未完成项深入核查与修复
对上一轮核查中标记的3项"未完成"进行深入分析，确认均为误判：
- `_empty_tables`追踪逻辑：方案删除建议基于BUG-01未修复时的判断，修复后此逻辑正常工作，产生数据降级警告（line 949-951），是活代码，无需删除。
- `evaluate()`死参数`dims`/`signals`/`lifecycle`：是8个维度引擎的统一接口签名（`status_engine.py:237-238`已注释），非死参数，无需修改。
- BUG-02 diff描述错误：代码已正确实现，功能无误，属方案文档问题。

**验证新发现并修复**：ruff lint发现`F841`错误——`df_fina`变量在`_compute_valuation()`中赋值但未使用（BUG-01修复遗留：添加了获取但对应的`if df_fina.empty`检查已被删除）。已删除`dim7_valuation_engine.py:723-726`的未使用`df_fina`获取块。验证：F841错误消除（15→14），模块导入正常，相关文件无回归。

### 010 — 维度引擎数据访问路径冲突全面核查与407号方案编制
基于405号方案（dim6路径统一），全面核查dim1-dim5、dim7是否存在类似的双重调用和内部多源数据访问冲突。

**核查结论**：
- **双重调用**：dim1-dim6均存在双重调用问题（旧路径signal_computation_service + 新路径status_engine），dim7仅在新路径中被调用，无此问题
- **内部多源**：dim1是纯分析引擎（不读取数据库），dim2/dim3读取daily_cache（必要的原始计算），dim4读取3张表（daily_cache+moneyflow_cache+daily_basic_cache，资源浪费最严重），dim5读取2张表（daily_cache+daily_basic_cache），dim6读取daily_cache（405号已记录），dim7读取5张财务表（必要的估值计算）

**编制407号修正方案**：
- Phase 1（P0，0.2天）：剔除旧路径dim1-dim6调用，统一走新路径
- Phase 2（P1，1.0天）：dim4/dim5部分计算移到RAW层预计算
- dim2/dim3/dim7保持现状（内部多源是必要的）

**核对发现**：dim1虽然是纯分析引擎（不读取数据库），但仍存在双重调用问题，已在方案中补充说明。

### 011 — 407号方案核对与修订
对407号方案进行核对，发现并修正1处遗漏：dim1虽然是纯分析引擎（不读取数据库），但仍存在双重调用问题，已在方案中补充说明。所有行号已逐一验证无误。
