# 2026-09-19 沟通记录

### 001 — RAW预计算环节全面核查
用户要求核查RAW环节（原料加工）的代码配置，包括：计算正确性、预计算有效性、输出存储正确性、代码是否集中在统一文件中。核查范围严格限定在RAW环节，不包含SIG（现状分析）环节。

**核查方法**：4个并行explore子代理 + 主代理直接代码审查。

**核查结论**：
- 发现25个问题（HIGH 4个、MED 11个、LOW 10个）
- 代码集中度约70%——编排层集中在data_daemon.py，但condition_evaluator.py（4个独立计算器）和chanlun_strategy.py/volume_price_strategy.py（独立calc_macd）存在散落
- HIGH问题：RSI公式非标准（SMA vs Wilder EMA）、MA120/250未预计算、60min K线聚合对齐错误、mootdx tick伪造OHLC
- pre_feat_cache有活跃消费者（StatusEngine → dim1-dim7），不是废弃产出

**修正事项**（用户纠正）：
1. SIG是与RAW平级的独立管道阶段，不是RAW的一部分——报告中SIG相关内容已修正
2. dim2/dim3属于SIG环节，其calc_macd的precomputed版本是SIG的职责，不是RAW
3. condition_evaluator.py的定位需讨论：是策略模板独立执行路径还是SIG管道内触发

**产出**：`002-方案存档/414-RAW预计算环节全面核查报告与整改方案.md`
