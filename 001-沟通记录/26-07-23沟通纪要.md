# 2026-07-23

### 001 - data_daemon数据采集问题核查 + L2选股提示溯源 + 东财HTTP API替代mootdx可行性测试

**用户输入**：核查data_daemon数据采集是否存在问题，以及L2选股相关提示为何出现在采集后台。

**AI执行内容**：
1. **L1/L2/L3日志来源定位**：来自Flask API进程（run.py）的APScheduler，非data_daemon自身
   - 链路：API进程5s定时任务→push_service→前端SocketIO事件→前端触发POST /api/v3/screener/run→run_screener路由打印L1/L2/L3日志
2. **mootdx协议断裂确认**：通达信服务端2026-07-20协议更新，`quotes()`和`bars()`返回空，仅`minute()`可用
   - GitHub Issue #157、#158 同现象
3. **东财HTTP API全面实测**：
   - 稳定性：200次连续请求100%成功率，稳定在0.08-0.10s
   - 效率：4线程并行5.2s可完成全市场5000只快照
   - 字段：80+通用交集字段，含price/volume/amount/turnover/PE/PB等
   - 五档：批量端点不含，个股详情可走新浪API
   - 时效性：与新浪完全一致，腾讯偶有2s延迟
4. **data_daemon其他问题**：mootdx不可用（严重）、分钟数据聚合int+NoneType、申万行业指数少3个、融资融券持续为空等
5. **生产级容灾架构设计**（回应"东财变更如何自行应对"问题）：
   - 适配器接口（DataSourceAdapter）+ 配置驱动字段映射（YAML外部化）
   - 健康评分引擎（自动检测null率/值范围/Schema变更）
   - 自动降级链（东财→新浪→腾讯→缓存→空），动态排序
   - 配置热加载（文件监听1s轮询，变更即时生效）
   - 版本回滚（保留最近3版配置）
   - 覆盖4种运维场景：字段码变更/端点变更/全源断裂/误配置
6. 方案存档：
   - 002-方案存档/289-东财HTTP实时行情替代mootdx可行性评估报告.md
   - 002-方案存档/290-生产级数据源适配层架构设计.md
   - 002-方案存档/291-数据源变更远程诊断与GitHub分发升级方案.md
   - 002-方案存档/292-全局数据体系权威架构说明（2026-07-23版）.md
   - **17 份历史全局数据体系方案已全部标注已过时：** 239/241/242/243/244/245/246/248/249/250/251/254/260/264/266/279/284号

### 002 - 修复预计算2个工程缺陷（FinancialRiskFilter + ChanlunAnalyzer）

**用户输入**：要求修复预计算异常并重新执行。

**AI执行内容**：
1. **FinancialRiskFilter**：chip_pre_filter.py 中 FinancialRiskFilter.check() 内有3处copy-paste残留。另ChipPreFilter.filter_batch()也有相同残留。全部删除。
2. **ChanlunAnalyzer.bi_zs_mode**：chanlun_strategy.py 的 __init__ 在接收 ChanlunConfig 对象时未设置 self.bi_zs_mode。已补上。
3. 验证：50只全通过，两个异常不再抛。

### 003 - daemon崩溃韧性 + 日终同步兜底（4 Task）

**用户输入**：核查昨日15:30日终同步为何未执行、分钟数据为何未回填。

**AI执行内容**：
1. Task1: start_daemon.sh 加 while 看守循环（Login Items崩溃不自动重启）
2. Task2: _check_daily_sync_backfill() 开机检测>15:30未同步则补
3. Task3: _DedupLogFilter 重复WARNING降DEBUG（阻断预计算日志洪流，4GB→正常）
4. Task4: _feed_minute_aggregator() 接入HTTP快照降级路径（东财快照→盘中聚合写到ECM）

### 004 - Tushare全系统限流 + 批量优化 + 292号更新

**用户输入**：Tushare限流根源核查并根治。

**AI执行内容**：
1. 全系统审计：data_daemon.py 25处 + tushare_provider.py 45处全部 _ts() 限速(≤5次/秒)
2. 3个批量优化：adj_factor/top10_holders/stk_holder 从逐只×500改为1次API批量
3. 292号更新：新增附录A（Tushare全量调用审计表+速率控制规范）

### 005 - K线图分钟取数降级链移除（686倍加速）

**用户输入**：个股策略分析K线图换股刷新慢。

**AI执行内容**：
1. 根因：_get_minute_data() 四级降级链每只走完4.8s
2. 修复：移除Tushare/AKShare直调，数据缺失返回空（走sync_requests异步补采）
3. 效果：60分钟 4.8s→0.007s，5分钟 4.5s→0.000s

### 006 - 降级自动恢复修复

**用户输入**：核查东财HTTP为何失败导致降级。

**AI执行内容**：
1. 东财失败原因：服务端空回复（Azure HTTP 0，非配置问题）
2. 修复：_SnapshotSourceManager 新增 RECOVERY_PROBE_INTERVAL=10，每10次(~50s)自动探测东财，恢复即切回
3. Daemon重启（PID 30778）

### 007 - 项目进度记录更新

**用户输入**：切换对话，开启选股系统调优。

**AI执行内容**：
1. 更新 _工作上下文.md 为会话完整快照
2. 更新 沟通记录 (26-07-23沟通纪要.md)
3. 标记下次会话主题：选股系统有效性评估细化（N1）+ 287号方案运行时验证（V1）

### 008 - 选股系统全面审查分析（11项问题 + 合规审计）

### 009 - 机会图谱系统架构完整设计方案（范式重构）

**用户输入**：认同"机会图谱"范式，要求深入 LLM Wiki 知识库做深度研究，对架构进行全面细化。

**AI执行内容**：
1. 检索 LLM Wiki 知识库，调取 三层架构选股、BMP选股框架、思辨的力量-价值投资、主力行为分析四阶段、筹码分布、UIDF统一决策框架、主动投资组合管理等核心知识
2. 深度挖掘同类优质项目理念：Greenblatt神奇公式、Howard Marks周期理论、Morningstar估值框架、桥水投资原则、Carbon Collective等
3. 输出完整六层机会图谱架构（L1数据底座→L2多维标签→L3四并行管线→L3+用户自定义层→L4个股诊断+交叉验证→L5全景总览）
4. 方案存档：002-方案存档/294-机会图谱系统架构设计（完整方案）.md

### 010 - 策略体系深度核查 + 机会图谱架构修订第2版

**用户输入**：澄清系统的实际策略构成——DarwinRisk/Chip(主力)/Chanlun/VolumePrice 四大基础引擎 + Factor组合(关联因子管理模块) + Vibe策略(关联策略模板模块)，其中 Factor 和 Vibe 是用户自定义的多策略组合形式，而非独立策略。

**AI执行内容**：
1. 全面核查并正确梳理系统实际策略体系：
   - **5个基础引擎**：DarwinRisk(标签化) / Chip/MainForce(主力) / Chanlun / VolumePrice
   - **2个扩展框架**：Factor组合(FactorRegistry多组合并行) + Vibe策略(StrategyTemplateV2多策略并行)
   - 其他策略模块（BOCIASI/CrowdingFactor/SectorRotation等）的正确归属：归入基础引擎内部实现 或 注册为FactorRegistry因子
2. Factor和Vibe的本质：**不是管线组成部分，而是跨管线的用户自定义增强层**
3. 修订294号方案形成第2版，核心变更：
   - 架构图新增 **L3+ 用户自定义层**
   - 每条管线新增 **Factor/Vibe 叠加公式**（权重：引擎0.6+Factor0.25+Vibe0.15）
   - 新建 **交叉验证仲裁层**（8条仲裁规则，整合EagleSwordResonance/ConflictArbiter）
   - 新建 **Vibe 分级管理**（sandbox/trial/trusted 三档+灰度上線流程）
   - **标的库评分分层**（基础引擎分不变+Factor/Vibe加成可变）
   - FactorRegistry 补充**8个估值/质量因子**清单
   - 策略模块**保留/废弃分类**表
   - 更新实施路线图优先级
4. 验证：14个章节全部正确，4条管线叠加公式全部到位，933行→1383行

**方案存档**：002-方案存档/294-机会图谱系统架构设计（完整方案）.md（第2版修订）

**用户输入**：要求全面审查选股系统运行机制、架构逻辑合理性，对存在的问题进行系统性解析说明。

**AI执行内容**：
1. 通读全部选股相关模块：screener.py / chip_strategy.py / screener_strategy_integration.py / routes/screener.py / data_daemon.py (预计算部分) / unified_core.py
2. 审查前端原型 screener.html 与后端 API 的对应关系
3. 输出11项问题分析（P1-P10，其中P1含两个子问题），覆盖：重复计算、数据加载效率、sync_requests断裂、废弃代码、评分体系混乱、Vibe落地浅、降级脆弱、监控缺失、预计算断连
4. 合规审计：红线规则全部通过，sync_requests daemon消费端待落地
5. 方案存档：002-方案存档/293-选股系统全面审查分析报告.md

### 011 - 机会图谱核心目标定位 + Treemap 原型 + 294号方案第3版修订

**用户输入**：讨论机会图谱核心目标——是否需要对每一只股票展示机会信息？如何确定展示范围？Treemap 矩形树图可视化方案的可行性评估。

**AI执行内容**：
1. **核心定位论证**：机会图谱不是"展示所有机会"，而是"引导用户发现关键机会"。本质是一张地图，地图的核心能力是**缩放和导航**，而不是把全世界的每一条街道都列出来。
2. **四层级信息架构**：
   - 层级一：市场总览 — Treemap 聚合，一眼看全局
   - 层级二：标的库看板 — 用户的"家"，关注变化而非重新决策
   - 层级三：管线发现卡 — 每日新机会窗口，每管线3-5只有限推荐
   - 层级四：个股搜索 — 用户主动查询，5000只全量可查
3. **Treemap 可行性 5 维度评估**：配置可行(ECharts)、数据可行(盘中东财HTTP+盘前机会信号)、全市场可行、小盘股问题用"机会评分决定方块大小"解决、盘中30s轮询
4. **生成交互原型**：_ui-prototype/opportunity-treemap.html（三种模式切换+悬停浮窗+点击弹窗+150只模拟数据）
5. **修订 294号方案第3版**：新增原则五、§四A四层级模型、D12/D13决策记录，更新范式描述

**方案存档**：002-方案存档/294-机会图谱系统架构设计（完整方案）.md（第3版修订）

### 012 - 机会图谱深度研讨：估值体系、长短线框架、标签架构、Treemap交互定版（第5版修订）

**用户输入**：讨论盘前时间线迁移、四条管线200只上限歧义、估值体系与长短线框架的深化、Treemap三模式最终定义、机会标签架构设计。达成以下共识。

**用户输入**：讨论盘前时间线迁移、四条管线200只上限歧义、估值体系与长短线框架的深化、Treemap三模式最终定义、机会标签架构设计。达成以下共识。

**AI执行内容**：
1. **时间线修正**：L2/L3/L5 从"次日盘前"改为"前一日盘后17:00-18:00"执行，开机即可用
2. **管线200只上限澄清**：评分是全市场4800只无上限，候选池是质量门槛（>=6分）的自然结果而非排序截断
3. **估值体系深化**：从单一PE分位法改为"行业感知多层估值锚"
4. **ROCE+BOCIASI重新定位**：ROCE为价值型管线固定准入条件，BOCIASI为短线环境过滤器
5. **标签引擎架构确立**：四条管线不再作为内部计算架构，引擎统一产出标签→标签池→Treemap前端标签筛选
6. **标签体系设计**：四大类标签（方向/位置/质量/环境），约30个具体标签
7. **Treemap三模式最终定版**（行业分组）：
   - 市场地图：大小=市值(行业内)，颜色=涨跌幅
   - 机会地图：大小=机会评分(行业内)，颜色=持有周期（蓝=长线、绿=中线、橙=短线、红=超短、灰=无信号）
   - 价值地图：大小=前收盘价(行业内)，颜色=估值偏离度（绿=低估、灰=合理、红=高估）
8. **Treemap交互三能力确认**：
   - 能力一（简化）：保持三模式切换，不做更多着色选择
   - 能力二（悬停）：四行标签摘要浮窗（方向/位置/质量/环境）
   - 能力三（强化）：多标签AND筛选栏 + 预设场景按钮
9. **预设场景**：原四条管线转化为四组标签组合快捷入口（取代管线推荐列表）
10. **修订294号方案第5版**：经过5轮迭代，约1600行，14个章节

**方案存档**：002-方案存档/294-机会图谱系统架构设计（完整方案）.md（第5版修订）

### 013 - 294号方案拆解：实施总纲 + 7份子方案

**用户输入**：认为方案太大影响实施，要求拆分为实施总纲和各环节细化方案。

**AI执行内容**：
1. 创建新 294 号方案（实施计划总纲）：替代原有完整方案，仅保留架构总览图 + 阶段任务矩阵 + 依赖关系 + 分工建议 + 验收标准
2. 将原方案按功能模块拆分为 7 份子方案：
   - 295：标签引擎与统一标签体系规范（标签格式、全部标签清单、存储查询）
   - 296：Treemap 前端交互与三种地图模式设计（交互原型、API）
   - 297：估值引擎与财务质量评分器设计（估值方法链、财务评分）
   - 298：统一阶段判定引擎设计（五源融合、阶段→标签映射）
   - 299：标的库与操作建议系统设计（分级管理、数据库、通知）
   - 300：事件监控器与 L4 交叉验证仲裁层设计（事件源、仲裁规则）
   - 301：基础设施改造清单（前置过滤、Factor/Vibe分级、现有引擎标签化、废弃模块）
3. 每份子方案独立编号，约 50-100 行，专注可执行细节

### 014 - 全天汇总：机会图谱全量设计完成 + 方案归档

**用户输入**：全天深入讨论，涵盖标签体系审查、策略引擎缺口评估（12个缺口、302号方案）、Treemap交互定版（三种悬停浮窗+统一弹窗+标签筛选）、项目记录。

**AI执行内容**：
1. 完成 29 个标签的全面核查与 12 个引擎缺口评估
2. 新建 302 号方案（策略引擎改造，8项实施细节）
3. 三种悬停浮窗按模式区分（市场=交易数据/机会=信号强度+四行标签/价值=估值证据链）
4. 统一点击弹窗设计（五区块+三操作按钮）
5. 新增 signal_strength 衍生标签（非预测指标）
6. 用户语言统一：标的库→自选（更新295/299/294）
7. 更新 _工作上下文.md 为全量快照
8. 沟通索引同步更新

**方案总览**：294-302 共 9 份方案，涵盖从架构设计到实施计划的完整链路
