# 2026-07-23

### 001 - data_daemon数据采集问题核查 + L2选股提示溯源 + 东财HTTP API替代mootdx可行性测试

**用户输入**：核查data_daemon数据采集是否存在问题，以及L2选股相关提示为何出现在采集后台。

**AI执行内容**：
1. **L1/L2/L3日志来源定位**：来自Flask API进程（run.py）的APScheduler，非data_daemon自身
   - 链路：API进程5s定时任务→push_service→前端SocketIO事件→前端触发POST /api/v3/screener/run→run_screener路由打印L1/L2/L3日志
2. **mootdx协议断裂确认**：通达信服务端2026-07-20协议更新，`quotes()`和`bars()`返回空，仅`minute()`可用
   - GitHub Issue #157、#158 同现象
3. **东财HTTP API全面实测**：
   - 稳定性：200次连续请求100%成功率，稳定在0.08-0.10s
   - 效率：4线程并行5.2s可完成全市场5000只快照
   - 字段：80+通用交集字段，含price/volume/amount/turnover/PE/PB等
   - 五档：批量端点不含，个股详情可走新浪API
   - 时效性：与新浪完全一致，腾讯偶有2s延迟
4. **data_daemon其他问题**：mootdx不可用（严重）、分钟数据聚合int+NoneType、申万行业指数少3个、融资融券持续为空等
5. **生产级容灾架构设计**（回应"东财变更如何自行应对"问题）：
   - 适配器接口（DataSourceAdapter）+ 配置驱动字段映射（YAML外部化）
   - 健康评分引擎（自动检测null率/值范围/Schema变更）
   - 自动降级链（东财→新浪→腾讯→缓存→空），动态排序
   - 配置热加载（文件监听1s轮询，变更即时生效）
   - 版本回滚（保留最近3版配置）
   - 覆盖4种运维场景：字段码变更/端点变更/全源断裂/误配置
6. 方案存档：
   - 002-方案存档/289-东财HTTP实时行情替代mootdx可行性评估报告.md
   - 002-方案存档/290-生产级数据源适配层架构设计.md
   - 002-方案存档/291-数据源变更远程诊断与GitHub分发升级方案.md
   - 002-方案存档/292-全局数据体系权威架构说明（2026-07-23版）.md
   - **17 份历史全局数据体系方案已全部标注已过时：** 239/241/242/243/244/245/246/248/249/250/251/254/260/264/266/279/284号

### 002 - 修复预计算2个工程缺陷（FinancialRiskFilter + ChanlunAnalyzer）

**用户输入**：要求修复预计算异常并重新执行。

**AI执行内容**：
1. **FinancialRiskFilter**：chip_pre_filter.py 中 FinancialRiskFilter.check() 内有3处copy-paste残留。另ChipPreFilter.filter_batch()也有相同残留。全部删除。
2. **ChanlunAnalyzer.bi_zs_mode**：chanlun_strategy.py 的 __init__ 在接收 ChanlunConfig 对象时未设置 self.bi_zs_mode。已补上。
3. 验证：50只全通过，两个异常不再抛。

### 003 - daemon崩溃韧性 + 日终同步兜底（4 Task）

**用户输入**：核查昨日15:30日终同步为何未执行、分钟数据为何未回填。

**AI执行内容**：
1. Task1: start_daemon.sh 加 while 看守循环（Login Items崩溃不自动重启）
2. Task2: _check_daily_sync_backfill() 开机检测>15:30未同步则补
3. Task3: _DedupLogFilter 重复WARNING降DEBUG（阻断预计算日志洪流，4GB→正常）
4. Task4: _feed_minute_aggregator() 接入HTTP快照降级路径（东财快照→盘中聚合写到ECM）

### 004 - Tushare全系统限流 + 批量优化 + 292号更新

**用户输入**：Tushare限流根源核查并根治。

**AI执行内容**：
1. 全系统审计：data_daemon.py 25处 + tushare_provider.py 45处全部 _ts() 限速(≤5次/秒)
2. 3个批量优化：adj_factor/top10_holders/stk_holder 从逐只×500改为1次API批量
3. 292号更新：新增附录A（Tushare全量调用审计表+速率控制规范）

### 005 - K线图分钟取数降级链移除（686倍加速）

**用户输入**：个股策略分析K线图换股刷新慢。

**AI执行内容**：
1. 根因：_get_minute_data() 四级降级链每只走完4.8s
2. 修复：移除Tushare/AKShare直调，数据缺失返回空（走sync_requests异步补采）
3. 效果：60分钟 4.8s→0.007s，5分钟 4.5s→0.000s

### 006 - 降级自动恢复修复

**用户输入**：核查东财HTTP为何失败导致降级。

**AI执行内容**：
1. 东财失败原因：服务端空回复（Azure HTTP 0，非配置问题）
2. 修复：_SnapshotSourceManager 新增 RECOVERY_PROBE_INTERVAL=10，每10次(~50s)自动探测东财，恢复即切回
3. Daemon重启（PID 30778）

### 007 - 项目进度记录更新

**用户输入**：切换对话，开启选股系统调优。

**AI执行内容**：
1. 更新 _工作上下文.md 为会话完整快照
2. 更新 沟通记录 (26-07-23沟通纪要.md)
3. 标记下次会话主题：选股系统有效性评估细化（N1）+ 287号方案运行时验证（V1）
