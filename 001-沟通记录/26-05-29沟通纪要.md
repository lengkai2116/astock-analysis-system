# 2026-05-29

### 006 - 筹码策略Phase 1收尾与Phase 2实施

**主题**：筹码分布策略 Phase 1 数据层收尾 + Phase 2 策略核心逻辑构建

**关键词**：筹码策略、Phase1收尾、Phase2信号增强、SSRP穿越、RSI背离、K线形态、ConfirmLayer

**文档位置**：本文件（26-05-29沟通纪要.md）

**关键结论**：

Phase 1 收尾（3项修改已完成）：
1. **资金流向缓存层** — `enhanced_cache_manager.py` 新增 `moneyflow_cache` 表（大单净额字段），配套 `cache_moneyflow_data()` / `get_cached_moneyflow()` 方法
2. **DataManager资金流向接口** — 新增 `sync_moneyflow_data()` / `get_cached_moneyflow()`
3. **换手率接入策略链** — `signal_computation_service.py` 中 `_compute_chip_signal()` 从 daily_basic 获取换手率，传入 `_build_chip_evidence()` 展示

Phase 2 完整实施（7个子任务全部完成）：
- **2A.1 SSRP穿越信号** — `_check_s_buy()` 增加收盘价>SSRP条件；`_check_s_sell()` 增加连续2日<SSRP卖出
- **2A.2 RSI顶底背离** — `chip_indicators.py` 新增 `detect_rsi_divergence()`，支持顶/底背离+二次背离检测
- **2A.3 S_DIVERG_SELL** — 新增背离减仓信号，初次减仓30%，二次减仓70%
- **2B.1 KLinePatternVerifier** — 新建模块，6种K线形态识别（大阳线/十字星/跳空高开/跳空低开/冲高回落/高开低走大阴线）
- **2B.2 形态→置信度映射** — 各形态对BUY/SELL信号的置信度加成表
- **2C.1 ConfirmLayer** — 新建模块，含换手率验证+转移速度验证+假突破过滤器
- **2C.2 假突破过滤器** — 量能(1.5倍)+时间(3日未回落)+深度(CYQKL>=20%)三维确认

**决策**：
- Phase 2 的顺序: RSI背离→SSRP穿越→S_DIVERG_SELL→K线形态→ConfirmLayer，从前置依赖往后推进
- S_DIVERG_SELL 纳入优先级链：S_WASH_STOP(1) > S_SELL(2) > S_DIVERG_SELL(3) > S_BUY(4) > S_WASH_END(5) > S_BOUNCE(6) > HOLD(99)
- ConfirmLayer 中 QMT 五档盘口验证标注为可选增强项，不影响主逻辑

**待办事项（Phase 3 启动）**：
- 3.1 ChipPositionManager — 5阶段仓位状态机
- 3.2 ChipRiskExecutor 止损（硬止损8%/跟踪止损回撤15%/时间止损20交易日）
- 3.3 ChipRiskExecutor 止盈（筹码止盈/RSI止盈/放量滞涨止盈/移动止盈）

**涉及文件**：
- 修改: `chip_indicators.py`, `chip_strategy_impl.py`
- 新建: `kline_pattern.py`, `confirm_layer.py`
- 前置已完成: `enhanced_cache_manager.py`, `data/__init__.py`, `signal_computation_service.py`
