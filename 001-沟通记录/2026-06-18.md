# 2026-06-18 沟通纪要

### 001 — 个股策略分析页面UI调整

**用户输入：** 按5项任务清单对个股策略分析页面进行逐一处理和核实：
1. 删除大盘指数行（上证/深证/创业/北证/科创）
2. 删除叠加说明文字（"点击切换叠加·蓝底=已叠加"）+ 核实叠加按钮配置
3. 核实页面Tab（行情分析/个股资料/相关资讯/图文F10）功能对接情况
4. 核实周期按钮配置 + 竞品研究（同花顺/招商证券/通达信）
5. 核实右侧按钮（前复权/不复权/F10/AI判读/回测验证）功能规划

**用户第二轮指令：** 
1. 页面Tab全部无功能 → 删除
2. 周期/叠加按钮太难看 → 结合竞品重新设计
3. 周期添加F8循环快捷键 + 新增15分周期 + 排序调整
4. 前复权/不复权/F10/AI判读/回测验证全部删除（AI和回测后续在策略板块规划）

**AI执行内容：**

| # | 修改项 | 详情 |
|:--|:-------|:------|
| ① | 删除大盘指数行 | 移除 `index-bar` HTML区块（上证/深证/创业/北证/科创）+ 清理CSS无引用样式（`index-bar`/`index-item`/`index-name`/`index-val`）|
| ② | 删除页面Tab | 移除 `page-tabs` 区块（行情分析/个股资料/相关资讯/图文F10）|
| ③ | 删除叠加说明文字 | 移除 `<span class="ov-label">点击切换叠加 · 蓝底=已叠加</span>` |
| ④ | 删除右侧无用按钮 | 移除 `adj-btn`（前复权/不复权）和 `toolbar-right` 区块（F10/AI判读/回测验证）|
| ⑤ | 周期按钮美化 | 旧: 12px/padding 8px 40px/`bg-surface`透明底/ `border-default` 不可见边框 → **新: 13px/padding 5px 14px/`rgba(255,255,255,0.04)` 浅底/`rgba(255,255,255,0.12)` 半透明可见边框/6px圆角/hover加强至 `rgba(255,255,255,0.08)`背景 |
| ⑥ | 叠加按钮美化 | 旧: 10px/padding 4px 24px/`bg-muted`透明底。→ **新: 12px/padding 4px 14px/`rgba(255,255,255,0.04)` 浅底/`rgba(255,255,255,0.08)` 薄边框/4px圆角/hover可见变化 |
| ⑦ | 周期排序+补充 | 旧顺序: 分时/5日/日线/周线/月线/60分(10px)/30分(10px) → **新顺序: 分时/5日/日线/周线/月线/60分/30分/15分**（去掉inline小字体、新增15分、全局统一13px）|
| ⑧ | F8快捷键 | 新增 `keydown` 监听，F8触发 `.period-tab` 循环切换激活态（日线→周线→月线→60分→30分→15分→分时→5日→日线）|
| ⑨ | gap统一 | `period-tabs` gap: 8px (`var(--space-2)`) → 6px，`overlay-tabs` gap: 16px (`var(--space-4)`) → 6px |

**浏览器验证结果：**

| 检查项 | 结果 |
|:-------|:-----|
| 页面Tab 4个全部移除 | ✅ 不存在于DOM |
| 大盘指数行移除 | ✅ 不存在于DOM |
| 叠加说明文字移除 | ✅ 不存在于DOM |
| 右侧前复权/不复权/F10/AI判读/回测验证移除 | ✅ 不存在于DOM |
| 周期按钮CSS: 13px/5px14px/浅底半透明边框 | ✅ `font-size:13px padding:5px 14px border:rgba(255,255,255,0.12)` |
| 周期按钮激活态: 蓝底白字 | ✅ |
| 叠加按钮CSS: 12px/4px14px/浅底薄边框 | ✅ `font-size:12px padding:4px 14px border:rgba(255,255,255,0.08)` |
| 叠加按钮hover: 背景加强 | ✅ `rgba(255,255,255,0.09)` |
| 周期顺序: 8个(含15分) | ✅ 分时/5日/日线/周线/月线/60分/30分/15分 |
| F8循环: 日线→周线 | ✅ |
| F8循环: 完整一圈回到日线 | ✅ |
| `btn-sm` CSS 仅用于下方策略卡片(非误删) | ✅ 保留2个 `.btn-sm` 在策略解读栏 |

**涉及文件：** `_ui-prototype/indicator-ide.html`（CSS + HTML 约20处修改）

**后续规划：**
- 166号方案需同步更新：§6.2.1 顶部栏布局、§6.5 待实施清单
- AI判读/回测验证 功能将在后续于个股策略分析页面下方策略板块统一规划

### 002 — 侧栏导航折叠图标间距不一致修复

**用户反馈：** 个股策略分析页面左侧导航收起时，图标间距与其他页面存在差异。

**根因：** indicator-ide.html 内嵌了完整的侧栏折叠 CSS 副本（`/* 侧栏折叠 */` 块），覆盖了 `base.css` 的共用样式。具体差异：
- `padding: var(--space-3) 0` — base.css 为 `var(--space-8) 0`，折叠时图标间距过小
- 缺少 `.sidebar.collapsed .sidebar-item .icon { margin:0 auto }` — 图标不居中
- 缺少 `.sidebar.collapsed .sidebar-item .badge-new { display:none }` — NEW 标签在折叠时可见

**修复：** 删除 indicator-ide.html 中 `/* 侧栏折叠 */` ~ `pointer-events:none; }` 共18行（原44-61行），使其完全继承 base.css 的共用样式。

**浏览器验证：**
| 对比项 | indicator-ide | dashboard |
|:-------|:-------------|:----------|
| sidebar width (collapsed) | 64px | 64px |
| .icon x position | 22.5px | 22.5px |
| .icon width | 18px | 18px |
| .sidebar-item justify-content | center | center |

**涉及文件：** `_ui-prototype/indicator-ide.html`（删除18行CSS）

### 004 — 个股策略页面三问题修复（3列排布+浮动标记+缩放响应）

**用户输入：** 用户提供四张截图（图1=我们的原型，图2=招商证券，图3/4=缩放异常），指出三个差异问题：
1. 基础数据信息应参照招商证券按三列排布，减少上下宽度；chart-section容器应控制在主图+两个副图的合理范围；右侧信息板块上下边界应与chart-section对齐
2. 第一副图位置出现标记"24.9% 主力流入"的圆框，需核实问题
3. 浏览器放大或缩小时整体页面变动异常，需刷新才能恢复布局

**AI执行内容：**

| # | 问题 | 根因 | 修复方案 |
|:--|:-----|:------|:---------|
| ① | 2列排布占用过多垂直空间，右侧面板与chart-section上下不对齐 | `.zszq-grid` 使用 `grid-template-columns:1fr 1fr`（2列14行） | 改为 **3列排布**（`1fr 1fr 1fr` → 3列10行），字段按"基础行情/成交市值/估值股本"三组逻辑分组，字体缩小至12px |
| ② | 第一副图（成交量区域）出现"24.9% 主力流入"浮动圆框 | 可能为ECharts多grid渲染时canvas缩放/重绘导致的历史图形残留，或副图header定位异常 | 添加 `.chart-wrap` / `#sub-chart-area` **overflow:hidden** 裁剪溢出内容；添加 **window resize事件监听**（150ms debounce），浏览器缩放时自动调用 `chart.resize()` 重绘canvas，消除残留图形 |
| ③ | 浏览器缩放时右侧面板超出/左侧图表截断 | `.main-area` 使用固定 `340px` 宽度，缩放时不自动缩放 | 改为 `minmax(260px,340px)` 弹性宽度；添加 `.side-panel { min-width:260px }` 防过度收缩；`overflow-x:hidden` 防止水平溢出 |

**涉及文件：** `_ui-prototype/indicator-ide.html`
- CSS修改：`.zszq-grid` 3列、`.main-area` minmax、`.chart-wrap`/`#sub-chart-area` overflow、`.side-panel` min-width、`.zszq-item` 字体12px
- HTML修改：28个字段重排为3列分组
- JS修改：新增 window resize 监听 + debounce chart.resize()

**文件位置：** 2026-06-18.md §004
**状态：** ✅ 已完成

---

### 005 — MA动态值+图例删除+布局嵌套修复+文档更新

**用户输入：**
1. 询问主图上端"一类买/二类买/三类买/■ 中枢"是否有数据连接 — 确认全部为硬编码静态文字，无对应数据
2. 要求：删除一类买/二类买/三类买/中枢；MA数值改为随十字光标变动显示对应日期数据，无光标时显示K线图最后数据
3. 刷新后页面结构出现重大问题（主图/副图未显示）— 修复chart-header缺少`</div>`闭合导致的chart-wrap宽度为0
4. 右侧栏变动到下面的布局问题 — 修复HTML嵌套：side-panel被吞入chart-flex
5. 盘口footer"笼子：±2% 18.59~19.15"与"连续竞价：买入≤102.51万"排版问题 — `--space-6`未定义，改为显式值
6. 要求更新166号171号文档并上传GitHub备份

**AI执行内容：**

| # | 修改项 | 详情 |
|:--|:-------|:------|
| ① | 删除legend-bar | 移除一类买/二类买/三类买/中枢4项（无数据支撑） |
| ② | MA动态值 | MA5/10/20/60改为`<b id="ma5-val">`标签，新增`updateMAValues()`函数，在`updateCrosshair()`中调用（光标移动时更新，离开时恢复最后K线）|
| ③ | chart-header闭合修复 | 追加缺失`</div>`，chart-wrap脱离flex容器，K线宽度从0px恢复722px |
| ④ | HTML嵌套重构 | chip-panel提至chart-flex内（而非chart-section内），side-panel恢复为main-area直接子元素 |
| ⑤ | order-footer间距 | `gap:var(--space-6)` → `column-gap:28px; row-gap:4px; flex-wrap:wrap` |
| ⑥ | 文档更新 | 166.md新增§8.12实施记录；171.md新增更新条目 |

**浏览器验证：**

| 检查项 | 结果 |
|-------|------|
| MA5 初始值（动态） | 18.32（非硬编码10.21） |
| 一类买等已删除 | ✅ DOM不存在 |
| K线图宽度 | 722px |
| main-area子元素 | chart-flex/side-panel/sub-below |
| chart-section底部=side-scroll底部 | 943.1875/943.1875 (diff=0) |
| side-scroll max-height | 834px |
| 控制台错误 | 无 |

**涉及文件：** `_ui-prototype/indicator-ide.html`、`002-方案存档/166-前端UI原型逐页调整记录.md`、`002-方案存档/171-功能视图API映射对照表.md`

**状态：** ✅ 已完成

### 003 — 个股策略分析右侧面板招商证券样式精确调整（178号方案）

**输入：** 用户提供招商证券MAC金融终端截图，要求对比个股策略分析右侧面板差异

**关键结论：**
- 通过截图对比发现6项结构性差异（布局/盘口列数/Tab导航/资金流向标注/5日流向/子订单）
- 后端全部28个字段可从现有Tushare/AkShare获取
- 需新增4个后端API路由（待实施）

**执行：**
- 产出 `178-股票分析右侧面板招商证券样式调整方案.md`
- 修订 `166-前端UI原型逐页调整记录.md` — §6.2.4/§6.5/§8.7更新
- 修订 `171-功能视图API映射对照表.md` — §2.2新增「待建设」API表（4个端点）
- 修改 `_ui-prototype/indicator-ide.html` 原型右侧面板：2列网格28字段 + 竞价标签 + 盘口5列(笼子价/连续竞价/分隔线) + Tab导航 + 资金流向(子订单+5柱)

### 006 — chart-title + top-bar 动态化处理

**用户输入：** 核实主图左上端"000762.SZ 西藏矿业 · K线 + 缠论"是否有对应接口，并要求动态化，删除" + 缠论"；同时核实整体左上角的股票代码input、选择框中的西藏矿业、12.34最新价、-2.10%涨跌幅是否有数据接口。

**核实结论：**
| 元素 | 是否有后端接口 | 说明 |
|:-----|:--------------|:------|
| chart-title（"000762.SZ 西藏矿业 · K线"）| ✅ `GET /api/v3/chart/kline/<ts_code>` 返回值 `data.stock.ts_code` + `data.stock.name` | 硬编码文字改为从API动态拼接 |
| 顶部栏 `000762.SZ` 股票代码 | ✅ chart.py 含 `get_stock_info(ts_code)` 返回 name/ts_code | input value已改为动态 |
| 顶部栏 `西藏矿业` 选择框 | ✅ 同上 + `GET /api/v3/chart/stock-list` 搜索接口 | select已改为动态追加 |
| 顶部栏 `12.34` 最新价 | ✅ K线最后一条的 `close` | response新增 `data.latest.close` |
| 顶部栏 `-2.10%` 涨跌幅 | ❌ 原缺失 | kline追加 `pct_chg` + 新增 `data.latest.pct_chg` |

**AI执行内容：**
| # | 修改 | 详情 |
|:--|:-----|:------|
| ① | chart.py: kline追加 `pct_chg` | kline_data每个item增加 `pct_chg` 字段（chart.py:181） |
| ② | chart.py: response新增 `latest` | 返回 `latest: {close, pct_chg}` 供顶部栏展示（chart.py:320-333） |
| ③ | HTML: top-bar元素加id | stock-code / stock-name / latest-price / latest-pct（.html:177-181） |
| ④ | HTML: chart-title去"+缠论" + 加id | chart-title id + 删除" + 缠论"（.html:211） |
| ⑤ | 新增JS: fetchStockData() | 调用API更新所有头部字段，API失败静默降级到Mock（.html:893-940） |
| ⑥ | 新增JS: updateTopBarPrice() | 根据涨跌切换红绿颜色 |
| ⑦ | 页面init追加API调用 | `fetchStockData('000762.SZ')`（.html:967） |

**涉及文件：** `_ui-prototype/indicator-ide.html`、`backend/app/routes/chart.py`
**文档同步：** 166.md §8.13 新增、171.md §2.2 更新条目

### 007 — 股票搜索自动补全 + 选择切换联动（原型实施）

**用户输入：** 核实顶部栏股票代码输入框是否支持输入`0007`搜索所有0007开头的股票、通过下拉选择精准定位、选择后带动图表及对应数据变化。

**核实结论：** ❌ 不支持。输入框为纯静态`<input value="000762.SZ">`无任何事件监听；下拉写死2个option；`switchStock()`未实现。

**执行内容：**

| # | 修改项 | 详情 |
|:--|:-------|:------|
| ① | CSS新增搜索下拉 | `.stock-search-wrap` 定位容器 + `#search-dropdown` 弹出层 + `.search-item`(代码/名称/行业三栏) + loading/empty状态 |
| ② | HTML结构调整 | 移除`<select>`，改为`<div class="stock-search-wrap">`包裹 `<input>` + `<div id="search-dropdown">` |
| ③ | JS: `searchStocks(query)` | 200ms防抖 → `GET /api/v3/chart/stock-list?keyword=xxx&limit=15` → 渲染下拉 |
| ④ | JS: `handleStockSelect(tsCode)` | 去重判断 → `switchStock()` |
| ⑤ | JS: `switchStock(tsCode)` | 更新输入框 → API刷新chart-title/最新价/涨跌幅 → 右侧面板代码更新 → console输出完整刷新待办 |
| ⑥ | JS: `initStockSearch()` | 绑定 input(200ms防抖)/focus(展开)/click关闭/Escape关闭/Enter确认 |
| ⑦ | 页面init替换 | `fetchStockData('000762.SZ')` → `initStockSearch()` |

**完整联动刷新待建设项（switchStock仅更新了头部栏）：**
- K线图数据切换（需K线API+前端echart重绘）
- 右侧面板28字段刷新（需quote/moneyflow/orderbook/stk-limit 4个路由）
- 筹码分布刷新（需K线API数据重算）
- 资金流向5柱刷新（需moneyflow路由）
- 盘口信息刷新（需orderbook路由）
- 缠论/量价策略卡片刷新（需策略引擎对接）

**涉及文件：** `_ui-prototype/indicator-ide.html`（CSS 12行 + HTML结构调整 + JS 55行）
**文档同步：** 166.md §8.14 新增、171.md §2.2.1 新增（接口依赖全景 + 伪代码 + 后端改造清单）

### 008 — 股票选择重构为代码输入框+自选股选择+自选状态标记

**用户输入：** 在讨论方案冲突后，确定了最终方案：
1. 代码输入框改为 `代码+空格+名称` 形式展示当前股票
2. 名称下拉去掉，改为"自选股选择"按钮，点击展开纯自选列表浮层（固定大小+滚动条，只显示股票名称）→ 选中后填写代码输入框并切换K线
3. 新增自选状态标记（✅ 自选 / ＋ 非自选）
4. 非自选时点击状态标记弹出添加确认弹窗（×关闭/取消不添加/确认添加调API）
5. **核心原则**：两个控件各自独立触发，不互相同步；代码输入框为唯一数据源

**AI执行内容：**

| # | 修改项 | 详情 |
|:--|:-------|:------|
| ① | CSS: `.stock-code-input` 改为220px | 容纳"000762.SZ  西藏矿业"长度 |
| ② | CSS: 删除`.stock-name-select` | 旧的选择框已移除 |
| ③ | CSS: 新增`watchlist-trigger` 自选按钮样式 | width:110px / font-size:12px / cursor:pointer / position:relative |
| ④ | CSS: 新增`#watchlist-dropdown` 自选浮层样式 | width:160px / max-height:240px / overflow-y:auto / box-shadow |
| ⑤ | CSS: 新增`.stock-status` 自选状态标记 | 绿色底=自选(不可点击)、黄色底=非自选(可点击打开弹窗) |
| ⑥ | CSS: 新增`.modal-overlay/.modal-box` 添加确认弹窗 | fixed居中 / 半透明遮罩 / ×关闭按钮 / 确认+取消按钮 |
| ⑦ | HTML: stock-select区域替换 | 删除`<select>`，新增`<input readonly>`触发按钮 + `<span>`状态标记 + 自选浮层`<div>` |
| ⑧ | HTML: 新增模态弹窗 | `</body>`前添加`<div id="add-modal">` |
| ⑨ | JS: `updateCodeInput(tsCode, name)` | 代码输入框显示"代码  +  名称"格式 |
| ⑩ | JS: `selectStockByCode(tsCode, name)` | 选中后更新输入框+状态标记+触发switchStock（旧版两框同步逻辑全部删除） |
| ⑪ | JS: `loadWatchlistToDropdown(showAfter)` | 后台预加载自选缓存（保持隐藏），点击时仅`showAfter=true`才显示浮层 |
| ⑫ | JS: `renderWatchlistDropdown()` | 从`_watchlistCache`渲染自选浮层（不再依赖`<select>`） |
| ⑬ | JS: `selectFromWatchlist(tsCode, name)` | 自选浮层选中 → 更新代码输入框 + switchStock |
| ⑭ | JS: `updateStockStatus(tsCode)` | 检查`_watchlistCache`判断当前股票是否在自选列表中 |
| ⑮ | JS: `showAddModal()` / `closeAddModal()` / `confirmAddWatchlist()` | 添加确认弹窗控制 + `POST /api/v3/phase3/watchlist`调用 |
| ⑯ | JS: `initStockSearch()` 绑定事件 | 代码输入(防抖搜索/focus/Escape/Enter) + 自选按钮点击切换 + 外部点击关闭两个浮层 |
| ⑰ | CSS路径修复 | `assets/base.css` → `/assets/base.css`（避免`/indicator-ide/`路径下CSS加载失败） |

**浏览器验证结果：**

| 检查项 | 结果 |
|:-------|:------|
| 输入框显示 "000762.SZ  西藏矿业" | ✅ width:220px / dark bg |
| 自选按钮显示 "自选股选择" | ✅ width:110px / cursor:pointer |
| 初始自选状态 | ✅ "✅ 自选"（绿色底） |
| 自选浮层初始隐藏 | ✅ display:none |
| 点击自选按钮展开浮层 | ✅ 显示"西藏矿业""平安银行"（mock） |
| 添加弹窗初始隐藏 | ✅ display:none |
| 弹窗HTML（×/确认/取消） | ✅ 正确渲染 |
| JS错误/警告 | 无 |
| dark主题加载 | ✅ body bg = rgb(10,10,10) |

**涉及文件：** `_ui-prototype/indicator-ide.html`（CSS ~30行 + HTML调整 + JS 130行重写）

### 009 — 图表布局间距重分配 + 叠加指标副图不变 + Ctrl+滚轮缩放

**用户要求（多轮沟通）**：
1. 固定 indicator-tabs 到 chart-section 底部间距为 12px，主图高度锁死 355px，主图→副图间距锁死 60px，各副图间间距改为 30px，最后一个副图→tabs 间距 30px（含 tabs margin-top 8px）
2. 核实叠加指标切换时副图数据变化问题（已确认原型 mock 随机数据导致，生成环境无影响）
3. 新增 Ctrl+滚轮缩放：鼠标位于主图时按住 Ctrl 滚动滚轮，阻止页面滚动，改为调整时间轴缩放

**执行内容：**

| # | 修改项 | 详情 |
|:--|:-------|:------|
| ① | chart-section padding-bottom | 12px → **0px**（使内容填满容器） |
| ② | rebuildChart: totalH 动态计算 | 写死 743 → 动态 `425 + N*133 + (N-1)*30 + 22`，新增副图自动扩展 |
| ③ | 锁死参数 | mainGrid=355, mainSub=60, subSub=30, subH=133, tabsBot=~12px |
| ④ | 副图 series 加唯一 name | 所有 subDefs.getSeries 返回的 series 增加 `name:'sub_xxx_'+gi`，`replaceMerge:['series']` 按 name 正确匹配 |
| ⑤ | 随机 mock 数据缓存 | 成交额/大单净量/主力密码/融资融券/均量对比/资金仓位 从 `Math.random()` 改为 `_mockRand` 一次性缓存 |
| ⑥ | Ctrl+滚轮阻止页面滚动 | 新增 `.chart-section` 的 `wheel` 监听（`{passive: false}`），Ctrl/Meta 按下时 `preventDefault()` |

**浏览器验证：**

| 检查项 | 结果 |
|:-------|:------|
| main→sub1 间距 | 60px ✅ |
| sub1→sub2 间距 | 30px ✅ |
| tabs→chart-section 底部 | 11.2px ✅ |
| chart-section 高度 | 834px（右侧对齐）✅ |
| 切换叠加指标，成交额数据不变 | dataSame=true ✅ |
| 新增副图（MACD），容器自动扩展 | subCount=3, csH=987, kH=906 ✅ |
| Ctrl+滚轮 preventDefault | 返回 true ✅ |

**涉及文件：** `_ui-prototype/indicator-ide.html`
**状态：** ✅ 已完成（本轮全部完成）

### 010 — 四个原型页面数据源全量排查 + 166/171号文档修订

**用户输入：**
1. 要求全面排查仪表盘、选股系统、自选监控、个股策略分析四个原型页面，检查所有UI展示数据是否有对应的数据源及API接口支持（初始要求：仅排查不做修改）
2. 明确原则：所有原型UI沟通修订对应的后台数据实现和接口情况，必须在171号文档中标注
3. 要求对166号和171号文档进行详细修订，修正错误和不明确的数据及接口方式

**排查范围：** `dashboard.html` / `screener.html` / `watchlist.html` / `indicator-ide.html` 四个原型文件 + `002-方案存档/166.md` / `002-方案存档/171.md` 两份文档

**发现的主要问题汇总：**

| 页面 | Mock区块数 | Mock占比 | 核心问题 |
|:-----|:-----------|:---------|:---------|
| 仪表盘 | 9个 | ~70% | 仅4张指数卡片有真实API，其余全部Math.random()或硬编码 |
| 选股系统 | 5个 | ~80% | 因子组合名硬编码（§166§4.1.1要求动态同步），筛选结果/弹窗全Mock |
| 自选监控 | 7个 | ~100% | 5只×61列≈305字段全部硬编码，evaluateStock()使用Math.random() |
| 个股策略分析 | 10个 | ~70% | 顶部栏已动态化✅，K线待接入，右侧/筹码/资金流向100%硬编码Mock |

**关键发现：**
- 融资融券副图指标已在§166§6.8标为"已取消"，但 `indicator-ide.html:289` 的Tab按钮仍有残留（含getSeries函数和updateSubHeaders分支），需删除
- 涨幅前十/跌幅前十在§166§3.9中仅描述为"Tab切换"，未标注为硬编码Mock
- 因子组合列表在§166§4.2中未标注Mock状态，补充了缺失行
- 右侧面板28字段/盘口/资金流向/筹码在§166§6.2.4中未标注Mock状态
- 自选监控61列全部硬编码Mock，§166§5缺少对应标注

**171号文档修订：**
- §2.1.1 新增仪表盘9个Mock区块全量标注表（含数据详情+缺失路由+状态）
- §2.3.1 新增选股系统5个Mock区块全量标注表
- §2.4.1 新增自选监控7个Mock区块全量标注表（含61列数据源核实表）
- §2.2.2 新增个股策略分析11个Mock区块全量标注表
- §7 新增近端待建设API路由总表（15条路由，P0-P2）
- §8 建议行动项（原§7重编号）

**166号文档修订：**
- §3.9 补标注涨幅前十/跌幅前十为"⚠️ 硬编码Mock"
- §4.2 新增因子组合列表Mock状态行
- §5.6 追加自选监控全量Mock标注Action Item
- §6.2.4 右侧28字段/盘口/资金流向全部标为"⚠️ 全部硬编码Mock"
- §6.8 融资融券行追加HTML残留Tab按钮删除提醒
- §6.8.1 新增个股策略分析Mock占比汇总表
- §7.2 新增7行融资融券HTML残留提醒

**待建设路由总表（15条, P0-P2）：** 详见171号§7。关键路由：`quote`(P0)、`moneyflow`(P1)、`orderbook`(P1)、`stk-limit`(P2)、`sector-moneyflow`(P2)、`daily-top`(P2)、`dashboard/summary`(P2)、`chip/distribution`(P2)、`big-order`(P2)、批量行情聚合接口(P2)、分钟级周期支持(P2)

**涉及文件：** `002-方案存档/166-前端UI原型逐页调整记录.md`、`002-方案存档/171-功能视图API映射对照表.md`
**状态：** ✅ 已完成
