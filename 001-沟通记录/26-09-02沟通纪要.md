# 2026-09-02 沟通纪要

### 001 - dim2结构位置引擎全面核查与396号方案编制

**结论**：对dim2_structure_engine.py（4582行）进行ai-code-review逐行审核，发现1项错误（F-01 bsp_detector.detect()方法不存在）、2项HIGH、8项MEDIUM、5项LOW。编制396号方案并全部实施修复，46/46测试通过。

**关键发现**：
- F-01：`BuySellPointDetector`只有`find()`方法，dim2 evaluate()调用不存在的`detect()`→买卖点永远为空
- F-15：`fractal_threshold_pct`默认值被误改为0（362号核查已确认应为0.5）
- F-16：`only_last`快速模式为死代码（SIG全量计算，无简化场景）
- F-17：`ChanlunAnalyzer.__init__`未调用`_default_config()`，所有默认值从未生效

### 002 - dim2数据源获取核查

**结论**：dim2的5个数据源（daily_cache/indicator_ma/indicator_other/indicator_macd/tags）均为真实数据，分库路由正确。发现**indicator表日期落后daily_cache 3天**（8/28 vs 9/1），根因为RAW-1管道写入被锁死。

### 003 - compute_cache.db 44个fd根因排查

**结论**：`ShardingManager.get_connection()`存在竞态条件（check-then-act无锁），daemon启动时并发调用导致连接泄漏（44+43=87个fd）。修复方案：双重检查锁（Double-Checked Locking），修复后fd降至2+2=4。

### 004 - daemon盘中写入审计

**结论**：实时采集写market_cache.db，预计算写compute_cache.db，SIG/JUD/OUT写snapshot_cache.db——三库独立，不存在跨库竞争。暂停采集写入让路预计算在数据库层面不必要。

### 005 - indicator补算与管道恢复

**结论**：手动补算8/31（5126只）和9/1（已有5521只）的indicator数据，覆盖率99%。daemon重启后fd正常（2个），管道恢复运行。

### 006 - dim2输出内容真实性与消费链路核查

**结论**：dim2所有输出均为真实核算分析（非固化模板）。13个核心字段被dim8/dim_adapter/conflict_matrix消费，8-11个evidence字段未被下游消费。编制397号方案建立evidence字段系统性核查框架。

**关键发现**：
- vs_chip为381号方案遗留的无意义占位符，直接清除（删除变量+函数+参数）
- divergence_evidence空值为正确行为（无背驰时无需证据）
- BuySellPoint.__repr__中position=None时崩溃→已修复

### 007 - dim2输出质量标准讨论（待实施）

**用户核心观点**：
1. dim8降级兜底（空字段→读plain→输出"数据不足"）是**为有输出而输出**，掩盖了数据管道问题
2. 空字段应区分两类：数据管道故障→应驱动补采/补预计算；正常分析结论→应输出有意义的"无"文本
3. "无买卖点"不应输出空列表，应输出"无明显买卖点信号"
4. dim2发现的数据质量问题：
   - BuySellPoint.position=None → price=0.00, date=''（位置数据缺失）
   - level_trends中"月unknown"（月线数据不足）
   - buy_sell_points的price=0.00是错误数据，非正确分析结果
