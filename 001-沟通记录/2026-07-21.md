# 2026-07-21 沟通记录

### 001 - 选股系统筛选超时根因排查
**用户输入**: 开机后运行选股系统提示筛选超时，请重试，要求核查具体原因，不修改代码。
**AI执行内容**: 系统化调试排查，追踪到 `routes/screener.py:102` 中 `dm.sync_daily_data(ts_code, use_cache=False)` 在冷启动时逐只调 Tushare API，5000 只股票 × 每只 5 年数据，远超前端 60s 超时。

### 002 - 全局数据体系违规根本原因分析
**用户输入**: 为何配置未按全局数据体系要求执行，为何总是发生此类情况。
**AI执行内容**: 全系统违规审计发现 10 个文件 20+ 处违规。根因：(1) 无数据就绪保障机制——daemon 异步启动，API 被动降级；(2) "先查缓存→查不到直调"双路径反模式泛滥于 7+ 端点；(3) 红线规则无执行保障。

### 003 - 方案设计确认与整改计划建立
**用户输入**: 要求建立标准整改方案，同步核查全系统违规板块，修订项目规则。
**AI执行内容**: 
- 产出 `002-方案存档/284-全局数据体系违规审计与架构整改方案.md`（含完整违规审计表、系统性质因分析、分层整改计划、合规验证标准）
- 修订 AGENTS.md：红线规则1扩大至所有非采集层代码；新增规则2（禁止 `use_cache=False`）、规则8（sync_requests 异步队列机制）；新增"数据就绪保障机制"章节；更新§13降级优先级
- 记录全系统 10 个文件 20+ 处违规历史到 AGENTS.md "已知技术细节"

### 004 - data_daemon 采集覆盖审计与修复
**用户输入**: 近期数据端支持不足，要求核查自启动采集端口是否全量覆盖、配置是否正确。
**AI执行内容**: 审计发现 4 类数据未覆盖（adj_factor/top10_holders/stk_holder/finance_report），且 API 进程和 data_daemon 双系统并行冲突。新增 4 个 batch 采集函数并注册到完整性检查、日终同步、sync_requests 队列。禁用 API 进程 scheduler_manager 的日终同步。更新 AGENTS.md 完整采集覆盖清单。

### 005 - 301448.SZ 策略分析三项问题修复
**用户输入**: 环境定位无可用数据、主力成本不可用、缠论中枢33个月疑似错误。
**AI执行内容**: 追查三条数据链路：
- 环境定位：市场数据完整（HS300, 5951行），无可用数据是 DeepSeek 输出措辞问题，非数据缺失
- 主力成本：moneyflow_cache 无 amount/vol 列 + 单位错误（手/万元未转换）→ 修复改用 buy_lg_vol + buy_lg_amount + 正确单位换算
- 缠论中枢：_evolve_zhongshu 规则"任何价格重叠即延伸"导致无限延伸 → 改为"线段终点中枢外即detach"，触发已有扩张/新生逻辑

### 006 - 缠论分形/笔/线段识别全面修复
**用户输入**: 笔数不对，线段方向不交替，链接LLM Wiki知识库核实标准定义。
**AI执行内容**: 查阅LLM Wiki知识库，发现 min_klines=4→5、分形确认阈值0.5%、K线计数使用包含后位置。修复线段方向交替断裂问题。但修复后仍只有1个中枢（S2段23笔不释放）。

### 007 - 成熟缠论项目对标研究与制图方案制定
**用户输入**: 研究czsc/chan.py等成熟项目对比配置差异，制定制图可视化方案。
**AI执行内容**: 
- 对比czsc(5.6k⭐)/chan.py(1.9k⭐)发现：czsc默认使用笔中枢（跳过线段），min_bi_len=6
- 当前系统特征序列分型几乎不触发断线，靠9笔cap打断→跳过笔→段数不足→中枢不足
- 制图方案：选定 lightweight-charts-python 自建路径，中枢矩形通过 chart.run_js() 调用JS API
- czsc plotly版本核查发现其不渲染中枢，仅可用作分型/笔的交叉验证
- 产出285号方案文档（完整版含审计/整改/制图三部分）
- 全量执行285号方案P0-P2（不含制图）：KLineMerger递归深度10→3+merge_depth配置；min_klines=6、threshold=0默认值；BiZhongshuFinder笔中枢检测器 → 301448.SZ 笔中枢 9 个（原仅1个）

### 008 - P3+P4 全量交付
**用户输入**: 完成制图系统P3和API集成P4。
**AI执行内容**: 
- P3 ChartBuilder：基于 lightweight-charts JS v4.2.1，自包含HTML，支持分型/笔(彩色折线)/中枢(上下沿水平线)/买卖点，87KB
- P4 API+前端：`GET /api/v3/chanlun/chart?ts_code=X&period=long|short` 端点；前端缠论勾选自动加载日线+60分钟双图，iframe srcdoc嵌入现状深读上方

### 009 - 策略分析体系全面问题审计与问题文档建立
**用户输入**: 核查系统中个股策略分析和板块策略分析的工作流程和工作逻辑是否存在问题。
**AI执行内容**: 
- 全系统代码审计覆盖策略分析体系所有文件（routes/strategy_analyze.py、services/signal_computation_service.py、engine/framework/各策略、data_daemon 预计算等），识别9个问题
- **P1(高)**: 实时计算不读预计算缓存，每次请求完整跑5个策略，日终预计算形同虚设
- **P2(高)**: 板块策略实质缺失—无独立板块分析路由，SectorRotationModel以个股收益率冒充板块数据，五维卡4"情绪环境"仅显示行业名称
- **P3(中)**: compute_for_stock 460行巨函耦合过重，25个try/except静默失败
- **P4(中)**: 冲突检测粗糙，ConflictArbiter四级仲裁器完成但从未被调用
- **P5(中)**: 数据缺失静默跳过，不走sync_requests机制
- **P6(低)**: 预计算仅日终触发一次，覆盖面有限
- P7-P9为低优先级的冗余/合规问题
- 产出 `002-方案存档/286-策略分析体系问题全览与跟踪文档.md`，含问题清单、优先级矩阵、7期逐期跟进计划

### 010 - 策略预计算可行性与渠道统一方案讨论
**用户输入**: 讨论 P1 问题的本质——全市场策略预计算的可行性、两个渠道计算一致性、`_adjust_score_with_context` 逻辑的去留。
**AI执行内容**:
- 查实：所有策略数据源均为盘后数据（daily_cache/moneyflow_cache/daily_basic_cache 等），15:30后全部就绪
- 确认：daemon 的策略信号预计算非系统原始规划，是"写数据但无人消费"的无主代码
- 测算：5000只×0.8s/只≈66分钟(单线程)，18小时窗口绰绰有余
- 查实：两个渠道调用的底层策略引擎相同，但因子评分走两套代码（选股: _FACTOR_COMPUTERS / 个股: FactorRegistry），缠论评分选股独有 _adjust_score_with_context 修正
- **决策**：同意统一核心计算层 UnifiedStrategyCore；_adjust_score_with_context 直接剔除（理由：个股多维度分析+DeepSeek已覆盖此逻辑）；选股因子评分迁移到 FactorRegistry
- 更新 `286-策略分析体系问题全览与跟踪文档.md` v1.1，新增 P10 跟进项

### 011 - 总纲修订+专项方案287号建立
**用户输入**: 286号文件作为总纲，每个问题单独专项方案存档，287号记录本次预计算讨论。
**AI执行内容**:
- 重写 `286-策略分析体系问题全览与跟踪文档.md` v2.0：精简为总纲格式，每个问题仅保留简述+严重度+指向专项方案编号，完整讨论内容移至独立方案
- 产出 `287-全市场策略预计算可行性评估与方案设计.md`：全量文档含数据源核查、耗时估算、渠道差异全景、统一架构设计(UnifiedStrategyCore)、5项改动清单、预计算缓存设计、6.5天实施计划、风险对策、因子对照附录

### 012 - 前端调用适配方案补充到287号文件
**用户输入**: 需要将前端策略调用的适配方案纳入287号文件。
**AI执行内容**:
- 全量审计前端 API 调用：indicator-ide.html 6处调用 + screener.html 3处调用
- 初步产出结论为"零前端改动可上线"

### 013 - 核心修正：预计算缓存存储方案重设计
**用户输入**: 指出"原来提取的是策略核算需要的数据，现在提取的是计算结果"，预计算缓存不一定够用。
**AI执行内容**:
- 核查 `cache_strategy_signals()` 发现只存了5字段：`{ts_code, trade_date, signal_name, signal_value(=confidence), signal_level(=signal)}`
- status_recognition、chanlun_analysis_detail、evidence 等五维展示所需的关键数据**全部被丢弃**
- 重新设计缓存方案：新增 `strategy_signal_detail` 表（`signal_json TEXT` 存完整 signal dict）
- 重写 287号方案 §八 为"数据缓存链路设计（核心修正）": 选股走strategy_signals(轻量)、个股走strategy_signal_detail(完整JSON)
- 287号更新至v2.0，实施计划阶段四从1天调整为1.5天

### 014 - DeepSeek上下文数据依赖补全
**用户输入**: 指出 DeepSeek 的股票现状描述也需要预计算的数据支持。
**AI执行内容**:
- 核查 `_get_deepseek_status_text()` → `AiContextBuilder.build_context()` 数据链路
- 发现重大问题：`build_context()` 内部也调 `compute_for_stock()`！等于单次请求**实时计算跑了两遍**（第一遍为五维，第二遍为DeepSeek）
- 确认 SnapshotAssembler 的9个extractor全部可从缓存的signal dict还原
- 唯一例外是 `market_context`（大盘数据/乖离率/大单净额等），需在预计算时一并序列化缓存
- 287号更新至v2.1，完善缓存设计：`strategy_signal_detail.signal_json` 包含全部5个signal dict + market_context

### 015 - 前端调用调整方案最终评估
**用户输入**: 明确前端调用需要怎么调整。
**AI执行内容**:
- 分析E12时序：当前1-5s策略计算→1-3s DeepSeek=2-8s；预计算后5-50ms缓存→1-3s DeepSeek=1-3s
- 关键发现：**DeepSeek是同步调用**，即使策略计算秒回，E12总耗时仍受DeepSeek限制（1-3s）
- 给出三个方案录入287号v2.2：
  - 方案A（零改动）：直接上线，1-3s返回
  - 方案B（渐进式加载）：后端拆分DeepSeek，五维5-50ms先渲染，DeepSeek 1-3s后更新，前端需改~20行
  - 方案C（缓存DeepSeek）：daemon预调用DeepSeek写入缓存，5-50ms全量返回，无前端改动，每晚约50元API成本
