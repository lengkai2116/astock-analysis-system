# 2026-09-14 沟通记录

## 01 — 431 四项待决策项落地（①开新号 433 ＋ ②③④按推荐方案执行）＋ dim2 图景修正

用户对上轮说明的**四项待单独决策项**拍板：**①开新号**（IC 权重月度滚动重估作业）、**②③④按推荐方案执行**（docstring 口径统一 / `tr` NameError 修复 / dim2 缠论副本收敛）。本批只读核查先行，发现 **④ 推荐方案的整体前提与实况不符**，按实际可收敛面执行并如实报告。

### 复核结论（只读，先于任何改动）

- **② docstring 口径依据**：313 号方案原文 `:121`「该维度对后续 **10/20 日**收益的截面相关性」——10/20 均被方案认可、未定死；代码 `horizon=20` 与「每 20 交易日取一个截面」采样节奏自洽 ⇒ **取 20 日**，docstring/注释统一、代码零改动。
- **③ `tr` NameError 定位**：`_assess_risk_level`（`dim6_risk_engine.py:262-267`）——`tr = float(...)` 在 try 内赋值，`except: pass` 后 `:267` 行 `'高' if tr < 1.0 else '低'` 引用未定义 `tr` ⇒ 脏值（None/''/'N/A'）时 **NameError**；`Dim6RiskEngine.evaluate`（:1074）裸调用无兜底。对照组 `_list_risk_factors`（:312-315）同构但**安全**（tr 仅在 try 内使用）。
- **④ dim2 图景修正（关键）**：431 §018「framework 份无 live 消费方」**仅适用于 `chanlun_level_validator.py`**（全仓零 import，仅 dim2 注释提及）；但 framework 缠论**并非整体无消费方**——
  - `chanlun_strategy.py`：**大量 live**（`data_daemon.py:2715/:2969`、`routes/strategy_analyze.py:1444`、`chart_builder.py:9`、`dim2:269`、`chanlun_multi_level.py:16`、`tests/test_chanlun_verification.py` 14+ 处）＝**活核心，权威**；
  - `chanlun_config.py`：被 `chanlun_multi_level.py:15` 消费（live）；
  - `chanlun_level_validator.py`：**零 live 消费方** ✓ 唯一可收敛。
  - dim2 内联区块实测：`# === chanlun_config.py ===`（:29）、`# === chanlun_level_validator.py ===`（:147）、`# === chanlun_strategy.py ===`（:501，含 ChanlunAnalyzer :2554 / ChanlunScorer :2956），且 `Dim2StructureEngine.evaluate`（:3830）**直接用 dim2 内联的 ChanlunAnalyzer/ChanlunScorer**（活代码）。
  - ⇒ 推荐方案 A「dim2 为权威、framework 从 dim2 导入/删除」的**可执行面仅 `chanlun_level_validator.py`**；`chanlun_config`/`chanlun_strategy` 双份均 live，合并=行为变更，**另行决策**。

### 实施内容

| # | 对象 | 改动 |
|:--:|---|---|
| ① | `002-方案存档/433-IC权重月度滚动重估作业（写侧链路）.md` | **开新号 433**（v1.0）：背景（313 §4.2 第三层从未运行——`recompute_ic_weights`/`save_ic_weights` 零调用点、`data/ic_weights.json` 静态快照 97B/08-04）、现状事实 5 项、方案设计（频率=月度、触发=月末最后交易日收盘后、**承载面四选一**：`data_daemon.py` 钩子〔推荐〕/ `scheduler_manager.py`〔次选，应用需常驻〕/ `scheduler.py` / `rule_scheduler.py`〔不适配〕、**失败不覆盖旧权重**、上线前验证三件套）、实施批次 1-3、验收标准 4 条、关联（431 E1 / 313 §4.2 / 前置口径已决） |
| ② | `potential_engine.py` docstring/注释 | 3 处「10 日」→「20 日」：`:287` docstring「预测未来 20 日收益（horizon=20，与采样间隔一致）」、`:305`「截面日 + horizon（20）日后的收益日」、`:313`「20 日后收盘」；**代码 `horizon=20` 零改动** |
| ③ | `dim6_risk_engine.py` `_assess_risk_level` | `risk_sources.append(流动性风险…)` **移入 try**（脏值时跳过该风险源，不再 NameError；与 `_list_risk_factors` 同构）＋ 433 号注释 |
| ④ | `framework/chanlun_level_validator.py` | **`git rm` 删除**（零 live 消费方，git 可恢复；dim2 内联份较新且 live＝唯一权威）；dim2 `:149-155` 431 G1 标注注释更新（framework 份已删、勿重建） |
| 5 | `001-目录索引.md` | 431 行尾句四项待议→已闭环（433 开号/口径统一/tr 修复/level_validator 删除）；**新增「## 十五、后续专项（432-433号）」节**（432 号此前未登记，一并补齐） |
| 6 | `431-….md` | §四 E1 处置状态 blockquote「未动（待单独决策）」四项 →「后续处置（2026-09-14 已决，另见 433 号）」；处置进度 §019 行「待单独决策」→「后续已处置 + 残余待议（dim2 config/strategy 双份 live）」 |
| 7 | `001-沟通记录/2026-09-14.md`（本文件） | 记录本批 |

### 验证证据

| 项 | 证据 |
|---|---|
| **ruff** | `potential_engine.py` / `dim6_risk_engine.py` / `dim2_structure_engine.py` → **`All checks passed!`** |
| **py_compile** | 同上 3 文件 → **COMPILE_OK** |
| **③ 行为断言** | 脏值（None/''/'N/A'/'--'）→ 不再 NameError、流动性风险源被跳过；正常值行为不变（0.5→高 / 5.0→低 / 缺省999→低） |
| **dim2/dim6 回归** | `tests/test_411_pipeline.py -k "Dim2 or Dim6"` → **9 passed, 21 deselected** |
| **framework 缠论回归** | `tests/test_chanlun_verification.py` 合成数据子集（含 `ChanlunAnalyzer` full pipeline / `ChanlunScorer`）→ **6 passed, 9 deselected**（删 level_validator 无影响） |
| **引用面复核** | 删前全仓 `grep chanlun_level_validator` = 仅 dim2 3 处（docstring/区块注释），**无 import** ⇒ 删除零断裂 |
| git 状态 | 本批受控改动 = `M potential_engine.py` / `M dim6_risk_engine.py` / `M dim2_structure_engine.py` / `D framework/chanlun_level_validator.py` / `M 001-目录索引.md` / `M 431-….md` ＋ 新建 `433-….md`（`??`）＋ 本文件（`??`）；未混入用户自有未提交改动 |

### 需知悉三点

1. **④ 与 431 §018 的衔接**：§018 记「dim2 为整份 vendored 副本、framework 份无 live 消费方」——**该表述只对 `chanlun_level_validator.py` 成立**；`chanlun_strategy.py` 实为活核心（daemon/routes/chart_builder/dim2 多路消费），故本批只删 level_validator。`chanlun_config`/`chanlun_strategy` 的双份收敛（含 dim2 `:501-:3143` 内联区块 vs framework 活版、`BiConfig` 少 `@dataclass`）**保留待议**——建议并入 433 号批次或另行决策，**未动**。
2. **433 号仅开号、未实施功能**：写侧仍零调用点；批次 1（承载面定稿+挂载）起才动 `data_daemon.py`/调度器。433 §3.3 已定关键约束——**重估失败不得用兜底 `DIM_WEIGHTS` 覆盖 `ic_weights.json`**。
3. **431 仍 v1.3**（fix 批次不 bump）；本批新增实体 **433 号**、未新增 431 字母类（A–G 仍七类）。

**产出**：`002-方案存档/433-IC权重月度滚动重估作业（写侧链路）.md`（开号）＋ `potential_engine.py`（docstring/注释 3 处）＋ `dim6_risk_engine.py`（`tr` NameError 修复）＋ `framework/chanlun_level_validator.py`（删除）＋ `dim2_structure_engine.py`（注释更新）＋ `001-目录索引.md`（十五节 + 431 行更新）＋ `431-….md`（§019 后续处置表述）＋ 本文件。**残余待议**：dim2 `chanlun_config`/`chanlun_strategy` 双份收敛（建议并入 433 或另行决策）。下一步：**待定**（可启动 433 批次 1）。

## 02 — 433 号 v1.1 更新（四重视角评估增补：批次 0 前置审计 + 验收修订）

用户要求以量化分析师 / 系统架构师 / 代码专家 / 产品经理四重视角评估 433 号方案并整理更新入方案文档（仅文档更新，未改代码）。**评估前只读核查 6 项事实（F6-F11）**，发现**写侧计算口径存在前置量化缺口**，工程骨架（批次/承载面/失败语义）不变，但新增强制前置批次。

### 评估发现（核查事实）

- **F6 因子错配（2/4 维度）**：`compute_potential` 评分信号＝`val=valuation_deviation`、`trend=trend_alignment` 标签；`recompute_ic_weights` 却用 `val=1/pe_ttm`（EP）、`trend=20日动量` 算 IC ⇒ **重估权重与评分语义脱节**（earn/fund 两侧一致）。证据：`potential_engine.py:146/:184` vs `:287/:332`。
- **F7 earn 前视 + 多期混合**：`fina_indicator_cache` **无 `trade_date` 列**（ts_code/end_date/ann_date/roe…）；roe 查询无日期过滤 ＋ `dict(zip())` 多期后者覆盖前者（依赖 SQLite 返回序，不稳定）＋ roe_map 静态 ⇒ 截面 IC 不独立、聚合虚高、前视。
- **F8 fund 样本不足**：`moneyflow_cache` 仅覆盖 **2026-07-20~09-11（38 交易日）**（218,234 行/5562 只）⇒ 每 20 日一截面最多 1-2 个 fund 截面，且 `break` 按 val 计数 ⇒ **fund IC 近 2-3 个月统计不可用**。
- **F9 读侧天然生效（正面）**：daemon 每轮 JUD 预计算都 `PotentialEngine()`＋`build_percentile_tables()` ⇒ 落盘后下一轮自动读新权重，**无需缓存刷新机制**（`data_daemon.py:4188-4193`）。
- **F10 返回语义不可区分**：数据不足/合法无正 IC/异常三路径均返回 `DIM_WEIGHTS` ⇒ 作业层无法区分「失败」vs「合法保留」。
- **F11 性能风险**：每截面 5 次 SQL，`px_prev` 窗口函数在 641 万行 daily_cache 上每截面全表扫 ⇒ 预估分钟级，挂 daemon 主循环会阻塞采集。

### 433 v1.1 更新内容（全量并入方案文档，206 行，v1.0 事实一字未删）

1. **§二 新增 F6-F11 六项事实**（v1.1 增补表）。
2. **§3.1/3.2/3.3 补充口径纪律**：交易日口径窗口判断（复用 432 R3）、盘后 18:00 数据齐备纪律（432 R4 教训）、轻钩子不阻塞主循环、幂等/原子写（tmp+`os.replace`）/`last_recalc` 标记（432 双 daemon 教训）、云部署适配（纯函数+状态文件）、返回结构带 status 区分三路径（批次 1 落地）。
3. **§3.5 新增可观测性**：每次重估并行写 `ic_report.json`（窗口/各维 IC 均值+std+截面数+样本量/status/新旧权重），日志+文件双通道。
4. **§3.6 新增权重映射决策清单（W1-W5，批次 3 落地）**：0.05 阈值无统计依据、负 IC 方向信息丢失、sector/event 被被动稀释（docstring「中位」与实现不符）、月度权重跳变（建议 `w_new=α·w_ic+(1-α)·w_old` 平滑＋上下限）、3 截面 break 噪声大（提升至 ≥5-6）。
5. **§四 批次重构**：新增**批次 0（强制前置）＝因子对齐与数据可用性审计**（val/trend 口径对齐、roe 按 `ann_date` 过滤、fund 覆盖实测→缩窗口或暂停），产出口径定稿报告；原批次 1-3 扩为 1-4（批次 3＝W1-W5 落地）。
6. **§五 验收修订**：验收 2 改「有重估动作且可解释证据（ic_report + last_recalc）」——消除「合法无正 IC 时 json 不变被误报」风险；**新增质量门禁**（回看验证新旧权重 IC/排名无劣化、无前视审计通过）。

### 验证与登记

- 433 `wc -l` 135 → **206**；结构锚点：§二 v1.1 事实表 `:59` / §3.5 `:131` / §3.6 `:148` / 批次 0 `:169` / 验收 5 `:181` / 处置进度 `:197`（批次 0-4）。
- `001-目录索引.md` 433 行状态改「📋 已开号（v1.1，待按批次实施；批次 0 审计前置）」＋ 说明补 v1.1 摘要。
- 本文件 §02 ＋ 沟通索引。
- **未改任何代码**（433 写侧仍零调用点）；受控改动＝`M 002-方案存档/433-….md` ＋ `M 001-目录索引.md` ＋ 本文件（`??`）。

### 需知悉

1. **批次 0 是强制前置**：在 val/trend 因子口径、roe 前视、fund 覆盖定稿前，**不得挂写侧自动化**——否则会把错误口径的 IC 升级为主动覆盖权重的机制（比不做更糟）。
2. **F9 结论可放心**：读侧无需刷新机制（daemon 每轮重建引擎），后续批次不用为「落盘后如何生效」额外设计。
3. **433 版本 v1.0 → v1.1**（本号非 431 fix 批次，版本随内容更新合理）。下一步：**待定**（建议先做批次 0 只读审计，产出口径定稿报告）。

## 03 — 433 号批次 0 审计执行（口径定稿 4 条：val/trend/fund 暂停重估、earn 修正后保留）

用户指示**执行批次 0 审计**（只读+验证，未改任何业务代码）。审计脚本 `backend/scripts/433_batch0_audit.py`（全 SELECT 只读、可复现），数据源 snapshot/market/financial 三库（2026-09-11 快照时点）。

### 审计结果（实测）

| 维度 | 结果 | 判定 |
|:--:|---|---|
| val | EP（1/pe_ttm）vs `valuation_deviation` **Spearman=0.329、Pearson=-0.031**（3900 样本）；`treemap_snapshot` **仅 1 个快照**（2026-09-11）→ 评分信号无历史截面 | **EP 不可代理** → val 暂停重估（保持配置 0.20） |
| trend | 20 日动量 vs `trend_alignment` **Spearman=0.060**（5535 样本）；标签分布 down 2729/mixed 1435/up 1343，up 组动量均值 -0.0027（非动量离散化）；无历史截面 | **动量不可代理** → trend 暂停重估（保持配置 0.20） |
| earn | roe 与评分信号一致；5554/5555 股票多期（end_date 23 期）、**ann_date 仅 191/22407 非空（99.1% 缺失）** | **可重估但须修正**：取每只最新 end_date 的 roe +「报告期+披露滞后」可得性近似 |
| fund | moneyflow 40 交易日（07-20~09-11）→ 2 截面 < 3 | **样本不足** → 暂停重估直至 ≥60 交易日 |

### 口径定稿（4 条，已入 433 §二 v1.2 小节）

1. val / trend 暂停 IC 重估，权重保持配置值；长线方向 = treemap_snapshot 历史快照留存（新增**批次 5 可选**，不阻塞 1-4）。
2. earn 保留重估：每只最新 end_date 的 roe（单值）+ 披露滞后近似（Q1≤4月底/半年≤8月底/Q3≤10月底/年报≤次年4月底）。
3. fund 暂停重估：启动条件 = 覆盖 ≥60 交易日且 ≥3 个有效截面。
4. **`recompute_ic_weights` 须 earn-only 重构**（批次 1）：val/trend/fund 跳过；权重映射仅在 earn 生效或整体降级「earn 监控 + 保留配置权重」（二选一批次 1 定稿）。**313 第三层「全维度 IC 驱动」当前数据条件下不可达成，本号目标修订为「有限维（earn）自适应 + 全维监控」**。

### 验证与登记

- 审计脚本两次运行通过（首次修正 snap 查询缺 trend_alignment 列后全绿）；动量 SQL 抽查 000001.SZ（up_aligned 且动量正，标签合理）。
- 433 v1.1 → **v1.2**（`wc -l` 206 → **228**）：frontmatter/头部 v1.2 增补、§二 新增「v1.2 批次 0 审计结果与口径定稿」小节、§四 批次表（批次 0 ✅ + 批次 1 改 earn-only 重构 + **新增批次 5 快照留存**）、处置进度表批次 0 ✅。
- 受控改动 = `M 002-方案存档/433-….md` ＋ 新增 `backend/scripts/433_batch0_audit.py`（`??`）＋ 本文件（`??`）＋ 沟通索引；**未改任何业务代码**（写侧仍零调用点）。

### 需知悉

1. **审计结论超出 v1.1 预判**：v1.1 假定「val/trend 对齐评分信号或声明等价」二选一——实测两口径截面相关性极低（0.329 / 0.060）且评分信号无历史，**等价声明不成立**，只能暂停重估；earn 的 ann_date 缺失（99.1%）使「按 ann_date 过滤」的 v1.1 假设也不成立，改用 end_date+披露滞后近似。
2. **313 第三层目标收缩**：从「全维度 IC 驱动」变为「earn 有限维自适应 + 全维监控」——这是数据现实约束下的务实修订，若用户认可请在下批确认。
3. 批次 1 起按 earn-only 重构，批次 5（快照留存）为长线铺路可选。下一步：**待定**（可启动批次 1）。

## 04 — 433 号批次 1 落地（earn-only 重构 + daemon 月度轻钩子）

用户确认口径（earn 有限维自适应 + 全维监控）后**启动批次 1**。代码改动 2 文件（`potential_engine.py` / `data_daemon.py`），**写侧首次出现 live 调用点**（daemon 主循环 → `run_monthly_ic_recalc`）。

### 实施内容

| # | 改动 | 说明 |
|:--:|---|---|
| C1 | `recompute_ic_weights` **earn-only 重构** | val/trend/fund 维度跳过（口径定稿 4 条）；earn 取每只「截面日当时可得」最新 end_date roe（披露滞后近似 `_avail`：Q1+1月/半年+2月/Q3+1月/年报+4月）；返回 `{status, weights, ic_report}`；截面门槛 ≥6（W5）；**一次性拉取**窗口 daily + fina（F11 消除——不再有 px_prev 全表窗口函数） |
| C2 | `save_ic_weights` 原子写 + `last_recalc` | tmp + `os.replace`（防双实例/中断写坏）；json 内带 `last_recalc` 时间戳（幂等标记；load 侧只要求 6 键，extra 无影响） |
| C3 | 新增 `write_ic_report` / `run_monthly_ic_recalc` / `_load_current_weights` | `ic_report.json`（§3.5 可观测性落地）；门面按 status 处置：ok 落盘 / no_signal 保留并写报告 / 异常不落盘+告警 |
| C4 | `data_daemon` 月度轻钩子 `_maybe_monthly_ic_recalc` | 主循环 `_drive_pipeline` 后挂载：幂等（`last_recalc` 月份 != 当月才触发）、`_run_with_timeout(300s)`、try/except 不阻塞主循环、整月未运行则下月启动自动补跑 |

**权重映射（earn 生效）**：`ic_mean > 0` → `w_e = clip(0.15×(1+2·ic), 0.15, 0.35)`，其余 5 维按 DIM_WEIGHTS 相对比例缩放补足 1.0；`ic_mean <= 0` → `no_signal` 保留配置权重（不覆盖，写报告）。W4 α 平滑留待批次 3（当前映射温和）。

### 验证证据

| 项 | 证据 |
|---|---|
| ruff | `potential_engine.py` → `All checks passed!`；`data_daemon.py` **32 处告警均为既有遗留**（E401/I001/W293/F541/F841，非本批引入，432 号亦未处理），本批新增行无告警 |
| py_compile | 两文件 → `COMPILE_OK` |
| mock ecm 断言 | earn IC 正（0.6428）→ status=ok、earn 0.15→0.3428、6 键归一化；earn IC 负（-0.5581）→ no_signal、保留配置权重 |
| **只读直连真实分库** | 耗时 **3.4s**（F11 分钟级风险消除）；earn IC=**0.0213**（6 截面 / 32471 样本 / 窗口 2025-12-16~2026-09-11）；权重 0.15 → **0.1564** 温和上调；6 键归一化 1.0 |
| 回归 | `test_411_pipeline -k Dim6` **3 passed**；`test_322_s7_stretch_fix -k map_score` **5 passed** |
| git | `M potential_engine.py`（+~100 行）/ `M data_daemon.py`（+39 行）＋ 433 v1.3 ＋ 本文件；未混入用户自有改动 |

### 需知悉

1. **真实库端到端落盘未执行**：`data_daemon` 正在运行持有开发态共享库（`database is locked`），**未 kill daemon**；`run_monthly_ic_recalc` 实际落盘验证留待 daemon 空闲窗口或批次 2/3。逻辑已由只读直连 + mock 双重验证。
2. **写侧首次 live**：daemon 每 30s tick 调用钩子（O(1) 幂等判断），本月未算且满足条件时触发一次（~3.4s）——**`recompute_ic_weights`/`save_ic_weights` 不再零调用点**（验收 1 达成）。
3. **数据现实**：真实 earn IC=0.0213（弱正），映射仅 +0.006——符合「有限维自适应」的温和预期；若某月 earn IC 转负将 no_signal 保留权重（不劣化）。433 v1.2 → **v1.3**。下一步：**待定**（批次 2：验证脚本 + 窗口单测）。

## 05 — 433 号批次 2 落地（验证脚本 + 单测 + 无前视审计）

用户指示**批次 2 继续**。产出 2 个文件（验证脚本 + 正式单测），全部 DB-free / 只读，不碰开发态基准库。

### 产出

| # | 文件 | 内容 |
|:--:|---|---|
| V1 | `backend/scripts/433_batch2_verify.py` | 只读直连分库（绕开 daemon 写锁）三项验证：① earn-only 重算实测（耗时/status/weights/ic_report）；② **无前视审计**——逐截面日输出「当时可得」报告期分布并断言 `end_date+lag <= d0`；③ 新旧权重对比表 |
| V2 | `backend/tests/test_433_ic_recalc.py` | 10 用例：`recompute_ic_weights` 分支（正 IC→ok 上调 / 负 IC→no_signal 保留 / 数据不足 / 6 键归一化）、`save_ic_weights` 原子写+`last_recalc`、`write_ic_report`、`run_monthly_ic_recalc` 门面（ok 落盘 / no_signal 不覆盖但写报告）、`data_daemon._maybe_monthly_ic_recalc` 幂等（本月已算→跳过 / 上月→触发） |

### 验证证据

- `433_batch2_verify.py`：耗时 **2.3s**、status=ok、earn IC=0.0213、权重 0.15→0.1564；**无前视审计通过**——截面日 2025-12-16~2026-04-20 用 2025-09-30（Q3，+1 月可得），2026-05-21 起切换 2025-12-31（年报，+4 月）/2026-03-31（Q1，+1 月）——披露滞后逻辑正确、无未来报告期；新旧权重对比（earn +0.0064，其余 5 维等比微降）。
- `test_433_ic_recalc.py`：**10 passed**；与 `test_431_g2_config_loading.py` 联合 **29 passed**；`test_411_pipeline -k Dim6` **3 passed**；ruff 两新文件 `All checks passed!`（修复本批引入的 5 处：verify 脚本 `ok` 未用变量 + F541、单测 import 排序）。

### 登记

433 v1.3 → **v1.4**（frontmatter、批次表批次 2 ✅、新增 §3.8、处置进度批次 2 ✅）；`001-目录索引.md` 433 行（v1.4，批次 0/1/2 已落地）；`2026-09-14.md` §05；沟通索引；记忆。

### 需知悉

1. **批次 2 全部 DB-free/只读**：无前视审计以真实分库独立验证（非 mock），证明 earn-only 披露滞后近似正确。
2. **真实落盘端到端仍留批次 4**（daemon 空闲窗口或用户授权时执行 `run_monthly_ic_recalc` 实际落盘 + 回看验证）。
3. 433 v1.4。下一步：**待定**（批次 3：权重映射 W1-W5 决策清单——α 平滑/负 IC 方向/阈值显著性/上下限）。

## 06 — 433 号批次 3 落地（权重映射 W1-W5 优化）

用户指示**批次 3 继续**。代码改动 `potential_engine.py`（§3.6 W1-W5 按 earn-only 裁剪落地）。

### 实施内容

| 项 | 落地 |
|:--:|---|
| W1 | **显著性门槛**：`recompute_ic_weights` 新增 `sig_t_crit=1.0`——earn IC 须为正且 `ic_mean > sig_t_crit × ic_std/√n` 才判 `ok`，否则 `no_signal` 保留配置权重；`ic_report.earn` 增补 `se`/`significant` |
| W2 | **负 IC 告警**：`run_monthly_ic_recalc` no_signal 分支区分——earn IC 负 → `logger.warning`（因子反向）；不显著 → `logger.info` |
| W3 | **sector/event 语义确认**：其余 5 维按 DIM_WEIGHTS 相对比例等比缩放（sector/event 相对比例不变，仅被 earn 整体挤压）——无代码改动 |
| W4 | **权重平滑**：`recompute_ic_weights` 新增 `smooth_alpha=0.4`/`prev_earn`——`earn_new = α·w_ic + (1-α)·prev_earn` 再 clip [0.15,0.35]；门面传当前文件 earn 为 prev |
| W5 | 截面门槛 ≥6：批次 1 已落地 |

### 验证证据

- **行为变化（真实数据）**：W1 落地后真实 earn IC=0.0213、se=0.0246 → **不显著 → no_signal**（批次 1/2 的「ic>0 即 ok 上调 0.006」不再发生）——系统从「每月微调」变「**显著才调**」，更严谨；no_signal 不覆盖文件、写 ic_report 监控。
- ruff（potential_engine + test 文件 `All checks passed!`）；py_compile OK；`test_433_ic_recalc.py` **13 passed**（新增 3 用例：弱正不显著→no_signal〔drift_scale=0.0 探测实证〕/ 高门槛强制 no_signal / 平滑介于 prev 与纯 ic 值之间——首版断言「prev=base 时不变」错误，修正为「平滑后 earn 介于 prev 与 w_ic 之间」）；`test_431` 联合 **32 passed**；`test_411 -k Dim6` **3 passed**；只读直连真实数据耗时 **1.3s**。

### 登记

433 v1.4 → **v1.5**（frontmatter、批次表批次 3 ✅、新增 §3.9、处置进度批次 3 ✅）；`001-目录索引.md` 433 行（v1.5，批次 0/1/2/3）；`2026-09-14.md` §06；沟通索引；记忆。

### 需知悉

1. **W1 是重要的行为收紧**：当前真实 IC 不显著 → 月度钩子会走 no_signal（保留配置权重 + ic_report 监控），**不会主动改权重**——直到某月 earn IC 显著为正。这正是「有限维自适应 + 全维监控」的预期行为。
2. W1 门槛（1 倍标准误）可调（`sig_t_crit`），如需更保守/激进可改参数。
3. 433 v1.5。下一步：**待定**（批次 4：首轮真实月度重估 + 落盘确认 + 回看验证——需 daemon 空闲窗口或用户授权）。

## 07 — 433 号批次 4 落地（首轮真实月度重估 + 落盘确认 + 幂等缺口修复）

用户授权**关停 daemon（开发阶段）**执行批次 4。端到端流程：

### 执行过程

1. **daemon 关停**：先停看守（start_daemon.sh bash 4600）再 SIGTERM 优雅停 daemon（4628）——双进程，防看守自动重启。
2. **生产路径重估**：`run_monthly_ic_recalc(ecm, data_dir)`（`get_ecm_instance` 全局单例）→ 耗时 **1.7s**、**status=no_signal**（earn IC=0.0213 不显著，W1 门槛生效）。
3. **落盘确认**：
   - `ic_weights.json` **保持 97 B 初始原样**（8月4日，无 last_recalc）——no_signal **不覆盖**权重文件 ✓
   - `ic_weights_report.json` **已写**（552 B）——窗口（2025-12-16~2026-09-11，6 截面，32471 样本）/ earn IC=0.0213 / se=0.0246 / significant=false / status=no_signal / old=新=DIM_WEIGHTS
4. **回看验证**：no_signal 场景新旧权重一致（ic_weights.json 字节级未变）；ok 落盘端到端由单测覆盖。
5. **端到端发现并修复幂等缺口（重要）**：no_signal 不写 `last_recalc` → 钩子幂等标记永不设置 → **每 30s tick 重算**（每 tick ~1.7s + 日志刷屏）。修复：`write_ic_report` 每次重估写 **`recalc_at`**；`_maybe_monthly_ic_recalc` 幂等判断 = `ic_weights.json.last_recalc` **或** `ic_weights_report.json.recalc_at` 任一命中本月即跳过。补单测 `test_monthly_hook_skips_when_report_recalc_at_current_month`。
6. **daemon 重启**：经 Terminal 启动器恢复（看守 16327 + daemon 16394 主循环运行）；日志确认无 IC 重估刷屏（幂等生效）。

### 验证证据

- `test_433_ic_recalc.py` **14 passed** + `test_431` 联合 **33 passed** + `test_411 -k Dim6` **3 passed**；ruff `All checks passed!`；py_compile OK
- 钩子幂等实证：report 本月 recalc_at（2026-09-14T10:52:43）→ `_maybe_monthly_ic_recalc` **0 次触发**
- daemon 重启后主循环运行（10:54:31），日志无「月度 IC 重估」刷屏

### 登记

433 v1.5 → **v1.6**（frontmatter、批次表批次 4 ✅、新增 §3.10、处置进度批次 4 ✅）；`001-目录索引.md` 433 行（v1.6，批次 0-4）；`2026-09-14.md` §07；沟通索引；记忆。

### 需知悉

1. **首轮真实重估结果为 no_signal 是正确行为**（earn IC 0.0213 不显著）——系统进入「全维监控」状态：每月钩子检查、不显著不改权重、写 ic_report 留证；某月 earn IC 显著为正才会真正调权。
2. **幂等缺口是端到端才暴露的**：单测覆盖了 last_recalc 路径，但 no_signal 不写 last_recalc 的组合只在真实运行时出现——批次 4 价值在于用真实执行补上了这个漏洞（否则 daemon 每 30s 重算 1.7s）。
3. 433 v1.6，**批次 0-4 全部落地**；批次 5（treemap 历史快照留存）为可选长线项。下一步：**待定**（批次 5 或收尾）。

## 08 — 433 号批次 5 落地（treemap 历史快照留存——433 全量收官）

用户指示**完成批次 5**（避免 433 遗留遗忘）。本批为数据留存基建 + 脏数据清理，**无业务代码改动**（归档机制已存在）。

### 核查发现（关键）

**历史留存机制已存在**：`data_daemon.py` OUT 步骤 `_archive_outcome`（:4606-4647）每天把 `treemap_snapshot` 以**显式列名**归档到 `treemap_snapshot_history`（47 列完整，含 valuation_deviation / trend_alignment；sharding_manager 已注册 snapshot_cache.db、`idx_treemap_hist_date` 索引已建）。核查时已积累 **4 个快照日期**（2026-08-19 / 08-23 / 09-10 / 09-11，22222 行）。

**批次 0 表述精确化**：批次 0 说「valuation_deviation / trend_alignment 无历史截面」——指 **treemap_snapshot 当前表**（原子表替换仅最新一天）；而 **treemap_snapshot_history 已在积累**（08-19 起）。精确表述：**历史留存机制已就绪并在积累，但当前仅 4 天（远不足 ≥6 个月重估门槛）**——不影响批次 0 结论，「无历史」应理解为「不足」而非「无机制」。

### 本批落地

| # | 动作 | 结果 |
|:--:|---|---|
| A | **清理脏数据**：history 表存在 **5571 行旧版 SELECT \* 错位遗留**（snapshot_date 被 name 填充、整行左移 1 列——早期表结构变更前归档造成；含 1 行空 snapshot_date） | `DELETE WHERE NOT (snapshot_date GLOB 'YYYY-MM-DD')` 删除 5571 行；剩 **22222 行 / 4 快照日期 / 0 残留** |
| B | **归档一致性验证**：history 09-11 = treemap_snapshot 当前 **5553 行**；抽查 5 只 valuation_deviation / trend_alignment **完全一致**；字段覆盖 **valuation_deviation 99.8% / trend_alignment 99.2%** | 归档正确、可直接作为未来 val/trend 数据源 |
| C | **未来启用接口说明**：积累 ≥6 个月后 `recompute_ic_weights` 扩展 val/trend 时读 `treemap_snapshot_history`（snapshot_date 切片 + join daily_cache 收益），复用 earn-only 截面框架 | 记录 §3.11，不写代码（待积累） |

### 验证与登记

- 清理前已充分核查（脏行形态：name 进 snapshot_date、close 进 trade_date、sector_heat 进 valuation_deviation——确认错位污染）；清理后总行 22222 / 快照日期 4 / 残留 0。
- 433 v1.6 → **v1.7**（frontmatter status **✅ 已全量完成**、批次表批次 5 ✅、新增 §3.11、处置进度批次 5 ✅）；`001-目录索引.md` 433 行（v1.7，✅ 全量完成）；`2026-09-14.md` §08；沟通索引；记忆。

### 需知悉

1. **433 全量完成（批次 0-5）**：earn 有限维自适应已上线（全维监控 + 显著才调 + daemon 月度轻钩子）；val/trend 历史留存机制已就绪（脏数据已清、每日随 OUT 步骤自动积累）；fund 待 moneyflow ≥60 交易日；**val/trend/fund 直接重估待 6 个月后数据积累**（届时扩展 recompute 即可，框架已备）。
2. **脏数据清理是批次 5 的真实价值**：若不清，未来 val/trend 按 snapshot_date 切片时会混入 5571 行错位数据（name 当日期）导致 IC 计算污染——早期 SELECT * 归档的历史债在此收口。
3. 433 全部批次 ✅。系统现处于「earn 全维监控 + 数据积累」稳态。残余待议（非 433）：dim2 `chanlun_config`/`chanlun_strategy` 双份收敛。

## 09 — dim2 缠论双份收敛：方案评估与决策交接（新对话执行）

用户就残余待议项（dim2 `chanlun_config`/`chanlun_strategy` 双份收敛）要求详细建议，并明确新约束：**须保留 dim2 在 SIG dim1-dim8 体系中的合理性**。只读核查后给出基于 SIG 体系规划的方案，用户**拍板方向并将切换新对话执行**，本记录为交接。

### 核查事实（新对话勿重做，已入项目记忆 `dim2-chanlun-merge.md`）

| # | 事实 |
|:--:|---|
| F1 | dim2 内联 4 区块：chanlun_config(:29) / chanlun_level_validator(:147) / trend_structure_detector(:411) / chanlun_strategy(:502~:3819) |
| F2 | dim2 内联 chanlun_strategy（3318 行/27 符号）是 framework 版（3275 行/25 符号）的**超集**（+`_load_precomputed_macd`、`calc_support_resistance`）；相似度 **~87%**（dim2 独有 116 行 / framework 独有 80 行） |
| F3 | framework 版消费方：data_daemon×2 / routes / chart_builder / multi_level / tests 14+；dim2 内联版消费方：evaluate / AlphaModel / get_tags |
| F4 | **dim2 内部混用两份（隐患）**：`_analyze_level`(:270) 动态 import framework 版，其余用内联版 |
| F5 | **SIG 体系模式（决定性）**：dim3/dim5/dim6 全部 import framework——**dim2 是唯一内联 vendored 的异类**；dim 引擎既定模式 = import framework 共享底座 |
| F6 | framework/chanlun_config.py 被 multi_level 消费；BiConfig dim2 份少 @dataclass；dim2 内联 level_validator 是较新版 |

### 方案决策

- **方案 A（选定方向）**：framework 权威 + **dim2 回归「dim-import-framework」**——符合 SIG dim1-dim8 规划（dim 引擎统一 import framework），保留 dim2 结构维度引擎角色不变（合并后与 dim5 同构）；缠论能力全局单一代码源。
- **方案 B（明确排除）**：framework re-export dim2——框架层依赖维度层（倒挂）、data_daemon 被拉入 4000 行 dim2 巨模块，与 SIG 定位冲突。
- **方案 C**：第一步止血（dim2:270 消除混用，3 行）保留。

### 执行计划（新对话接手）

- **第一步（先做）**：① dim2:270 改内联（3 行）消除 F4 混用；② 产出《缠论双份差异审计报告》（13% 差异逐类定性，含 chanlun_config BiConfig diff）；③ 验证（py_compile/ruff/test_396_dim2_engine/test_chanlun_verification 子集）；④ 登记。
- **第二步（另开新号 434）**：方案 A 全量——回迁 dim2 独有增量（含 level_validator 较新版）到 framework → 统一 BiConfig → dim2 删 4 内联区块（~3800 行）改 import → 全量回归（test_396/test_chanlun_verification/test_411 Dim2/data_daemon 冒烟）→ 登记闭环 431 残余待议。

### 记录

- 项目记忆新建 `dim2-chanlun-merge.md`（F1-F6 + 决策 + 执行计划 + 注意事项），MEMORY.md 已登记。
- 本文件 §09。431 §019 残余待议表述保持（待第一步落地后更新）。

## 10 — dim2 缠论双份收敛·第一步落地（F4 混用消除 + 差异审计报告）＋ 关键发现：dim2 内联版背驰检测失效

用户切换新对话执行第一步。按交接计划完成四项，并审计出一个**翻转 F2 假设的关键发现**。

### 实施内容

| # | 动作 | 结果 |
|:--:|---|---|
| 1 | **消除 F4 混用**：`ChanlunLevelValidator._analyze_level`（dim2 :268-:270）删动态 import `from app.engine.framework.chanlun_strategy import ChanlunAnalyzer, ChanlunScorer`，改用模块级内联版 | 3 行改动，dim2 内部统一内联版 |
| 2 | **《缠论双份差异审计报告》**（`002-方案存档/缠论双份差异审计报告（dim2内联vs framework，431残余待议·第一步）.md`） | 4 区块 diff 逐类定性（等价/修复/增强/重命名/dim2 私有优化）＋ 回迁增量清单 |
| 3 | 验证 | py_compile OK / ruff `All checks passed!` / `test_396_dim2_engine` **13 passed** / `test_chanlun_verification` 合成子集 **12 passed** |
| 4 | 登记 | 431 §019 残余待议 → 第一步已落地；本文件 §10；沟通索引；001-目录索引 |

### 审计关键发现（翻转交接 F2）

- **符号层**：dim2 27 ⊇ fw 25（+`_load_precomputed_macd`/`calc_support_resistance`）——**符号超集成立**（F2 前半句对）。
- **函数体层（新发现）**：**dim2 内联版 `DivergenceDetector.detect()` 丢失背驰检测主逻辑**——fw 版完整（`_detect_trend_divergence`→`_detect_trend_backtesting`→`_detect_consolidation_divergence`→`_detect_zhongshu_divergence` + 力度法 `_check_strength_method`/`dual_confirmed`），dim2 版（:1788-:1792）只剩 `if len(strokes) < 4: return None` + 3 行字段赋值，**无 return ⇒ 恒返回 None**；`_detect_trend_*` 等方法在 dim2 仍存在但**零调用点（死代码）**。**根因推断**：411 Phase 5 预计算 MACD 改造时误删背驰调用链。
- 另：dim2 **删除了** `only_last` 快速模式（`BuySellPointDetector.find` / `ChanlunAnalyzer.only_judge_last` / analyze 第 8 步），fw 有（含 342 号修复）。
- **dim2 独有增量（须回迁 fw）**：`_load_precomputed_macd` + `calc_macd(precomputed)`（411 Phase 5）、`calc_support_resistance(df, indicator_ma_df)`（412 B1，与 shared 版单参签名不同）、`fractal_threshold_pct=0.5`（fw=0）、`TrendStructureDetector.detect` 返回 `assumption1/2/3` 键、level_validator 内联份（fw 已删、唯一权威）。
- **对方案 A 的影响**：**强化**——dim2 删 4 区块改 import framework 后，背驰检测（B1）与快速模式（B2/B3/B4）**自动恢复**（修复向），BiConfig 统一 `@dataclass`；dim2 独有增量回迁 fw 保住。第二步（434 号）回迁清单见审计报告 §四-4。

### 验证证据

| 项 | 证据 |
|---|---|
| py_compile | `dim2_structure_engine.py` → OK |
| ruff | → `All checks passed!` |
| 回归 dim2 | `test_396_dim2_engine.py` → **13 passed** |
| 回归缠论合成子集 | `test_chanlun_verification.py`（跳过 DB/集成类 3 项）→ **12 passed** |
| git 状态 | 受控改动＝`M dim2_structure_engine.py`（1 行净删）+ `?? backend/scripts/_audit_dim2_chanlun_diff.py` + 新建审计报告 + 431/沟通记录/索引；未混入用户自有改动 |

### 需知悉

1. **`_analyze_level` 改用内联版后其背驰检测随内联版缺陷一并失效**（切换前动态 import framework 版为完整背驰）——这是第一步消除混用的**已知副作用**，第二步（434 号 dim2 回归 framework）自动恢复；两步之间不建议保留 fw import（违背混用消除目标）。
2. **审计推翻「dim2 较新为权威」的单向假设**：dim2 确有较新增强（411/412/fractal 0.5/assumption 键），但也含功能退化（背驰检测失效、快速模式被删）——方案 A（framework 权威 + 回迁增量）是唯一正确方向。
3. **431 仍 v1.3**（第一步为 fix 批次不 bump）；新审计报告不带编号（第二步 434 号开号后引用）。第二步执行计划不变（434 号：回迁增量 → 统一 BiConfig → dim2 删 4 区块 ~3800 行改 import → 全量回归 → 闭环 431 残余待议）。

**产出**：`dim2_structure_engine.py`（F4 消除 1 行净删）＋ `backend/scripts/_audit_dim2_chanlun_diff.py`（审计脚本）＋ `002-方案存档/缠论双份差异审计报告（dim2内联vs framework，431残余待议·第一步）.md` ＋ 431 §019 更新 ＋ 本文件 §10 ＋ 沟通索引。**残余待议**：dim2 `chanlun_config`/`chanlun_strategy` 双份收敛的**第二步**（另开 434 号执行方案 A）。下一步：**待定**（434 号开号或用户指示）。

## 11 — dim2 缠论双份收敛·第二步（434 号方案 A 全量）——**431 残余待议闭环**

用户指示**开 434 号，启动第二步**。按方案 A 全量落地（framework 权威 + dim2 回归 dim-import-framework），**431 §019 残余待议闭环**。

### 实施内容

| # | 动作 | 结果 |
|:--:|---|---|
| 1 | **开 434 号**（`002-方案存档/434-dim2缠论双份收敛（方案A全量）.md`，v1.0） | 背景/现状（引用审计报告 F1-F8）/方案设计（回迁 R1-R8 + 删除 D1-D5 + 行为变更 B1-B5）/批次/验收 |
| 2 | **批次 1：framework 回迁**——`chanlun_strategy.py`（R1-R5：`_load_precomputed_macd`+`calc_macd(precomputed)`+`DivergenceDetector precomputed`+`analyze` 预计算接线+`fractal_threshold_pct=0.5`）、`trend_structure_detector.py`（R6：`assumption1/2/3` 键）、**重建 `chanlun_level_validator.py`**（R7：dim2 内联份为源）、`shared_support_resistance.py`（R8：合并 dim2 增强版 `indicator_ma_df` 可选参） | 4 文件 py_compile/ruff 通过 |
| 3 | **批次 2：dim2 删 4 区块**（脚本 `_434_batch2_dim2_trim.py`，4000→258 行）改模块级 import framework + shared | import 身份验证（`ChanlunAnalyzer`≡fw、`calc_support_resistance`≡shared）、区块标记零残留 |
| 4 | **批次 3：全量回归** | 见验证表 |
| 5 | **批次 4：登记** | 434 v1.2 + 431 §019 闭环 + 本文件 §11 + 沟通索引 + 001-目录索引 + 记忆 |

### 关键决策（434 §3.2 F8）

**`calc_support_resistance` 归属 = shared 共享服务，非 framework**——dim2 内联版位于 chanlun_strategy 区块尾部（随删除），framework 无此函数，`shared_support_resistance.py` 为单参版（被 data_daemon + `__init__` re-export 消费）。合并增强版（`indicator_ma_df` 可选参）进 shared，data_daemon 单参调用行为不变，消除第四份分叉。

### 行为变更（方案 A 修复向）

| # | 变更 | 说明 |
|:--:|---|---|
| B1 | dim2 背驰检测恒 None → **framework 完整版** | 411 改造误删的背驰调用链恢复；DB-free 实测 `divergence: True` |
| B2 | dim2 恢复 `only_last`/`only_judge_last` | 342 号快速模式恢复；**396 号 F-16 曾判定其为死代码清理，本方案按 framework 权威恢复** |
| B3 | `fractal_threshold_pct` 0→0.5 | F-15 规格 |
| B4 | dim2 `calc_support_resistance` → shared 合并版 | 等价（消费方只读 4 键） |
| B5 | `_analyze_level` 背驰检测恢复 | 第一步副作用消除 |

### 验证证据

| 项 | 证据 |
|---|---|
| `test_396_dim2_engine` | **13 passed**（3 个测试从「dim2 退化固化」更新为「framework 恢复」语义：`only_last`/`only_judge_last`/死代码清理） |
| `test_chanlun_verification` | **15 passed 全量**（autouse fixture mock `_load_precomputed_macd` 保持 DB-free——daemon 运行中跨进程锁挂起的环境处置，见 434 §六） |
| `test_411_pipeline -k "Dim2 or macd or MACD or Phase5 or precomputed"` | **10 passed** |
| `test_main_path_unified` | **10 passed**（412 B5，`calc_support_resistance` 签名兼容） |
| `test_t68_trend_structure` | **3 passed** |
| data_daemon import 面冒烟 | ChanlunAnalyzer/get_chanlun_tags/calc_support_resistance 全解析；framework 27 符号齐全 |
| ruff / py_compile | 全部改动文件通过 |
| git 状态 | `M dim2_structure_engine.py`（4000→258）`M framework/chanlun_strategy.py` `M shared_support_resistance.py` `M test_396` `M test_chanlun_verification` ＋ 新建 `framework/chanlun_level_validator.py`（git 视 D+??）/434 文档/审计脚本/trim 脚本；未混入用户自有改动 |

### 需知悉

1. **test_396 的 3 个测试被更新**（从「only_last 应被移除」改为「framework 权威含 only_last」）——这是方案 A 的既定行为变更（B2），非测试篡改。396 号 F-16 在 dim2 内联版上的「清理死代码」决策与 framework 权威（342 修复）冲突，以 framework 为准。
2. **环境阻塞处置**：framework `analyze()` 对带 `ts_code` 的 df 读预计算 MACD 表（411 Phase 5），daemon 运行中跨进程锁挂起测试——`test_chanlun_verification` 加 mock 保持 DB-free；真实预计算读取由 data_daemon 冒烟覆盖。
3. **dim2 从 4000 行减到 258 行**（删 ~3740 行内联），缠论能力全局单一代码源（framework）；`Dim2StructureEngine` 私有逻辑零改动。

**产出**：434 号（v1.2 ✅ 全量完成）＋ framework 3 文件回迁/重建 ＋ shared 合并 ＋ dim2 删区块（4000→258 行）＋ test_396/test_chanlun_verification 更新 ＋ 431 §019 闭环 ＋ 本文件 §11 ＋ 沟通索引。**431 残余待议（dim2 缠论双份收敛）✅ 闭环**。下一步：**待定**。
