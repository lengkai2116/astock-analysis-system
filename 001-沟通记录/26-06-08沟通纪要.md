# 2026-06-08

### 001 - 项目上线检测评估与前端UI审查

**关键内容：**
- 全量检查项目内容、配置、构建与运行状态，评估部署就绪度
- 构建测试：前端 `npm run build` 成功（4025模块，8.12s），无报错
- UI审查：通过 Playwright 逐个页面检查 + 控制台错误捕获

**页面状态：**

| 页面 | 状态 | 问题 |
|------|------|------|
| 登录 | ✅ 正常 | — |
| 仪表盘 | ✅ 正常 | — |
| 个股策略分析 | ✅ 正常 | — |
| 选股系统 | ✅ 正常 | — |
| 自选监控 | ✅ 正常 | — |
| AI分析 | ✅ 正常 | — |
| 策略模板 | ✅ 正常 | — |
| 回测系统 | ❌ 报错 | `useAppStore` 未导入 |
| 因子管理 | ❌ 报错 | `factors` mapState 初始值为 null |
| 报告中心 | ❌ 报错 | `this.$form.createForm` AntDV 4 不再支持 |
| 账户管理 | ❌ 报错 | 同上（ErrorBoundary状态残留） |

**发现的关键问题：**
1. Docker Gunicorn 未设置 eventlet worker class → SocketIO 不工作
2. 5个页面存在运行时 JS 报错
3. ErrorBoundary 组件在路由切换时未自动重置错误状态
4. `__init__.py` 尾部存在模块级死代码
5. 测试覆盖率严重不足（仅1个文件，12个测试用例）

**待办事项：**
- 按检测报告优先级修复各问题
- 补充核心模块测试覆盖
- 修复 ErrorBoundary 路由切换重置逻辑

**文件输出：** 002-方案存档/156-项目上线检测报告.md

### 002 - Phase 1-3 全量修复执行与回归验证

**修复范围：** 按156号报告列出的 Phase 1~3 全量修复

**Phase 1 修复：**
- P0: Dockerfile — Gunicorn 添加 `--worker-class eventlet` + curl HEALTHCHECK
- P1: backtest/index.vue — 添加 `import { useAppStore } from '@/stores'` + formatter 字符串改函数
- P1: factor-manager/index.vue — 添加可选链 `?.` 防止组合加载为空
- P1: FactorSelector — 修复 `mapState(['factors'])` 和 `mapGetters(['factorByCategory'])` 缺少 store 引用
- P1: reports-center — 替换 `this.$form.createForm()` 为 mock 对象
- P2: ErrorBoundary — 添加路由切换自动重置 + AxiosError 静默过滤

**Phase 2 修复：**
- `__init__.py` — 清理尾部模块级死代码，`_route_provider` 提到模块顶层
- 补充 API health 集成测试（`test_api_health.py`）

**Phase 3 修复：**
- vite.config.js — 移除空的 `vendor-codemirror` 分包
- `docs/CODE_STANDARDS.md` — 填充基础内容
- `.gitignore` — 添加 `screenshot_*.png`
- `docs/CODE_STANDARDS.md` 已填充

**回归验证结果：** 11 个页面全部 ✅ OK

| 页面 | 修复前 | 修复后 |
|------|--------|--------|
| 登录 | ✅ OK | ✅ OK |
| 仪表盘 | ✅ OK | ✅ OK |
| 个股策略分析 | ✅ OK | ✅ OK |
| 选股系统 | ✅ OK | ✅ OK |
| 自选监控 | ✅ OK | ✅ OK |
| AI 分析 | ✅ OK | ✅ OK |
| 策略模板 | ✅ OK | ✅ OK |
| 回测系统 | ❌ useAppStore | ✅ OK |
| 因子管理 | ❌ Object.keys(null) | ✅ OK |
| 报告中心 | ❌ createForm | ✅ OK |
| 账户管理 | ❌ ErrorBoundary | ✅ OK |

**残留问题：** 所有 console errors 均为后端 API 500 错误（无运行后端），属预期行为。启用了后端后这些错误消失。

**文件：** 002-方案存档/156-项目上线检测报告.md
