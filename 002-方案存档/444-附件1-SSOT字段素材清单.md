---
title: 444号附件1——RAW 预计算层 SSOT 字段素材清单（全组全键）
type: 核查存档（444号 ② SSOT 表字段素材）
date: 2026-09-16
source: 444号 §四 SSOT 表（v0.2）
---

# 444号 附件1｜RAW 预计算层字段素材清单

> 来源：`backend/data_daemon.py::_raw2_one()`（3143–3591）产出 `pre_feat_cache.features_json` 的 20 组特征全键清单 + 消费方核查。
> 扁平化规则（`status_engine.py:135 _flatten_pre_feat`）：按组插入顺序平铺、**同名键后者覆盖前者**、**None 值直接丢弃**——这决定了多处"覆盖/丢失"结论。
> 重要：**没有 `relative_strength` 组**（相对强弱在独立表 relative_strength_cache，flat tags 无此键）；**没有 `sector_heat` 组**（板块热度是 sector 组内键 + 独立表 sector_heat_cache）。

## 1. valuation 组（行 3159–3178；生产者 ValuationEngine.compute_tags @ valuation_estimator.py:703）

| 键名 | 生产行号 | 口径/公式摘要 | 单位 | 状态 | 消费方(dim2-7) | 备注 |
|---|---|---|---|---|---|---|
| valuation_level | 3163 | composite 截面百分位分档(5/20/80/95) | 枚举 extreme_low/low/fair/high/extreme_high | 正常 | dim6:323/542, status_engine:402/542 | |
| valuation_deviation | 3163 | composite×20 | 偏离值 | 正常 | dim6(间接), dim7:855 | |
| pe_percentile_5y | 3163 | 近5年 PE 分位 | % | 正常 | dim7 自算不读此键 | |
| pb_percentile_5y / ps_percentile_5y | 3163 | 同 | % | 正常 | dim7 自算不读此键 | |
| fcf_yield | 3163 | FCF/(total_mv×1e4)×100（元/万元换算已修） | % | 正常 | dim7 自算不读此键 | |
| dividend_yield | 3163 | dv_ttm | % | 正常 | dim7 自算不读此键 | |
| revenue_growth | 3163 | 同月跨年营收同比 | % | 正常 | dim7 自算不读此键 | |
| fina_health | 3163 | pass/fail/suspicious | 枚举 | 正常 | dim6:246/296, dim7:857, status_engine:408/533 | |
| composite_rating | 3163 | 五锚加权 [-2,2] | 分 | 正常 | dim7 自算 | |
| asset/earnings/cashflow/adjusted_anchor_rating | 3163 | 四锚评级 | 分 | 正常 | 无消费方(dim 层) | |
| **pe_percentile / pb_percentile / ps_percentile** | 3163(白名单) | — | — | **键错位** | 无 | compute_tags 只返 `_5y` 键，白名单无 `_5y` 的非 `_5y` 三键永不产出。flat `pe_percentile` 实际被 market_stats 同名键（市场级）填充 |
| **roe** | 3163(白名单) | — | — | **键错位** | dim7:862 读 roe（实际拿到 valuation_ext.roe） | compute_tags 返的是 roce_pass 非 roe |
| **roce_pass** | 3163 | 计算了但白名单无此键 | 布尔 | **算对未落库** | — | 白名单 18 键不含 roce_pass |

## 2. sentiment 组（行 3174–3202）

| 键名 | 生产行号 | 口径 | 单位 | 状态 | 消费方 | 备注 |
|---|---|---|---|---|---|---|
| sentiment_phase | 3182-3183 | ms.get_sentiment_phase()，无数据回退全局 `_sentiment_phase_global` 或 'neutral' | 六段论枚举 | 正常（市场级，每股相同） | dim4:675, dim5:291/481, dim6:357, dim7:884, status_engine:558 | |
| bociasi_signal | 3193-3195 | HS300 收盘 5日均值>20日均值→bullish/bearish；<20根→neutral | 枚举 | 正常（市场级） | **无消费方** | dim5 的 BOCIASI 快慢线自算，不读此键 |

## 3. sector 组（行 3204–3218）

| 键名 | 生产行号 | 口径 | 单位 | 状态 | 消费方 | 备注 |
|---|---|---|---|---|---|---|
| sector_heat | 3211 | sr.evaluate→all_heat[industry]['heat_level'] | top_10/top_20/normal/none | 正常（442 缺陷④已修） | dim4:676, dim5:299, dim7:865 | |
| sector_rank | 3213 | 板块排名 | 名次(0起) | 正常 | dim 层无（仅未接线的 emotion_ext 用） | |
| sector_momentum | 3214 | strength 0-1 | 0-1 | 正常 | **无消费方** | |
| is_sector_leader | 3215 | sr.evaluate **不返回此键** | 布尔 | **恒值**(恒 False) | **无消费方** | |

## 4. style 组（行 3221–3242）

| 键名 | 生产行号 | 口径 | 单位 | 状态 | 消费方 | 备注 |
|---|---|---|---|---|---|---|
| style_exposure | 3238 | `_compute_style_exposure(code, {}, df)`（@4255；行业×市值映射；传入 tags={} → vl='fair'） | large_value/large_growth/small_value/small_growth/cyclical/none | 正常 | **无消费方** | |
| size_factor | 3239 | circ_mv>5e10→large_cap；>1e10→mid_cap；否则 small_cap | 枚举 | 恒值倾向（见多口径⑤） | **无消费方** | **A1 已知**：circ_mv 万元未×1e4；且阈值 5e10 万元=5000 亿，与 _compute_style_exposure 的 500 亿（total_mv×1e4>5e10）差 10 倍 |

## 5. timing 组（行 3244–3252）

| 键名 | 生产行号 | 口径 | 单位 | 状态 | 消费方 | 备注 |
|---|---|---|---|---|---|---|
| time_rhythm | 3249 | tre.compute_tags（BOLL 带宽+30日振幅+横盘天数） | approaching_turn/mid_consolidation/early_consolidation/unknown | 正常 | advice_engine.py:439 / advice_builder.py:504（JUD 层） | dim 层无消费 |
| cycle_position / turnover_signal | 3249(白名单) | TimeRhythmEngine.compute_tags **从不产出** | — | **键错位** | 无 | 白名单 3 键实际只有 time_rhythm 落地 |

## 6. volume_price 组（行 3254–3269；`_add_vp_simple_tags` @3611）

| 键名 | 生产行号 | 口径 | 单位 | 状态 | 消费方 | 备注 |
|---|---|---|---|---|---|---|
| kline_pattern | 3261 | `_detect_kline_patterns` 的 pattern_signal（预跌型优先） | 形态名/none | 正常 | dim 层无（dim3 用自带 PatternEngine） | |
| ma_alignment | 3262 | MA5>10>20>60 bullish / 反 bearish / mixed | 枚举 | 正常 | dim2:216, dim3:120 | |
| volume_price_fit | 3263 | 10日价量趋势阈值（价涨≥2%+量增≥15% healthy 等） | healthy/diverging/neutral | 正常 | dim3:98/104/138/276, dim5:314/482, status_engine:210 | |
| gap_type | 3264 | 近5日跳空>0.5% | none/common/breakaway | 正常 | **无消费方** | |
| breakout_attempts | 3265 | 近60日放量1.5×贴近现价次数(min 4) | 次(0-4) | 正常 | **无消费方** | |
| **volume_ratio** | 3266 | `_simple.get('volume_ratio', 1.0)` — `_add_vp_simple_tags` **从不写 volume_ratio** | 倍 | **恒值**(恒 1.0) | dim3:117（量能判定恒"正常"） | **伪事实**：dim3 量能维度全部失真。真生产点在 `_compute_volume_ratio`（data_daemon.py:388，写 daily_basic_cache.volume_ratio）未接进 RAW 特征 |

## 7. chanlun 组（行 3271–3282；生产者 get_chanlun_tags @ chanlun_strategy.py:2464）

| 键名 | 生产行号 | 口径 | 单位 | 状态 | 消费方 | 备注 |
|---|---|---|---|---|---|---|
| buy_sell_point | 3278 | 买点右侧优先(二买>三买>一买)，否则卖点 | 枚举 | 正常（唯一产出键） | dim4:657/5898, derived 各派生 | |
| trend_direction / zhongshu_count / bi_count / duan_count | 3279-3280(白名单) | get_chanlun_tags **只产 buy_sell_point** | — | **键错位**（A2 已知） | 无 | 白名单 5 键实产 1 键 |

## 8. chip 组（行 3284–3291；生产者 cde.get_tags @ chip_distribution_service.py:164）

| 键名 | 生产行号 | 口径 | 单位 | 状态 | 消费方 | 备注 |
|---|---|---|---|---|---|---|
| chip_position | 3288 | 现价 vs 筹码峰：>1.2×峰 above_peak / >0.85× in_peak / below_peak | 枚举 | 正常 | derived.profit_ratio（错误代理） | |
| chip_concentration | 3288 | asr_est=std/mean：>2 concentrating / <1 dispersing / stable | 枚举 | 正常 | dim2:234, dim3:122, dim4:5861 | |
| asr / cyqkl | 3288(白名单) | cde.get_tags **不产** | — | **键错位** | dim4:5865/5874（实际拿到 chip_fund_ext 版本） | 名义两处、实产一处 |

## 9. event 组（行 3293–3305；生产者 EventMonitor.compute_tags @ event_monitor.py:934）

| 键名 | 生产行号 | 口径 | 单位 | 状态 | 消费方 | 备注 |
|---|---|---|---|---|---|---|
| catalyst_event | 3298 | 事件类型映射 | none/earnings/lhb/buyback/breakout/concept… | 正常 | dim6:251/302/1094, dim7:868, status_engine:414/527/536 | |
| catalyst_impact | 3299 | high/medium/low | 枚举 | 正常 | dim 层无 | |
| event_composite_score | 3300 | 方向×最大强度 [-5,5] | 分 | 正常 | dim 层无 | |
| event_details | 3301 | 事件明细列表 | list | 正常 | dim6:1089 | |
| event_risk_factors | 3302 | 风险因子列表 | list | 正常 | dim6:1090 | |

## 10. depth 组（行 3307–3323）— **整组死键**

三个 extractor（tag_extractor.py）实际产出的键（support_resistance/zhongshu_strength/chip_peak/asr/net_lg_amount_5d/margin_cost_price 等）**全部不在本组 8 键白名单内**；而白名单 8 键 extractor 均不产 → 8 键全为 None → 扁平化时**整组被丢弃**。

| 键名 | 生产行号 | 口径 | 状态 | 消费方 | 备注 |
|---|---|---|---|---|---|
| main_force_phase | 3314 | — | **未接线**(恒 None) | dim6:256/309, dim4:5840/5904, status_engine:539/644（全读到空） | **A4 已知**：真生产者在 dim4 PhaseDetectionEngine.compute_tags（JUD 层） |
| phase_confidence | 3317 | — | 未接线(恒 None) | — | 同上，真生产者在 JUD |
| fund_flow / capital_nature | 3318-3319 | — | 未接线(恒 None) | dim4:5850/5905, dim7:874, status_engine | 真生产者在 MainForceScorer.get_tags（chip_strategy.py:1265，JUD 路径） |
| hold_float_ratio | 3314 | — | **无生产者** | — | 全代码无生产者 |
| turnover_rate | 3315 | — | 无生产者 | dim6:262/314（恒走 999 兜底→"低流动性风险"恒判） | ChipIndicators 有 turnover_rate 但 RAW 未传该参数、extractor 也不取 |
| main_force_presence / presence_evidence | 3320-3321 | — | **未接线**(恒 None) | status_engine:644（恒不触发） | 真生产者在 `_compute_main_force_presence`（data_daemon.py:4376，snapshot/JUD 路径） |

## 11. derived 组（行 3332–3354）

| 键名 | 生产行号 | 口径 | 单位 | 状态 | 消费方 | 备注 |
|---|---|---|---|---|---|---|
| price_position | 3335 | buy_sell_point∈(一/二买)→low_zone、∈(一/二卖)→high_zone、否则 mid | 枚举 | **代理**（A5 已知：买卖点代理真实价格位置；buy_sell_point 常 'none' → 恒 mid） | dim2:147, status_engine:426/644 | |
| support_resistance | 3336 | `_depth_f.get('support_resistance','{}')` — depth 组无此键 | JSON 字符串 | **默认兜底**(恒 '{}') | dim 层无（route 层读的是 P2 信号版本，非本键） | |
| volatility_level | 3338 | `volume_ratio and (|vol_ratio-1|>0.5→'high' else 'low')` — volume_ratio 恒 1.0 | 枚举 | **恒值/代理**（A3 已知） | 被 risk_ext 同名键覆盖 | 恒 'low'；vol_ratio=0 时甚至落 int 0 |
| risk_level | 3339 | main_force_phase=='shipping'→HIGH else LOW | 枚举 | **默认兜底**(恒 'LOW') | dim6:236/288（"缠论风险"恒低） | main_force_phase 恒缺；且比较值是 'shipping' 而 JUD 用 'distributing'，双重错位 |
| right_side_confirm | 3341 | 一/二买 and vpf==healthy → strong_confirm else unconfirmed | 枚举 | 代理 | dim6:362（判 `=='否决'`，值域不符**恒不触发**）, dim8:521, status_engine:454 | 值域错位 |
| pattern_signal | 3342 | kline_pattern 拷贝 | 枚举 | 正常（重复键） | dim 层无 | |
| active_signal | 3344 | buy_sell_point（none→None） | 枚举/None | 代理 | status_engine:467（当 **dict** 用 `or {}`，实为 str — 类型错位）, advice_engine:560, advice_builder:206 | |
| state_label | 3346 | `_cl.get('trend_direction','unknown')` — trend_direction 恒缺 | 枚举 | **默认兜底**(恒 'unknown') | dim2:142, status_engine:334/342/634 | 走势面伪事实 |
| trend_alignment | 3347 | trend_direction=='up' and ma bullish → aligned else misaligned | 枚举 | **默认兜底**(恒 'misaligned') | dim7:877(TREND_SCORE), status_engine:635（后者判 'up_aligned'/'down_aligned'，值域又对不上） | 恒 misaligned |
| profit_ratio | 3349 | `_chip_f.get('chip_position', 0)` — 取的是**枚举字符串**不是比例 | — | **代理**（错误代理变量；恒 0 或 'above_peak' 等字符串） | dim6:328(float() 会 ValueError 被吞), dim2:236, dim4:5881, status_engine:640 | **被 chip_fund_ext.profit_ratio 覆盖**（扁平化后者胜出）；仅当 chip_fund_ext 失败时露出脏值 |

## 12. risk_ext 组（行 3357–3382；生产者 calc_geometric @ dim6_risk_engine.py:117）

| 键名 | 生产行号 | 口径 | 单位 | 状态 | 消费方 | 备注 |
|---|---|---|---|---|---|---|
| volatility_percentile | 3360 | 20日波动率历史分位（<2 观测兜底 0.5） | 0-1 | 正常（兜底 0.5 有歧义） | dim6:1138 | |
| support_price / resistance_price | 3363-3364 | calc_geometric（60日高低/MA20+MA60 聚合） | 元 | 正常 | dim6:1113/1114 | **被 structure_ext 同名键覆盖**（见多口径②） |
| dist_to_support_pct / dist_to_resistance_pct / risk_reward / signal_days / dist_to_prev_high_pct | 3365-3369 | calc_geometric | %、倍数、天、% | 正常 | dim6:1115-1119 | |
| atr_14d / atr_pct | 3371-3372 | `_calc_volatility(df, {})`（TR 14日均值） | 元 / % | 正常 | dim6:1132-1136 | |
| **volatility_level** | 3373 | `_calc_volatility(df, {})` — **tags 传空 {}** → `tags.get('volatility_level','medium')` 恒 'medium' | 枚举 | **默认兜底**（A3 已知） | dim6:194/241/292/1137（恒 medium → dim6"波动率非 high"条件恒满足） | **扁平化最终值=本键**（覆盖 derived 的恒 'low'） |

## 13. chip_fund_ext 组（行 3385–3431；生产者 ChipIndicators.calculate_all_indicators @ chip_indicators.py:21）

| 键名 | 生产行号 | 口径 | 单位 | 状态 | 消费方 | 备注 |
|---|---|---|---|---|---|---|
| ssrp | 3407 | 加权平均持仓成本 | 元 | 正常（443 R1 已修） | dim4:621 `_dim_ssrp` | |
| asr | 3408 | ±5% 带内筹码占比 | 0-100 | 正常 | dim4:5865 | flat 最终值（chip 组白名单不产） |
| concentration | 3409 | P95-P5 价差 | 元差 | 正常 | dim4 data_context（3317-3334） | |
| profit_ratio | 3410 | 获利盘比例 | 0-1 | 正常 | dim2:236, dim4:5881, dim6:328, status_engine:640 | **覆盖 derived.profit_ratio** |
| cyqkl | 3411 | K线穿越筹码强度 | 值 | 正常 | dim4:5874 | |
| rsi | 3412 | RSI14 | 0-100 | 正常（算对键名错） | **无**（dim3:124 读的是 `rsi14` 键——**键名错位**，拿不到） | 多口径⑨ |
| fund_flow_strength | 3420 | get_sub_scores.total/10 | 0-1 | 正常 | **无消费方**（dim4 用自己的 get_fund_flow_strength） | |
| chip_transfer | 3427 | main_force_phase∈(accumulating,shipping)→phase else 'neutral' | 枚举 | **默认兜底**(恒 'neutral') | **无消费方** | main_force_phase 恒缺 |
| control_degree | 3428 | `_depth.get('hold_float_ratio')` 恒 None | — | **默认兜底**(恒 None→扁平化丢弃) | **无消费方** | |

## 14. fund_5d_ext 组（行 3434–3457）

| 键名 | 生产行号 | 口径 | 单位 | 状态 | 消费方 | 备注 |
|---|---|---|---|---|---|---|
| net_lg_5d | 3448 | moneyflow 近5日 net_lg_amount 求和 | 元 | 正常 | dim4:508（`_dim_fund`） | |
| net_lg_5d_positive_ratio | 3450 | 5日中净流入日占比 | 0-1 | 正常 | dim4:513 | |
| net_lg_5d_consecutive | 3455 | 连续净流入天数 | 天 | 正常 | **无消费方** | |

## 15. valuation_ext 组（行 3460–3486）

| 键名 | 生产行号 | 口径 | 单位 | 状态 | 消费方 | 备注 |
|---|---|---|---|---|---|---|
| pe_ttm / pb / ps_ttm / total_mv | 3474-3477 | daily_basic 最新行 | 倍数 / 倍数 / 倍数 / 万元 | 正常 | dim 层无（dim7 自己从表重算） | |
| roe | 3480 | fina_indicator 最新 roe | % | 正常 | dim7:862（flat `roe` 实际来自本键） | 补 valuation 白名单空位 |
| roce / grossprofit_margin | 3481-3482 | 同上 | % | 正常 | **无消费方** | |

## 16. cost_ext 组（行 3489–3501）— **类型错位高危**

| 键名 | 生产行号 | 口径 | 单位 | 状态 | 消费方 | 备注 |
|---|---|---|---|---|---|---|
| main_force_cost | 3496 | `_calc_main_force_cost` 返回**整个 dict** `{cost_price, distance_pct, near_cost}` | dict（内含 元/%/布尔） | 正常计算但**类型错位** | dim4 `_dim_ssrp`:634 与 `identify_phase`:3554 均做 `mfc > 0`（期望标量）→ **dict>0 抛 TypeError** → compute_tags 整体失败 → dim4 阶段判定静默降级 unknown | 生产=dict、消费=标量；测试 test_443_r2_cost_ext.py 用 float 传参**掩盖了此错位**；而 snapshot_assembler.py:91 又按 dict 取 `.get('cost_price')` —— 同一键双口径 |
| margin_cost_price | 3497 | `_calc_margin_cost_price` 返回 `{cost_price, distance_pct}` dict | dict | 同上类型错位风险 | 同上 | |

## 17. volume_ext 组（行 3504–3522）

| 键名 | 生产行号 | 口径 | 单位 | 状态 | 消费方 | 备注 |
|---|---|---|---|---|---|---|
| vol_ma5 / vol_ma10 / vol_ma20 | 3510-3512 | vol 滚动均值 | 手/股 | 正常 | **无消费方**（仅 dim1 门禁完备性检查） | |
| volatility_20d | 3516 | 20日收益年化波动率 | 0-1 | 正常 | 无消费方 | |
| roc_20 | 3520 | 20日涨跌幅 | % | 正常 | 无消费方 | |

## 18. emotion_ext 组（行 3525–3547）— **整组断链**

| 键名 | 生产行号 | 口径 | 单位 | 状态 | 消费方 | 备注 |
|---|---|---|---|---|---|---|
| emotion_temperature | 3533 | calc_emotion_temperature，但入参 `limit_up_count=0 恒`、`sealing_rate=50 恒`（sentiment 组只存了 phase/bociasi_signal，**丢掉了 ms.get_sentiment_phase() 返回的 limit_up_count/sealing_rate**） | 0-100 | **断链+默认兜底**（上限被压至约 67，权重 25% 项恒为 0/50） | **无消费方** | **B1 已知**：dim5:499 用自己拷贝的副本函数重算（emotion_temperature.py 与 dim5 内 272 行双份实现），温度事实双口径 |
| market_emotion | 3543 | sentiment_phase 拷贝 | 枚举 | 拷贝 | 无消费方 | |
| sector_emotion | 3544 | sector_rank<=10→hot | 枚举 | 正常 | 无消费方 | |
| stock_emotion | 3545 | volume_price_fit 映射 | 枚举 | 拷贝 | 无消费方 | |

## 19. structure_ext 组（行 3553–3564）

| 键名 | 生产行号 | 口径 | 单位 | 状态 | 消费方 | 备注 |
|---|---|---|---|---|---|---|
| support_price / resistance_price | 3558-3559 | `calc_support_resistance`（shared_support_resistance.py:12，另一套实现） | 元 | 正常 | dim6:1113/1114（**扁平化后 dim6 实际消费本版本**，risk_ext 的被覆盖） | 多口径②：两套支撑阻力实现并存 |
| indicator_status | 3562 | `f"ma={_ma},trend={_trend}"` — trend_direction 恒缺 → 恒 `trend=` | 字符串 | 部分兜底 | dim2:246, dim_adapter:124 | |

## 20. market_stats 组（行 3567–3573；生产者 `_precompute_market_stats` @ 2777，指标计算 2829-2945）

全市场级、**每股写入相同值**（ponytail 注释明示）。426 P0-1 后任一无源数据则整组不落库。

| 键名 | 生产行号 | 口径 | 单位 | 状态 | 消费方 | 备注 |
|---|---|---|---|---|---|---|
| ma20_ratio | 2829 | 全市场 close>SMA20 占比 | 0-1 | 正常 | dim5 BociasiQuadrant（data_context['market_stats']） | |
| turnover_percentile / limit_ratio / erp_percentile / margin_trend / pe_percentile | 2851/2867/2900/2918/2936 | 各自市场级聚合 | 0-1 | 正常 | 同上 | |
| rsi_percentile | 2884 | 全市场 RSI14 中位分位 | 0-1 | 正常但**概念错位** | dim2:256（**当个股 RSI 分位**展示"RSI分位xx"） | 市场级被当个股级 |
| pe_percentile | 2936 | 全市场平均 PE 分位 | 0-1 | 正常但**概念错位** | dim 层无直接消费 | **与 valuation 白名单键同名** → flat `pe_percentile`=市场级，个股估值分位事实被顶掉 |

---

# 重点摘要

## 一、"伪事实"键清单（按 8 现状面归类，应删除或修复）

**价格位置面**
- `price_position`（derived:3335）：买卖点代理，buy_sell_point 常 'none' → 恒 mid（A5）
- `support_resistance`（derived:3336）：恒 '{}' 字符串
- `profit_ratio`（derived:3349）：取 chip_position 枚举字符串/0，纯代理（好在被 chip_fund_ext 覆盖）

**走势面**
- `trend_direction`/`zhongshu_count`/`bi_count`/`duan_count`（chanlun 白名单）：永不产出（A2）
- `state_label`（derived:3346）：恒 'unknown'
- `trend_alignment`（derived:3347）：恒 'misaligned'；且 status_engine:635 判 'up_aligned' 值域再错位
- `cycle_position`/`turnover_signal`（timing 白名单）：永不产出

**持仓面**
- depth 组 8 键全部恒 None 被扁平化丢弃（main_force_phase A4 / hold_float_ratio 无生产者 / turnover_rate 无生产者 / main_force_presence 真生产者在 JUD）
- `chip_transfer`（恒 neutral）、`control_degree`（恒 None）

**资金面**
- `fund_flow`/`capital_nature`（depth 恒 None；真生产者 JUD MainForceScorer.get_tags）
- `volume_ratio`（volume_price:3266 恒 1.0）→ **dim3 量能判定全失真**

**风险面**
- `volatility_level`：三口径全废（derived 恒 low / risk_ext 恒 medium，扁平化取 medium）→ dim6 波动率条件恒满足
- `risk_level`（derived:3339 恒 LOW，且比较值 'shipping' 与 JUD 'distributing' 不一致）
- `right_side_confirm`：dim6:362 判 '否决' 值域不符恒不触发

**环境面**
- `emotion_temperature`（emotion_ext:3533）：limit_up/sealing 入参断链恒默认，且整组无消费（dim5 自算副本）
- `bociasi_signal`：无消费

**信号面**
- `active_signal`：status_engine:467 当 dict 用（`or {}`）实为 str —— 类型错位
- `pattern_signal`：kline_pattern 拷贝、无消费

## 二、多口径分叉（同一概念多份实现/多键）

1. **volatility_level 三口径**（_add_vp_simple_tags 真波动率 / derived volume_ratio 代理 / risk_ext 恒 medium）——A3
2. **support_price/resistance_price 两套实现**：calc_geometric（risk_ext）vs calc_support_resistance（structure_ext，扁平化胜出；dim2 还自算第三遍）——且 dim6 消费的是被覆盖的 structure_ext 版本
3. **profit_ratio 双键**：derived（枚举/0）被 chip_fund_ext（真 0-1）覆盖
4. **情绪温度双副本**：emotion_temperature.py vs dim5:272 拷贝函数（B1），RAW 版还断链
5. **大小盘两口径**：size_factor（circ_mv>5000亿，且 A1 万元未换算）vs _compute_style_exposure.is_large（total_mv×1e4>500亿）——阈值差 10 倍、基准不同
6. **asr/cyqkl 名义双键**：chip 组白名单（不产）vs chip_fund_ext（实产）
7. **main_force_cost/margin_cost_price 类型双口径**：RAW 存 dict、dim4 消费标量（TypeError）、snapshot_assembler 又按 dict 读
8. **fund_flow 概念双生产**：flat 恒缺；dim4 PhaseDetectionEngine 内部自算另一套；dim7/dim4 的 tags.get('fund_flow') 恒 None
9. **rsi 键名三岔**：chip_fund_ext.rsi（生产）≠ dim3 读的 rsi14 ≠ dim2 读的市场级 rsi_percentile（概念错位）

## 三、现状面事实字段缺口（无可靠生产点）

- **持仓面**：控盘度/持仓比例（hold_float_ratio 无生产者）、换手率（turnover_rate 无生产者）、主力在场证据（main_force_presence 真生产在 JUD `_compute_main_force_presence`，RAW 未接线）
- **资金面**：主力阶段（main_force_phase/phase_confidence 未接线 A4）、资金性质（capital_nature 未接线）、量比（volume_ratio 恒值）
- **走势面**：缠论趋势/中枢/笔/段（A2 未产出）、时间节奏 cycle_position/turnover_signal（白名单空）
- **价格位置面**：真实价格位置（无独立计算，全靠 A5 代理）、支撑阻力 JSON（derived 恒 '{}'，唯一真实现 risk_ext 被 structure_ext 覆盖）
- **风险面**：波动率档位（三口径皆伪）、风险等级（恒 LOW）
- **环境面**：情绪温度的涨停/封板成分（sentiment 组丢弃 limit_up_count/sealing_rate，算对未落库）
- **信号面**：rsi14 个股键（chip_fund_ext 产的是 `rsi`，dim3 读 `rsi14` —— 键名错位导致个股 RSI 事实断链）、相对强弱（relative_strength 不在 pre_feat，仅独立表，flat tags 无此键）

## 附：需要立刻修的运行时级问题（非口径问题）

1. **cost_ext 类型错位**（最高优先级）：dim4 `_dim_ssrp`(634)/`identify_phase`(3554) 对 dict 做 `> 0` 抛 TypeError → 整只股票的 PhaseDetectionEngine 静默失败。测试用 float 掩盖，生产写 dict。修法：生产侧存 `cost_price` 标量，或消费侧按 dict 取 `cost_price`。
2. **active_signal 类型错位**：status_engine:467 对 str 做 `or {}`。
3. **volume_ratio 恒 1.0**：`_add_vp_simple_tags` 未产出该键（真量比在 `_compute_volume_ratio` 管道，data_daemon.py:388，写 daily_basic_cache.volume_ratio——存在真生产点却未接进 RAW 特征）。
