---
title: 332总纲整体实施计划（生产引擎修复与现状判定引擎落地排期）
type: 计划
date: 2026-08-15
status: 🟢 已存档 v1.0（08-15 组5 排期确认：P0→S1→S2→S3 四阶段整体工作计划，P0 待用户发起实施）
---

# 338 — 332 总纲整体实施计划

> **定位**：332 号总纲（现状判定引擎统一方案 v3.0）及分档方案（333/334/335/336/337）的**整体实施排期与任务分解**。讨论阶段已完成（组1-4 定稿，见 332-1 计划清单），本计划为实施阶段的工作指导。
>
> **执行顺序**：**P0 → S1 → S2 → S3**（332 §六 依赖链；S1 依赖 P0 真实维度输入，S2 依赖 S1，S3 依赖 S2）。

---

## 一、阶段总览

| 阶段 | 名称 | 内容 | 前置 | 预估会话 | 依据 |
|:--:|---|---|:--:|:--:|---|
| **P0** | 生产引擎健康修复 | P2 六引擎修复（量价/因子 bug + 筹码静默 + Vibe 剔除）+ 管道顺序调整 + 全量重算覆盖验证 | —（可立即发起） | 1-2 | 332 §六 P0 + 333 落地项 1/3/6 |
| **S1** | 口径统一 | state_reason 修复 + 弹窗共识五维过渡 + 降级共享函数 + 顶层字段对齐 | P0 完成 | 1 | 332 §六 S1 + 336 §八 |
| **S2** | 生产流水线模块化 | status_engine.py（L1/L0/L2/成品仓）+ 配置化 + 快照切换 + 清理 | S1 完成 | 2-3 | 332 §六 S2 + 334/335/336/337 |
| **S3** | 呈现层对齐 | 弹窗九维灯 + 个股页七维模板 + 颜色校准 + conflict 展示 | S2 完成 | 1 | 332 §六 S3 + 334 §四/336/337 §五 |

---

## 二、P0 生产引擎健康修复（332 §六 P0 + 333 落地项 1/3/6）

| # | 任务 | 位置 | 验收标准 |
|:--:|---|---|---|
| P0.1 | 修量价信号 NameError（`market_env` 未定义） | `signal_computation_service.py:945`（改 `market_context.get('market_env')`） | 量价信号不再静默失败 |
| P0.2 | 修因子信号 ImportError（`get_data_manager` 不存在） | `signal_computation_service.py:2461`（改正确导入 `app.data.DataManager`） | 因子信号正常产出 |
| P0.3 | 筹码静默失败加异常暴露与回退 | signal_computation_service.py（筹码计算 try/except 链） | 失败可观测（日志/回退值） |
| P0.4 | Vibe 剔除出 P2 | `signal_computation_service.py:1007-1009` 调用移除 + `:3111` 方法删除 | P2 收敛为 6 引擎；analyze/diagnose 无 Vibe 报错 |
| P0.5 | P2 引擎输出结构统一（每引擎产出"维度状态+证据+置信度"） | signal_computation_service.py 各信号函数 | 输出可被 L1 直接消费（334 §2.2 协议） |
| P0.6 | P2 全量重算 + 覆盖度验证 | `data_daemon.py:3327` `_precompute_strategy_signals` 重跑 | 全市场 strategy_signal_detail：量价/因子/筹码覆盖 ≥90%（Vibe 不纳入） |
| P0.7 | **管道顺序调整**（P4 标签先算当日 → P2 后算，杜绝消费昨日标签） | `data_daemon.py:1531/1556` `_run_precompute` 顺序 | P2 消费标签日期基线 = 当日（333 落地项 1） |
| P0.8 | SQLite 轻量预调（daemon 内部写串行化单写队列 + P2 计算缓存扩展） | data_daemon + analysis_cache | API 并发无 database is locked（333 落地项 3） |

**验证**：①覆盖度 ≥90% ②603897.SH 五维（chanlun/volume_price/chip/emotion/factor）不再因信号缺失 neutral ③P2 输出结构满足协议 ④新增 P2 信号引擎单测 ⑤现有测试全量通过。

## 三、S1 口径统一（332 §六 S1 + 336 §八 过渡口径）

| # | 任务 | 位置 | 验收标准 |
|:--:|---|---|---|
| S1.1 | 修 state_reason 覆盖 bug（降级后无条件覆盖真实原因） | `advice_builder.py:197-198`（按降级类型保留具体原因） | 603897 显示"盈亏比不足"而非"方向冲突" |
| S1.2 | 弹窗摘要层共识改五维推导（过渡口径，消灭 100% vs 67%） | `cross_validate.py:417/:824/:888`（consensus/verdict/opportunity_summary） | 弹窗/个股页共识一致（五维口径） |
| S1.3 | 仓位降级抽共享函数 | cross_validate + advice_builder（`_apply_degradation` 共享） | 双模块同路径 |
| S1.4 | 顶层字段结构对齐（stop_loss 等） | cross_validate/advice_builder 输出层 | 弹窗/个股页字段同构 |
| S1.5 | `_extract_real_dimensions` 字段补全（rotation_state/sector 提取） | `cross_validate.py:102-110` | 603897 sentiment 维证据完整 |

**验证**：603897.SH 双端点 4 处残留差异全消（摘要层/买点证据/sentiment 证据/state_reason）；现有测试通过。

## 四、S2 生产流水线模块化（332 §六 S2 + 334/335/336/337 全量落地）

| # | 任务 | 依据 | 验收标准 |
|:--:|---|---|---|
| S2.1 | 新建 `status_engine.py`（生产环节核心） | 332 §5.1 | 分析/诊断消费成品仓，无双源路径 |
| S2.1a | L1 十维判定（维度输出协议 {state/evidence/conclusion/plain/light}） | 334 §2 | 每维产出协议素材 |
| S2.1b | 信号注册表接入（5 类 × 触发/验证/生命周期）+ 信号生命周期计算 | 334 §5 | 注册表判定可单测 |
| S2.1c | L0 分级（L0a regulatory/ST、L0b soft_risks 系数、L0c 已延伸持有期） | 335 §2/§5 | 分级正确作用于状态/仓位 |
| S2.1d | L2 聚合（维度共识计票 + conflict_evidence 扩展 2 条） | 336 §2/§4 | 共识=维度推导、冲突 6 条规则 |
| S2.1e | 成品仓输出（status_snapshot 行 + advice_params） | 337 §3/§4 | 全市场日频成品落库 |
| S2.2 | 阈值配置化：`config/status_engine.yaml` + `config/signal_registry.yaml`（版本化可回滚） | 334 §6 / 336 §7 | 阈值从 yaml 读取，arbiter 硬编码迁移 |
| S2.3 | arbiter 调整：删 P2 deep 硬否决（改强提示）+ 阈值 0.67 迁移 yaml | 335 §3 / 336 §3 | test_319/321 更新后通过 |
| S2.4 | ST 检测迁移（chip_pre_filter → event_monitor） | 335 §4 | L0a ST 拦截生效 |
| S2.5 | 快照字段来源切换（treemap_snapshot 的 consensus_rate/right_side_confirm 改 L1/L2 输出）+ 新增 conflict 列 | 336 §5 | 快照无 tags 轻量投票路径 |
| S2.6 | status_snapshot 建表 + 日终生成（S1 管道扩展） | 337 §3/§4 | 全市场行数=快照行数、字段完整 |
| S2.7 | advice_builder 重构为套算函数（apply_advice_params）+ 七维模板组装函数（build_seven_dim_report） | 337 §5/§6 | 套算 vs 完整重跑一致（抽样 10 只） |
| S2.8 | signal_records 扩展右侧信号类型字段（验证环挂接注册表） | 334 §6.1 | 新信号记录关联注册表类型 |
| S2.9 | 清理：选股器路由（screener.py/screener_strategy_integration）+ 个股页 Step 0 重复风控 + 前端入口；保留 pipeline 熔断/标签来源 | 335 §6 | make check 通过、无残留 import |

**验证**：①analyze/diagnose 均消费成品仓（代码评审无绕过）②10 只样本（含 300759/301119/000066）口径完全一致 ③共识分布统计 → 校准深蓝/浅蓝阈值（336 §9）④全量 pytest 通过。

## 五、S3 呈现层对齐（332 §六 S3）

| # | 任务 | 依据 | 验收标准 |
|:--:|---|---|---|
| S3.1 | 弹窗九维灯（读 status_snapshot dim_states） | 334 §3 / 337 §7 | 弹窗展示九维+因子辅助灯 |
| S3.2 | 个股页七维模板渲染（build_seven_dim_report 输出） | 334 §4 / 337 §5 | 七段模板+大白话+红绿灯 |
| S3.3 | 颜色校准（按共识分布更新深蓝/浅蓝阈值） | 336 §3.2 | 可入场占比 5-10% |
| S3.4 | conflict_evidence 风险边界展示增强（含新增 2 条规则） | 336 §4 | 弹窗/个股页展示矛盾 |
| S3.5 | 图例/分享文案更新（"三处一致优先于颜色激进"） | 336 §6 | 文案同步 |

**验证**：弹窗/个股页渲染一致；历史问题股抽样正常；前端无报错。

## 六、实施风险与前置

| 风险/前置 | 说明 | 应对 |
|---|---|---|
| P0 重算耗时 | P2 全量重算（1.6h 历史基线） | 333 落地项 3（计算缓存扩展）先行；后台执行 |
| SQLite 锁竞争 | daemon 重算期与 API 并发 | P0.8 写串行化 + 现有 WAL/busy_timeout |
| 测试环境锁冲突 | daemon 预计算期 pytest 部分失败（已知 13 项） | 验证避开预计算期或暂停 daemon |
| 前端原型模式 | _ui-prototype 无构建体系 | S3 直接改 HTML 原型（惯例） |
| 关联待办不阻塞 | 个性地图/国债收益率源/量价收敛 | 按各自时机（338 起编号或既有待办），不阻塞本计划 |

## 七、状态跟踪

| 阶段 | 状态 | 沟通纪要 | 完成日期 |
|:--:|:--:|---|---|
| P0 生产引擎健康修复 | ✅ 已完成 | 26-08-15 纪要 007 | 2026-08-15 |
| S1 口径统一 | ✅ 已完成 | 26-08-15 纪要 008 | 2026-08-15 |
| S2 生产流水线模块化 | ✅ 已完成 | 26-08-15 纪要 009 | 2026-08-15 |
| S3 呈现层对齐 | ✅ 已完成 | 26-08-15 纪要 010 | 2026-08-15 |

---

## 八、关联文档

- **332（现状判定引擎统一方案 v3.0）— 本计划的依据总纲**（§六 落地步骤）
- 333（P2 原料供给与双渠道稳定性）— 落地项 1/3/6 随 P0、项 2/4 随 P0/S2
- 334（L1 维度判定细则与信号注册表）— S2 的 L1/信号注册表/阈值管理
- 335（L0 风险分级实施细则）— S2 的 L0/清理/ST 迁移/deep 调整
- 336（L2 聚合与闸门收敛细则）— S2 的共识/冲突/快照切换/颜色校准
- 337（成品仓落库与实时套算机制）— S2 的 status_snapshot/套算层/模板组装
- 332-1（待讨论计划清单）— 讨论阶段完成记录（组1-4 定稿）

**文档版本**: v1.0（08-15 组5 排期确认：P0→S1→S2→S3 四阶段整体实施计划）
**编制日期**: 2026-08-15
