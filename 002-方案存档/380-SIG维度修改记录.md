---
title: SIG维度修改记录
type: 修改记录
date: 2026-08-29
version: v1.0
status: 📋 持续更新中（每次修改后记录）
related:
  - 377-SIG环节系统配置核查与问题清单（v1.2）
  - 378-SIG环节8维度能力梳理与输出确认（v1.1）
  - 379-SIG维度审查缺失数据记录（v1.0）
---

# 380 — SIG维度修改记录

> **目的**：记录SIG环节各维度引擎的每次修改内容，包括修复的bug、新增的功能、架构调整等，方便后续查看和追溯。
>
> **使用规则**：每完成一次代码修改后，按模板记录。只记录修改内容，不记录能力梳理（见378号），不记录缺失数据（见379号）。

---

## 一、修改记录模板

```
### [日期] 维度N：[修改标题]

**修改类型**：Bug修复 / 功能新增 / 架构调整 / 配置变更
**修改文件**：
**修改内容**：
**验证方式**：
**关联问题**：
```

---

## 二、修改记录

### 2026-08-29 维度2：BiZhongshuFinder min_width绝对价格改为百分比

**修改类型**：Bug修复
**修改文件**：`dim2_structure_engine.py`
**修改内容**：
- `BiZhongshuFinder.__init__`：`min_width: float = 0.5` → `min_width_pct: float = 0.005`
- `BiZhongshuFinder.find`：`if high - low < self.min_width` → `if avg_price > 0 and (high - low) / avg_price < self.min_width_pct`
- `ZhongshuAnalyzer.__init__`：`min_width: float = 1.0` → `min_width_pct: float = 0.005`
- `ZhongshuAnalyzer` 3处 `self.min_width` → `self.min_width_pct * (high + low) / 2`

**验证方式**：7只股票测试（11元~1297元），全部产出17-20个中枢（修复前为0）
**关联问题**：377号P2（中枢0%产出）

---

### 2026-08-29 维度2：chanlun_result key名不匹配修复

**修改类型**：Bug修复
**修改文件**：`dim2_structure_engine.py`
**修改内容**：
- `evaluate()` L3878：`chanlun_result.get('trend_direction')` → `chanlun_result.get('trend')`
- `_assess_vs_zhongshu` L3908：`chanlun_result.get('zhongshu_list', [])` → `chanlun_result.get('zhongshu', chanlun_result.get('zhongshu_list', []))`
- `audit` L3892：`chanlun_result.get('trend_direction', '无')` → `chanlun_result.get('trend', '无')`

**验证方式**：7只股票测试，chanlun_direction正确输出up/down（修复前全为'未知'）
**关联问题**：377号P2（trend方向永远'未知'）

---

### 2026-08-29 维度2：latest_close字段缺失修复

**修改类型**：Bug修复
**修改文件**：`dim2_structure_engine.py`
**修改内容**：
- `evaluate()` 新增 `_latest_close` 变量从analyzer.klines或chanlun_result.zhongshu提取
- `_assess_vs_zhongshu` 新增 `latest_close` 参数，替代 `chanlun_result.get('latest_close', 0)`
- 调用处 `_assess_vs_zhongshu(tags, dims, chanlun_result)` → `_assess_vs_zhongshu(tags, dims, chanlun_result, _latest_close)`

**验证方式**：7只股票测试，vs_zhongshu正确判定价格在中枢上方/内部/下方（修复前全为'数据不足'）
**关联问题**：377号P2（vs_zhongshu永远'数据不足'）

---

### 2026-08-29 维度2：诊断日志清理

**修改类型**：配置变更
**修改文件**：`dim2_structure_engine.py`
**修改内容**：
- 3处 `logger.warning(f"[dim2诊断]...")` → `logger.debug(f"dim2 ...")`
- 移除冗余的 `# 诊断：检查analyze返回值` 注释块

**验证方式**：语法检查通过，生产日志不再输出诊断信息
**关联问题**：临时调试日志清理

---

### 2026-08-29 维度2：多级别联立接入

**修改类型**：功能新增
**修改文件**：`dim2_structure_engine.py`
**修改内容**：
- `evaluate()` 新增 Step 2b：调用 `ChanlunLevelValidator().validate(_diag_df)`
- `status_description` 新增 `level_cross_score`/`level_confirmed`/`level_adjustment`/`level_trends` 字段
- `audit` 新增"多级别联立"条件（级别趋势一致性校验）

**验证方式**：000001.SZ测试：日线down/周线up→cross_score=0.63，confirmed=False（趋势冲突正确识别）
**关联问题**：378号维度2改进项"多级别联立接入"

---

### 2026-08-29 维度2：123法则接入

**修改类型**：功能新增
**修改文件**：`dim2_structure_engine.py`
**修改内容**：
- `evaluate()` 新增 Step 2c：调用 `TrendStructureDetector().detect(_diag_df)`
- `status_description` 新增 `trend_structure_signal`/`trend_structure_strength`/`trend_structure_detail` 字段
- `audit` 新增"123法则"条件（趋势结构信号检测）

**验证方式**：000001.SZ测试：signal=higher_low（突破趋势线+回调不创新低）
**关联问题**：378号维度2改进项"123法则接入"

---

### 2026-08-29 维度2：ChanlunLevelValidator key名修复

**修改类型**：Bug修复
**修改文件**：`dim2_structure_engine.py`
**修改内容**：
- `_analyze_level` L263：`result.get('trend', {}).get('direction', 'unknown')` → 判断类型后分别处理（字符串直接取值，字典取direction）
- `_analyze_level` L262：`result.get('zhongshu_list', [])` → `result.get('zhongshu', result.get('zhongshu_list', []))`

**验证方式**：ChanlunLevelValidator单独测试，日线/周线/月线分析正常产出
**关联问题**：多级别联立接入时发现的附加bug

---

### 2026-08-29 维度2：df变量作用域修复

**修改类型**：Bug修复
**修改文件**：`dim2_structure_engine.py`
**修改内容**：
- `evaluate()` 中 `df` 变量重命名为 `_diag_df`，避免 `'df' in dir()` 检测在异常路径下的不确定性
- `calc_support_resistance` 调用改为 `_diag_df if _diag_df is not None and not _diag_df.empty else None`

**验证方式**：语法检查通过，calc_support_resistance正确接收DataFrame
**关联问题**：代码健壮性改进

---

### 2026-08-29 维度2：中枢上下沿作为支撑阻力

**修改类型**：功能新增
**修改文件**：`dim2_structure_engine.py`
**修改内容**：
- `_assess_vs_support_resistance` 重构：优先使用中枢上下沿（缠论核心支撑阻力），降级使用calc_support_resistance
- 新增参数 `chanlun_result` 和 `latest_close`，从最近中枢提取支撑/压力位
- 返回值新增 `source` 字段（'zhongshu'/'calc'/'none'）标识数据来源
- `evaluate()` 调用处更新为 `_assess_vs_support_resistance(geo, chanlun_result, _latest_close)`

**验证方式**：000001.SZ测试：中枢支撑10.62元(-8.8%)/压力11.13元(-4.5%)（source=zhongshu），vs旧calc支撑11.39元(-2.3%)/压力11.75元(+0.9%)
**关联问题**：378号维度2改进项"中枢上下沿作为支撑阻力"

---

### 2026-08-29 维度2：笔特征提取结构强度

**修改类型**：功能新增
**修改文件**：`dim2_structure_engine.py`
**修改内容**：
- 结构强度评分从单一ChanlunScorer改为综合评分：缠论评分70% + 方向一致性15% + 幅度因子15%
- 方向一致性：近6笔中同向笔占比（越高趋势越明确）
- 幅度因子：近6笔平均幅度/幅度范围（幅度越大走势越有力）

**验证方式**：000001.SZ测试：base=0.500, consistency=0.500, amp=0.771, enhanced=0.541
**关联问题**：378号维度2改进项"笔特征提取结构强度"

---

### 2026-08-29 维度2：背驰信号利用

**修改类型**：功能新增
**修改文件**：`dim2_structure_engine.py`
**修改内容**：
- `status_description` 新增 `divergence_type`/`divergence_strength`/`divergence_detail` 字段
- 从 chanlun_result.divergence 提取背驰类型（趋势背驰/盘整背驰）和强度
- 背驰作为趋势衰竭确认信号

**验证方式**：测试股票均无背驰（divergence_type=none），符合当前市场状态
**关联问题**：378号维度2改进项"背驰信号利用"

---

### 2026-08-29 维度2：买卖点信号利用

**修改类型**：功能新增
**修改文件**：`dim2_structure_engine.py`
**修改内容**：
- `judgment` 新增 `bsp_signal`（buy/sell/mixed）和 `bsp_count` 字段
- 买卖点信号增强灯色判定：纯买点→green，纯卖点→red，混合→mixed
- 买卖点作为结构位置的确认信号

**验证方式**：601318.SH测试：buy=2, sell=1→bsp_signal=mixed（符合当前下行趋势中的反弹买点）
**关联问题**：378号维度2改进项"买卖点信号利用"

---

### 2026-08-29 维度2：vs_indicator从indicator表读取（381号P0-1）

**修改类型**：功能新增
**修改文件**：`dim2_structure_engine.py`
**修改内容**：
- `_assess_vs_indicator` 重构：新增`indicator_data`参数，从indicator_other（rsi14/kdj_k/kdj_d/kdj_j）和indicator_macd（macd_dif/macd_dea/macd_hist）读取
- 输出增加evidence字段，包含RSI超买超卖/KDJ金叉死叉/MACD多空状态
- `evaluate()` 中新增indicator表读取逻辑（从compute_cache.db的indicator_other和indicator_macd表查询）
- 降级路径：indicator表无数据时回退到tags读取

**验证方式**：000001.SZ测试：RSI=65(中性)，KDJ(78/64/104)(金叉向上)，MACD(0.158/0.134)(多头，柱体为正)
**关联问题**：381号方案P0"vs_indicator数据源修正"

---

### 2026-08-29 维度2：vs_ma从indicator_ma读取（381号P0-2）

**修改类型**：功能新增
**修改文件**：`dim2_structure_engine.py`
**修改内容**：
- `_assess_vs_ma` 重构：新增`ma_data`参数，从indicator_ma表读取ma5/ma10/ma20/ma60
- 输出增加具体MA数值、排列判定（bullish/bearish/mixed）、BIAS_20乖离率
- `evaluate()` 中新增indicator_ma表读取逻辑
- 降级路径：indicator_ma无数据时回退到tags的ma_alignment标签

**验证方式**：000001.SZ测试：MA5=11.62>MA10=11.39>MA20=11.39>MA60=10.96，多头排列，BIAS_20=+2.3%
**关联问题**：381号方案P0"vs_ma数据源修正"

---

### 2026-08-29 维度2：vs_chip移至dim4（381号P0-3）

**修改类型**：架构调整
**修改文件**：`dim2_structure_engine.py`
**修改内容**：
- `evaluate()` 中vs_chip调用替换为静态输出（"筹码分析由dim4负责"）
- dim2的5子维度从vs_zhongshu/vs_ma/vs_sr/vs_chip/vs_indicator调整为vs_zhongshu/vs_ma/vs_sr/vs_indicator
- vs_chip由dim4（Dim4ChipFundEngine，5817行）负责，dim4已有完整筹码分析能力

**验证方式**：4只股票测试，dim2不再输出筹码数据，dim4独立产出筹码分析
**关联问题**：381号方案P0"vs_chip维度归属修正"

---

### 2026-08-29 维度2：vs_zhongshu增加distance_pct/duration/type（381号P1-1）

**修改类型**：功能增强
**修改文件**：`dim2_structure_engine.py`
**修改内容**：
- `_assess_vs_zhongshu` 重构：增加distance_pct（距中枢百分比）、duration_days（中枢持续天数）、zhongshu_type（中枢类型）、zhongshu_count（活跃中枢数量）
- 增加evidence证据链输出

**验证方式**：000001.SZ测试：distance_pct=4.67, duration_days=81, zhongshu_count=17
**关联问题**：381号方案P1"vs_zhongshu增强"

---

### 2026-08-29 维度2：vs_sr增加source/freshness/risk_reward（381号P1-2）

**修改类型**：功能增强
**修改文件**：`dim2_structure_engine.py`
**修改内容**：
- `_assess_vs_support_resistance` 重构：增加source标注（中枢上下沿/历史高低点）、freshness时效性（活跃/历史）、risk_reward盈亏比
- 输出结构增加support_price/resistance_price/freshness/risk_reward字段

**验证方式**：000001.SZ测试：source=中枢上下沿, freshness=历史, risk_reward=-0.5
**关联问题**：381号方案P1"vs_sr增强"

---

### 2026-08-29 维度2：chanlun_direction增加笔定理四种状态（381号P1-3）

**修改类型**：功能增强
**修改文件**：`dim2_structure_engine.py`
**修改内容**：
- status_description新增chanlun_phase字段
- 基于最后一笔方向+趋势方向判定笔定理四种状态：(1,1)向上笔延续·未病 / (-1,1)向下笔延续·未病 / (1,0)向上笔可能结束·欲病 / (-1,0)向下笔可能结束·欲病

**验证方式**：000001.SZ测试：phase=(-1,1) 向下笔延续·未病
**关联问题**：381号方案P1"chanlun_direction增强"

---

### 2026-08-29 维度2：divergence多算法透明化（381号P1-4）

**修改类型**：功能增强
**修改文件**：`dim2_structure_engine.py`
**修改内容**：
- divergence输出增加divergence_multi_algo字段，透明化多算法结果（area/slope/amp）
- 从DivergenceDetector的details中提取各算法结果

**验证方式**：语法检查通过（当前测试股票无背驰，无法验证多算法输出）
**关联问题**：381号方案P1"divergence增强"

---

### 2026-08-29 维度2：buy_sell_points增加级别+确认（381号P2-1）

**修改类型**：功能增强
**修改文件**：`dim2_structure_engine.py`
**修改内容**：
- 新增buy_sell_points_detail字段，每个买卖点包含type/buy/sell/level/confirmed信息
- 级别从字符串中提取（日线/周线/60分钟等），默认日线
- 形态学买卖点默认confirmed=True

**验证方式**：语法检查通过（当前测试股票无买卖点，结构已就绪）
**关联问题**：381号方案P2"buy_sell_points增强"

---

### 2026-08-29 维度2：chanlun_strength透明化（381号P2-2）

**修改类型**：功能增强
**修改文件**：`dim2_structure_engine.py`
**修改内容**：
- 新增chanlun_strength_components字段，包含chanlun_score/consistency/amp_factor三个组成部分
- 评分公式透明化：缠论评分70% + 方向一致性15% + 幅度因子15%

**验证方式**：000001.SZ测试：score=0.541, components={chanlun=0.500, consistency=0.500, amp=0.771}
**关联问题**：381号方案P2"chanlun_strength透明化"

---

## 三、修改统计

| 维度 | Bug修复 | 功能新增 | 功能增强 | 架构调整 | 配置变更 | 合计 |
|------|--------|---------|---------|---------|---------|------|
| 维度2 | 5 | 6 | 6 | 1 | 1 | 19 |
| **合计** | **5** | **6** | **6** | **1** | **1** | **19** |

---

**文档版本**: v1.2
**编制日期**: 2026-08-29
**编制人**: MiMoCode
**v1.1 更新**: 新增381号方案P0三项实施记录
**v1.2 更新**: 新增381号方案P1+P2六项实施记录（vs_zhongshu增强/vs_sr增强/chanlun_direction笔定理/divergence多算法/buy_sell_points级别确认/chanlun_strength透明化）

---

## 三、维度3修改记录（2026-08-29）

### 2026-08-29 维度3：evaluate()数据源修复（T15）

**修改类型**：架构调整
**修改文件**：`dim3_vp_engine.py`
**修改内容**：
- `evaluate()`从从`dims.get('vp',{}).get('state')`和`tags.get('volume_ratio')`读取 → 调用`VolumePriceStrategy.analyze()`使用StageDetector/VolumeStateAnalyzer/VolumePriceRelationAnalyzer/VolumePriceSignalGenerator自身组件分析
- 新增`strategy = VolumePriceStrategy()`实例化和`analysis = strategy.analyze(df)`调用
- Stage字段名映射修正：`stage_dict.get('name')` → `stage_dict.get('current_stage')`（Stage.to_dict()返回current_stage），`valuation` → `aux_price_position.zone`

**验证方式**：4只股票（000001.SZ/600519.SH/002594.SZ/000002.SZ）全部通过，stage/vol_state/relation/signal正确输出
**关联问题**：382号B1（evaluate()从tags/dims读取，未消费自身组件）

---

### 2026-08-29 维度3：状态机输出接入status_description（T16）

**修改类型**：功能新增
**修改文件**：`dim3_vp_engine.py`
**修改内容**：
- 新增7个状态机字段到status_description：`state_machine_state`/`state_machine_direction`/`state_machine_confidence`/`state_machine_label`/`state_machine_position`/`state_machine_holding`/`state_machine_evidence`
- 从`signal_output`读取状态机的方向/置信度/标签/仓位/持有周期/证据链

**验证方式**：4只股票全部输出状态机详细信息（如000001.SZ: direction=HOLD, confidence=0.55, label=买入, position=60%, holding=2-4周）
**关联问题**：382号B5（状态机28字段仅使用~8个）

---

### 2026-08-29 维度3：evidence字段补全（T17）

**修改类型**：功能新增
**修改文件**：`dim3_vp_engine.py`
**修改内容**：
- 新增6个evidence字段：`divergence_evidence`/`volume_energy_evidence`/`pattern_evidence`/`granville_evidence`/`state_machine_evidence`/`evidence`
- evidence内容包含具体数值和判定条件（如"量比=1.50，量能趋势=STABLE"）

**验证方式**：31/32字段有值（vol_state_institutional因置信度不足为空）
**关联问题**：382号B4（status_description无evidence字段）

---

### 2026-08-29 维度3：量价背离定义修正（T18）

**修改类型**：Bug修复
**修改文件**：`dim3_vp_engine.py`
**修改内容**：
- 原实现：价格创新高+MACD未新高（价格动量背离）
- 修正为：价格上涨+成交量萎缩（量价背离）+价格下跌+成交量放大（恐慌抛售）
- 代码：`price_rising = close_vals[-1] > close_vals[-5:].mean()` + `vol_shrinking = vol_vals[-1] < vol_vals[-5:].mean() * 0.7`
- 同时保留RelationAnalyzer的背离检测作为MACD辅助确认

**验证方式**：4只股票背离检测正确（000001.SZ无量价背离但RelationAnalyzer检测到top背离）
**关联问题**：382号B3（背离检测实为MACD背离非量价背离）

---

### 2026-08-29 维度3：量价三定律输出（T19+T23）

**修改类型**：功能新增+增强
**修改文件**：`dim3_vp_engine.py`
**修改内容**：
- T19：将`relation_dict.get('three_laws')`输出到`status_description.three_laws`
- T23：`_apply_three_laws()`从35行增强至~70行：
  - 定律1（供求定律）：增加价格/量能变动百分比（如"量缩价涨→供给不足（价格+0.8%，量能-22.7%）"）
  - 定律2（因果定律）：增加盘整蓄势检测（横盘天数+60日振幅）+天量天价/地量地价信号
  - 定律3（投入产出定律）：新增匹配度判定（价格变动% vs 量能变动%）
  - 返回值从{sync_score, notes}扩展为{supply_demand, cause_effect, effort_result, sync_score, notes}

**验证方式**：4只股票三定律输出正确（如000001.SZ: 供求=量缩价涨→供给不足，因果=横盘11天→盘整蓄势，投入产出=正常）
**关联问题**：382号B6（三定律已计算未输出）

---

### 2026-08-29 维度3：健康度评分移至JUD（T20）

**修改类型**：架构调整
**修改文件**：`dim3_vp_engine.py`
**修改内容**：
- `evaluate()`移除`hs`（0-10分）和`sl`（强健康/健康/中性/弱/严重背离）计算
- 移除`vp_score`/`ve`/`ms`/`cs`/`is_`/`dp`/`pattern_deviation`多因子加权公式
- status_description移除`health_score`字段
- judgment保留`score = sm_confidence * 10`供JUD消费

**验证方式**：status_description中无health_score/hs字段，judgment.score正确输出
**关联问题**：382号B2（健康度评分属于JUD范畴）

---

### 2026-08-29 维度3：格兰威尔8种从detect_all()移除（T21）

**修改类型**：清理
**修改文件**：`dim3_vp_engine.py`
**修改内容**：
- `EnhancedPatternDetector.detect_all()`的checks列表中移除8种格兰威尔检测（格兰维尔买点1-4/卖点1-4）
- 方法本身保留（`_is_granville_buy1`~`_is_granville_sell4`），仅从检测列表移除
- 理由：格兰威尔已通过`_classify_granville()`单独输出到`granville`字段，避免重复

**验证方式**：detect_all() checks列表从88条减至80条，语法验证通过
**关联问题**：382号B10（格兰威尔重复检测）

---

### 2026-08-29 维度3：ValuationZones缺少@dataclass装饰器（Bug修复）

**修改类型**：Bug修复
**修改文件**：`dim3_vp_engine.py`
**修改内容**：
- `ValuationZones`类缺少`@dataclass`装饰器，导致`ValuationZones(short_30d=short, ...)`调用报错"takes no arguments"
- 在`class ValuationZones:`前添加`@dataclass`

**验证方式**：StageDetector.detect()正常执行，阶段判定结果正确（如000001.SZ: UPTREND_ACTIVE）
**关联问题**：dim3阶段〇审查发现的隐藏bug

---

### 2026-08-29 维度3：多时间框架联动（T24）

**修改类型**：功能新增
**修改文件**：`dim3_vp_engine.py`
**修改内容**：
- evaluate()中新增周线分析：日线OHLCV重采样为周线（resample('W')）
- 对周线数据调用`VolumePriceStrategy.analyze()`获取周线分析结果
- 新增7个字段到status_description：`multi_timeframe_consistency`/`multi_timeframe_weekly_stage`/`multi_timeframe_weekly_direction`/`multi_timeframe_weekly_confidence`/`multi_timeframe_weekly_pattern`/`multi_timeframe_weekly_divergence`
- 一致性判定：日线+周线方向一致→信号强化，方向冲突→需谨慎

**验证方式**：4只股票日线+周线分析全部通过，方向一致性判定正确
**关联问题**：382号P3（多时间框架联动）

---

### 修改记录汇总（维度3）

| 修改类型 | 数量 | 涉及任务 |
|---------|------|---------|
| 架构调整 | 2 | T15 evaluate()数据源修复、T20 健康度评分移至JUD |
| 功能新增 | 4 | T16 状态机接入、T17 evidence补全、T19+T23 三定律、T24 多时间框架 |
| Bug修复 | 2 | T18 背离定义修正、ValuationZones @dataclass |
| 清理 | 1 | T21 格兰威尔移除 |
| **合计** | **9** | T15-T24 |

---

**文档版本**: v1.3
**编制日期**: 2026-08-29
**编制人**: MiMoCode
**v1.3 更新**: 新增维度3修改记录（T15-T24共9项：架构调整2/功能新增4/Bug修复2/清理1）

---

## 四、维度4修改记录（2026-08-29）

### 2026-08-29 维度4：evaluate()增强版（T26-T29）

**修改类型**：功能新增+Bug修复
**修改文件**：`dim4_chip_fund_engine.py`
**修改内容**：
- **T26 evidence字段补全**：新增phase_evidence/fund_flow_evidence/cost_evidence/signal_evidence/retail_institution_evidence/margin_evidence/crowding_evidence/evidence共8个evidence字段
- **T27 PhaseDetectionEngine输出暴露**：新增pde_price_position/pde_trend/pde_conflict/pde_vote_ratio + phase_confidence详细字段
- **T28 辅助函数组件化**：phase/fund_flow改为PhaseDetectionEngine优先+tags降级；cost_structure/signal/retail_institution/margin保留tags读取+数值补全
- **T29 信号输出接入**：新增signal_type/signal_evidence/cost_asr/cost_cyqkl/cost_profit_ratio/cost_quality/crowding_level/crowding_score详细字段
- **Bug修复**：PHASE_UNKNOWN常量未定义→添加`PHASE_UNKNOWN = 'unknown'`

**验证方式**：4只股票（000001.SZ/600519.SH/002594.SZ/000002.SZ）PhaseDetectionEngine+CrowdingFactor分析通过，000001.SZ输出lifting(拉升期)/置信度0.661/资金流入/拥挤度MODERATE
**关联问题**：384号方案B1-B5（evaluate从tags读取/evidence缺失/输出不丰富）

---

### 修改记录汇总（维度4）

| 修改类型 | 数量 | 涉及任务 |
|---------|------|---------|
| 功能新增 | 3 | T26 evidence补全、T27 PDE输出暴露、T29 信号接入 |
| 架构调整 | 1 | T28 辅助函数组件化 |
| Bug修复 | 1 | PHASE_UNKNOWN未定义 |
| **合计** | **5** | T26-T29 |

---

**文档版本**: v1.4
**编制日期**: 2026-08-29
**编制人**: MiMoCode
**v1.4 更新**: 新增维度4修改记录（T26-T29共5项：功能新增3/架构调整1/Bug修复1）

---

## 五、维度5修改记录（2026-09-15）

### 2026-09-15 维度5：_bociasi_slowline数据源修正（T32/B1）

**修改类型**：Bug修复
**修改文件**：`dim5_emotion_engine.py`
**修改内容**：
- evaluate() L948-951：slowline数据源从`ecm.get_cached_daily_basic(ts_code)`改为合并`daily+daily_basic`
- 新增逻辑：将`daily_basic.pe_ttm`注入`daily`的DataFrame，使slowline同时拥有close列和pe_ttm列
- 降级路径：若daily不可用，仍用daily_basic（仅ERP可用，股债位置差跳过）

**验证方式**：语法检查通过，slowline不再因KeyError返回NEUTRAL
**关联问题**：384号B1（慢线股债位置差功能完全失效）

---

### 2026-09-15 维度5：板块热度功能恢复（T33/B2）

**修改类型**：Bug修复
**修改文件**：`dim5_emotion_engine.py`
**修改内容**：
- evaluate() L998-1009：板块热度从`tags.sector_heat`（data_daemon预计算）读取
- 移除`SectorRotationModel().evaluate()`直接调用（缓存未预热问题）
- 保留SectorRotationModel仅用于获取行业名称

**验证方式**：语法检查通过，sector_heat不再全部为'none'
**关联问题**：384号B2（板块热度功能完全失效）

---

### 2026-09-15 维度5：个股情绪数据源修正（T34/B9）

**修改类型**：Bug修复
**修改文件**：`dim5_emotion_engine.py`
**修改内容**：
- `_assess_stock_emotion()`重构：三级降级路径
  1. 优先：`tags.emotion_ext.stock_emotion`（data_daemon预计算：positive/negative/neutral）
  2. 降级1：`dims.volume_price.status_description.state_machine_direction`（dim3引擎输出）
  3. 降级2：`dims.vp.state`（旧格式兼容）
- 情绪标签从'健康'/'关注'改为'积极'/'消极'/'中性'（与emotion_ext对齐）
- audit条件同步更新：'极度消极'改为'消极'

**验证方式**：语法检查通过，个股情绪有三级降级保障
**关联问题**：384号B9（个股情绪数据源不正确）

---

### 2026-09-15 维度5：情绪温度参数补全（T35/B4）

**修改类型**：功能增强
**修改文件**：`dim5_emotion_engine.py`
**修改内容**：
- evaluate()新增MarketSentimentService接入：获取limit_up_count和sealing_rate
- 新增板块排名sector_rank传递给calc_emotion_temperature
- 7维度权重因子从2个真实数据扩展到5个真实数据

**验证方式**：语法检查通过，温度计算使用更多真实数据
**关联问题**：384号B4（情绪温度7维度中仅2个有真实数据）

---

### 2026-09-15 维度5：时间节奏接入（T36/B3）

**修改类型**：功能新增
**修改文件**：`dim5_emotion_engine.py`
**修改内容**：
- evaluate()新增`_time_rhythm(df_daily)`调用
- 时间节奏结果输出到status_description.time_rhythm
- 输出值：approaching_turn(变盘临近)/mid_consolidation(盘整中)/early_consolidation(盘整初期)/unknown

**验证方式**：语法检查通过
**关联问题**：384号B3（时间节奏功能定义但从未调用）

---

### 2026-09-15 维度5：status_description增强+evidence补全（T37/B7+B8）

**修改类型**：功能增强
**修改文件**：`dim5_emotion_engine.py`
**修改内容**：
- status_description从8个字段扩展到25+字段，分A/B/C三层
- 新增market_evidence/sector_evidence/stock_evidence三个evidence字段
- 新增温度等级(_temp_level)、时间节奏显示、行业名称/排名
- 兼容旧字段（market/sector/stock/bociasi_quick/bociasi_slow/quadrant/plain）

**验证方式**：语法检查通过
**关联问题**：384号B7+B8（输出字段不足+无evidence）

---

### 2026-09-15 维度5：消费emotion_ext预计算数据（T38/B5）

**修改类型**：架构优化
**修改文件**：`dim5_emotion_engine.py`
**修改内容**：
- evaluate()优先从`tags.emotion_ext`读取预计算数据
- 减少重复计算：不再重新计算已由data_daemon预计算的emotion数据

**验证方式**：语法检查通过
**关联问题**：384号B5（emotion_ext已预计算但dim5未消费）

---

### 2026-09-15 维度5：慢线新增股债收益差指标（T39/B6）

**修改类型**：功能新增
**修改文件**：`dim5_emotion_engine.py`
**修改内容**：
- `_bociasi_slowline()`新增第3个指标：股债收益差（yield_spread = dividend_yield - bond_yield）
- 阈值：>2%=BULLISH(0.65), >1%=BULLISH(0.50), <0%=BEARISH(0.60), <0.5%=BEARISH(0.45)
- 降级：无dividend_yield时用ERP近似
- 数据要求从`len(df) < 60`放宽到`len(df) < 20`
- 返回值新增yield_spread和sbs_signal字段

**验证方式**：语法检查通过
**关联问题**：384号B6（慢线4指标中股债收益差未实现）

---

### 2026-09-15 维度5+market_sentiment_service：涨停生态补全（T40）

**修改类型**：功能新增
**修改文件**：`dim5_emotion_engine.py` + `market_sentiment_service.py`
**修改内容**：
- MarketSentimentService.get_sentiment_phase()新增：first_board_count(首板家数) + broken_rate(炸板率)
- 六段论映射阈值与知识库对齐：使用broken_rate(炸板率)替代sealing_rate(封板率)作为判定条件
  - 冰点：涨停<15+炸板率>50%+连板≤2
  - 发酵：涨停30-80+炸板率<30%+连板≥3
  - 高潮：涨停>80+炸板率<20%

**验证方式**：语法检查通过
**关联问题**：384号K1（涨停生态5指标仅实现3个→现实现4个）

---

### 2026-09-15 维度5：dividend_yield传入slowline（M4修复）

**修改类型**：Bug修复
**修改文件**：`dim5_emotion_engine.py`
**修改内容**：
- evaluate()中两处`_bociasi_slowline()`调用新增`dividend_yield`参数
- 数据来源：`emotion_ext.get('dividend_yield') or tags.get('dividend_yield')`
- 效果：慢线第3指标"股债收益差"从无法计算→正常计算

**验证方式**：语法检查通过
**关联问题**：379号M4（dividend_yield已存在于pre_feat_cache但dim5未传入）

---

### 修改记录汇总（维度5）

| 修改类型 | 数量 | 涉及任务 |
|---------|------|---------|
| Bug修复 | 4 | T32(B1数据源), T33(B2板块热度), T34(B9个股情绪), M4(dividend_yield) |
| 功能新增 | 4 | T36(时间节奏), T37(evidence), T39(股债收益差), T40(涨停生态) |
| 功能增强 | 2 | T35(温度参数), T37(status_description增强) |
| 架构优化 | 1 | T38(emotion_ext消费) |
| **合计** | **11** | T32-T41 + M4 |

---

**文档版本**: v1.5
**编制日期**: 2026-09-15
**编制人**: MiMoCode
**v1.5 更新**: 新增维度5修改记录（T32-T41共10项：Bug修复3/功能新增4/功能增强2/架构优化1）

---

## 六、维度6修改记录（2026-09-15）

### 2026-09-15 维度6：消除assess_risk_level的dims循环依赖（T42/B1）

**修改类型**：Bug修复
**修改文件**：`dim6_risk_engine.py`
**修改内容**：
- `_assess_risk_level()` L228-235：移除对`dims.get('risk')`的读取
- 原代码存在循环依赖：dims['risk']由StatusEngine从dim6输出生成，读取它会形成循环
- 修复后：仅依赖tags和l0进行风险等级判定

**验证方式**：3只真实股票验证通过
**关联问题**：385号B1

---

### 2026-09-15 维度6：status_description新增evidence字段（T43/B4）

**修改类型**：功能新增
**修改文件**：`dim6_risk_engine.py`
**修改内容**：
- evaluate()新增`risk_evidence`证据链字段
- status_description从10字段扩展到20+字段
- 新增字段：risk_level/risk_detail/support_price/resistance_price/rr_value/rr_level/volatility_level/atr_14d/event_count/event_summary/risk_evidence等

**验证方式**：000001.SZ测试：risk_evidence输出完整证据链
**关联问题**：385号B4

---

### 2026-09-15 维度6：EventMonitor修复+事件结果输出（T44/B5）

**修改类型**：Bug修复+功能新增
**修改文件**：`dim6_risk_engine.py`
**修改内容**：
- EventMonitor实例化改为`EventMonitor(data_manager=self._get_dm())`
- status_description新增event_count和event_summary字段
- 事件检测结果现在正确输出到status_description

**验证方式**：000001.SZ测试：event_count=1, event_summary=["突破: 站上60日线+20日新高"]
**关联问题**：385号B5

---

### 修改记录汇总（维度6）

| 修改类型 | 数量 | 涉及任务 |
|---------|------|---------|
| Bug修复 | 4 | T42(B1循环依赖), T44(B5 EventMonitor), T45(B6风险源对齐), T46(B7数据源) |
| 功能新增 | 3 | T43(B4 evidence字段), T48(CSCVValidator标记), T49(事件结构化输出) |
| **合计** | **7** | T42-T49 |

---

**文档版本**: v1.6
**编制日期**: 2026-09-15
**编制人**: MiMoCode
**v1.6 更新**: 新增维度6修改记录（T42-T49共7项）

---

## 七、维度7修改记录（2026-09-15）

### 2026-09-15 维度7：status_description重构（B5/B6修复）

**修改类型**：功能增强
**修改文件**：`dim7_valuation_engine.py`
**修改内容**：
- evaluate()输出从10字段扩展到20字段
- 值类型从格式化字符串改为原始类型（如valuation_level从"高估（composite=-1.0）"改为"high"）
- 新增4个锚评分字段（asset_anchor_rating/earnings_anchor_rating/cashflow_anchor_rating/adjusted_anchor_rating）
- 新增ps_percentile_5y/roce_pass字段
- 新增valuation_evidence证据链字段
- valuation_level_cn单独字段（中文估值等级）

**验证方式**：27/27字段逐条核对通过
**关联问题**：386号B5+B6

---

### 修改记录汇总（维度7）

| 修改类型 | 数量 | 涉及任务 |
|---------|------|---------|
| 功能增强 | 1 | evaluate()输出重构(B5+B6) |
| 架构调整 | 3 | B1(build_composite_percentile改DataManager), B2(build_fcf_percentile改daily_cache), B3(build_potential_percentile_tables改DataManager) |
| **合计** | **4** | — |

---

**文档版本**: v1.7
**编制日期**: 2026-09-15
**编制人**: MiMoCode
**v1.7 更新**: 新增维度7修改记录（4项：功能增强1+架构调整3）
