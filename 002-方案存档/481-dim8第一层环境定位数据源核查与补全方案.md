# 481号｜dim8 第一层环境定位数据源核查与补全方案（最终确认版）

- **状态**：v2.0 · 最终确认版（2026-09-24）· 用户目标=「最完整解决 + 后续平稳持续运行」
- **关联**：437 总纲第一层「环境定位」｜479-dim8 现状描述改造｜464-dim1 输出项核查
- **覆盖**：①大盘趋势 ②行业情况 ③个股行业位置 全接入 + 2 个深水 bug 根治 + 运行态可靠性

---

## 〇、设计目标（用户拍板口径）

1. **最完整**：①大盘指数趋势 ②行业收益/轮动/资金 ③个股行业位置百分位 三项全部落地，dim8 第一层环境定位成为完整三段。
2. **平稳持续运行**：
   - 根治既有 2 个数据链路 bug（板块定位句缺失根因、市场状态句缺失根因）；
   - 不依赖 daemon 实时重算即可生效（daemon 停态的探针/测试场景也稳定输出）；
   - 沿用 437「有数据则显、缺则降级」——任一数据源缺失时该句空串、不产 NULL、不阻塞其余段；
   - 写入/读取均幂等、分库路由一致，与 daemon 生命周期解耦。

---

## 一、三项诉求 + 2 个深水 bug 完整清单

| # | 项 | 根因/数据 | 处置 | 改动面 |
|---|---|---|---|---|
| B1 | 板块定位句缺失 | `get_cached_sector_heat` SQL `LIMIT 1` 只取 1 行业（sector_heat_cache 是 long 格式） | 改 `WHERE stat_date=(MAX(...))` 取全部行业 | 1 行 SQL |
| B2 | 市场状态句缺失 | `pre_feat['market_stats']={}`：daemon 停态全局缓存空 → RAW-2 写空 | 装配优先直接读 `market_stats_cache` 表（fallback 才用 pre_feat） | dim1 装配 + 兜底 |
| ① | 大盘指数趋势 | BenchmarkService 沪深300/上证 K 线（实测 76 行） | 新增 `_index_trend_sentence`：近 5/20/60 日涨跌 + 60 日线上下 | dim8 新句 |
| ② | 行业完整情况 | SectorAnalysisService 全套现成（收益/排名/轮动/资金）但只接 strategy_analyze | 接进 dim8 环境定位段，取代/补充 sector_heat 档位 | dim8 新句 |
| ③ | 个股行业位置 | 原料在（Stock.industry 成分股 + daily_cache），无现成字段 | 新增行业成分股收益百分位计算 + 产字段 | 新逻辑 + daemon 重算 + 定稿 |

---

## 二、各子项实施明细

### B1｜板块热度读取 bug 根治（先决，1 行 SQL）
**文件**：`app/data/enhanced_cache_manager.py` `get_cached_sector_heat` (:1499-1519)
**改动**：
```python
# 原：SELECT * FROM sector_heat_cache ORDER BY stat_date DESC LIMIT 1
row = self._query_shard('sector_heat_cache',
    "SELECT * FROM sector_heat_cache "
    "WHERE stat_date = (SELECT MAX(stat_date) FROM sector_heat_cache)", [])
```
**影响**：dim1 装配 `data_context['sector_heat']` 恢复 109 行业 → `_sector_position_sentence`（dim8:1109）出「板块定位」句；dim5 情绪段板块热度档位同步恢复。
**验证**：单测——多行业表下 `get_cached_sector_heat()` 返回全部行业且含目标行业；600519 白酒定位句复现。

### B2｜市场状态句数据装配根治（daemon 停态也稳）
**根因**：dim1 `data_context['market_stats']` 读 `pre_feat_cache` 快照，而快照由 daemon 全局 `_market_stats_cache` 写入；daemon 停/重启过即空。
**改动**：`dim1_signal_engine.py` category3 的 market_stats 组读取后，增加**表兜底**——若 `loaded_data['market_stats']` 为空 dict，改为 `dm.cache.get_cached_market_stats()` 直读 `market_stats_cache` 表（该表 daemon 已持久化，实测有 09-21 及历史多行）。
**影响**：`_market_state_sentence`（dim8:1076）恢复「全市场MA20占比/涨停/封板率」广度句；dim5 四象限的 breadth 也受益。
**验证**：单测——daemon 停态下装配 market_stats 非空、广度句复现。

### ① 大盘指数趋势句（接线，`_index_trend_sentence`）
**数据**：`BenchmarkService.get_index_daily(000300.SH/000001.SH)`，取近 1/5/20/60 日，拼一句：
> `大盘：沪深300 4517(当日-0.6%)，近20日-0.8%/近60日-9.3%，60日线下方(偏弱)`

- 判趋势：近60日>0 且站上60日线→偏强；近60日<-5%→偏弱；其间→中性。
- 缺数据降级空串。
**接入**：并入 dim8 summary 前置环境段（`_market_state_sentence` 旁新增，`build_seven_dim_report` 装配）。

### ② 行业完整情况句（接线，`_sector_full_sentence`）
**数据**：`SectorAnalysisService.get_sector_context(ts_code)` 现成（sector_1/5/20d_return、rank、rotation_state、moneyflow）。拼：
> `白酒：近20日-2.2%，行业中游(排名第N)，轮动NEUTRAL，资金净流出`

- rotation_state 中文化：LEADING→领涨/LAGGING→落后/STRENGTHENING→增强/WEAKENING→转弱/NEUTRAL→中性。
- 取代/补充现有 `_sector_position_sentence`（保留前者 heat_level 档位信息，叠加收益/轮动）。
**验证**：600519 白酒收益/轮动/资金句复现。

### ③ 个股行业位置百分位（能力缺口，独立扩产）
**数据**：行业成分股集合（`Stock` 表 `WHERE industry=?`）+ 各成分股 `daily_cache` 近 20 日收益。
**产出**：新增字段（daemon 预计算，随 RAW 或独立步骤写 pre_feat/sector 组）：
> `industry_ret_rank`：目标股近20日收益在行业全部成分股中的名次/百分位
> `industry_position`：top25%/中上/中游/中下/bottom25% 五档

**接入**：dim8 环境定位段一句 `白酒板块内涨幅第8/15(前53%)`。
**注意**：涉及全行业成分股取数（数百只×近20日收益），**需 daemon 重算、独立开号**走「取数→产字段→定稿话术」三步；本号先登记架构，实施单列。
**运行态**：缺数据降级空串，不阻塞；计算放 daemon 侧（避免实时逐票查）。

---

## 三、平稳运行的工程保障（贯穿全子项）

1. **与 daemon 解耦**：B2 走表兜底、① 走 BenchmarkService 独立查询、② 走 SectorAnalysisService 独立服务——均不依赖 daemon 内存缓存，daemon 停态探针/测试稳定输出。
2. **437「缺则降级」**：每个新句独立 try/空值判断，任一源缺失仅空串，不拖垮整个 summary。
3. **分库路由一致**：B1/B2 已走 `_query_shard` 分库路由（compute_cache.db），B1 修复仅改 SQL 不改路由，避免"总库空壳表"历史陷阱（426 号教训）。
4. **幂等**：B1 改读不改写；② 只读服务不改状态；③ 写路径沿用 `INSERT OR REPLACE`。
5. **加字段=加行**：① ② 作为独立函数挂 summary 前置，不动引擎契约键（前端无破坏）。

---

## 四、落地顺序（一次成型，减少反复）

1. **B1**（1 行 SQL）→ 单测 → 复验板块定位句 + dim5 热度。
2. **B2**（装配表兜底）→ 单测 → 复验市场状态广度句。
3. **①大盘趋势 + ②行业完整**两句接入 dim8 → 单测 + 600519 全链实证。
4. **③个股行业位置**：独立子号记录架构，走 daemon 重算 + 字段 + 定稿话术三步，本号登记关联。

---

## 五、验证口径

- 单元：B1/B2/①/② 各新函数 + 既有 dim8 回归（479/479-5/437a/420 全过）。
- 集成：600519.SH（及 000002 等）dim8 summary 前后置——环境定位三段齐全（大盘/板块/相对强弱）。
- 运行态：daemon **停态**下重跑探针，三段稳定输出（B2 关键）。
- 回归：`pytest backend/tests` 全绿，无 445 冻结键（评分/灯色）改动。

---

## 六、范围边界

- **不改 445 冻结**：共振强度、评分/灯色键一律不动；本方案只补数据类现状句（「因」层）。
- **③不并入本号落地**：能力缺口独立开号，本号定架构 + ①② 接线 + B1/B2 根治。
- **不新增前端契约键**：①②挂 summary 前置，前端无需改动即可读到增强后的环境定位段。
