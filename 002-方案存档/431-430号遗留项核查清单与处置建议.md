---
title: 430号遗留项核查清单与处置建议
type: 核查报告（含处置建议）
date: 2026-09-13
version: v1.3（首版核查存档 + v1.1 补录 §9 未列入发现与未启动规划项 + v1.2 补录 JUD 报告 §6.2/§1.1 残留文案 + v1.3 补录存量待议事项：残留 env 键漂移 7 键 / `.env.local` 孤儿文件 / B1 行号引用漂移）
status: 📋 核查存档（逐项处置——已落地批次与剩余项见文末「处置进度」）
related:
  - 430-SIG环节配置全面核查报告
  - 391-390号方案落地实施全面核查报告
  - 338-332总纲整体实施计划（生产引擎修复与现状判定引擎落地排期）
  - 342-全链路核查方法论方案（标签-判定-共识三层验证）
  - 399-dim梳理产出报告
---

# 431 — 430号遗留项核查清单与处置建议

> **性质**：核查存档（含处置建议）。本轮**不实施任何修复**，仅把 430 号执行完毕后残留的问题逐条固定为可追溯清单，供后续单独决策。
>
> **核查对象**：430 号 §9 第 3/4/5/6 条落地后残留的**路径口径未扫全覆盖项、归档文档内嵌失效快照、391 号错误结论、失效可执行命令、环境收尾项与真功能缺口**。
>
> **v1.1 增补（2026-09-13）**：按用户决策，本文档同时作为 **430 号收尾的唯一入口**，故补录两类原先不在册的未完成项——**F 类**（430 号**从未纳入 §9 处置清单**的 4 项发现：问题 #4 两套加载器、问题 #8 维度引擎零配置、§7.4-3 静默吞异常、§7.4-4 dim6 死码）与 **G 类**（§9 第 7、8 条＝未启动的规划项）。仍**不实施任何修复**。
>
> **v1.2 增补（2026-09-13）**：补录 **B1 同文件 §6.2 命名映射表的 2 行**（`:679` / `:680`）与核查时**新发现**的 `:26`（「非缓存」表述失真），共 3 处残留文案；并澄清 v1.1「已移除键」表述的适用范围（**不含** `:679`，见 B 类）。仍**不修改 JUD 报告本体**。
>
> **v1.3 增补（2026-09-13）**：新增 **§九 存量待议事项**，归档三类**事实已查清、处置方式未定**的收尾项——① E3 范围外的**残留 env 键漂移 7 键**（模板独有 4 键 + 运行态独有 3 键，其中 `REDIS_URL` **全仓零消费方＝失效键候选**）；② **`.env.local` 孤儿文件**（14 键，10 键与 `.env` 同名，存在覆盖风险；**§015 订正**：§014 从 `.env` 删除 `REDIS_URL` 后实为 **9 同名 / 5 独有**，详见 §9.2）；③ **B1 本体**对 JUD 报告的行号引用漂移（`:709` → 现 `:711`，**非统一偏移**，成因未确认）。均**未实施任何修复**。
>
> **核查手段**：`grep_search` 全仓正则 + `read_file` 逐行核对 + `git ls-files` / `ls -1` 存在性验证 + `git status`。**全程只读**。
>
> **数据快照时间**：2026-09-13。
>
> **编号说明**：`002-方案存档/` 当前最大编号为 430，故本文档取 **431**。（429 号已被 425/426 号预留给「daemon 卡死根治」，无实体文件，未占用编号空间。）

---

## 一、背景与范围

430 号执行时采纳了「本轮只修 P0 + §9 第 3/4/5/6 条，其余留待后续」的范围裁剪。执行完毕后对全仓做了交叉复扫，残留与未完成项归为七类（**A–E** 为首版五类，**F/G** 为 v1.1 补录）：

| 类别 | 性质 | 条数 | 运行时行为影响 |
|:--:|---|:--:|---|
| **A** | 纯路径 / 失效键文案残留 | 7 | 无（仅文档可读性） |
| **B** | 归档文档内嵌失效快照 | 1 | 无（归档失真） |
| **C** | 失效可执行命令（路径口径的硬后果） | 2 | 无（但按文档执行必失败） |
| **D** | 需重写结论的错误核查报告 | 1 | 无（但结论已污染） |
| **E** | 真功能缺口 / 破坏性收尾项 | 3 | **有**（E1 为功能缺口） |
| **F** | 430 §9 **未列入**的未完成发现（v1.1 补录） | 4 | **有**（F1 为 P1） |
| **G** | 430 §9 **未启动**的规划项（v1.1 补录） | 2 | 无（规划项，非缺陷） |

**范围边界**：
- 430 号 §9 第 7、8 条（P2 硬编码常量收敛、配置加载测试）原不在 v1.0 范围（视作「尚未启动的规划项」而非遗留缺陷）；**v1.1 起已补录为 G 类**，性质判定不变，仍需另行决策。
- 430 号 §9 **从未列入**的 4 项未完成发现（问题 #4 两套加载器 P1、问题 #8 维度引擎零配置 P2、§7.4-3 静默吞异常 P3、§7.4-4 dim6 死码 P3）在 v1.1 补录为 **F 类**。
- **不含** 已按用户决策明确保留的历史证据（见 §七）。

---

## 二、核查方法

1. **全仓正则扫描**（而非按目录抽样），以消除 430 号的扫描盲区：
   - `config/status_engine\.yaml`（区分「项目根口径」与「`backend/config/` 权威口径」）
   - `config/signal_registry\.yaml`
   - `color_low|color_high|neutral_is_zero`（430 §9 第 5 条已从 yaml 移除的键）
   - `dimension_weights`（430 §9 第 5 条已移除的键）
2. **逐行读取**：`scripts/calibrate_signal_thresholds.py`（全文）、`backend/docs/JUD环节代码实审报告.md`（头/§3.1/尾）、`002-方案存档/391-…md`（结论段）。
3. **存在性验证**：`ls -1` 确认 `.env*`、项目根 `config/`、`backend/tests/test_390_integration.py` 的实际状态。
4. **归属验证**：`git ls-files docs/compose/` 确认 `docs/compose/**` 属**受版本管理的文档**（非临时产物）。

---

## 三、结论汇总表

| 编号 | 位置 | 问题摘要 | 类别 | 建议 |
|:--:|---|---|:--:|---|
| **A1** | `002-方案存档/338-332总纲整体实施计划（…）.md:64` | S2.2 行的 `config/signal_registry.yaml` 漏改（同行的 `status_engine.yaml` 已改为 `backend/config/`） | A | 改路径（纯文案） |
| **A2** | `002-方案存档/342-全链路核查方法论方案（…）.md:186` | 仍引用 430 §9 第 5 条已移除的键 `color_low` | A | 改键名/删引用 |
| **A3** | `002-方案存档/334-L1维度判定细则与信号注册表（332总纲·分档二）.md:147` | 目录树标注 `config/signal_registry.yaml` | A | 改路径 |
| **A4** | `002-方案存档/361-【实现方案】七维现状描述系统实现.md:49` | 表格内 `config/signal_registry.yaml` | A | 改路径 |
| **A5** | `002-方案存档/364g-Phase7-第1维信号确认7类分类.md:92` | 标题内 `config/signal_registry.yaml`（含行区间 `:1-41`） | A | 改路径（行区间需复核） |
| **A6** | `scripts/calibrate_signal_thresholds.py:17,:91` | 文档字符串/print 中的 `config/signal_registry.yaml` | A | 改路径（**仅 2 处**，见 §四-A6） |
| **A7** | `002-方案存档/336-…分档四）.md:137-140,:158`、`002-方案存档/352-…执行计划）.md:126` | yaml 草案仍含 430 §9 第 5 条已移除的 `color_high`/`color_low`/`neutral_is_zero`/`dimension_weights` | A | 标注失效/改草案 |
| **B1** | `backend/docs/JUD环节代码实审报告.md`（`:709`、`:439`、`§3.1`） | ①`:709` 项目根口径与同文 `:5`/`:441` 自相矛盾；②内嵌 yaml「完整内容」快照已失真 4 处；③`(56行)` 行数过期（实际 41）；④`invalidate_config_cache()` 文档化了一个**不存在的函数** | B | 追加「订正注记」，**不重写**审计快照 |
| **C1** | `docs/compose/plans/2026-09-17-方案390集成与旧组件清理.md`（8 处） | 3 条命令**现已必然失败**：`:64` `cat config/status_engine.yaml \| head -20`、`:91`/`:835` `git add config/status_engine.yaml` | C | 改路径（命令可用性） |
| **C2** | `docs/compose/reports/方案390集成与旧组件清理.md`（3 处） | `:36/:49/:55` 项目根口径；`:49` 另载「`config/status_engine.yaml` \| 56 \| 版本开关配置」 | C | 改路径 + 订正行数 |
| **D1** | `002-方案存档/391-390号方案落地实施全面核查报告.md` | 4 处路径（`:37/:124/:387/:414`）+ 7 处**错误结论**（`:38/:45/:111/:118/:124/:131/:387/:414/:418`）：断言 yaml「不存在」、`jud_engine_version`「不存在」、新管线「永远不会被调用」、`test_390_integration.py`「❌ 不存在」——**均与事实相反** | D | 重写结论段 + 加勘误头（430 §9 第 9 条） |
| **E1** | `backend/app/opportunity_atlas/potential_engine.py` | IC 权重**写侧零调用点**（`recompute_ic_weights`/`save_ic_weights`），`data/ic_weights.json` 为静态快照；**写侧 7 处查询走总库连接恒空 → 静默退回 DIM_WEIGHTS**（阻断性；2026-09-14 §019 已收敛） | E | 需新增月度滚动重估作业（**新功能**；另立新功能号，§019 待决策） |
| **E2** | `.env.bak.331`（1241B，08-13 20:06） | 430 §9 第 10 条已列，仍未清理 | E | 删除（破坏性，需确认） |
| **E3** | `.env` / `.env.example` / `.env.local` | 三份 env 键集漂移：`QUALITY_*`(4 键) 三份皆无但代码定义 2 处；`L4_EXTRA_VOTES` 无处声明；`CN_10Y_BOND_YIELD` 在 `.env`/`.env.example` 有、`.env.local` 无 | E | 对齐键集（需确认口径） |
| **F1** | `backend/app/services/status_config.py` + `backend/config/__init__.py` | **两套配置加载器并存**（430 问题 #4，**P1**，§9 未列）：`status_config.py:18/27/32`（`load_yaml`/`get_status_engine_config`/`get_signal_registry`，`@lru_cache`）与 `backend/config/__init__.py:7/22`（`load_strategy_config`/`get_strategy_config`）；后者的**唯一消费方**是 `app/engine/pipeline.py:97-98` | F | 合并加载器（需先定权威入口） |
| **F2** | `backend/app/opportunity_atlas/dimensions/` | **维度引擎零配置引用**（430 问题 #8，P2，§9 未列）：`dimensions/` 对 `status_engine.yaml`/`signal_registry`/`get_*_config` 引用数 **0**；注册表 `verify_days`/`verify_rule`（及 `trigger`/`lifecycle.*.days`）在 `app/` 下消费方 **0** → dim1–dim8 实由代码常量驱动 | F | 定「配置驱动」真实边界（需先定口径） |
| **F3** | `backend/app/opportunity_atlas/arbiter.py:72-73`（`:96-97` 同类） | **静默吞异常**（430 §7.4-3，P3，§9 未列）：`_load_thresholds()` 以 `except Exception: pass` 结束，阈值读取失败会**静默**退回硬编码 `_BEARISH_STRONG`/`_BULLISH_ENTER = 0.67` | F | 改显式告警/日志（低风险小改） |
| **F4** | `backend/app/opportunity_atlas/dimensions/dim6_risk_engine.py:269/:271/:324` | **dim6 `hard_veto` 死码**（430 §7.4-4，P3，§9 未列）：`:269` 自注「`dims['l0']` 始终为空（T42 循环依赖设计限制），`hard_veto` 永远不触发」，`:271`/`:324` 的 `if l0.get('hard_veto')` 分支不可达 | F | 删死码并记录（2026-09-14 §019 已落地）；修 T42 另议 |
| **G1** | `cross_validate.py` / `weight_engine.py` / `status_engine.py` / `valuation_estimator.py` / `dim7_valuation_engine.py` | **§9 第 7 条（P2）未启动**：硬编码常量 5 组未收敛——`cross_validate.py:1295-1316`（`0.3/0.5/0.7/0.8`）、`weight_engine.py:14` `STATIC_WEIGHTS`、`weight_engine.py:22` `EMOTION_MULTIPLIERS`、`status_engine.py:567` 函数内 `MARKET_REGIME_WEIGHTS`、`QUALITY_*` 4 常量双份（`valuation_estimator.py:32-35` + `dim7_valuation_engine.py:35-38`）；另含 dim5 阈值组 | G | 收敛到 YAML（规划项，与 F2 同源） |
| **G2** | `backend/tests/` | **§9 第 8 条（P2）未启动**：配置加载零测试覆盖——`status_config\|signal_registry\|_CONFIG_DIR\|get_status_engine_config` 命中 **0** | G | 补配置加载单测（规划项） |

---

## 四、逐项详情

### A 类｜纯路径 / 失效键文案

#### A1 — `338-…实施计划….md:64`

430 §9 第 3 条统一口径时，本行的 `backend/config/status_engine.yaml` 已改，但**同一格的 `config/signal_registry.yaml` 被漏掉**。原文：

```
| S2.2 | 阈值配置化：`backend/config/status_engine.yaml` + `config/signal_registry.yaml`（版本化可回滚） | 334 §6 / 336 §7 | 阈值从 yaml 读取，arbiter 硬编码迁移 |
```

实际：`config/signal_registry.yaml` 已随 430 §9 第 3 条通过 `git rm` 迁移为 `backend/config/signal_registry.yaml`（当前 `git status` 显示为 `R  config/signal_registry.yaml -> backend/config/signal_registry.yaml`）。

#### A2 — `342-…方法论方案….md:186`

430 §9 第 5 条从 yaml 移除了 `color_low`，但本行仍在引用它。原文结尾：

```
…落地时须联动 `backend/config/status_engine.yaml`（enter_threshold/bearish_strong/color_low）+ 前端颜色语义 + 336 号文档口径同步。
```

> 注：本行的 `backend/config/status_engine.yaml` 路径是**正确的**（430 §9 第 3 条已改），仅 `color_low` 一词失效。

#### A3 / A4 / A5 — `signal_registry.yaml` 项目根口径残留

| 编号 | 文件:行 | 原文（节选） |
|:--:|:--:|---|
| A3 | `334-…分档二）.md:147` | `config/signal_registry.yaml        ← 每类信号的触发/验证/生命周期阈值（带版本号）` |
| A4 | `361-【实现方案】七维现状描述系统实现.md:49` | `\| \`config/signal_registry.yaml\` \| 增加属性分类阈值配置 \| 0.5h \|` |
| A5 | `364g-Phase7-第1维信号确认7类分类.md:92` | `### 1.5 signal_registry.yaml 当前信号（config/signal_registry.yaml:1-41）` |

> A5 除路径外，行区间 `:1-41` 亦需复核（该文件迁移后未重新核行）。

#### A6 — `scripts/calibrate_signal_thresholds.py`（**口径订正**）

430 号称该脚本有 **4 处**（L17/L33/L77/L91）项目根口径。逐行读取后**实际只有 2 处**：

| 行 | 原文 | 是否口径错误 |
|:--:|---|:--:|
| `:17` | docstring：`人工确认后更新 config/signal_registry.yaml（version+1，可回滚）。` | ✅ 是 |
| `:91` | `print("\n结论：样本≥100 的信号类型按 P50/P80 分位更新 config/signal_registry.yaml（version+1）")` | ✅ 是 |
| `:33` | `_INITIAL = {'initial_dist': 5.0, 'extended_dist': 12.0}  # %` 附近仅出现**裸文件名** `signal_registry.yaml`（无路径前缀） | ❌ 否 |
| `:77` | 同上的 print，仅**裸文件名** | ❌ 否 |

**另需澄清 430 号的措辞**：430 描述该脚本「仍写项目根 `signal_registry.yaml`」，字面易被理解为**写文件**。实际该脚本**全程只读**——只做分位统计并 `print` 结论，**从不写入任何 yaml**。此处仅为文案口径问题，无数据风险。

#### A7 — 其他文档中残留的「已移除键」

430 §9 第 5 条从 `status_engine.yaml` 移除了 `color_high`/`color_low`/`neutral_is_zero` 与整个 `dimension_weights` 块，但以下两处的 yaml **草案**仍在示范这些键：

| 位置 | 原文（节选） |
|:--:|---|
| `336-…分档四）.md:137-140` | `color_high: 0.70  # 深蓝（高共识），待分布校准` / `color_low: 0.67  # 浅蓝下限…` / `neutral_is_zero: true  # 中性维度不稀释共识…` / `dimension_weights:  # 预留维度权重…默认等权` |
| `336-…分档四）.md:158` | `- \`dimension_weights\` 预留但**默认等权**（知识库"先简单后复杂"；等权可解释性好）` |
| `352-…执行计划）.md:126` | `**改动范围**：status_engine.py 1 处（_build_dimensions 新增维度）+ backend/config/status_engine.yaml（dimension_weights 不加 signal_confirm）` |

> 注：336 号是 332 总纲的分档设计文档，其 yaml 块属**设计草案**而非当前配置实录，因此不构成错误结论；但草案与现行 yaml 已不一致，后续若有人照抄会重新引入死键，建议在草案处标注「已于 430 §9 第 5 条移除，勿照抄」。

---

### B 类｜归档文档内嵌失效快照

**B1 — `backend/docs/JUD环节代码实审报告.md`**（711 行，审计日期 2026-08-28）

| 位置 | 内容 | 判定 |
|:--:|---|:--:|
| `:5` | `> 审计范围：\`backend/app/opportunity_atlas/\` + \`backend/data_daemon.py\` + \`backend/config/status_engine.yaml\`` | ✅ **正确** |
| `:439`/`:441` | `**文件**: \`backend/config/status_engine.yaml\` (56行)` | 路径✅ / 行数❌（实际 **41** 行） |
| `§3.1`（`:443` 起） | 内嵌「完整内容」yaml 快照 | ❌ **失真 4 处** |
| `:479-484` | `- \`load_yaml()\` 带缓存机制（\`invalidate_config_cache()\`可清缓存）` | ❌ 该函数**不存在**（430 号问题 #11） |
| `:709` | `\| \`config/status_engine.yaml\` \| 56 \| 全量配置 \|` | ❌ 项目根口径，**与同文 `:5`/`:441` 自相矛盾** |

§3.1 内嵌快照的 4 处失真：
1. `version: 1` → 现为 `2`
2. `consensus.color_high: 0.70` / `color_low: 0.67` / `neutral_is_zero: true` → **均已移除**
3. 整个 `dimension_weights:` 块 → **已移除**
4. `l0.hard_risks: [regulatory, st, delist]` → 现为 `[regulatory]`

**处置建议（不重写快照）**：该文件是**有日期的审计快照**（2026-08-28），静默重写会抹掉审计证据。建议：
- 修正 `:709` 的路径口径（这是自相矛盾，非快照内容）；
- 在 `§3.1` 追加一行**「订正注记」**，例如：
  `（订正于 2026-09-13，430 号 §9：yaml 已升 v2，移除 dimension_weights / color_* / neutral_is_zero，hard_risks 收敛为 [regulatory]，文件实际 41 行。）`

---

#### B1 补录（v1.2，2026-09-13）— 同文件 §6.2 命名映射表 / §1.1 说明的残留文案

按约定口径复核（**裸 `status_engine.yaml` ＝缺陷**，因与项目根同名；代码实参 `load_yaml('status_engine.yaml')` 与 `status_engine.py` 裸文件名不算），全文残留恰为 **3 处**：

| 位置 | 内容 | 判定 |
|:--:|---|:--:|
| `:679` | `\| consensus.enter_threshold \| 0.67 \| status_engine.yaml L11 \|` | ❌ 缺陷**仅 1 处**：来源列裸路径。键 `consensus.enter_threshold` **并未移除**，且**仍在 `backend/config/status_engine.yaml:11`**（L11 行号正确） |
| `:680` | `\| dimension_weights.* \| 1.0(等权) \| status_engine.yaml L17-27 \|` | ❌ **三重缺陷**：① 引用**已移除**的 `dimension_weights` 块（430 §9 第 5 条）；② 行号 `L17-27` 已失效（该区间现为 `conflict_rules.rules`）；③ 来源列裸路径 |
| `:26` | `- **配置源**: \`status_engine.yaml\`，每次实例化时加载（非缓存）` | ❌ **新发现（本次核查首次列出，430/431 原清单均无）**：① 裸路径；② **「非缓存」表述失真**——`get_status_engine_config()` → `load_yaml()` 实为 `@lru_cache` 缓存（同文 `:471` 订正注记已确认缓存由 `lru_cache` 承载；F1 合并后缓存单点化） |

> **口径澄清（v1.1 表述的适用范围）**：v1.1 的 B1 表只覆盖 `:439`/`:441`（行数）与 `:479-484`/`:709`（口径），**未覆盖 §6.2 命名映射表**；其「430 §9 第 5 条**已移除**的键（`color_high` / `color_low` / `neutral_is_zero` / `dimension_weights`）」这一表述**仅适用于 §3.1 快照区与 `:680`**，**不适用于 `:679`**——`consensus.enter_threshold` 是**保留键**，仍在 `backend/config/status_engine.yaml:11`（`wc -l` = 41 行；`consensus` 段仅 `enter_threshold` / `bearish_strong`）。本补录把 `:679` 单列，以避免「已移除」被误扩到该行。

**处置建议（同 B1，仅改来源列，不重写审计快照）**：3 处的**来源列**由裸 `status_engine.yaml` 改为 `backend/config/status_engine.yaml`；`:680` 另需把来源列改为「**已移除**（430 §9 第 5 条）」；`:26` 另需把「（非缓存）」改为「（`lru_cache` 缓存）」。

---

### C 类｜失效可执行命令（**新发现，430 号扫描盲区**）

430 号 §233 的全仓统计只覆盖了 `002-方案存档/`（得「8 份文档」），`docs/compose/` 未被纳入扫描。全仓复扫后另有 **11 处**，集中于两份 2026-09-17 日期文档：

#### C1 — `docs/compose/plans/2026-09-17-方案390集成与旧组件清理.md`（8 处）

| 行 | 引用类型 | 备注 |
|:--:|---|---|
| `:18` | 路径引用 | |
| `:32` | 路径引用 | |
| `:54` | 路径引用 | |
| **`:64`** | **命令** | `cat config/status_engine.yaml \| head -20` → **现在必失败**（文件已删） |
| `:69` | 路径引用 | |
| **`:91`** | **命令** | `git add config/status_engine.yaml` → **现在必失败** |
| `:817` | 路径引用 | |
| **`:835`** | **命令** | `git add config/status_engine.yaml` → **现在必失败** |

#### C2 — `docs/compose/reports/方案390集成与旧组件清理.md`（3 处）

| 行 | 内容 |
|:--:|---|
| `:36` | 路径引用（项目根口径） |
| `:49` | `\| \`config/status_engine.yaml\` \| 56 \| 版本开关配置 \|`（路径❌ + 行数❌） |
| `:55` | 路径引用 |

**归属确认**：`git ls-files docs/compose/` 显示该目录下文档均受版本管理（非临时产物），其中包含 7 月/8 月的历史计划与报告。故此 11 处属**需订正的文档口径**，与 `002-方案存档/` 的 11 处同类。

> **风险等级**：C 类虽不影响运行时行为，但 `:64`/`:91`/`:835` 是**可执行命令**，按文档照做会直接报错（`No such file or directory` / `pathspec did not match`），因此**优先级高于 A 类**。

---

### D 类｜需重写结论的错误核查报告

**D1 — `002-方案存档/391-390号方案落地实施全面核查报告.md`**（426 行，frontmatter `date: 2026-09-17`）

391 号开篇即写「**存在 3 个 P0 级阻塞问题需立即修复**」，其核心结论已被事实推翻：

**（1）4 处项目根路径口径**：`:37`、`:124`、`:387`、`:414`

**（2）错误结论逐条**

| 行 | 391 号原文（节选） | 事实 |
|:--:|---|---|
| `:37` | `\| \`config/status_engine.yaml\` 是否存在 \| **不存在**。\`config/\` 目录下仅有 \`strategy_defaults.yaml\`，无 \`status_engine.yaml\` \|` | 文件一直在 `backend/config/status_engine.yaml`（`status_engine.py` 的 `_CONFIG_DIR` 解析至 `backend/config/`） |
| `:38` | `\| \`jud_engine_version\` 字段 \| **不存在**于任何 yaml 文件中 \|` | 一直存在（值 `v390`） |
| `:45` | `因 \`status_engine.yaml\` 不存在，\`jud_engine_version\` **永远取到默认值 \`'legacy'\`**，新管线永远无法激活。` | 反了：新管线**一直在生效** |
| `:111` | `**⚠️ 部分落地原因**：代码逻辑完整，但因配置文件缺失（#1），新管线在生产环境中**永远不会被调用**。` | 反了 |
| `:118` | `\| \`test_390_integration.py\` \| **❌ 不存在** \|` | **文件存在**：`backend/tests/test_390_integration.py`（6984 B，9月6日 15:44），且可正常收集执行 |
| `:124` | `\| 1 \| 配置开关 \| ❌ 未落地 \| \`config/status_engine.yaml\` 缺失，\`jud_engine_version\` 不存在 \|` | 反了 |
| `:131` | `\| 8 \| status_engine集成 \| ⚠️ 部分落地 \| 代码完整，但因配置缺失新管线无法激活 \|` | 反了（应为 ✅ 已落地） |
| `:387` | `\| 1 \| **\`config/status_engine.yaml\` 缺失**，新管线无法激活 \| 创建文件，写入 \`jud_engine_version: "v390"\` \|` | 该动作**当年即属多余**（文件已存在） |
| `:414` | `1. **配置文件缺失（P0）**：…**整个新管线在生产环境中从未被执行**` | 反了 |
| `:418` | 基于上述 P0 的后续建议 | 失去前提 |

**（3）391 号内部自相矛盾**——同一份报告的三行已正确记录新管线存在：

| 行 | 原文 |
|:--:|---|
| `:106` | `\| evaluate() 含 jud_engine_version 分支 \| ✅ 第148-159行 \|` |
| `:107` | `\| \`_aggregate_v390()\` 方法 \| ✅ 第563-697行，完整实现 \|` |
| `:108` | `\| 新管线调用链 \| ✅ convert_to_factors→assess→consensus_compute→conflict_detect→factor_arbitrate→compute_advice \|` |

**（4）已有前置判定**：430 号 §430/§435 已把此现象定性为「**结论污染**」；430 号 §436 判定 `418-…md:23`「描述准确，已被超越」。本次复扫将 391 号一并纳入同一处理路径（对应 430 §9 **第 9 条**）。

**处置建议**：保留原报告主体（作为历史核查记录），但：
1. 在 frontmatter 后追加**勘误头**，明确「第 2.1 / 2.8 / 2.9 / 2.10 / 五 / 六 节关于『配置缺失导致新管线永不激活』的结论**已被证伪**，原因：核查时以项目根 `config/` 为口径，未核 `backend/config/`」；
2. 修正 4 处路径口径；
3. 将 `:118` 的「❌ 不存在」订正为「✅ 存在」。

---

### E 类｜真功能缺口 / 破坏性收尾项（**本次不修**）

#### E1 — IC 权重写侧链路（真实功能缺口）

- **写侧零调用点**：`backend/app/opportunity_atlas/potential_engine.py` 的 `recompute_ic_weights` / `save_ic_weights` 在全仓**无任何调用点**。
- **读侧已生效**：`backend/data_daemon.py` L6286-6295 在 `try/except` 内注入 `_pe.IC_WEIGHTS_FILE = os.path.join(data_dir, 'ic_weights.json')`，并有启动日志。
- **现状**：`data/ic_weights.json`（97 B）是**静态快照**，内容与 `DIM_WEIGHTS = {"val":0.20,"earn":0.15,"sector":0.15,"event":0.10,"fund":0.20,"trend":0.20}` 一致。

这是 430 §9 **第 6 条**「仅注入 `IC_WEIGHTS_FILE`（读侧生效）」决策的**有意残留**：接上写侧需要为 `data_daemon` 新增一个**月度滚动 IC 重估作业**，属新功能开发，非缺陷修复。

> **处置状态（2026-09-14 §019 落地；批次14 精确化 + 批次15 收敛）**：用户授权口径＝**E1 就地部分收敛**（写侧 7 处 `_query_df` → `_query_shard` + 删 `sample_size` 死参数）＋「IC 权重月度滚动重估」**另立新功能号**；431 保持 **v1.3 不 bump**。**精确化（批次14，只读复核）**：E1 从「写侧零调用点」升级为「**写侧 7 处查询走总库连接恒空（阻断性）**」——`recompute_ic_weights` 内 7 处 `ecm._query_df(...)` 经总库连接（duckdb/stock_cache.db，无 `daily_cache` / `daily_basic_cache` / `moneyflow_cache` / `fina_indicator_cache` 这 4 张表）且**吞异常返空 df** → `dates` 空 → **静默退回 DIM_WEIGHTS**。**落地（批次15，2026-09-14）**：7 处 `_query_df` 全部改 `_query_shard`（`daily_cache`×4 / `daily_basic_cache`×1 / `moneyflow_cache`×1 / `fina_indicator_cache`×1；分库归属：前三者 `market_cache.db`、后者 `financial_cache.db`）并各加「356号分库」标记注释；同时删除 `sample_size: int = 500` 死参数（函数体零引用、全仓仅此一处；`recompute_ic_weights` / `save_ic_weights` 仍零调用点 ⇒ 删参零风险）。**分库实测行数**：daily_cache **641 万** / daily_basic **92 万** / moneyflow **21 万** / fina **2.2 万**。**后续处置（2026-09-14 已决，另见 433 号）**：① 月度滚动重估作业 **已开 433 号**（承载面评估：`data_daemon.py` 钩子〔推荐〕/ `scheduler_manager.py` / `scheduler.py` / `rule_scheduler.py` 不适配，见 433 §3.2）；② docstring「预测未来 10 日收益」与 `horizon=20` 不一致——**已决：口径统一为 20 日**（313 方案原文认可「10/20 日」，20 与采样节奏自洽；docstring/注释已同步，代码零改动）；③ `_assess_risk_level` 中 `tr` 潜在 NameError——**已修复**（append 移入 try，与 `_list_risk_factors` 同构）；④ dim2 缠论 vendored 副本——**部分收敛 + 第一步落地（2026-09-14）**（framework/`chanlun_level_validator.py` 零消费方已删，dim2 份为唯一权威；`chanlun_config`/`chanlun_strategy` 双份均 live，另行决策——第一步已消除 dim2 内 F4 混用并产出《缠论双份差异审计报告》，详见该报告）。

#### E2 — `.env.bak.331` 残留

项目根存在 `.env.bak.331`（1241 B，8月13日 20:06）。430 §9 第 10 条 / 430 问题 #13 已列，尚未清理。

#### E3 — env 键集漂移

| 键 | `.env` | `.env.example` | `.env.local` | 代码 |
|---|:--:|:--:|:--:|:--:|
| `QUALITY_*`（4 键） | ❌ | ❌ | ❌ | ✅（定义 2 处） |
| `L4_EXTRA_VOTES` | ❌ | ❌ | ❌ | — |
| `CN_10Y_BOND_YIELD` | ✅ | ✅ | ❌ | — |

（文件大小/时间：`.env` 1331 B / 8月13日 20:11；`.env.example` 1725 B / 8月5日 10:54；`.env.local` 754 B / 7月11日 08:58）

#### E2 / E3 补记（v1.2，2026-09-13）— 已处置

- **E2 已删除**：`.env.bak.331` 经用户确认于 2026-09-13 删除。该文件是 **331 号 PG→SQLite 迁移的回滚点**（与 `.env` 的唯一实质差异为 `DATABASE_URL`），被 `.gitignore:105 *.bak.*` 覆盖，**无 git 兜底 → 删除不可恢复**。现状 `ls -1 .env*` = `.env` / `.env.example` / `.env.local`。
- **E3 已对齐（命名范围）**：按「**补模板 + 补运行态 `.env`**」口径落地——`QUALITY_*`（4 键）与 `L4_EXTRA_VOTES` 已补入两侧，`CN_10Y_BOND_YIELD` 维持原状；键集＝`.env` **17** 键（`wc -l` = 44）/ `.env.example` **18** 键（`wc -l` = 61）。补入值均为**代码默认值**，行为零变更。上表的「❌」为 v1.0 快照，现状以本补记为准。
- **`.env.local` 保留不动（待议）**：754 B / `wc -l` = 22 / 14 键 / 7月11日 08:58。**孤儿文件**——`docker-compose.yml` 无 `env_file:`、项目根无 `run.py`，`.env` 仅由 `backend/app/__init__.py:4,10`、`backend/sync_data.py:13`、`backend/data_daemon.py:26-28` 加载，故其当前**零消费方**。独有键 `FLASK_APP` / `LM_STUDIO_ENDPOINT` / `LM_STUDIO_MODEL`，另有 **10 键与 `.env` 同名**（含 `DATABASE_URL` / `DATA_DIR` / `SECRET_KEY`）——**若将来误接线会覆盖运行态**，保留期间需留意。
- **E3 范围外的残留键漂移（本次未动，仅记录）**：①**模板独有 4 键** `AUTH_TOKEN` / `DEEPSEEK_BASE_URL` / `QMT_ACCOUNT_ID` / `QMT_ENABLED`（可选／默认值类，运行态未设不影响启动）；②**运行态独有 3 键** `FLASK_ENV`（消费方 `backend/run.py:37`，另由 `scripts/migrate_indicator_to_wide.py:17`、`scripts/precompute_all_indicators.py:15`、`scripts/migrate_sqlite_to_pg.py:22` 以 `os.environ` 就地设定）、`LLM_WIKI_API_TOKEN`（消费方 `routes/ai_analysis.py:379`、`routes/health.py:165,168`）、`REDIS_URL`（**全仓无任何消费方**，仅 `app/config.py:28` 定义，属旧 Gunicorn 多 Worker SocketIO 广播设计的遗留 → **失效键候选**）。7 键均未在 `.env.example` 登记。

---

### F 类｜430 §9 未列入的未完成发现（v1.1 补录）

430 号的 §0 共列 13 项问题、§9 共列 10 条处置建议，但**有 4 项发现从未进入 §9 的处置清单**，因此在 430 执行与 431 首版复扫中均未被承接（431 v1.0 §五 只记录了「§9 未列：B1、C1/C2」，未含此 4 项）。v1.1 补录如下，逐项均已在本轮只读取证。

#### F1 — 两套配置加载器并存（430 问题 #4，**P1**；430 里唯一的 P1）

| 加载器 | 位置 | 职责 |
|:--:|---|---|
| ① | `backend/app/services/status_config.py:18/27/32` | `load_yaml()` / `get_status_engine_config()` / `get_signal_registry()`，`@lru_cache` 缓存 |
| ② | `backend/config/__init__.py:7/22`（`:19` 模块级 `_strategy_config`、`:24-25` `global`） | `load_strategy_config()` / `get_strategy_config(reload=False)` |

- **唯一消费方**：②号仅被 `backend/app/engine/pipeline.py:97-98` 引用（`from ...config import get_strategy_config` → `self.strategy_config = config or get_strategy_config().get('chip_distribution', {})`）。
- **为何是 P1**：两套加载器各自缓存、各自解析路径，在 `.env` / `DATA_DIR` 切换或需要配置热更新时**行为不一致**；而 430 §9 **未列**此项，故无任何后续承接。
- **处置方向**：先定「权威入口」（建议统一到 `backend/config/`，与 430 §9 第 3 条的 yaml 权威位置裁定保持一致），再迁移消费方；属**重构**，需单独决策。

#### F2 — 维度引擎零配置引用（430 问题 #8，P2）

- `backend/app/opportunity_atlas/dimensions/` 对 `status_engine.yaml` / `signal_registry` / `get_status_engine_config` / `get_signal_registry` 的引用数 = **0**。
- 注册表新增字段（`trigger` / `verify_days` / `verify_rule` / `lifecycle.*.days`）在 `app/` 下消费方 = **0**（`verify_days`/`verify_rule` 直接命中 0；`trigger` 的命中均属 `entry_rules`/`stop_loss` 等无关字段）。
- **含义**：dim1–dim8 的阈值/权重**全部来自代码常量**，YAML 与注册表只是声明面。这与 430 号总结的「表象『配置驱动』、实态『代码常量驱动』」直接对应，但 §9 未列处置项。
- **处置方向**：与 **G1**（第 7 条常量收敛）**同源**——若做常量收敛，须一并决定「哪些 YAML/注册表字段真正接线、哪些明确废弃」，否则收敛后仍是一批死配置。建议 F2 与 G1 合并规划。

#### F3 — `arbiter._load_thresholds` 静默吞异常（430 §7.4-3，P3）

```python
# backend/app/opportunity_atlas/arbiter.py:72-73（:96-97 为同类）
    except Exception:
        pass
```

阈值读取失败时**无日志、无告警**，直接沿用硬编码 `_BEARISH_STRONG = 0.67` / `_BULLISH_ENTER = 0.67`，故障不可观测。处置＝改为显式 `logger.warning(...)` 或抛出（**低风险小改**）。

#### F4 — dim6 `hard_veto` 死码（430 §7.4-4，P3）

- `backend/app/opportunity_atlas/dimensions/dim6_risk_engine.py:269` 自注：`# 404号DATA-04: dims['l0']始终为空（T42循环依赖设计限制），hard_veto永远不触发`
- `:271`（及 `:324`）的 `if l0.get('hard_veto'):` 分支为**不可达代码**，系已知的设计缺陷（T42 循环依赖）。
- **处置方向**：或修复 T42 循环依赖使 `l0` 真正可用，或删除死码并记录；**属功能改动**，需决策。

> **处置状态（2026-09-14 §019 登记；2026-09-13 已落地）**：用户授权口径＝**删死码并记录**（不触碰运行时行为；T42 循环依赖另议）。落地 6 处（dim6_risk_engine.py **1261 → 1247 行**）：① `_assess_risk_level(dims, l0, tags)` → `(tags)`（docstring 去掉「和l0」）；② 删其 `if l0.get('hard_veto')` 死分支（立即 return 极高/red 段）；③ `_list_risk_factors(tags, dims, l0)` → `(tags)`；④ 删其 hard_veto 死分支；⑤ 删 `for sr in l0.get('soft_risks', [])` 死循环；⑥ `evaluate` 调用点改 `_assess_risk_level(tags)` / `_list_risk_factors(tags)`，删 `l0 = dims.get('l0', {}) …` 行。另加两处防回流注释（约 `:269` / `:318`）：「L0 硬否决由 StatusEngine 统一处置（l0 在 dim 引擎之后由 _apply_l0 生成，T42 时序），本引擎不参与」。**验证**：ruff → `All checks passed!`；py_compile → `COMPILE_OK`；`pytest tests/test_411_pipeline.py -k "Dim6" -q` → **3 passed, 27 deselected**。

---

### G 类｜430 §9 未启动的规划项（v1.1 补录）

这两项**不是遗留缺陷**，而是 430 号 §9 建议但**从未启动**的规划项（430 执行时即被裁出范围）。v1.1 纳入本文档，以形成 430 收尾的完整入口。

#### G1 — §9 第 7 条（P2）硬编码常量收敛｜⬜ 未启动

本轮取证（全部仍写死 / 双份）：

| 常量/矩阵 | 位置 | 现状 |
|:--:|:--:|:--:|
| 软风险系数 | `backend/app/opportunity_atlas/cross_validate.py:1295-1316` | 仍为 `max_ratio * 0.3 / 0.5 / 0.5 / 0.7 / 0.7 / 0.5 / 0.8` |
| `STATIC_WEIGHTS` | `weight_engine.py:14` | 仍存在 |
| `EMOTION_MULTIPLIERS` | `weight_engine.py:22` | 仍存在 |
| `MARKET_REGIME_WEIGHTS` | `status_engine.py:567` | 仍为函数内局部矩阵 |
| `QUALITY_ROE_THRESHOLD` / `QUALITY_ROE_NORM` / `QUALITY_PREMIUM` / `QUALITY_FAIL_PENALTY` | `valuation_estimator.py:32-35` **与** `dimensions/dim7_valuation_engine.py:35-38` | **双份定义并存** |

> 另含 dim5 阈值组（本轮未单独取证）。G1 与 **F2** 同源，建议合并规划。

#### G2 — §9 第 8 条（P2）配置加载测试｜⬜ 未启动

`backend/tests/` 对 `status_config|signal_registry|_CONFIG_DIR|get_status_engine_config` 命中 **0**：配置加载、`@lru_cache` 缓存、路径解析（`_CONFIG_DIR`）**无任何单测覆盖**。F1 的两套加载器若合并，应先补齐此测试作为回归网。

---

## 五、与 430 号 §9 的映射

| 430 §9 条目 | 状态 | 本文档覆盖 |
|---|:--:|---|
| 第 3 条 `status_engine.yaml` 唯一权威位置 + 统一 8 份文档路径口径 | ✅ 已落地 | **A1/A3/A4/A5/C1/C2 为其未扫全的残留**（A1 是同行半改；C1/C2 属 `docs/compose/` 盲区） |
| 第 4 条 `_apply_l0` 补 `emotion_position_cap` 生产写入 | ✅ 已落地 | — |
| 第 5 条 清理/接线 YAML 死键 | ✅ 已落地 | **A2/A7 为其未扫全的残留**（引用已删键） |
| 第 6 条 打通 IC 权重链路（仅读侧） | ✅ 已落地（读侧） | **E1** = 有意保留的写侧缺口 |
| 第 7 条 硬编码常量收敛（P2） | ⬜ 未实施 | **G1**（v1.1 补录；规划项，非缺陷） |
| 第 8 条 配置加载测试（P2） | ⬜ 未实施 | **G2**（v1.1 补录） |
| 第 9 条 P3：391 号错误结论订正 | ⬜ 未实施 | **D1** |
| 第 10 条 P3：`calibrate_signal_thresholds.py` / `.env.bak.331` / env 键集 | ⬜ 未实施 | **A6 / E2 / E3** |
| **§9 未列入** | — | **B1**（430 §7.3 已识别但未纳入 §9 处置清单）、**C1/C2**（扫描盲区）、**F1/F2/F3/F4**（v1.1 补录：430 问题 #4、#8、§7.4-3、§7.4-4） |

---

## 六、分级处置建议（本次均不执行）

### P3-low｜纯文案，零风险

| 顺序 | 条目 | 动作 |
|:--:|---|---|
| 1 | A3 / A4 / A5 / A1 / A6 | `config/signal_registry.yaml` → `backend/config/signal_registry.yaml`（A5 另需复核行区间） |
| 2 | A2 / A7 | 删除/替换失效键 `color_low`（A2）；336/352 草案处标注「已于 430 §9 第 5 条移除」（A7） |
| 3 | C2 | 3 处路径口径 + `:49` 行数（56→41） |
| 4 | C1 | 8 处路径口径（**优先**：`:64`/`:91`/`:835` 三条命令） |

### P3-med｜归档可信度

| 顺序 | 条目 | 动作 |
|:--:|---|---|
| 5 | B1 | 修 `:709` 路径 + `§3.1` 追加「订正注记」+ `:439` 行数（56→41） |
| 6 | D1 | 追加勘误头 + 修 4 处路径 + 订正 `:118` |
| 7 | B1 补录（v1.2） | JUD `§6.2` 两行（`:679`/`:680`）来源列改全路径 + `:680` 另注「已移除」+ `:26` 删「非缓存」（改 `lru_cache`） |

### 需单独决策｜真功能 / 破坏性

| 条目 | 动作 | 性质 |
|:--:|---|---|
| E1 | 为 `data_daemon` 新增月度滚动 IC 重估作业（**另立新功能号**；写侧 7 处查询恒空已收敛，§019 落地） | **新功能**（非修复） |
| E2 | 删除 `.env.bak.331` | **破坏性**（需确认） |
| E3 | 对齐三份 env 键集（补/删 `QUALITY_*`、`L4_EXTRA_VOTES`、`CN_10Y_BOND_YIELD`） | 需先定口径（以哪份为准） |

### P3-low｜F3（低风险小改）

| 条目 | 动作 |
|:--:|---|
| F3 | `arbiter._load_thresholds()` 的 `except Exception: pass` 改为显式日志/告警（不动阈值逻辑） |

### 需单独决策｜F1 / F2 / F4 与 G1 / G2（v1.1 补录）

| 条目 | 动作 | 性质 |
|:--:|---|---|
| F1 | 合并两套配置加载器（先定权威入口，建议 `backend/config/`） | **重构**（430 唯一 P1） |
| F2 | 定「配置驱动」真实边界：哪些 YAML/注册表字段真正接线、哪些明确废弃 | 需先定口径（与 G1 同源） |
| F4 | 删 dim6 `hard_veto` 死码（§019 已落地）；修 T42 循环依赖使 `l0` 可用 另议 | **功能改动**（已决：删死码） |
| G1 | 硬编码常量 5 组收敛到 YAML（含 dim5 阈值组） | **规划项**（与 F2 同源） |
| G2 | 补配置加载单测（`backend/tests/`） | **规划项**（F1 的前置回归网） |

---

## 七、明确保留（不改）的引用

以下出现同名路径/键但**属历史记录或既成证据，不应修改**：

| 位置 | 理由 |
|---|---|
| `002-方案存档/430-SIG环节配置全面核查报告.md`（14 处） | 430 号报告本体，是**问题发现的历史证据** |
| `002-方案存档/418-JUD管线接入与signal消费链补齐方案.md:23/:239` | 已由 430 §436 判定「描述准确，已被超越」 |
| `001-沟通记录/2026-09-13.md` | 当日沟通记录（本文档的产生依据） |
| `001-沟通记录/沟通索引.md` | 沟通索引本体 |
| `001-沟通记录/26-08-15沟通纪要.md:71/:149` | 历史纪要 |
| `001-沟通记录/26-08-16沟通纪要.md:14` | 历史纪要 |
| `001-沟通记录/26-09-17沟通纪要.md:11` | 历史纪要（`:26` 已自行订正「✅ 已存在v390（T1审计有误）」） |
| `003-代码备份/**` | 备份目录，保持原样 |
| `backend/$TMPDIR/` | 临时沙箱目录，不清理 |

---

## 八、证据索引

**核查命令（只读，可复现）**

```
grep_search  "config/signal_registry\.yaml"                      → 17 命中
grep_search  "color_low|color_high|neutral_is_zero"              → 定位 A2 + 336/JUD报告
grep_search  "[^k]config/status_engine\.yaml"                    → 69 命中（含 docs/compose 盲区）
grep_search  "config/strategy_defaults\.yaml"                    → 无残留
git ls-files docs/compose/                                       → 确认 C1/C2 受版本管理
ls -1 .env*                                                      → .env / .env.bak.331 / .env.example / .env.local
ls -1 backend/tests/test_390_integration.py                      → 存在（6984 B，9月6日 15:44）
ls -1 config/                                                    → 仅 data_sources.yaml / description_config.json / prompts/（status_engine.yaml 已删）

# —— v1.1 补录项取证（只读，2026-09-13）——
grep "def load_yaml\|def get_status_engine_config\|def get_signal_registry" backend/app/services/status_config.py   → :18 / :27 / :32
grep "def load_strategy_config\|def get_strategy_config" backend/config/__init__.py                                 → :7 / :22
grep "get_strategy_config\|status_config" backend/app/engine/pipeline.py                                            → :97-98（唯一消费方）
grep -rn "status_engine.yaml\|signal_registry\|get_status_engine_config\|get_signal_registry" backend/app/opportunity_atlas/dimensions/ → 0 命中（F2）
grep -rn "verify_days\|verify_rule" backend/app/ --include=*.py                                                      → 0 命中（F2）
grep -n "except Exception" -A1 backend/app/opportunity_atlas/arbiter.py                                              → :72-73、:96-97（F3）
grep -n "hard_veto" backend/app/opportunity_atlas/dimensions/dim6_risk_engine.py                                     → :269 / :271 / :324（F4）

# —— v1.2 补录项取证（只读，2026-09-13）——
grep -n "status_engine\.yaml" backend/docs/JUD环节代码实审报告.md      → :4 / :21 / :26 / :437 / :439 / :471 / :479 / :679 / :680 / :711
grep -n "配置源\|非缓存" backend/docs/JUD环节代码实审报告.md           → :26（「每次实例化时加载（非缓存）」）
sed -n '671,681p' backend/docs/JUD环节代码实审报告.md                  → §6.2 命名映射表 6 行；其中 :679 / :680 来源列为裸 status_engine.yaml
grep -n "enter_threshold\|^consensus:" backend/config/status_engine.yaml → :10 consensus: / :11 enter_threshold: 0.67（→ :679 的 L11 行号**仍正确**）
grep -n "dimension_weights" backend/config/status_engine.yaml          → 0 命中（块已移除 → :680 引用的是**已移除项**）
wc -l backend/docs/JUD环节代码实审报告.md                              → 712
wc -l backend/config/status_engine.yaml                                → 41

# —— v1.3 补录项取证（只读，2026-09-13）——
ls -1 .env*                                                            → .env / .env.example / .env.local（.env.bak.331 已按 E2 删除）
wc -l .env .env.example .env.local                                      → 44 / 61 / 22
grep_search "REDIS_URL" backend/ --include=*.py                         → 仅 app/config.py:28（自身定义），**0 消费方**
grep_search "FLASK_ENV" backend/ --include=*.py                         → run.py:37 + scripts/{migrate_indicator_to_wide:17, precompute_all_indicators:15, migrate_sqlite_to_pg:22}
grep_search "LLM_WIKI_API_TOKEN" backend/ --include=*.py                → routes/ai_analysis.py:379、routes/health.py:165,168
grep_search "AUTH_TOKEN|DEEPSEEK_BASE_URL|QMT_ACCOUNT_ID|QMT_ENABLED" backend/ → 30 命中（均有消费方且带默认值，明细见 §9.1）
grep_search "config/status_engine" backend/docs/JUD环节代码实审报告.md   → :4 / :439 / :471 / :711（`:711` 现为 `| backend/config/status_engine.yaml | 41 | 全量配置 |`）
grep_search "dimension_weights" backend/config/status_engine.yaml        → 0 命中（仍无该块）
```

**v1.1 / v1.2 / v1.3 存档时**：本清单**本体**未修改任何代码、配置、既有文档（存档动作本身零 diff）。后续各批次落地改动见文末「处置进度」表，逐条记录于 `001-沟通记录/2026-09-13.md`。

---

## 九、存量待议事项（v1.3 补录，2026-09-13）

> 本节与 §四（核查发现项）性质不同：§四 是「扫出来的问题」，本节是**事实已查清、处置方式未定**的存量事项。v1.3 补录时全部**未动手**，仅归档待议（后续处置见各节处置状态注记与文末「处置进度」表）。

### 9.1 — E3 范围外的残留 env 键漂移（7 键，消费方均已实测）

E3（§四 E 类第 3 项）只对齐了 `QUALITY_*`（4 键）/ `L4_EXTRA_VOTES` / `CN_10Y_BOND_YIELD`。对齐后**仍有 7 键单侧存在**（模板独有 4 + 运行态独有 3），本轮仅记录、**未改任何 `.env*`**：

| # | 键 | 出现侧 | 消费方（实测） | 处置建议 |
|:--:|:--|:--:|:--|:--|
| 1 | `AUTH_TOKEN` | 仅模板 | `app/auth.py:25`（未设则**跳过鉴权**＝开发模式；后备 `data/auth_token.txt`） | 可选类，模板保留即可 |
| 2 | `DEEPSEEK_BASE_URL` | 仅模板 | `app/config.py:32`（默认 `https://api.deepseek.com/v1`）；可运行期由 `routes/system_config.py:169` 覆盖 | 同上 |
| 3 | `QMT_ENABLED` | 仅模板 | `app/config.py:43`（默认 `'false'`）、`init_qmt_config.py:29`、`app/__init__.py:346` | 同上 |
| 4 | `QMT_ACCOUNT_ID` | 仅模板 | `app/config.py:44`（默认 `''`）、`init_qmt_config.py:30` | 同上 |
| 5 | `FLASK_ENV` | 仅运行态 | `run.py:37`（`debug_mode`）；另由 `scripts/migrate_indicator_to_wide.py:17`、`scripts/precompute_all_indicators.py:15`、`scripts/migrate_sqlite_to_pg.py:22` 就地设定 | 建议登记进模板（默认 `production`） |
| 6 | `LLM_WIKI_API_TOKEN` | 仅运行态 | `routes/ai_analysis.py:379`、`routes/health.py:165`（`:168` 状态文案） | 建议登记进模板（占位空值） |
| 7 | `REDIS_URL` | 仅运行态 | **全仓零消费方**——仅 `backend/app/config.py:28` 自身定义（`os.getenv('REDIS_URL', 'redis://localhost:6379/0')`；§013 加注释后该定义行现位于 `:29`） | **已处置（§014）**：`.env` / `docker-compose.yml` / 启动器 plist 三处同步删键；`config.py:27` 注释保留澄清 |

> 说明：**模板独有 4 键均有真实消费方且有默认值**，运行态未设不影响启动，故属「可选」而非缺失；**运行态独有 3 键**中 `FLASK_ENV` / `LLM_WIKI_API_TOKEN` 有真实消费方、仅缺模板登记，`REDIS_URL` 无任何消费方，是其中唯一可考虑删除的键。本次取证范围为 `backend/`（未逐一复扫前端与 `docs/`）。

> **处置状态（2026-09-13 §013 落地）**：第 **5、6** 键已登记进 `.env.example`——新增「运行环境」段 `FLASK_ENV=production`，LLM 段末新增 `LLM_WIKI_API_TOKEN=`（空占位，注「留空则健康检查显示未配置」）。两键消费方均为「值比较 / 空值即假」语义（`run.py:37` 与 `"development"` 比较；`routes/health.py:165`、`routes/ai_analysis.py:379` 均为 `if token` 真假判断），故空占位**不改变任何运行时判定**（行为中性）。第 **7** 键 `REDIS_URL` 已在 `backend/app/config.py:27` 就地澄清为「残留键：当前全仓无任何消费方」，**`.env` 侧删键仍待议**（`docker-compose.yml:38`、`com.stock.analyzer.backend.plist:28` 等非代码位置仍设该键，删前需一并确认）。第 **1–4** 键（模板独有）**无动作**，维持「可选、模板保留即可」。本轮**未改任何 `.env` / `.env.local` 值**（`grep -c ''` 仍为 44 / 22）。

> **处置状态（2026-09-13 §014 落地，本项收尾）**：第 **7** 键 `REDIS_URL` 已按用户决策**三处同步删键**——① **`.env`**：删该键专属注释行 + 键行（`wc -l` 44 → **41**、键 **17 → 16**，`grep -cE '^[[:space:]]*REDIS_URL'` = 0）；② **`docker-compose.yml`**：删 backend `environment:` 中的 `- REDIS_URL=…` 一行（`git diff --numstat` = `0 1`；**compose 的 `redis:` 服务 / `backend.depends_on.redis` / 顶层 `redis_data` 卷按决策保留不动**，YAML 仍可解析、无悬空 `depends_on`）；③ **`com.stock.analyzer.backend.plist`**：删 `<key>REDIS_URL</key>` + `<string>…</string>` 两行（numstat = `0 2`；`plutil -lint` **OK**；该 plist 由 launchd 加载，需重载才生效，且零消费方 ⇒ 无行为影响）。**行为中性已证**：`.env` 中被删的值本就**等于代码默认值**（`redis://localhost:6379/0`），删键后 `Config.REDIS_URL` 仍解析为该默认值 ⇒ 与删键前逐位一致；叠加全仓零消费方，运行态无任何变化。**文档口径同步**：`docs/deployment/可移植性体检清单.md:49` 键名清单移除 `REDIS_URL`、`:55` 改注「已废弃并移除（单进程直接广播 + TTLCache）」；**`_项目运行手册.md:426` 保留不动**（225号阶段一交付清单属历史规划快照，不重写历史）。**仍未动的同类位置**：`.env.local`（14 键中仍有 `REDIS_URL`，属 §9.2 待议，本轮未碰）、`002-方案存档/227|243|057|062|063|073|100|157|430-*.md` 等历史方案文档中的记录。`backend/app/config.py` 本轮**未再改动**（`:29` 定义行按「零消费方残留」保留，`:27` 注释继续有效）。

### 9.2 — `.env.local`（孤儿文件，14 键）——已决：保留不动

- **事实**：`wc -l` = 22 / 754 B / **14 键** / 时间戳 7月11日 08:58（本轮未改）。
- **零消费方**：`docker-compose.yml` 无 `env_file:` 配置、项目根无 `run.py`；`.env` 仅由 `backend/app/__init__.py:4,10`、`backend/sync_data.py:13`、`backend/data_daemon.py:26-28` 加载——**无任何代码读 `.env.local`**。
- **键集实测（§015 订正）**：**9 键与 `.env` 同名、5 键独有**。
  - **独有 5 键**：`DEEPSEEK_BASE_URL` / `FLASK_APP` / `LM_STUDIO_ENDPOINT` / `LM_STUDIO_MODEL` / `REDIS_URL`（`REDIS_URL` 位于 `.env.local:4`；`DEEPSEEK_BASE_URL` 仅模板有、`.env` 无；后 4 键 `.env.example` 亦无）。
  - **同名 9 键中 5 键值不同**（即覆盖风险核心）：`DATABASE_URL` / `DEEPSEEK_API_KEY` / `FLASK_ENV` / `LLM_PROVIDER` / `SECRET_KEY`。
  - **同名 4 键值相同**：`DATA_DIR` / `DEEPSEEK_MODEL` / `LLM_WIKI_API_TOKEN` / `TUSHARE_TOKEN`（后两者为**真实凭据的重复副本**）。
  - **无空值键**。
  - > **原文「独有键 3 / 另有 10 键与 `.env` 同名」的成因（订正说明）**：① "10 同名"在 v1.3 时点**成立**——当时 `.env` 尚含 `REDIS_URL`，§014 从 `.env` 删该键后降为 **9**；② 原列举漏列 `DEEPSEEK_BASE_URL`，故 10+3=13 与 14 键不符，属**列举不全**而非计数错。**结论：`一旦将来被接线，即会覆盖运行态同名键`的主判断不受影响**（危险面由 5 个值不同的同名键构成）。
- **消费方（§015 复验三证）**：① 代码/配置/脚本 `grep -rIn -F '.env.local'` 扫 `backend` / `frontend` / `scripts` / `docker-compose.yml` + 两个启动器 plist + `Makefile` → **0 命中**（其余命中全在文档/方案/沟通记录）；② compose **无 `env_file:`**，且只自动读 `.env`；③ 全仓 `load_dotenv*` 仅 4 处（`backend/app/__init__.py`、`backend/sync_data.py`、`backend/data_daemon.py`、`backend/app/config.py` 注释）**全部加载 `.env`**；`backend/run.py` 自身不调 dotenv；项目根无 `run.py`；frontend 无 `.env*`、无 `envDir`。
- **来源（设计意图已死）**：162/164 号记载「`.env` 供 `docker-compose.yml` 生产部署、`.env.local` 供本地 `python run.py` 开发调试」，但该设计的消费者（**项目根 `python run.py`**）**从未存在** ⇒ 纯静态残留。
- **版本控制状态**：未跟踪；`git check-ignore -v` 显示 `.env.local` 由 `.gitignore:13` 覆盖（`.env` 为 `:12`，另有 `:14` `.env.*.local`）；`git ls-files .env .env.local .env.example` → **仅 `.env.example`**。
- **附带发现（本批登记为 §9.4）**：`项目整体评估与升级建议.md:267` 断言 `.env` 与 `.env.local`「都在版本控制中」——**今日为假**，详见 §9.4。

> **处置状态（2026-09-13 §015 落地）**：用户决策＝**保留不动**——`.env.local` **本体一字未改**（`wc -l` 仍 22 / 754 B / 14 键 / 7月11日 08:58），本批只做三件事：① 订正上列键数口径（**9 同名 / 5 独有**，其中 5 键值不同＝覆盖风险核心）并写明成因；② **复验零消费方三证**（代码/配置/脚本 0 命中、compose 无 `env_file:`、全部 dotenv 调用点只加载 `.env`）；③ 登记附带发现（`:267` 陈旧断言）为 **§9.4**。**配套订正**：本文档「v1.3 增补」blockquote 中同记的「10 键与 `.env` 同名」一并加注订正。将来若清理，应先确认无历史脚本或本地流程依赖，再删（该文件被 `.gitignore:13` 覆盖 ⇒ **删除无 git 兜底、不可逆**，同 E2 `.env.bak.331` 情形）。本批**零代码、零配置、零 `.env*` 改动**；fix 批次不 bump 版本 ⇒ **431 仍为 v1.3**。

### 9.3 — B1 本体行号引用已漂移，且**非统一偏移**

B1 记的是 **v1.0 时点**的核查快照，其正文对 JUD 报告行号的引用如今已不成立：

| 项 | B1（v1.0 时点）所述 | 现状（2026-09-13 复核） | 判定 |
|:--|:--|:--|:--:|
| 行号 | 附录清单行在 `:709` | 该行现位于 `:711` | ❌ 已漂移 |
| 内容 | `\| config/status_engine.yaml \| 56 \| 全量配置 \|` | `\| backend/config/status_engine.yaml \| 41 \| 全量配置 \|` | ❌ → ✅ 已由批次2（§007）修正 |

- 文档总行数由 B1 所述 **711 行** → 现 `wc -l` = **712 行**（**+1**），而目标行位移 **+2** ⇒ **不是统一偏移，成因未确认**（本文档不对成因下断言）。
- B1 正文另引用的 `:439` / `:441` 与 `:479-484` 区块**至今仍吻合**（编号未动）。
- **处置建议：不改 B1 本体**——它是有日期的审计快照，行号系**当时**事实；内容缺陷已由 `:471` 的订正注记（批次2）就地闭环。此处仅登记漂移事实。

### 9.4 — `项目整体评估与升级建议.md` §2.8 陈旧断言（已决：段落级订正注记；§015 登记 / §016 落地）

- **事实（文档原文）**：`项目整体评估与升级建议.md` **§2.8「基础设施与工程化」**（评估日期 **2026-05-28**，属有日期的审计快照）表格 **2.8.1–2.8.6** 共六条断言；其中 **2.8.1**（`:267`）记「`.env` 和 `.env.local` 都在版本控制中，包含敏感 Token（TUSHARE_TOKEN）」，严重程度标 **🔴 高**。§015 时仅据此登记 `:267` 一行，`:268`/`:269` 未复核。

**原文断言与 2026-09-13 复核实况**：

| # | 原文断言 | 复核实况 | 判定 |
|:--:|:--|:--|:--:|
| 2.8.1 | `.env` 和 `.env.local` 都在版本控制中 | 两者均被 `.gitignore:12`/`:13` 覆盖；`git ls-files .env .env.local .env.example` → **仅 `.env.example` 受管理**（另有 `:14` `.env.*.local`） | **不成立** |
| 2.8.2 | 根目录 6 个 `chanlun_*.py` + `sync_data.py`，与 `backend/` 重复 | 根目录 `chanlun_*.py` 实为 **3 个**（`chanlun_correct.py` / `chanlun_demo.py` / `chanlun_demo_simple.py`）＋ `sync_data.py`；与 `backend/` 同名者 **2 个**，且**内容已各自分叉**（`chanlun_demo_simple.py` 11069 vs 10889 B、`sync_data.py` 4453 vs 2761 B） | **数量失真** |
| 2.8.3 | 没有 `Makefile` 或统一的快捷命令入口 | 根目录 `Makefile` 已存在（138 行，含 `dev-*`/`docker-*`/`lint`/`typecheck`/`test` 等 20+ 目标） | **不成立** |
| 2.8.4 | `backend/tests/test_core.py` 存在但为空 | 该文件现为 **10593 B** 非空测试 | **不成立** |
| 2.8.5 | compose 内部 5000 映射外部 5001，与 `run.py` 默认 5001 内外不一致 | `docker-compose.yml` 现为 `"5001:5001"`，与 `backend/run.py:35` `default=5001` **一致** | **不成立** |
| 2.8.6 | 同时存在 `CacheManager`（旧）和 `EnhancedCacheManager`（新） | `backend/app` 下**已无 `class CacheManager`**，仅 `EnhancedCacheManager`（`enhanced_cache_manager.py:121`）与 `memory_cache.py` | **不成立** |

- **结论**：**六条中五条不成立**（2.8.1/2.8.3/2.8.4/2.8.5/2.8.6）；**2.8.2 数量失真**（6→3），其实质部分——根目录松散脚本与 `backend/` 同名文件是否收敛——**已决（§017 分类处置，已落地）**。
- **顺带发现（同文档）**：§2.8「建议」两条**已完成**（`.env` 入 `.gitignore` ＋ 提供 `.env.example`；创建 `Makefile` 管理常用命令）；**Phase 1 任务表** 的 **1.4**（`.env` 入 `.gitignore` ＋ 建 `.env.example`）、**1.6**（`run.py` 移除 `debug=True`，现为 `FLASK_ENV` 驱动的 `debug_mode`）**均已实施**；1.1–1.3 / 1.5 / 1.7 **未复核**。
- **性质**：**自始即为误述，非后续漂移**——`.gitignore` 自 **2026-05-28 首次提交**即含 `.env`/`.env.local` 两行（`git log -S'.env.local' -- .gitignore` 仅命中该提交，与本报告评估日期**同日**）；`002-方案存档/164-162号报告问题修复核查报告.md:126` 当时亦已写明「两个文件均已加入 `.gitignore`」。
- **归属说明**：属「文档断言与事实相反」，与本清单 §四 **A 类**（路径文案失真）同族，但**不在 431 已列核查范围内**（原核查范围未覆盖 `项目整体评估与升级建议.md`）⇒ §015 时**仅登记、不改本体**。

> **处置状态（2026-09-13 §016 落地）**：用户决策＝**段落级「订正注记」**。实施 **2 处，均仅追加、不改原文**：① 该文档头部「评估日期/范围/方法」之后新增一行「**阅读须知**」（声明本文为 2026-05-28 评估快照、原文保留不改、以各节带日期的订正注记为准）；② **§2.8「建议」块之后**追加 `> **订正注记（2026-09-13）**` blockquote（六条逐条复核表 ＋ 建议块两条已完成 ＋ Phase 1 1.4/1.6 顺带发现 ＋ 指向本清单 §9.4）。**原表格 6 行、建议 4 行、Phase 1 任务表一律未改**（该文为有日期的审计快照，与 §9.3「不改 B1 本体」同理）。文档 `wc -l` **377 → 391**（读取工具显示的 392 系 `wc -l` + 1，勿采信）。**注意**：§015 的口径是「不改该文档本体」，本批经用户明确决策**首次改动其本体**（仅追加）；本清单**未新增字母类**（A–G 仍为七类）。

> **处置状态（2026-09-13 §017 落地）**：用户决策＝**分类处置**。先只读复核（13 个根目录受控 `*.py` 全量比对：用途／引用方／内容差异／冻结时点／244 号迁移证据链），再实施 **15 处，全部为删除与归置，零业务代码改动**：① **删除 10 个**——3 组已分叉同名对的根副本（`sync_data.py` / `bulk_sync.py` / `chanlun_demo_simple.py`；活版在 `backend/`，全仓唯一活子进程调用即 `backend/app/routes/cache.py:189` 指向 `backend/bulk_sync.py`）、同名无关的根 `scheduler.py`（职责已由 `backend/data_daemon.py` ＋ `backend/app/scheduler.py` 取代）、废弃 demo／初始化脚本 4 个（`chanlun_demo.py` / `chanlun_correct.py` / `init_duckdb_cache.py` / `init_duckdb_simple.py`；`backend/` 侧对应物已分别于 2026-06-29 `865fb54`、2026-07-04 `e78b7df` 删除）、受控图片 2 个（`chanlun_demo.png` / `chanlun_correct.png`）；② **归置 5 个**——2026-08-06 `1b2cc7f` 的一次性审计脚本 `rebuild_snapshot.py` / `verify_311_v2.py` / `verify_312_b1.py` / `verify_312_impl.py` / `verify_313_full.py` 经 `git mv` 迁入 **`backend/scripts/`**（目录已存在、无同名冲突），其中 2 个因路径假设随位置失效而**就地各改 1 行**（`verify_312_impl.py` / `verify_313_full.py` 的 `sys.path.insert(..., "backend")` → `".."`；其余脚本内容一字未改，5 个仍须自项目根调用）。**依据**：全仓 `*.{sh,command,bat,plist}` / `Makefile` / `AGENTS.md` / `_项目运行手册.md` / 全部配置类文件的引用扫描 **零命中**；根副本仍走 PG ORM（`app.models.{Stock,DailyData}`）与硬编码旧绝对路径，与 331 号（PG→SQLite）、244 号（DuckDB 化）后的架构不符，属迁移前僵尸。**范围界定**：§016 处置 2.8.2 的**数量失真**（原文「6 个 `chanlun_*.py」实为 3 个）；本节处置其**实质部分**（根目录松散脚本与 `backend/` 同名文件重复）——经本批分类处置后**已不存在**。本批**未新增字母类**（A–G 仍为七类），亦**未 bump 版本**（fix 批次按约定仅登记进度）。

> **处置状态（2026-09-13 §018 落地）**：F2 ＋ G1 同源（决策＝**标注 + 最小真冗余收敛**）。**先只读复核**（dim1–8 配置引用面逐一比对）：**F2 结论**——`opportunity_atlas/dimensions/` 11 文件**无 `import yaml`／无 `app.services.status_config`／无任何 `get_status_engine_config`、`get_signal_registry`、`get_strategy_config` 调用**（唯一例外 `dim7_valuation_engine.py` 曾用 `os.getenv`，env 非 yaml）；**真接线面**仅 `status_engine.yaml`（`arbiter._load_thresholds` 读 `consensus.*`、`_load_conflict_rules` 读 `conflict_rules.*`、`StatusEngine._apply_l0` 读 `l0.hard_risks` / `soft_risk_coeff` / `emotion_position_cap` / `hold_only_stages`）与 `signal_registry.yaml`（**仅** `*.lifecycle.{initial,extended}.dist_pct` 被读，见 `status_engine.py` / `advice_builder.py` / `advice_engine.py`）；`strategy_defaults.yaml` 仅 `chip_distribution` **部分接线**；**死字段**＝`*.trigger` / `*.verify_days` / `*.verify_rule` / `*.lifecycle.*.days`。**再实施两类处置**：① **最小真冗余收敛 4 组（仅"双方 live 的字节级重复"，零行为变更）**——(a) `cross_validate._build_operation_advice` 软系数块 **7 处字面量改读 `status_engine.yaml l0.soft_risk_coeff`**（与 `StatusEngine._apply_l0` 同源读法，字面量降级为**配置缺失兜底**）；(b) `dim7_valuation_engine.py` 删除 **8 个**与 `valuation_estimator.py` / `potential_engine.py` **逐字节重复**的常量定义，改为**单向导入**（`CN_10Y_BOND_YIELD_PCT` / `QUALITY_ADJUST` / `INDUSTRY_CATEGORY` / `CATEGORY_WEIGHTS` ← `valuation_estimator`；`SENTIMENT_WEIGHT` / `EVENT_SCORE` / `TREND_SCORE` / `POTENTIAL_DIM_WEIGHTS` ← `potential_engine.DIM_WEIGHTS`），并保留本模块独有的 `LEVEL_CN` / `LEVEL_LIGHT`（注明"非重复，勿删"）。**证据 6 项**：ruff `All checks passed!`；8 常量对权威源逐一 `identity=True equal=True`；13 项行为断言（真 yaml 结果与旧字面量**逐一相同**＝零行为变更／哨兵值随 yaml 变化＝**真驱动**／空配置回落字面量＝兜底有效）；反向依赖 `grep` 仅 4 处且**全部只导入 `Dim7ValuationEngine` 类**；`tests/test_431_g2_config_loading.py` **19 passed**；`tests/test_fix_315_316.py` DB-free 子集 **8/8 passed**。② **标注-only 6 文件 8 处（不改值、不删除、不改导入）**——`weight_engine.py`（**整模块零消费方＝死代码**；`STATIC_WEIGHTS` ≡ `MARKET_REGIME_WEIGHTS` 实测 `== True`；`EMOTION_MULTIPLIERS` 无对应物）、`status_engine.py::MARKET_REGIME_WEIGHTS`（标注**唯一 live 权威**，消费于 `_aggregate` / `_aggregate_v390`）、`dim5_emotion_engine.py`（5 组硬编码常量，**无 yaml 对应物**）、`dim2_structure_engine.py`（缠论内联副本 2 处）、`dim4_chip_fund_engine.py` 与 `phase_detector.py`（`PhaseDetectionEngine._DIM_WEIGHTS` 双份，实测相等）。**本次复核对 431 原判的 2 处精确化**：(a) `dim2` 的 `ChanlunLevelValidator` **并非**字节级重复，而是**同源分叉**——本份较新（多级别重采样 amount 列可选、月度 `'ME'` 取代已废弃的 `'M'`，共 **23 行差异**），`app/engine/framework/` 那份**无 live 消费方**（仅被 `003-代码备份/` 引用）；(b) `dim2_structure_engine.py` 实为缠论框架的**整份 vendored 副本**（`chanlun_config` ＋ `chanlun_level_validator` ＋ `ChanlunAnalyzer` ＋ `multi_level`），其中 `chanlun_config` 7 类**6 类逐字节相同**、仅 `BiConfig` 少 `@dataclass` ⇒ **已另立待议，不在本批处置范围**。**未动**：删除 `weight_engine.py`、dim1–8 接线 yaml（属行为变更）、`dim5` 常量、`MARKET_REGIME_WEIGHTS` 取值、`dimension_weights` 键复活、**E1 / F4**。**环境说明**：`test_319_valuation_consistency.py` 及本批无关的 DB 型用例（`test_fix_315_316::test_f2_*` 等）在**运行中的 `data_daemon.py` 持有开发态共享库**时挂起于 `EnhancedCacheManager._execute`（faulthandler 栈为证），属**环境阻塞，非本次改动引入**。本批**未新增字母类**（A–G 仍为七类），亦**未 bump 版本**（fix 批次按约定仅登记进度）。

---

**核查日期**：2026-09-13（v1.1 / v1.2 / v1.3 增补同日）
**核查范围**：430 号执行后全仓复扫（路径口径 / 死键 / 归档快照 / 失效命令 / 环境收尾）＋ **v1.1 补录**（两套加载器 / 维度引擎配置引用 / 静默吞异常 / dim6 死码 / §9 第 7、8 条）＋ **v1.2 补录**（JUD 报告 `§6.2` 两行来源列 · `§1.1`「非缓存」表述）＋ **v1.3 补录**（残留 env 键漂移 7 键 / `.env.local` 孤儿待议 / B1 行号漂移）
**处置状态**：🔄 按批次落地中（逐项进度见下表）

### 处置进度（按批次，2026-09-13）

| 批次 | 条目 | 状态 | 记录 |
|:--:|---|:--:|---|
| §006 | A 类 + C 类（纯文案） | ✅ 已落地 | `001-沟通记录/2026-09-13.md` §6 |
| §007 | B1 + D1（归档可信度） | ✅ 已落地 | `001-沟通记录/2026-09-13.md` §7 |
| §008 | F3（静默吞异常） | ✅ 已落地 | `001-沟通记录/2026-09-13.md` §8 |
| §009 | G2（配置加载单测） | ✅ 已落地 | `001-沟通记录/2026-09-13.md` §9 |
| §010 | F1（合并两套配置加载器；430 唯一 P1） | ✅ 已落地 | `001-沟通记录/2026-09-13.md` §10 |
| §011 | E2（删 `.env.bak.331`）+ E3（env 键集对齐） | ✅ 已落地 | `001-沟通记录/2026-09-13.md` §11 |
| §012 | 431 v1.3 补录（存量待议归档：残留 env 键漂移 7 键 / `.env.local` 孤儿 / B1 行号漂移；纯文案） | ✅ 已落地 | `001-沟通记录/2026-09-13.md` §12 |
| §013 | 431 §9.1 残留 env 键漂移 7 键（登记模板 2 键 + `REDIS_URL` 注释澄清） | ✅ 已落地 | `001-沟通记录/2026-09-13.md` §13 |
| §014 | `REDIS_URL` 删键收尾（`.env` + `docker-compose.yml` + 启动器 plist 三处同步移除；§9.1 第 7 键闭环） | ✅ 已落地 | `001-沟通记录/2026-09-13.md` §14 |
| §015 | §9.2 `.env.local` 去向（决策＝**保留不动**；订正键数口径为 **9 同名 / 5 独有**并复验零消费方三证；新增 **§9.4** 登记 `项目整体评估与升级建议.md:267` 陈旧断言） | ✅ 已落地 | `001-沟通记录/2026-09-13.md` §15 |
| §016 | §9.4 `项目整体评估与升级建议.md` §2.8 陈旧断言（决策＝**段落级订正注记**；复核 §2.8 全表六条：五条不成立、2.8.2 数量失真；该文档头部加「阅读须知」＋ §2.8 段末追加带日期订正注记，**原表格/建议/Phase 1 一字未改**） | ✅ 已落地 | `001-沟通记录/2026-09-13.md` §16 |
| §017 | §9.4/2.8.2 实质部分——根目录松散脚本与 `backend/` 同名文件重复（决策＝**分类处置**；删 10 个〔3 组同名对根副本 + `scheduler.py` + 4 个废弃 demo/初始化脚本 + 2 张受控图片〕／`git mv` 归置 5 个至 `backend/scripts/`，其中 2 个各改 1 行 `sys.path`；**零业务代码改动**） | ✅ 已落地 | `001-沟通记录/2026-09-13.md` §17 |
| §018 | F2 + G1 同源（决策＝**标注 + 最小真冗余收敛**）。① 收敛 4 组（零行为变更）：`cross_validate._build_operation_advice` 软系数块改读 `status_engine.yaml l0.soft_risk_coeff`；`dim7_valuation_engine.py` 删 8 个逐字节重复常量改单向导入；`QUALITY_ADJUST`；`INDUSTRY_CATEGORY`/`CATEGORY_WEIGHTS`；潜力 4 常量。② 标注-only 6 文件 8 处（死模块 `weight_engine` ／ `MARKET_REGIME_WEIGHTS` ／ `dim5` 常量组 ／ dim2 缠论内联副本 ／ dim4 ＋ phase_detector `_DIM_WEIGHTS` 双份）。**并精确化 431 原判 2 处**（`ChanlunLevelValidator` 实为同源分叉、dim2 为整份 vendored 副本） | ✅ 已落地 | `001-沟通记录/2026-09-13.md` §18 |
| §019 | **E1 就地部分收敛**（写侧 7 处 `_query_df` → `_query_shard`：`daily_cache`×4 / `daily_basic_cache`×1 / `moneyflow_cache`×1 / `fina_indicator_cache`×1 ＋ 删 `sample_size` 死参数 ＋ 分库标记注释；写侧查询恒空阻断消除）＋ **F4 删死码**（dim6 6 处：两签名收敛 ＋ hard_veto 死分支×2 ＋ 死循环 ＋ 调用点与 `l0` 变量清除，1261 → 1247 行；2026-09-13 已落地，本批补登记）。**后续处置（2026-09-14）**：IC 权重月度滚动重估作业**已开 433 号**；docstring 口径统一 20 日；`tr` NameError 修复；dim2 level_validator framework 份删除。**残余待议**：dim2 `chanlun_config`/`chanlun_strategy` 双份均 live（合并=行为变更，另行决策）——**已闭环（2026-09-14）**：第一步（F4 混用消除 + 差异审计）＋ 第二步（**434 号**方案 A 全量：framework 权威 + dim2 回归 dim-import-framework，含背驰检测/快速模式恢复、shared 支撑阻力合并、dim2 删 4 区块 ~3740 行，**434 全量完成 v1.2**） | ✅ 已落地 | `001-沟通记录/2026-09-13.md` §19 ＋ `2026-09-14.md` §10/§11 |
