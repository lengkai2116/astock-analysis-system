# 数据情况核查报告

**报告编号**：063  
**报告日期**：2026-05-20  
**核查范围**：缓存数据、数据库配置、日线数据存储情况

---

## 一、核查背景

用户反映"缓存数据中怎么会没有日线的数据"，需要核查方案存档内容并确认数据库配置情况。

---

## 二、核查发现

### 2.1 数据库文件分布情况

| 数据库路径 | 文件大小 | 类型 | 实际数据量 | 说明 |
|-----------|---------|------|-----------|------|
| `quant_strategy_analyzer/data/historical_data.db` | 11MB | SQLite | **0条记录** | 所有表均为空 |
| `stock_analyzer_desktop/server/data/screening/cache/stock_cache.db` | **2GB** | SQLite | 21,016条记录 | JSON格式财务数据 |
| `01-A股股票分析系统/docker-postgres/stock_analysis` | PostgreSQL | PostgreSQL | 10只股票基本信息，0条日线数据 | 当前系统使用 |

---

### 2.2 历史数据库详细检查

#### 2.2.1 historical_data.db（应为日线主数据库）

**文件路径**：`/Users/kalence/Desktop/测试/quant_strategy_analyzer/data/historical_data.db`

**表结构检查**：
```sql
CREATE TABLE daily (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    ts_code TEXT,
    trade_date TEXT,
    open REAL,
    high REAL,
    low REAL,
    close REAL,
    pre_close REAL,
    change REAL,
    pct_chg REAL,
    vol REAL,
    amount REAL,
    UNIQUE(ts_code, trade_date)
);
```

**数据统计**：
- daily表：**0条记录** ❌
- stk_limit表：**0条记录** ❌
- moneyflow表：**0条记录** ❌
- top_list表：**0条记录** ❌

**结论**：虽然数据库文件存在，表结构正确，但所有数据表均为空。

---

#### 2.2.2 stock_cache.db（财务数据缓存）

**文件路径**：`/Users/kalence/Desktop/测试/stock_analyzer_desktop/server/data/screening/cache/stock_cache.db`

**表结构**：
```sql
CREATE TABLE stock_cache (
    ts_code TEXT,
    data_type TEXT,
    data TEXT,  -- JSON字符串
    update_time TIMESTAMP,
    PRIMARY KEY (ts_code, data_type)
);
```

**数据统计**：21,016条记录

**数据类型示例**：
- ts_code: "000001.SZ"
- data_type: "financial"（财务数据）
- data: 包含资产负债、利润表等财务指标的JSON字符串

**结论**：此数据库存储的是**财务报表数据**（资产负债表、利润表），不是**日线行情数据**。

---

### 2.3 方案存档核查

#### 2.3.1 051号报告：数据下载完成总结报告

**报告日期**：2026-05-17

**报告声称**：
- ✅ 总记录数：17,234,699条
- ✅ 日线行情：7,560,712条（2020-01-02 到 2026-05-15）
- ✅ 涨跌停限制：9,556,948条
- ✅ 龙虎榜数据：117,039条

**实际情况**：
- ❌ historical_data.db数据库中**所有表均为0条记录**
- ❌ 无法找到声称的1700万条数据

**矛盾分析**：
1. 报告声称下载成功，但数据库为空
2. 可能原因：
   - 数据下载脚本执行但未成功保存
   - 数据存储在其他位置
   - 报告生成时间早于实际下载失败
   - 数据库被清空或覆盖

---

#### 2.3.2 057号文档：阶段二数据层开发计划

**制定日期**：2026-05-19

**计划内容**：
1. ✅ 数据库表结构设计（stocks, daily_data, signals, holdings）
2. ✅ Tushare数据服务代码实现
3. ✅ DuckDB缓存配置
4. ❌ **未执行**：实际数据同步到PostgreSQL数据库

**计划执行状态**：仅完成代码实现，未实际同步数据

---

## 三、当前系统数据库配置

### 3.1 PostgreSQL配置

**环境变量**（.env）：
```env
DATABASE_URL=postgresql://stock_user:stock_password@postgres:5432/stock_analysis
```

**数据库名称**：stock_analysis

**表结构**：
```sql
-- 阶段一/二已创建的表
- stocks (10条记录) ✅
- daily_data (0条记录) ❌
- signals (已有表结构)
- holdings (已有表结构)

-- 阶段三新增的表
- technical_indicators ✅
- watchlist ✅
- user_memory ✅
- portfolio ✅
- portfolio_holdings ✅
- paper_trades ✅
- notification_rules ✅
- notification_history ✅
```

**数据情况**：
- 股票基本信息：10只股票（000001.SZ, 600519.SH等）
- 日线数据：**0条记录** ❌
- 技术指标数据：无数据（等待日线数据）

---

### 3.2 Redis配置

**环境变量**：
```env
REDIS_URL=redis://redis:6379/0
```

**当前状态**：运行正常 ✅

**用途**：缓存热点数据、会话管理

---

### 3.3 DuckDB配置（计划中）

**计划路径**：`/data/duckdb/stock_cache.db`

**当前状态**：未创建 ❌

**原计划用途**：
- 热数据缓存
- 快速查询支持
- 技术指标计算加速

---

## 四、问题根因分析

### 4.1 日线数据缺失的核心原因

| 阶段 | 问题 | 影响 |
|------|------|------|
| 数据下载阶段 | historical_data.db数据库为空 | 数据未成功保存 |
| 数据同步阶段 | 未执行sync_data.py脚本 | PostgreSQL无数据 |
| API查询阶段 | 技术指标计算依赖日线数据 | 功能不可用 |

### 4.2 数据流转链路

```
Tushare API
    ↓
数据下载脚本
    ↓
SQLite (historical_data.db) [当前为空]
    ↓
sync_data.py [未执行]
    ↓
PostgreSQL (stock_analysis.daily_data) [当前为空]
    ↓
技术指标计算
    ↓
API返回结果
```

---

## 五、解决方案

### 5.1 立即解决方案：执行数据同步

**步骤1**：确保Tushare Token有效
```bash
# 检查Token配置
grep TUSHARE_TOKEN /Users/kalence/Desktop/测试/01-A股股票分析系统/.env
```

**步骤2**：执行数据同步脚本
```bash
# 进入后端容器
docker exec -it 01-a-backend-1 bash

# 执行同步
cd /app
python sync_data.py
```

**步骤3**：验证同步结果
```bash
docker exec 01-a-postgres-1 psql -U stock_user -d stock_analysis -c "SELECT COUNT(*) FROM daily_data;"
```

---

### 5.2 备选方案：使用现有数据

如果historical_data.db确实无法恢复，可以：

**方案A**：重新从Tushare下载
```python
# 修改sync_data.py，指定日期范围
python sync_data.py --start 2024-01-01 --end 2026-05-20
```

**方案B**：从stock_cache.db提取财务数据（如果需要基本面数据）

---

### 5.3 数据源配置优化

建议在sync_data.py中添加：
```python
# 数据源优先级配置
DATA_SOURCES = {
    'primary': 'tushare',      # 主要数据源
    'cache': '/path/to/cache', # 缓存路径
    'backup': 'sqlite_db'       # 备用数据源
}
```

---

## 六、结论

### 6.1 核查结论

1. ❌ **历史数据不存在**：虽然051号报告声称下载了1700万条数据，但historical_data.db实际为空
2. ❌ **当前系统无日线数据**：PostgreSQL的daily_data表为0条记录
3. ✅ **技术架构正确**：代码、数据库表结构、API接口均已就绪
4. ✅ **数据源可用**：Tushare Token配置正确（5000积分权限）

### 6.2 下一步行动

**立即执行**：
1. 执行sync_data.py同步日线数据到PostgreSQL
2. 验证数据同步成功
3. 测试技术指标计算功能

**建议优化**：
1. 建立数据同步监控机制
2. 配置数据备份策略
3. 添加数据完整性检查

---

## 七、附录

### 7.1 相关文件路径

| 文件 | 路径 |
|------|------|
| 阶段二开发计划 | `/Users/kalence/Desktop/测试/002-方案存档/057-A股股票分析系统-阶段二数据层开发计划.md` |
| 数据下载报告 | `/Users/kalence/Desktop/测试/002-方案存档/051-数据下载完成总结报告.md` |
| 当前系统数据库 | PostgreSQL: `stock_analysis` |
| 历史数据DB | `/Users/kalence/Desktop/测试/quant_strategy_analyzer/data/historical_data.db` |
| 财务缓存DB | `/Users/kalence/Desktop/测试/stock_analyzer_desktop/server/data/screening/cache/stock_cache.db` |

### 7.2 数据同步脚本位置

**当前系统**：
```bash
/Users/kalence/Desktop/测试/01-A股股票分析系统/sync_data.py
```

### 7.3 建议的同步命令

```bash
# 同步所有股票
docker exec -w /app 01-a-backend-1 python sync_data.py

# 查看同步日志
docker logs 01-a-backend-1 | grep -A 5 "sync"
```

---

**报告编制时间**：2026-05-20 10:00  
**报告状态**：核查完成  
**下一步**：执行数据同步
