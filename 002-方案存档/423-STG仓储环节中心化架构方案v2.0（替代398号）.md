---
title: STG仓储环节中心化架构方案 v2.0（替代398号）
type: 架构方案
date: 2026-09-10
version: v2.0
status: ✅ 已定稿（D1-D3 已确认，2026-09-10；待实施）
related:
  - 353-系统总体架构规划（统一命名·全链路梳理）
  - 356-第2层STG存储层分库实施方案与规则体系
  - 357-分库迁移（strategy_signal_detail 迁入 snapshot_cache.db，355号注记）
  - 396-dim2结构位置引擎全面核查报告与修复方案
  - 397-维度引擎evidence字段消费链路核查方案
  - 398-STG仓储环节中心化架构方案（v1.0，本方案替代）
  - 399-dim梳理产出报告
  - 408-dim1功能重构与SIG-JUD分工优化方案
  - 410-dim2-dim7层数据分层审计与RAW前置计算迁移方案
  - 412-SIG到RAW迁移实施状态审计与消费者接入补充方案
  - 420-dim8状态总结引擎消费面增强方案
  - 421-SIG信号写库路径对齐与失败暴露根治方案
  - 422-JUD-OU写分库验证暴露API误用根治与读方补齐方案
---

# 423 — STG仓储环节中心化架构方案 v2.0（替代398号）

> **本方案替代 398 号方案（v1.0，2026-09-02 编制）**。398 号作废。
> **替代原因**：398 号问题诊断基线已被 421/422 号方案部分修复（写入路由/失败暴露/读写分叉已闭环）；5 个设计缺陷需修正（阻塞式多轮核查、双补算通道、硬编码阈值、门禁放错层、告警通道不适配）；Phase A（dim 梳理）已完成，工期可压缩。v2.0 仅保留真实缺口（8 项）作为实施目标。

---

## 0. 修订摘要（v1.0 → v2.0）

| 维度 | v1.0 | v2.0 变更 |
|---|---|---|
| 诊断基线 | 基于 09-02 代码 | 剔除已闭环项（421/422），仅保留真实缺口（8 项） |
| WriteGateway | 新建锁机制 | 薄封装：校验+日志，锁/事务交给 sharding_manager |
| QualityChecker | 硬编码 min_rows=4000 | 动态覆盖率基准（以 daily_cache 当日行数为锚） |
| RecomputeScheduler | 独立补算队列 | 复用 `_run_pipeline_step` + `run_integrity_check`；与 sync_requests 划清边界 |
| 多轮核查 | `time.sleep(300)` 阻塞 | 事件驱动（复用主循环 30s tick） |
| OutputSemanticController | 放 dim8 内抛异常 | 拆分：数据门禁在 daemon 侧（STG），语义化在 OUT 侧（dim8） |
| 工期 | Phase A+B+C ≈ 14 天 | ≈ 10 天（Phase A 已完成，免除） |

---

## 1. 问题诊断更新

### 1.1 已闭环项（不再纳入 v2.0 实施范围）

| v1.0 问题 | 修复方案 | 证据 | 状态 |
|---|---|---|---|
| SIG 写库路由不一致 | 421 R1 | `_batch_write_signal_detail` 走 `sharding_manager.execute_batch_insert` → snapshot_cache.db 分库 | ✅ 闭环 |
| 写失败静默吞错 | 421 R3 | `SignalWriteError` → SIG failed → 管道重试 ≤3 次 | ✅ 闭环 |
| snapshot 分库 busy_timeout 不足 | 421 R2 | 10s → 30s | ✅ 闭环 |
| JUD/OUT 直连写主库 | 422 R1/R5 | 4 处写路径 API 误用根治 + 主库残留清理 | ✅ 闭环 |
| 管道不可恢复 | 既有机制 | `_run_pipeline_step` retry_count≤3 + 4h timeout 重置 + 幂等锁 | ✅ 闭环 |

### 1.2 真实缺口（v2.0 目标，8 项）

| # | 缺口 | 严重度 | 对应章节 |
|---|---|---|---|
| G1 | 无统一"写前/写后校验"封装（各写入点散落） | P0 | §3.2 WriteGateway |
| G2 | 无按表质量规则 + 跨表日期对齐校验 | P0 | §3.1/§3.3 QualityChecker |
| G3 | SIG 计算结果无自检（signal_json/dim_results_json/seven_dim_json 无 JSON/非空验证） | P0 | §3.3 |
| G4 | 无"问题→责任环节"定向补算映射 | P1 | §3.4 RecomputeScheduler |
| G5 | 无多轮核查 + 告警升级闭环 | P1 | §3.5 |
| G6 | dim8 空字段仍静默降级，未区分"管道故障/正常结论" | P1 | §3.6 语义化 |
| G7 | 数据库健康监控未系统化（WAL/写锁冲突/重试无聚合） | P2 | §4 |
| G8 | 历史日期缺口无自动检测修复 | P2 | §3.5 历史巡检 |

---

## 2. 架构设计（关键修订）

### 2.1 修订原则

```
STG 网关 = sharding_manager（路由+锁+幂等，既有）
         + WriteGateway（薄封装：校验+日志，新增）
         + QualityChecker（质量规则，新增）
         + RecomputeScheduler（定向补算，复用既有执行通道）
         + 语义化门禁（daemon 侧判定管道故障；OUT 侧输出语义文本）
```

**分层边界（v2.0 核心修正）**：
- **数据门禁层（daemon/STG）**：校验不通过 → 补算/标记 failed → **不产出**。抛出/记录的是"管道故障"，不落 snapshot 成品。
- **语义化层（OUT/dim8）**：只处理"校验已通过"的数据，把正常空值（无背驰/无买卖点）转为有意义文本。
- v1.0 把 `DataPipeFailureError` 放在 dim8 内部不合理——API 进程无法触发 daemon 补算，且会污染前端异常。**v2.0 上移门禁到写入前**。

### 2.2 组件 1：WriteGateway（薄封装）

```python
class WriteGateway:
    """统一写入网关——sharding_manager 之上的薄封装
    职责：写前格式校验 → 委托 sharding_manager 执行（锁/事务/幂等）
         → 写后行数校验 → 写入审计日志
    不持有锁、不建连接池；全部委托既有 sharding_manager。
    """
    def write_batch(self, table: str, rows: list,
                    pipeline_date: str, expected_ratio: float = 0.95) -> WriteResult:
        # 1) 写前校验：validate_before_write()（行非空、必填字段、JSON 可解析）
        # 2) 委托 sharding_manager.execute_batch_insert(table, sql, rows)
        #    —— 复用 _write_locks 串行化 + INSERT OR REPLACE 幂等
        # 3) 写后校验：count(table, pipeline_date) >= daily_base * expected_ratio
        # 4) 审计：写入 qa_audit_log（pipeline_date/table/rows/校验结果）
```

**待改造写入点（5 处现状表，均已在分库目标位）**：

| 写入点 | 当前位置 | 现状 | v2.0 改造 |
|---|---|---|---|
| `_batch_write_signal_detail` | data_daemon.py:5063 | 已走 sharding_manager（421R1） | 包裹 `write_batch`，加写前 JSON 校验 + 写后行数校验 |
| `_build_status_snapshot` | data_daemon.py:4329 | 已走分库（422R1） | 包裹 `write_batch`，加写后校验 |
| `_build_treemap_snapshot` | data_daemon.py:3862 | 已走分库（422R1） | 同上 |
| `cache_indicators_wide` | enhanced_cache_manager.py:1293 | 直写 compute_cache.db | 走 `write_batch`（表=indicator_*） |
| `_insert_from_df` | enhanced_cache_manager.py:186 | 通用写入口 | 可选接入（按表路由校验规则） |

### 2.3 组件 2：QualityChecker（动态覆盖率）

**v2.0 修正 v1.0 的两个规则缺陷**：
1. 全表 `min_rows` 阈值错误：`factor_cache`（3715 万行累积）/`pre_feat_cache`/`daily_cache` 均为**累积表**，总行数阈值无意义。改为**按目标日期当日行数**校验。
2. 4000 行硬编码含义不清（≈全市场 73%）。改为**覆盖率基准**：以 `daily_cache` 目标日期行数 N 为锚，其他表行数 ≥ N×ratio。

```python
RULES = {
  # 每表校验维度：rows_ratio（对 N 的覆盖率）/ date_align / required_fields / json_fields
  'daily_cache':        {'rows_ratio': 1.00, 'date_align': True},
  'indicator_ma':       {'rows_ratio': 0.95, 'date_align': True, 'required_fields': ['ma5','ma20'],
                          'src': 'daily_cache'},
  'indicator_macd':     {'rows_ratio': 0.95, 'date_align': True, 'required_fields': ['macd_dif']},
  'indicator_other':    {'rows_ratio': 0.95, 'date_align': True, 'required_fields': ['rsi14']},
  'pre_feat_cache':     {'rows_ratio': 0.95, 'date_align': True, 'json_fields': ['features_json']},
  'factor_cache':       {'rows_ratio': 0.95, 'date_align': True},  # 按当日批次，非总行数
  'strategy_signal_detail': {'rows_ratio': 0.95, 'date_align': True,
                          'required_fields': ['signal_json','dim_results_json'],
                          'json_fields': ['signal_json','dim_results_json','seven_dim_json'],
                          'seven_dim_keys': 7},   # seven_dim_json 须含 7 个 dim 键（G3）
  'status_snapshot':    {'rows_ratio': 0.95, 'date_align': True,
                          'required_fields': ['dim_states','status_bar']},
  'treemap_snapshot':   {'rows_ratio': 0.95, 'date_align': True},
}
```

**G3 专项（SIG 质量自检，v2.0 新增）**：在 `_precompute_strategy_signals` 批量写库前，对每条 record 校验 `signal_json`/`dim_results_json` 可 `json.loads` 且非空、`seven_dim_json` 含 7 个 dim 键；任一条不合法 → 整批标记 failed（复用 421R3 的 `SignalWriteError` 重试路径），杜绝"计算出来但内容是坏的"。

### 2.4 组件 3：RecomputeScheduler（复用既有通道）

```
问题类型 → 责任环节 → 执行入口（全部复用既有函数，不新建采集/计算通道）
```

| 问题 | 责任环节 | 执行入口 | 通道 |
|---|---|---|---|
| daily_cache 缺失/不对齐 | COL | `run_integrity_check` 补采 / `_batch_daily` | 完整性检查（既有） |
| indicator_* 缺失/不对齐 | RAW-1 | `_precompute_indicators` (data_daemon.py:4974) | 管道步骤重跑 |
| pre_feat_cache 缺失 | RAW-2 | `_precompute_raw_features` | 管道步骤重跑 |
| factor_cache 缺失 | RAW-3 | `_precompute_preset_combos` | 管道步骤重跑 |
| strategy_signal_detail 缺失/JSON 无效 | SIG | `_precompute_strategy_signals` (data_daemon.py:4994) | 管道步骤重跑 |
| status/treemap 缺失 | JUD/OUT | `_build_status_snapshot` / `_build_treemap_snapshot` / `_out_transmit_seven_dim` | 管道步骤重跑 |

**双通道边界（v2.0 明确定义）**：

| 通道 | 触发方 | 用途 | 执行 |
|---|---|---|---|
| 管道内补算（本方案） | daemon 侧 QualityChecker | 日终/全管道校验发现缺口 | `_run_pipeline_step` 重跑指定步骤 |
| sync_requests（既有，327号） | API 调用层 | 前端请求时发现数据缺失 | `_consume_sync_requests_batch` (data_daemon.py:4531) |

禁止 RecomputeScheduler 写 sync_requests，反之亦然——两条通道互不串扰，避免队列语义混乱。

### 2.5 组件 4：输出语义化（门禁上移后的拆分设计）

```
daemon 侧（STG 门禁，本方案）：
  QualityChecker 发现 signal_json/seven_dim_json 缺失或管道未 done
    → 不产出 → 触发补算 → 补算仍失败 → pipeline_status 记 failed + 告警
  → 前端拿到的是"确定性数据"或明确的 503/空态

OUT 侧（dim8 语义化，并入 397 号方案实施【D1 已确认 2026-09-10】）：
  dim8 _build_*_text() 对"校验已通过但正常为空"的字段输出语义文本
  EMPTY_SEMANTICS = {'buy_sell_points': '无明显买卖点信号',
                     'divergence_type': '无背驰信号', ...}
  —— 此部分不再抛 DataPipeFailureError（门禁已上移）
  —— 由 397 号方案统一承接（与 evidence 消费同批次改造），423 不在 dim8 侧做改动
```

---

## 3. 时序模型（v2.0 修订：去阻塞）

| Round | 触发时机 | 内容 | 执行方式 |
|---|---|---|---|
| R1 | 每写入点完成后 | `write_batch` 内写后行数/日期校验（轻量） | 同步，毫秒级 |
| R2 | 全管道步骤 done 后 | 跨表对齐校验（indicator↔daily 等） | daemon 主循环下一 tick 触发（不 sleep） |
| R3 | 次日开盘前 / 定时巡检 | 多轮核查 + 历史日期巡检（G8） | 事件驱动：失败→打 QA-CHECK pending→下轮再校验 |

**事件驱动多轮核查（替代 v1.0 `time.sleep(300)`）**：

```
主循环 tick（30s）：
  若存在 QA-CHECK pending 或管道全部 done：
    QualityChecker.multi_round_once(pipeline_date)   # 单轮，非阻塞
      → passed：QA-CHECK 标 done
      → failed：schedule_recompute() → QA-CHECK retry_count+1
                 → retry_count > 3 → 告警升级（L2/L3）
  主循环照常继续（采集/补采不受影响）
```

QA-CHECK 复用 `pipeline_status` 的一个 step_id【D3 已确认 2026-09-10】，天然复用幂等锁 + retry_count + timeout 机制，不新增状态表；问题明细写入 detail 字段（JSON 序列化）。

---

## 4. 数据库健康监控（适配 SQLite 实际）

| 监控项 | v1.0 | v2.0（适配单机 SQLite 短连接） | 动作 |
|---|---|---|---|
| WAL 文件大小 | >500MB 触发 checkpoint | 保留，并入 wal_maintenance.py | checkpoint |
| 连接池使用率 | >80% 告警 | **删除**（无连接池）→ 改为监控**写锁冲突次数**（catch `database is locked` 计数） | 告警 |
| 写入队列深度 | >100 告警 | 改为 `sync_requests` pending 积压数（data_daemon.py:4531 已可查） | 告警 |
| 校验失败率 | >5% | 保留，按 qa_audit_log 统计 | 告警+暂停写入 |
| 补算重试次数 | >3 | 保留（QA-CHECK retry_count） | 告警升级 |

新增可观测指标（用于验证收益，§5）：`写锁冲突计数`、`校验失败率`、`补算触发次数`，经 `/api/v3/health` 聚合暴露。

---

## 5. 告警分级（适配桌面单机，无邮件基础设施）

| 级别 | 通道 | 触发 |
|---|---|---|
| L1 | daemon 日志 | 单轮校验失败（日志） |
| L2 | `/api/v3/health` + `/ready` 暴露 QA 状态 | retry>3 或校验失败率>5% |
| L3 | 前端状态页可见（本轮仅 L1+L2）【D2 已确认 2026-09-10】 | 次日开盘前仍未通过 |

【D2 已确认】本轮仅实现 L1+L2；代码预留 webhook 扩展点（配置项 + 空实现接口），未来需主动推送时填充实现，不返工。

---

## 6. 实施计划（v2.0，工期 ≈ 10 天）

| 阶段 | 内容 | 工时 | 前置 | 交付物 |
|---|---|---|---|---|
| C-0 | 修订方案定稿（本文档；D1-D3 已确认，397 边界已划清） | 0.5天 | — | 方案定稿 |
| C-1 | 校验规则注册表（§2.3 RULES）+ qa_audit_log DDL | 1天 | C-0 | 规则配置 + 表结构 |
| C-2 | WriteGateway（薄封装）+ 5 处写入点改造 | 2天 | C-1 | WriteGateway + 改造后代码 |
| C-3 | QualityChecker（单表 + 跨表对齐 + G3 SIG 自检） | 1.5天 | C-2 | QualityChecker |
| C-4 | RecomputeScheduler（问题→步骤映射，复用 `_run_pipeline_step`） | 1.5天 | C-3 | RecomputeScheduler |
| C-5 | 事件驱动多轮核查（QA-CHECK）+ 告警 L1/L2 + 历史巡检(G8) | 1.5天 | C-4 | 多轮核查闭环 |
| C-6 | 健康监控指标 + 写锁冲突计数 | 0.5天 | C-5 | 监控聚合 |
| C-7 | 故障注入集成测试 + 收益基线对比 | 1.5天 | C-6 | 测试报告 |

**减免说明**：v1.0 的 Phase A（dim 梳理 3 天）已完成（399号 dim2-dim7 + 408号 dim1），Phase B（397号）单独排期，不占本方案工期。

### C-7 故障注入测试用例（v2.0 修订 v1.0 的"5 并发"）

| 用例 | 注入 | 期望 |
|---|---|---|
| 写后校验失败 | 写入后删行/日期错位 | 识别缺失 → 触发补算 |
| SIG JSON 损坏 | 注入非法 signal_json | 整批 failed → 管道重试 → 校验通过 |
| 跨表不对齐 | indicator 日期缺一日 | R2 检出 → RAW-1 定向补算 |
| 连续失败告警 | 持续注入缺失 | 3 轮后 QA-CHECK 告警 L2 |
| 事件驱动非阻塞 | 注入长补算 | 主循环采集 tick 不受阻塞（回归保障） |

---

## 7. 关联方案更新

| 方案 | 关系 | 状态 |
|---|---|---|
| 353/356/357 | 架构/分库基础 | 沿用 |
| 396/397 | 问题驱动 | 397 承接 dim8 侧语义化+evidence 改造（D1 已确认） |
| 398 | v1.0 本方案替代 | **作废** |
| 408 | dim1 重构（Phase A 已免除的依据） | 已完成 |
| 420 | dim8 消费面增强 | 已完成，语义化需与其对齐 |
| 421/422 | 本方案 §1.1 已闭环项 | 已完成 |
| 410/412 | RAW 迁移审计 | 已完成，校验规则基于其最终表结构 |

---

## 8. 验收标准（可量化）

1. 全市场目标日期：daily_cache / indicator_* / strategy_signal_detail / status_snapshot / treemap_snapshot 覆盖率 ≥95%，跨表日期对齐 100%
2. 注入 SIG JSON 损坏 → 自动补算成功，前端无"坏数据"产出
3. 写锁冲突计数较基线下降（记录改造前 7 天基线）
4. 故障注入 5 用例全绿；主循环采集 tick 延迟不因 QA 增加
5. `/api/v3/health` 正确聚合 QA 状态与监控指标

---

## 9. 已确认决策（定稿记录，2026-09-10）

| 编号 | 决策点 | 确认结果 |
|---|---|---|
| D1 | dim8 语义化改造归属 | **并入 397 号方案**——423 只做 daemon 侧门禁；dim8 语义化 + evidence 消费合并为 397 一次改造批次，避免重复改 `_build_*_text()` |
| D2 | L3 告警通道 | **仅 L1+L2**（日志 + health 聚合）；代码预留 webhook 扩展点，未来需要时填充 |
| D3 | QA-CHECK 状态存储 | **复用 pipeline_status step_id**——天然复用幂等锁/retry/timeout，问题明细存 detail 字段 JSON |

> 决策理由详见沟通记录 2026-09-10 §005 说明。

---

**方案编制日期**：2026-09-10（v2.0 草案 → 定稿，D1-D3 已确认）
**编制依据**：398号 v1.0 方案 + 421/422 号实施结果核查 + 系统运行实测数据
