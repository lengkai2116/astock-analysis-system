# 475 号｜dim6 现状层补产出与修正（P1-P5 + P7 + P16）

**定位**：实施《464-dim8-dim6现状描述输出定稿（风险边界+价格位置）》§六 登记的 dim6 现状层补产出与修正（对标 **468 号 = dim4 定稿 §六 补产出实施** 先例）。
**范围**：仅 SIG 现状层（`status_description`/`audit` 呈现与透传 + 可观测性）；**不碰判定逻辑（果）**（445 冻结边界）；audit④ 仅对齐文案（用户 4B 裁定）。

## 起因
dim6 定稿（2026-09-23）逐键深挖发现 9 项问题 + 补充核查新增 4 项（⑩-⑬）；本号实施其中**不触碰判定逻辑**的 7 项：P1-P5、P7、P16。
- **P6 已撤销**：补充核查确认 `signal_days` 的 null 是语义正确（未突破前 60 日高点 → 无信号天数），非供给缺口——`shared.calc_support_resistance` 已并入该键、daemon `risk_ext` 已正常写入（`data_daemon.py:3655`）。
- **P8 顺延**：judgment 冗余键属 439 批次。
- **P9-P15 不做**：属 dim8 归集层跨维共性（用户裁定 B 层不做）。

## 实施清单与设计

| # | 项 | 实现 |
|---|---|---|
| P1 | `risk_sources` 明细透传 | `_assess_risk_level` 已产 `risk_sources`（5 源 name/level）；`evaluate` 捕获后写入 `status_description['risk_sources']` = `["缠论风险：高", ...]`（list[str]，对齐 `risk_factors` 风格、供 dim8 `_flatten_value` 渲染）。**⑫约束**：事件升格/PIERS 分支原以 `risk_info = {新 dict}` 整体替换会丢 `risk_sources` → 改为 `{**risk_info, ...}` 合并 |
| P2 | `piers_leverage` 未触发也透传 | `status_description['piers_leverage']` 改为**总是**输出可用 metrics（`{debt_to_assets, roce}` 去 None）；新增 `piers_leverage_triggered`(bool) 消歧（原"空 dict = 未触发"语义不再可辨） |
| P3 | 兜底「无显著风险」矛盾 | 兜底从 `_list_risk_factors` 内移出 → `evaluate` 在 `event_risks` + PIERS-E **全部 extend 之后**判空补；删除 471 号仅覆盖 event_risks 的临时去重段（已成死码） |
| P4 | `risk_detail` 三路文案统一 + 保留源计数 | `_assess_risk_level` 基础文案改为 `N个高风险源：{names}` / `无高风险源`；升格路径改为**追加后缀**（`（事件升格：xxx）` / `（PIERS 硬性否决事件：造假/退市/ST退市）`），不再覆写计数 |
| P5 | audit④ 文案与实现对齐 | 条件名 `无高风险事件` → **`无极高风险事件`**（实现 = 无 `severity=='极高'`），threshold 维持「无极高风险」；**不改判据** |
| P7 | `right_side_confirm` 注释订正 | `_build_invalidation` 注释删除「死代码（404 DATA-03）」断言——实测 RAW pre_feat 产 `'否决'`（茅台/招行命中 priority-3） |
| P16 | 静默吞异常改 warning | `_assess_liquidity` 两处 bare `except Exception: pass` → `logger.warning`；`evaluate` ECM 日线读失败 `df=None` → 补 `logger.warning`（对齐 464-7 dim4 先例） |
| ⑪ | geo fallback 补键 | `evaluate` df 缺失兜底 geo dict 补 `dist_to_prev_high_pct: None` |

## 影响面核查（实施前）
- `piers_leverage` / `risk_sources`：全仓 **无生产消费方**（仅测试）。
- audit 条件名 `无高风险事件`：被 `tests/test_459_dim6_riskaudit.py`（`_audit()` helper）、`tests/test_453_dim6_st_warning.py`（2 处）按名消费 → 同步更新。
- `risk_detail` / `无显著风险`：无测试断言。

## 验证计划
- 新增 `tests/test_475_dim6_passthrough.py`（P1-P5/P7/P16 + ⑪ 覆盖）。
- 回归：dim6 系列（448/452/453/459/461_dim6/461_dim11）+ 更新后的 459/453。
- 8 股全链路探针（`_dim6_definition_probe.py`）复跑，核对 P1-P5 实际产出与定稿示例一致。

## 落地状态（✅ 已实施 2026-09-23）
- **代码**：`backend/app/opportunity_atlas/dimensions/dim6_risk_engine.py`（P1-P5/P7/P16 + ⑪ 全部落地；582 → 604 行）。
- **测试**：
  - 新增 `backend/tests/test_475_dim6_passthrough.py` **12 passed**（P1、⑫、P2×2、P3×2、P5×2、P7×2、P16、⑪）。
  - 同步更新按条件名消费的既有测试：`test_459_dim6_riskaudit.py`（`_audit()` helper）、`test_453_dim6_st_warning.py`（2 处）→ `无极高风险事件`。
  - 回归：**59 passed**（475 12 + 459 5 + 453 12 + 448 13 + 452 17，0.61s）；另 **12 passed**（461_dim6_depth_dead_keys 5 + 461_dim11_support_resistance_unify 7）。
- **真实数据核验**（8 股全链路 `StatusEngine.evaluate`，daemon 停止态）：
  - P1 8 股均产 5 源明细；茅台 `risk_sources=['缠论风险：高','财务风险：低','事件风险：低','主力风险：高','流动性风险：低']`。
  - P2 茅台（未触发）`piers_leverage={debt_to_assets:12.81, roce:33.42}` + `triggered=False`；万科 `triggered=True`。
  - P3 300750 由 `['综合：无显著风险（无）','PIERS-E：资本回报率偏低…']` → `['PIERS-E：资本回报率偏低（ROCE 9.1%<15%）（中）']`（矛盾消除）。
  - P4 茅台 `2个高风险源：缠论风险、主力风险`；万科 `1个高风险源：财务风险（事件升格：longhubang）`（源计数保留 + 升格原因可见）。
  - P5 audit 条件名 `无极高风险事件`（8 股全部）；⑫ 万科升格路径源明细完整。
- **测试环境提示**：dim6 测试须 **daemon 停止态**运行（SQLite 写锁：daemon 持锁时 pytest 会 0 CPU 阻塞于 DB 读）。

