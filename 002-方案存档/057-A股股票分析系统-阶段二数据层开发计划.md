# A股股票分析决策支持系统 - 阶段二：数据层开发

> **项目编号**：055
> **阶段**：阶段二 - 数据层开发
> **制定日期**：2026-05-19
> **方案编号**：057

---

## 目录

1. [阶段目标](#1-阶段目标)
2. [任务分解](#2-任务分解)
3. [数据库表创建](#3-数据库表创建)
4. [数据获取服务](#4-数据获取服务)
5. [缓存服务](#5-缓存服务)
6. [数据管理API](#6-数据管理api)
7. [部署与测试](#7-部署与测试)

---

## 1. 阶段目标

| 目标 | 说明 | 优先级 |
|------|------|--------|
| 数据库表结构创建 | 完成stocks、daily_data、signals等核心表 | P0 |
| Tushare数据集成 | 实现从Tushare获取股票基础信息和日线数据 | P0 |
| 缓存服务搭建 | 配置Redis和DuckDB缓存 | P1 |
| 数据管理API | 提供股票列表、日线数据等基础API | P0 |

---

## 2. 任务分解

| 任务 | 预计时间 | 状态 |
|------|---------|------|
| 创建数据库表 | 4h | ⏳ |
| 配置数据库连接 | 2h | ⏳ |
| 实现Tushare数据服务 | 8h | ⏳ |
| 实现缓存服务 | 4h | ⏳ |
| 创建数据管理API | 6h | ⏳ |
| 测试验证 | 4h | ⏳ |

---

## 3. 数据库表创建

### 3.1 初始化SQL脚本

```sql
-- stocks表 - 股票基础信息
CREATE TABLE IF NOT EXISTS stocks (
    ts_code VARCHAR(10) PRIMARY KEY,
    symbol VARCHAR(10) NOT NULL UNIQUE,
    name VARCHAR(50) NOT NULL,
    industry VARCHAR(50),
    market VARCHAR(20),
    list_date DATE,
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    updated_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP
);

CREATE INDEX IF NOT EXISTS idx_stocks_industry ON stocks(industry);
CREATE INDEX IF NOT EXISTS idx_stocks_market ON stocks(market);

-- daily_data表 - 日线数据
CREATE TABLE IF NOT EXISTS daily_data (
    id SERIAL PRIMARY KEY,
    ts_code VARCHAR(10) NOT NULL,
    trade_date DATE NOT NULL,
    open DECIMAL(10, 2),
    high DECIMAL(10, 2),
    low DECIMAL(10, 2),
    close DECIMAL(10, 2),
    vol DECIMAL(20, 2),
    amount DECIMAL(20, 2),
    pct_chg DECIMAL(10, 2),
    UNIQUE(ts_code, trade_date)
);

CREATE INDEX IF NOT EXISTS idx_daily_ts_code ON daily_data(ts_code);
CREATE INDEX IF NOT EXISTS idx_daily_date ON daily_data(trade_date);
CREATE INDEX IF NOT EXISTS idx_daily_ts_date ON daily_data(ts_code, trade_date);

-- signals表 - 信号记录
CREATE TABLE IF NOT EXISTS signals (
    id SERIAL PRIMARY KEY,
    ts_code VARCHAR(10) NOT NULL,
    signal_date TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    signal_type VARCHAR(20) NOT NULL,
    confidence DECIMAL(5, 2),
    entry_price DECIMAL(10, 2),
    stop_loss DECIMAL(10, 2),
    take_profit DECIMAL(10, 2),
    indicators JSONB,
    reason TEXT,
    status VARCHAR(20) DEFAULT 'pending',
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    updated_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP
);

CREATE INDEX IF NOT EXISTS idx_signals_ts_code ON signals(ts_code);
CREATE INDEX IF NOT EXISTS idx_signals_date ON signals(signal_date);
CREATE INDEX IF NOT EXISTS idx_signals_type ON signals(signal_type);

-- holdings表 - 持仓记录
CREATE TABLE IF NOT EXISTS holdings (
    id SERIAL PRIMARY KEY,
    ts_code VARCHAR(10) NOT NULL,
    buy_date DATE,
    buy_price DECIMAL(10, 2),
    shares INTEGER,
    stop_loss DECIMAL(10, 2),
    take_profit DECIMAL(10, 2),
    status VARCHAR(20) DEFAULT 'holding',
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    updated_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP
);

CREATE INDEX IF NOT EXISTS idx_holdings_ts_code ON holdings(ts_code);
CREATE INDEX IF NOT EXISTS idx_holdings_status ON holdings(status);
```

---

## 4. 数据获取服务

### 4.1 Tushare服务配置

```python
# backend/app/data/tushare_provider.py
import os
import tushare as ts
import pandas as pd
from datetime import datetime

class TushareProvider:
    def __init__(self):
        self.token = os.getenv('TUSHARE_TOKEN', '')
        self.pro = ts.pro_api(self.token)
    
    def get_stock_list(self, market='all'):
        """获取股票列表"""
        try:
            data = self.pro.stock_basic(exchange='', list_status='L', fields='ts_code,symbol,name,industry,list_date,market')
            if market != 'all':
                data = data[data['market'] == market]
            return data.to_dict('records')
        except Exception as e:
            print(f"Error getting stock list: {e}")
            return []
    
    def get_daily_data(self, ts_code, start_date=None, end_date=None):
        """获取日线数据"""
        try:
            if start_date is None:
                start_date = (datetime.now() - pd.Timedelta(days=365)).strftime('%Y%m%d')
            if end_date is None:
                end_date = datetime.now().strftime('%Y%m%d')
            
            data = self.pro.daily(ts_code=ts_code, start_date=start_date, end_date=end_date)
            return data.to_dict('records')
        except Exception as e:
            print(f"Error getting daily data for {ts_code}: {e}")
            return []
    
    def get_index_daily(self, ts_code='000001.SH'):
        """获取指数日线数据"""
        try:
            data = self.pro.index_daily(ts_code=ts_code)
            return data.to_dict('records')
        except Exception as e:
            print(f"Error getting index data: {e}")
            return []
```

### 4.2 数据管理器

```python
# backend/app/data/data_manager.py
from .tushare_provider import TushareProvider
from app.models import Stock, DailyData
from app import db
from datetime import datetime

class DataManager:
    def __init__(self):
        self.tushare = TushareProvider()
    
    def sync_stock_list(self):
        """同步股票列表"""
        stocks = self.tushare.get_stock_list()
        
        for stock in stocks:
            existing = Stock.query.get(stock['ts_code'])
            if existing:
                existing.symbol = stock['symbol']
                existing.name = stock['name']
                existing.industry = stock.get('industry')
                existing.market = stock.get('market')
                existing.list_date = stock.get('list_date')
            else:
                new_stock = Stock(
                    ts_code=stock['ts_code'],
                    symbol=stock['symbol'],
                    name=stock['name'],
                    industry=stock.get('industry'),
                    market=stock.get('market'),
                    list_date=stock.get('list_date')
                )
                db.session.add(new_stock)
        
        db.session.commit()
        return len(stocks)
    
    def sync_daily_data(self, ts_code):
        """同步单只股票日线数据"""
        data = self.tushare.get_daily_data(ts_code)
        
        for item in data:
            existing = DailyData.query.filter_by(
                ts_code=item['ts_code'],
                trade_date=item['trade_date']
            ).first()
            
            if not existing:
                daily = DailyData(
                    ts_code=item['ts_code'],
                    trade_date=item['trade_date'],
                    open=item.get('open'),
                    high=item.get('high'),
                    low=item.get('low'),
                    close=item.get('close'),
                    vol=item.get('vol'),
                    amount=item.get('amount'),
                    pct_chg=item.get('pct_chg')
                )
                db.session.add(daily)
        
        db.session.commit()
        return len(data)
    
    def sync_all_daily_data(self):
        """同步所有股票日线数据"""
        stocks = Stock.query.all()
        count = 0
        
        for stock in stocks:
            count += self.sync_daily_data(stock.ts_code)
        
        return count
```

---

## 5. 缓存服务

### 5.1 DuckDB缓存配置

```python
# backend/app/data/cache_manager.py
import duckdb
import os

class CacheManager:
    def __init__(self):
        self.db_path = os.path.join(os.getenv('DATA_DIR', '/data'), 'duckdb', 'stock_cache.db')
        os.makedirs(os.path.dirname(self.db_path), exist_ok=True)
        self.conn = duckdb.connect(self.db_path)
        self._init_tables()
    
    def _init_tables(self):
        """初始化缓存表"""
        self.conn.execute("""
            CREATE TABLE IF NOT EXISTS daily_cache (
                ts_code VARCHAR,
                trade_date DATE,
                open DECIMAL,
                high DECIMAL,
                low DECIMAL,
                close DECIMAL,
                vol DECIMAL,
                amount DECIMAL,
                pct_chg DECIMAL,
                PRIMARY KEY (ts_code, trade_date)
            )
        """)
        
        self.conn.execute("""
            CREATE TABLE IF NOT EXISTS indicator_cache (
                ts_code VARCHAR,
                trade_date DATE,
                indicator_name VARCHAR,
                value DECIMAL,
                PRIMARY KEY (ts_code, trade_date, indicator_name)
            )
        """)
    
    def cache_daily_data(self, df):
        """缓存日线数据"""
        self.conn.register('temp_df', df)
        self.conn.execute("""
            INSERT OR REPLACE INTO daily_cache 
            SELECT * FROM temp_df
        """)
        self.conn.commit()
    
    def get_cached_daily(self, ts_code, start_date=None, end_date=None):
        """获取缓存的日线数据"""
        query = "SELECT * FROM daily_cache WHERE ts_code = ?"
        params = [ts_code]
        
        if start_date:
            query += " AND trade_date >= ?"
            params.append(start_date)
        if end_date:
            query += " AND trade_date <= ?"
            params.append(end_date)
        
        return self.conn.execute(query, params).df()
    
    def cache_indicator(self, ts_code, trade_date, indicator_name, value):
        """缓存指标数据"""
        self.conn.execute("""
            INSERT OR REPLACE INTO indicator_cache 
            (ts_code, trade_date, indicator_name, value)
            VALUES (?, ?, ?, ?)
        """, (ts_code, trade_date, indicator_name, value))
        self.conn.commit()
```

---

## 6. 数据管理API

### 6.1 市场数据路由

```python
# backend/app/routes/market.py
from flask import Blueprint, request, jsonify
from app.services.market_service import MarketService
from app.utils.decorators import json_response

market_bp = Blueprint('market', __name__)
market_service = MarketService()

@market_bp.route('/api/v1/stocks', methods=['GET'])
@json_response
def get_stocks():
    """获取股票列表"""
    page = request.args.get('page', 1, type=int)
    page_size = request.args.get('page_size', 20, type=int)
    industry = request.args.get('industry')
    market = request.args.get('market')
    
    result = market_service.get_stock_list(page, page_size, industry, market)
    return result

@market_bp.route('/api/v1/stocks/<ts_code>', methods=['GET'])
@json_response
def get_stock_detail(ts_code):
    """获取股票详情"""
    stock = market_service.get_stock_detail(ts_code)
    if not stock:
        return {'success': False, 'message': '股票不存在'}, 404
    return {'success': True, 'data': stock}

@market_bp.route('/api/v1/stocks/<ts_code>/daily', methods=['GET'])
@json_response
def get_daily_data(ts_code):
    """获取日线数据"""
    start_date = request.args.get('start_date')
    end_date = request.args.get('end_date')
    
    data = market_service.get_daily_data(ts_code, start_date, end_date)
    return {'success': True, 'data': data}

@market_bp.route('/api/v1/stocks/sync', methods=['POST'])
@json_response
def sync_stocks():
    """同步股票数据"""
    result = market_service.sync_stock_data()
    return result

@market_bp.route('/api/v1/market/index', methods=['GET'])
@json_response
def get_index_data():
    """获取指数数据"""
    indices = market_service.get_index_data()
    return {'success': True, 'data': indices}
```

### 6.2 市场服务

```python
# backend/app/services/market_service.py
from app.data.data_manager import DataManager
from app.data.cache_manager import CacheManager
from app.models import Stock, DailyData

class MarketService:
    def __init__(self):
        self.data_manager = DataManager()
        self.cache_manager = CacheManager()
    
    def get_stock_list(self, page, page_size, industry=None, market=None):
        """分页获取股票列表"""
        query = Stock.query
        
        if industry:
            query = query.filter(Stock.industry == industry)
        if market:
            query = query.filter(Stock.market == market)
        
        total = query.count()
        stocks = query.offset((page - 1) * page_size).limit(page_size).all()
        
        return {
            'success': True,
            'data': [s.to_dict() for s in stocks],
            'pagination': {
                'page': page,
                'page_size': page_size,
                'total': total,
                'pages': (total + page_size - 1) // page_size
            }
        }
    
    def get_stock_detail(self, ts_code):
        """获取股票详情"""
        stock = Stock.query.get(ts_code)
        return stock.to_dict() if stock else None
    
    def get_daily_data(self, ts_code, start_date=None, end_date=None):
        """获取日线数据"""
        cached = self.cache_manager.get_cached_daily(ts_code, start_date, end_date)
        
        if not cached.empty:
            return cached.to_dict('records')
        
        data = DailyData.query.filter_by(ts_code=ts_code)
        if start_date:
            data = data.filter(DailyData.trade_date >= start_date)
        if end_date:
            data = data.filter(DailyData.trade_date <= end_date)
        
        return [d.to_dict() for d in data.all()]
    
    def sync_stock_data(self):
        """同步股票数据"""
        try:
            count = self.data_manager.sync_stock_list()
            return {
                'success': True,
                'message': f'成功同步 {count} 只股票'
            }
        except Exception as e:
            return {
                'success': False,
                'message': f'同步失败: {str(e)}'
            }
    
    def get_index_data(self):
        """获取指数数据"""
        indices = [
            {'ts_code': '000001.SH', 'name': '上证指数'},
            {'ts_code': '399001.SZ', 'name': '深圳成指'},
            {'ts_code': '399006.SZ', 'name': '创业板指'}
        ]
        
        return indices
```

---

## 7. 部署与测试

### 7.1 环境变量配置

```env
# .env
FLASK_APP=run.py
FLASK_ENV=development
DATABASE_URL=postgresql://user:password@postgres:5432/stock_analysis
REDIS_URL=redis://redis:6379/0
TUSHARE_TOKEN=your_tushare_token_here
DATA_DIR=/data
```

### 7.2 Docker Compose配置

```yaml
version: '3.8'
services:
  postgres:
    image: postgres:16
    environment:
      POSTGRES_USER: user
      POSTGRES_PASSWORD: password
      POSTGRES_DB: stock_analysis
    volumes:
      - postgres_data:/var/lib/postgresql/data
      - ./backend/migrations/init.sql:/docker-entrypoint-initdb.d/init.sql
    ports:
      - "5432:5432"
  
  redis:
    image: redis:7
    volumes:
      - redis_data:/data
    ports:
      - "6379:6379"
  
  backend:
    build: ./backend
    environment:
      - FLASK_APP=run.py
      - FLASK_ENV=development
      - DATABASE_URL=postgresql://user:password@postgres:5432/stock_analysis
      - REDIS_URL=redis://redis:6379/0
      - TUSHARE_TOKEN=${TUSHARE_TOKEN}
      - DATA_DIR=/data
    volumes:
      - ./backend:/app
      - ./data:/data
    ports:
      - "5000:5000"
    depends_on:
      - postgres
      - redis

volumes:
  postgres_data:
  redis_data:
```

---

## 验收标准

| 序号 | 验收项 | 验收标准 |
|------|--------|---------|
| 1 | 数据库表 | stocks、daily_data、signals、holdings表创建成功 |
| 2 | 数据同步 | 调用/sync接口可同步股票列表 |
| 3 | 股票列表API | GET /api/v1/stocks 返回分页数据 |
| 4 | 日线数据API | GET /api/v1/stocks/{code}/daily 返回数据 |
| 5 | 缓存服务 | DuckDB缓存正常工作 |
| 6 | Docker启动 | docker-compose up 成功启动所有服务 |

---

**文档状态**：已完成
**审核状态**：待审核
**版本历史**：
- v1.0 (2026-05-19): 初版完成