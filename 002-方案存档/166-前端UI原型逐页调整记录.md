---
编号: 166
标题: 前端UI原型逐页调整记录
日期: 2026-06-12
版本: 1.0
状态: 进行中（待全部页面评估完成后定稿）
标签: [前端UI, 原型调整, 逐页评审, UI/UX]
builds_on: [165]
---

# 166 — 前端UI原型逐页调整记录

> **说明：** 本文档为 165 号方案（前端UI架构重构与全功能设计实施总方案）的配套调整记录。根据全局规则，按页面逐项评估原型，每次确认后更新本文档，待全部 17 页评估完成后定稿。

---

## 目录

1. [登录页（login）](#1-登录页-login)
2. [待评估页面清单](#2-待评估页面清单)

---

## 1. 登录页（login）

### 1.1 调整背景

对 165 号方案中登录页的设计进行合理性评估，用户提出以下疑问：
- 登录页为何存在？
- 访问令牌与 Tushare Token 的区别？
- Tushare Token 是否需要每次输入？
- 不输入 Token 是否影响使用？

经审查实际源码（`backend/app/auth.py` + `frontend/vue-project/src/views/login/index.vue`），明确了两类 Token 的真实作用，并指出原型设计中存在将 Tushare Token 误放登录页的问题。

### 1.2 调整确认内容

#### 1.2.1 访问令牌（AUTH_TOKEN）

| 属性 | 值 |
|:----|:----|
| 默认初始值 | `123456`（纯数字字符串）|
| 配置位置 | `backend/.env` 中 `AUTH_TOKEN=123456` |
| 作用 | 保护后端 API 入口，防止未授权访问 |
| 使用场景 | 部署到公网时启用；单机自用时保留默认值即可 |
| 可自定义 | ✅ 可在登录页通过「修改令牌」功能修改，持久化到 `data/auth_token.txt` |
| 免鉴权兜底 | `.env` 中不设置 `AUTH_TOKEN` 则完全免登录，直接进入系统 |

#### 1.2.2 Tushare Token

| 属性 | 值 |
|:----|:----|
| 性质 | 数据源 API 密钥（Tushare Pro 认证用）|
| 配置位置 | `backend/.env` 中 `TUSHARE_TOKEN=***` |
| 输入方式 | **后端静态配置，不需要用户在前端输入** |
| 缺失影响 | Tushare Pro 不可用，但 AKShare（已安装）作为备用源可正常工作 |
| 结论 | **从登录页移除** — 原型中之前放置 Tushare Token 输入框是错误设计 |

#### 1.2.3 修改令牌功能

新增「修改令牌」功能，交互流程如下：

```
[登录页] → 点击「修改令牌」按钮
    ↓
┌─ 弹窗 Step 1 ───────────────────────────┐
│  请输入当前令牌（验证身份）               │
│  [输入框]  placeholder: 输入当前使用的令牌 │
│  提示：若为默认令牌请输入 123456          │
└──────────────────────────────────────────┘
    ↓ 验证通过
┌─ 弹窗 Step 2 ───────────────────────────┐
│  请输入新令牌                             │
│  [输入框]  placeholder: 6位以上任意字符    │
│  提示：建议使用 8 位以上复杂字符串         │
└──────────────────────────────────────────┘
    ↓
┌─ 弹窗 Step 3 ───────────────────────────┐
│  请再次输入新令牌（二次确认）              │
│  [输入框]  placeholder: 与上一步一致      │
└──────────────────────────────────────────┘
    ↓ 两次一致 → 提交后端
┌─ 成功弹窗 ───────────────────────────────┐
│  ✅ 令牌修改成功                          │
│  请使用新令牌重新登录系统                  │
│  [我知道了]                              │
└──────────────────────────────────────────┘
    ↓ 关闭弹窗 → 登录框自动填入新令牌
```

**修改验证规则：**
- Step 1 → Step 2：旧令牌非空即通过（模拟验证，实际调用后端 `/api/auth/change-token` 校验）
- Step 2 → Step 3：新令牌 ≥ 6 位
- Step 3 → 提交：两次新令牌完全一致

**持久化方案：**
- 后端新增 `backend/data/auth_token.txt` 文件
- 容器启动时读取顺序：`.env` 的 `AUTH_TOKEN` → `data/auth_token.txt` → 默认 `123456`
- 修改令牌时写入 `data/auth_token.txt`（不修改 `.env`，避免 git 冲突）
- 重启容器后令牌保留

#### 1.2.4 最终登录页 UI 方案

| 元素 | 设计 |
|:----|:------|
| 输入字段 | **仅「访问令牌」一个输入框** |
| 默认值 | 预填 `123456` |
| 主按钮 | 「🔑 进入系统」|
| 副按钮 | 「🔑 修改令牌」（打开三步弹窗）|
| 认证状态指示器 | 顶部圆点 + 文字：鉴权开启/关闭 |
| 令牌预览区 | 显示当前令牌前 8 位（忘记时可查看）|
| 系统信息区 | 默认令牌说明 + 免鉴权说明 |
| Tushare Token | ❌ 已移除 |

### 1.3 涉及修改的文件

| 文件 | 改动内容 |
|:----|---------|
| `_ui-prototype/login.html` | 已更新：单输入框 + 修改令牌按钮 + 三步弹窗 + 成功弹窗 |
| `backend/app/auth.py` | 待实施：① 改为动态读取逻辑 ② 新增 `update_token()` 函数 ③ 新增 `/api/auth/change-token` 路由 |
| `backend/data/auth_token.txt` | 待新建：持久化令牌文件 |
| `frontend/.../views/login/index.vue` | 待实施：新增修改令牌功能 |

### 1.4 待确认事项

- [x] 初始令牌为 `123456`
- [x] 保留修改令牌功能
- [x] 保留免鉴权兜底
- [x] 登录页仅保留访问令牌一个输入框
- [x] Tushare Token 从登录页移除
- [x] 原型已更新

### 1.5 调整后原型截图说明

更新后登录页包含：
- 顶部认证状态指示（绿色圆点 + "服务端已开启令牌验证 · 默认令牌 123456"）
- 一个访问令牌密码输入框（默认预填 123456）
- 两个按钮：「🔑 进入系统」（主按钮）+ 「🔑 修改令牌」（副按钮）
- 令牌预览区（显示当前令牌前 8 位）
- 底部系统信息说明框
- 修改令牌弹窗：3 步流程（旧令牌 → 新令牌 → 确认新令牌）
- 成功弹窗：✅ 提示 → 自动填入新令牌

---

## 2. 待评估页面清单

| 序号 | 页面 | 状态 |
|:----:|:----|:----:|
| 1 | 登录页（login） | ✅ **已完成** |
| 2 | 仪表盘（dashboard） | ✅ **已完成** |
| 3 | 个股策略分析（indicator-ide） | ⏳ 待评估 |
| 4 | 选股系统（screener） | ⏳ 待评估 |
| 5 | 自选监控（watchlist） | ⏳ 待评估 |
| 6 | AI 分析（ai-analysis） | ⏳ 待评估 |
| 7 | 回测系统（backtest） | ⏳ 待评估 |
| 8 | 因子管理（factor-manager） | ⏳ 待评估 |
| 9 | 策略模板（strategy-templates） | ⏳ 待评估 |
| 10 | 筹码分析（chip-analysis） | ⏳ 待评估 |
| 11 | 数据管理（data-management） | ⏳ 待评估 |
| 12 | 复盘中心（playback） | ⏳ 待评估 |
| 13 | 预警管理（alert） | ⏳ 待评估 |
| 14 | 虚拟验证（virtual-verify） | ⏳ 待评估 |
| 15 | 系统诊断（system-health） | ⏳ 待评估 |
| 16 | 报告中心（reports-center） | ⏳ 待评估 |
| 17 | 账户管理（account） | ⏳ 待评估 |

> **注：** 每评估完成一页，对应行状态更新为 ✅，调整记录追加到本文档对应章节。

---

## 3. 仪表盘（dashboard）

### 3.1 调整内容

#### 3.1.1 概览指数卡片（顶部 4 张）

| 调整项 | 当前原型（错误） | 调整为 |
|:------|:---------------|:-------|
| 第一张 | 上证指数 | ✅ 上证指数（000001.SH）— 保留 |
| 第二张 | 深证成指 | ✅ 深证成指（399001.SZ）— 保留 |
| 第三张 | 沪深300 | ❌ → **北证50（899050.BJ）** |
| 第四张 | 两市成交额 | ❌ → **创业板指（399006.SZ）** |
| 成交额 | 占卡片位置 | 移至底部或行情信息区单独展示 |

#### 3.1.2 每个指数卡片内嵌 20 日迷你 K 线图

每张概览卡片下方增加 **120px 高的精简 K 线图**（ECharts candlestick），规格如下：

| 属性 | 值 |
|:----|:----|
| 数据周期 | 20 个交易日（OHLCV 四要素）|
| 图表高度 | 120px（含时间轴标签）|
| 宽度同步 | 与数值区同宽，自动撑满卡片父容器 |
| 时间轴 | 底部标注，每隔 5 日显示一个日期标签 |
| 涨跌色 | 红涨绿跌（同系统 Token）|
| 性能 | 4 个极小 ECharts 实例，总内存开销 < 1MB |

#### 3.1.3 后端补充

北证50（899050.BJ）需在后端 `akshare_provider.py` 的 `_get_index_name()` 和 `/api/v3/market/indexes` 路由中补充映射：

| 文件 | 改动 |
|:----|:------|
| `backend/app/data/akshare_provider.py`（第 145 行）| `_get_index_name()` 追加 `'899050.BJ': '北证50'` |
| `backend/app/routes/realtime.py`（第 204 行）| `/api/v3/market/indexes` 追加 `{'ts_code': '899050.BJ', 'name': '北证50'}` |
| `backend/app/services/benchmark_service.py`（第 17-30 行）| `BenchmarkIndex` 类追加 `BJ50 = "899050.BJ"` |

### 3.2 调整后概览卡片区域设计

```
┌──────────────────────────────────────────────────────────────────────────┐
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐│
│  │  上证指数      │  │  深证成指     │  │  北证50      │  │  创业板指     ││
│  │  3,287.45     │  │  10,834.26   │  │  1,285.60    │  │  2,180.56    ││
│  │  ↑ +0.87%    │  │  ↓ -0.32%    │  │  ↑ +1.23%    │  │  ↓ -0.34%    ││
│  │  ┌───────┐   │  │  ┌───────┐   │  │  ┌───────┐   │  │  ┌───────┐   ││
│  │  │ 🔴🟢K │   │  │  │ 🔴🟢K │   │  │  │ 🔴🟢K │   │  │  │ 🔴🟢K │   ││
│  │  │ 20日K │   │  │  │ 20日K │   │  │  │ 20日K │   │  │  │ 20日K │   ││
│  │  └───────┘   │  │  └───────┘   │  │  └───────┘   │  │  └───────┘   ││
│  └──────────────┘  └──────────────┘  └──────────────┘  └──────────────┘│
└──────────────────────────────────────────────────────────────────────────┘
```

### 3.3 AI 交易机会雷达（替换快捷入口）

#### 3.3.1 调整理由

| 论证 | 结论 |
|:----|:------|
| 侧栏已有完整导航 | ✅ 快捷入口卡片与侧栏功能重复 |
| 7 张卡片占空间大 | ✅ 横向轮播更紧凑 |
| 用户更关注 AI 推荐 vs 导航 | ✅ AI 雷达信息价值更高 |

#### 3.3.2 设计规格

横向**自动匀速滚动轮播条**，展示 AI 推荐的股票卡片，鼠标悬停暂停：

```
┌─ AI 交易机会雷达 ──────────────────────────────────────┐
│  ◄ [西藏矿业 ★92] [平安银行 ★88] [比亚迪 ★90] [宁德★85]... ► │
│     ← 自动匀速轮播 (CSS animation, hover 暂停) →          │
└─────────────────────────────────────────────────────────┘
```

| 属性 | 方案 |
|:----|:------|
| 轮播动画 | CSS `@keyframes` + `transform: translateX()` |
| 暂停/恢复 | `hover` 时 `animation-play-state: paused` |
| 点击交互 | 点击卡片跳转到 `indicator-ide.html?symbol=xxx` |
| 无限循环 | 复制列表实现无缝轮播 |
| 卡片高度 | ~72px（紧凑，仅显示关键信息）|

每张卡片字段：

| 雷达卡片字段 | 数据来源 |
|:-----------|:---------|
| 股票名称/代码 | Screener L3 输出（`symbol`, `name`）|
| AI 评分 | `combined_score`（L2+L3 融合）|
| 信号方向 | `direction`：bullish/bearish/neutral → 映射为看多/关注/看空 |
| 共振灯号 | `lamp_color` → 卡片左侧色条 |
| 价格/涨跌幅 | 实时行情（可选）|

### 3.4 策略信号汇总的数据源问题与修订

#### 3.4.1 发现问题

经排查，仪表盘"策略信号汇总"区域存在严重数据源问题：

| 检查项 | 结果 |
|:-------|:-----|
| 前端 API 调用 | `dataService.getStrategySignals()` → `/api/v3/signals/summary` |
| 后端路由 | ❌ **该路由不存在** |
| AI 信号 API | `getAIAnalysisSignals()` → `/api/v3/ai-analysis/signals` |
| 后端路由 | ❌ **该路由不存在** |
| 当前渲染来源 | 前端 mock 数据的兜底逻辑（硬编码 12/8/15/7/5）|
| 风险 | 仪表盘信号汇总长期无真实数据支撑 |

#### 3.4.2 修订方案

策略信号汇总的数据源应统一为 **Screener Pipeline 的 L3 场景A 输出**。

**场景A 输出格式（每只股票）：**
```json
{
  "ts_code": "000762.SZ",
  "name": "西藏矿业",
  "suggestion": "BUY",
  "direction": "bullish",
  "entry_window": "¥12.00-¥13.00",
  "position_pct": 25,
  "holding": "T+20",
  "score": 92,
  "strategy_detail": {
    "volume_price": {"direction": "bullish", "score": 0.85},
    "chanlun": {"direction": "bearish", "score": 0.30},
    "chip": {"direction": "bullish", "score": 0.78}
  }
}
```

**信号汇总区域由场景A输出聚合计算：**

| 当前（硬编码 mock） | 改为（从场景A聚合） |
|:-------------------|:-----------------|
| 量价看多: 12 | `L3 结果中 strategy_detail.volume_price.direction=='bullish' 数量` |
| 缠论信号: 15 | `L3 结果中 strategy_detail.chanlun.score > 0 的数量` |
| 筹码信号: 7 | `L3 结果中 strategy_detail.chip.score > 0 的数量` |
| AI 研判: 5 | `L3 结果中 direction=='bullish' 且 score > 80 的数量` |
| 今日共 47 个信号 | `L3 结果中所有信号汇总` |

**数据流修订：**
```
Screener Pipeline (L1→L2→L3)
    ↓ 场景A输出格式
┌───────────────────────────────┐
│  /api/v3/screener/results     │  ← 新建或扩展此端点
│  返回含场景A格式的排序列表     │
└───────────┬───────────────────┘
            ↓
    ┌───────┴───────┐
    ↓               ↓
AI交易雷达      策略信号汇总
(逐只卡片)      (聚合统计)
```

#### 3.4.3 后端需补充

| 改动 | 说明 |
|:----|:------|
| 扩展 `screener.py` 路由 | `/api/v3/screener/results` 增加场景A格式输出字段 |
| 或新建 `/api/v3/dashboard/summary` | 新端点聚合 L3 输出+统计，一次返回雷达+汇总所需数据 |

### 3.5 调整后仪表盘页面结构

```
┌─ 页头 ───────────────────────────────────────────
│  📊 仪表盘 · 2026-06-11 15:30 · 成交额 ¥8,742亿 ↑
│
├─ 四大指数卡片（各带 20 日迷你 K 线） ─────────────
│  [上证 3,287 ↑0.87%] [深证 10,834 ↓0.32%]
│  [📉miniK 20日]     [📈miniK 20日]
│  [北证50 1,285 ↑1.23%] [创业板 2,180 ↓0.34%]
│  [📈miniK 20日]       [📉miniK 20日]
│
├─ AI 交易机会雷达（替换快捷入口） ─────────────────
│  ◄ [西藏矿业★92🟢] [平安银行★88🟢] [比亚迪★90🟢]... ►
│           ← 自动轮播 / hover 暂停 →
│
├─ ECharts × 2 ──────────────────────────────────
│  [资金流向 近20日]    [行业板块涨跌幅]
│
├─ 涨幅排行 + 策略信号汇总（修订后数据源） ────────
│  [涨幅前十表格]       [信号汇总: 看多12/看空8/缠15/筹码7/AI5]
│                       ↓ 现在来自 Screener L3 聚合
│
└─ 底部免责声明
```

### 3.6 待实施 Action Items

| # | 任务 | 优先级 |
|:-:|------|:------:|
| 1 | 更新 `_ui-prototype/dashboard.html`：4指数+minK线+AI雷达+信号汇总 | 🔴 本批 |
| 2 | 补充 backend 北证50映射（akshare/routes/benchmark）| 🟡 后续 |
| 3 | 扩展 `/api/v3/screener/results` 输出场景A格式 | 🟡 后续 |
| 4 | 修复策略信号汇总API路由缺失 | 🟡 后续 |
