# 479号｜dim8 现状描述改造与全链路收口总方案（实例→话术 + 链路断点修复）

**状态**：v1.0 规划稿（2026-09-23，基于全量核查，**未改任何代码**）
**定位**：dim2-dim7 定稿全部完成（索引 §一 全绿）后的总收口方案——464 待办第 4 项（dim8「实例→话术」改造）的落地规划 + 前端链路断点修复 + 跨维遗留归集
**引用入口**：《464-dim8现状描述输出定稿基准索引》（各维定稿为准）；《445 引擎冻结边界》（判定逻辑不动）
**关联**：464（输出项梳理/§十二 dim8 架构）、437-A（字段级归集，2026-09-20 已核对）、436（B1-B4 已实施）、439（评分迁 JUD）、460-467（取数/数据层分阶段，465-4~8 遗留）

---

## 一、起因与核查方法

**起因**：用户要求对 dim2-dim7 现状描述定稿文档 + 系统实际代码 + 前端数据获取/输出管路做全面核查，确认未解决问题。核查完成（本号前置），产出全量问题清单与实施计划，存档待拍板。

**核查方法**：
1. 文档侧：6 份定稿（dim2~dim7）+ 定稿索引 + 464 总纲 §十二 + 437-A + 436 全部通读；
2. 代码侧：`dim8_summary_engine.py`（1104 行通读）、`status_engine.py`（委派入口）、`data_daemon.py`（SIG 组装/OUT 透传）、`stg_quality.py`（门禁）、dim7 引擎遗留逐项核实；
3. 专项代理 ×2：①dim2/3/5 引擎补产出落地状态（逐键核实，含 framework 层证据）②前端 seven_dim 输出链路（组装→存储→API→渲染 四环节逐段比对）；
4. 基于工作树 HEAD `1b8094f`（干净）；**未运行测试/探针**（daemon 持 SQLite 写锁，且本号只读核查）。

**总体结论**：dim2-dim7 **定稿全部完成**；dim4（468）/dim6（475）/dim7 前置（476-478）已闭环；436 B1-B4 + 437-A 字段级编排已实施；中文化网关 dim2/3/4 已接入。**但定稿"补产出项"绝大多数仍停留在"待开实施号"状态（dim2/3/5 共 16 项全部未落地），dim8「实例→话术」改造未开工，机会地图页存在真实链路断点。**

---

## 二、全量问题清单

### A. 引擎侧补产出项（16 项，均登记于各定稿 §六"待开实施号"，当前代码未落地）

> 补产出必须在**各引擎 status_description** 落地，dim8 才能消费（`_compose_dim_text` 只拼装不重算，白名单表只取引擎已产键）。

| 编号 | 定稿依据 | 问题 | 现状代码证据 |
|---|---|---|---|
| A1 | dim2 §六-① | 中枢区位比例未产出 | `_assess_vs_zhongshu`（dim2:285-318）只返回定性"上方/内部/下方"，无 `(price-zs_low)/(zs_high-zs_low)` 数值 |
| A2 | dim2 §六-③ | 买卖点 reason 中文+窗口截取未完成 | reason 已透传但类型仍英文 token（"zhongshu类型"未转"中枢背驰"）；全历史序列化无窗口截取（未用 `_recent_by_type` k=3）；**且 dim8 `_flatten_value`（dim8:440-449）只取 point_type+price，reason 被丢弃**——双重缺口 |
| A3 | dim2 §六-④ | divergence 透传 details+dual_confirmed 未做 | framework 已算出（力度法双确认、strength_ratio/macd_area_ratio），dim2 只透传 label/type/strength（:280-283） |
| A4 | dim2 §六-⑤ | theorem_check.details（11 定理逐条）未透传 | dim2 只读 summary.overall_score 产 chanlun_phase，status_description 无 theorem_check 键 |
| A5 | dim3 §六-① | vp_state 状态机 state_label+rule 未接线 | framework 状态机存在（VP_BREAKOUT 等 rule 字典），dim3 `vp_state` 仍是 tags.volume_price_fit 三值映射（:195）；需 data_daemon pre_feat + dim3 双侧 |
| A6 | dim3 §六-② | volume_energy 多日连续性未做 | 只有单日量比分档"量比6.7，显著放量"（:177-180），无"连续 N 日放量/缩量" |
| A7 | dim3 §六-④ | pattern_details 透传 conditions 未做 | `PatternResult.conditions` 存在（engine/patterns/__init__.py:60-77），dim3 只取前 3 形态名拼接（:184-186） |
| A8 | dim3 §六-⑤ | divergence_type/confidence/macd_confirmed 未透传 | `VolumePriceSignal` 三字段已算（volume_price_strategy.py:154-168），data_daemon `_vp_f` 未透传 |
| A9 | dim3 §六-⑦ | granville 返回值缺 price_chg/vr | `_classify_granville`（:246-334）内部已算出（:275/:282）但返回 dict 只有 rule/name/description |
| A10 | dim5 §六-① | 快线 details+indicators 未透传 | `_bociasi_quickline` 已返回 details+indicators（:128-143），status_description 只格式化 signal+confidence（:493） |
| A11 | dim5 §六-② | 慢线 details（erp 数值）未透传 | `_bociasi_slowline` 已返回 `details={erp, erp_signal}`（:236-239），status_description 丢弃（:494） |
| A12 | dim5 §六-③ | quadrant fast/slow_score+_cache 未进 status_description | fast/slow_score 仅在局部 dict 供温度修正（:389-390），details（_cache 7 指标）全丢（:386-393 未取 `full_quadrant['details']`） |
| A13 | dim5 §六-④ | temperature 五档话术未做 | `status_description['temperature']` 裸打"57.9/100"（:496）；dim8 anchors 也只拼"情绪温度{temp}"（dim8:407-409），全链路无五档映射 |

> 已闭环对照（勿重做）：dim4 §六 ①-⑤ = **468 号已实施**；dim6 §六 P1-P5/P7/P16/⑪ = **475 号已实施**（P6 撤销、⑬⑭观察项不改）。dim3 §六-③（T 表 vol_ratio 移除）、-⑥（rps 转表述）属 **dim8 层**（B 组）。

### B. dim8 归集层与定稿的偏差（464 待办第 4 项核心，均未实施）

> 引用规范：定稿与现状代码冲突以定稿为准（代码属改造对象）。

| 编号 | 定稿依据 | 问题 | 现状代码证据 |
|---|---|---|---|
| B1 | 464 §12.3 / 索引 §二 | **「实例→话术」改造未开工**：text 仍是 437-A 字段级"字段名:值"拼接，非定稿话术模板因果链叙事（因为→所以→验证）；audit.conditions 未用于话术 | `_segment_from_dim`/`_compose_dim_text`（dim8:583-643） |
| B2 | dim2 §七① / dim3 §五 / dim7 §七 | **T 表违反"归 JUD/去重"拍板**：structure 表含 `chanlun_strength`；volume_price 表含 `health_score`/`pattern_score`/`vol_ratio`；valuation 表（`_valuation_sentence`）拼入 `potential_score`/`potential_strength`/`fina_health`，**且缺"因"**（pe/pb 分位、FCF、股息、营收）——方向性错误：评分（果）进叙事、分位（因）只在 evidence | `_DIM8_T_SUBJECTS`（dim8:651-657）、`_valuation_sentence`（dim8:745-775） |
| B3 | dim6 §六 P13/P14 / dim2 定稿 | **E 表冲突**：risk 表含 `event_summary`（P13 应移出）、缺 `event_details`/`piers_leverage`（P14 应补）；structure 表含 `vs_chip`/`vs_support_resistance`/`chanlun_phase`（定稿均不产话术） | `_DIM8_E_FIELDS`（dim8:677-683） |
| B4 | dim6 §六 P9 | **audit.conditions 只透传 {name, satisfied}，actual/threshold 被裁掉**（现状本体丢失） | dim8:666-669 |
| B5 | dim6 §六 P10 | **evidence[:5] 硬截断仍在**（定稿要求放开/按需截断） | dim8:657 |
| B6 | dim6 §六 P11 + dim5/dim7 定稿 §八 | **中文化网关未覆盖 dim5/6/7**：volatility_level=low/medium、BOCIASI BUY/NEUTRAL、四象限 LL/MM 等英文残留；dim7 `potential_breakdown` JSON 无展示层解析（`_flatten_value` 对 dict 只取第一个非空子值，六维明细只剩一维） | `_to_display_text` 映射表（dim8:1-66）；`_flatten_value`（dim8:430-460） |
| B7 | dim6 §六 P15 | **risk 段未分「价格位置/风险状态」两小节** | `_DIM8_SUBSECTIONS` 只有 chip_fund（dim8:76） |
| B8 | dim2 §六-② | **trend_structure_signal 条件采用未落地**（signal≠none 才产句，现状恒读） | dim8 E 表 structure 含该键 |

### C. 前端输出链路问题

| 编号 | 类型 | 问题 | 证据 |
|---|---|---|---|
| C1 | 🔴 真实断点 | **机会地图页 sevenDimReport 恒 null**：treemap API（`get_treemap_snapshot_items`）与 `treemap_snapshot` 表均无 `one_liner_detail`/`seven_dim_report` 字段；前端 :753-754 读取 → 恒 null → :1707-1725「🧬 七维体检」分支永不渲染，静默回退旧画像；且 :1711 还要求 `_sdr.signal`（dim8 不产）——双保险不渲染 | `_ui-prototype/opportunity-treemap.html:753-754,1707-1725`；enhanced_cache_manager.py:3315-3410 |
| C2 | 🟡 未接线 | **dim8 富结构产出前端不消费**：evidence（E 字段+audit 满足项）与 subsections（fund_chip 两小节）无渲染点 | indicator-ide.html:3976-4035 只读 title/light/text/plain/judgment/audit |
| C3 | 🟡 设计缺口 | **signal 段名实不符**：前端 segOrder/dimOrder 含 `signal`，dim8 不产（2026-09-15 裁决由 JUD 单独产出），前端 `if(!s) return ''` 静默跳过 → 实际渲染 6 段；**需先确认 JUD 侧 signal 段产出现状再接线** | indicator-ide.html:3976；dim8 `SEVEN_DIM_SPEC`（:73-80）无 signal |
| C4 | 🟡 注释失真 | strategy_analyze.py:1063 注释称"按需组装 build_seven_dim_report"，实际是**从 DB 读** seven_dim_json | strategy_analyze.py:1006-1011,1063 |
| C5 | 🟡 弱匹配 | 前端读 `c.detail \|\| c.name`（indicator-ide.html:4029），各段 audit.conditions 无 `detail` 键 → 兜底 name，actual/threshold 语义丢失 | 与 B4（P9）同源 |

### D. 跨维系统级待办

| 编号 | 来源 | 问题 | 现状 |
|---|---|---|---|
| D1 | 439 同批 / dim2 §六-⑥ / dim6 P8 | **评分迁 JUD**：structure_health_score / health_score / pattern_score / crowding_score / potential_score / potential_strength + 各维 judgment 全链（灯色/方向/continuous_value） | 未实施；dim8 T 表现在还在展示评分键（与 B2 冲突的直接体现） |
| D2 | 445/dim7 定稿核查 | **dim7 引擎遗留代码**：①`dim7_valuation_engine.py` 与 `valuation_estimator.py` 双份方法体未收敛；②`_spearman`（dim7:136）死代码（无调用点）；③`ps_percentile_5y` 算了不产（dim7:949 产出但 12 键 status_description 无此键；data_daemon:3325 也读）；④`_fina_health` 对 None 不健壮；⑤audit「ROCE 判定 vs tags.roce 展示口径」未对齐 | 全部仍存在（本号核查确认） |
| D3 | 465 号 v0.4 | **465-4~465-8 待拍板**：plain 缺趋势结论句 / vs_ind 未入 plain / 多级别 vs 单级别矛盾（判定）/ 顶背驰+一买并存（判定/叙事）/ RSI 31-69 中性（判定） | 465-1/2/3 已实施（a2aeabb/f0c04c1），4~8 待用户拍板 |
| D4 | 466 号 §五 | sig_full_test 全量 dim 回归 + 前端渲染侧验证列为后续（466 已实施①②③④⑤） | 未做（并入本方案 B/C 验证环节） |

### E. 已闭环项（确认无问题，勿重复处理）

- dim4 补产出①-⑤ + 中文化网关全段扩展（**468 号**，2026-09-22，8 股验证 CLEAN）；
- dim6 补产出 P1-P5/P7/P16/⑪（**475 号**）；
- dim7 引擎侧无缺口（12+7+8 键全透传）+ SIG 分位表/行业映射/daily_basic 回补（**476/477/478 号**）；
- 436 B1-B4（dim8 归集器/daemon 接线/分级门禁 B4 升硬）+ 437-A 字段级编排（2026-09-20）；
- daemon 组装链路（data_daemon.py:6462-6496，ts_code 已传、dim_results 缺失写 NULL 语义正确）与 OUT 透传（:5250-5262）已通；
- 中文化网关 dim2/3/4 已接入（2026-09-22）。

---

## 三、实施计划（一号多子项，479-1 ~ 479-10）

> 按 461 号"一号合并多子项、子项独立实施验证记录同文档"模式；每子项独立 commit + 测试，全部落地后本方案标记完成。**依赖序**：B 组结构性修正（479-1）→ 引擎素材补全（479-2~4）→ 话术叙事收官（479-5）→ 前端修复（479-6/7 可与 479-1 并行）→ 跨维（479-8/9/10 可并行）。

### 阶段 1：dim8 结构性修正（P0，479-1）——不依赖引擎素材，可先行

**479-1｜dim8 T/E 表按定稿清理 + audit/evidence 透传 + 中文化扩展 + risk 两小节**
- 内容：
  - B2：structure T 表删 `chanlun_strength`；volume_price T 表删 `health_score`/`pattern_score`/`vol_ratio`（vol_ratio 并入 volume_energy 由 A6 供给）；`_valuation_sentence` 改为「因」叙事（valuation_level 主结论 + pe/pb 分位/FCF/股息/营收 入句 + 陷阱现状句 + potential_breakdown 六维明细），删评分/fina_health；
  - B3：risk E 表删 `event_summary`、补 `event_details`/`piers_leverage`；structure E 表删 `vs_chip`/`vs_support_resistance`/`chanlun_phase`；
  - B4（P9）：audit.conditions 透传 actual/threshold（体积控制另行：≤8 条 + 截断策略）；
  - B5（P10）：evidence 截断策略按定稿（放开硬截断，改按需/可折叠）；
  - B6（P11）：中文化网关接入 dim6（volatility_level/low-medium-high、liquidity 布尔）、dim7（`potential_breakdown` 六维 JSON 展示层解析，改 `_flatten_value` 对 dict 的分维展开）；
  - B7（P15）：risk 段分「价格位置/风险状态」两小节（`_DIM8_SUBSECTIONS` 扩展）；
  - B8：trend_structure_signal 条件采用（signal≠none 才产句）。
- 涉及文件：`dim8_summary_engine.py`、测试 `tests/test_479_1_*.py`
- 验证：单测（契约 7 键/段结构/audit actual-threshold/中文化样例断言）+ 8 股 StatusEngine.evaluate 全链路 + 门禁自检（stg_quality G3 不再报软校验）+ sig_full_test dim 回归（466 §五 遗留，一并闭环）
- 边界：只改 dim8 展示/归集层，不触各维引擎判定（445 冻结）；评分键保留在引擎 status_description（JUD 消费），仅 dim8 不产句

### 阶段 2：引擎素材补全（P1，479-2 ~ 479-4）

**479-2｜dim2 补产出（A1-A4）**
- A1：`_assess_vs_zhongshu` 增加区位比例 `(price-zs_low)/(zs_high-zs_low)`（无有效中枢→None）；
- A2：reason 中文化（divergence.type 映射"中枢背驰/盘整背驰"等）+ 窗口截取（对齐 framework `_recent_by_type` k=3）+ dim8 `_flatten_value` 补 reason 透传；
- A3：divergence 透传 details（strength_ratio/macd_area_ratio/current/prev_amplitude 等）+ dual_confirmed；
- A4：theorem_check.details 透传（11 定理逐条 passed/score/issues/description）。
- 涉及文件：`dim2_structure_engine.py`、framework `chanlun_strategy.py`（如只读不改，仅接线）、`dim8_summary_engine.py`（_flatten_value）
- 验证：单测 + 8 股 evaluate（茅台/万科实例对照定稿 §4.x）+ 全市场采样分布（如区位比例 None 率）
- 边界：A2 的 reason 中文映射为话术层；不动买卖点判定逻辑

**479-3｜dim3 补产出（A5-A9）**
- A5：data_daemon `_vp_f` 透传状态机 state_label/rule（pre_feat）+ dim3 `vp_state` 改读；
- A6：volume_energy 补"连续 N 日放量/缩量"（多日窗口，回退单日）；
- A7：pattern_details 透传 conditions（每条判定条件）；
- A8：data_daemon `_vp_f` 透传 divergence_type/confidence/macd_confirmed → dim3 status_description；
- A9：`_classify_granville` 返回补 price_chg/vr。
- 涉及文件：`dim3_vp_engine.py`、`data_daemon.py`（_vp_f 透传，RAW-2 预计算键）、测试
- 验证：单测 + 8 股 evaluate + RAW-2 定向重跑 8 只验证 pre_feat 新键（467 先例）
- 边界：状态机/背离判定逻辑不动（445），只透传已算结果

**479-4｜dim5 补产出（A10-A13）**
- A10：快线 details（vol_ratio/price_offset_pct/mom_5d_pct/amplitude_pct）+ indicators（4 布尔）进 status_description；
- A11：慢线 erp 数值 + erp_signal 进 status_description；
- A12：quadrant fast_score/slow_score + details（_cache 7 指标）进 status_description；
- A13：temperature 五档话术（冰冷/偏冷/中性/偏热/过热，阈值随定稿）+ dim8 anchors 同步。
- 涉及文件：`dim5_emotion_engine.py`、`dim8_summary_engine.py`（temperature 段）、测试
- 验证：单测 + 8 股 evaluate + 温度分档分布采样
- 边界：情绪判定逻辑/温度计算函数不动

### 阶段 3：话术叙事收官（P1，479-5）——消费全部素材

**479-5｜dim8「实例→话术」改造（B1）**
- 内容：按各维定稿话术模板重写 `_segment_from_dim` 的 text/evidence 生成：
  - structure/volume_price/fund_chip/emotion/risk/valuation 六段 + summary，采用「所以（{现状结论}）→ 因为（{被满足的条件=audit.conditions 中 satisfied=true 的项}）→ 验证（audit N/8 动态读）」因果链；
  - 跨维去重主源落地（筹码=dim4、支撑阻力 dim2↔dim6 同源、财务健康=dim6、情绪个股=dim3）；
  - 每段 judgment 保持仅 JUD 语义（不产操作建议）；audit.conditions 作为"因"主料；
  - potential_breakdown 六维明细按 dim7 定稿 4B 方案（六维全列 + 来源标注：资金→dim4、趋势→dim2/3、板块→第一层）。
- 涉及文件：`dim8_summary_engine.py`（重写 `_compose_dim_text`/`_compose_dim_evidence`）、测试
- 验证：单测（话术模板断言：含"因为/所以/验证"结构 + 因来自 audit satisfied 项）+ 8 股 evaluate 输出人工核对（对照各维定稿 §4.x 实例）+ 前端 indicator-ide 实际渲染回归（436 §5.5 一并执行）
- 边界：纯展示/话术层；audit.conditions 与判定逻辑不动

### 阶段 4：前端链路修复（P0-P1，479-6/479-7，可与 479-1 并行）

**479-6｜机会地图页 seven_dim 接线（C1）**
- 内容：
  - 后端：`get_treemap_snapshot_items`（enhanced_cache_manager.py:3315-3410）补 `one_liner_detail`（status_snapshot 已有该列，仅查询未取）；或 treemap_snapshot 表加列 + 写入；
  - 前端：treemap.html :1711 `_sdr.signal` 门禁适配（signal 键缺失时不阻断七维体检渲染，按现有段渲染）；
  - 436 §5.5 前端人工回归（机会地图 + 个股页双页核对）。
- 涉及文件：`enhanced_cache_manager.py`、`opportunity_atlas.py`（treemap 路由）、`opportunity-treemap.html`、daemon treemap 快照写入（如加列）
- 验证：起服务人工回归（436 §5.5）+ 接口字段断言
- 边界：不改变 treemap 快照主数据流；降级语义保持（缺素材仍回退旧画像）

**479-7｜个股页富结构消费 + signal 段接线 + 注释修正（C2/C3/C4/C5）**
- 内容：
  - C2：indicator-ide.html apSevenDim 补渲染 evidence（折叠展示）与 subsections（fund_chip 两小节标题）；
  - C3：先核查 JUD 侧 signal 段产出现状（探针 `_signal_probe`），确认后接线前端组合（或按裁决在 JUD 路径补 signal 段）；
  - C4：strategy_analyze.py:1063 注释改为"从 strategy_signal_detail.seven_dim_json 读取"；
  - C5：与 479-1（B4）联动，前端读 actual/threshold。
- 涉及文件：`indicator-ide.html`、`strategy_analyze.py`（仅注释）、探针
- 验证：人工回归 + 接口断言
- 边界：signal 段归属以 JUD 裁决为准，dim8 不产 signal（437-A 已拍板）

### 阶段 5：评分迁 JUD（P1，479-8，与 479-1 的 T 表清理联动）

**479-8｜评分与灯色全链迁 JUD（D1）**
- 内容：structure_health_score / health_score / pattern_score / crowding_score / potential_score / potential_strength + 各维 judgment 键（overall_light/overall_direction/continuous_value）从"dim8 展示"迁移为"仅 JUD 消费"：
  - 引擎 status_description 保留产出（数据源不变）；
  - dim8 T 表不再消费（479-1 已删）；
  - JUD（weight_engine/cross_validate/dim8 JUD summary）消费链确认无断点（439 同批预案）。
- 涉及文件：`dim8_summary_engine.py`（已由 479-1 覆盖）、JUD 侧核查探针、测试
- 验证：JUD 全量测试回归（439 相关 test_4xx）+ 8 股 JUD 路径 evaluate
- 边界：评分计算逻辑不动（445），只动"谁展示/谁消费"

### 阶段 6：dim7 引擎遗留（P2，479-9）

**479-9｜dim7 代码收敛与死代码清理（D2）**
- 内容：①双份方法体收敛（dim7_valuation_engine vs valuation_estimator，对齐 470 dim6 先例——删内嵌副本、统一引用）；②删 `_spearman` 死代码；③`ps_percentile_5y` 定夺（保留并进 status_description，或删产出）；④`_fina_health` None 输入加固；⑤audit ROCE 判定与 tags.roce 展示口径统一（对齐 449/472 语义）。
- 涉及文件：`dim7_valuation_engine.py`、`valuation_estimator.py`、`data_daemon.py`（ps_percentile_5y 消费点）、测试
- 验证：单测 + 8 股 evaluate（估值键值不变性比对：改前 vs 改后）+ 476 探针复用
- 边界：估值判定逻辑/阈值不动（445）；ps_percentile_5y 若定稿无键则删，若有则补键（需对照定稿 12 键清单）

### 阶段 7：465-4~8 拍板（P3，479-10，需用户拍板）

**479-10｜465-4~465-8 处置（D3）**
- 内容：待拍板项逐项定夺后实施（plain 缺趋势结论句[话术] / vs_ind 未入 plain[话术] / 多级别 vs 单级别矛盾[判定] / 顶背驰+一买并存[判定/叙事] / RSI 31-69 中性[判定]）；按 445 冻结边界分类：判定项独立号全链路验证、话术项改 plain 不触判定。
- 前置：用户对 465-4~8 逐项拍板（本方案不预设结论）
- 验证：按项单独测试

---

## 四、边界与约束（全部子项通用）

1. **445 冻结边界**：本方案只改**现状层产出/话术/归维**（事实层"因"），**不触碰引擎判定逻辑**（结论"果"）；涉及判定项的（479-10 判定类、D2-⑤口径）须独立开号并全链路验证；
2. **定稿为准**：定稿与现状代码冲突以定稿为准（索引 §一 引用规范）；本方案所有 B/A 项均以各维定稿 §六/§七/§4.x 为唯一依据；
3. **评分归 JUD（439）**：479-1 删 dim8 消费、479-8 迁 JUD，引擎侧保留产出（供 JUD）；
4. **daemon 操作规范**：测试/探针前停 daemon（kill 看守+daemon，`ps aux | grep` 确认无残留），测试后恢复（`is_background:true` + nohup start_daemon.sh）；禁止裸 `&`；
5. **RAW-2/回算纪律**：涉及 pre_feat 新键（479-3 A5/A8）须 RAW-2 定向重跑 8 只验证，全量回算 5550 只 ~18 分钟（476 先例）；回补/迁移前备份 cache DB；
6. **每子项独立验证记录同本文档**（461 模式）：子项实施后更新本方案状态表 + 登记定稿索引 §四（补产出项落实）。

---

## 五、实施顺序建议与依赖图

```
479-1（dim8 结构修正，P0） ──┬──→ 479-5（话术叙事，P1，消费 479-2~4 素材）
                            ├──→ 479-8（评分迁 JUD，P1，依赖 479-1 删消费）
479-2/3/4（引擎素材，P1）───┘
479-6（treemap 接线，P0）独立并行
479-7（前端增强，P1）依赖 479-1（audit actual/threshold）+ C3 探针
479-9（dim7 遗留，P2）独立
479-10（465-4~8，P3）需用户拍板
```

**建议开工顺序**：479-1（结构性修正，素材无关）→ 479-6（断点修复，独立）→ 479-2/3/4（素材）→ 479-5（叙事收官）→ 479-7/479-8 → 479-9 → 479-10（拍板后）。

---

## 六、状态登记

| 子项 | 内容 | 状态 | 验证 |
|---|---|---|---|
| 479-1 | dim8 T/E 表清理 + audit/evidence + 中文化 + risk 两小节 | ✅ **已实施**（2026-09-23） | 27 单测 + 8 股真实数据探针全绿（详见 §七） |
| 479-2 | dim2 补产出（A1-A4） | ✅ **已实施**（2026-09-23） | 18 单测 + 8 股探针全绿（详见 §七） |
| 479-3 | dim3 补产出（A5-A9） | ✅ **已实施**（2026-09-23） | 17 单测 + 8 股探针全绿（详见 §七） |
| 479-4 | dim5 补产出（A10-A13） | ✅ **已实施**（2026-09-23） | 9 单测 + 8 股探针全绿（详见 §七） |
| 479-5 | dim8 实例→话术改造（B1） | ✅ **已实施**（2026-09-24） | 9 单测 + 8 股探针全绿 + 回归 93/93（详见 §七） |
| 479-6 | 机会地图 seven_dim 接线（C1） | ✅ **已实施**（2026-09-23，后端+前端） | 5 单测 + 前端人工回归待做（436 §5.5，详见 §七） |
| 479-7 | 前端富结构消费 + signal 接线 + 注释（C2-C5） | ⏳ **推迟**（2026-09-24 用户拍板：待 JUD 调整 + 前端再调阶段） | — |
| 479-8 | 评分迁 JUD（D1） | ✅ **已实施**（2026-09-24，固化回归） | 5 单测 + 8 股探针全绿（详见 §七） |
| 479-9 | dim7 遗留收敛（D2） | ✅ **已实施**（2026-09-24） | 12 单测 + 8 股探针全绿（详见 §七） |
| 479-10 | 465-4~8 处置（D3） | ✅ **已实施**（2026-09-24，465-4~7 关闭 + 465-8 落地） | 465-8 单测 2 组 + 8 股探针全绿（详见 §七） |

> **479-7 推迟理由（2026-09-24 用户拍板）**：当前为后台 SIG 修订阶段，后续将进行 JUD 调整、再前端调整。479-7 属前端渲染消费（C2 渲染 evidence/subsections、C5 读 actual/threshold），其消费的 dim8 段结构/文本在 JUD 调整后很可能再次变化；C3（signal 段接线）依赖 JUD 裁决（方案 §4 边界：signal 段归属以 JUD 裁决为准），JUD 未定型。故 479-7 与 479-8（评分迁 JUD）同批，推迟到 JUD 盘查修正完成后随前端再调一并实施（对齐 439 号灯色迁移推迟口径）。C4 仅一行注释（strategy_analyze.py:1063），随 479-7 一并处理，无独立返工点。

## 七、实施记录（461 模式：子项独立实施验证记录同文档）

### 479-1 实施记录（2026-09-23）

**改动文件**：
- `backend/app/opportunity_atlas/dimensions/dim8_summary_engine.py`：
  - **T 表清理（B2）**：structure 删 `chanlun_strength`（dim2 定稿 §七① 归 JUD）；volume_price 删 `health_score`/`pattern_score`/`vol_ratio`（dim3 定稿：评分归 JUD、vol_ratio 去重）；valuation T 表文档化为 9 键（定稿键，供 `_valuation_sentence`）；
  - **E 表清理（B3/P13/P14）**：structure 删 `vs_chip`/`vs_support_resistance`/`chanlun_phase`/`level_cross_score`/`ts_strength`（dim2 定稿：不产话术/归 JUD）；risk 删 `event_summary`（P13）补 `event_details`/`piers_leverage`/`dist_to_prev_high_pct`/`liquidity_detail`/`signal_days`（P14）；
  - **P9**：`audit.conditions` 透传 `actual`/`threshold`（现状本体不丢失）；
  - **P10**：`evidence[:5]` → `[:12]`（茅台 13 条候选不再只显 5 条）；
  - **P11**：中文化网关新增 `_VOLATILITY_CN`（volatility_level low/medium/high → 低/中/高）+ 买卖点变体中文化（`_point_type_cn`：first_buy_p/third_buy_a 等 465-1B 变体）；dim7 `potential_breakdown` JSON 展示层解析（`_parse_potential_breakdown`，B 方案六维全列+来源标注）；
  - **P12**：`risk_factors` 呈现时剥离事件条目（T 表 text + subsections 双处，`_strip_event_factors`）；
  - **P15**：`_DIM8_SUBSECTIONS` 新增 risk「价格位置/风险状态」两小节；
  - **B8**：trend_structure_signal 条件采用（'none' 自动跳过，现状已满足、测试固化）；
  - **dim3 细项6**：rps 转表述（"64.5/100" → "RPS=64.5（近20日涨幅全市场前36%分位）"，数据缺失跳过）；
  - **dim7 定稿 D2=A**：`_valuation_sentence` 重写（主结论→因→陷阱→潜力六维→验证 N/8；**评分/fina_health 不再拼入**——仅 JUD/去重归 dim6）；`_generate_text` dim_names 移除 valuation（防双段 + potential_breakdown JSON 泄漏）；尾置去重判断移除。
- `backend/tests/test_479_1_dim8_remodel.py`（新增 27 测试）；`test_437a` 更新 2 处断言（rps 转表述/valuation 话术）；`test_436_b3_sandbox` 更新 3 处断言（signal 段 6 键契约 = 2026-09-15 裁决；缺维 5 键被 B4 段数<6 硬拦——**存量过时断言，HEAD 下即失败**）。

**验证**：
- 定向回归 181 passed（436/437a/411/418/423/462/479 全绿）；
- 8 股真实数据探针 `backend/scripts/_479_seven_dim_probe.py` 全绿：6 键契约、无评分句、rps 转表述、两小节、audit actual/threshold、波动率中文化、估值尾置（"估值条件 N/8 满足"）、环境定位句全在；300750 买卖点变体已中文（一买(295.5)）。

### 479-6 实施记录（2026-09-23）

**改动文件**：
- `backend/app/data/enhanced_cache_manager.py`：新增 `get_one_liner_detail_batch`（批量查 status_snapshot.one_liner_detail，非空过滤）；`get_treemap_snapshot_items` 给每个 item 补 `one_liner_detail`（缺 → None，前端回退旧画像，437 缺则降级）；
- `_ui-prototype/opportunity-treemap.html`：:1711 门禁 `_sdr.signal` → `Object.keys(_sdr).length > 0`（signal 段由 JUD 单独产出，原判据致七维体检永不渲染；dimOrder.filter 天然跳过缺失段）；
- `backend/tests/test_479_6_treemap_linkage.py`（新增 5 测试）。

**验证**：5 单测通过；**前端人工回归待做**（436 §5.5：机会地图 + 个股页双页核对七维体检渲染）——需本地起服务人工确认。

### 479-2 实施记录（2026-09-23）

**改动文件**：
- `backend/app/opportunity_atlas/dimensions/dim2_structure_engine.py`：
  - **A1 中枢区位比例**：`_assess_vs_zhongshu` 返回补 `ratio=(price-zs_l)/(zs_h-zs_l)`（区间内 0~1、上方>1、下方<0；无有效中枢 None）；status_description 加 `zhongshu_location_ratio`；
  - **A2 买卖点**：序列化改走 `_recent_by_type`（465-1A 对齐：每 type 取 idx 最近 K=3，防全历史序列化——万科历史多三卖）；reason 经 `_cn_reason` 中文化（'zhongshu类型'→'中枢背驰'）；dim8 `_flatten_value` 买卖点补 reason 展示（'一卖(1323.0)：上涨趋势背驰，中枢背驰'）——A2 双重缺口两半同时闭合；
  - **A3 背驰细节**：透传 `divergence_details`（`_fmt_divergence_details`：趋势背驰力度比/MACD面积比/确认 + 标准a+A+b+B+c 展开 + 盘整背驰 down_amplitude/exited_zhongshu——446-D6 后 framework 已补数值，**定稿④观察项解除**）+ `divergence_dual_confirmed`（面积法+力度法双确认）；中枢背驰仅 repr 跳过；
  - **A4 11 定理**：透传 `theorem_check_details`（`_fmt_theorem_details`：t1~t11 逐条 通过/未通过 + score + issues + description，**"数据不足，跳过/占位"标注保留**——定稿⑤ 与真实 FAIL 区分）。
- `backend/app/opportunity_atlas/dimensions/dim8_summary_engine.py`：
  - E 表 structure 加 `zhongshu_location_ratio`/`divergence_details`/`divergence_dual_confirmed`/`theorem_check_details`；`_DIM8_E_FORMAT` 加 `zhongshu_location_ratio`（'中枢区位比{v:.2f}'）；`_DIM8_FIELD_CN` 加 4 键标签；
  - **E 表 structure 字段序调整**：theorem_check_details 提前（11 定理=健康度叙事主素材，防截断挤掉）；P10 evidence 截断 12→16。
- `backend/tests/test_479_2_dim2_passthrough.py`（新增 18 测试）；`test_479_1` 截断断言 12→16。

**验证**：
- 全量定向回归 **297 passed**（dim2 全量 test_396/446/454/465 + dim8 全量 + 链路 436/437a/411/418/423/462/479）；
- 8 股探针 `backend/scripts/_479_2_dim2_probe.py` 全绿：中枢区位比（茅台 0.09 内部/000001 2.14 上方/300750 -31.9 下方）、万科买卖点窗口截取 4 条（每 type ≤3）、背驰细节+双确认透传、11 定理 11 条（含"数据不足，跳过"）、dim8 evidence 全消费。

### 479-3 实施记录（2026-09-23）

**改动文件**：
- `backend/app/engine/framework/volume_price_strategy.py`：`RelationResult.to_dict()` 补 `divergence_confidence`（+1 行只增不改，A8 数据源）；
- `backend/app/engine/patterns/engine.py`：`aggregate` details['patterns'] 补 `conditions`（+1 行，A7 数据源）；
- `backend/data_daemon.py`（volume_price 组 pre_feat）：调 `compute_volume_price_signal` 透传 **A5** `vp_state_label`/`vp_rule`（STATE_SIGNAL_MAP.rule 逗号前剥离操作建议——SIG-JUD 边界：rule 后段"加仓/减仓"为操作建议不产）+ **A8** `divergence_type`/`divergence_confidence`/`divergence_macd_confirmed`；
- `backend/app/opportunity_atlas/dimensions/dim3_vp_engine.py`：
  - **A5** status_description 透传 vp_state_label/vp_rule；
  - **A6** 量能连续性 `_calc_volume_consec`（连续 N 日量>基准×1.5=放量、<0.8=缩量，N≥2 才产；当日由 vol_ratio 表达）；
  - **A7** `_fmt_pattern_detail` 拼 conditions（"W底放量突破（两次探底、放量突破颈线）"）；
  - **A8** 透传 divergence 三字段 + div_txt 增强（"顶背离（置信0.60，MACD确认）"，无背离不占位）；
  - **A9** `_classify_granville` 返回补 price_chg/vr + status_description.granville 拼接（"量价井喷（…5日涨跌+26.2%，量能+96%）"）。
- `backend/app/opportunity_atlas/dimensions/dim8_summary_engine.py`：T 表 volume_price 加 `vp_state_label`/`vp_rule`（先因后果，定稿细项1）；E 表 volume_price 加 divergence 三字段；_DIM8_FIELD_CN 补标签。
- `backend/tests/test_479_3_dim3_passthrough.py`（新增 17 测试）。

**验证**：
- 全量定向回归 **356 passed**（dim3 全量 test_446/450/455/461/467/pattern + dim8 全量 + 链路）；
- 8 股探针 `backend/scripts/_479_3_dim3_probe.py` 全绿：状态机 current_pattern→state（价涨量增/天量天价/价跌量缩）、rule 剥离操作建议、背离三字段（300750 bottom 置信1.00）、A6 连续放量/缩量（万科连续2日放量、招行连续2日缩量）、A7 pattern conditions 全透传、A9 granville 数值、dim8 text 先因后果（"量价状态机:价涨量增(强势)；状态规则:…；量价状态:中性；…"）。

### 479-4 实施记录（2026-09-23）

**改动文件**：
- `backend/app/opportunity_atlas/dimensions/dim5_emotion_engine.py`：
  - **A10 快线透传**：status_description['bociasi_quick'] 改 `_fmt_quickline`（details：量比/价格偏离/5日动量/振幅 + indicators：4 布尔达标数，如"量比2.80、价格偏离+18.8%、5日动量+26.2%、振幅9.19%、量能放大、站上均线、动量>3%、振幅>3%达标4/4"）；
  - **A11 慢线透传**：改 `_fmt_slowline`（ERP 数值，如"个股慢线ERP=看多（0.66），ERP 3.50%"；erp None 数据不足不占位——万科 pe 为负正确降级）；
  - **A12 四象限透传**：evaluate quadrant dict 补 `details`（_cache 7 指标）；status_description 改 `_fmt_quadrant`（fast_score/slow_score + 7 指标，如"快线0.62/慢线0.67、MA20强势占比0%、换手分位100%、…"）；
  - **A13 温度五档话术**：`_temp_level_cn`（冰冷<20/偏冷20-40/中性40-60/偏热60-80/过热≥80）+ status_description['temperature'] 改"五档+数值/100"（"中性54.6/100"）。
- `backend/tests/test_479_4_dim5_passthrough.py`（新增 9 测试）；`test_471` 源码断言更新（bociasi_slow 构造移至 _fmt_slowline）。

**验证**：
- 全量定向回归 **354 passed**（dim5 全量 test_419/447/458/470/471/472 + dim2/3/8 + 链路）；
- 8 股探针 `backend/scripts/_479_4_dim5_probe.py` 全绿：快线 details+indicators（万科 4/4 达标）、慢线 ERP（茅台 3.50%/招行 13.04%/万科 erp None 降级）、四象限 fast_score+7 指标、温度五档（全部"中性 xx/100"）。

**遗留登记**：① 479-6 前端人工回归（436 §5.5）；② pre_feat 真实重算（479-3 A5/A8 新键，467 先例）；③ dim5 观察项（定稿 §七：stock 与 dim3 防重复、大市四象限全市场口径标注、temperature 7 入参明细透传——7 入参明细未透传，登记观察项）；④ 状态机/volume_price_fit 口径并存差异（479-3 观察项）。

**待拍板项（用户）**：① 479-10 的 465-4~8 逐项结论；② 479-9-③ ps_percentile_5y 去留（对照定稿 12 键）。

### 479-5 实施记录（2026-09-24）

**改动文件**：
- `backend/app/opportunity_atlas/dimensions/dim8_summary_engine.py`：
  - **B1 实例→话术收官**：`_compose_dim_text` 新增可选 `au` 参数——段级调用（`_segment_from_dim`）传入 audit 后，把纯字段平铺子句升级为**因果链话术**「所以（现状结论=字段话术）→ 因为（被满足条件=audit.conditions 中 satisfied 项 name）→ 验证（条件稽核 N/M 动态读）」；新增 `_narrative_so_so_yz` 承载嵌套逻辑。
  - **向后兼容**：`_generate_text`（summary 平铺）与 `strategy_analyze._build_p1_signal_summary` 不传 `au` → 保持纯字段话术（不回归）；`_compose_dim_text` 默认 `au=None`。
  - **不改判定**：纯展示/话术层；评分键（structure_health_score 等）已被 479-1 清理出 T 表，因果链句不产评分；未动审计判定逻辑（445 冻结）。
  - **跨维去重主源**：沿用 479-1 已落的 T/E 表主源（筹码=dim4、支撑阻力=dim6、个股情绪=dim3、fina_health=dim6）——本子项因果链在既有主源上消费，不新增去重。
- `backend/tests/test_437a_dim8_field_compilation.py`：`test_segment_fields_complete` 断言更新（段 text 由"量价状态:强健康" → "所以：量价状态:强健康"，479-5 语义升级）。
- `backend/tests/test_479_5_dim8_narrative.py`（新增 9 测试）：N1-N5（因果链/因来自 satisfied/空因省/空验证省/不传 au 兼容）+ N6（五公开段真实装配均含所以/因为/验证）+ N7（评分键不产句回归保护）。
- `backend/scripts/_479_5_dim8_narrative_probe.py`（新增）：8 股真实数据因果链核查探针。

**验证**：
- 定向回归 **93 passed**（479-1/5/2/3 + 437a + 436 + 420 + 461-mkt + 475）；`test_390_l6_advice_engine`/`test_475_dim6_passthrough` 需 DB/daemon 停止态，未纳入本次纯单测（改动前同状）。
- 8 股真实数据探针 `_479_5_dim8_narrative_probe.py` 全绿（daemon 停止态）：五公开段 text 均以「所以：」开头、含「因为：{satisfied 条件名}」+「验证：条件稽核 N/M」，`plain==text`；实例校验（茅台 structure "所以：缠论方向:下降；阶段:下降；…因为：趋势方向、价格vs中枢、背驰检测；验证：条件稽核 3/5" 等）。

**遗留登记**：dim5/dim7 段在 `_to_display_text` 中文网关的映射扩展（BUY/NEUTRAL 等 → 中文）——dim5 定稿 §八 观察项，479-7 一并处理；前端 indicator-ide 富结构消费（479-7 C2）待开工。

### 479-8 实施记录（2026-09-24）

**核查发现（本号前置，只读核实）**：评分键的 **dim8 展示剥离已由 479-1 完成**，本号主体为「固化回归 + 消费链确认」——
- `_DIM8_T_SUBJECTS`（T 表话术）已无 `structure_health_score`/`chanlun_strength`/`health_score`/`pattern_score`/`potential_score`/`potential_strength`/`crowding_score`；
- `_DIM8_E_FIELDS`（E 表佐证）无评分键；structure 已删 `chanlun_phase`/`level_cross_score`/`ts_strength`；
- `_valuation_sentence` 不拼 `potential_score/strength`/`fina_health`（保留 PE/PB 分位/FCF/股息/营收因 + 潜力六维 + 验证）；
- 段 `judgment` 仅透传 3 个 meta 键（`overall_light`/`overall_direction`/`continuous_value`）；
- **引擎侧保留产出**：dim2 `structure_health_score`、dim3 `health_score`/`pattern_score`、dim4 `crowding_score`、dim7 `potential_score`/`potential_strength` 均在 status_description；
- **JUD 消费链无断点**：`dim_adapter.convert_to_factors` 读 `structure_health_score`（:532）、`potential_score/strength`（:284/:294/:301/:873）、dim4 `phase_confidence`（:647）。
- 口径澄清：`crowding_score` 在 JUD 侧走 `phase_confidence`/`crowding_level` 消费，`crowding_score` 仅引擎自产、dim8 已不展示——符合「评分仅 JUD/不出现在 dim8」语义，无需删改。

**改动文件**：
- `backend/tests/test_479_8_score_migration.py`（新增 5 测试）：R1 五公开段 text 不含 6 评分键；R2 evidence 不含；R3 段 judgment 仅 3 meta 键；R4 `_valuation_sentence` 不含 potential_score/strength/fina_health；R5 引擎 status_description 保留评分键；R6 `convert_to_factors` 读 `structure_health_score` 归一贡献取得值（strength>0）。
- `backend/scripts/_479_8_score_migration_probe.py`（新增）：8 股真实数据核查。

**验证**：
- 单测 **5 passed**；定向回归 **89 passed**（479-8 + 479-5 + 479-1 + 437a + 436）；
- 8 股真实数据探针 `_479_8_score_migration_probe.py` **ALL_OK**（daemon 停止态）：评分键引擎侧保留（如茅台 structure_health_score=57、volume_price.health_score=6/10、potential_score=82/100）、dim8 段 text/evidence/judgment 无评分泄漏、JUD structure factor strength 取得值（0.34~0.67）。

**边界**：完全符合「只动谁展示/谁消费」——未改动任何引擎判定（445 冻结）、未改动 dim8 展示代码（479-1 已剥离，本号仅固化回归与消费链确认）。

### 479-9 实施记录（2026-09-24）

**核查发现（本号前置，只读核实）**：
- ① 双份方法体（dim7_valuation_engine vs valuation_estimator）——**两侧都是活通路**（dim7 运行时 evaluate / RAW daemon:3139/3172 预计算），470 纯删副本先例（删无引用死副本）**不适用**；删任一侧会断 fallback。且 dim7 的 composite/fina_health/level 已走 461-2/476「SSOT=tags 优先 + 本地兜底」模式。
- ② `_spearman`（dim7）是**死代码**（全文件无调用点）；`potential_engine._spearman`（:266/:371 月度 IC 重权）是**活副本**。
- ③ `ps_percentile_5y` 在 dim7 算出但全 engine 无消费、status_description 无此键（纯「算了不产」死键）；**且发现更宽关联问题**——`reliability_assessor._assess_valuation`（JUD L2，status_engine:752 活链路）读 pe/pb/ps `_5y` 数字键，但 dim7 status_description 实产的是中文串键（`pe_percentile`）无 `_5y` → **dim7 可靠性恒 0.3、PB+PS 极端加成（0.95）永不触发**。
- ④ `_fina_health` 两侧均 try/except 包 `get_cached_*`；缺口在入口 `ecm=None` 时 `ecm.get_cached_*` 抛 AttributeError 被吞 → 空表 → 全维 fail。
- ⑤ audit「ROCE达标」用 `val['roce_pass']`（`_fina_health` 实算）展示「通过/ROCE<15%」；`tags.roce` 由 data_daemon:3838-3846 兜底链产出——两处判定源不同，展示与判定可能对同一股给不同语义。

**改动文件**（`backend/app/opportunity_atlas/dimensions/dim7_valuation_engine.py`）：
- ② 删 `_spearman` 死代码 + 残留 IC 重估段注释（`potential_engine._spearman` 活副本保留）；
- ① pe/pb/ps 5年分位改 **SSOT 优先**：优先读 `tags.pe/pb/ps_percentile_5y`（RAW 预计算，与 composite/level 同构），本地 `df_basic` 实算降为纯兜底（直接 evaluate / tags 缺失时）——消除双份方法输出漂移；
- ③ status_description 补 `pe_percentile_5y`/`pb_percentile_5y`/`ps_percentile_5y` 数字键（**展示层仍用中文串键**，定稿 12 键展示语义不变）——reliability_assessor 读 `_5y` 键恢复 PE 存在性判定（0.8）与 PB+PS 极端加成（0.95）；
- ④ `_fina_health` 入口 `ecm=None` 加固（fallback DataManager().cache，仍 None 则空表兜底）；
- ⑤ audit「ROCE达标」`actual` 展示 `tags.roce` 真实数值（如 33.42%/-17.77%），**判定 `val['roce_pass']` 与阈值不动**（仅展示口径统一）。
- `backend/tests/test_479_9_dim7_convergence.py`（新增 12 测试）、`backend/scripts/_479_9_dim7_convergence_probe.py`（新增）。

**待拍板（本号前置，用户拍板）**：
- ① 收敛方式：**SSOT优先+本地兜底**（拍板，非 470 删副本）——因两侧都是活通路，删会断 fallback；
- ③ ps 去留：**补 _5y 数字键并留 ps**（拍板）——reliability 需要 ps 做 PB+PS 极端高估加成；
- ⑤ ROCE 口径：**仅 actual 数值对齐（判定不动）**（拍板）——判定层改动需另开号全链路验证（445 边界外）。

**验证**：
- 单测 **12 passed**（新增 test_479_9：②死代码删除含 potential 活副本保护、①SSOT 优先+兜底、③reliability 0.8/0.95/0.3 三态、④ecm=None 加固、⑤ROCE actual 数值/回退）；
- 定向回归 **56+36+16 passed** 全绿（test_476_dim7_benchmarks_ssot / test_418_jud_v390 / test_390_integration / test_464_dim4_phase_enum + test_479_1 / test_479_8 / test_461_dim9 + test_319 / test_317——dim7 与 reliability 消费链相关全部覆盖）；
- 8 股真实数据探针 `_479_9_dim7_convergence_probe.py` **ALL_OK**（daemon 停止态）：status_description 含 `_5y` 数字键 + 中文串展示键并存（茅台 pe=2.8/pb=5.0/ps=2.0 + "PE近5年2.8%分位"）、reliability 全部 **0.8**（原恒 0.3）、ROCE actual 数值展示（茅台 33.42% 通过 / 万科 -17.77% 不达标 / 300750 9.09%）、`_fina_health(ecm=None)` 不抛（茅台 pass / 万科 fail）。

**边界**：判定逻辑/阈值均未改动（445 冻结）——pe/pb/ps 分位 SSOT 采用 RAW 预计算值（与 composite/level 同构先例，属「现状因」口径统一）；ROCE 判定仍用 `val['roce_pass']` 不动；仅补 key（因透传）+ 死代码清理 + ecm 入参加固 + 展示对齐。

### 479-10 实施记录（2026-09-24）

**拍板结论（2026-09-24 用户逐项拍板）**：
- **465-4（plain 缺趋势结论句）——关闭**：dim2 `_structure_plain` 已删、各维 `plain` 键废除（dim8 唯一叙事口径）。dim8 structure.text 已产「缠论方向:下降；阶段:下降；趋势依据:…」趋势结论句（探针实证）。缺陷已天然消解，无需独立立项。
- **465-5（vs_ind 未入 plain）——关闭**：`_assess_vs_indicator`（RSI 强弱 + 均线形态）值已入 dim8 `_DIM8_E_FIELDS['structure']` evidence（探针实证 evidence 含「RSI 34 中性」）。已覆盖。
- **465-6（多级别 vs 单级别矛盾）——关闭**：446 重构后单级 judgment＝价格vs中枢主判据；探针 8 股 `judgment.structure` 与 `multi_level_direction_text` 全部一致（茅台由「上升」改判「下降」，原「上升 vs 周线降日线反弹」矛盾消解）。且多级别方向属 SIG 如实并列、判定归 JUD（439 边界），符合架构。
- **465-7（顶背驰+确认一买并存）——关闭**：实证万科 divergence=顶背驰(趋势背驰,down) 与一买（下跌趋势背驰）同在下跌维度、方向一致，非真冲突；audit「背驰检测 / 有确认买点」两条件已如实并列，判定归 JUD。符合「SIG 不仲裁、如实并列现状」标准。
- **465-8（RSI 31-69 全中性）——实施**：分档加中位参考档（判定逻辑/阈值项，按 445 §6.2 独立验证）。

**改动**（`backend/app/opportunity_atlas/dimensions/dim2_structure_engine.py` `_assess_vs_indicator`）：
- RSI 分档旧三档（≥70偏强 / ≤30偏弱 / else中性）→ 新五档：
  `≥70 偏强 / ≥60 偏强-中性 / ≤30 偏弱 / ≤40 偏弱-中性 / else 中性`。
- 弱侧（30-40）与强侧（60-70）信息不再吞为「中性」（实证 600519 RSI34、601318 RSI38 原"中性"偏弱侧丢失）。

**测试**：
- `tests/test_442_vs_indicator.py` 更新 test_ma_mixed_with_rsi（RSI 68→51 中性，因 68 改判偏强-中性）+ 新增 `test_4658_five_bins`（5 档值域）、`test_4658_threshold_boundaries`（70/60/40/30 阈值边界）。
- 单测 **45 passed**（test_442 + test_465_dim2_strength_design + test_454_dim2_audit）；定向回归 **87 passed**（test_464_chanlun_strength / test_446_dim2_trend / test_446_dim2_contract_keys / test_457_multi_level / test_479_1_dim8_remodel / test_479_4_dim5_passthrough）。
- 8 股真实数据探针 `_479_10_465_probe.py`（新增，daemon 停止态）：600519 RSI34→偏弱-中性、601318 RSI38→偏弱-中性（弱侧释放）；300750 RSI24偏弱、000002 RSI77偏强保持；其余 4 股中位值仍「中性」不受影响。

**边界**：465-8 为判定阈值项（445 §6.2 确证有错才动）——实证「RSI 34/38 偏弱侧被吞中性」为信息丢失错误，独立走本号全链路验证（单测+回归+8 股探针全绿）。仅动 dim2 展示分档，未触买卖点/trend/中枢/背驰判定。465-4~7 四项不改代码、仅登记关闭。

