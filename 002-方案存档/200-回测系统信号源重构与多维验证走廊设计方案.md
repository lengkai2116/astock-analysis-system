---
编号: 200
标题: 回测系统信号源重构与多维验证走廊设计方案
日期: 2026-06-23
类型: 技术方案
状态: 初稿
标签: [回测, 组合策略, 多维验证, 信号源重构, UI重构]
depends_on: [197, 198, 199]
---

# 200 — 回测系统信号源重构与多维验证走廊设计方案

## 1. 问题背景

### 1.1 当前设计中的架构错误

回测系统的信号源定义存在根本性偏差。198号方案将 indicator-ide 页面的"实操参考"板块设计为回测信号源，但这个设计在架构上是错误的：

| 概念 | 性质 | 能否作为回测信号源 |
|:----|:----|:-----------------|
| **实操参考** | 基于今日数据的AI推理结论，是时点静态快照 | ❌ 不可——无法在历史日期上评估 |
| **AI叙事情景推演** | 对未来的概率性描述（"放量突破概率40%"） | ❌ 不可——非机械条件 |
| **组合策略引擎（UPF/UIDF）** | 可在任何历史日期上独立运行的机械规则 | ✅ 可——有固定入参和输出 |

核心矛盾在于：**回测引擎接收的是静态信号列表（`signals: [{date, ts_code, signal: 1/-1/0}]`），而实操参考输出的是不可跨时间复用的AI结论。**

### 1.2 正确架构：回测应验证组合策略机制本身

回测回答的根本问题应该是：

> **"如果我选择这几种策略、按这个权重融合、用这套冲突仲裁逻辑，它们在历史上会表现得怎么样？"**

而不是：

> **"如果把今天的AI建议拿到历史上跑一遍会怎样？"**

前者验证的是**策略组合机制的有效性**，后者验证的是**一个静态规则的巧合吻合**——后者没有意义。

### 1.3 实操参考的新定位

实操参考应**与回测解耦**，仅作为面向用户的参考信息展示：

- ✅ 保留实操参考板块（已有开仓/平仓条件展示）
- ✅ 保留"仅供回测验证"标签
- ❌ **移除**"发送到回测"按钮关联——实操参考内容不送入回测引擎
- ❌ **移除**条件③中引用"情景A/B/C"的不可机械执行条件
- ✅ **新增**"仅供参考"提示，明确声明不构成操作建议

---

## 2. 回测信号源的精确定义

### 2.1 新链路

```
用户选择策略组合（配置面板）
         ↓
后端逐日期运行组合策略引擎：
  Date D：
    1.  取截至D日的数据
    2.  并行运行：缠论(lookback=120) / 量价(lookback=120) / 筹码(lookback=60) / 多因子(20) / BOCIASI
    3.  UPFEngine 四层融合（结构0.30/形态0.25/情绪0.20/因子0.25）
    4.  ConflictArbiter 四级裁定冲突
    5.  UIDFEngine 输出最终方向 + 置信度
    6.  回测引擎根据方向+置信度 → 择时执行买入/卖出
         ↓
回测完成后输出多维验证报告
```

### 2.2 与当前后端代码的对应关系

| 链路节点 | 当前后端状态 | 代码位置 |
|:--------|:-----------|:---------|
| ① 缠论/量价/筹码/多因子逐日运行 | ✅ 各策略独立可运行 | `chanlun_strategy.py`, `volume_price_strategy.py`, `chip_strategy.py`, `signal_computation_service.py` |
| ② UPF四层融合 | ✅ 已编码，路由待暴露 | `framework/unified_practical_framework.py` |
| ③ ConflictArbiter | ✅ 已编码 | `conflict_arbiter.py` |
| ④ UIDF决策 | ✅ 已编码，路由待暴露 | `framework/unified_investment_decision_framework.py` |
| ⑤ 回测引擎执行 | ✅ 已就绪 | `backtest_v2.py` |
| ⑥ 新建组合策略回测路由 | ❌ 未实现 | `routes/backtest.py` 新增端点 |

### 2.3 组合策略回测路由端点

| 方法 | 路径 | Phase | 输入 | 输出 |
|:----|:----|:-----|:----|:----|
| POST | `/api/v3/backtest/strategy-run` | P0 | ts_code, start_date, end_date, strategies[str list], weights[dict], presets[dict] | BacktestResultV2 + 各策略中间输出 |
| POST | `/api/v3/backtest/strategy-analyze` | P1 | 同上 | +走步前推/一致性/参数平原分析 |
| POST | `/api/v3/backtest/strategy-cscv` | P1 | 同上 | +CSCV/PBO评估 |

### 2.4 信号生成时序细节

组合策略的回测信号不是一次性生成的，而是在每个历史日期上**逐期执行**：

```python
for date in historical_dates:
    # 取截至该日期的数据（防止前视偏差）
    data_slice = price_data[:date]
    
    # 并发运行各策略（各策略按各自的 STRATEGY_WINDOWS 取数）
    chanlun_result = chanlun_strategy.analyze(data_slice, param)
    vp_result = volume_price_strategy.analyze(data_slice, param)
    chip_result = chip_strategy.analyze(data_slice, param)
    factor_result = signal_service.compute_factor_signal(data_slice)
    bociasi_signal = get_bociasi_signal(data_slice)
    
    # UPF融合
    context = {
        'chanlun_result': chanlun_result,
        'volume_price_result': vp_result,
        'factor_result': factor_result,
        'bociasi_signal': bociasi_signal,
    }
    upf_result = upf_engine.evaluate_all(context)
    
    # 冲突仲裁
    conflict_result = conflict_arbiter.arbitrate(upf_result)
    
    # UIDF决策
    uidf_input = AIInterpretInput(upf_result=upf_result, ...)
    decision = uidf_engine.consolidate(upf_result, ai_output=None)  # AI层可选
    
    # 生成交易信号
    signal = 1 if decision.signal == 'BULLISH' else -1 if decision.signal == 'BEARISH' else 0
    signals.append({'date': date, 'ts_code': ts_code, 'signal': signal})
```

---

## 3. 多维验证走廊设计

### 3.1 验证体系总览

```
┌──────────────────────────────────────────────────────────────┐
│                   回测结果展示（7区域）                        │
├──────────────────────────────────────────────────────────────┤
│ 区域① 绩效概览 — 5+X指标卡片                                │
│   必显5：总收益/年化/最大回撤/夏普比率/胜率                  │
│   可展开X：索提诺/卡玛/盈亏比/波动率/信息比率/偏度           │
├──────────────────────────────────────────────────────────────┤
│ 区域② 权益曲线+基准对比 — ECharts 双Y轴图                   │
│   组合策略权益曲线 + 基准指数叠加(HS300/ZZ500等)             │
│   回撤曲线（底部副图）                                       │
├──────────────────────────────────────────────────────────────┤
│ 区域③ 策略贡献度诊断 — 表格 + 雷达图                        │
│   各策略边际贡献率/信号一致率/冲突频率/错误方向占比           │
├──────────────────────────────────────────────────────────────┤
│ 区域④ 稳健性验证走廊 — 多判据并排展示                       │
│   ▢ 走步前推一致性 (≥0.85合格)                               │
│   ▢ CSCV / PBO (<20%低风险)                                 │
│   ▢ 参数平原宽度 (≥30%合格)                                  │
│   ▢ 蒙特卡洛收益分布 (95%不包含零? / 破产概率)               │
│   → 综合判定：🟢稳健 / 🟡需谨慎 / 🔴不可信                  │
├──────────────────────────────────────────────────────────────┤
│ 区域⑤ 情境分析 — 按市场状态切片                             │
│   牛/熊/震荡/高波 各状态下：收益/回撤/信号胜率               │
├──────────────────────────────────────────────────────────────┤
│ 区域⑥ 交易明细 — 可筛选/分页表格                            │
│   日期/方向/价格/盈亏/信号来源策略/冲突标记                   │
├──────────────────────────────────────────────────────────────┤
│ 区域⑦ 参数优化与应用（199号方案延续）                       │
│   三态切换：未优化→优化结果→已应用（含回退）                 │
└──────────────────────────────────────────────────────────────┘
```

### 3.2 四个稳健性判据的判定规则

#### 3.2.1 走步前推一致性（Walk-Forward Consistency）

**后端状态**：❌ 未实现

拆分时间窗口：S行窗口（如60天训练+20天验证，向前滚动）

核心指标：

```
一致性 = 1 − |训练期夏普 − 验证期夏普| / max(|训练期夏普|, |验证期夏普|)
```

| 范围 | 判定 |
|:----|:----|
| ≥ 0.95 | 🟢 优秀（几乎无过拟合） |
| 0.85 ~ 0.95 | 🟢 合格 |
| 0.75 ~ 0.85 | 🟡 需谨慎 |
| < 0.75 | 🔴 不可信 |

**最低验证标准**（知识库定义）：
- 夏普衰减 < 0.3
- 收益衰减比例 < 30%
- 回撤恶化 < 5%

#### 3.2.2 CSCV / PBO（过拟合概率）

**后端状态**：✅ `cscv_validator.py` 已实现（#56）

| PBO | 判定 |
|:----|:----|
| < 20% | 🟢 稳健，可考虑部署 |
| 20% ~ 30% | 🟢 较好 |
| 30% ~ 60% | 🟡 中等风险 |
| > 60% | 🔴 不可信 |

#### 3.2.3 参数平原分析（Parameter Plateau）

**后端状态**：✅ `optimizer.py` 已实现（#45）

| 判据 | 要求 |
|:----|:----|
| 宽度 | 参数跨度 ≥ 30%（如窗口20→40） |
| 深度 | 绩效波动 < 15% |
| 连通性 | 单连通区域，无断裂 |

满足全部三条 → 🟢 捕捉真实市场机制
满足两条 → 🟡 部分可信
满足少于两条 → 🔴 参数山峰，过拟合高风险

#### 3.2.4 蒙特卡洛分布（Monte Carlo）

**后端状态**：❌ 未实现

生成N条随机价格路径（或打乱交易顺序），观察收益分布：

- 如果95%置信区间不包含零 → 🟢 统计显著
- 如果收益分布与随机策略无显著差异 → 🔴 不可信
- 破产概率 > 5% → 🔴 风险过大

#### 3.2.5 综合判定规则

```
四项中通过 ≥ 3 项 → 🟢 结果可信
                   回测可作决策参考，且多维度间无矛盾
通过 2 项         → 🟡 需谨慎
                   回测结果仅作参考，需等待更多验证
通过 ≤ 1 项       → 🔴 不可信
                   回测结果不能作为任何决策依据
```

### 3.3 策略贡献度诊断

| 指标 | 计算方式 | 含义 |
|:----|:--------|:-----|
| 边际贡献率 | 去除该策略后的收益变化百分比 | 该策略对总收益的正/负贡献 |
| 信号一致率 | 该策略与最终决策方向一致的信号占比 | 该策略的"正确率" |
| 冲突频率 | 该策略与其他策略信号方向相反的次数占比 | 该策略的"争议程度" |
| 错误方向占比 | 该策略看多但亏钱 / 看空但少亏的占比 | 该策略的实际可靠性 |

### 3.4 情境分析设计

按市场状态（TRENDING_BULL / TRENDING_BEAR / RANGING / HIGH_VOL）切片统计：

```
┌────────────────────────────────────────────────────────────────┐
│ 市场状态     | 区间     | 收益 | 年化夏普 | 最大回撤 | 信号胜率 │
│──────────────|──────────|──────|─────────|─────────|─────────│
│ 🟢 牛市      | 2020-06~2021-02 | +28% | 1.85 | -8% | 65% │
│ 🔴 熊市      | 2021-02~2022-10 | -9% | -0.42 | -22% | 42% │
│ 🟡 震荡      | 2022-10~2024-01 | +4% | 0.35 | -11% | 51% │
│ ⚡ 高波动    | 2024-01~2024-10 | +12% | 0.72 | -15% | 55% │
└────────────────────────────────────────────────────────────────┘
```

---

## 4. 配置面板设计（3栏结构）

```
┌─ 策略组合 ───────────────────────────────┐
│ ☑ 缠论（✅已实现）  权重 [━━━━━━━○──] 35% │
│ ☑ 量价（✅已实现）  权重 [━━━━━○────] 25% │
│ ☑ 筹码（✅已实现）  权重 [━━━━○─────] 20% │
│ ☑ 多因子（✅已实现）权重 [━━━━○─────] 20% │
│ ☐ 情绪环境（⏳P1） 权重 [──────────]      │
│ ☐ 主力行为（⏳P1） 权重 [──────────]      │
│ ───────────────────────────────────        │
│ 合计：100% ✅                               │
├─ 策略参数 ────────────────────────────────┤
│ 缠论 lookback [ 120 ▼ ]                    │
│ 量价 lookback [ 120 ▼ ]                    │
│ 筹码 lookback [ 60 ▼ ]                     │
│ 多因子窗口 [ 20 ▼ ]                        │
├─ 回测模式 ────────────────────────────────┤
│ ○ 基础回测（一次性）                        │
│ ● 走步前推（训练60日+验证20日，向前滚动）    │
│ ○ CSCV/PBO评估                             │
│ ○ 参数平原分析                              │
├─ 基准选择 ────────────────────────────────┤
│ ☑ 沪深300  ☐ 中证500  ☐ 中证1000         │
│ ☐ 上证指数  ☐ 深证成指  ☐ 创业板指        │
│ ☐ 科创50                                   │
├─ 日期/资金 ───────────────────────────────┤
│ 起始 [2020-01-01]  结束 [2026-06-01]       │
│ 初始资金 ¥[1,000,000]                      │
├─ 高级选项（折叠） ────────────────────────┤
│ 佣金率 0.03% / 滑点 0.01% / 最低佣金 ¥5   │
└────────────────────────────────────────────┘
      [▶ 开始回测]  [💾 保存配置]
```

### 4.1 关键交互行为

- **策略多选**：勾选策略后自动显示权重滑块，未勾选的策略权重自动归零
- **权重联动**：拖动任一滑块，其他权重按比例缩放保持100%
- **预设权重**：提供三种预设（均衡/保守/进取），一键切换
- **模式切换**：切换回测模式时，底部的"开始回测"按钮文案变化（基础→一致性→PBO→平原）

---

## 5. 结果面板布局

### 5.1 区域①：绩效概览卡片

```
┌────────────┐ ┌────────────┐ ┌────────────┐ ┌────────────┐ ┌────────────┐
│ 总收益     │ │ 年化收益   │ │ 最大回撤   │ │ 夏普比率   │ │ 胜率       │
│ +35.2%     │ │ +5.8%      │ │ -12.3%     │ │ 0.85       │ │ 58.3%      │
│ vs基准+22% │ │ vs基准+3%  │ │ vs基准-15% │ │ 评级: 良好 │ │ 冲突率12%  │
└────────────┘ └────────────┘ └────────────┘ └────────────┘ └────────────┘
[📊 展开更多指标 → 索提诺1.12 / 卡玛0.47 / 盈亏比1.85 / 波动率18.5% / 信息比率0.62]
```

### 5.2 区域②：权益曲线 + 基准对比

- ECharts 双线图：组合策略收益曲线 + 基准指数叠加
- 底部副图：回撤曲线，标注最大回撤点位（X标记）
- 可切换基准（下拉框切换后ECharts刷新）

### 5.3 区域③：策略贡献度诊断

```
┌──────────────────────────────────────────────────────────────────────┐
│ 策略      | 权重 | 边际贡献 | 信号一致率 | 冲突频率 | 错误方向占比  │
│───────────|──────|─────────|───────────|─────────|──────────────┤
│ 🟢 缠论   | 35%  | +12.5%  | 72%       | 18%     | 22%          │
│ 🟢 量价   | 25%  | +8.2%   | 65%       | 25%     | 28%          │
│ 🟡 筹码   | 20%  | +3.1%   | 58%       | 31%     | 35%          │
│ 🟢 多因子 | 20%  | +5.4%   | 70%       | 15%     | 25%          │
│───────────|──────|─────────|───────────|─────────|──────────────┤
│ 整体      | 100% | +29.2%  | 65%       | 22%     | 27%          │
└──────────────────────────────────────────────────────────────────────┘
诊断结论：缠论和量价是主要正贡献策略；筹码信号频繁冲突（与量价方向相反），建议评估其在组合中的作用
```

### 5.4 区域④：稳健性验证走廊

```
┌────────────────────────────────────────────────────────────────────┐
│  🛡️ 稳健性验证走廊                                                │
├────────────────────────────────────────────────────────────────────┤
│ ┌─────────────┐ ┌─────────────┐ ┌─────────────┐ ┌────────────────┐│
│ │ 🟢 走步前推  │ │ 🟢 CSCV     │ │ 🟡 参数平原  │ │ 🔴 蒙特卡洛    ││
│ │ 一致性 0.92  │ │ PBO = 15%   │ │ 宽度: 25%   │ │ p=0.08 > 0.05 ││
│ │ 合格         │ │ 低风险     │ │ <30% 需谨慎 │ │ 统计不显著    ││
│ └─────────────┘ └─────────────┘ └─────────────┘ └────────────────┘│
│                                                                   │
│ 综合判定：🟡 需谨慎（4项通过3项，蒙特卡洛未通过）                  │
├────────────────────────────────────────────────────────────────────┤
│ 💡 解读：该策略组合在走步前推和CSCV验证中表现良好，参数平原边界值  │
│    接近合格，但蒙特卡洛统计不显著（收益分布与随机策略差异不大）。   │
│    建议：增加数据量（至少2倍）后重新评估。                        │
└────────────────────────────────────────────────────────────────────┘
```

### 5.5 区域⑤：情境分析

```
┌────────────────────────────────────────────────────────────────────┐
│  📈 按市场状态切片分析                                             │
├────────────────────────────────────────────────────────────────────┤
│ 市场状态     | 区间       | 收益 | 夏普 | 回撤 | 胜率 | 信号频次 │
│──────────────|────────────|──────|──────|──────|──────|─────────│
│ 🟢 TRENDING_BULL | 2020-06~2021-02 | +28% | 1.85 | -8% | 65% | 47次 │
│ 🔴 TRENDING_BEAR | 2021-02~2022-10 | -9% | -0.42 | -22% | 42% | 38次 │
│ 🟡 RANGING     | 2022-10~2024-01 | +4% | 0.35 | -11% | 51% | 52次 │
│ ⚡ HIGH_VOL    | 2024-01~2024-10 | +12% | 0.72 | -15% | 55% | 29次 │
├────────────────────────────────────────────────────────────────────┤
│ 结论：牛市和趋势市场表现优异；震荡市信号分歧大、收益平庸；          │
│ 熊市对冲能力不足。聚焦趋势型行情，震荡市建议降低仓位。            │
└────────────────────────────────────────────────────────────────────┘
```

### 5.6 区域⑥：交易明细

| 日期 | 方向 | 价格 | 数量 | 盈亏 | 累计盈亏 | 触发策略 | 仲裁标记 |
|:----|:----|:----|:----|:----|:--------|:--------|:--------|
| 2022-03-15 | ▲ 买入 | 25.60 | 3,900 | — | — | 缠论+量价共振 | ✅一致 |
| 2022-05-20 | ▼ 卖出 | 28.30 | 3,900 | +10,530 | +10,530 | 缠论顶背驰 | ✅一致 |
| 2022-08-10 | ▲ 买入 | 29.10 | 3,300 | — | — | 结构支撑位 | ⚠️量价反对 |
| ... | | | | | | | |

### 5.7 区域⑦：参数优化与应用（199号方案延续，保留不变）

保留现有的三态切换面板：未优化→优化结果→已应用

---

## 6. 实施路径

### Phase 0 — 基础链路（约3天）

| 任务 | 详细 | 涉及文件 |
|:----|:----|:---------|
| 0.1 后端：新增组合策略回测端点 | 封装逐日期UPF→ConflictArbiter→UIDF→回测引擎链路 | `routes/backtest.py` 新增 `/api/v3/backtest/strategy-run` |
| 0.2 后端：UPF/UIDF路由暴露 | 将已编码的UPFEngine/UIDFEngine暴露为可调用服务 | `framework/unified_practical_framework.py` + `framework/unified_investment_decision_framework.py` |
| 0.3 后端：ConflictArbiter接入 | 冲突仲裁结果汇入回测日志 | `conflict_arbiter.py` 接入回测链路 |
| 0.4 前端：配置面板重写 | 从单策略下拉→组合多选+权重滑块+模式选择 | `backtest.html` 配置区 |
| 0.5 前端：结果面板重排 | 7区域布局骨架（全Mock数据） | `backtest.html` 结果区 |
| 0.6 前端：实操参考解耦 | 移除③情景引用 + 移除"发送到回测"关联 | `indicator-ide.html` 实操参考板块 |

### Phase 1 — 多维验证（约3天）

| 任务 | 详细 | 涉及文件 |
|:----|:----|:---------|
| 1.1 后端：走步前推模块 | 固定窗口/滚动窗口/扩张窗口三种模式 + 一致性计算 | 新建 `walk_forward.py` |
| 1.2 后端：蒙特卡洛模块 | 随机价格路径生成 + 随机交易顺序打乱 | 新建 `monte_carlo.py` |
| 1.3 后端：策略贡献度统计 | 边际贡献/信号一致率/冲突频率/方向错误率计算 | `routes/backtest.py` 新增 |
| 1.4 后端：情境分析切片 | 按市场状态分段统计 + 状态边界识别 | 新建 `scenario_analysis.py` |
| 1.5 前端：稳健性走廊UI | 4判据卡片 + 综合判定 + 解读文本 | `backtest.html` 区域④ |
| 1.6 前端：贡献度UI | 表格 + 雷达图 + 诊断结论 | `backtest.html` 区域③ |
| 1.7 前端：情境分析UI | 按市场状态分色展示 | `backtest.html` 区域⑤ |

### Phase 2 — 优化与持久化（约2天）

| 任务 | 详细 | 涉及文件 |
|:----|:----|:---------|
| 2.1 后端：优化计算接入组合回测 | PRESET_GRIDS 接入组合回测链路 + 参数平原分析 | `optimizer.py` 扩展 |
| 2.2 后端：回测配置持久化 | 策略组合/权重/参数保存到DB | 新建 `backtest_config_service.py` |
| 2.3 前端：参数优化面板接入 | 优化结果与当前组合回测结果联动 | `backtest.html` 区域⑦ |
| 2.4 文档同步 | 166号+171号全量重写回测章节 | 两份文档 |

### 实施顺序图

```
Phase 0 (3d) ──→ Phase 1 (3d) ──→ Phase 2 (2d)
                  ↗                   ↗
        基础链路贯通            全部UI就绪
        Mock数据占位            真实数据驱动
```

---

## 7. 与现有设计的变更对照

| 现有设计（v1.0） | 新设计（v2.0） | 变更说明 |
|:----------------|:--------------|:---------|
| 信号源：实操参考规则 | 信号源：组合策略引擎逐日期运行输出 | 核心变更 |
| 配置面板：预设三选一 + 基准多选 + 日期/资金 | 配置面板：策略多选+权重 + 参数 + 模式 + 基准 + 日期/资金 + 高级选项 | 重构 |
| 结果面板：5卡片 + 2图表 + 健康度 + 解读 + 可信度 + 冲突成本 + 交易明细 + 优化面板 | 结果面板：绩效概览 + 权益曲线 + 贡献度诊断 + 稳健性走廊 + 情境分析 + 交易明细 + 优化面板 | 重构 |
| 实操参考：回测信号源 | 实操参考：仅供用户阅读参考 | 解耦 |
| 稳健性：单一可信度评估（PBO+平原+跨周期） | 稳健性：4判据并行 + 综合判定 | 扩展 |
| 策略健康度：5策略状态表 | 策略贡献度：边际贡献/信号一致率/冲突频率/错误方向 | 重新定义 |
| 回测解读：自然语言版 | 情境分析：按市场状态切片 | 替换 |

---

## 8. 回退方案

如果实施过程中发现组合策略逐日期运行的性能不可接受（预计单只股票6年数据约1500行，全链路约3-5秒/标的）：

| 方案 | 性能 | 精度 |
|:----|:----|:----|
| **完整逐期运行**（推荐） | 慢（3-5s）但准确 | ✅ 最高 |
| 按月取样（每月末运行一次） | 中（~0.5s） | ⚠️ 信号可能遗漏 | 
| 缓存策略中间结果 | 快（首次慢，后续复用） | ✅ 最高 |
| 简化策略（部分策略固定输出） | 快（~0.2s） | ❌ 无法反映参数变化影响 |

建议先用完整逐期运行，如果性能瓶颈则启用缓存策略中间结果。

---

## 9. 附录：核心数据流图

```
┌─ 用户配置 ─────────────────────────────────────────────────┐
│ 策略A[☑]权重0.30  策略B[☑]权重0.25  策略C[☑]权重0.20 ... │
│ 模式:基础回测  基准:HS300  日期:2020-2026  资金:100万     │
└────────────────────────┬───────────────────────────────────┘
                         │ POST /strategy-run
                         ▼
┌─ 后端组合策略回测引擎 ─────────────────────────────────────┐
│ for each date in [start_date, end_date]:                   │
│   ├─ 取截至该日期的DF（防前视偏差）                        │
│   ├─ 并行运行各策略（各取各自窗口）                        │
│   ├─ UPFEngine.evaluate_all() → FusedResult               │
│   ├─ ConflictArbiter.arbitrate()                          │
│   ├─ UIDFEngine.consolidate() → signal: 1/-1/0           │
│   └─ backtest_v2.execute_buy/sell(signal)                │
└────────────────────────┬───────────────────────────────────┘
                         │ BacktestResultV2
                         ▼
┌─ 多维验证引擎 ─────────────────────────────────────────────┐
│ walk_forward() → consistency: 0.92                         │
│ cscv_validator() → PBO: 15%                               │
│ optimizer.preset_grid() → plateau width: 28%              │
│ monte_carlo() → p_value: 0.08                             │
│ → 综合判定: 🟡 需谨慎                                      │
│ 策略贡献度() → 边际贡献/一致率/冲突率/错误率                │
│ 情境分析() → 按市场状态切片                                │
└────────────────────────┬───────────────────────────────────┘
                         │
                         ▼
┌─ 前端展示 ─────────────────────────────────────────────────┐
│ ①绩效概览 ②权益曲线 ③贡献度 ④稳健性走廊 ⑤情境 ⑥明细 ⑦优化  │
└────────────────────────────────────────────────────────────┘
```

---

## 10. 文件变更记录

| 文件 | 变更类型 | 说明 |
|:----|:--------|:-----|
| `002-方案存档/200-回测系统信号源重构与多维验证走廊设计方案.md` | 新建 | 本文档 |
| `_ui-prototype/backtest.html` | 重构 | 配置面板+结果面板全量重写（按§4+§5设计） |
| `_ui-prototype/indicator-ide.html` | 小幅修改 | 实操参考解耦+移除条件③情景引用+移除发送到回测关联 |
| `backend/app/routes/backtest.py` | 改造 | 新增组合策略回测端点 |
| `backend/app/engine/framework/unified_practical_framework.py` | 增强 | 批量/历史模式支持 |
| `backend/app/engine/framework/unified_investment_decision_framework.py` | 增强 | 批量/历史模式支持 |
| `backend/app/engine/conflict_arbiter.py` | 增强 | 批量/历史模式支持 |
| `backend/app/engine/walk_forward.py` | 新建 | 走步前推模块 |
| `backend/app/engine/monte_carlo.py` | 新建 | 蒙特卡洛模块 |
| `backend/app/engine/scenario_analysis.py` | 新建 | 情境分析模块 |
| `002-方案存档/166-前端UI原型逐页调整记录.md` | 更新 | §10全量重写（回测UI新设计） |
| `002-方案存档/171-功能视图API映射对照表.md` | 更新 | §2.6全量重写（新端点+新信号源） |
