# _工作上下文.md — 项目当前状态快照

> 最后更新: 2026-07-12 (会话结束，准备新会话)
> 用途: 新会话启动时读取此文件恢复上下文

---

## 一、本会话完成的工作（共 15 项）

### 分析阶段
1. **268号方案** — 系统操作效率瓶颈分析（6层12个问题）
2. **269号方案** — 全局数据体系提效评估（8项架构问题）

### Step 1：缺失索引 + 数据保留策略
3. 添加 `idx_indicator_ts_name`, `idx_factor_ts_name` 次级索引
4. `clean_indicator_cache()`, `clean_factor_cache()` + `_run_data_cleanup()` 调用链

### Step 2：核心数据体系重构
5. **indicator_cache EAV→宽表**（3 张表: indicator_ma/macd/other）— 行数 85.5M→17.1M (-80%)
6. **factor_cache 统一到 ECM** — 删除全部独立 `sqlite3.connect()`，5 个 CRUD 方法纳入 ECM
7. PrecomputeIndicatorManager 写入双路径（EAV+宽表）
8. chart.py 消除 EAV pivot 开销，DataManager 宽表优先读取
9. 迁移脚本执行成功（`scripts/migrate_indicator_to_wide.py`，314s，17.1M 行宽表）

### Phase 3：缓存 + 聚合 + 前端加速
10. **TieredMemoryCache 扩容** — 各级容量翻倍，新增 `dashboard` 级别(60s/500条)
11. **API 缓存装饰器** — `utils/api_cache.py`，应用到 4 个高频端点
12. **CDN 本地化** — ECharts(760K) + Socket.IO(49K) → `_ui-prototype/assets/`, 6 个 HTML 引用更新
13. **仪表盘聚合 API** — `GET /api/v3/dashboard/full` 聚合 7 个数据源，前端聚合加载 + 独立请求降级

### 收尾
14. **三项次要优化** — `catch_up_if_needed()` 延迟 30s、前端 `_cachedFetch(ttl)`、`get_cached_daily()` 消除 `SELECT *`
15. **文档合规核查** — AGENTS.md DuckDB→SQLite WAL、227/244 号方案过时标记、251 号方案表统计修正

---

## 二、数据体系关键指标（重构后）

| 表 | 行数 | 状态 |
|:---|:----:|:----:|
| daily_cache | 5,751,424 | ✅ |
| **indicator_ma**（新宽表） | 5,695,400 | ✅ 取代旧 EAV |
| **indicator_macd**（新宽表） | 5,695,400 | ✅ 取代旧 EAV |
| **indicator_other**（新宽表） | 5,695,400 | ✅ 取代旧 EAV |
| factor_cache | 37,146,171 | ✅ 已统一到 ECM |
| indicator_cache（旧 EAV） | 85,499,927 | 💡 待决策是否删除 |
| minute_kline_cache | 1,582,672 | ✅ |
| moneyflow_cache | 642,667 | ✅ |
| stock_cache.db 大小 | 21GB（含 16GB EAV 待释放） | 💡 VACUUM 后可缩至 13-16GB |

---

## 三、待决策

- **💡 旧 EAV 数据删除** — 85.5M 行旧 `indicator_cache` 表（约 5-8GB），读已全走宽表，建议观察 2-3 天后删除 + VACUUM
- **💡 136 报告 9 个突破方向** — 等待用户确认优先级
- **💡 前端 SPA 重构** — 建议阶段一（仪表盘+选股系统→Vue，3 天）
- **💡 PostgreSQL 缓存层** — 当前不急需，设 30GB/2 亿行预警

## 四、可做的工作（按建议优先级）

1. 🟡 SPA 阶段一（仪表盘+选股系统迁移到 Vue，3 天）
2. 🟡 186号P2剩余 #34量价窗口自适应 / #35均线面积法
3. 🟡 sector-sector 降级路径死代码清理
4. 🟡 前端 vue-project 目录修复 / stock-analysis 独立页面

## 五、阻塞项（无需处理）

- D3剩余冷门股(1429只) — mootdx不支持，有交易时自动覆盖
- PostgreSQL 缓存层迁移 — 当前不急需
- concept_cache(0行) / index_member_cache(0行) — Tushare API 不可用
