# 工作上下文

> 最近更新：2026-09-10 19:2x（✅ 424号方案实施逐项核查闭环：P0-P2 全部落地，仅 1 处遗漏
> treemap_snapshot_history 冗余列已修复；P2-3 东财主源 push2→push2delay 修复）

## 当前任务: 无进行中任务（424号方案已实施并核查闭环）

- **状态**: 424号方案 P0-P2 全部落地并逐项核查通过。daemon(53544)/API(53546) 运行中，
  health healthy，8 分库 WAL 全 <500MB，管道 20260910 15/15 done
- **下一项**: 20260911 管道批次待日终同步后由 daemon 自动推进（COL→RAW→SIG→JUD→OUT）

## ✅ 2026-09-10 完成事项

### 1. 启动验证执行（暴露并根治昨日"写分库修复"致命 bug）
- daemon 今早 09:00 已由看守自动拉起，但 20260909 全 done → 正常重启不重跑
- 运行中重置 pending 撞 daemon 长期写锁 → SIGKILL 空窗重置（用户授权）
- **第一轮重跑暴露 bug**：昨日修复用错 API `get_connection('status_snapshot')`（参数应为分库文件名，
  误传表名）→ 写入野文件 `data/duckdb/status_snapshot`(290MB)/`treemap_snapshot`(8.7MB)
- 修正 4 处为 `get_connection(get_db_for_table(...))` + 删野文件 → 第二轮重跑验证
  **分库 status/treemap = 2026-09-10（5549/5544）** ✅

### 2. 附带修复 3 处同源缺陷
- treemap 归档 `SELECT *` 列错位（历史表残留 seven_dim_report 48vs47）→ 显式列名
- OUT watchlist 段 3 处读 status_snapshot 切分库
- **R4a 读方补齐 8 处** → 全改 `_query_shard`/分库连接

### 3. 主库残留清理 + 最终验证
- 新建 `backend/scripts/migrate_status_treemap_shard.py`：主库 status 5549→0、treemap 5544→0
- OUT 重跑（10:27 done）：treemap 归档成功、读取方路由命中分库

### 4. 423号方案实施 + 运行验证闭环 + 6 项偏差补齐
- 新增 `app/data/stg_quality.py`（QUALITY_RULES + WriteGateway + QualityChecker + RecomputeScheduler）
- QA-CHECK 真实运行**自动发现并驱动根治 11 项分库读写路径缺陷**，SIG/JUD/OUT/pre_feat 追平 09-09
- 6 项偏差补齐（B1-B6）：indicator 写后校验、sync_requests 监控、WAL PASSIVE checkpoint、
  AlertNotifier 接线、写锁冲突基线、2 测试

### 5. 424号方案实施逐项核查 + 2 处修复（本会话）
- **逐项核查 P0-P2 全部落地**（代码+数据双维度）：
  - P0-1 margin 日期归一化 ✅ / P0-2 WAL 全分库治理（8 库全 <500MB）✅
  - P0-3 Step A 读路径根治（E1 读方法全改 `_query_shard`/E2 pre_feat 统一分库/E4 pattern_score 走 compute_conn）
    + Step B 残留清理 ✅
  - P0-4 决策①②④（保留 as_sector_ranking+as_market_snapshot、chip 消费 chip_fund_ext、
    6 个 `_batch_*` 收敛 TushareProvider）✅
  - P1-1 QA 规则扩展 14 表 ✅ / P1-2 win_rate 写入 ✅ / P1-3 as_market_snapshot ✅
  - P2-1 snapshot 索引 ✅ / P2-2 死表清理 ✅
  - **P2-3 东财主源修复**：push2.eastmoney.com 不可达（5 次重试全 RemoteDisconnected）→
    `_fetch_eastmoney` host 改 push2delay，实测返回 2 条（1285.13/11.85）
- **发现并修复 1 处遗漏**：`treemap_snapshot_history` 实际 48 列（多废弃 `seven_dim_report`）vs
  代码 CREATE 47 列 → 重建为 47 列规范结构（22240 行保留，seven_dim_report 移除）
- §9 验收标准 10 项全部验证通过

## 数据库状态（2026-09-10 19:2x）

| 库/表 | 状态 |
|--------|------|
| snapshot_cache.db status_snapshot | 2026-09-10 · 5549 ✅ |
| snapshot_cache.db treemap_snapshot | 2026-09-10 · 5544 ✅ |
| snapshot_cache.db treemap_snapshot_history | **47 列规范结构 · 22240 行**（seven_dim_report 已移除）✅ |
| stock_cache.db status/treemap | 0 行（备份可回滚）✅ |
| stock_cache.db pipeline_status 20260910 | **15/15 done** ✅ |
| 8 分库 WAL | 全 <500MB（compute 0/financial 0/history 11.7/market 4.3/market_snapshot 1.1/snapshot 0/stock 1.0/system 0.1）✅ |

**daemon**：PID 53544；**API**：PID 53546（health healthy，uptime 58 分钟）

## 🔑 关键认知

- **`sharding_manager.get_connection()` 参数是分库文件名，表→库映射须经 `get_db_for_table()` 转换**
  （两段式）——昨日 4 处误传表名生成野文件，验证需查连接目标而非仅 grep
- 读写迁移必须成对实施：写方切分库后所有读方（含回退分支）须同步切分库
- 正常重启 daemon **不重置** pipeline_status（全 done 不触发重跑）——需 SIGKILL 空窗重置 pending
- `_query_df` 只读主库（read_conn），`_query_shard` 才按表路由分库——读分库表必须用后者
- **东财主源 host 已改 push2delay**（push2.eastmoney.com 不可达，同 ulist.np 端点）
- **treemap_snapshot_history 规范结构 = 47 列**（无 seven_dim_report，370号S6 已废弃）

## 遗留待办（可选，待议）

1. **测试 2 failed（数据依赖）**：600519 等 `strategy_signal_detail.signals` 为空 →
   `_restore_signals_from_cache` 返回 `[]`（test_322 / test_320::p2_signal_summary）
2. `_verify_out_completeness` active_count 读主库 daily_cache（分库表）→ OUT-CHECK 跳过（低影响）
3. `mark_step_running` 补 commit（建议）
4. dim2/dim3 `_load_precomputed_macd` 直调 DataManager 残留（可选）
5. 主库 stock_cache.db 未 VACUUM（清理后空间未释放，可选）
