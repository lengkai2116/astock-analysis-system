## [已完成] 仪表盘后端全链路修复
- **涉及文件**: 
  - `backend/app/data/enhanced_cache_manager.py` — DDL + INSERT + 日期格式修复
  - `backend/app/services/dashboard_service.py` — 移除 AKShare 直调 + 3个缺失Tushare回退路径
- **Fix 1 - moneyflow_cache 表 DDL 修复** (enhanced_cache_manager.py):
  - 新增6列 + ALTER TABLE 迁移代码（幂等安全）
  - 统一日期格式 YYYYMMDD → YYYY-MM-DD
- **Fix 2 - get_moneyflow_summary() 合规回退** (dashboard_service.py):
  - 移除 AKShare直调 → DuckDB缓存 → Tushare回退
  - 修复: `DataManager` 未导入(NameError)
- **Fix 3 - get_sector_moneyflow() 合规回退** (dashboard_service.py):
  - 移除 AKShare直调 → 行业映射+DuckDB → Tushare回退
  - 修复: `DataManager` 未导入(NameError)
- **Fix 4 - get_daily_top() 新增Tushare回退** (dashboard_service.py):
  - DuckDB空时→Tushare get_daily_by_date() 取最新交易日涨跌幅榜
- **Fix 5 - get_sector_changes() 新增Tushare回退** (dashboard_service.py):
  - DuckDB空时→Tushare get_daily_by_date() + 行业聚合
- **Fix 6 - get_dashboard_summary() 新增Tushare回退** (dashboard_service.py):
  - DuckDB空时→Tushare get_daily_by_date() 取Top8涨幅+信号统计
- **新增**: `_try_get_latest_trade_date()` 共用辅助方法
- **验证**: 后端启动后请求6个仪表盘API端点均应返回数据

## [已完成] 全局数据体系全量修复（P0/P1 问题修复）
- **背景**: 全局数据体系核对报告发现 6 项问题（P0×3, P1×3, P2×1），一次性全量修复
- **涉及文件**: 
  - `backend/app/data/enhanced_cache_manager.py` — +494 行
  - `backend/app/data/akshare_collector.py` — +10 行
  - `backend/app/data/realtime_pg.py` — +40 行
  - `backend/app/scheduler_manager.py` — +44 行
- **P0-1: DuckDB as_* 9张表 DDL + 20 个读写方法** (enhanced_cache_manager.py):
  - 9 张 as_* 表：as_market_snapshot, as_top_stocks, as_sector_ranking, as_concept_ranking, as_limit_pool, as_minute_kline, as_lhb_detail, as_news, as_quote_cache
  - 8 个写入方法：write_as_market_snapshot, write_as_top_stocks, write_as_sector_ranking, write_as_concept_ranking, write_as_limit_pool, append_as_minute_kline, write_as_lhb_detail, write_as_news
  - 12 个读取方法：read_as_market_snapshot, read_as_market_snapshot_by_codes, read_as_top_stocks, read_as_sector_ranking, read_as_concept_ranking, read_as_limit_pool, read_as_minute_kline, read_as_lhb_detail, read_as_news, read_as_quote_cache, clean_as_minute_kline, get_as_table_stats
- **P0-2/P0-3: collector/reader 调用路径激活** — 方法现在实际可用，不再 AttributeError 静默崩溃
- **P1-4: 盘后清理链** — `scheduler_manager.py:run_daily_sync()` 增加 5 步清理：DuckDB as_minute_kline → InMemoryStateStore → TieredMemoryCache → 采集器关注列表 → PG realtime_* 表
- **P1-4b: AkshareCollector.clear_watchlist() 新增** (akshare_collector.py)
- **P1-4b: realtime_pg.cleanup_old_data() 新增** (realtime_pg.py)
- **验证**: 6 个文件全部 Python 语法检查通过
