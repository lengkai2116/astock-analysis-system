# _工作上下文.md — 项目当前状态快照

> 最后更新: 2026-07-14 (第3轮: 缠论策略验证已完成)
> 用途: 新会话启动时读取此文件恢复上下文

---

## 一、本会话完成的工作（2026-07-14）

### 第3轮 — 缠论策略综合验证 + 270号方案差异项补齐（本轮）

**第3.1轮 — 缠论策略验证（已完成）**
- 发现并修复3个bug（上涨背驰NameError、__post_init__重复、下跌背驰area硬编码）
- 11/11 验证测试通过

**第3.2轮 — 270号方案9项差异补齐（已完成）**

| 项目 | 原状态 | 实施内容 | 涉及文件 |
|:-----|:------:|:---------|:---------|
| **BOCIASI快线BEARISH** | ❌ | 新增BEARISH信号状态（≤1项通过+价格弱势+负动量） | `bociasi_quickline.py` |
| **筛选L3快线评分** | ✅已实现 | 已存在，未修改 | `screener_strategy_integration.py:1378-1386` |
| **情绪周期定位** | ⚠️ | FMZ→四阶段映射也注入快线state_label | `signal_computation_service.py` |
| **缠论买卖点提取** | ⚠️ | momentum.level从背驰方向改为买卖点中文名 | `signal_computation_service.py` |
| **量价因果链描述** | ⚠️ | 增强NLG渲染器因果关系（放量上攻→上涨动能充足 等6类+平量类） | `nlg/volume_renderer.py`, `nlg/trend_renderer.py` |
| **级别间背驰对比** | ❌ | level_cross_info增强（含验证reasons），status_recognition新增multi_level | `signal_computation_service.py` |
| **双路径缓存** | ❌ | 筛选路径优先读取AnalysisCache再计算 | `screener_strategy_integration.py` |
| **信号/状态主次关系** | ❌ | 5维direction优先从status_recognition推导 | `strategy_analyze.py` |

### 第1轮 — 个股策略分析页面五维现状修复（P0-P3）

**P0（数据正确性）**:
- P0.1: 卡1字段映射修复 — position(中枢上方/内部/下方)、ma_alignment(多头排列/空头排列)、buy_point(买卖点列表)、direction(中文趋势) 全部从 `chanlun_analysis_detail` 提取 ✅
- P0.2: 卡3筹码假值修复 — avg_cost(主力成本价/筹码主峰)、concentration(集中度百分比)、large_order_net(从evidence解析大单净额)、profit_ratio(None时显示--) ✅
- P0.3: 卡6 Mock替换 — 静态4条conflict-pair删除，替换为 `renderDimensionRelations()` + `renderUncertainties()` 动态渲染，接入E13数据 ✅

**P1（空状态填充）**:
- P1.1: dim-footer全维度填充（后端注入footer_text，前端读取渲染）✅
- P1.2: verification chain conflict_detail 根据 signal 方向冲突动态生成 ✅
- P1.3: NLG 导入路径从静默降级改为 logger.warning ✅
- P1.4: 情绪维度新增 `render_emotion()` NLG 渲染 ✅

**P2/P3（策略输出增强）**:
- 量价形态命名通过 `current_pattern` 字段传递 ✅
- BOCIASI快线 state NEUTRAL→BEARISH 错误映射修复 ✅
- 因子来源扩展（BOCIASI/BIAS/REVERSAL 3个因子）✅
- BOCIASI快线加入筛选L3评分 ✅

### 第2轮 — 缠论策略架构重构（271号方案4批次22项）

**批次1 — 核心数据结构修正（P0）**:
- 中枢延伸: `_evolve_zhongshu` 改为 extend/expand/detach 三态，不再 min/max 扩展边界
- 中枢扩张: `_expand_zhongshu` + `sub_zhongshu_list` 保留子中枢链
- 中枢合并: `_merge_zhongshu` 合并时保留各自为子中枢
- Zhongshu 数据类: 新增 level/sub_zhongshu_list/duration 字段
- 笔振幅过滤: min_amplitude_pct 默认值 3.0→0.0
- 三路分支: find()循环完整处理 extend/expand/detach

**批次2 — 算法能力扩展（P1-P2）**:
- `chanlun_config.py` 含6个配置数据类（Bi/Segment/Zhongshu/Divergence/BuySell/MultiLevel）
- `chanlun_multi_level.py` 周线/日线/60分钟三级联立+区间套验证
- `FractalDetector` 新增 `fx_check` 参数
- `DivergenceDetector` 新增6种背驰算法（area/peak/slope/amp/diff/volume）
- `BuySellPointDetector` 新增 `bs_type`/`bsp2_follow_1`/`bsp3_follow_1` 可配置
- `ZhongshuAnalyzer` 新增 `algo`(normal/over_seg/auto) + `combine_mode`(zs/peak) 可配置
- `ChanlunAnalyzer` 支持通过 `ChanlunConfig` 对象构造

**批次3 — 输出优化（P2）**:
- `render_chanlun_trend()` 三段式叙事（趋势→信号→价位）
- `_build_chanlun_dimension` 输出添加 multi_level/zhongshu_list
- 支撑/阻力标注距当前价距离
- latest_close 传输链路贯通

**批次4 — 性能模式（P3）**:
- `trigger_step` + `process_bars()` + `process_bar_single()` 逐Bar增量
- `only_judge_last` 快速选股模式（只算最后K线）
- `TieredMemoryCache` 跨级别缓存（TTL=3600s）

### 并行修复项（跨两轮）
- 知识库 KnowledgeReader: FTS5 AND→OR查询修复 + `min_tier` 过滤 + `_classify_tier` 自动分级
- interpret_status prompt: 注入 dimension_relations 策略间关系
- aggregate_v2: 新增 `_build_dimension_relations` 输出
- 量价因果链: `_build_evidence()` 新增因果合成

---

## 二、验证状态

- `pytest tests/` — **63 passed, 2 skipped, 9 xfailed**
- `python -m py_compile` — 全部修改文件编译通过
- JS 语法检查 — `indicator-ide.html` 0 错误

---

## 三、后续工作

### 即将开始: 缠论修改后验证（271号方案验证）
> 已在上一轮分析中确认需要验证的项：

1. **688114.SH（华大智造）E12 端点验证** — 中枢不应再输出 34~106
2. **E12 返回多中枢列表** — 应出现 2-3 个有意义的日线中枢
3. **NLG 三段式叙事** — 验证趋势→信号→价位的连贯输出
4. **距离标注** — 支撑/阻力应显示"距当前+XX%"
5. **多级别联立** — direction_text 应包含级别信息
6. **KnowledgeReader `min_tier`** — 知识库查询只返回 T1+T2

### 已知待办（优先级排序）
| 优先级 | 事项 | 说明 |
|:------:|:-----|:------|
| P1 | 271号方案验证 | 上方的 6 项验证 |
| P2 | 多级别联立集成到 `_compute_chanlun_signal` | 当前 standalone，需接入主流程 |
| P3 | `only_judge_last` 批量选股脚本 | 利用快速模式做每日扫描 |
| P3 | `trigger_step` 实盘接入 | 监听新K线推送触发增量计算 |

---

## 四、关键文件索引

| 文件 | 说明 |
|:-----|:------|
| `002-方案存档/270-*.md` | 个股策略分析流水线优化方案（已实施） |
| `002-方案存档/271-*.md` | 缠论策略架构重构方案 v3.0（已全部实施） |
| `backend/app/engine/framework/chanlun_strategy.py` | 缠论策略主文件（ZhongshuAnalyzer/DivergenceDetector/BuySellPointDetector） |
| `backend/app/engine/framework/chanlun_config.py` | 缠论统一配置（6数据类） |
| `backend/app/engine/framework/chanlun_multi_level.py` | 多级别联立分析器 |
| `backend/app/services/nlg/trend_renderer.py` | NLG 三段式趋势描述 |
| `backend/app/services/knowledge_reader.py` | FTS5 知识库读取器 |
| `backend/app/routes/strategy_analyze.py` | E12/E13 路由 + 五维维度组装 |
| `backend/tests/test_chanlun_verification.py` | 缠论策略综合验证测试（15个测试用例） |

---

### 验证范围与发现

**发现的3个bug（已修复）:**

1. **[BUG-01 — 严重] `_detect_trend_divergence` 变量引用错误** (`chanlun_strategy.py:1123-1124`)
   - 上涨趋势背驰分支引用 `area_ratio`/`macd_confirmed`，实际应使用 `metric_ratio`/`metric_confirmed`
   - 触发条件时抛出 `UnboundLocalError` → 导致分析崩溃
   - 修复: 变量名修正为 metric_ratio/metric_confirmed

2. **[BUG-02 — 中等] `Zhongshu.__post_init__` 重复定义** (`chanlun_strategy.py:125-152`)
   - 两个 `__post_init__` 方法，后者覆盖前者
   - `duration` 字段始终为 None
   - 修复: 合并为一个方法

3. **[BUG-03 — 低] 下跌背驰固定使用 MACD area 算法** (`chanlun_strategy.py:1138-1139`)
   - 下跌趋势背驰分支直接调 `_calc_stroke_macd_area`，未使用 `_calc_stroke_metric`
   - `macd_algo='slope'/'volume'/'peak'` 等配置在下跌分支中失效
   - 修复: 改为 `_calc_stroke_metric`，遵循 `macd_algo` 配置

**其他修复:**
- `ChanlunAnalyzer.__init__` 支持 `ChanlunConfig` 对象作为 config 参数（之前仅支持 dict）
- `chanlun_multi_level.py` 中 `TieredMemoryCache` 构造参数修正

**验证结果:** 11/11 测试通过 + 全量85个测试 74/74/9xfail 通过, 0 回归
