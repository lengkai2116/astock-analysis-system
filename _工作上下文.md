# _工作上下文.md — 项目当前状态快照

> 最后更新: 2026-07-17 (272/273/273a号方案全量实施完成，已合并到main并推送GitHub)
> 用途: 新会话启动时读取此文件恢复上下文

---

## 一、本次会话完成的工作（2026-07-16）

### 272号方案全量实施（4个阶段24项任务全部完成 ✅）

**Phase 0 — 基础建设（3项）**
| 任务 | 交付物 | 文件 |
|------|--------|------|
| P0-1 G5概念文件 | 股票现状描述规范（207行） | `data/knowledge/concepts/股票现状描述规范.md` |
| P0-2 G2 fallback函数 | fallback_description（74行，6组测试通过） | `backend/app/services/fallback_description.py` + 集成到 `strategy_analyze.py` |
| P0-3 Schema文档 | 结构化快照Schema（148行，9区块） | `docs/schemas/structured_snapshot.md` |

**Phase 1 — 数据完善（8项）**
| 任务 | 新增字段 | 文件 |
|------|---------|------|
| P1-1 个股相对强弱 | `stock_vs_index_20d` | `signal_computation_service.py` |
| P1-2 买卖点过滤+中枢降级 | `active_signal/near_levels_filtered` + 3辅助函数 | `signal_computation_service.py` |
| P1-3 C1夹层区间 | `margin_cost_price/sandwich_zone` | `signal_computation_service.py` |
| P1-4 C2散户反向指标 | `retail_vs_institutional`（4态） | `signal_computation_service.py` |
| P1-5 C3情绪拥挤度 | `sentiment_crowding/label` | `signal_computation_service.py` |
| P1-6 乖离率+历史分位 | `bias_ma5/20/60 + percentile_250d` | `signal_computation_service.py` |
| P1-7 BOLL+粘合+级别上限 | `boll_bandwidth/ma_convergence/level_upper_limit` | `signal_computation_service.py` |
| P1-8 资金博弈暴露 | `net_elg_amount/net_sm_amount` → chip signal | `signal_computation_service.py` + `strategy_analyze.py` |

**Phase 2 — 管道集成（9项）**
| 任务 | 交付物 | 文件 |
|------|--------|------|
| P2-1 SnapshotAssembler | 10个提取函数 + 组装类（200行） | `backend/app/services/snapshot_assembler.py` |
| P2-2 System Prompt | 三段式prompt（角色+数据+九层规范+few-shot） | `config/prompts/stock_status_description.txt` |
| P2-3 ai_context_builder | build_context调用SnapshotAssembler；to_prompt_section输出JSON | `ai_context_builder.py` |
| P2-4 量价形态分类 | `_classify_vp_basic()` 八种形态+确认/异常 | `signal_computation_service.py` |
| P2-5 C4主力联动 | `_apply_phase_weight()` phase_map | `signal_computation_service.py` |
| P2-6 V1形态量化中枢 | 通过已有字段满足（zhongshu+chip暴露） | snapshot_assembler |
| P2-7 V2放量止跌/止涨 | `_detect_volume_reversal_sequence()` | `signal_computation_service.py` |
| P2-8 V3动态权重 | market_state分支集成在_apply_phase_weight | `signal_computation_service.py` |
| P2-9 V4 VAP支撑/阻力 | 通过已有near_levels+chip_peak满足 | snapshot_assembler |

**Phase 3 — 切换清理（4项）**
| 任务 | 交付物 | 文件 |
|------|--------|------|
| P3-1 前端适配 | narrativeText()三级降级 | `_ui-prototype/indicator-ide.html` |
| P3-2 配置切换 | auto/deepseek/fallback三档 | `config/description_config.json` |
| P3-3 精简trend_renderer | 258行→68行（-74%） | `backend/app/services/nlg/trend_renderer.py` |
| P3-4 新数据源规划 | 情绪四阶段+财务排雷+收益驱动接入方案 | `002-方案存档/273a-第三阶段新数据源规划.md` |

### 272号方案文档同步更新

| 章节 | 内容 |
|------|------|
| §7 | 九层描述框架（走势结构三要素/量价形态50+种/资金博弈评估） |
| §8 | 数据就绪度评估 + 阶段性实施路线 + C1-C4/V1-V4提升点 |
| §10 | G1-G5解决方案（Prompt工程/Fallback/数据管道/6角色/概念文件） |
| §10.2 | DeepSeek输出增强四措施 |
| §10.3 | 系统影响评估 + 4阶段渐进切换路径 |

### 新增档案

| 文档 | 说明 |
|------|------|
| `002-方案存档/272-策略分析NLG输出架构重设计报告.md` | 272号方案v2（九层框架+G1-G5完整方案） |
| `002-方案存档/273-272号方案全量实施计划.md` | 4阶段24项实施计划（全部完成） |
| `002-方案存档/273a-第三阶段新数据源规划.md` | 情绪四阶段/财务排雷/收益驱动接入方案 |

---

## 二、关键文件索引

| 文件 | 说明 |
|------|------|
| `data/knowledge/concepts/股票现状描述规范.md` | 九层框架概念文件（DeepSeek prompt知识来源） |
| `backend/app/services/fallback_description.py` | DeepSeek不可用时的兜底描述 |
| `backend/app/services/snapshot_assembler.py` | 策略信号→结构化JSON组装器 |
| `config/prompts/stock_status_description.txt` | 九层描述system prompt模板（配置外置） |
| `config/description_config.json` | 描述源切换配置（auto/deepseek/fallback） |
| `docs/schemas/structured_snapshot.md` | 结构化快照Schema定义 |
| `backend/app/services/nlg/trend_renderer.py` | 已精简（68行，保留兼容签名） |

---

## 三、验证状态

- `py_compile` — 全部修改文件编译通过
- fallback_description — 6组测试全部通过
- Phase 1辅助函数 — 7组测试全部通过
- Phase 2量价增强 — 8组测试全部通过
- trend_renderer精简 — 编译通过，导出函数签名向后兼容

### 2026-07-17 更新：273a方案实施完成

- **情绪四阶段接入**:
  - ECM 新增 `sentiment_pool_cache` 表 + write/read 方法
  - AKShare 采集器新增 `_collect_sentiment_pool()` 线程（Thread 6, 1800s）
  - DataManager 新增委派方法 `get_cached_sentiment_pool()`
  - NEW `backend/app/services/market_sentiment_service.py` — 涨停池聚合 → 四阶段映射（冰点/复苏/高潮/退潮/中性）
- **财务排雷接入**:
  - ECM 新增 `finance_report_cache` 表 + write/read 方法
  - `sync_fina_indicator_data()` 同步时自动写入 finance_report_cache
  - DataManager 新增委派方法 `get_cached_finance_report()`
  - NEW `backend/app/services/finance_report_service.py` — ROCE/速动比率/现金流/负债率 规则引擎
- **Snapshot 集成**:
  - `_extract_verification()` → `assemble()` 新增 `verification` 区块
- **测试**: `backend/tests/test_273a_implementation.py`（26用例）
- **回归**: 122 tests: 117 passed, 3 skipped, 2 xfailed

### 2026-07-17 更新：补写273号测试文件 + DeepSeek真实API验证

- **新增测试文件**: `backend/tests/test_273_implementation.py`（76个测试用例）
  - Phase 0: fallback_description(6) + _fmz_to_emotion_cycle(4) + _dedupe_near_levels(2)
  - Phase 1: _build_filtered_levels(5) + sandwich_zone(4) + C2(5) + C3(3) + BOLL/MA(5)
  - Phase 2: _classify_vp_basic(7) + _apply_phase_weight(7) + V2反转序列(4) + _build_active_signal(5) + _build_active_label(3) + SnapshotAssembler(5)
  - Phase 3: config(3) + trend_renderer(1) + system prompt(2)
  - 整合: 模块可导入(1) + 真实信号组装(1) + DeepSeek API(3)
- **DeepSeek真实API验证**: 系统已配置 `LLM_PROVIDER=deepseek`，`.env`中有有效API Key
  - `test_deepseek_health` ✅ 返回 `provider=deepseek, configured=True`
  - `test_snapshot_prompt_integration` ✅ 九层描述prompt + SnapshotAssembler可成功调用DeepSeek
- **运行结果**: 91 passed, 3 skipped, 2 xfailed（含全部273测试 + test_core + test_services）
- **文档化问题**: 
  - `_classify_vp_basic()` 条件顺序bug: `天量天价/地量地价`不可达（被前面的量增价涨/量缩价跌拦截）
  - `_build_active_label()` 依赖 `_compute_chanlun_signal` 的 BP_TYPE_CN 局部变量作用域

---

## 四、后续可选方向

| 优先级 | 事项 | 说明 |
|:------:|:-----|:------|
| ★★★ | DeepSeek稳定性观察 | 已从 mock 切换为真实 deepseek 模式，需持续观察 API 可用性和输出质量 |
| ★★★ | 情绪四阶段数据接入（273a方案） | 涨停家数/连板高度/炸板率 数据采集与聚合 |
| ★★ | 财务排雷数据接入（273a方案） | Tushare fina_indicator 财务数据管道 |
| ★★ | C1/C2/C3前端展示接入 | 夹层区间/散户反向指标/情绪拥挤度的可视化 |
| ★★ | 修复_classify_vp_basic条件顺序bug | 天量天价/地量地价条件应前置，否则永不可达 |
