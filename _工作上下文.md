## [已完成] 任务: 244号方案全量实施 - 进度: 全量完成
- **Phase A (存储层)**: A1 删除 DailyData 模型 ✅ A2 删除 _sync_cached_to_postgres ✅ A3 startup_data_check 用 DuckDB ✅ A4 scripts/clean_realtime_tables.py ✅ A5 scripts/clean_as_tables.py ✅ A6 minute_cache 已含 days_back=30 ✅ A7 Stock 月度同步启动检查 ✅
- **Phase B (Tushare 直调修复)**: B1 watchlist.py ✅ B2 benchmark_service.py ✅ B3 backtest_service.py(无需改) ✅ B4 sandbox_service.py ✅ B5 account_service.py ✅
- **Phase C (Dashboard PG→DuckDB)**: C1 get_daily_top ✅ C2 get_sector_changes ✅ C3 get_dashboard_summary ✅ C4 _try_get_index_daily log 标记 ✅
- **Phase D (其他修复)**: D1 screener.py Stock.count ✅ D2 review_engine.py DataManager 替换 ✅ D3 cache.py(文件不存在，跳过) ✅ D4 realtime.py(无 Tushare 引用，跳过) ✅ D5 Stock 月度 cron + startup_data_check ✅
- **涉及文件**:
  - 删除: `backend/app/models/__init__.py` — class DailyData (30-59行)
  - 修改: `backend/app/data/__init__.py` — 移除 PG 同步/降级/预热代码
  - 修改: `backend/app/scheduler_manager.py` — catch_up_if_needed DuckDB 化 + _run_stock_sync + _run_stock_sync_if_needed + 月度 cron
  - 修改: `backend/app/services/dashboard_service.py` — 3处 PG→DuckDB 迁移 + log 标记
  - 修改: `backend/app/services/review_engine.py` — 2处 DailyData→DataManager
  - 修改: `backend/app/services/market_service.py` — DailyData 移除 + get_daily_data 改用 DataManager
  - 修改: `backend/app/services/account_service.py` — 移除 unused DailyData import
  - 修改: `backend/app/routes/watchlist.py` — 移除 TushareProvider (Phase B)
  - 修改: `backend/app/routes/screener.py` — DailyData→Stock.count
  - 修改: `backend/app/routes/cache.py` — DailyData→DuckDB count
  - 修改: `backend/app/routes/phase3.py` — 移除 unused DailyData import
  - 新建: `backend/scripts/clean_realtime_tables.py`
  - 新建: `backend/scripts/clean_as_tables.py`
- **Phase B 已完成文件 (之前会话)**:
  - `backend/app/services/benchmark_service.py`
  - `backend/app/services/sandbox_service.py`
  - `backend/app/services/account_service.py`
  - `backend/app/routes/watchlist.py`
- **已迁移验证**: DailyData 模型已删除，全量引用清除完成
- **244号方案文档**: `002-方案存档/244-全局数据体系分层架构方案（规则C修订版）.md`

## [已完成] 任务: 全局数据体系生产化（243号方案） - 进度: 全量完成
- 涉及文件: 
  - 修改: `backend/app/data/enhanced_cache_manager.py`(路径+重试+旧PID+corrupted上限)
  - 修改: `backend/app/__init__.py`(采集器start+stop+Redis message_queue+PG init)
  - 修改: `backend/app/data/akshare_collector.py`(5线程PG双写+_get_pg惰性导入)
  - 修改: `backend/app/config.py`(连接池+REDIS_URL)
  - 修改: `.env`(DATABASE_URL→PG+REDIS_URL+FLASK_ENV=production)
  - 修改: `backend/requirements.txt`(gunicorn+psycopg2-binary)
  - 修改: `prepare-dev.command`(PG/Redis安装)
  - 修改: `start-backend.command`(开发模式SQLite)
  - 修改: `stop-dev.command`(Gunicorn停止)
  - 新建: `backend/app/data/realtime_pg.py`(8张盘中实时表+连接池+UPSERT)
  - 新建: `backend/gunicorn_config.py`(4 worker+on_starting hook)
  - 新建: `backend/run_gunicorn.sh`
  - 新建: `com.stock.analyzer.backend.plist`(launchd)
  - 新建: `backend/scripts/migrate_sqlite_to_pg.py`
- 运行时验证: Flask启动✅ 5线程采集器✅ ECM路径✅ DuckDB重试✅
- 待办: 用户跑 `prepare-dev.command` 安装 PG/Redis + psycopg2
- 方案: 243号方案已生成

已完成三大问题深度分析：AKShare 在 sandbox 内 DNS 被拦截但本机运行正常、ECM 双库路径 + 热重载导致 corrupted 文件膨胀至4GB、peer 竞品架构调研完成。见 242号方案存档。

文档清理完结：
- 227号方案全量更新适配 PG+Gunicorn+Redis 当前架构（§1-§11）
- 237/238 文档已添加 `status: ✅ 已完成`
- 239-243 文档已全部标注完成

## 任务: Tushare盘后数据验证 + 启动补采机制 - 进度: 已完成
- 涉及文件: 
  - 修正: `backend/app/scheduler_manager.py:353` — `_record_sync_start()` session rollback
  - 修正: `backend/app/scheduler_manager.py:312-330` — `_sync_by_type()` 方法名匹配 `sync_daily_basic_data` 等
  - 新增: `backend/app/scheduler_manager.py` — `catch_up_if_needed()` 方法
  - 新增: `backend/app/__init__.py:266` — 启动时调用 `catch_up_if_needed()`
- 发现:
  - DuckDB daily_cache 226万行/最新07-01 ✅ / daily_basic_cache 0行 ❌
  - PG daily_data 247万行/最新07-01 ✅
  - SyncLog最后一次有效同步: 07-02 03:23 (daily 470k + moneyflow 15k, basic 0)
  - SyncLog主键序列迁移未重置 → UniqueViolation → session rollback → 链式崩溃
  - _sync_by_type() 方法名与 DataManager 实际签名不匹配
  - 采集器5线程在非交易时段启动后立即停止(0采集) — 正常行为（未到交易时段）
- 修复: 
  - 同步方法名对齐
  - sync_log session rollback 异常处理
  - 启动补采机制（开机自动检测缺失数据并同步）
- 待验证: 用户需要执行 psql 序列修复 + 重启 Gunicorn
