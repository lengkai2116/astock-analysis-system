# 工作上下文

## 2026-07-23 会话 — 全局数据体系定版 + daemon韧性 + Tushare限流根治

### 已完成工作
1. **L1/L2/L3日志溯源**：确认来自API进程APScheduler，非data_daemon
2. **mootdx断裂确认**：通达信2026-07-20协议更新，quotes/bars返回空
3. **东财HTTP API替代mootdx**：289号方案，实测200次100%成功率
4. **生产级数据源适配层**：290号方案（健康评分+降级链+配置热加载）
5. **远程诊断+GitHub分发**：291号方案（diagnostic_export + upgrade_manager）
6. **全局数据体系权威架构定版**：292号方案，替代17份历史方案
7. **Bug修复**：FinancialRiskFilter copy-paste残留 / ChanlunAnalyzer bi_zs_mode缺失 / 预计算WARNING去重
8. **Daemon韧性**：看守脚本(start_daemon.sh) / 日终兜底(_check_daily_sync_backfill) / 分钟聚合接入HTTP降级路径
9. **Tushare全系统限速**：data_daemon(25处)+tushare_provider(45处)全部_ts()包装 + 3个逐只函数改为批量(adj_factor/top10_holders/stk_holder)
10. **分钟取数合规**：移除_get_minute_data()中Tushare/AKShare直调降级链
11. **降级自动恢复**：_SnapshotSourceManager新增RECOVERY_PROBE_INTERVAL，每10次周期探测东财恢复

### 当前daemon状态（PID 30778）
- 快照: 新浪HTTP(91只) — 东财临时断连，降级自动恢复探测已生效
- 分钟: mootdx minutes() 可用
- Tushare: 全调用_ts()限速 ≤5次/秒

### 已知遗留问题
- 申万行业指数代码只列了28个（应有31个）
- 融资融券margin_cache持续为空
- 分钟数据聚合int+NoneType（minute_backfill.py:176）
- 选股系统有效性评估细化（N1，下次会话重点）
- 缠论绘图修复验证（需重启Flask）

### 下次会话：选股系统调优
- N1: 选股系统有效性评估细化（核心）
- V1: 287号方案步骤2运行时验证
