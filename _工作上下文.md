# 工作上下文

> 最后更新: 2026-07-09 16:15 | 最新提交: `8fa1a35` (main，待新提交)
> 下一会话启动时优先阅读此文件恢复状态

---

## 当前系统运行状态

| 进程 | PID | 状态 |
|------|-----|------|
| API 进程 (run.py) | 3057 | ✅ 运行中 :5001 |
| 数据进程 (data_daemon.py) | 16019 | ✅ 运行中，含新修复（窗口扩大、指数日线同步） |
| PostgreSQL | 977 | ✅ stock_analysis 库连接正常 |
| mootdx TCP | — | ⚠️ 连通但 tdxpy NotImplementedError 日志(非关键) |
| WebSocket | — | ✅ 已连接 |

## 本轮变更总结 (commit 8fa1a35)

### 自选监控 WebSocket 实时推送
- **WsBridge**: 新增 `_watchlist_codes` 线程安全集合 + `_broadcast_watchlist_quotes()` → watchlist 房间
- **realtime.py**: `subscribe_watchlist` 注册代码到桥接器
- **watchlist.html**: SocketIO 客户端 + `stock:quotes` 事件实时更新（3s节流）
- **watchlist.py**: 新增 `GET /api/v3/watchlist/quotes` 批量行情聚合(41+字段) + `/dashboard` 端点
- **screener.py**: 新增 `POST /api/v3/screener/evaluate-batch` 批量策略评分端点

### 选股系统持久化 (screener.html)
- localStorage 缓存筛选结果 + 权重配置
- 页面往返/bfcache 恢复
- 修复 `})();})();` 语法错误导致整个 IIFE 中断
- 修复 results-table 默认 `display:none` 导致恢复后看不见数据

### 仪表盘日终同步修复 (data_daemon.py)
- 新增 `_batch_index_daily()` 同步四大指数日线
- 日终同步窗口 15:30→15:30-15:35（5分钟，防错过）
- 启动完整性检查 `backfill_days 1→3`
- 完整性检查新增独立指数日线补采

### Bug 修复
- **screener.py**: 补充缺失的 `ChipScorer` import（否则 `run_layer3` NameError）
- **ruff --fix**: 10项 import 排序/未使用 import 自动修复

## 本轮变更总结 — 258号方案全量实施（8个Phase完成）

### 实施的8个Phase
| Phase | 内容 | 涉及文件 | 行数 |
|:----:|------|---------|:----:|
| 0 | 修复两个API断裂(自选股列表+添加) | `indicator-ide.html` | ~5 |
| 1 | 分钟K线通道(MinuteDataManager接入) | `chart.py` | ~50 |
| 2+3 | K线主图+副图生产化(闭包mock→真实API) | `indicator-ide.html` | ~130 |
| 4 | BBI/ENE/九转后端+前端实现 | `indicators/__init__.py`, `chart.py`, `indicator-ide.html` | ~60 |
| 5 | 9周期切换绑定onclick事件+switchPeriod | `indicator-ide.html` | ~60 |
| 6 | SocketIO实时行情接入(stock:quotes) | `indicator-ide.html` | ~40 |
| 7 | 策略同步选股配置(syncFromScreener→真实API) | `indicator-ide.html` | ~25 |
| 8 | 后端数据管道(data_availability追踪+财务同步+完整性扩展) | `signal_computation_service.py`, `strategy_analyze.py`, `data_daemon.py` | ~100 |

### 修改文件清单
- `_ui-prototype/indicator-ide.html` — 核心前端页面重构(闭包mock→真实API+周期切换+WebSocket+策略同步)
- `backend/app/routes/chart.py` — 分钟K线通道+BBI/ENE/九转叠加支持+指标列表扩展
- `backend/app/routes/strategy_analyze.py` — data_availability嵌入响应
- `backend/app/services/signal_computation_service.py` — data_availability追踪
- `backend/app/indicators/__init__.py` — BBI/ENE/九转计算函数
- `backend/data_daemon.py` — 财务数据同步+完整性检查扩展(lhb_cache+财务表)
- `002-方案存档/258-个股策略分析板块数据连接现状核查与改善方案.md` — 更新为v2.0全量实施路线图

### 关键变更说明
1. **K线数据不再是mock**：`switchStock()` 调用 `applyRealData()` 将API返回的真实K线填入全局数组，所有指标重新基于真实数据计算
2. **周期切换真正可用**：9个标签页绑定onclick，调用后端API按周期取数
3. **实时价格**：SocketIO连接监听 `stock:quotes` 事件，盘中自动更新价格
4. **BBI/ENE/九转**：后端计算+前端渲染都已就位
5. **自选股没有404了**：两个端点已改为正确的 `/api/v3/watchlist`
6. **财务数据开始同步**：data_daemon日终同步后启动后台线程执行财务指标和利润表同步
7. **数据可用性透明**：API返回中携带 `data_availability` 字段，前端可展示数据状态

### 自选监控 WebSocket 实时推送
- **WsBridge**: 新增 `_watchlist_codes` 线程安全集合 + `_broadcast_watchlist_quotes()` → watchlist 房间
- **realtime.py**: `subscribe_watchlist` 注册代码到桥接器
- **watchlist.html**: SocketIO 客户端 + `stock:quotes` 事件实时更新（3s节流）
- **watchlist.py**: 新增 `GET /api/v3/watchlist/quotes` 批量行情聚合(41+字段) + `/dashboard` 端点
- **screener.py**: 新增 `POST /api/v3/screener/evaluate-batch` 批量策略评分端点

### 选股系统持久化 (screener.html)
- localStorage 缓存筛选结果 + 权重配置
- 页面往返/bfcache 恢复
- 修复 `})();})();` 语法错误导致整个 IIFE 中断
- 修复 results-table 默认 `display:none` 导致恢复后看不见数据

### 仪表盘日终同步修复 (data_daemon.py)
- 新增 `_batch_index_daily()` 同步四大指数日线
- 日终同步窗口 15:30→15:30-15:35（5分钟，防错过）
- 启动完整性检查 `backfill_days 1→3`
- 完整性检查新增独立指数日线补采

### Bug 修复
- **screener.py**: 补充缺失的 `ChipScorer` import（否则 `run_layer3` NameError）
- **ruff --fix**: 10项 import 排序/未使用 import 自动修复

## 待解决事项 (详见 001-沟通记录/待解决事项.md)

### 🔴 High
| # | 事项 | 截止 |
|---|------|------|
| 1 | DeepSeek模型迁移 `deepseek-chat-v4` → `v4-flash` | 2026-07-24 |
| 2 | 财务数据接入 data_daemon 日终同步（258号方案 P0）| ✅ 已实施 |
| 3 | 前端接入E14叙事渲染+挂载3路由（259号方案 Phase A+B）| 待审核 |

### 🟡 Medium
| # | 事项 |
|---|------|
| 4 | APScheduler `_replenish_history()` date比较bug |
| 5 | Vibe策略S1/S3待实现（P1）|
| 6 | 前端 vue-project 目录不完整 |
| 7 | 仪表盘 strategy_signal 字段名与实际不符（待清理）|
| 8 | 数据源降级对上游显式反馈（258号方案 Phase 8，已实施）|
| 9 | factors/builtin 因子族联通 SignalComputationService（258号方案 Phase 8，已实施）|
| 10 | 盘口E8/笼子E9/筹码E11路由（259号方案 Phase C）|
| 11 | 主力密码/资金仓位算法+融资融券清理（259号方案 Phase D）|

### 💡 待规划
| # | 事项 | 附着模块 |
|---|------|---------|
| 6 | AI交易机会雷达 → 对接监控通知（用户自设条件→L1核查→轮播）| #224 监控通知 |
| 7 | 板块资金流向(sector-moneyflow)为空 | 独立缺陷 |

## 关键文件索引

| 路径 | 说明 |
|------|------|
| `backend/data_daemon.py` | 数据守护进程（采集+日终同步） |
| `backend/app/data/ws_bridge.py` | WebSocket 桥接器（采集→推送） |
| `backend/app/routes/watchlist.py` | 自选监控API（蓝图 + 批量聚合） |
| `backend/app/routes/screener.py` | 选股系统API（含evaluate-batch） |
| `backend/app/routes/realtime.py` | 实时行情SocketIO事件+订阅 |
| `backend/app/services/dashboard_service.py` | 仪表盘8端点数据服务 |
| `_ui-prototype/watchlist.html` | 自选监控前端（SocketIO实时） |
| `_ui-prototype/screener.html` | 选股系统前端（localStorage持久化） |
| `001-沟通记录/待解决事项.md` | 全量待办清单 |
| `001-沟通记录/沟通索引.md` | 所有历史沟通摘要索引 |
| `002-方案存档/258-个股策略分析板块数据连接现状核查与改善方案.md` | 全量实施路线图 v2.0（8个Phase，待审核） |

## 已知问题（未修改）

1. **sector-moneyflow 空** — PG Stock 表 industry 映射不足或 moneyflow 聚合失败（258号方案§5.4 P2）
2. **sector-sector 不完整** — 仅5涨0跌，pct_chg 字段缺失
3. **daily_top 空** — 依赖 mootdx 盘中实时数据（盘前/非交易时段正常）
4. **mootdx tdxpy NotImplementedError** — 不影响功能，但日志有噪音
5. **4个ruff E501/F841** — 全部预存问题，不在本轮修改范围
6. **财务数据未入日终同步** — 258号方案 Phase 8，待审核
7. **indicator-ide.html 全量mock已修复** — 258号方案 Phase 0~7，K线/指标/周期/副图/实时价格已生产化
8. **前端E14叙事未接入** — 卡叙事/情景推演/行动建议仍为硬编码Mock（259号方案 Phase A）
9. **盘口/笼子/筹码无路由** — E8/E9/E11未实现（259号方案 Phase B+C）
10. **E14 性能瓶颈** — 3.04秒DeepSeek API调用，需异步加载优化
