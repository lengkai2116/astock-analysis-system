# 工作上下文

> 最近更新：2026-09-10 10:50（✅ 启动验证清单 3 项全部完成：重跑验证 + JUD/OUT 写分库 + 主库清理；
> 期间发现并根治昨日"写分库修复"的 API 误用 bug）

## 当前任务: 无进行中任务（2026-09-10 验证清单已闭环）

- **状态**: 全部完成。422 号方案存档；daemon(7437)/watchdog(7395) 运行中，20260909 管道 SIG/JUD/OUT done

## ✅ 2026-09-10 完成事项

### 1. 启动验证执行（暴露并根治昨日"写分库修复"致命 bug）
- daemon 今早 09:00 已由看守自动拉起（加载昨日修复代码），但 20260909 全 done → 正常重启不重跑
- 运行中重置 pending 撞 daemon 长期写锁（`mark_step_running` 无 commit）→ SIGKILL 空窗重置（用户授权）
- **第一轮重跑暴露 bug**：昨日修复用错 API `get_connection('status_snapshot')`（参数应为分库文件名，
  误传表名）→ 写入野文件 `data/duckdb/status_snapshot`(290MB)/`treemap_snapshot`(8.7MB)，
  **分库从未被写入**（昨日仅 grep"无 `_ecm.conn` 写"漏检连接目标）
- 修正 4 处为 `get_connection(get_db_for_table(...))` + 删野文件 → 第二轮重跑验证
  **分库 status/treemap = 2026-09-10（5549/5544）** ✅

### 2. 附带修复 3 处同源缺陷
- treemap 归档 `SELECT *` 列错位（历史表残留 seven_dim_report 48vs47）→ 显式列名
- OUT watchlist 段 3 处读 status_snapshot 切分库
- **R4a 读方补齐 8 处**（valuation_estimator/cross_validate/potential_engine/ECM 3 方法/strategy_analyze）
  → 全改 `_query_shard`/分库连接

### 3. 主库残留清理 + 最终验证
- 新建 `backend/scripts/migrate_status_treemap_shard.py`：主库 status 5549→0、treemap 5544→0
  （备份 `_bak` 可回滚）
- OUT 重跑（10:27 done）：treemap 归档成功、读取方路由命中分库

## 数据库状态（2026-09-10 10:50）

| 库/表 | 状态 |
|--------|------|
| snapshot_cache.db status_snapshot | **2026-09-10 · 5549** ✅ |
| snapshot_cache.db treemap_snapshot | **2026-09-10 · 5544** ✅ |
| snapshot_cache.db status_snapshot_history | 含 09-10 批次 ✅ |
| snapshot_cache.db treemap_snapshot_history | 含 09-10 批次（新写入正确）✅ |
| stock_cache.db status/treemap | **0 行**（备份 status_snapshot_bak/treemap_snapshot_bak 可回滚）✅ |
| stock_cache.db pipeline_status 20260909 | SIG/JUD/OUT done ✅ |
| strategy_signal_detail（分库） | 最新 trade_date=08-26（09-09 21:06 写入） |
| 主库/分库 journal_mode | 均 WAL ✅ |

**daemon**：PID 7437（10:24 启动，加载全部修复）；watchdog PID 7395

## 🔑 关键认知

- **`sharding_manager.get_connection()` 参数是分库文件名，表→库映射须经 `get_db_for_table()` 转换**
  （两段式）——昨日 4 处误传表名生成野文件，验证需查连接目标而非仅 grep
- 读写迁移必须成对实施：写方切分库后所有读方（含回退分支）须同步切分库，否则清理主库残留
  会静默破坏读方
- 正常重启 daemon **不重置** pipeline_status（20260909 全 done 不触发重跑）——需 SIGKILL
  空窗重置 pending
- daemon 内部连接可能长期持主库写锁（`mark_step_running` 无 commit 等）→ 外部写主库需空窗期
- `_query_df` 只读主库（read_conn），`_query_shard` 才按表路由分库——读分库表必须用后者

## 遗留待办（可选，待议）

1. **测试 2 failed（数据依赖）**：600519 等 `strategy_signal_detail.signals` 为空 →
   `_restore_signals_from_cache` 返回 `[]`（test_322 / test_320::p2_signal_summary）
2. **treemap_snapshot_history 5571 行历史错位脏数据**（snapshot_date 列存股票名，旧 `SELECT *`
   遗留）——可选迁移重建
3. `_verify_out_completeness` active_count 读主库 daily_cache（分库表）→ OUT-CHECK 跳过（低影响）
4. `mark_step_running` 补 commit（建议）
5. dim2/dim3 `_load_precomputed_macd` 直调 DataManager 残留（可选）
