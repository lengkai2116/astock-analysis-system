## ✅ 自选监控策略评估 Bug 修复（2026-07-09）
- **根因1（评分=—）**: `evaluateStock()` 调用 `/strategy/analyze`，该端点返回 `{code:0,...}` 不含 `success` 字段；前端检查 `resp.success` 恒为 false，始终走 `fallbackEvaluate` 把评分写成字符串 `'—'`
- **根因2（不持久）**: 评估结果仅存 `stockData` 内存，切页面后 `/watchlist/quotes` 不返回策略评分（ECM 无此数据）
- **修复**: 
  - `evaluateStock()` 改为调用 `/screener/evaluate-batch`（单只），该端点返回 `success: true`，评分映射正确
  - 抽取 `_applyEvalToStock()` / `_saveEvalToCache()` / `_loadEvalFromCache()` 三函数
  - 评估成功自动存 `sessionStorage`（key=`wl_eval_{code}`），`loadWatchlistData()` 加载 API 数据后覆盖 sessionStorage 中缓存的评分结果
  - 切页面/刷新不丢失评分状态（同一 tab session 内）
- **涉及文件**: `_ui-prototype/watchlist.html`

## ✅ 选股系统筛选结果持久化修复（2026-07-09）
- **根因**: sessionStorage 写入/读取在部分浏览器环境（私密模式/file://等）可能静默失败，原 `try/catch(e){}` 无声吞掉异常。无服务端兜底机制
- **修复**:
  - 抽取 `_saveScreenerCache()` / `_loadScreenerCache()` / `_restoreScreenerCache()` 三函数
  - 写入失败时 `console.warn` 输出可见日志（不再静默吞异常）
  - 新增服务端缓存兜底: `_restoreScreenerCache()` 中 sessionStorage 恢复失败时，异步调用 `POST /api/v3/screener/run?useCache=true` 从后端获取缓存
  - `runScreening` 中的写入点改为调用 `_saveScreenerCache(res.data)`
- **涉及文件**: `_ui-prototype/screener.html`

## ✅ 选股系统缓存终极修复（2026-07-09）
- **根因**: 前两轮 sessionStorage 方案未解决实际问题。改用 `localStorage` + 彻底简化逻辑
- **修复要点**:
  - 用 `localStorage` 替代 `sessionStorage`（不随标签页关闭清除，更可靠）
  - 每次筛选成功时保存 `{rows: _screenResults, w: 权重配置}` 到 `ss_results` key
  - 页面加载时同步从 `localStorage` 读取并立即恢复：
    1. 恢复 `_screenResults` 结果数据
    2. 恢复权重滑块DOM值
    3. 调用 `renderPage()` 渲染表格
    4. 更新结果计数
  - bfcache 恢复通过 `window.addEventListener('pageshow')` 捕获
  - 移除了之前所有复杂的 pending restore/兼容层代码
- **验证**: 服务端缓存有 87 条筛选结果，两个进程运行正常

### 待解决事项.md 全量核查与更新
- **问题**：原文件最后更新 2026-06-07，大量已完结项仍标注为"待办"
- **动作**：对照 MEMORY.md（382行）+ 代码库逐项核查，全量重写
- **结果**：248行→277行，16章节。26项已完结标记为✅，保留真正未完成项
- **新增章节**：§十二全局数据体系遗留/§十三Vibe策略现状/§十四186号P2/P3剩余/§十五代码质量
- **新增🔴项**：DeepSeek模型迁移（2026/07/24截止，6处 hardcode）
- **涉及文件**：`001-沟通记录/待解决事项.md`（全量重写）、`001-沟通记录/沟通索引.md`（追加索引）、`001-沟通记录/26-07-08沟通纪要.md`（纪要）、`_工作上下文.md`

## ✅ 2026-07-08 全量工作完成

### 选股系统全链路改造

#### AGENTS.md 更新
- 从 48 行→166 行，新增架构要点、关键命令、配置表、重要约束、测试说明、目录体系

#### 页面路由修复
- `backend/app/__init__.py` 添加通用路由 `/<path:filename>.html`，所有原型页面可访问

#### 数据接口核查与修正
- `DataManager.get_stock_list` 支持 industry 过滤
- stats 端点 stock_count 修复（原 `row[0]` 遍历 dict 导致异常）
- `enhanced_cache_manager.py` ECM 日期格式确认：moneyflow_cache 为 YYYY-MM-DD（ISO），查询须匹配

#### L3 权重/组合/Vibe 参与度修复（原为 UI 假象）
- 前端 runScreening 动态 POST weights+combinations+vibe IDs
- 后端 `_compute_combined_score()` 接收 weights 参数并加权归一化
- 未勾选策略自动归零，权重可调零（min=0）
- 因子评分简化实现：基于 OHLCV 计算 4 基础因子

#### z-score 归一化
- 两阶段架构：阶段一计算原始分，阶段二归一化后再加权
- 权重影响力偏差从 12.3% 降至 2.3%，综合区分度提升 29%

#### L2 主力识别重构（chip_strategy.py）
- ChipScorer(OHLCV-only) → MainForceScorer(资金流向moneyflow_cache驱动)
- 四维评分：A 资金流向(0-3)+B 价量四阶段识别(0-3)+C 筹码集中度(0-2)+D 散户反向指标(0-2)
- 硬截断 Top100 → 阈值筛选（≥6.4），top20兜底
- 新增 `identify_phase()` 标注建仓/洗盘/拉升/出货
- L2 3715→106 只（通过率 2.9%），真正发挥筛选作用
- 验证：旧 ChipScorer 类保留不变，不影响个股策略分析板块

#### LLM Wiki 知识库接入优化
- 读取主力行为分析、筹码分布、三重过滤系统等 Wiki 概念页
- 新增资金连续性评分（max_streak）、散户反向指标（small-order contrarian）
- 价量评分改为四阶段制取代累积加分
- 阶段感知评分：`_phase_timing_bonus(l2_phase, cl_signal)` 共振矩阵
- 建仓+买点=+2.0, 拉升+买点=+1.5, 出货=-1.0

#### L3 因子组合真实接入（screener_strategy_integration.py）
- `_compute_factor_score(df, symbol, combo_ids)` 按 PRESET_COMBOS 逐因子计算并加权
- `_FACTOR_COMPUTERS` 映射表（9 因子注册），未映射因子中性分 5.0 回退
- 无组合→factor_score=0 ✅
- p3+p5→差异化因子分（4.6~7.8）✅

#### L3 Vibe 策略执行引擎
- `_execute_single_strategy(code, df)` 对每只股票独立执行 code_template
- 注入 OHLCV+MA/RSI/MACD/STD 函数，捕获 signal 变量
- 自动处理 AI 代码的 `return signal`（cleaned_code 预处理）
- 降级模式：无 df 时统一 +0.5/个
- 验证：连续策略 106 只→98 种不同评分 ✅

#### Vibe 策略管理
- 从 DB 加载（`StrategyTemplateV2.vibe=True`），排除 L1/L2/L3 内置策略
- 排除名单：DarwinRiskStrategy, MainForceTrackingStrategy, ChipStrategy, ChanlunStrategy, VolumePriceStrategy
- 创建新策略对接真实 API：`/api/v2/strategy/generate` → `/api/v2/strategy/templates`
- 存档 1 个验证策略（id=20），清理 7 个测试策略

#### 前端改动（screener.html）
- 分页：每页 20 条，页码导航栏
- 操作按钮：☆添加自选（POST /api/v3/watchlist）+ 📈策略分析（跳转 indicator-ide.html）
- 权重联动：缠论/量价/因子/Vibe 勾选取消时自动归零/恢复默认
- 筛选结果 sessionStorage 缓存（切页面返回不丢失）
- Vibe 创建对接 AI 生成 + 策略模板保存

#### 性能优化
- ECM `preload_all()` + `get_cached_daily_batch()` 全量预加载
- 重复刷新耗时：19s→14.5s（-24%）

#### Bug 修复
- `chart.py` `_format_time` 时区 Bug：统一 UTC 时间戳，`test_format_time` 21/21 通过
- `screener_strategy_integration.py` SyntaxError：merged function defs 修复
- `chip_strategy.py` f-string 反斜杠、logger 缺失、timedelta 未导入
- `init_system_templates.py` Vibe 策略标记持久化

#### Vibe 策略待开发项（记录到待解决事项 §十）
- S1 市场情绪周期策略（P1）
- S3 趋势通道识别策略（P1）
- S4 涨停板短线策略（P2）
- S6 波浪理论阶段识别策略（P3）
- S7 斐波那契时间周期策略（P3）

#### 代码提交
- 15 个文件，+2503/-271 行
- commit `df8353c` → 已推送 GitHub（含 filter-branch 清理 1.35GB 大文件）
- data_daemon.py: 采集+日终同步（批量 API <2s）+ 完整性检查
- run.py: 仅 API，不再启动采集器 (DATA_DAEMON_RUNNING=1 跳过)
- start.command: 编排启动（数据→API→浏览器）
- 一键清理：根目录 9→4 个启动文件

### 今日修复
- tushare_provider.py: 3 处 raw string 日志 bug
- precompute_indicator_manager.py: raw string + datetime binding bug
- dashboard_service.py: 新增 _get_best_trade_date() 防残缺交易日
- ECM: margin_cache 重建（列名匹配 Tushare）+ minute_kline_cache 重命名
- mootdx L1 快照 → 1min 窗口聚合 → `minute_kline_cache` 表
- 验证：1885只/分钟，实时写入成功
- API: `/api/minute/<ts_code>` 优先读 cache (source=cache)

### Phase 2 ✅ ECM 13 张新表 + 方法
- 建表：minute_kline_cache, fina_indicator_cache, income_cache, balancesheet_cache, cashflow_cache, forecast_cache, margin_cache, stk_limit_cache, lhb_cache, top10_holders_cache, stk_holder_cache, concept_cache, index_member_cache
- ECM `cache_xxx()` + `get_cached_xxx()` 方法全量实现
- DataManager `sync_xxx()` 同步方法全量实现

### Phase 3 ✅ 日终调度注册
- `_sync_by_type()` 新增 13 个分支
- 默认 `data_types` 配置追加全部新增类型

### Phase 4 ✅ 预计算指标完成（2026-07-07 10:30→10:51）
- `indicator_cache`: **85,242,254 行 / 4,999 只 / 15 指标**
- 覆盖：ma5/10/20, macd_dif/dea/hist, rsi14, kdj_k/d/j, boll_upper/mid/lower, vol_ma5/10
- 耗时：21分钟
- 数据库膨胀：~3G → 15G（SQLite WAL）
- 修复：precompute_indicator_manager.py raw string bug + datetime binding bug

### Phase 5 ✅ E07/E10 改造
- E07 28字段：ECM缓存优先，Tushare降级。23/28字段已有数据
- E10 资金流向：moneyflow_cache 优先，Tushare降级

### 回填工作 — 进行中（2026-07-07）
- **baostock 分钟K线 (PID 18538)**: 2770/5004只 (55%), 100190行 → 运行中
- **Tushare 财务数据 (PID 19409)**: fina_indicator_cache 7082行 → 运行中
- 两个进程后台并行，互不干扰
### 结果：全部通过 ✅
| 端点 | 状态 | 数据 |
|------|------|------|
| `/market/overview` | ✅ | 4指数+迷你K线（盘中实时） |
| `/market/index-daily` | ✅ | 20日成交额 |
| `/market/daily-top` | ✅ | 涨/跌幅各10只 |
| `/market/sector-sector` | ✅ | 5涨5跌板块（NaN已修复） |
| `/dashboard/summary` | ✅ | 4988只+8雷达+5信号 |
| `/market/moneyflow-summary` | ✅ | 20资金流向日 |
| `/market/sector-moneyflow` | ✅ | 8流入5流出板块 |
### 修复的问题：
1. **NaN板块** — PostgreSQL中11只Stock.industry='NaN'字符串导致聚合后出现NaN条目。已清理DB + 代码防御
2. **mootdx未安装** — venv缺少mootdx，实时数据由DuckDB日线降级。已安装，TCP直连启动成功
3. **sector-sector reader空数据** — AKShare HTTP受Claude代理阻断，as_sector_ranking表无有效数据。reader路径跳过空数据后降级DuckDB聚合
4. **source字段缺失** — reader返回路径缺少source字段（已补齐）

## [已完成] 247号方案全量实施（2026-07-05）
- **涉及文件 (5个)**:
  - `backend/app/engine/framework/screener.py` — 2处修复
  - `backend/app/data/__init__.py` — 新增 sync_daily_basic_history()
  - `backend/app/scheduler_manager.py` — 新增 _catch_up_daily_basic() + 修复非交易日跳过Bug
  - `backend/app/engine/framework/volume_price_strategy.py` — VP Bug修复（上轮）
  - `backend/app/engine/framework/volume_price_strategy.py` — NumPy RuntimeWarning压制 `analyze()` `with np.errstate(all='ignore')`
  - `backend/app/data/akshare_collector.py` — stock_zt_pool_em API兼容修复 + 连续失败退避日志降噪 + no_proxy防护
  - `backend/app/routes/screener.py` — 补充 industry、pct_chg 输出字段
  - `backend/app/engine/framework/screener_strategy_integration.py` — 补充 industry 占位
  - `_ui-prototype/screener.html` — 新增行业列、涨跌幅红涨绿跌渲染、昨收标注
- **注意**: AkshareCollector 5个采集线程在用户网络环境下全部因 RemoteDisconnected 失败（东方财富API被环境网络/代理限制）。影响的是盘中实时展示（行情/板块/涨跌停/分时图/新闻），仪表盘有自动降级到 daily_cache 日线数据。选股等核心计算不依赖盘中采集，不受影响。

## [已完成] mootdx 方案可行性验证（2026-07-06）
- **根因定位**: 所有 AKShare HTTP 采集失败的原因是 Claude Code 注入 Shell 的代理变量（`HTTP_PROXY=http://srt:***@localhost:52769`）拦截了外发流量，非网络本身问题
- **mootdx 验证**: TCP直连通达信行情服务器可用 ✅
  - `stocks()` 返回 27530 只股票 ✅
  - 底层 `get_security_quotes()` 返回 45 字段 L1 行情（price, last_close, open, high, low, vol, amount, 五档盘口）✅
  - `quotes()` 高层方法因 symbol 格式问题返回空（后续需修复）
- **备份提交前状态**: main 分支当前有未提交变更
- **待实施**: 将 akshare_collector.py 改为以 mootdx 为核心、AKShare 为低频补充的架构

## Phase 0 完成 ✅ mootdx TCP 连通性验证（2026-07-06）
- **Quotes.factory()**: 成功 (StdQuotes)
- **stocks()**: 27530 只（A股 2761 只，北交所 1121 只）
- **quotes()**: 46列 L1 行情 + 五档盘口，0.033s/5只
- **bars(freq=9)**: 日线 OHLC 数据齐全，范围至 2026-07-06
- **批量(500只)**: 0.05s ✅
- **北交所**: 不支持（返回空）
- **分钟K线**: `minutes()` 仅 3 列(price/vol/volume) 不完整；`bars(freq=5)` 返回周线。需要进一步验证 mootdx 的分钟级 OHLC 数据源
- **mootdx 版本**: 0.11.7（已安装，不在 requirements.txt 中）
- **结论**: mootdx TCP 直通本机，L1 行情可用，可以进入 Phase A 实施

## Phase A 完成 ✅ mootdx_collector.py 创建（2026-07-06）
- **新文件**: `backend/app/data/mootdx_collector.py` (403行)
  - mootdx 客户端工厂（惰性初始化+重连）
  - `collect_market_snapshot()` — 分批采集全市场 A 股 L1 行情（每批~100只，28批总耗时~1s）
  - `_compute_top_stocks()` — 从快照自算涨跌榜
  - `_compute_limit_pools()` — 从快照自算涨跌停池
  - `_MootdxThread` 采集线程 + `MootdxCollector` 管理器
  - 写入 InMemoryStateStore + WsBridge 推送（与 AKShare 共用链路）
- **修改文件**:
  - `backend/requirements.txt` — 添加 mootdx>=0.11.7
  - `backend/app/__init__.py` — 启动时初始化 MootdxCollector
  - `backend/gunicorn_config.py` — on_starting/on_exit 钩子
- **验证结果**:
  - 分批采集: 1909 只有效（2761 只 A 股中有 1909 只今日有交易）
  - 涨跌榜: 20 只涨幅/20 只跌幅 ✅
  - 涨停池: 40 只 ✅
  - 五档盘口: 完整 ✅
  - Flask app factory 导入: 通过 ✅
- **待办**: 下一阶段将 AKShare 对应线程瘦身（Thread 1 market_snapshot + Thread 2 top_stocks）

## Phase B 完成 ✅ akshare_collector.py 瘦身（2026-07-06）
- **瘦身结果**: 847 行 → 326 行（-521 行）
- **移除内容**:
  - 全局令牌桶 _throttle/_throttle_bump_success/failure (~40行)
  - monkey-patch requests (push2子域名重写 ~30行)
  - _em_get 三层 HTTP 兜底（urllib→requests→curl, ~55行）
  - test_eastmoney_connectivity (~20行)
  - _collect_market_snapshot 全函数 (~57行)
  - _collect_top_stocks 全函数 (~42行)
  - 涨跌停池 AKShare 采集部分 (_collect_sector_and_limit 中 ~63行)
  - _get_pg() + 全部 PG 写入代理 (~28行 + 8处调用)
  - watchlist 老机制（保留精简版用于分钟线）
  - clear_watchlist 简化为仅清分钟线列表
- **保留**: 3 线程（sector_and_limit 30min / minute_kline 5min / lhb_and_news 30min）
- **修改**:
  - `backend/app/routes/realtime.py` — connectivity 端点改为 mootdx 诊断
  - `backend/app/__init__.py` — AkshareCollector 启动日志改为"3 线程, 低频补充"

## Phase C 完成 ✅ 移除 realtime_pg.py（2026-07-06）
- **删除文件**:
  - `backend/app/data/realtime_pg.py` (~200行，PG 盘中实时表写入层)
  - `backend/scripts/clean_realtime_tables.py` (~51行，PG 实时表清理脚本)
- **清理外部引用 (3处)**:
  - `backend/app/__init__.py` — 移除 `init_realtime_pg()` 调用
  - `backend/gunicorn_config.py` — 移除 `init_realtime_pg()` 调用
  - `backend/app/scheduler_manager.py` — 移除 `cleanup_old_data()` 调用，替换为废弃说明注释
- **验证**: Flask app factory 启动正常，无 realtime_pg ImportError
- **效果**: 盘后清理链从 5 步减少到 4 步（不再清理 PG realtime_* 表）
- **数据接管**: 盘中数据全部由 InMemoryStateStore（mootdx TCP 写入）+ DuckDB as_* 表（归档）承载

## Phase D 完成 ✅ PG 实时表配置清理（2026-07-06）
- **数据库模型**: 无 SQLAlchemy 模型定义 realtime_* 表（realtime_pg 用原生 SQL，无 ORM 层）✅
- **_项目运行手册.md**: 无 realtime_* 表/配置引用 ✅
- **docker-compose.yml**: PostgreSQL 仍用于业务表（策略/账户/信号等），与 realtime 无关 ✅
- **migrations/init.sql**: 无 realtime_* 表定义 ✅
- **`.env`**: `DATABASE_URL` 指向业务 PG，与 realtime 无关 ✅
- **`__init__.py` 中 `realtime_bp`**：REST API 路由（从 InMemoryStateStore 读数据），功能正常 ✅
- **结论**: PG 盘中实时表配置 0 处残留，Phase D 确认完成

## Phase E 完成 ✅ 端到端验证（2026-07-06）
### 通过项
| 验证项 | 结果 | 数据 |
|--------|------|------|
| mootdx TCP 采集 → InMemoryStateStore | ✅ | 2057 只有效，33 字段完整包含五档盘口 |
| 从快照自算涨跌榜 | ✅ | Top 20 涨幅/跌幅 |
| 从快照自算涨跌停池 | ✅ | 40+43 只 |
| WsBridge 读取 store + 推送调用 | ✅ | 无 socketio 环境静默跳过 |
| store.get_by_code() 查询 | ✅ | 3/3 命中 |
| store.batch_get() 批量查询 | ✅ | 3/3 命中 |
| store meta/age/stale | ✅ | 未过期 |
| minute_kline (AKShare) | ✅ | 144 条 / 3 只股票 |
| news (AKShare) | ✅ | 10 条 |
| REST API realtime 查询 | ✅ | /api/v3/market/realtime 200 |
| REST API daily-top | ✅ | /api/v3/market/daily-top 200 |
| REST API indexes | ✅ | /api/v3/market/indexes 200 |
| REST API industries | ✅ | /api/v3/market/industries 200 |
| REST API connectivity | ✅ | mootdx status=running |
| Tushare 日线 | ✅ | 2 条 |
### 发现的问题
1. ~~**指数代码格式不兼容** — mootdx 将上证指数 000001 标记为市场 0(SZ)而非 1(SH)，存储在 `000001.SZ` 下；但 WsBridge/dashboard_service 查找 `1.000001` 或 `000001.SH`。导致四大指数行情不可用。已修复：使用 mootdx `index()` 替代 `quotes()` 获取指数数据 ✅~~
2. ~~**DuckDB 持久损坏** — 已根治：替换为 SQLite WAL，删除所有 corrupted 文件 ✅~~
3. ~~**AKShare HTTP 板块排行不可用** — 已修复：使用 mootdx L1 快照 + Tushare stock_basic 行业分类在内存中自算板块排行 ✅~~
4. **🔴 北交所数据缺失** — mootdx TCP 不支持北交所（通达信协议不传输北交所数据）。无当前解决方案，标记为待解决。后续可考虑 AKShare/efinance 等 HTTP 方案补充

## 250号方案已实施 ✅ DuckDB → SQLite WAL 切换完成（2026-07-06）
- `enhanced_cache_manager.py` 重写（1498→398行，-1100行）
  - duckdb → sqlite3 (WAL 模式)
  - DECIMAL → REAL, VARCHAR → TEXT, BOOLEAN → INTEGER
  - `register('temp', df)` + `SELECT FROM temp` → `_insert_from_df()` (使用 df.to_sql)
  - `fetchdf()` → `_query_df()` (使用 pandas.read_sql)
  - 移除 DuckDB 重试/降级/ corrupted 备份逻辑（SQLite 无需此机制）
- 清理：删除 data/duckdb/ 下所有 .db/.backup/.corrupted/.wal/.tmp 文件（~1.3GB）
- 依赖：requirements.txt 移除 duckdb==1.0.0
- 效果：Flask 启动日志干净，无 serialization 错误
- 清理：2 个脚本 (clean_as_tables.py / check_cache_data.py) 移除 duckdb 引用
- 验证: Python 代码中 duckdb 引用 0 处残留 ✅

## [已完成] 仪表盘6个缺失API端点修复完成（2026-07-06）
- **修复内容**: 解决6个仪表盘端点中的5个数据不可用/返回空问题
- **根因分析**:
  - `daily_cache` 无指数行情数据 → 调用 `sync_index_daily_data()` 补4大指数20764行
  - `moneyflow_cache` 空 → 调用 `sync_moneyflow_data()` 补5194行
  - 行业映射空（Stock表无行业数据）→ `sync_stock_list()` 补5527条 + Tushare直调降级
  - `ECM.cache_moneyflow_data()` 的 `str.replace` 不能处理 `datetime.date` → 改为 `pd.to_datetime().dt.date`
  - `DataManager.sync_index_daily_data()` 将Tushare原始字段直传 `cache_daily_data()` → 过滤多余列
- **修改文件 (3个)**:
  - `backend/app/services/dashboard_service.py` — get_sector_changes() 增加Tushare行业映射降级
  - `backend/app/data/__init__.py` — sync_index_daily_data() 过滤多余列
  - `backend/app/data/enhanced_cache_manager.py` — cache_moneyflow_data() 日期处理修复
- **新建文件 (1个)**: `backend/scripts/sync_dashboard_data.py` — 数据同步脚本
- **验证结果 (6/6通过)**:
  | 端点 | 状态 |
  |------|------|
  | `/api/v3/market/index-daily` | ✅ 20日成交额 |
  | `/api/v3/dashboard/summary` | ✅ 8雷达+7信号 |
  | `/api/v3/market/daily-top` | ✅ 10只涨跌 |
  | `/api/v3/market/sector-sector` | ✅ 5涨5跌行业 |
  | `/api/v3/market/moneyflow-summary` | ✅ 1日(需历史同步) |
  | `/api/v3/market/sector-moneyflow` | ✅ 8行业板块 |
- **遗留**: moneyflow仅1日数据(2026-07-06)，需要更多历史数据的日终同步；APScheduler date类型bug

## [已完成] 全局数据体系集采补齐：资金流向历史回填（2026-07-06）
- **之前**: moneyflow_cache 仅 1 天 (2026-07-06) ❌
- **之后**: 120 天完整，621,891 行 ✅
- **范围**: 2026-01-05 ~ 2026-07-06
- **同步脚本**: `backend/scripts/bulk_sync_moneyflow.py`（可复用）
- **验证**:
  - `/api/v3/market/moneyflow-summary` ✅ 20日真实资金流向
  - `/api/v3/market/sector-moneyflow` ✅ 13板块带个股明细
- **遗留**: as_* 表0行（已废弃）；indicator_cache/chip_distribution_cache 0行（引擎计算结果，非同步数据）

## [已完成] 全局数据体系全量集采补齐（2026-07-06）
- **Phase 1**: adj_factor_cache — 508万行/1212天/5548只 (93s)
- **Phase 2**: chip_distribution_cache — 60万行/4998只 (78s)
- **Phase 3**: factor_cache — 3715万行/994只/30因子 (75s)
- **总耗时**: ~4分钟
- **新建脚本**: sync_adj_factor.py / compute_chip_distribution.py / compute_core_factors.py
- **ECM新增**: adj_factor_cache 表 + cache/get 方法
- **最终状态**: 全部可同步数据已补齐，仅剩引擎计算结果表（indicator_cache 未使用；as_* 已废弃）
