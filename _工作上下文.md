# _工作上下文.md — 项目当前状态快照

> 最后更新: 2026-07-12 (会话结束，准备新会话)
> 用途: 新会话启动时读取此文件恢复上下文

---

## 一、当前会话完成的工作

### P0项（2项）
- **Vibe 5策略**：S1/S3/S4/S6/S7 code_template 注入真实分析逻辑（情绪周期/趋势通道/涨停板/波浪理论/斐波那契）
- **FactorCalculator缓存优先**：`calculate_single_factor()` 新增 `ts_code` 参数 + `factor_cache` 降级

### P1项（7项）
- APScheduler date bug修复（`scheduler_manager.py:445`）
- dashboard_service.py ruff修复（12 E501 + 2 I001）
- Makefile venv路径修正（`PYTHON`/`RUFF`/`MYPY` 变量）
- akshare_provider `get_index_member()` AKShare实现
- API版本清理（v1/v2弃用标注 + 6个缺失v3路由）
- LLM Wiki全链路（`_search_llm_wiki()` + `wiki_context` 填充）
- 新闻情绪接入策略层（NewsProvider ±5% confidence修正）

### D3分钟补齐
- mootdx TCP 补齐 2267只 / 544,080行 / 3.2分钟
- 已删除3个临时脚本（mootdx/AKShare/Tushare版）

### 5角色AI融合引擎
- `AiConsensusEngine`：共识聚合 + 辩论检测 + 辩论配对（`ai_consensus_service.py`）
- `POST /api/v3/ai/consensus` 端点
- 方向标签从AI报告提取（修复角色默认值bug）
- 移除 Mock 数据（删除 `_mock_role_result()` 140行）
- 动态建议生成（含角色名+关键点+分歧方）
- 关键词匹配反驳（`_COUNTER_RULES` 16条规则替代固定模板）
- 阈值加入完成度因子（<3完成角色强制"数据不足"）

### 提交
- `commit 1be6602` — 29 files, +3450/-785
- 已推送到 GitHub main

---

## 二、系统当前状态

### 数据体系关键指标
| 表 | 行数 | 状态 |
|:---|:----:|:----:|
| daily_cache | 5,751,424 | ✅ |
| minute_kline_cache | 655,668+ | ✅ D3补齐(新增544K行) |
| margin_cache | 194,106 | ✅ D1修复 |
| indicator_cache | 85,468,877 | ⚠️ 预计算 |
| factor_cache | 37,146,171 | ✅ FactorCalculator已消费 |

### AI引擎状态
| 组件 | 状态 |
|:-----|:------|
| 6角色独立研判 | ✅ 前/后端完整 |
| 方向提取(从AI报告) | ✅ 修复（非角色默认值） |
| 共识聚合 | ✅ AiConsensusEngine |
| 辩论配对+反驳 | ✅ 关键词匹配16条规则 |
| Mock数据 | ❌ **已移除** |
| §13合规 | ✅ 遵守 |

---

## 三、新会话起点—建议

### 待决策
- **136报告9个突破方向** — 等待用户确认优先级

### 可做的工作（按建议优先级）
1. 🟡 186号P2剩余 #34量价窗口自适应 / #35均线面积法
2. 🟡 186号P2剩余 #29stock-analysis独立页面
3. 🟡 sector-sector降级路径死代码清理
4. 🟡 dashboard_service.py ruff F401/F541
5. 🔵 策略输出格式规范文档化

### 阻塞项（无需处理）
- D3剩余冷门股(1429只) — mootdx不支持，有交易时自动覆盖
- 5角色AI融合引擎 — ✅ **已完成**
- ComboCard/ResonanceLamp — ✅ **已吸收**
